---
phase: 03-cli-and-reports
plan: 03
type: tdd
wave: 3
depends_on: [03-02]
files_modified:
  - src/loadtest/report.rs
  - src/loadtest/mod.rs
  - src/commands/loadtest/run.rs
autonomous: true
requirements: [METR-05]

must_haves:
  truths:
    - "After every load test run, a JSON report file is written to `.pmcp/reports/loadtest-YYYY-MM-DDTHH-MM-SS.json`"
    - "The JSON report has a top-level `schema_version` field set to `\"1.0\"`"
    - "The JSON report contains: schema_version, timestamp, target_url, duration_secs, config (full resolved config), metrics (latency percentiles, throughput, error_rate, total_requests), errors (by classification)"
    - "The report embeds the full resolved config (VUs, duration, scenario, timeout) for reproducibility"
    - "The report uses `HashMap<String, u64>` for error counts and operation counts (not non-string enum keys)"
    - "The `.pmcp/reports/` directory is auto-created on first run"
    - "The `--no-report` flag suppresses JSON report output"
    - "The JSON report structure is stable enough for external tools (CI/CD pipelines) to parse reliably"
    - "Timestamps use ISO-8601 format; filenames use hyphens not colons for cross-platform compatibility"
  artifacts:
    - path: "src/loadtest/report.rs"
      provides: "LoadTestReport struct with Serialize, write_report() function"
      contains: "LoadTestReport"
    - path: "src/loadtest/mod.rs"
      provides: "Updated module declarations with `pub mod report;`"
      exports: ["report"]
  key_links:
    - from: "src/loadtest/report.rs"
      to: "src/loadtest/metrics.rs"
      via: "LoadTestReport.metrics built from MetricsSnapshot"
      pattern: "MetricsSnapshot"
    - from: "src/loadtest/report.rs"
      to: "src/loadtest/config.rs"
      via: "LoadTestReport.config embeds LoadTestConfig (Serialize)"
      pattern: "LoadTestConfig"
    - from: "src/commands/loadtest/run.rs"
      to: "src/loadtest/report.rs"
      via: "execute_run calls write_report() after render_summary"
      pattern: "write_report"
---

<objective>
Create the JSON report serialization struct with schema versioning and wire it into the `loadtest run` command to write reports automatically after every test.

Purpose: The JSON report enables CI/CD pipeline integration and historical performance tracking. Without it, load test results are ephemeral terminal output. The schema-versioned JSON file with embedded config makes results reproducible, parseable by external tools, and suitable for the future pmcp.run ingestion pipeline.

Output: `src/loadtest/report.rs` (LoadTestReport struct + write_report function), updated `src/commands/loadtest/run.rs` (report writing integration).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cli-and-reports/03-CONTEXT.md
@.planning/phases/03-cli-and-reports/03-RESEARCH.md
@.planning/phases/03-cli-and-reports/03-01-SUMMARY.md
@.planning/phases/03-cli-and-reports/03-02-SUMMARY.md

<interfaces>
<!-- Key types the report module needs. -->

From src/loadtest/metrics.rs (after Plan 03-02):
```rust
#[derive(Debug, Clone)]
pub struct MetricsSnapshot {
    pub p50: u64,
    pub p95: u64,
    pub p99: u64,
    pub error_p50: u64,
    pub error_p95: u64,
    pub error_p99: u64,
    pub success_count: u64,
    pub error_count: u64,
    pub total_requests: u64,
    pub error_rate: f64,
    pub operation_counts: HashMap<OperationType, u64>,
    pub per_operation_errors: HashMap<OperationType, u64>,
    pub error_category_counts: HashMap<String, u64>,
}
```

From src/loadtest/config.rs (after Plan 03-01 -- now derives Serialize):
```rust
#[derive(Debug, Deserialize, Serialize, Clone)]
pub struct LoadTestConfig {
    pub settings: Settings,
    pub scenario: Vec<ScenarioStep>,
}

#[derive(Debug, Deserialize, Serialize, Clone)]
pub struct Settings {
    pub virtual_users: u32,
    pub duration_secs: u64,
    pub timeout_ms: u64,
    pub expected_interval_ms: u64,
}

#[derive(Debug, Deserialize, Serialize, Clone)]
#[serde(tag = "type")]
pub enum ScenarioStep {
    #[serde(rename = "tools/call")]
    ToolCall { weight: u32, tool: String, arguments: serde_json::Value },
    #[serde(rename = "resources/read")]
    ResourceRead { weight: u32, uri: String },
    #[serde(rename = "prompts/get")]
    PromptGet { weight: u32, prompt: String, arguments: HashMap<String, String> },
}
```

From src/loadtest/engine.rs:
```rust
pub struct LoadTestResult {
    pub snapshot: MetricsSnapshot,
    pub elapsed: Duration,
    pub final_active_vus: u32,
}
```

From src/loadtest/metrics.rs:
```rust
pub enum OperationType {
    Initialize, ToolsCall, ResourcesRead, PromptsGet,
    ToolsList, ResourcesList, PromptsList,
}

impl fmt::Display for OperationType {
    // "initialize", "tools/call", "resources/read", etc.
}
```

PITFALL (from Research): `MetricsSnapshot.operation_counts` uses `HashMap<OperationType, u64>`.
Serde JSON cannot serialize non-string map keys. The report struct MUST convert to `HashMap<String, u64>`
using `OperationType::to_string()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoadTestReport struct with Serialize and write_report function</name>
  <files>
    src/loadtest/report.rs
    src/loadtest/mod.rs
  </files>
  <action>
**Step 1: Create src/loadtest/report.rs**

```rust
//! JSON report serialization for load test results.
//!
//! Produces a schema-versioned JSON report file containing latency percentiles,
//! throughput, error classification, and the full resolved config for
//! reproducibility. Designed for CI/CD pipeline consumption.

use serde::Serialize;
use std::collections::HashMap;
use std::path::{Path, PathBuf};
use std::time::Duration;

use crate::loadtest::config::LoadTestConfig;
use crate::loadtest::engine::LoadTestResult;
use crate::loadtest::metrics::MetricsSnapshot;

/// Schema version for the JSON report format.
///
/// Increment when making breaking changes to the report structure.
/// External tools key on this field to determine parser compatibility.
const SCHEMA_VERSION: &str = "1.0";

/// Top-level JSON report structure.
///
/// Contains all information needed to understand what test was run
/// and what the results were. Designed to be self-contained: anyone
/// reading just the JSON file should understand the test configuration
/// and outcomes.
#[derive(Debug, Serialize)]
pub struct LoadTestReport {
    /// Report format version for parser compatibility.
    pub schema_version: String,
    /// ISO-8601 timestamp when the report was generated.
    pub timestamp: String,
    /// Target MCP server URL that was tested.
    pub target_url: String,
    /// Actual test duration in seconds.
    pub duration_secs: f64,
    /// Full resolved configuration (with CLI overrides applied).
    pub config: ReportConfig,
    /// Aggregate performance metrics.
    pub metrics: ReportMetrics,
    /// Error counts by classification type.
    pub errors: HashMap<String, u64>,
}

/// Resolved test configuration embedded in the report.
///
/// Uses the full LoadTestConfig (which now derives Serialize) to capture
/// VUs, duration, timeout, scenario steps -- everything needed to reproduce
/// the test.
#[derive(Debug, Serialize)]
pub struct ReportConfig {
    /// Number of virtual users configured.
    pub virtual_users: u32,
    /// Test duration in seconds (from config, not actual).
    pub duration_secs: u64,
    /// Per-request timeout in milliseconds.
    pub timeout_ms: u64,
    /// Expected interval for coordinated omission correction.
    pub expected_interval_ms: u64,
    /// Scenario steps as JSON values for stable serialization.
    pub scenario: Vec<serde_json::Value>,
}

/// Aggregate performance metrics in the report.
#[derive(Debug, Serialize)]
pub struct ReportMetrics {
    /// Total number of requests made.
    pub total_requests: u64,
    /// Number of successful requests.
    pub success_count: u64,
    /// Number of failed requests.
    pub error_count: u64,
    /// Error rate as a fraction (0.0..=1.0).
    pub error_rate: f64,
    /// Throughput in requests per second.
    pub throughput_rps: f64,
    /// Latency percentile breakdown.
    pub latency: LatencyMetrics,
    /// Per-operation request counts (keys are operation type strings like "tools/call").
    pub operation_counts: HashMap<String, u64>,
    /// Per-operation error counts.
    pub operation_errors: HashMap<String, u64>,
}

/// Latency percentile metrics in milliseconds.
#[derive(Debug, Serialize)]
pub struct LatencyMetrics {
    /// 50th percentile (median) latency in milliseconds.
    pub p50_ms: u64,
    /// 95th percentile latency in milliseconds.
    pub p95_ms: u64,
    /// 99th percentile latency in milliseconds.
    pub p99_ms: u64,
    /// Error latency 50th percentile in milliseconds.
    pub error_p50_ms: u64,
    /// Error latency 95th percentile in milliseconds.
    pub error_p95_ms: u64,
    /// Error latency 99th percentile in milliseconds.
    pub error_p99_ms: u64,
}

impl LoadTestReport {
    /// Build a report from load test results, config, and target URL.
    ///
    /// Converts `OperationType` enum keys to strings for JSON compatibility
    /// and computes throughput from total requests and elapsed time.
    pub fn from_result(
        result: &LoadTestResult,
        config: &LoadTestConfig,
        url: &str,
    ) -> Self {
        let snap = &result.snapshot;
        let elapsed_secs = result.elapsed.as_secs_f64();
        let throughput_rps = if elapsed_secs > 0.0 {
            snap.total_requests as f64 / elapsed_secs
        } else {
            0.0
        };

        // Convert OperationType keys to String keys for JSON serialization
        let operation_counts: HashMap<String, u64> = snap
            .operation_counts
            .iter()
            .map(|(op, count)| (op.to_string(), *count))
            .collect();

        let operation_errors: HashMap<String, u64> = snap
            .per_operation_errors
            .iter()
            .map(|(op, count)| (op.to_string(), *count))
            .collect();

        // Convert scenario steps to JSON values for stable serialization
        let scenario_values: Vec<serde_json::Value> = config
            .scenario
            .iter()
            .map(|step| serde_json::to_value(step).unwrap_or(serde_json::Value::Null))
            .collect();

        let timestamp = chrono::Utc::now().to_rfc3339();

        Self {
            schema_version: SCHEMA_VERSION.to_string(),
            timestamp,
            target_url: url.to_string(),
            duration_secs: elapsed_secs,
            config: ReportConfig {
                virtual_users: config.settings.virtual_users,
                duration_secs: config.settings.duration_secs,
                timeout_ms: config.settings.timeout_ms,
                expected_interval_ms: config.settings.expected_interval_ms,
                scenario: scenario_values,
            },
            metrics: ReportMetrics {
                total_requests: snap.total_requests,
                success_count: snap.success_count,
                error_count: snap.error_count,
                error_rate: snap.error_rate,
                throughput_rps,
                latency: LatencyMetrics {
                    p50_ms: snap.p50,
                    p95_ms: snap.p95,
                    p99_ms: snap.p99,
                    error_p50_ms: snap.error_p50,
                    error_p95_ms: snap.error_p95,
                    error_p99_ms: snap.error_p99,
                },
                operation_counts,
                operation_errors,
            },
            errors: snap.error_category_counts.clone(),
        }
    }
}

/// Write a JSON report file to the `.pmcp/reports/` directory.
///
/// Creates the reports directory if it does not exist. The filename
/// is timestamped: `loadtest-YYYY-MM-DDTHH-MM-SS.json` (hyphens, not
/// colons, for Windows compatibility).
///
/// Returns the path to the written report file.
pub fn write_report(
    report: &LoadTestReport,
    base_dir: &Path,
) -> Result<PathBuf, std::io::Error> {
    let reports_dir = base_dir.join(".pmcp").join("reports");

    // Auto-create reports directory
    if !reports_dir.exists() {
        std::fs::create_dir_all(&reports_dir)?;
    }

    // Generate filename with timestamp (hyphens, not colons)
    let filename = format!(
        "loadtest-{}.json",
        chrono::Utc::now().format("%Y-%m-%dT%H-%M-%S")
    );
    let report_path = reports_dir.join(&filename);

    // Serialize and write
    let json = serde_json::to_string_pretty(report)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e))?;
    std::fs::write(&report_path, json)?;

    Ok(report_path)
}

/// Generate the report filename for a given timestamp.
///
/// Exposed for testing. Uses hyphens instead of colons for
/// cross-platform filename compatibility.
pub fn report_filename(timestamp: &chrono::DateTime<chrono::Utc>) -> String {
    format!(
        "loadtest-{}.json",
        timestamp.format("%Y-%m-%dT%H-%M-%S")
    )
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::loadtest::config::{LoadTestConfig, ScenarioStep, Settings};
    use crate::loadtest::engine::LoadTestResult;
    use crate::loadtest::metrics::{MetricsSnapshot, OperationType};
    use std::collections::HashMap;
    use std::time::Duration;

    fn test_config() -> LoadTestConfig {
        LoadTestConfig {
            settings: Settings {
                virtual_users: 10,
                duration_secs: 60,
                timeout_ms: 5000,
                expected_interval_ms: 100,
            },
            scenario: vec![ScenarioStep::ToolCall {
                weight: 100,
                tool: "echo".to_string(),
                arguments: serde_json::json!({"text": "hello"}),
            }],
        }
    }

    fn test_snapshot() -> MetricsSnapshot {
        MetricsSnapshot {
            p50: 42,
            p95: 120,
            p99: 350,
            error_p50: 100,
            error_p95: 200,
            error_p99: 500,
            success_count: 950,
            error_count: 50,
            total_requests: 1000,
            error_rate: 0.05,
            operation_counts: HashMap::from([(OperationType::ToolsCall, 1000)]),
            per_operation_errors: HashMap::from([(OperationType::ToolsCall, 50)]),
            error_category_counts: HashMap::from([
                ("timeout".to_string(), 30),
                ("jsonrpc".to_string(), 15),
                ("http".to_string(), 5),
            ]),
        }
    }

    fn test_result() -> LoadTestResult {
        LoadTestResult {
            snapshot: test_snapshot(),
            elapsed: Duration::from_secs(60),
            final_active_vus: 10,
        }
    }

    #[test]
    fn test_report_schema_version() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );
        assert_eq!(report.schema_version, "1.0");
    }

    #[test]
    fn test_report_target_url() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );
        assert_eq!(report.target_url, "http://localhost:3000/mcp");
    }

    #[test]
    fn test_report_embeds_config() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );
        assert_eq!(report.config.virtual_users, 10);
        assert_eq!(report.config.duration_secs, 60);
        assert_eq!(report.config.timeout_ms, 5000);
        assert_eq!(report.config.scenario.len(), 1);
    }

    #[test]
    fn test_report_metrics() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );
        assert_eq!(report.metrics.total_requests, 1000);
        assert_eq!(report.metrics.success_count, 950);
        assert_eq!(report.metrics.error_count, 50);
        assert!((report.metrics.error_rate - 0.05).abs() < 0.001);
        assert!((report.metrics.throughput_rps - 16.666).abs() < 0.1);
        assert_eq!(report.metrics.latency.p50_ms, 42);
        assert_eq!(report.metrics.latency.p95_ms, 120);
        assert_eq!(report.metrics.latency.p99_ms, 350);
    }

    #[test]
    fn test_report_operation_counts_use_string_keys() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );
        // OperationType::ToolsCall.to_string() = "tools/call"
        assert_eq!(report.metrics.operation_counts.get("tools/call"), Some(&1000));
    }

    #[test]
    fn test_report_error_categories() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );
        assert_eq!(report.errors.get("timeout"), Some(&30));
        assert_eq!(report.errors.get("jsonrpc"), Some(&15));
        assert_eq!(report.errors.get("http"), Some(&5));
    }

    #[test]
    fn test_report_serializes_to_valid_json() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );
        let json = serde_json::to_string_pretty(&report).expect("should serialize");

        // Verify it parses back to a Value
        let parsed: serde_json::Value =
            serde_json::from_str(&json).expect("should parse back");

        assert_eq!(parsed["schema_version"], "1.0");
        assert_eq!(parsed["target_url"], "http://localhost:3000/mcp");
        assert!(parsed["timestamp"].is_string());
        assert!(parsed["metrics"]["latency"]["p50_ms"].is_u64());
    }

    #[test]
    fn test_report_filename_format() {
        let ts = chrono::Utc::now();
        let filename = report_filename(&ts);
        assert!(filename.starts_with("loadtest-"));
        assert!(filename.ends_with(".json"));
        // Verify no colons (Windows incompatible)
        assert!(!filename.contains(':'), "Filename must not contain colons: {}", filename);
    }

    #[test]
    fn test_write_report_creates_file() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );

        let tmp_dir = tempfile::tempdir().expect("should create temp dir");
        let result = write_report(&report, tmp_dir.path());
        assert!(result.is_ok(), "write_report failed: {:?}", result.err());

        let path = result.unwrap();
        assert!(path.exists(), "Report file should exist at {:?}", path);

        // Verify the content is valid JSON
        let content = std::fs::read_to_string(&path).expect("should read file");
        let parsed: serde_json::Value =
            serde_json::from_str(&content).expect("should be valid JSON");
        assert_eq!(parsed["schema_version"], "1.0");
    }

    #[test]
    fn test_write_report_creates_reports_directory() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );

        let tmp_dir = tempfile::tempdir().expect("should create temp dir");
        // The .pmcp/reports/ directory does not exist yet
        let reports_dir = tmp_dir.path().join(".pmcp").join("reports");
        assert!(!reports_dir.exists());

        let result = write_report(&report, tmp_dir.path());
        assert!(result.is_ok());
        assert!(reports_dir.exists(), "Reports directory should be auto-created");
    }

    #[test]
    fn test_report_duration_from_elapsed() {
        let report = LoadTestReport::from_result(
            &test_result(),
            &test_config(),
            "http://localhost:3000/mcp",
        );
        assert!((report.duration_secs - 60.0).abs() < 0.001);
    }
}
```

**Step 2: Update src/loadtest/mod.rs**

Add `pub mod report;` to the module declarations.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk/cargo-pmcp && cargo check 2>&1 | tail -5 && cargo test --lib loadtest::report -- --test-threads=1 2>&1 | tail -15 && cargo clippy -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `src/loadtest/report.rs` exists with `LoadTestReport`, `ReportConfig`, `ReportMetrics`, `LatencyMetrics` structs
    - All structs derive `Serialize`
    - `LoadTestReport::from_result()` builds report from `LoadTestResult` + `LoadTestConfig` + URL
    - `write_report()` writes JSON to `.pmcp/reports/loadtest-YYYY-MM-DDTHH-MM-SS.json`
    - Reports directory auto-created if missing
    - OperationType keys converted to String via `to_string()` for JSON compatibility
    - `schema_version` is "1.0"
    - Timestamps use ISO-8601; filenames use hyphens not colons
    - `src/loadtest/mod.rs` declares `pub mod report;`
    - All unit tests pass including file I/O tests with tempdir
    - `cargo check` and `cargo clippy -- -D warnings` succeed
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire JSON report writing into the loadtest run command</name>
  <files>
    src/commands/loadtest/run.rs
  </files>
  <action>
**Wire the report writer into execute_run in src/commands/loadtest/run.rs.**

After the terminal summary is printed (from Plan 03-02), add the report writing logic:

1. Import the report module:
```rust
use crate::loadtest::report::{LoadTestReport, write_report};
```

2. After `render_summary` is called and printed, add:
```rust
// Write JSON report (unless --no-report)
if !no_report {
    let report = LoadTestReport::from_result(&result, engine.config(), &url);
    let cwd = std::env::current_dir()?;
    match write_report(&report, &cwd) {
        Ok(path) => {
            eprintln!();
            eprintln!("Report written to: {}", path.display());
        }
        Err(e) => {
            eprintln!();
            eprintln!("Warning: Failed to write report: {}", e);
            // Non-fatal -- the test still completed successfully
        }
    }
}
```

**IMPORTANT:** Report writing failure is non-fatal. The test completed and results were shown in the terminal. A filesystem error (permissions, disk full) should not cause the command to fail.

3. Ensure the `result` variable is NOT consumed before writing the report. The `render_summary` call borrows `result` immutably, so this should work. Check that no `let _ = (result, no_report);` placeholder from Plan 03-01 interferes -- remove it if present.

4. Add a test that verifies execute_run respects the no_report flag. This is hard to test in isolation (requires a running server), so instead add a comment noting that integration testing will cover this in Phase 4 or a dedicated integration test plan.

**Step 2: Update the let binding for engine**

The `execute_run` function needs to keep a reference to the engine's config after `engine.run()`. Since `run()` borrows `&self`, the engine is still available. Ensure the code:
```rust
let engine = LoadTestEngine::new(config, url.clone());
// ... with_iterations, with_no_color ...
let result = engine.run().await?;
// engine.config() is still valid here
let summary = render_summary(&result, engine.config(), &url);
let report = LoadTestReport::from_result(&result, engine.config(), &url);
```

The engine is not consumed by `run()` since it takes `&self`, so `engine.config()` works fine.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk/cargo-pmcp && cargo check 2>&1 | tail -5 && cargo test --lib commands::loadtest -- --test-threads=1 2>&1 | tail -15 && cargo clippy -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `execute_run` calls `LoadTestReport::from_result()` after rendering summary
    - `write_report()` called unless `--no-report` flag is set
    - Report path printed to stderr after writing
    - Report write failure is non-fatal (warning printed, command still succeeds)
    - `result` and `engine` remain available after summary rendering for report building
    - `cargo check` and `cargo clippy -- -D warnings` succeed
  </done>
</task>

</tasks>

<verification>
1. `cargo check` succeeds
2. `cargo test --lib loadtest::report -- --test-threads=1` passes all report tests
3. `cargo test --lib commands::loadtest -- --test-threads=1` passes all CLI tests
4. `cargo clippy -- -D warnings` shows zero warnings
5. `LoadTestReport` serializes to valid JSON with `schema_version: "1.0"`
6. Report filename uses hyphens not colons (grep for `%H-%M-%S` confirms)
7. OperationType enum keys converted to String in report (grep `to_string()` in report.rs)
8. `write_report` creates `.pmcp/reports/` directory automatically
9. `--no-report` flag prevents report writing (code path in run.rs)
10. Report JSON passes round-trip: serialize then deserialize back to Value
</verification>

<success_criteria>
- JSON report is written automatically after every load test run
- Report has schema_version "1.0" at the top level
- Report contains all required fields: config, metrics, errors, timestamp, target_url
- Report embeds full resolved config for reproducibility
- Report uses String keys for all HashMap fields (no enum keys)
- Reports directory is auto-created
- --no-report flag suppresses report output
- Report filename is cross-platform compatible (no colons)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-and-reports/03-03-SUMMARY.md`
</output>
