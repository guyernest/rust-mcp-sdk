// Originated from pmcp-run/built-in/shared/mcp-server-common/src/auth.rs
// (https://github.com/guyernest/pmcp-run). Lifted into rust-mcp-sdk for Phase 83.

//! `AuthProvider` impls (`StaticAuthProvider`, `BearerAuthProvider`). Lifted from mcp-server-common.
