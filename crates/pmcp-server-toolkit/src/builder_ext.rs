// Net-new code for Phase 83 PATTERNS §13 (builder extension surface).
// Hosts the `ServerBuilderExt` trait + `try_*` fallible variants per review R7.

//! `ServerBuilderExt` trait — `tools_from_config` + `try_tools_from_config` (review R7) + `code_mode_from_config` + `try_code_mode_from_config` builder extensions.
