// Net-new code for Phase 83 TKIT-09 (code-mode wiring surface).
// Adapter layer that re-exports `pmcp-code-mode` essentials and supplies
// toolkit-side wiring helpers (`executor_from_config`, `assemble_code_mode_prompt`).

//! Code-mode wiring: re-exports from `pmcp-code-mode` + `executor_from_config` + `assemble_code_mode_prompt`.
