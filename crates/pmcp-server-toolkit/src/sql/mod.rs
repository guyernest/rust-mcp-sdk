// Net-new code for Phase 83 (SQL toolkit surface).
// 2-method MVP per review R2 — narrow trait + dialect enum so backend impls (Athena,
// SQLite, …) land in later plans without breaking the public API.

//! `SqlConnector` trait stub (2-method MVP per review R2) + `Dialect` enum.
