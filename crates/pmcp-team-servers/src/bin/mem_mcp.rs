//! mem-mcp server binary. Placeholder stub — the real `#[tokio::main]` binary
//! is implemented in 109-03.
fn main() {
    eprintln!("pmcp-team-servers: mem-mcp binary — implemented in a later 109 plan");
}
