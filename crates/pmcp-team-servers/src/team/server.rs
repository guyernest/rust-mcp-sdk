//! Builds the team-mcp `pmcp::Server` that forwards member `tools/call`s
//! through a [`crate::compose::resolver::PackageResolver`] under the
//! [`super::guards`] checks.
//!
//! Empty documented seam — implemented in 109-05.
