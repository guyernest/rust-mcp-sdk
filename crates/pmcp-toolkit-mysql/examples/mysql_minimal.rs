//! Shape C example — Plan 06 fills.
//!
//! Wave 0 ships a buildable shell so `cargo build --example mysql_minimal`
//! succeeds. Plan 06 replaces the body with the ≤15-line Shape C usage.

fn main() {}
