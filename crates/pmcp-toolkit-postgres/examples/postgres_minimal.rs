//! Shape C example — Plan 05 fills.
//!
//! Wave 0 ships a buildable shell so `cargo build --example postgres_minimal`
//! succeeds. Plan 05 replaces the body with the ≤15-line Shape C usage.

fn main() {}
