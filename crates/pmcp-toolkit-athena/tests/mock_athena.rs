//! Mock fixture for Plan 07 — authentic in-process Athena mock.
//!
//! Wave 0 leaves this module empty. Plan 07 fills it with the spike-005
//! `athena_mock` shape: `?` placeholders, Glue-Data-Catalog-styled
//! `schema_text()`, and a cheap fixture query engine. The mock does NOT model
//! the StartQueryExecution polling loop — the trait surface hides it.
