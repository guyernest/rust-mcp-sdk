# Standalone fixture for `cargo expand` snapshot of `#[mcp_resource]` (Phase 75 Wave 0 Task 1).
[package]
name = "example_mcp_resource_fixture"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
pmcp = { path = "../../../..", features = ["macros", "schema-generation"] }
pmcp-macros = { path = "../../.." }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1"
tokio = { version = "1.46", features = ["full"] }
