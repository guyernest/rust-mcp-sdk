---
phase: 260516-b2p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/streamable_http.rs
  - tests/streamable_http_properties.rs
  - Cargo.toml
  - .github/workflows/ci.yml
  - crates/mcp-tester/Cargo.toml
  - cargo-pmcp/Cargo.toml
  - crates/pmcp-server/Cargo.toml
  - crates/pmcp-server/pmcp-server-lambda/Cargo.toml
  - crates/pmcp-tasks/Cargo.toml
  - CHANGELOG.md
autonomous: true
requirements:
  - pmcp.run-phase-90-agent-oauth-foundation-chord
user_setup: []

must_haves:
  truths:
    - "AuthProvider::on_unauthorized() has a default no-op impl — existing AuthProvider impls (ProxyProvider, NoOpAuthProvider, test mocks) compile unchanged with no override"
    - "Retry is single-shot — a second 401 on the retried request is returned to the caller as-is (no infinite loop, no second on_unauthorized call)"
    - "Non-401 responses (200, 4xx other than 401, 5xx) NEVER trigger on_unauthorized()"
    - "When auth_provider is None, no retry occurs and on_unauthorized() is never invoked"
    - "Retry preserves byte-identical body, method, session ID, resumption token, extra_headers, and middleware chain (only Authorization changes — recomputed from a freshly-vended token)"
    - "MSRV is 1.91 — let-chain syntax (&& let Some(...) = ...) is permitted"
    - "Workspace publishes as pmcp 2.8.0 with downstream pin ripple (mcp-tester 0.7.0, cargo-pmcp 0.14.0, pmcp-server, pmcp-server-lambda, pmcp-tasks all bumped pmcp pin to 2.8.0)"
  artifacts:
    - path: "src/shared/streamable_http.rs"
      provides: "AuthProvider::on_unauthorized() trait method with default no-op + transport retry-once block at send call site"
      contains: "async fn on_unauthorized(&self) -> Result<()>"
    - path: "tests/streamable_http_properties.rs"
      provides: "proptest covering the on_unauthorized invariant — retry-count is at most 1 regardless of response sequence"
      contains: "proptest!"
    - path: "Cargo.toml"
      provides: "pmcp 2.8.0 + rust-version 1.91.0"
      contains: "version = \"2.8.0\""
    - path: ".github/workflows/ci.yml"
      provides: "MSRV CI gate updated to 1.91"
      contains: "dtolnay/rust-toolchain@1.91"
    - path: "CHANGELOG.md"
      provides: "[2.8.0] entry with on_unauthorized hook, MSRV bump, and behavior note"
      contains: "## [2.8.0]"
    - path: "crates/mcp-tester/Cargo.toml"
      provides: "mcp-tester own version bump + pmcp 2.8.0 pin"
      contains: "pmcp = { version = \"2.8.0\""
    - path: "cargo-pmcp/Cargo.toml"
      provides: "cargo-pmcp own version bump + pmcp 2.8.0 pin + mcp-tester pin bump"
      contains: "pmcp = { version = \"2.8.0\""
  key_links:
    - from: "src/shared/streamable_http.rs (trait def ~line 1048-1073)"
      to: "src/shared/streamable_http.rs (retry block ~line 741-790, send_with_options)"
      via: "self.config.read().auth_provider then provider.on_unauthorized().await? then build_request_with_middleware re-call"
      pattern: "on_unauthorized\\(\\)\\.await"
    - from: "Cargo.toml"
      to: ".github/workflows/ci.yml:263"
      via: "MSRV must match between manifest rust-version and CI toolchain pin"
      pattern: "rust-toolchain@1\\.91"
    - from: "Cargo.toml (pmcp 2.8.0)"
      to: "crates/mcp-tester/Cargo.toml, cargo-pmcp/Cargo.toml, crates/pmcp-server/Cargo.toml, crates/pmcp-server/pmcp-server-lambda/Cargo.toml, crates/pmcp-tasks/Cargo.toml"
      via: "downstream pin ripple — every crate that pins pmcp version must point to 2.8.0"
      pattern: "pmcp = \\{ version = \"2\\.8\\.0\""
    - from: "src/server/auth/proxy.rs (ProxyProvider, NoOpAuthProvider)"
      to: "src/shared/streamable_http.rs (AuthProvider trait)"
      via: "existing impls must compile unchanged — default impl on on_unauthorized() preserves backcompat"
      pattern: "impl AuthProvider"
---

<objective>
Land the AuthProvider::on_unauthorized() hook + transport retry-once-on-401 behavior, bump MSRV to 1.91, and release pmcp v2.8.0 with full downstream pin ripple.

Purpose: pmcp.run Phase 90 (Agent OAuth Foundation Chord) needs a transport-level hook so OutboundOAuthAuthProvider can evict its cached short-lived OAuth token on 401 and have the SDK auto-retry once — without forcing every MCP caller to wrap each call in a retry loop.

Output: A backward-compatible minor-version release (2.7.0 → 2.8.0) shipping (a) the `on_unauthorized()` default-no-op trait method, (b) transport retry-once at the send call site, (c) 5 unit tests + 1 proptest, (d) MSRV bump from 1.83 → 1.91, (e) CHANGELOG entry, (f) downstream pin ripple across all workspace crates that depend on `pmcp`.

Current state (read before editing): the core code work — trait extension at `src/shared/streamable_http.rs:1048-1073`, retry block at `:741-790`, and Tests 1–5 in the inline `mod tests` (`:1168-1458`) — is **already in tree** and was implemented in a prior session. Root `Cargo.toml` already reads `version = "2.8.0"`. This plan finishes the remaining mechanical work: proptest, MSRV, downstream pins, CHANGELOG, and the gate.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# The change request context (locked decisions D-01 .. D-12 from the spawning conversation)
# is embedded directly in tasks below — there is no separate CONTEXT.md for quick plans.

# Source surfaces (already implemented — DO NOT re-author; verify shape only before adding proptest)
@src/shared/streamable_http.rs

<interfaces>
<!-- Already in-tree at src/shared/streamable_http.rs. Executor reads these directly, no exploration needed. -->

Trait (lines 1048-1073):
```rust
#[async_trait]
pub trait AuthProvider: Send + Sync + Debug {
    async fn get_access_token(&self) -> Result<String>;

    /// Called by the SDK transport immediately after receiving an HTTP 401 response,
    /// before the retry's get_access_token() call.
    ///
    /// Default impl is a no-op for backward compatibility.
    /// SDK invokes on_unauthorized() at most once per request.
    async fn on_unauthorized(&self) -> Result<()> {
        Ok(())
    }
}
```

Retry block (lines 741-790, inside send_with_options after first POST response):
```rust
let retried_unauthorized = response.status() == StatusCode::UNAUTHORIZED;
let response = if retried_unauthorized {
    let auth_provider = self.config.read().auth_provider.clone();
    if let Some(provider) = auth_provider {
        provider.on_unauthorized().await?;
        // ... rebuild request via build_request_with_middleware with body_bytes_snapshot ...
        // ... re-send via self.client.request(retry_request) ...
    } else { response }
} else { response };
```

CountingProvider test helper (lines 1095-1139) — already exists with `get_count`, `unauthorized_count`, `call_order` tracking for use in proptest if desired.

Existing inline tests (DO NOT modify):
- Test 1 (line 1173): test_on_unauthorized_default_noop_compiles_and_succeeds
- Test 2 (line 1198): test_max_one_retry_on_401
- Test 3 (line 1237): test_on_unauthorized_not_called_for_non_401
- Test 4 (line 1288): test_retry_body_and_headers_are_byte_identical
- Test 5 (line 1416): test_on_unauthorized_called_before_get_access_token_on_retry
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add proptest for on_unauthorized retry-count invariant</name>
  <files>tests/streamable_http_properties.rs</files>
  <behavior>
    Invariant under test: For any sequence of HTTP status codes returned by a mock server,
    the SDK calls `on_unauthorized()` at most once per `send_with_options()` invocation, AND
    issues at most 2 HTTP requests (1 original + at most 1 retry), regardless of the input
    sequence.

    Test cases (proptest-generated, ≥256 cases):
    - Input: a Vec&lt;u16&gt; of HTTP status codes (constrained to {200, 202, 400, 401, 403, 500, 503})
      with length 1..=4.
    - The mock server returns codes[0] on request 1, codes[1] on request 2, ... (cycles or stays
      on last if SDK makes fewer attempts than codes.len()).
    - After one `send_with_options(ping)` call, assert:
        1. provider.unauthorized_count ≤ 1 (proves single-retry invariant)
        2. provider.get_count ≤ 2 (proves at most 2 HTTP attempts)
        3. If codes[0] != 401, then unauthorized_count == 0 (proves D-06: non-401 never triggers)
        4. If codes[0] == 401 AND auth_provider is Some, then unauthorized_count == 1
  </behavior>
  <action>
    Append a new `proptest! { ... }` block to `tests/streamable_http_properties.rs`. The file already imports `proptest::prelude::*` (line 9) and uses `ProptestConfig::with_cases(...)` patterns (lines 148, 351, 385). Match the existing style.

    Implementation outline:
    1. At the top of the new block, import what's needed from the crate root that isn't already imported. Likely additions: `use pmcp::shared::streamable_http::{AuthProvider, StreamableHttpTransport, StreamableHttpTransportConfigBuilder, SendOptions};` plus `use async_trait::async_trait;`, `use std::sync::atomic::{AtomicUsize, Ordering};`, `use std::sync::Arc;`. Check what's already in scope at the top of the file before re-importing.
    2. Define a local `CountingProvider` test helper inline within the test module (cannot reuse the one in `src/shared/streamable_http.rs` because that's in an inline `#[cfg(test)] mod tests` and is not exported). Keep it minimal: `token: String`, `get_count: AtomicUsize`, `unauthorized_count: AtomicUsize`, with `AuthProvider` impl that increments counters and returns `Ok(self.token.clone())` / `Ok(())` respectively.
    3. Use `mockito::Server` (already a test-dev-dep — confirm via `grep mockito Cargo.toml`; if not present, the inline tests at `src/shared/streamable_http.rs:1083` import it so it IS a dev-dep). Use `tokio::runtime::Runtime` to drive the async test from inside the sync `proptest!` body, matching the pattern of other async-driving proptests in this file if any, OR use `tokio_test::block_on`. Check the file for the existing pattern — if no async proptest exists in this file, use `tokio::runtime::Builder::new_current_thread().enable_all().build().unwrap().block_on(async { ... })`.
    4. Configure mockito to return `codes[0]` first; for subsequent requests it returns `codes[1 % codes.len()]` (mockito's `.expect(n)` is per-mock; use a single mock that matches all POSTs and cycles via shared state, OR set up `codes.len()` separate mocks each with `.expect(1)` in order — the latter is simpler).
    5. Constrain the input strategy: `prop::collection::vec(prop_oneof![Just(200u16), Just(202), Just(400), Just(401), Just(403), Just(500), Just(503)], 1..=4)`.
    6. Use `ProptestConfig::with_cases(64)` (NOT 1000 — these tests spawn a mock server and are slower; per CLAUDE.md "Performance Tests: Benchmarks for performance-critical features" but proptests should still finish in &lt;60s total).
    7. Mark the test `#[cfg(not(target_arch = "wasm32"))]` to match existing patterns and avoid wasm CI breakage.

    Anti-patterns to avoid:
    - DO NOT modify existing proptests (lines 148, 351, 385). Append a new block at end of file.
    - DO NOT re-export `CountingProvider` from `src/shared/streamable_http.rs` — define a local one in the proptest.
    - DO NOT use `expect(0..=N)` ranges in mockito mocks (proptest exploration will hit unexpected counts and fail flakily). Use explicit `.expect(n)` per status code or `expect_at_least(0)` then verify counts via the provider counters, not mock assertions.

    Per CLAUDE.md "ALL code changes via pmat quality-gate proxy" — write through the proxy if available, but plain Write is acceptable for additive test code; quality-gate at the end of the task catches issues.
  </action>
  <verify>
    <automated>cargo test --test streamable_http_properties --features streamable-http</automated>
  </verify>
  <done>
    New proptest block appended to `tests/streamable_http_properties.rs`. Running `cargo test --test streamable_http_properties --features streamable-http` exits 0. The new test exercises ≥64 cases and asserts the four invariants listed in `<behavior>`. No flakiness on three consecutive runs.
  </done>
</task>

<task type="auto">
  <name>Task 2: MSRV bump 1.83 → 1.91 (Cargo.toml + ci.yml)</name>
  <files>Cargo.toml, .github/workflows/ci.yml</files>
  <action>
    Per locked decision D-07: bump MSRV to enable let-chain syntax `&& let Some(...) = ...` at 1.91.

    Edit 1 — `Cargo.toml` line 14:
    - Change `rust-version = "1.83.0"` → `rust-version = "1.91.0"`.

    Edit 2 — `.github/workflows/ci.yml` lines 257 and 263:
    - Line 257: `name: Minimum Supported Rust Version (1.82)` → `name: Minimum Supported Rust Version (1.91)`
    - Line 262: `- name: Install Rust 1.82` → `- name: Install Rust 1.91`
    - Line 263: `uses: dtolnay/rust-toolchain@1.82` → `uses: dtolnay/rust-toolchain@1.91`

    Verify rust-toolchain version matches in any other CI workflow:
    - Grep `grep -rn "rust-toolchain@1\\.8\\|rust-toolchain@1\\.9\\|Rust 1\\.8\\|Rust 1\\.9\\|rust-version" .github/ Cargo.toml` to surface any other MSRV references that need updating (e.g., release.yml, quality-badges.yml). Update each to 1.91.

    DO NOT update `rust-version` in workspace member crates unless they currently declare one — most use the root's. Run `grep -l rust-version crates/ cargo-pmcp/` to check.

    Per CLAUDE.md Release Steps step 1: `rustup update stable` then `rustc --version` should be ≥ 1.91 locally before proceeding. If local toolchain is older than 1.91, run `rustup install 1.91` and `rustup default 1.91` (or accept that quality-gate may fail with let-chain syntax until upgraded).

    Anti-pattern to avoid: do NOT bump MSRV only in `Cargo.toml` without updating CI — CI/local mismatch is "the #1 cause of CI failures" per CLAUDE.md Pre-Flight Checklist item 1.
  </action>
  <verify>
    <automated>grep -q 'rust-version = "1.91.0"' Cargo.toml &amp;&amp; grep -q 'rust-toolchain@1.91' .github/workflows/ci.yml &amp;&amp; ! grep -rn 'rust-toolchain@1\.8\|Rust 1\.82' .github/</automated>
  </verify>
  <done>
    `Cargo.toml` declares `rust-version = "1.91.0"`. All CI workflow files reference `rust-toolchain@1.91` (not `@1.82` or `@1.83`). MSRV-job display name reads "1.91". `cargo build --workspace --all-features` succeeds on 1.91 locally.
  </done>
</task>

<task type="auto">
  <name>Task 3: Downstream pmcp pin ripple (2.7.0 → 2.8.0 across workspace)</name>
  <files>crates/mcp-tester/Cargo.toml, cargo-pmcp/Cargo.toml, crates/pmcp-server/Cargo.toml, crates/pmcp-server/pmcp-server-lambda/Cargo.toml, crates/pmcp-tasks/Cargo.toml</files>
  <action>
    Per locked decision D-08: pmcp 2.7.0 → 2.8.0 with downstream pin ripple. Six pin sites + own-version bumps for crates that re-publish.

    Edit 1 — `crates/mcp-tester/Cargo.toml`:
    - Line 3: `version = "0.6.0"` → `version = "0.7.0"` (own bump because pmcp dep changed)
    - Line 21: `pmcp = { version = "2.7.0", path = "../../", features = ["streamable-http", "oauth"] }` → change `version = "2.7.0"` → `version = "2.8.0"`.

    Edit 2 — `cargo-pmcp/Cargo.toml`:
    - Line 3: `version = "0.13.0"` → `version = "0.14.0"` (own bump because pmcp + mcp-tester deps changed)
    - Line 52: `pmcp = { version = "2.7.0", ... }` → `version = "2.8.0"`
    - Line 53: `mcp-tester = { version = "0.6.0", ... }` → `version = "0.7.0"`

    Edit 3 — `crates/pmcp-server/Cargo.toml`:
    - Line 30: `pmcp = { version = "2.7.0", ... }` → `version = "2.8.0"`
    - Line 31: `mcp-tester = { version = "0.6.0", ... }` → `version = "0.7.0"`
    - Own version: bump only if pmcp-server is published to crates.io. Check with `grep '^publish' crates/pmcp-server/Cargo.toml` — if `publish = false` or `publish = ["..."]` excludes crates.io, skip own-bump. Otherwise patch-bump (e.g., 0.X.Y → 0.X.(Y+1)).

    Edit 4 — `crates/pmcp-server/pmcp-server-lambda/Cargo.toml`:
    - Line 17: `pmcp = { version = "2.7.0", ... }` → `version = "2.8.0"`
    - Own version: same publish-check rule.

    Edit 5 — `crates/pmcp-tasks/Cargo.toml`:
    - Line 10 (deps): `pmcp = { version = "2.7.0", ... }` → `version = "2.8.0"`
    - Line 36 (dev-deps with `features = ["full"]`): same bump.
    - Own version: same publish-check rule.

    DO NOT bump:
    - `crates/pmcp-code-mode/Cargo.toml` line 17: uses `>=2.2.0` range — 2.8.0 already satisfies, no edit needed.
    - `crates/pmcp-code-mode-derive/Cargo.toml` line 26: same `>=2.2.0` range — no edit.
    - `crates/pmcp-widget-utils/Cargo.toml`: per CLAUDE.md publish-order list it's leaf with "no internal deps" — no pmcp pin to bump.
    - `crates/mcp-preview/Cargo.toml`: already 0.3.1; check for pmcp dep — grep showed no `pmcp =` line in earlier audit, so likely no pin. Confirm with `grep '^pmcp' crates/mcp-preview/Cargo.toml`; if absent, skip.
    - `crates/pmcp-server/src/tools/scaffold.rs:33`: that's a scaffold string literal `pmcp = { version = "1", ... }` shown to end-users — leave at "1" (it's a major-version constraint, not a workspace pin).

    Per CLAUDE.md Version Bump Rules: "Only bump crates that have changed since their last publish. Downstream crates that pin a bumped dependency must also be bumped." Every direct `pmcp` dependent gets its own bump.

    After edits, run `cargo update -p pmcp` to refresh `Cargo.lock` against the new version, then `cargo build --workspace --all-features` to verify the workspace still resolves.
  </action>
  <verify>
    <automated>cargo build --workspace --all-features &amp;&amp; ! grep -rn 'pmcp = { version = "2\.7\.0"' crates/ cargo-pmcp/Cargo.toml</automated>
  </verify>
  <done>
    All workspace crates that pin `pmcp` directly now pin `"2.8.0"`. `crates/mcp-tester` is at `0.7.0`. `cargo-pmcp` is at `0.14.0` with `mcp-tester = "0.7.0"`. Downstream crates that re-publish (pmcp-server, pmcp-server-lambda, pmcp-tasks if publishable) got their own patch bumps. `cargo build --workspace --all-features` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 4: CHANGELOG.md [2.8.0] entry</name>
  <files>CHANGELOG.md</files>
  <action>
    Per locked decision D-10: insert a new `## [2.8.0]` section immediately after the header (line 6) and before the existing `## [2.7.0]` entry (line 8).

    Use today's date. The release date should be the date of this commit; use ISO format `YYYY-MM-DD`.

    Required content (three bullets per D-10):

    ```markdown
    ## [2.8.0] - YYYY-MM-DD

    ### Added

    - **`AuthProvider::on_unauthorized()` transport hook.** New default-method on the
      `pmcp::shared::streamable_http::AuthProvider` trait, invoked by the
      streamable-HTTP transport immediately after a 401 response, before the
      single auto-retry's `get_access_token()` call. Default impl is a no-op,
      preserving backward compatibility for all existing implementers
      (`ProxyProvider`, `NoOpAuthProvider`, downstream user impls). Implementers
      of cached-token providers (e.g. pmcp.run's `OutboundOAuthAuthProvider`)
      should override this method to evict stale cached tokens so that the
      SDK's subsequent retry fetches a fresh credential. See
      `src/shared/streamable_http.rs` doc comment for the retry guarantee.

    ### Changed

    - **Streamable-HTTP transport: single retry on HTTP 401.** When an `AuthProvider`
      is configured and the upstream server returns 401, the SDK now calls
      `on_unauthorized()` and re-sends the request exactly once with a freshly-vended
      token. The retry preserves the original method, byte-identical body,
      session ID, resumption token, extra_headers, and middleware chain — only
      the `Authorization` header is recomputed. A second 401 on the retry is
      returned to the caller as-is (no infinite loop). For requests with no
      `auth_provider` configured, behavior is unchanged. Behavioral note:
      callers running against a server that returns 401 will now see exactly one
      extra round-trip per failed request; this is the intended fix for stale-token
      flows and is negligible in normal operation. Five unit tests + one proptest
      cover the invariant.

    - **MSRV bumped to Rust 1.91.** Required by transport-internal use of the
      let-chain syntax stabilized in 1.91. CI's MSRV job now pins
      `dtolnay/rust-toolchain@1.91`.

    ### Internal

    - Workspace pin ripple: `mcp-tester` 0.7.0, `cargo-pmcp` 0.14.0; `pmcp` 2.8.0
      pin updated across `crates/mcp-tester`, `cargo-pmcp`, `crates/pmcp-server`,
      `crates/pmcp-server/pmcp-server-lambda`, and `crates/pmcp-tasks`.
    ```

    Replace `YYYY-MM-DD` with the actual commit date (use `date -u +%Y-%m-%d`).

    Anti-patterns to avoid:
    - DO NOT touch the existing `[2.7.0]` entry.
    - DO NOT remove or reformat the Keep-a-Changelog header (lines 1-6).
    - DO NOT add a `Security` subsection unless there's an actual CVE — there isn't here.
  </action>
  <verify>
    <automated>grep -q '## \[2.8.0\]' CHANGELOG.md &amp;&amp; grep -q 'on_unauthorized' CHANGELOG.md &amp;&amp; grep -q 'MSRV bumped to Rust 1.91' CHANGELOG.md</automated>
  </verify>
  <done>
    CHANGELOG.md has a new `## [2.8.0]` section above the existing `## [2.7.0]` entry, with the three required topics (on_unauthorized hook, MSRV 1.91, one-extra-roundtrip behavior note), formatted per Keep-a-Changelog conventions.
  </done>
</task>

<task type="auto">
  <name>Task 5: Quality gate + final verification</name>
  <files>(none — verification only)</files>
  <action>
    Per CLAUDE.md "ZERO TOLERANCE FOR DEFECTS" and "Before pushing a new commit or a PR you need to run `make quality-gate`": run the full quality gate, matching CI exactly.

    Step 1 — Confirm local Rust toolchain matches MSRV bump:
    ```
    rustc --version  # MUST be ≥ 1.91.0
    ```
    If older: `rustup update stable && rustup install 1.91`.

    Step 2 — Run the full gate. Per CLAUDE.md: "Do NOT run individual cargo commands; `make quality-gate` matches CI exactly":
    ```
    make quality-gate
    ```

    Step 3 — Run the four "ALWAYS" testing requirement targets per CLAUDE.md:
    ```
    make test-unit
    make test-property
    make test-doc
    make test-integration
    ```
    `make test-fuzz` and `make test-examples` are not gates for this change (no new fuzz target, no new example), but if `test-examples` is part of quality-gate it runs automatically.

    Step 4 — Spot-check that the inline `mod tests` (5 unit tests already in src/shared/streamable_http.rs) all pass:
    ```
    cargo test --features streamable-http --lib shared::streamable_http::tests
    ```
    Expect Tests 1-5 to all pass.

    Step 5 — Confirm backward-compat: existing AuthProvider impls compile without override.
    ```
    cargo build -p pmcp --features "streamable-http,oauth"  # exercises ProxyProvider, NoOpAuthProvider
    cargo test --test auth_context_integration_test
    cargo test --test streamable_http_integration
    cargo test --test streamable_http_oauth_integration
    ```
    All four should pass with zero changes to those test files (they use the default no-op).

    Step 6 — If quality-gate fails on PMAT cognitive complexity (the retry block at `:741-790` may have raised the cog count on `send_with_options`):
    - Per CLAUDE.md "P1–P6" patterns: consider extracting the retry block into a helper `async fn retry_once_on_401(&self, ...) -> Result<Response>`. Apply this refactor ONLY if PMAT reports a violation; otherwise leave the inline code.
    - DO NOT use `#[allow(clippy::cognitive_complexity)]` as a first resort — first try extraction.
    - If after extraction PMAT still flags a function as irreducibly complex, apply the `// Why:` annotated allow per the Phase 75 template, hard cap cog 50.

    Step 7 — If quality-gate fails on clippy with let-chain or MSRV-related lints: confirm Step 1 toolchain. Some pedantic clippy lints stabilize per-version; CI uses latest stable so locally `rustup update stable` is required.

    DO NOT commit until quality-gate is green. Per CLAUDE.md: "Cannot commit without passing all quality checks. Zero tolerance: formatting, clippy, build, tests."

    Once green, the work is ready for the user to commit. This task does NOT commit (per the planner protocol: no commits without explicit user instruction). It only verifies the working tree is shippable.
  </action>
  <verify>
    <automated>make quality-gate</automated>
  </verify>
  <done>
    `make quality-gate` exits 0. All make test targets (`test-unit`, `test-property`, `test-doc`, `test-integration`) pass. Existing AuthProvider integration tests pass unmodified. Working tree is in a state where `git commit` would not be blocked by the pre-commit hook. Plan is complete.
  </done>
</task>

</tasks>

<verification>
End-to-end verification (the goal-backward truths, expressed as concrete checks):

1. **on_unauthorized has default no-op** —
   `Test 1` in `src/shared/streamable_http.rs:1173` defines `MinimalProvider` with only `get_access_token` and asserts the default `on_unauthorized()` returns `Ok(())`. PASS = trait method is genuinely defaulted.

2. **Retry is single-shot** —
   `Test 2` (`:1198`) configures mockito to return 401 on every request and asserts `unauthorized_count == 1` and `get_count == 2` (no infinite loop). The proptest adds a stronger statement across arbitrary status sequences.

3. **Non-401 never triggers on_unauthorized** —
   `Test 3` (`:1237`) covers 200 and 500 paths; the proptest sweeps 200/202/400/403/500/503.

4. **auth_provider=None is a no-op** —
   The retry branch at `src/shared/streamable_http.rs:748-749` is guarded by `if let Some(provider) = auth_provider`. With no provider, the 401 response is returned unchanged. Covered by existing integration tests (`tests/streamable_http_integration.rs` and `tests/streamable_http_unit_tests.rs`) which exercise the no-auth code path and must continue to pass post-change.

5. **Retry preserves byte-identical body/method/headers** —
   `Test 4` (`:1288`) captures the on-wire bytes of both requests via a custom hyper server and asserts method, body, and non-Authorization headers are identical; Authorization differs (new token).

6. **on_unauthorized called BEFORE get_access_token on retry** —
   `Test 5` (`:1416`) uses `CountingProvider::with_order_tracking` and asserts the recorded call sequence.

7. **MSRV is 1.91** —
   `Cargo.toml:14` reads `rust-version = "1.91.0"`. `.github/workflows/ci.yml:263` pins `dtolnay/rust-toolchain@1.91`. `cargo build --workspace` succeeds on local 1.91 toolchain.

8. **Downstream pin ripple complete** —
   `! grep -rn 'pmcp = { version = "2\.7\.0"' crates/ cargo-pmcp/Cargo.toml` returns no matches.

9. **CHANGELOG entry present** —
   `## [2.8.0]` exists above `## [2.7.0]` with three bullets covering the hook, MSRV, and behavior note.

10. **Backward compat** —
    `src/server/auth/proxy.rs` (`ProxyProvider`, `NoOpAuthProvider`) and all test-mock AuthProvider impls in `tests/streamable_http_*.rs` and `tests/auth_context_integration_test.rs` compile unchanged. None has an `on_unauthorized` override; all use the default no-op.

11. **Quality gate green** —
    `make quality-gate` exits 0. PMAT cognitive-complexity gate passes (no function exceeds cog 25, or has a documented `// Why:` allow ≤ cog 50).
</verification>

<success_criteria>
This plan is complete when:
- [ ] Proptest added to `tests/streamable_http_properties.rs` covering the four invariants in Task 1 `<behavior>`, ≥64 cases, three consecutive runs pass
- [ ] `Cargo.toml` reads `rust-version = "1.91.0"`
- [ ] All CI workflow files pin `dtolnay/rust-toolchain@1.91`
- [ ] All workspace crates that pin `pmcp` directly pin `"2.8.0"` (verified by grep)
- [ ] `mcp-tester` is at `0.7.0`, `cargo-pmcp` is at `0.14.0`, with their `mcp-tester` cross-pin updated where applicable
- [ ] `CHANGELOG.md` has the `[2.8.0]` entry with the three required topics
- [ ] `make quality-gate` exits 0
- [ ] `make test-unit && make test-property && make test-doc && make test-integration` all exit 0
- [ ] Existing AuthProvider impls (`ProxyProvider`, `NoOpAuthProvider`, all test mocks) compile and pass without an `on_unauthorized` override
- [ ] All 5 inline unit tests in `src/shared/streamable_http.rs` still pass
- [ ] Working tree is in a shippable state (commit not blocked by pre-commit hook)
</success_criteria>

<output>
After completion, create `.planning/quick/260516-b2p-add-authprovider-on-unauthorized-hook-tr/260516-b2p-SUMMARY.md` summarizing:
- Final pmcp version, MSRV, downstream crate versions
- Number of tests added (1 proptest) and unit tests verified (5 inline)
- Files modified count + key line ranges
- `make quality-gate` exit status
- Any deviations from the locked decisions (D-01 through D-12) with justification (expect none)
- Readiness for the release workflow described in CLAUDE.md "Release Steps"
</output>
