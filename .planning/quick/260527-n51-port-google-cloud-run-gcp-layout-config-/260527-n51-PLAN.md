---
quick_id: 260527-n51
type: execute
mode: quick
title: "Port Google Cloud Run [gcp]+[layout] config, multi-crate Dockerfile, local docker-build deploy; aws() accessor; unify --target"
autonomous: true
files_modified:
  - cargo-pmcp/src/deployment/config.rs
  - cargo-pmcp/src/commands/deploy/deploy.rs
  - cargo-pmcp/src/deployment/targets/aws_lambda/deploy.rs
  - cargo-pmcp/src/deployment/targets/aws_lambda/init.rs
  - cargo-pmcp/src/deployment/targets/aws_lambda/mod.rs
  - cargo-pmcp/src/commands/configure/resolver.rs
  - cargo-pmcp/src/deployment/targets/google_cloud_run/mod.rs
  - cargo-pmcp/src/deployment/targets/google_cloud_run/dockerfile.rs
  - cargo-pmcp/src/deployment/targets/google_cloud_run/deploy.rs
  - cargo-pmcp/src/deployment/targets/google_cloud_run/env.rs
  - cargo-pmcp/src/deployment/targets/google_cloud_run/init.rs
  - cargo-pmcp/src/commands/deploy/mod.rs
  - cargo-pmcp/tests/cloud_run_local_build.rs

must_haves:
  truths:
    - "A GCR deploy.toml carrying [gcp] + [layout] and NO [aws]/memory_mb/timeout_seconds parses into DeployConfig"
    - "Existing aws-lambda / pmcp-run / cloudflare-workers deploy.toml files still parse and deploy unchanged"
    - "config.aws() returns the AwsConfig for AWS targets; the 7 read sites compile against the accessor"
    - "The multi-crate-isolated Dockerfile is generated with surgical per-crate COPYs + cargo build --manifest-path <primary>/Cargo.toml --bin <name> (no COPY . .)"
    - "Local docker buildx build path is wired for GCR deploy (build → push → gcloud run deploy)"
    - "cargo pmcp deploy --target <value> resolves a known backend type OR a configured named target, else hard-errors with a helpful message"
    - "cargo pmcp deploy --target pmcp-run still works"
  artifacts:
    - path: "cargo-pmcp/src/deployment/config.rs"
      provides: "GcpConfig, LayoutConfig, RuntimeConfig, aws:Option, gcp, layout, runtime fields, aws() accessor, default_for_cloud_run_server, is_multi_crate_isolated"
      contains: "pub fn aws(&self) -> &AwsConfig"
    - path: "cargo-pmcp/src/deployment/targets/google_cloud_run/dockerfile.rs"
      provides: "render_dockerfile + builder_stage_multi_crate_isolated + runtime_stage (distroless default)"
      contains: "--manifest-path"
    - path: "cargo-pmcp/src/deployment/targets/google_cloud_run/deploy.rs"
      provides: "layout-aware manifest sanity check + local docker buildx build"
      contains: "buildx"
    - path: "cargo-pmcp/src/deployment/targets/google_cloud_run/env.rs"
      provides: "render_set_env_vars deterministic --set-env-vars helper"
    - path: "cargo-pmcp/src/deployment/targets/google_cloud_run/init.rs"
      provides: "init_google_cloud_run writing Cloud-Run-shaped deploy.toml + Dockerfile/.dockerignore/cloudbuild.yaml"
    - path: "cargo-pmcp/tests/cloud_run_local_build.rs"
      provides: "double-gated (PMCP_RUN_DEPLOY_TEST env + #[ignore]) real local docker build integration test"
  key_links:
    - from: "config.aws() accessor"
      to: "7 AWS read sites (deploy/deploy.rs ×4, aws_lambda deploy.rs/init.rs/mod.rs)"
      via: "mechanical .aws → .aws() rewrite"
      pattern: "config\\.aws\\(\\)\\."
    - from: "google_cloud_run/mod.rs"
      to: "env + init submodules"
      via: "mod env; mod init; init delegates to init::init_google_cloud_run"
      pattern: "mod (env|init);"
    - from: "DeployCommand::get_target_id / resolve_target_flag"
      to: "TargetRegistry valid types + TargetConfigV1 named targets"
      via: "validation that hard-errors on unknown --target value"
      pattern: "(aws-lambda|google-cloud-run|pmcp-run|cloudflare-workers)"
---

<objective>
PORT the Google Cloud Run config schema, multi-crate Dockerfile generation, and local
docker-build deploy path from the 0.6.x reference branch
(`~/Development/mcp/sdk/rust-mcp-sdk-gcp-deploy`) into 0.14.0 `main`. Make `aws` optional
behind an `aws()` accessor, and unify the `--target` CLI flag so it accepts EITHER a known
backend type OR a configured named target (hard-erroring on anything else).

This is a faithful PORT. A complete, working reference implementation exists and is the
source of truth. READ the reference files, then adapt to 0.14.0's diverged surrounding code
(the Phase-77 named-target resolver, `--manifest-path`/`resolve_project_root`, `IamConfig`,
`WidgetsConfig`, the `--target-type` rename). Preserve every 0.14.0-only field and behavior.

Purpose: A GCR deploy.toml carrying `[gcp]` + `[layout]` currently fails to parse against the
AWS-shaped 0.14.0 `DeployConfig` ("missing field memory_mb" → timeout_seconds → aws), and the
GCR Dockerfile generator cannot handle multi-crate-isolated layouts where the deploy dir has no
Cargo.toml. The user wants local Docker builds to GCR working (GCR is the project's intentional
Docker deployment example alongside Lambda=serverless and Cloudflare=WASM), and `--target` to
behave consistently.

Output: A 0.14.0 `cargo-pmcp` that parses GCR configs, generates the surgical multi-crate
Dockerfile (byte-matching the reference golden), runs the real local `docker buildx` deploy
path, and validates `--target` against known types + named targets.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<reference_anchor>
**The reference tree is READ-ONLY and is the source of truth. Always diff/read the reference
file before adapting. Do NOT blind-copy — 0.14.0 has diverged.**

Reference root: `~/Development/mcp/sdk/rust-mcp-sdk-gcp-deploy/cargo-pmcp/`

Reference files (read these first for each task):
- `src/deployment/config.rs` — the desired schema (lines 6-234: DeployConfig + GcpConfig +
  LayoutConfig + RuntimeConfig + ServerConfig; 686-826: default_for_server / default_for_cloud_run_server /
  `aws()` accessor; 971-1231: the full test module that is the golden parse/roundtrip contract).
- `src/deployment/targets/google_cloud_run/dockerfile.rs` — render_dockerfile +
  builder_stage_multi_crate_isolated (lines 144-209 = the multi-crate golden text) + runtime_stage
  (distroless default) + the full test module (lines 528-990) including the golden COPY/--manifest-path
  asserts and the two property tests.
- `src/deployment/targets/google_cloud_run/deploy.rs` — layout-aware manifest sanity check
  (lines 63-81) + local `docker buildx` build (84-108) + push + `gcloud run deploy` with
  `--memory/--cpu/--region/--project` + resolve_params (252-323) + resolve_params test.
- `src/deployment/targets/google_cloud_run/env.rs` — render_set_env_vars (verbatim port).
- `src/deployment/targets/google_cloud_run/init.rs` — init_google_cloud_run + write_deploy_toml +
  the two init tests.
- `src/deployment/targets/google_cloud_run/mod.rs` — the `mod env; mod init;` wiring + the
  module-level rustdoc + `init()` delegating to `init::init_google_cloud_run`.

**KEY DIVERGENCE — Genchi Genbutsu (verify against the actual reference, not the issue text):**
1. The reference `aws()` accessor uses **panic-on-None** semantics (`self.aws.as_ref().expect("deploy.toml is missing the required [aws] block — …")`), NOT a "stored/static default". The
   locked decision text said "stored/static default"; the ACTUAL reference is `.expect()`. **Port the
   reference verbatim (panic-on-None).** Note this in the commit/summary: locked-decision text differed
   from reference; reference wins per "Port 0.6.x semantics verbatim."
2. The reference also ships a `[runtime]` block (RuntimeConfig: base + apt_packages) and `runtime_stage`
   distroless/debian split. This is part of the same #258/#259/#260 feature set and is required for the
   dockerfile.rs port to compile and its golden tests to pass. **Port RuntimeConfig too** (it is not
   separable from the dockerfile port).
3. 0.14.0 has `configure/resolver.rs` (Phase-77, NOT in the reference) with two `DeployConfig.aws`
   READ sites that run for ALL targets including google-cloud-run. These must NOT use the panicking
   `aws()` accessor — adapt them to read the Option directly (see Task 1).
4. 0.14.0 renamed the deploy flag `--target` → `--target-type` (with `--target` as an `alias`, mod.rs
   line ~152) and `self.target` → `self.target_type`. The reference's `get_target_id` reads `self.target`.
   Adapt to 0.14.0's `self.target_type` (Task 3).
5. 0.14.0 `DeployConfig` carries `iam: IamConfig`, `widgets: WidgetsConfig`, `post_deploy_tests`,
   `api_gateway` fields the reference lacks. **Preserve all of them** — the schema port ADDS gcp/layout/
   runtime and changes `aws` to Option; it must not drop any 0.14.0-only field. (The reference's
   `default_for_server` / `default_for_cloud_run_server` constructors omit these — keep the 0.14.0
   field initializers for iam/widgets/post_deploy_tests when porting the constructors.)
</reference_anchor>

<context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/CLAUDE.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/cargo-pmcp/src/deployment/config.rs
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/cargo-pmcp/src/deployment/registry.rs

<interfaces>
<!-- Contracts the executor needs. Extracted from the 0.14.0 codebase. -->

The 7 DeployConfig.aws READ sites to migrate to `.aws()` (Task 1):
```
cargo-pmcp/src/commands/deploy/deploy.rs:48   println!("🌍 Region: {}", config.aws.region);
cargo-pmcp/src/commands/deploy/deploy.rs:66   &config.aws.region,
cargo-pmcp/src/commands/deploy/deploy.rs:109  .env("AWS_REGION", &config.aws.region);
cargo-pmcp/src/commands/deploy/deploy.rs:112  if let Some(account_id) = &config.aws.account_id {
cargo-pmcp/src/deployment/targets/aws_lambda/deploy.rs:26   &config.aws.region
cargo-pmcp/src/deployment/targets/aws_lambda/init.rs:8      .with_region(&config.aws.region)
cargo-pmcp/src/deployment/targets/aws_lambda/mod.rs:179     &config.aws.region
```
These all run only on AWS-target paths → safe to use the panicking `config.aws()` accessor
(matches the reference exactly: `config.aws().region`).

The 2 resolver.rs READ sites + 1 test WRITE site that are 0.14.0-only (NOT in reference) — adapt by hand:
```
cargo-pmcp/src/commands/configure/resolver.rs:277  deploy_config.map(|d| d.aws.region.clone()).filter(|s| !s.is_empty())
cargo-pmcp/src/commands/configure/resolver.rs:288  deploy_config.and_then(|d| d.aws.account_id.clone()).filter(|s| !s.is_empty())
cargo-pmcp/src/commands/configure/resolver.rs:753  (test) deploy.aws.account_id = Some("999888777666".into());
```
The resolver runs for ALL targets (incl. google-cloud-run with aws=None) → MUST NOT panic.
Adapt 277 → `deploy_config.and_then(|d| d.aws.as_ref().map(|a| a.region.clone())).filter(|s| !s.is_empty())`
Adapt 288 → `deploy_config.and_then(|d| d.aws.as_ref().and_then(|a| a.account_id.clone())).filter(|s| !s.is_empty())`
Adapt 753 (test) → `if let Some(a) = deploy.aws.as_mut() { a.account_id = Some("999888777666".into()); }`
(or set `deploy.aws = Some(AwsConfig { region: ..., account_id: Some("999888777666".into()) })`).

NOT DeployConfig.aws — DO NOT migrate these:
```
cargo-pmcp/src/secrets/registry.rs:35-37   config.providers.aws.*   (SecretsConfig, different struct)
cargo-pmcp/src/secrets/config.rs:254       .join(".aws").join("credentials")  (a filesystem path literal)
```

TargetRegistry valid backend types (cargo-pmcp/src/deployment/registry.rs:20-23):
```
aws-lambda, cloudflare-workers, google-cloud-run, pmcp-run
```
TargetEntry::type_tag() (cargo-pmcp/src/commands/configure/config.rs:185-192) returns the same
kebab-case tags. Named targets live in TargetConfigV1::read(default_user_config_path()).targets
(a BTreeMap<String, TargetEntry>).

0.14.0 get_target_id (cargo-pmcp/src/commands/deploy/mod.rs:1071-1084):
```rust
fn get_target_id(&self, project_root: &PathBuf) -> Result<String> {
    if let Some(target) = &self.target_type {   // --target-type (alias --target)
        return Ok(target.clone());
    }
    if let Ok(config) = crate::deployment::DeployConfig::load(project_root) {
        return Ok(config.target.target_type.clone());
    }
    Ok("aws-lambda".to_string())
}
```
Consumed at mod.rs:592 `let target_id = self.get_target_id(&project_root)?;` then mod.rs:596
`registry.get(&target_id)?` (which errors with a bare "Unknown deployment target: {id}").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the GCP/layout/runtime config schema + aws() accessor + migrate the AWS call sites</name>
  <files>
    cargo-pmcp/src/deployment/config.rs,
    cargo-pmcp/src/commands/deploy/deploy.rs,
    cargo-pmcp/src/deployment/targets/aws_lambda/deploy.rs,
    cargo-pmcp/src/deployment/targets/aws_lambda/init.rs,
    cargo-pmcp/src/deployment/targets/aws_lambda/mod.rs,
    cargo-pmcp/src/commands/configure/resolver.rs
  </files>
  <behavior>
    Port the reference test module from `~/Development/mcp/sdk/rust-mcp-sdk-gcp-deploy/cargo-pmcp/src/deployment/config.rs` (lines 971-1231) into 0.14.0 config.rs `mod tests`. These tests are the contract — write/port them first, watch them fail against the AWS-shaped schema, then implement until green:
    - `aws_lambda_deploy_toml_still_loads`: an aws-lambda toml with [aws]/memory_mb=256/timeout_seconds=30 parses; aws present, gcp/layout/runtime None.
    - `cloud_run_deploy_toml_loads`: a toml with [gcp]+[layout]+[runtime] and NO [aws]/memory_mb/timeout_seconds parses; aws None, gcp present, memory_mb/timeout_seconds DEFAULT to 512/30, layout.kind == "multi-crate-isolated", layout.primary == "gcp-cloud-run", layout.path_deps == ["auth-echo-core"].
    - `default_for_cloud_run_server_roundtrips`, `default_for_server_roundtrips_as_aws_lambda`.
    - `aws_helper_panics_when_aws_absent`: `config.aws()` on a gcp-only config panics with "deploy.toml is missing the required [aws] block".
    - `minimum_schema_cloud_run_loads_without_auth_or_observability`, `partial_observability_block_loads`, `runtime_apt_packages_defaults_empty`.
    Preserve the existing 0.14.0 `iam_wave1_tests` and `iam_wave2_tests` modules verbatim — they must still pass (backward-compat invariant; the cost-coach fixture builds on default_for_server with memory_mb:512/timeout:30).
  </behavior>
  <action>
    READ the reference config.rs FIRST (lines 6-234, 686-826, 971-1231). Then in 0.14.0 config.rs:
    1. Add `GcpConfig` (project_id, region), `LayoutConfig` (kind, primary, path_deps with `#[serde(default)]` on path_deps) + its `is_multi_crate_isolated()` method, and `RuntimeConfig` (base: Option<String>, apt_packages: Vec<String>) — copy the reference docstrings and `#[serde]` attrs verbatim.
    2. Change `DeployConfig.aws: AwsConfig` → `pub aws: Option<AwsConfig>` with `#[serde(default, skip_serializing_if = "Option::is_none")]`. Add `pub gcp: Option<GcpConfig>`, `pub layout: Option<LayoutConfig>`, `pub runtime: Option<RuntimeConfig>` (all `#[serde(default, skip_serializing_if = "Option::is_none")]`). PRESERVE every existing 0.14.0 field: `iam: IamConfig`, `widgets: WidgetsConfig`, `post_deploy_tests`, `api_gateway`, `assets`, `composition`, `auth`, `observability`, `environment`, `secrets`, `project_root`. Match the reference's `#[serde(default)]` on `auth`/`observability` and the per-field `ObservabilityConfig` defaults so minimum-schema Cloud Run loads (reference made ObservabilityConfig fields `#[serde(default)]` + derive Default).
    3. Add `memory`/`cpu`/`ingress`/`allow_unauthenticated`/`max_instances`/`min_instances`/`binary` Cloud Run fields to `ServerConfig`, and make `memory_mb`/`timeout_seconds` `#[serde(default = "default_memory_mb")]`/`#[serde(default = "default_timeout_seconds")]` with the two free functions (return 512 / 30) — verbatim from reference.
    4. Add the `aws()` accessor VERBATIM from the reference (panic-on-None via `.expect(...)` — see reference_anchor KEY DIVERGENCE #1). Add `default_for_cloud_run_server` and `save_if_missing` (reference lines 669-684, 734-791). Update `default_for_server` to set `aws: Some(AwsConfig {..})`, `gcp: None`, `layout: None`, `runtime: None`, and KEEP the 0.14.0 `iam`/`widgets`/`post_deploy_tests` initializers.
    5. Migrate the 7 AWS READ sites to `config.aws()` (see <interfaces>) — mechanical, exactly as the reference does (`config.aws().region` etc.).
    6. Adapt the 2 resolver.rs READ sites + 1 test WRITE site by hand (see <interfaces> — these are 0.14.0-only and MUST NOT use the panicking accessor since the resolver runs for gcp-only configs).
    cognitive complexity ≤25 per fn; zero SATD. No "v1"/"placeholder"/"for now" — port the full schema.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo test -p cargo-pmcp --lib deployment::config -- --test-threads=1 && cargo test -p cargo-pmcp --lib commands::configure::resolver -- --test-threads=1 && cargo build -p cargo-pmcp</automated>
  </verify>
  <done>GCR config (gcp+layout+runtime, no aws/memory_mb/timeout) parses; aws-lambda config still parses with memory_mb=256; existing iam_wave1/iam_wave2 tests still pass; config.aws() panics on gcp-only; all 7 AWS sites + resolver compile; `cargo build -p cargo-pmcp` succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the multi-crate Dockerfile generation + layout-aware local docker-build deploy path</name>
  <files>
    cargo-pmcp/src/deployment/targets/google_cloud_run/dockerfile.rs,
    cargo-pmcp/src/deployment/targets/google_cloud_run/deploy.rs,
    cargo-pmcp/src/deployment/targets/google_cloud_run/env.rs,
    cargo-pmcp/src/deployment/targets/google_cloud_run/init.rs,
    cargo-pmcp/src/deployment/targets/google_cloud_run/mod.rs,
    cargo-pmcp/tests/cloud_run_local_build.rs
  </files>
  <behavior>
    Port the reference dockerfile.rs test module (golden contract) FIRST, then implement until green. The golden multi-crate Dockerfile (reference dockerfile.rs lines 144-209, asserted by `dockerfile_multi_crate_isolated_emits_surgical_copy_pairs`) must contain, for layout{primary:"gcp-cloud-run", path_deps:["auth-echo-core"], binary:"server"}:
    - `WORKDIR /app`
    - `COPY gcp-cloud-run/Cargo.toml gcp-cloud-run/Cargo.toml`
    - `COPY gcp-cloud-run/src gcp-cloud-run/src`
    - `COPY auth-echo-core/Cargo.toml auth-echo-core/Cargo.toml`
    - `COPY auth-echo-core/src auth-echo-core/src`
    - `RUN cargo build --release \  --manifest-path gcp-cloud-run/Cargo.toml \  --bin server`
    - NO `COPY . .`; no `lambda` in any COPY directive.
    Plus the full reference test set: simple/workspace templates, binary fall-back to server.name, crate-name SANITIZATION (no `;`, no `..`, no `rm -rf` leaking), distroless default runtime (no apt/useradd/HEALTHCHECK), debian opt-in + apt_packages, the two property tests (prop_sanitize_path_dep_yields_safe_chars_only, prop_render_set_env_vars_is_sorted_and_well_formed), env.rs's 3 render tests, deploy.rs's resolve_params test, and init.rs's 2 init tests.
    The env-gated integration test `cargo-pmcp/tests/cloud_run_local_build.rs` is double-gated: early-return unless `std::env::var("PMCP_RUN_DEPLOY_TEST").is_ok()` AND `#[ignore]` — it performs a REAL local `docker build` of a tiny synthesized multi-crate-isolated fixture (mirror the Phase 86 `PMCP_RUN_DEPLOY_TEST` env-gate + `#[ignore]` double-gate pattern). It MUST be a no-op in CI/quality-gate (Docker absent → the env-gate short-circuits before any docker call).
  </behavior>
  <action>
    READ the reference dockerfile.rs / deploy.rs / env.rs / init.rs / mod.rs FIRST. Then:
    1. Replace 0.14.0 dockerfile.rs entirely with the reference dockerfile.rs (render_dockerfile, resolve_binary_name, BUILDER_APT_LAYER, FIND_AND_COPY_BINARY, builder_stage_workspace/simple/multi_crate_isolated, runtime_stage + distroless/debian split + is_debian_family, generate_dockerfile/dockerignore/cloudbuild + the full test module). Keep the reference's `FROM rust:1-slim` (not 0.14.0's `rust:1.83-slim`) — that is the reference golden.
    2. Replace 0.14.0 deploy.rs with the reference deploy.rs (deploy_to_cloud_run with layout-aware manifest sanity check + local `docker buildx build --platform linux/amd64 --load .` + push + `gcloud run deploy` sourcing --memory/--cpu/--region/--project/--max-instances/--min-instances/--ingress/--set-env-vars from config; CloudRunParams + resolve_params + its test).
    3. Create env.rs verbatim from the reference (render_set_env_vars `pub(super)` + 3 tests).
    4. Create init.rs verbatim from the reference (init_google_cloud_run + write_deploy_toml + 2 tests). Note: 0.14.0 already has `DeployConfig::save_if_missing` from Task 1.
    5. Update mod.rs: add `mod env;` and `mod init;` to the module decls; change `async fn init` to delegate to `init::init_google_cloud_run(config)`; port the module-level rustdoc from the reference. Keep all other 0.14.0 trait-impl methods (destroy/outputs/logs/metrics/secrets/test/rollback/build) as-is.
    6. Create `cargo-pmcp/tests/cloud_run_local_build.rs` — the double-gated real-docker-build test (env-gate + #[ignore]). It synthesizes a multi-crate-isolated fixture (primary crate + one path_dep, each a 3-line hello binary), renders the Dockerfile via the public `init`/`generate_dockerfile` path, runs `docker build`, asserts exit 0, and cleans up. If `docker` is missing it still must not run because the PMCP_RUN_DEPLOY_TEST gate returns first.
    If the Dockerfile generator function exceeds cog 25, keep the reference's helper-extracted shape (builder_stage_* / runtime_stage_* are already split for exactly this reason) — do not inline.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo test -p cargo-pmcp --lib deployment::targets::google_cloud_run -- --test-threads=1 && cargo build -p cargo-pmcp --tests</automated>
  </verify>
  <done>Multi-crate-isolated Dockerfile byte-matches the reference golden (surgical COPYs + --manifest-path gcp-cloud-run/Cargo.toml --bin server, no COPY . .); distroless default + debian opt-in tests pass; sanitization + both property tests pass; env/init/resolve_params tests pass; cloud_run_local_build.rs compiles and is `#[ignore]`+env-gated; `cargo build -p cargo-pmcp --tests` succeeds. The quality-gate does NOT invoke Docker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unify --target — accept known backend type OR configured named target, hard-error otherwise</name>
  <files>
    cargo-pmcp/src/commands/deploy/mod.rs
  </files>
  <behavior>
    Write the resolution unit tests FIRST (in a `#[cfg(test)] mod target_resolution_tests` in deploy/mod.rs), then implement until green:
    - known type → ok: `--target aws-lambda`, `--target google-cloud-run`, `--target cloudflare-workers`, `--target pmcp-run` each resolve to that type id. (`--target pmcp-run` MUST keep working — it is the user's everyday flow.)
    - configured named target → ok: a named target "prod" of kind aws-lambda present in the user config resolves to its `type_tag()` ("aws-lambda").
    - unknown → Err: `--target totally-bogus` (not a known type, not a configured name) returns an Err whose message (a) lists the valid backend types, (b) lists the configured named-target names, and (c) if the value is a near-miss of a known type, hints `--target-type`. NEVER falls through to `<unset>` or a bare "Unknown deployment target".
    - `--target-type` still works as an explicit synonym for the backend-type path (it bypasses named-target lookup — it is type-only).
    Keep the tests hermetic: do not depend on the operator's real `~/.pmcp/config.toml`. Inject the candidate type-set and the named-target name-set into a small pure resolver fn (e.g. `resolve_target_flag(value, &valid_types, &named_target_kinds) -> Result<String>`) so the unit tests can call it without filesystem state; `get_target_id` becomes the thin wrapper that gathers `valid_types` from `TargetRegistry` and `named_target_kinds` from `TargetConfigV1::read(default_user_config_path())`.
  </behavior>
  <action>
    In cargo-pmcp/src/commands/deploy/mod.rs:
    1. Extract a pure, testable `fn resolve_target_flag(value: &str, valid_types: &[&str], named: &BTreeMap<String, String>) -> Result<String>` where `named` maps configured-target-name → its `type_tag()`. Logic: if `valid_types.contains(value)` → Ok(value). Else if `named.contains_key(value)` → Ok(named[value].clone()) (the backend type the named target deploys to). Else → bail! with the helpful message (valid types list + configured names list + near-miss hint when `value` is within edit-distance-1 / is a prefix of a known type, suggesting `--target-type <type>`).
    2. Rewrite `get_target_id` to: if `self.target_type` is Some → call `resolve_target_flag(value, valid_types, &named)` (this is the unified `--target`/`--target-type` path); else fall through to `DeployConfig::load(...).target.target_type`; else default "aws-lambda". Gather `valid_types` from `TargetRegistry::new()` ids (aws-lambda, cloudflare-workers, google-cloud-run, pmcp-run) and `named` from `TargetConfigV1::read(default_user_config_path())?.targets` mapping name → `entry.type_tag().to_string()` (treat a missing/unreadable user config as an empty map — never error just because the user has no named targets).
    3. Because `--target` is the `alias` for `--target-type` on the DeployCommand struct (mod.rs ~152), a single `self.target_type` field already carries both — no clap struct change needed. Keep `--target-type` semantics as the explicit type-only synonym: it still flows through `resolve_target_flag` (a real type passes; a named-target name would also pass via the `named` branch — that is acceptable and consistent with the unified design, and keeps `--target` and `--target-type` behaving identically per the locked decision "keep --target-type working as a synonym").
    cognitive complexity ≤25 per fn (split the near-miss hint into a helper); zero SATD. Do NOT weaken the existing registry.get error — the new hard-error fires BEFORE registry.get so the operator sees the helpful message, not "Unknown deployment target".
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo test -p cargo-pmcp --lib commands::deploy -- --test-threads=1 && cargo build -p cargo-pmcp</automated>
  </verify>
  <done>`--target {aws-lambda|google-cloud-run|cloudflare-workers|pmcp-run}` resolve to the type; a configured named target resolves to its backend type; `--target pmcp-run` still works; an unknown value hard-errors with a message listing valid types + configured names + a `--target-type` hint; never falls to `<unset>`; `--target-type` still works as synonym; `cargo build -p cargo-pmcp` succeeds.</done>
</task>

</tasks>

<verification>
Scope all gate commands to `cargo-pmcp` (the broad `make quality-gate` is blocked by KNOWN pre-existing
unrelated failures — RUSTSEC-2023-0071 audit via sqlx-mysql, and pmcp-widget-utils rust-1.95 pedantic
lints — documented in `.planning/STATE.md` deferred items; NEVER weaken a gate to work around these):

```bash
cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk
cargo fmt -p cargo-pmcp --check
cargo clippy -p cargo-pmcp --all-targets -- -D warnings -W clippy::pedantic -W clippy::nursery
cargo test -p cargo-pmcp -- --test-threads=1
```

Backward-compat gate (explicit): the existing config.rs tests using `memory_mb: 512` / `timeout: 30`
(`iam_wave1_tests`, `iam_wave2_tests`) MUST still pass, and aws-shaped round-trip tests MUST still pass.

Docker gate boundary: the quality-gate MUST NOT invoke Docker. The real-build test
(`cargo-pmcp/tests/cloud_run_local_build.rs`) is double-gated (PMCP_RUN_DEPLOY_TEST env early-return +
`#[ignore]`); `cargo test -p cargo-pmcp` skips it cleanly with no docker call. The operator runs it
locally via `PMCP_RUN_DEPLOY_TEST=1 cargo test -p cargo-pmcp --test cloud_run_local_build -- --ignored`.

If any gate stops for a documented pre-existing-unrelated reason, record it in the SUMMARY and proceed
(do NOT fix unrelated lints; do NOT weaken the gate).
</verification>

<success_criteria>
- A GCR deploy.toml with `[gcp]`+`[layout]` and no `[aws]`/`memory_mb`/`timeout_seconds` PARSES (unit test).
- The generated multi-crate-isolated Dockerfile BYTE-MATCHES the reference golden (surgical per-crate
  COPYs + `cargo build --release --manifest-path gcp-cloud-run/Cargo.toml --bin server`, no `COPY . .`).
- `--target` resolution: known type → ok, configured named → ok, unknown → Err with hint,
  `--target pmcp-run` still works (unit tests).
- `config.aws()` panics with the reference message on a gcp-only config; all 7 AWS read sites compile via
  the accessor; the 2 resolver.rs sites read the Option safely (no panic on gcp-only).
- Local `docker buildx` deploy path is wired (build → push → gcloud run deploy) and exercised by the
  env-gated #[ignore] integration test (operator-run, not CI).
- Existing aws-lambda / pmcp-run / cloudflare-workers deploy.toml shapes still parse and deploy unchanged.
- `cargo fmt -p cargo-pmcp --check` + `cargo clippy -p cargo-pmcp --all-targets -- -D warnings
  -W clippy::pedantic -W clippy::nursery` + `cargo test -p cargo-pmcp -- --test-threads=1` all pass
  (modulo documented pre-existing-unrelated gate stops); cognitive complexity ≤25; zero SATD.
</success_criteria>

<output>
After completion, create `.planning/quick/260527-n51-port-google-cloud-run-gcp-layout-config-/260527-n51-SUMMARY.md`
documenting: files ported, the panic-vs-static-default divergence resolution (reference won), the actual
count of migrated `.aws` sites (7 AWS + 2 resolver + 1 test, NOT 18 — the providers.aws / .aws-credentials
sites are different structs), the RuntimeConfig port (required for the dockerfile golden), and any
documented pre-existing-unrelated gate stops.
</output>
