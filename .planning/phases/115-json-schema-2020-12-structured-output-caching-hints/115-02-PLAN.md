---
phase: 115-json-schema-2020-12-structured-output-caching-hints
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/v1_lists_golden.rs
autonomous: true
requirements: [SCHM-03]

must_haves:
  truths:
    - "Today's v1 wire bytes for tools/list, prompts/list, resources/list, resources/templates/list and resources/read are pinned, captured before any caching-hint field exists"
    - "A future change that adds a key, reorders keys, or turns an omission into an explicit null on any of those five v1 responses fails a test by name"
    - "The comparison is raw-text, so it can see key order, whitespace and omission-versus-null — the three things a structural assert cannot"
  artifacts:
    - path: "tests/v1_lists_golden.rs"
      provides: "Pre-change golden byte fixtures for the five v1 list/read responses"
      min_lines: 300
      contains: "v1_lists_golden_"
  key_links:
    - from: "tests/v1_lists_golden.rs"
      to: "tests/common/v2.rs"
      via: "spawn_stateless_config + post + v1_body over real loopback HTTP"
      pattern: "common::v2::"
---

<objective>
Capture the CURRENT v1 wire bytes of the five list/read responses as raw-string golden fixtures,
driven over real loopback HTTP against a not-opted-in server — **before** any plan in this phase adds
a field to those result types.

Purpose: D-13. Once `ttl_ms` / `cache_scope` land on `ListToolsResult` and its four siblings
(115-05), the pre-change bytes are unrecoverable. D-11 makes v1 byte-identity the severability
precedent for Phases 116-119, and CONTEXT.md explicitly rejected absence-assertions in favour of
goldens: absence assertions prove only the fields you thought to check and would miss collateral
drift in the same responses. 114-02 captured exactly these fixtures for `tasks/*` for exactly this
reason, and they caught real drift.

**This plan must land before 115-05.** It is wave 1 for that reason and no other.

Output: `tests/v1_lists_golden.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-PATTERNS.md
@tests/v1_tasks_golden.rs
@tests/common/v2.rs

<interfaces>
<!-- The shared v2/v1 HTTP harness. Import these; do not write a second one. -->
From tests/common/v2.rs:
  pub const V1: &str                                          (:72, = LATEST_PROTOCOL_VERSION)
  pub async fn spawn_stateless_config(server: Server) -> (SocketAddr, JoinHandle<()>)   (:389)
  pub async fn teardown<S: Send>(handle: JoinHandle<()>, sockets: S)                    (:528)
  pub fn v1_body(method: &str, id: Value, params: Value) -> String                      (:660)
  pub async fn post(addr: SocketAddr, extra: &[(String, String)], body: &str) -> Resp   (:801)
  pub struct Resp                                             (:717, carries status + raw text)

From tests/v1_tasks_golden.rs — the instrument being copied:
  struct DynamicField { key, token, shape, shape_description }   (:82-92)
  struct V1Golden<'a> { id, raw, frame, dynamics, meta }         (:262-273)
  fn assert_v1_bytes(raw: &str, golden: &V1Golden<'_>)           (:292, four-step contract at :275-291)
  fn wire_break_message(raw: &str) -> String                     (:254)
  enum Frame { Result(Value), Error(Value) }
  enum MetaExpectation { Absent, RelatedTaskOnly }
  async fn spawn(server: Server) -> (SocketAddr, JoinHandle<()>) (:508)
  async fn shutdown(handle: JoinHandle<()>)                      (:~520, uses teardown for D-113-T order)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the harness and capture the five v1 golden frames</name>
  <files>tests/v1_lists_golden.rs</files>
  <read_first>
    - tests/v1_tasks_golden.rs — read lines 1-70 (module doc + feature gate + imports),
      254-360 (`wire_break_message`, `V1Golden`, `assert_v1_bytes`, `assert_meta`) and 500-560
      (spawn/shutdown trio). This is the instrument being copied; restate its doctrine, do not
      re-derive it.
    - tests/common/v2.rs lines 380-400, 520-540, 655-700, 795-830 — `spawn_stateless_config`,
      `teardown`, `v1_body`, `post`, `Resp`
    - src/server/core.rs lines 565-580, 850-1000 — `handle_list_tools`, `handle_list_prompts`,
      `handle_list_resources`, `handle_list_resource_templates`, `handle_read_resource`:
      the exact structs whose serialization is being pinned
    - src/types/tools.rs lines 428-453, src/types/resources.rs lines 131-156 / 297-322 /
      354-395, src/types/prompts.rs lines 244-269 — the five result structs as they are TODAY
  </read_first>
  <action>
Create `tests/v1_lists_golden.rs`.

Copy the feature gate verbatim from `tests/v1_tasks_golden.rs:49-53`:
`#![cfg(all(feature = "streamable-http", feature = "http-client", not(target_arch = "wasm32")))]`,
then `mod common;` and `use common::v2::{post, spawn_stateless_config, v1_body, Resp};`.

Write a module doc restating the three headings from `v1_tasks_golden.rs`, retargeted:
`# Read this before you change a literal in this file` (a diff here is a **v1 WIRE BREAK**, not a
stale fixture — make the change v2-only rather than re-recording); `# Why a RAW-STRING comparison`
(this crate builds `serde_json` with `preserve_order` at `Cargo.toml:55`, so key order is observable;
a structural assert cannot see order, whitespace, or omission-versus-explicit-null); and
`# What these five fixtures are for` naming SCHM-03 / D-11 / D-13 and stating that they were captured
BEFORE `ttlMs` / `cacheScope` existed on any result type.

Build ONE fixed test server with `pmcp::server::Server::builder()` carrying deterministic content —
no timestamps, no UUIDs, no ordering that depends on a hash map iteration:
  - exactly two tools with fixed names, descriptions and input schemas
  - exactly two prompts with fixed names and descriptions
  - a `ResourceHandler` returning a fixed two-entry `ListResourcesResult`, a fixed one-entry
    `ListResourceTemplatesResult`, and a fixed `ReadResourceResult` for one known URI
Register them in a stable order and assert in the fixtures that the order is what you pinned. If any
response turns out to be non-deterministic across runs, that is a FINDING — record it in the SUMMARY
and pin it with a `DynamicField`; do not paper over it by relaxing the comparison.

Because none of these five responses carries a minted id or a timestamp, declare
`const NO_DYNAMICS: &[DynamicField] = &[];` with the rustdoc `v1_tasks_golden.rs` uses for its
router-backed path: nothing is normalized, so these goldens are pinned verbatim, byte for byte.

Add the spawn/shutdown trio (`spawn` delegating to `spawn_stateless_config`, `shutdown` delegating to
`common::v2::teardown(handle, ()).await` for the D-113-T teardown order) and a
`fn lists_body(id: i64, method: &str, params: Value) -> String` delegating to `v1_body`.

Write five `#[tokio::test]` functions, each: spawn → `post` a v1 body → shutdown →
`assert_eq!(resp.status, 200)` → `assert_v1_bytes(&resp.text, &golden)`. Every function name MUST
begin with the file stem `v1_lists_golden_` so both `binary(v1_lists_golden)` and
`test(/v1_lists_golden/)` select it (115-RESEARCH § Pitfall 4 — a `test()` selector that matches
nothing exits 0 and reports success):
  - `v1_lists_golden_tools_list`                 → `tools/list`, id 1
  - `v1_lists_golden_prompts_list`               → `prompts/list`, id 2
  - `v1_lists_golden_resources_list`             → `resources/list`, id 3
  - `v1_lists_golden_resource_templates_list`    → `resources/templates/list`, id 4
  - `v1_lists_golden_resources_read`             → `resources/read`, id 5, params carrying the known URI

To obtain the golden literals: write each `const` as an empty string first, run the test, and copy
the raw response verbatim out of the failure message produced by `wire_break_message`. Paste it into
a Rust raw string (`r#"..."#`). Do NOT hand-construct the expected JSON from the struct definitions —
115-RESEARCH § Finding 8 notes that injected keys land in insertion order at the END of the object,
so a struct-derived expectation and the real wire can disagree.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'binary(v1_lists_golden)'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --features full -E 'binary(v1_lists_golden)'` exits 0 and its Summary line reports exactly 5 tests run (zero-selection is a FAILURE)
    - Every test function name in `tests/v1_lists_golden.rs` begins with `v1_lists_golden_`
    - Each of the five golden constants is a non-empty Rust raw string containing `"jsonrpc":"2.0"` and `"result"`
    - Running the suite twice in a row produces identical results (determinism): `cargo nextest run --features full -E 'binary(v1_lists_golden)' && cargo nextest run --features full -E 'binary(v1_lists_golden)'` both exit 0
    - `grep -c 'assert_v1_bytes' tests/v1_lists_golden.rs` returns at least 5
    - Negative control: temporarily change one character inside one golden literal; the suite MUST fail with the `V1 WIRE BREAK` message naming that test; restore afterwards
  </acceptance_criteria>
  <done>The five v1 list/read responses are pinned raw, byte for byte, from a real HTTP round trip against a not-opted-in server.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the assertion contract with the SCHM-03 leak guards</name>
  <files>tests/v1_lists_golden.rs</files>
  <read_first>
    - tests/v1_lists_golden.rs — the file created in Task 1
    - tests/v1_tasks_golden.rs lines 275-332 — the four-step `assert_v1_bytes` contract and its
      existing `resultType` / `serverInfo` leak guards
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md
      § D-11 — "Caching hints are era-gated OFF on v1; v1 responses stay byte-identical"
  </read_first>
  <action>
Port `assert_v1_bytes` into this file with its four-step rustdoc contract intact (width invariant →
raw-string comparison → structural comparison → v2 leak guards), and EXTEND step 4's must-not-appear
set for this phase.

The v1 leak guard must reject all four of these substrings in the raw response text:
`resultType`, `serverInfo`, `ttlMs`, `cacheScope`. Give the two new ones their own assertion with a
message naming D-11 and stating that a v1 response carrying a v2 field breaks the milestone's
severability story — this is the assertion that becomes load-bearing in wave 4 when 115-06's
projection lands, and it is deliberately vacuous today.

Add a `MetaExpectation::Absent` check for all five fixtures (none of these responses carries `_meta`
today — verify that against the captured bytes rather than assuming it; `ReadResourceResult` DOES
have a `_meta` field with `skip_serializing_if`, so confirm the captured `resources/read` golden
really has no `_meta` key before pinning `Absent`).

Add one dedicated anti-vacuity test, `v1_lists_golden_leak_guard_is_load_bearing`, that feeds
`assert_v1_bytes`-style guard logic a synthetic raw string containing `"ttlMs":0` and asserts the
guard rejects it (use `std::panic::catch_unwind` or factor the guard into a
`fn v1_leak_guard(raw: &str) -> Result<(), String>` the test can call directly). Without this, the
new guards would pass today for the trivial reason that the keys do not exist yet, and nobody would
learn that the guard itself was mis-wired until wave 4.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'binary(v1_lists_golden)'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --features full -E 'binary(v1_lists_golden)'` exits 0 and its Summary line reports exactly 6 tests run
    - `tests/v1_lists_golden.rs` contains all four literal guard strings: `resultType`, `serverInfo`, `ttlMs`, `cacheScope`
    - `tests/v1_lists_golden.rs` contains a test function named `v1_lists_golden_leak_guard_is_load_bearing`
    - The leak-guard failure message contains the literal text `D-11`
    - `cargo fmt --all -- --check` exits 0
    - `make lint` exits 0
  </acceptance_criteria>
  <done>A v1 response that ever gains `ttlMs` or `cacheScope` fails a named test, and the guard itself is proven to fire rather than merely proven to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → v1 client | A v1-negotiated client receives bytes it will parse against the 2025-11-25 contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-04 | Tampering | the five v1 list/read responses | mitigate | Raw-text golden fixtures captured pre-change over real loopback HTTP, plus an explicit `ttlMs`/`cacheScope` leak guard with its own anti-vacuity test. A v2 field reaching a v1 wire fails a named test rather than shipping silently. |
| T-115-10 | Repudiation | golden re-recording | mitigate | The module doc states that a red golden is a wire break, not a stale fixture, and names re-recording as the prohibited response — the same doctrine `tests/v1_tasks_golden.rs` carries and which caught real drift in Phase 114 |
| T-115-SC | Tampering | package installs | mitigate | This plan installs NO package and touches no manifest. `115-RESEARCH.md` § Package Legitimacy Audit shows zero `[ASSUMED]`/`[SUS]`/`[SLOP]` entries, so no blocking legitimacy checkpoint applies. |
</threat_model>

<verification>
- `cargo nextest run --features full -E 'binary(v1_lists_golden)'` — green, 6 tests
- `git diff --stat -- src/ Cargo.toml` is EMPTY — this plan changes zero production bytes
- `cargo fmt --all -- --check` and `make lint` both green
</verification>

<success_criteria>
- Five raw-text v1 golden fixtures exist, captured from real HTTP before any field addition
- The leak guard covers `resultType`, `serverInfo`, `ttlMs` and `cacheScope`, and is proven to fire
- Zero production bytes changed by this plan
</success_criteria>

<output>
Create `.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-02-SUMMARY.md` when done.
Record in it: the five captured raw frames (or their sizes), and any response that turned out to be
non-deterministic across runs — that is a finding, not a nuisance.
</output>
