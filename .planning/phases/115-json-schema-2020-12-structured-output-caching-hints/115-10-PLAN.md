---
phase: 115-json-schema-2020-12-structured-output-caching-hints
plan: 10
type: execute
wave: 6
depends_on: ["115-01", "115-02", "115-03", "115-04", "115-05", "115-06", "115-07", "115-08", "115-09"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md
autonomous: false
requirements: [SCHM-01, SCHM-02, SCHM-03]

must_haves:
  truths:
    - "make quality-gate is green across the whole workspace with every Phase 115 change in the tree"
    - "The three SCHM requirements are booked with a verdict that cites measured evidence, not an inherited habit"
    - "Every deviation this phase took from its own requirement text is written down where a reader will hit it"
    - "Every out-of-scope discovery names an owner or says explicitly that it is unowned"
    - "The phase is signed off by the owner rather than self-approved"
  artifacts:
    - path: ".planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md"
      provides: "The phase's ledger of out-of-scope discoveries, each with an owner or an explicit unowned marker"
      min_lines: 60
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCHM-01/02/03 booked with cited evidence"
      contains: "SCHM-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "schema/vendored/core-2026-07-28/PROVENANCE.md"
      via: "the booking cites the pinned artifact as its published evidence"
      pattern: "core-2026-07-28"
---

<objective>
Run the whole-phase gate, book the three SCHM requirements on measured evidence, write down every
deviation and every unowned discovery, and take a real sign-off.

Purpose: D-15 states the phase's booking target plainly — *"Phase 115 has NO publication hold and
must not inherit a `[~]` booking from Phase 114 by habit."* The contingency D-15 kept available (the
Phase-113 HTTP-04 split) **did not fire**: `CacheableResult` is in the published core schema, and
115-01 vendored it at a pinned commit with digests. So this plan books `[x]`, and it books it by
CITING that artifact rather than by assertion.

Three deviations this phase took must be recorded, not buried:
1. `jsonschema` shipped at **0.49**, while SCHM-01's text says 0.48.
2. **SIX** result types carry caching hints, while SCHM-03's text and CONTEXT.md both say five —
   `DiscoverResult extends CacheableResult` in the pinned schema.
3. `server/discover` is included for that same reason, so a v2 client's FIRST call is conformant.

Output: a green gate, booked requirements, an updated ROADMAP and STATE, a deferred-items ledger, and
an owner sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/114-tasks-extension-migration/deferred-items.md

<interfaces>
Gate targets (Makefile):
  quality-gate       :673  = fmt-check → lint → build → test-all → pmcp-package-gate → audit →
                             unused-deps → check-todos → check-unwraps → validate-always →
                             purity-check → comply
  test-all           :361  = test-unit test-doc test-property test-examples test-integration
  test-feature-flags :303
  wasm-build         :59
  validate-always    :366  = test-fuzz → test-property → test-unit → test-examples

Requirement lines to book — .planning/REQUIREMENTS.md:143-145, currently all `[ ]`:
  SCHM-01  Schema validation runs Draft 2020-12 explicitly pinned (jsonschema 0.48, no `$schema`
           auto-detect), staying wasm-clean and SEP-2106-compliant (no external `$ref` dereference)
  SCHM-02  On v2, `structuredContent` accepts any JSON value (scalar/array/null/object); v1-negotiated
           tools keep the existing object-shaped behavior
  SCHM-03  The five list/read results carry `ttlMs`/`cacheScope` caching hints (additive fields)

Booking-format precedent: the TASK block at REQUIREMENTS.md:118-139 — a `[x]`/`[~]` marker, then an
indented block quote carrying the measured evidence and any judgement the booking makes EXPLICITLY
rather than absorbing.

Ledger-format precedent: `.planning/phases/114-tasks-extension-migration/deferred-items.md` — one
`## D-115-<letter>` heading per item, each naming filer, subject, owner (or **unowned**), and why it
was not fixed. ⚠ That file records an ID-collision incident: three plans appended without checking
for the next free letter, which defeated the point of an ID. Run `grep -c "^## D-115-"` before
assigning one.
</interfaces>

<deviations_and_discoveries_to_record>
<!-- Assembled from the nine preceding plans. VERIFY each against the landed SUMMARYs rather than
     copying — a SUMMARY that contradicts a row here is itself a finding and must be reported. -->

Deviations from requirement text:
  jsonschema 0.49 vs SCHM-01's literal "0.48"             — 115-03 Task 1
  SIX cacheable results vs SCHM-03's "five"               — 115-01 Task 3, 115-05, 115-06, 115-07
  `server/discover` included as the sixth                 — 115-06 Task 2, 115-07 Task 1

Deferred / unowned discoveries:
  `pmcp-agent` `src/iteration/decide.rs:218` `validator_for` is unpinned; allowlisted in 115-08 with
      a written justification (it validates agent submit-results, not the MCP `outputSchema` seam)
  `pmcp-server-toolkit` carries a DEAD optional `jsonschema` dep (zero usages anywhere);
      `make unused-deps` is currently a no-op (`Makefile:204`, "cargo machete not installed - skipping")
  `src/server/wasm_server.rs` is a THIRD dispatcher, not on the `inject_v2_result_envelope`
      chokepoint, emitting no v2 caching hints — correct today because it carries no era awareness
  No server-builder-level override for the three dispatcher-built cacheable results
      (`tools/list`, `prompts/list`, `server/discover`); they always emit the SDK default
  v2 `outputSchema` mismatch stays warn-only; escalation to a hard error is a NEW production failure
      mode and was deliberately not decided inside a plan (115-RESEARCH § Open Question 1)
  `Cargo.lock` is gitignored, so the jsonschema bump has no reviewable lockfile diff and re-resolves
      on every machine and CI run
  The D-10 cross-import tripwire: discretion exercised (declined at the types layer in 115-05 in
      favour of reciprocal rustdoc; optionally added as a structural assertion in 115-08)
  `src/server/traits.rs` is an ORPHAN — no `mod traits` declaration, never compiled on any target —
      yet it constructs `ListToolsResult`; 115-05 edited it anyway and a green build proves nothing
      about it

Inherited items to re-assert or close:
  `D-114-R` — "published core schema not vendored" — **CLOSED by 115-01**
  `D-114-S` — nothing watches `modelcontextprotocol/ext-tasks` for publication — **still unowned**;
      115-01's generalized provenance test makes a second vendored tree cheap but is NOT a watcher,
      and CONTEXT.md deferred the watcher explicitly
  `D-113-U` — still needs an owner before this branch merges
  `D-114-U` — the +13 `make test-feature-flags` dead-code lints — still unowned, not this phase's
</deviations_and_discoveries_to_record>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whole-phase gate run, measured as deltas against a phase base</name>
  <files>.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-10-SUMMARY.md</files>
  <read_first>
    - Makefile lines 55-70, 120-135, 185-215, 295-315, 355-375, 660-700 — `wasm-build`, `build`,
      `audit`, `unused-deps`, `test-feature-flags`, `test-all`, `validate-always`, `quality-gate`
    - ./CLAUDE.md § "CI Quality Gates" — the PMAT complexity gate pinned to 3.15.0 and the rule that
      it must not be disabled, weakened or removed
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md § Architecture Patterns — the six P1-P6
      refactor techniques, and the hard cap of cog 50 for an annotated allow
    - Every `115-0N-SUMMARY.md` produced so far, for the per-plan test counts to re-verify
  </read_first>
  <action>
Establish a phase base FIRST, then measure deltas. An absolute violation count says nothing about
whether THIS phase caused one.

From the merge-base commit for this phase's work, capture and record: the
`pmat analyze complexity --format json --max-cognitive 25` violation count for `src/`, the
`make test-feature-flags` warning count, and `cargo public-api` output.

Then run on the CURRENT tree and record each result:
  `make quality-gate`
  `make wasm-build`
  `make test-feature-flags`
  `cargo public-api diff` (or `cargo semver-checks check-release`)
  `pmat analyze complexity --format json --max-cognitive 25`

Additionally run every Phase 115 suite BY NAME so a zero-selection cannot hide inside `test-all`:
  `cargo nextest run --features full -E 'binary(vendored_schema_provenance) + binary(v2_core_schema_facts) + binary(v1_lists_golden) + binary(structured_tool_output) + binary(v2_caching_hints) + binary(v2_schema_tripwires) + binary(property_tests)'`
  `cargo nextest run --lib --features full -E 'test(/output_validation::/) + test(/types::caching/) + test(/inject_v2_result_envelope/)'`
Transcribe each Summary line's test count into the SUMMARY. A count BELOW the owning plan's
acceptance criterion is a FAILURE, not a rounding difference — 115-RESEARCH § Pitfall 4 measured that
a mis-written selector runs nothing and exits 0.

Assert as DELTAS: zero NEW pmat complexity violations attributable to Phase 115 files; zero NEW
`test-feature-flags` warnings; `cargo public-api` shows ADDITIONS ONLY — expected additions are
`CacheScope`, `DEFAULT_TTL_MS`, `CallToolResult::structured_value`, the ten `with_ttl_ms` /
`with_cache_scope` builders, the six pairs of result-type fields, and `Hash` on `Era` — with zero
removals and zero signature changes.

Do NOT weaken any gate to make it pass. If `pmat` flags a Phase 115 function, apply one of the six
P1-P6 refactors from `75-RESEARCH.md`; a `// Why:`-annotated `#[allow(clippy::cognitive_complexity)]`
is the last resort and is hard-capped at cog 50. If a gate cannot be made green, STOP and report
rather than adjusting the gate.
  </action>
  <verify>
    <automated>make quality-gate && make wasm-build && make test-feature-flags</automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gate` exits 0
    - `make wasm-build` exits 0
    - `make test-feature-flags` exits 0 with no MORE warnings than the recorded phase base
    - `pmat analyze complexity --format json --max-cognitive 25 | jq '[.violations[] | select(.path | startswith("src/"))] | length'` is not greater than the recorded phase-base count
    - `cargo public-api diff` (or `cargo semver-checks check-release`) reports additions only — zero removals, zero signature changes
    - Every named Phase 115 suite reports a test count at least equal to its owning plan's acceptance criterion, and the counts are transcribed into the SUMMARY
    - `git diff --stat` shows no gate file (`Makefile`, `.github/workflows/ci.yml`, `deny.toml`) modified anywhere in this phase
  </acceptance_criteria>
  <done>The whole workspace is green under the real gate, semver is additive-only, and every Phase 115 suite is proven to have actually run rather than to have selected nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Book the three requirements, sweep stale docs, write the deferred-items ledger</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 115-150 — the SCHM block and the TASK block above it, whose
      block-quote booking form is the house style
    - .planning/ROADMAP.md lines 2512-2524 (the Phase 115 entry) and the milestone progress table
      near line 2578
    - .planning/STATE.md — the frontmatter progress counters, `## Current Position`, and the trailing
      `Stopped at:` / `Next:` lines
    - .planning/phases/114-tasks-extension-migration/deferred-items.md lines 1-60 — the ledger form
      AND its recorded ID-collision incident
    - schema/vendored/core-2026-07-28/PROVENANCE.md — the artifact the booking cites
    - All nine `115-0N-SUMMARY.md` files
  </read_first>
  <action>
**(a) Book the requirements.** Set SCHM-01, SCHM-02 and SCHM-03 to `[x]` in
`.planning/REQUIREMENTS.md`, each followed by an indented block quote carrying:
  - MEASURED evidence by test binary name and count (e.g. `binary(v2_caching_hints)` — N tests over
    six methods, two eras and both dispatchers)
  - the published-evidence citation: `schema/vendored/core-2026-07-28/` at pinned commit
    `271ecc9accafdd9b83a3c869fa67c22953b2af80`, digest-fenced by
    `tests/vendored_schema_provenance.rs`, with the wire facts re-derived by
    `tests/v2_core_schema_facts.rs`
  - an explicit sentence that this is `[x]` and NOT `[~]`: D-15's contingency did not fire,
    `CacheableResult` IS in the published core schema, and booking `[~]` here would be exactly the
    habit D-15 named
  - the deviations, INSIDE the booking rather than in a footnote:
      SCHM-01 — shipped `jsonschema = "0.49"`, not the literal `0.48`, because 0.48.0-0.48.2 carry
                packaging defects and 0.49 is additive-only over 0.48
      SCHM-03 — SIX result types, not five: `DiscoverResult extends CacheableResult` in the pinned
                schema; excluding `server/discover` would have shipped a knowingly non-conformant
                first call for every v2 client
  - for SCHM-01, the judgement this booking makes rather than absorbs: the naive pin was MEASURED to
    be a silent validation bypass, so "Draft 2020-12 explicitly pinned" is satisfied by
    normalize-then-compile, fenced by a draft-07 test whose negative control was observed to fire
    (cite `115-03-SUMMARY.md`)
  - for SCHM-02, that Finding 6 held: there was no object-only guard in pmcp to remove; the
    constraint lived in v1 spec text, and pmcp's pre-existing v1 over-permissiveness is FROZEN by
    D-05 rather than corrected

**(b) Sweep stale docs.** Find and fix every in-repo statement this phase falsified. At minimum
check `src/server/output_validation.rs`'s module doc (115-03 amended it — verify), any rustdoc or
book text asserting `structuredContent` must be an object, README/`pmcp-book` mentions of
schema-draft auto-detection, and `schema/vendored/ext-tasks/PROVENANCE.md`'s statement about what
`schema/` contains. Use:
  `grep -rn 'auto-detect\|autodetect' --include='*.rs' --include='*.md' src/ pmcp-book/src/ README.md`
  `grep -rn 'restricted to.*object' --include='*.rs' --include='*.md' src/ pmcp-book/src/`
Documentation CHAPTERS are Phase 119's (DOCS-05/06) — do NOT write new book chapters here. Fix
statements that are now FALSE and leave the rest.

**(c) Write the ledger.** Create
`.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md` in the
Phase 114 form. Before assigning any ID run `grep -c "^## D-115-"` on the file to find the next free
letter — Phase 114's ledger records an ID-collision incident caused by exactly that omission.

Every item in this plan's `<deviations_and_discoveries_to_record>` block gets an entry, VERIFIED
against the landed SUMMARYs rather than copied. Each entry names an owner or says **unowned**
explicitly — an unowned item is acceptable, an undocumented one is not. Additionally:
  - record `D-114-R` ("published core schema not vendored") as **CLOSED by 115-01**, with a redirect
    note so a reader arriving from Phase 114's ledger finds the closure
  - re-assert `D-114-S` (nothing watches `ext-tasks` upstream) as **still unowned** — 115-01's
    generalized provenance test makes a second vendored tree cheap but is NOT a watcher, and
    CONTEXT.md deferred the watcher explicitly
  - carry `D-113-U` forward as still needing an owner before this branch merges

**(d) Update ROADMAP and STATE.** In `.planning/ROADMAP.md`: tick the ten Phase 115 plan entries,
mark the phase entry complete, and update the milestone progress table row
(`115. JSON Schema 2020-12 + Caching Hints`) to `10/10` with today's date. Add a one-line note under
that phase's Success Criteria recording the five-vs-six deviation, so the roadmap stops asserting
"five" after the code ships six. In `.planning/STATE.md`: update the frontmatter progress counters,
`## Current Position`, and the trailing `Stopped at:` / `Next:` lines, carrying forward the two
standing obligations (D-114-S's watch, D-113-U's owner) that the previous entry already names.
  </action>
  <verify>
    <automated>test "$(grep -c '^- \[x\] \*\*SCHM-0' .planning/REQUIREMENTS.md)" -eq 3 && test -f .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md && grep -q 'core-2026-07-28' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*SCHM-0' .planning/REQUIREMENTS.md` returns exactly 3
    - `grep -c '\[~\]' .planning/REQUIREMENTS.md` shows no `[~]` on any SCHM line
    - `.planning/REQUIREMENTS.md` contains the literal strings `271ecc9accafdd9b83a3c869fa67c22953b2af80`, `0.49` and `DiscoverResult` within the SCHM block
    - `.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md` exists, contains at least 8 `## D-115-` headings, and every heading's body contains either `Owner:` or the literal `**unowned**`
    - The ledger contains `D-114-R`, `D-114-S` and `D-113-U`
    - `grep -n 'D-115-' .planning/phases/115-*/deferred-items.md | awk -F'D-115-' '{print $2}' | cut -c1 | sort | uniq -d` returns nothing — no duplicate IDs
    - `.planning/ROADMAP.md` Phase 115 entry lists ten ticked plans and the progress table row reads `10/10`
    - `.planning/ROADMAP.md` contains a note recording the five-vs-six deviation under Phase 115's Success Criteria
    - `.planning/STATE.md` `## Current Position` names Phase 115 as complete and `Next:` names Phase 116 or 117
    - The stale-doc greps return no remaining FALSE statement (a hit that is still true is fine; record which hits were reviewed and left)
  </acceptance_criteria>
  <done>The three requirements are booked `[x]` with cited evidence and their deviations stated inside the booking; every out-of-scope discovery has an owner or an explicit unowned marker; ROADMAP and STATE reflect reality.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase sign-off</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the three bookings written in Task 2
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/deferred-items.md
    - All ten `115-*-SUMMARY.md` files
  </read_first>
  <what-built>
Phase 115 in full: the 2026-07-28 core schema vendored at a pinned commit with digests and a
generalized provenance tripwire; pre-change v1 golden byte fixtures for five list/read responses;
`jsonschema` at 0.49 with Draft 2020-12 pinned on v2 via normalize-then-compile and an era-keyed
validator cache; `CallToolResult::structured_value` plus non-object `structuredContent` coverage on
both dispatchers; a closed `CacheScope` enum and caching-hint slots on six result types; the v2
projection and v1 strip at the one shared chokepoint; on-the-wire coverage of six methods across two
eras and both dispatchers; SEP-2106 and single-projection tripwires; a fuzz target, property tests
and a runnable example; and a green `make quality-gate`.
  </what-built>
  <action>
Present the sign-off below to the owner and STOP. Do not proceed past this task and do not answer it
yourself. If the checkpoint is returned unanswered, leave it unanswered and report that — Phase 114's
own record shows the correct handling: its sign-off was returned unanswered rather than
self-approved, and was then answered by the owner.

If the owner approves, record in the SUMMARY who approved, when, and any corrections they made, then
re-assert on disk that nothing beyond the sign-off changed as a result: the three `[x]` bookings, the
ledger, ROADMAP and STATE are already written by Task 2, and the approval closes the SIGN-OFF and
nothing else. If the owner rejects any item, record the objection verbatim and do NOT close the phase.
  </action>
  <how-to-verify>
1. Read the three `[x]` bookings in `.planning/REQUIREMENTS.md` lines 143-145 and their block
   quotes. Confirm you accept the two recorded deviations:
   (a) `jsonschema` shipped at **0.49**, not the literal **0.48** in SCHM-01's text
   (b) **six** result types carry caching hints, not the **five** in SCHM-03's text —
       `server/discover` is included because `DiscoverResult extends CacheableResult` in the
       pinned schema
2. Read `deferred-items.md` and confirm you accept every item marked **unowned**, in particular:
   the unpinned `pmcp-agent` `validator_for`, the absent server-builder-level default override,
   the wasm server emitting no v2 caching hints, and v2 `outputSchema` mismatch staying warn-only
   rather than escalating to a hard error.
3. Run `make quality-gate` yourself and confirm it exits 0.
4. Run `cargo run --example s52_v2_caching_hints --features full` and confirm the printed output
   shows `ttlMs` / `cacheScope` on the v2 responses and neither on the v1 response.
5. Confirm you accept the SCHM booking as `[x]` rather than `[~]`. Phase 114 is held on publication;
   Phase 115 is NOT, because its wire values come from the published core schema vendored at
   `schema/vendored/core-2026-07-28/`. If you disagree, say so — the hold is yours to impose, and it
   must not be released OR imposed by an agent.
  </how-to-verify>
  <acceptance_criteria>
    - The checkpoint is answered by the owner. It MUST NOT be self-approved by the executing agent
    - If approved, the SUMMARY records who approved, when, and any corrections they made
    - If any item is rejected, the phase does NOT close: record the objection verbatim and stop
  </acceptance_criteria>
  <verify>
    <automated>test -n "$(grep -n 'Sign-off' .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-10-SUMMARY.md 2>/dev/null)"</automated>
  </verify>
  <done>The owner has answered the sign-off. If approved, the SUMMARY records who, when and any corrections, and the `[x]` bookings stand. If rejected, the objection is recorded verbatim and Phase 115 stays open.</done>
  <resume-signal>Type "approved" to close Phase 115, or describe what must change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → project record | A booking, a ledger entry and a roadmap tick are claims a future reader will trust without re-deriving |
| agent → quality gate | A gate that can be weakened to pass is not a gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-26 | Repudiation | requirement booking | mitigate | Each `[x]` cites a named test binary and count plus the pinned artifact and its commit SHA, so the claim is re-derivable. The two deviations are recorded INSIDE the booking, not in a footnote a reader can miss. |
| T-115-27 | Tampering | quality gate | mitigate | An acceptance criterion asserts no gate file (`Makefile`, `ci.yml`, `deny.toml`) was modified anywhere in the phase, and the plan states that a gate which cannot be made green is a STOP, not an adjustment. pmat and semver are measured as DELTAS against a recorded phase base. |
| T-115-28 | Repudiation | self-approval | mitigate | The sign-off is a `checkpoint:human-verify gate="blocking"` and the acceptance criteria state it must not be self-approved. It explicitly asks the owner to accept or reject the `[x]` booking, because a hold is the owner's to impose. |
| T-115-29 | Repudiation | undocumented discoveries | mitigate | Every out-of-scope item names an owner or is explicitly **unowned**, and a duplicate-ID check runs over the ledger — the exact defect Phase 114's ledger records having suffered. |
| T-115-SC | Tampering | package installs | mitigate | This plan installs NO package and modifies no manifest. `115-RESEARCH.md` § Package Legitimacy Audit records `slopcheck install jsonschema` → `[OK]`, verdict Approved, with zero `[ASSUMED]`/`[SUS]`/`[SLOP]` entries across the phase, so no blocking legitimacy checkpoint applies. `make audit` runs inside `make quality-gate` in Task 1. |
</threat_model>

<verification>
- `make quality-gate`, `make wasm-build`, `make test-feature-flags` — all green
- `cargo public-api diff` — additions only
- Every Phase 115 suite re-run by name with its test count transcribed
- `grep -c '^- \[x\] \*\*SCHM-0' .planning/REQUIREMENTS.md` returns 3
- `deferred-items.md` exists with ≥8 items, no duplicate IDs, every item owned or explicitly unowned
- The sign-off checkpoint is answered by the owner
</verification>

<success_criteria>
- The full quality gate is green with every Phase 115 change in the tree, measured as deltas against a recorded phase base
- SCHM-01/02/03 are booked `[x]` with cited, re-derivable evidence and their deviations stated inside the booking
- ROADMAP and STATE reflect a complete Phase 115, including the five-vs-six note
- Every out-of-scope discovery names an owner or is explicitly unowned, with no duplicate IDs
- The phase is signed off by the owner, not self-approved
</success_criteria>

<output>
Create `.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-10-SUMMARY.md` when done.
Record in it: the phase-base vs current pmat / feature-flag / public-api figures, the per-suite test
counts, the full deferred-items ID list, and the sign-off (who, when, and any corrections made).
</output>
