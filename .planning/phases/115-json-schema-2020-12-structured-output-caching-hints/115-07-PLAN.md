---
phase: 115-json-schema-2020-12-structured-output-caching-hints
plan: 07
type: execute
wave: 5
depends_on: ["115-06"]
files_modified:
  - tests/v2_caching_hints.rs
  - tests/v1_lists_golden.rs
autonomous: true
requirements: [SCHM-03]

must_haves:
  truths:
    - "Over a real socket, all six v2 list/read/discover responses carry ttlMs and cacheScope"
    - "The defaults on the wire are exactly 0 and \"private\""
    - "A handler-set hint reaches the v2 wire unmodified"
    - "The same six methods on v1 carry neither key, and the five list/read v1 responses are still byte-identical to the pre-change capture"
    - "Both dispatchers are covered — Server and ServerCore, not just the one where the logic lives"
  artifacts:
    - path: "tests/v2_caching_hints.rs"
      provides: "On-the-wire SCHM-03 coverage across six methods, two eras and both dispatchers"
      min_lines: 400
      contains: "server_core_"
  key_links:
    - from: "tests/v2_caching_hints.rs"
      to: "tests/common/v2.rs"
      via: "spawn_stateless_config + post + v2_body/v2_headers/v1_body over real loopback HTTP"
      pattern: "common::v2::"
---

<objective>
Prove SCHM-03 on the wire: six methods, two eras, both dispatchers, raw response bytes.

Purpose: 115-06 proved the projection at unit level over a synthetic `JSONRPCResponse`. This plan
proves the whole path — dispatcher builds the result, serde serializes it, the chokepoint projects
it, the HTTP transport writes it — because that is the only level at which "every v2 list/read
response carries both fields" is a checkable claim.

Two failure modes this plan exists to catch, both recorded as having bitten this repo before:
- **Pitfall 6, twin-site drift.** The high-level `Server` has its own dispatch and its own injection
  call at `mod.rs:1705`. A `ServerCore`-only change leaves half the SDK non-conformant, and the
  repo's own comments call this out because it has happened.
- **Pitfall 7, the missing sixth type.** The requirement text and CONTEXT.md both say "five".
  `server/discover` — the v2 replacement for `initialize`, the FIRST thing a v2 client calls — is
  `DiscoverResult extends CacheableResult` in the pinned schema. A suite with exactly five method
  tests is the shape of that defect.

Output: `tests/v2_caching_hints.rs`, plus a v1 leak fixture added to `tests/v1_lists_golden.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-RESEARCH.md
@.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-PATTERNS.md
@tests/common/v2.rs

<interfaces>
From tests/common/v2.rs — the real-HTTP harness:
  pub const V2: &str                                                    (:66, = "2026-07-28")
  pub const V1: &str                                                    (:72)
  pub async fn spawn_stateless_config(server: Server) -> (SocketAddr, JoinHandle<()>)   (:389)
  pub async fn teardown<S: Send>(handle: JoinHandle<()>, sockets: S)    (:528)
  pub fn v2_body(method: &str, id: Value, params: Value) -> String      (:553)
  pub fn v2_body_with_caps(method, id, params, caps) -> String          (:569)
  pub fn v1_body(method: &str, id: Value, params: Value) -> String      (:660)
  pub fn v2_headers(method: &str, name: &str) -> Vec<(String, String)>  (:688)
      — `Mcp-Name` is ALWAYS emitted; pass "" for a name-less method such as `tools/list`
  pub async fn post(addr, extra: &[(String,String)], body: &str) -> Resp (:801)
  pub struct Resp                                                        (:717, status + raw text)

From tests/structured_tool_output.rs — the twin-dispatcher naming rule to copy:
  every behaviour gets BOTH a `server_*` and a `server_core_*` test (:136-220)

From tests/common/duplex.rs:
  call_via_server / call_via_core — the in-process ServerCore path, for the dispatcher-parity half
</interfaces>

<method_matrix>
The six cacheable methods, and how each is reached:

| Method                      | Result type                  | v2 reachable via              | Handler-settable? |
|-----------------------------|------------------------------|-------------------------------|-------------------|
| `tools/list`                | ListToolsResult              | v2_body + v2_headers("tools/list", "")   | no — dispatcher-built |
| `prompts/list`              | ListPromptsResult            | v2_body + v2_headers          | no — dispatcher-built |
| `resources/list`            | ListResourcesResult          | v2_body + v2_headers          | YES via ResourceHandler |
| `resources/templates/list`  | ListResourceTemplatesResult  | v2_body + v2_headers          | YES via ResourceHandler |
| `resources/read`            | ReadResourceResult           | v2_body + v2_headers, params.uri | YES via ResourceHandler |
| `server/discover`           | ServerDiscoverResult         | v2_body + v2_headers("server/discover", "") | no — dispatcher-built |

`server/discover` rides the crate-private internal-request route (`Server::handle_discover` →
`core::build_discover_response`), NOT the `ClientRequest` dispatch, so its hints come from the
`Cacheable::Yes` named at `core.rs:1794` rather than from `request_is_cacheable`.
</method_matrix>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove all six v2 methods carry the hints, with the safe defaults</name>
  <files>tests/v2_caching_hints.rs</files>
  <read_first>
    - tests/common/v2.rs lines 60-80, 380-400, 520-540, 545-600, 685-700, 715-730, 795-830 —
      the constants, spawn, teardown, body builders, header builder and `Resp`
    - tests/structured_tool_output.rs lines 130-220 — the `server_*` / `server_core_*` twin pairing
    - src/server/core.rs — `request_is_cacheable`, `Cacheable`, `project_result_caching_hints`
      and the four production call sites as written by 115-06
    - src/types/caching.rs — `CacheScope`, `DEFAULT_TTL_MS`
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-RESEARCH.md
      § Pitfall 4 (the nextest selector trap), § Pitfall 6, § Pitfall 7
  </read_first>
  <action>
Create `tests/v2_caching_hints.rs` with the same feature gate `tests/v1_tasks_golden.rs` uses:
`#![cfg(all(feature = "streamable-http", feature = "http-client", not(target_arch = "wasm32")))]`,
`mod common;` and the harness imports.

**Name every test function with the file stem prefix `v2_caching_hints_`** so both
`binary(v2_caching_hints)` and `test(/v2_caching_hints/)` select it. 115-RESEARCH § Pitfall 4
measured that `-E 'test(/name/)'` against a file whose test names lack the stem selects ZERO tests
and exits 0 — a plan can be "verified" having run nothing, and that exact form appears in
`114-16-PLAN.md`.

Build one v2-opted-in fixture server carrying: two tools, two prompts, and a `ResourceHandler`
returning a fixed two-entry `ListResourcesResult`, a fixed one-entry `ListResourceTemplatesResult`
and a fixed `ReadResourceResult` for a known URI — none of them setting any caching hint. Opt the
server into v2 through the same accept-list mechanism the other v2 suites use (read
`tests/common/v2.rs`'s spawn helpers and the `supported_protocol_versions` opt-in; do not invent a
second opt-in path).

Write six tests, one per method in the `<method_matrix>`, each: spawn → `post` with `v2_headers` and
a `v2_body` → shutdown → assert status 200 → parse the response and assert on `result`:
  - `ttlMs` is present and equals `0`
  - `cacheScope` is present and equals `"private"`
  - assert on the RAW response text too that both key spellings appear exactly as `"ttlMs"` and
    `"cacheScope"` (camelCase), because a struct-level `rename_all` regression is invisible to a
    parsed-value assertion
Failure messages must name D-07 (both fields are required on the v2 projection) and D-08 (the
default is the safe, inert one).

Add `v2_caching_hints_non_cacheable_methods_gain_neither_key`: call `tools/call` on v2 and assert
neither key appears in the raw response. This is the fail-closed direction for
`request_is_cacheable`.

Add `v2_caching_hints_discover_is_the_sixth_cacheable_result` as a dedicated, separately-named test
(rather than folding `server/discover` into the loop) whose rustdoc states the deviation explicitly:
the requirement text says "five", the pinned schema has six, `tests/v2_core_schema_facts.rs` asserts
the set, and excluding it would ship a knowingly non-conformant v2 `server/discover`. A future reader
who wonders why the count disagrees with the requirement finds the answer at the test.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'binary(v2_caching_hints)'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --features full -E 'binary(v2_caching_hints)'` exits 0 and its Summary line reports at least 8 tests run (zero-selection is a FAILURE)
    - Every test function name in `tests/v2_caching_hints.rs` begins with `v2_caching_hints_`
    - The file contains all six method strings: `tools/list`, `prompts/list`, `resources/list`, `resources/templates/list`, `resources/read`, `server/discover`
    - The file contains the raw-text assertions `"ttlMs"` and `"cacheScope"` (with the quotes, i.e. the wire spelling)
    - `tests/v2_caching_hints.rs` contains a test named `v2_caching_hints_discover_is_the_sixth_cacheable_result`
    - Negative control: temporarily change `request_is_cacheable` to return `Cacheable::No` for `ListTools`; the `tools/list` test MUST fail; revert
  </acceptance_criteria>
  <done>All six v2 responses are proven on the wire to carry both hints with the safe defaults, and a non-cacheable method is proven to gain neither.</done>
</task>

<task type="auto">
  <name>Task 2: Prove handler-set values survive on v2 and are stripped on v1, across both dispatchers</name>
  <files>tests/v2_caching_hints.rs, tests/v1_lists_golden.rs</files>
  <read_first>
    - tests/v2_caching_hints.rs — the file created in Task 1
    - src/types/resources.rs — the `with_ttl_ms` / `with_cache_scope` builders added by 115-05, and
      the `ResourceHandler` return types that make them reachable
    - tests/common/duplex.rs — `call_via_core` and `call_via_server`, for the in-process
      `ServerCore` half
    - tests/v1_lists_golden.rs — the leak guard and the `assert_v1_bytes` contract from 115-02
    - .planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-CONTEXT.md § D-11
  </read_first>
  <action>
**(a) Handler-set hints on v2.** Add a second fixture server whose `ResourceHandler` sets real hints
via the 115-05 builders — `.with_ttl_ms(300_000).with_cache_scope(CacheScope::Public)` on
`ListResourcesResult`, and a different pair (say `60_000` + `Private`) on `ReadResourceResult`, so
the two cannot be confused for each other. Add:
  - `v2_caching_hints_handler_set_values_reach_the_wire_unmodified` — `resources/list` on v2 returns
    `ttlMs == 300000` and `cacheScope == "public"`; `resources/read` returns `60000` / `"private"`.
    This is what makes the builders meaningful and proves the projection uses `or_insert` semantics
    rather than overwriting.

**(b) The v1 strip.** Using the SAME handler-set fixture server, add:
  - `v2_caching_hints_v1_strips_handler_set_values` — `resources/list` and `resources/read` over a
    **v1** body carry NEITHER key in the raw response text. The failure message must state that a v1
    response carrying a v2 field breaks D-11 and the milestone's severability story for Phases
    116-119, and that the remedy is to fix the projection, never to relax the assertion.
    This is the single most important test in this plan: it is the only place where the strip half of
    115-06's projection is exercised end to end against a handler that genuinely opted in.

**(c) Twin-dispatcher parity.** The HTTP tests above exercise the high-level `Server`. Add an
in-process `ServerCore` half using `tests/common/duplex.rs`, named with the `server_core_` marker
inside the stem prefix (e.g. `v2_caching_hints_server_core_list_resources_carries_defaults` and
`v2_caching_hints_server_core_v1_strips_handler_set_values`), covering at minimum: defaults on v2,
handler-set survival on v2, and the v1 strip. If `duplex.rs` has no seam for a list/read call, extend
it minimally rather than dropping the `ServerCore` half — Pitfall 6 records that a suite with no
`server_core_` test is the shape of the twin-site defect, and `grep -c 'server_core_'` is an
acceptance criterion for exactly that reason.

**(d) Make the v1 golden leak guard load-bearing.** In `tests/v1_lists_golden.rs`, add ONE fixture
using a handler that sets `ttl_ms` and `cache_scope`, driven over v1, and assert through the existing
`assert_v1_bytes` leak guard that neither key appears. Until now that guard was vacuous (the fields
did not exist); this is the fixture that makes it real. Do NOT modify any existing golden literal in
that file — if one turns red, that is a v1 WIRE BREAK and must be reported, not re-recorded.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'binary(v2_caching_hints) + binary(v1_lists_golden)'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --features full -E 'binary(v2_caching_hints) + binary(v1_lists_golden)'` exits 0; the Summary line reports at least 14 tests run for `v2_caching_hints` and at least 7 for `v1_lists_golden`
    - `grep -c 'server_core_' tests/v2_caching_hints.rs` returns at least 3
    - `tests/v2_caching_hints.rs` contains the literal values `300000` and `60000`, and both `"public"` and `"private"`
    - `tests/v2_caching_hints.rs` contains a test named `v2_caching_hints_v1_strips_handler_set_values`
    - `git diff tests/v1_lists_golden.rs | grep '^-' | grep -c 'r#"{\\"jsonrpc\\":\\"2.0\\"'` returns 0 — no pre-existing golden literal was removed or edited
    - Negative control: temporarily make `project_result_caching_hints` return early on non-v2; `v2_caching_hints_v1_strips_handler_set_values` and the new `v1_lists_golden` leak fixture MUST both fail; revert afterwards and record both observed failures in the SUMMARY
    - `make lint` and `cargo fmt --all -- --check` both exit 0
  </acceptance_criteria>
  <done>Handler-set hints are proven to survive on v2 and to be stripped on v1, on both dispatchers, and the v1 golden leak guard is no longer vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → v2 client / caching intermediary | The response's `cacheScope` is read by a shared gateway deciding whether to reuse it across principals |
| server → v1 client | A v1-negotiated client must receive only 2025-11-25-shaped bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03 | Information Disclosure / Spoofing | on-the-wire `cacheScope` | mitigate | ASVS V4. Six per-method tests assert the emitted default is literally `"private"` on the raw response text, so a change to `"public"` fails six named tests rather than passing a structural check. |
| T-115-04 | Tampering | v1 wire | mitigate | `v2_caching_hints_v1_strips_handler_set_values` drives a handler that genuinely opted in over a v1 body and asserts on RAW bytes; the pre-change golden fixtures from 115-02 are re-run in the same command; a negative control proves both fire. |
| T-115-19 | Tampering | twin-site drift | mitigate | Every behaviour has a `server_core_` twin, enforced by a `grep -c 'server_core_'` acceptance criterion. Pitfall 6 records this defect class as recurring in this repo. |
| T-115-20 | Repudiation | vacuous verification | mitigate | Every selector uses `binary(<stem>)`, every test name carries the file stem, and every acceptance criterion states a minimum test count — Pitfall 4 measured that a `test(/stem/)` selector against a non-prefixed file selects zero and exits 0. |
| T-115-SC | Tampering | package installs | mitigate | This plan installs NO package and touches no manifest. `115-RESEARCH.md` § Package Legitimacy Audit shows zero `[ASSUMED]`/`[SUS]`/`[SLOP]` entries. |
</threat_model>

<verification>
- `cargo nextest run --features full -E 'binary(v2_caching_hints)'` — green, ≥14 tests
- `cargo nextest run --features full -E 'binary(v1_lists_golden)'` — green, ≥7 tests, no golden literal edited
- `cargo nextest run --features full -E 'binary(structured_tool_output)'` — still green
- `make lint` and `cargo fmt --all -- --check` — green
</verification>

<success_criteria>
- All six v2 methods proven on the wire to carry `ttlMs` + `cacheScope` with the values `0` / `"private"`
- A non-cacheable v2 method proven to carry neither
- Handler-set hints proven to reach the v2 wire unmodified
- Handler-set hints proven to be STRIPPED on v1, with the pre-change golden fixtures still green
- Both dispatchers covered, with `server_core_` twins present
- The `server/discover` sixth-type deviation from the requirement's "five" is stated at a named test
</success_criteria>

<output>
Create `.planning/phases/115-json-schema-2020-12-structured-output-caching-hints/115-07-SUMMARY.md` when done.
Record in it: the observed failures from both negative controls, whether `tests/common/duplex.rs`
needed extending for the `ServerCore` half, and confirmation that no pre-existing v1 golden literal
changed.
</output>
