---
phase: 96-shape-b-scaffold-dialect-version-declaration-generalization-validation
plan: 03
type: execute
wave: 2
depends_on: [96-01]
files_modified:
  - crates/pmcp-workbook-compiler/src/fixture_author.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
  - crates/pmcp-workbook-compiler/tests/fixtures/leap1900-probe.xlsx
  - crates/pmcp-workbook-compiler/SPIKE-1900-leap.md
autonomous: true
requirements: [WBEX-01, WBEX-02]
must_haves:
  truths:
    - "A #[cfg(test)] helper authors a .xlsx with genuine Excel identity that passes the trusted-fixture override path"
    - "The authoring recipe produces an .xlsx whose cached cell values serve as the reconcile oracle"
    - "The 1900-leap-year quirk is resolved: expressible as serial arithmetic OR documented as a scalar_eval-only limitation (no date functions added)"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/fixture_author.rs"
      provides: "reusable #[cfg(test)] rust_xlsxwriter fixture author with Excel identity + cached oracle"
      contains: "rust_xlsxwriter"
    - path: "crates/pmcp-workbook-compiler/SPIKE-1900-leap.md"
      provides: "the resolved disposition of the 1900-leap-year quirk (Open Question 1)"
      contains: "Disposition"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/fixture_author.rs"
      to: "crates/pmcp-workbook-compiler/src/reemit_golden.rs"
      via: "produces .xlsx consumable by compile_workbook_with_fixture_override"
      pattern: "compile_workbook_with_fixture_override"
---

<objective>
Resolve the two WBEX critical-path landmines BEFORE authoring the loan/quirk fixtures:

1. The .xlsx authoring gap (Pitfall 1): there is NO in-repo .xlsx generator — `tax-calc.xlsx` is a
   committed binary. Build a reusable `#[cfg(test)]` `rust_xlsxwriter` fixture author that sets
   `DocProperties` for genuine Excel identity (`<Application>Microsoft Excel</Application>` + an
   `<AppVersion>` + non-sentinel calcId -> `ProvenanceClass::ExcelTrusted`) and writes formulas WITH
   cached results (the reconcile oracle), so the loan workbook (Plan 04) and the quirk corpus (Plan 05)
   author their fixtures through one proven helper that rides the trusted-fixture override.
2. The 1900-leap-year Open Question (Pitfall 3 / RESEARCH Open Question 1): no date-serial code exists
   and DATE() is not whitelisted. Spike a tiny probe fixture to determine whether the leap-year serial
   offset is expressible with whitelisted ops over `f64`, and record the disposition so Plan 05 has a
   decided path — WITHOUT adding date functions (deferred-boundary violation).

Purpose: De-risk the critical path so the two WBEX gates (Plans 04, 05) build on a proven authoring
helper and a decided quirk disposition rather than stalling.
Output: A `fixture_author.rs` `#[cfg(test)]` module, a leap-year probe fixture + spike findings doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-CONTEXT.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-RESEARCH.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-PATTERNS.md

<interfaces>
<!-- The trusted-fixture authoring + compile recipe. Sources verified in-repo. -->
- crates/pmcp-workbook-compiler/src/reemit_golden.rs:1-72 — WHY the proof lives in src/ under
  #[cfg(test)] (CR-01: the override is #[cfg(test)]-only, invisible to an external tests/ crate); the
  genuine-Excel-identity rationale; `compile_fixture()` / `place_fixture()` recipe; and:
  `compile_workbook_with_fixture_override(&xlsx, &out_root, bundle_id, version, approver) -> Result<...>`
  (FreshnessPolicy::TrustedFixture; DEMOTES fullCalcOnLoad staleness; CANNOT soften non-Excel-identity).
- crates/pmcp-workbook-compiler/Cargo.toml:81-91 — `rust_xlsxwriter = { version = "0.95",
  default-features = false }` is ALREADY a dev-dep; the `Formula::set_result` cached-oracle note.
- crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.provenance-override.json — the
  `{kind:"trusted-fixture", fixture, reason, authored_by:"rust_xlsxwriter", scope:"test-path-only"}`
  marker shape to clone per fixture.
- crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs:29-42 — `apply()` dispatch (the 13 whitelisted
  fns the probe may use: IF + arithmetic; NO DATE).
- crates/pmcp-workbook-dialect/src/lib.rs:35 — the WHITELIST (DATE/DATEVALUE are NOT in it; do not add).
- crates/pmcp-workbook-compiler/src/lib.rs — declare the new `#[cfg(test)] mod fixture_author;`
  alongside the existing `#[cfg(test)]` test modules (reemit_golden etc.).
- RESEARCH Pattern 4 (the two sanctioned recipes), Pitfall 1 (the authoring landmine), Pitfall 3 +
  Open Question 1 (the 1900-leap disposition + the spike recommendation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reusable #[cfg(test)] rust_xlsxwriter fixture author (Excel identity + cached oracle)</name>
  <files>crates/pmcp-workbook-compiler/src/fixture_author.rs, crates/pmcp-workbook-compiler/src/lib.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/reemit_golden.rs (the #[cfg(test)] rationale at lines 1-23, the
      provenance-identity requirements, and place_fixture/compile_fixture at lines 48-72)
    - crates/pmcp-workbook-compiler/Cargo.toml (the rust_xlsxwriter dev-dep + the Formula::set_result
      cached-oracle note at lines 81-91)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.provenance-override.json (the marker shape)
    - crates/pmcp-workbook-compiler/src/stage1.rs (FreshnessPolicy + ProvenanceClass — what makes a
      fixture pass: genuine Excel identity, not the fabricated/umya one)
    - crates/pmcp-workbook-dialect/src/lib.rs:43-45 (the colour-role palette: blue-font `FF0000FF` input,
      `FFE2EFDA` constant)
    - 96-RESEARCH.md Pattern 4 + Pitfall 1 + "Don't Hand-Roll" (the fixture-author + override rows)
    - 96-PATTERNS.md "Trusted-fixture authoring + override (the .xlsx landmine)"
  </read_first>
  <action>
    Create crates/pmcp-workbook-compiler/src/fixture_author.rs as a `#[cfg(test)]` module exposing a
    reusable author API used by Plans 04 and 05 (e.g. a `WorkbookAuthor` builder or an
    `author_xlsx(path, sheets, defined_names, doc_props)` fn). It MUST: (a) use `rust_xlsxwriter` to write
    cells, formulas (via `Formula::set_result` so the cached `<v>` is the reconcile oracle), defined
    names / named ranges, and the colour-role palette inputs (blue-font `FF0000FF` input, `FFE2EFDA`
    constant per dialect lib.rs:43-45); (b) set `DocProperties` so app.xml carries
    `<Application>Microsoft Excel</Application>` + an `<AppVersion>` + a non-sentinel calcId, classifying
    `ProvenanceClass::ExcelTrusted` (the ONLY sanctioned identity — umya-authored is REFUSED); (c) provide
    a helper to write the paired `*.provenance-override.json` marker (clone the tax-calc marker, retarget
    `fixture`). Add a self-test that authors a trivial 1-formula workbook into a tempdir, compiles it via
    `compile_workbook_with_fixture_override`, and asserts the compile succeeds (proves the recipe passes
    the freshness gate end-to-end). Declare `#[cfg(test)] mod fixture_author;` in lib.rs. Do NOT add date
    functions; do NOT author via umya; do NOT edit any existing fixture.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler fixture_author</automated>
  </verify>
  <acceptance_criteria>
    - crates/pmcp-workbook-compiler/src/fixture_author.rs exists, gated `#[cfg(test)]`, and uses rust_xlsxwriter
    - a self-test authors a workbook and compiles it via compile_workbook_with_fixture_override returning Ok
    - the authored .xlsx carries genuine Excel identity (the compile does NOT fail with `oracle/non-excel-app`)
    - `cargo test -p pmcp-workbook-compiler fixture_author` exits 0
    - no existing fixture under tests/fixtures/ is modified (git diff clean except new files)
  </acceptance_criteria>
  <done>A proven, reusable #[cfg(test)] fixture author that produces Excel-identity .xlsx with cached oracles, consumable by both WBEX gates.</done>
</task>

<task type="auto">
  <name>Task 2: 1900-leap-year quirk spike + disposition doc (Open Question 1)</name>
  <files>crates/pmcp-workbook-compiler/tests/fixtures/leap1900-probe.xlsx, crates/pmcp-workbook-compiler/SPIKE-1900-leap.md</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/fixture_author.rs (the author from Task 1 — used to build the probe)
    - crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs:29-42 (apply() — the whitelisted ops available; NO DATE)
    - crates/pmcp-workbook-runtime/src/scalar_eval.rs (the scalar evaluator — the fallback assertion layer if the quirk is not DAG-expressible)
    - crates/pmcp-workbook-dialect/src/lib.rs:35 (the WHITELIST — confirm DATE/DATEVALUE absent; do NOT add)
    - 96-RESEARCH.md Pitfall 3 + Open Question 1 (the serial-number framing + the spike recommendation:
      if not expressible without date code, document as scalar_eval-only or a known limitation)
    - 96-CONTEXT.md D-09 (the 1900-leap quirk is one of the four named)
  </read_first>
  <action>
    Spike whether the Excel 1900-leap-year quirk (Excel treats 1900 as a leap year, so serial 60 is the
    phantom Feb-29-1900, shifting all post-1900-02-28 serials by 1) is expressible as SERIAL-NUMBER
    arithmetic over bare `f64` using ONLY whitelisted ops (IF + arithmetic) — NOT a date function. Use the
    Task-1 author to build a tiny probe fixture (crates/pmcp-workbook-compiler/tests/fixtures/leap1900-probe.xlsx)
    whose cached values encode the Excel-serial offset, and attempt to compile + reconcile it. Record the
    outcome in crates/pmcp-workbook-compiler/SPIKE-1900-leap.md with one of three DECIDED dispositions for
    Plan 05: (A) DAG-expressible -> a real reconcile fixture (preferred); (B) not DAG-expressible without
    date primitives -> a `scalar_eval`-only numeric assertion documenting the serial offset; (C)
    genuinely not expressible with whitelisted ops -> a documented known-limitation note in the quirk
    corpus. The doc MUST state the chosen disposition explicitly (a "## Disposition" section) so Plan 05
    implements it without re-deciding. Under NO circumstance add DATE/DATEVALUE to the whitelist (would
    break the WBDL-01 doc<->const binding and violate the deferred-functions boundary).
  </action>
  <verify>
    <automated>test -f crates/pmcp-workbook-compiler/SPIKE-1900-leap.md && grep -q "Disposition" crates/pmcp-workbook-compiler/SPIKE-1900-leap.md && cargo test -p pmcp-workbook-compiler</automated>
  </verify>
  <acceptance_criteria>
    - crates/pmcp-workbook-compiler/SPIKE-1900-leap.md exists with a "## Disposition" section naming one of (A) DAG reconcile fixture / (B) scalar_eval-only / (C) documented limitation
    - if disposition (A): the probe fixture compiles+reconciles; if (B)/(C): the doc states why and what Plan 05 should assert instead
    - the WHITELIST in pmcp-workbook-dialect is UNCHANGED (no DATE/DATEVALUE added) — git diff clean on dialect lib.rs
    - `cargo test -p pmcp-workbook-compiler` exits 0
  </acceptance_criteria>
  <done>The 1900-leap quirk has a single decided disposition recorded for Plan 05, with no date functions added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored fixture .xlsx -> compiler freshness/provenance gate | Test-authored fixtures must carry genuine Excel identity to pass; the override is the only sanctioned softener. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07 | Elevation | production bypass of the provenance gate via the test override | mitigate | `compile_workbook_with_fixture_override` is `#[cfg(test)]`-only and unreachable from any default/`--all-features` build (CR-01); fixture_author lives under `#[cfg(test)]`; the existing production-refusal counter-test (reemit_golden.rs) remains the regression guard. |
| T-96-08 | Spoofing | a fabricated (umya) Excel identity passing the freshness gate | mitigate | The author uses rust_xlsxwriter with genuine DocProperties identity (WBCO-07 classifies umya-fabricated identity as a distinct refused class); the self-test fails if identity is wrong. |
| T-96-09 | Tampering | the leap-year quirk smuggling date functions into the whitelist | accept-by-design | Spike is explicitly bounded to whitelisted ops over f64; a disposition that needs date code is recorded as a known limitation, never an added function (deferred boundary; WBDL-01 doc<->const guard would fail anyway). |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages (rust_xlsxwriter/tempfile already vetted dev-deps) — no install task -> no legitimacy checkpoint. |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-compiler fixture_author` green (the recipe compiles through the override).
- SPIKE-1900-leap.md exists with a decided disposition.
- `git diff` shows no edits to existing fixtures or to the dialect WHITELIST.
</verification>

<success_criteria>
- The critical-path .xlsx authoring landmine is retired: a reusable, proven Excel-identity author exists for Plans 04 and 05.
- The 1900-leap-year Open Question is closed with a single decided disposition, with zero date-function scope creep.
</success_criteria>

<output>
Create `.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-03-SUMMARY.md` when done
</output>
