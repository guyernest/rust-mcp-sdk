---
phase: 96-shape-b-scaffold-dialect-version-declaration-generalization-validation
plan: 04
type: execute
wave: 3
depends_on: [96-03]
files_modified:
  - crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx
  - crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.provenance-override.json
  - crates/pmcp-workbook-compiler/src/reemit_loan.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
autonomous: true
requirements: [WBEX-01]
must_haves:
  truths:
    - "A second, non-lighthouse loan workbook compiles end-to-end through the generic compile_workbook driver"
    - "The loan bundle loads via the toolkit and its manifest reflects the loan's OWN inputs/outputs (NOT tax-calc's)"
    - "The loan workbook uses VLOOKUP/INDEX-MATCH/IFERROR/nested-IF/ROUND/CEILING and NO PMT/POWER/exponentiation"
    - "The loan manifest has multiple named outputs with no privileged single headline"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx"
      provides: "the second-workbook generalization fixture (loan/mortgage rate-tier calculator)"
    - path: "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
      provides: "in-crate compile-and-serve proof asserting the loan's OWN manifest"
      contains: "load_bundle"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
      to: "crates/pmcp-server-toolkit::workbook"
      via: "load_bundle(LocalDirSource) asserts cell_map inputs/outputs"
      pattern: "load_bundle"
    - from: "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
      to: "crates/pmcp-workbook-compiler/src/fixture_author.rs"
      via: "authors loan-calc.xlsx via the shared author"
      pattern: "fixture_author"
---

<objective>
Deliver the WBEX-01 generalization gate: a second, non-lighthouse loan/mortgage example workbook
(D-01) that is deliberately divergent from `tax-calc` — a fixed-cell DAG whose divergence comes from
whitelist-legal lookup families (VLOOKUP / INDEX-MATCH against a rate-tier table, IFERROR guards,
nested-IF tiering, ROUND/CEILING to currency; NO PMT/POWER/exponentiation per D-02). It compiles and
serves end-to-end through the generic SDK path with ZERO per-workbook Rust, and its `get_manifest` /
`tools/list` schema reflects ITS OWN inputs with multiple named outputs and no privileged single
output — proving the manifest-driven §5 fix generalized.

Purpose: This is the generalization proof — the gate that demonstrates a second workbook serves its
OWN schema behind the same five generic tool names with no shared Rust.
Output: The loan-calc.xlsx fixture + provenance marker, and an in-crate `#[cfg(test)]`
compile-and-serve proof (`reemit_loan.rs`) asserting the loan's own manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-CONTEXT.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-RESEARCH.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-PATTERNS.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-03-SUMMARY.md

<interfaces>
<!-- The compile-and-serve proof + the loan workbook content rules. Sources verified in-repo. -->
- crates/pmcp-workbook-compiler/src/reemit_golden.rs (FULL — clone the structure: golden_dir/place_fixture/
  compile_fixture at :42-72; the seven-member-present check at :124-142; BUNDLE.lock recompute via
  build_bundle_lock at :144-175; the toolkit-load check `structural_eq_check4_loads_via_toolkit` at
  :178-192; the production-refusal counter-test at :239-252). For loan: assert INTRINSIC structure +
  ITS OWN manifest, NOT equality to a golden (no loan golden exists).
- crates/pmcp-workbook-compiler/src/fixture_author.rs (Plan 03 — the shared author used to produce loan-calc.xlsx)
- crates/pmcp-server-toolkit::workbook — `load_bundle(&LocalDirSource::new(&bundle))` returns a loaded
  bundle whose `stamp.bundle_id`, `cell_map.inputs`, `cell_map.outputs` are assertable (the manifest
  behind the 5 generic tools). Compiler already dev-deps pmcp-server-toolkit with `workbook` feature
  (Cargo.toml:97).
- `compile_workbook_with_fixture_override(&xlsx, &out_root, "loan-calc", "1.0.0", "proof-approver")`
  (reemit_golden.rs:68) — the trusted-fixture compile.
- crates/pmcp-workbook-compiler/src/lib.rs — `promote_named_outputs` consumes `out_*` named ranges
  (lib.rs:264-269 doc) — the loan workbook must use the `out_*` convention for its named outputs.
- crates/pmcp-workbook-runtime/src/sheet_ir/semantics.rs:29-42 — confirms VLOOKUP/INDEX/MATCH/IFERROR/
  ROUND/CEILING/IF are already dispatched (no new runtime code needed).
- crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx — the CONTRAST case (3 inputs / 4 outputs);
  the loan must differ visibly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: author the loan-calc.xlsx fixture (rate-tier DAG, whitelist-legal) + provenance marker</name>
  <files>crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx, crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.provenance-override.json</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/fixture_author.rs (Plan 03 — the author API + provenance-marker helper)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx + tax-calc.provenance-override.json (the
      contrast fixture + the marker shape to clone, retargeting `fixture: "loan-calc.xlsx"`)
    - crates/pmcp-workbook-dialect/src/lib.rs:35 (the 13-fn WHITELIST — the loan workbook MUST stay within
      it; NO PMT/POWER/`^`) and :43-45 (colour-role palette)
    - crates/pmcp-workbook-compiler/src/lib.rs:264-269 (promote_named_outputs / the `out_*` named-output convention)
    - crates/pmcp-workbook-runtime/src/sheet_ir/rounding.rs:30-60 (excel_round/excel_ceiling — the
      currency-rounding source of truth the loan's cached values must match)
    - 96-CONTEXT.md D-01, D-02 + 96-PATTERNS.md "tests/fixtures/loan-calc.xlsx" + RESEARCH Anti-Pattern
      "A privileged single output in the loan manifest"
  </read_first>
  <action>
    Using the Plan-03 fixture author, author crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx:
    a fixed-cell formula DAG modeling a loan/mortgage rate-tier calculation. Inputs (blue-font
    `FF0000FF`, e.g. loan_amount, term_months, credit_score or ltv_band) feed a rate-tier lookup TABLE
    (constant cells `FFE2EFDA`) resolved via VLOOKUP and/or INDEX-MATCH (credit/LTV band -> applied
    rate), guarded by IFERROR, with nested-IF tiering, and ROUND/CEILING to currency. NO PMT, NO POWER,
    NO exponentiation (arbitrary-term `(1+r)^n` is NOT expressible — deferred). Emit MULTIPLE named
    outputs via the `out_*` convention (e.g. `out_applied_rate`, `out_monthly_interest`,
    `out_total_interest`) — NO privileged single headline output (Anti-Pattern). Set each formula's
    cached `<v>` (via the author's Formula::set_result path) to the Excel-correct value computed against
    excel_round/excel_ceiling so the reconcile oracle is right. Add an explicit `pmcp_dialect_version`
    cell (e.g. "1.0") to exercise the WBDL-02 present-path (Plan 01) — keep it compatible so the loan
    compiles. Write the paired loan-calc.provenance-override.json (clone tax-calc's, retarget `fixture`).
    Do NOT edit any existing fixture.
  </action>
  <verify>
    <automated>test -f crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx && test -f crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.provenance-override.json && cargo test -p pmcp-workbook-compiler fixture_author</automated>
  </verify>
  <acceptance_criteria>
    - crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx and loan-calc.provenance-override.json exist
    - the workbook's formulas use only whitelisted functions (no PMT/POWER/`^`) — asserted by a successful lint during compile (no dialect-refuse finding)
    - the workbook declares at least 3 distinct `out_*` named outputs (no single privileged output)
    - the workbook contains a `pmcp_dialect_version` cell with a compatible value
  </acceptance_criteria>
  <done>A whitelist-legal, divergent loan/mortgage fixture authored with genuine Excel identity and a correct reconcile oracle, with multiple named outputs. (Verify asserts the authored fixtures exist and the proven Plan-03 fixture author still passes; the full loan compile-and-serve proof is exercised by Task 2's `reemit_loan` test.)</done>
</task>

<task type="auto">
  <name>Task 2: reemit_loan.rs compile-and-serve proof asserting the loan's OWN manifest</name>
  <files>crates/pmcp-workbook-compiler/src/reemit_loan.rs, crates/pmcp-workbook-compiler/src/lib.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/reemit_golden.rs (FULL — the clone source: compile_fixture recipe
      :62-72, seven-member-present :124-142, BUNDLE.lock recompute :144-175,
      structural_eq_check4_loads_via_toolkit :178-192, production-refusal counter-test :239-252)
    - crates/pmcp-workbook-compiler/tests/fixtures/loan-calc.xlsx (Task 1 — the fixture under proof)
    - crates/pmcp-server-toolkit/src/workbook/mod.rs (load_bundle + LocalDirSource + the loaded bundle's
      cell_map/stamp surface the assertion reads)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx (the contrast — the proof must assert the
      loan's inputs/outputs are NOT tax-calc's 3-in/4-out)
    - crates/pmcp-workbook-compiler/src/lib.rs (where to declare `#[cfg(test)] mod reemit_loan;`)
    - 96-PATTERNS.md "crates/pmcp-workbook-compiler/src/reemit_loan.rs"
  </read_first>
  <action>
    Create crates/pmcp-workbook-compiler/src/reemit_loan.rs as a `#[cfg(test)]` module cloning
    reemit_golden.rs's structure but swapping the oracle to the loan workbook's OWN values and asserting
    INTRINSIC structure rather than equality to a golden (no loan golden exists). Implement: (a) a
    compile_fixture helper that copies loan-calc.xlsx into a TempDir and compiles via
    `compile_workbook_with_fixture_override(&xlsx, &out_root, "loan-calc", "1.0.0", "proof-approver")`;
    (b) the seven-member-present check; (c) BUNDLE.lock recompute via build_bundle_lock; (d) the
    generalization assertion — `load_bundle(&LocalDirSource::new(&bundle))` then assert
    `stamp.bundle_id == "loan-calc"` AND the loaded `cell_map.inputs` / `cell_map.outputs` reflect the
    LOAN's own cells (the `out_*` outputs from Task 1) and are DISTINCT from tax-calc's input/output set
    (THIS difference is the generalization proof — the manifest behind the 5 generic tool names differs);
    (e) the production-refusal counter-test asserting bare `compile_workbook` (Enforce) refuses the same
    bytes. Declare `#[cfg(test)] mod reemit_loan;` in lib.rs. The reconcile (penny path) is exercised by
    the compile succeeding; assert named-output count/dtypes intrinsically.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler reemit_loan</automated>
  </verify>
  <acceptance_criteria>
    - crates/pmcp-workbook-compiler/src/reemit_loan.rs exists, gated `#[cfg(test)]`, declared in lib.rs
    - a test loads the loan bundle via load_bundle(LocalDirSource) and asserts stamp.bundle_id == "loan-calc"
    - a test asserts the loan's manifest inputs/outputs differ from tax-calc's (the generalization proof) and that there are multiple named outputs with no privileged headline
    - a test asserts bare compile_workbook (Enforce) refuses the loan bytes (production-refusal counter-test)
    - `cargo test -p pmcp-workbook-compiler reemit_loan` exits 0
  </acceptance_criteria>
  <done>An in-crate compile-and-serve proof that the loan workbook serves its OWN manifest end-to-end with zero per-workbook Rust — the WBEX-01 generalization gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loan fixture .xlsx -> compiler freshness/provenance gate | The loan fixture is test-authored and rides the trusted-fixture override; production must still refuse it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-10 | Elevation | the loan fixture passing the production freshness gate | mitigate | reemit_loan.rs includes the production-refusal counter-test: bare `compile_workbook` (Enforce) refuses the same bytes; only the `#[cfg(test)]` override accepts it (CR-01). |
| T-96-11 | Tampering | a per-workbook Rust shortcut faking generalization | mitigate | The proof asserts the manifest is produced by the GENERIC driver (no loan-specific Rust); the assertion reads the loaded cell_map, not a hand-built manifest. |
| T-96-12 | Information disclosure | customer/TowelRads material leaking into the fixture | mitigate | The loan fixture is synthetic (rate-tier toy data); SUMMARY confirms zero customer/TowelRads content (hard constraint, 96-CONTEXT canonical_refs). |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages — no install task -> no legitimacy checkpoint. |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-compiler reemit_loan` green.
- SUMMARY confirms the loan fixture is synthetic with zero customer/TowelRads material.
- `git diff` shows no edits to existing fixtures.
</verification>

<success_criteria>
- WBEX-01 satisfied: a second, non-lighthouse workbook compiles and serves end-to-end through the SDK path; its manifest reflects its OWN inputs (distinct from tax-calc), with multiple named outputs and no privileged single output, proving the manifest-driven §5 fix generalized.
</success_criteria>

<output>
Create `.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-04-SUMMARY.md` when done
</output>
