---
phase: 96-shape-b-scaffold-dialect-version-declaration-generalization-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cargo-pmcp/src/commands/new.rs
  - cargo-pmcp/src/templates/workbook_server.rs
  - cargo-pmcp/src/templates/mod.rs
  - cargo-pmcp/src/templates/workbook_bundle/
  - cargo-pmcp/examples/workbook_server_scaffold.rs
autonomous: true
requirements: [WBCL-05]
must_haves:
  truths:
    - "A developer can run `cargo pmcp new --kind workbook-server <name>` and get a runnable crate"
    - "The scaffolded crate's Cargo.toml uses default-features = false + workbook-embedded + http (purity-safe)"
    - "The emitted main.rs matches the workbook_server_http example modulo harness lines (drift-locked)"
    - "The carried embedded bundle bytes match the committed tax-calc@1.1.0 golden"
  artifacts:
    - path: "cargo-pmcp/src/templates/workbook_server.rs"
      provides: "Shape B workbook-server template emitter"
      contains: "workbook-embedded"
    - path: "cargo-pmcp/src/commands/new.rs"
      provides: "workbook-server --kind dispatch arm"
      contains: "workbook-server"
  key_links:
    - from: "cargo-pmcp/src/commands/new.rs"
      to: "cargo-pmcp/src/templates/workbook_server.rs"
      via: "execute_workbook_server -> templates::workbook_server::generate"
      pattern: "templates::workbook_server::generate"
    - from: "cargo-pmcp/src/templates/workbook_server.rs"
      to: "crates/pmcp-server-toolkit/examples/workbook_server_http.rs"
      via: "emitted main.rs drift-locked to the example via golden test"
      pattern: "EmbeddedSource"
---

<objective>
Add the WBCL-05 Shape B scaffold: `cargo pmcp new --kind workbook-server <name>` emits a single
runnable crate (Cargo.toml + `main.rs` over `EmbeddedSource` + sample `pmcp.toml` + the source
`tax-calc.xlsx` + the pre-compiled embedded `tax-calc@1.1.0` bundle) — a thin shell over the Phase-92
served-tool toolkit module, mirroring the existing `--kind sql-server` dispatch + template-module
pattern (D-06, D-07). The scaffold reuses the proven `tax-calc` golden so the ONLY newly-authored
.xlsx this phase is the loan workbook (handled in Plan 04).

Purpose: Ship the Shape B authoring on-ramp so a developer gets `cargo run` -> 5 tools immediately,
then can edit the workbook -> `cargo pmcp workbook compile` -> rerun (full authoring loop).
Output: A new `workbook_server.rs` template module, a `workbook-server` dispatch arm, the carried
bundle/xlsx/pmcp.toml payload, drift-lock + bundle-bytes golden tests, and a runnable example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-CONTEXT.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-RESEARCH.md
@.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-PATTERNS.md
@.planning/phases/95-shape-a-binary-pmcp-workbook-server/95-CONTEXT.md

<interfaces>
<!-- The dispatch + template-module pattern to clone. Sources verified in-repo. -->
- cargo-pmcp/src/commands/new.rs:70-80 — `match kind.as_deref()` dispatch; the `bail!` supported-list
  string MUST be updated to add `workbook-server`.
- cargo-pmcp/src/commands/new.rs:116-140 — `validate_crate_name(name)` (path-traversal guard; rejects
  `/`, `\`, `..`, leading digit, non-`[A-Za-z0-9_-]`). Call FIRST; do NOT reimplement.
- cargo-pmcp/src/commands/new.rs:146-156 — `execute_sql_server` body to mirror for `execute_workbook_server`.
- cargo-pmcp/src/templates/sql_server.rs:33-44 — the `generate(dir, name)` orchestrator shape (one
  private `generate_<file>` per file, raw `fs::write` + `format!` with `{{`/`}}` escaping; NO template engine).
- cargo-pmcp/src/templates/sql_server.rs:285-339 — the `wiring_lines()` normalizer + the
  `emitted_main_matches_example_modulo_setup` drift-lock golden test to clone.
- crates/pmcp-server-toolkit/examples/workbook_server_http.rs:65-78 — the canonical EmbeddedSource
  wiring the emitted main.rs must match. Import SOLELY `pmcp_server_toolkit::workbook::{EmbeddedSource,
  WorkbookBuilderExt}` (D-11; never name pmcp-workbook-runtime). The example's `include_dir!` path is
  `$CARGO_MANIFEST_DIR/...tests/fixtures/tax-calc@1.1.0`; rewrite to the scaffold-local
  `$CARGO_MANIFEST_DIR/bundle/tax-calc@1.1.0`.
- crates/pmcp-workbook-server/Cargo.toml — the Shape A purity posture: `default-features = false,
  features = [...]` with the T-95-06 rationale (code-mode defaults leak pmcp-code-mode/SWC into the
  served tree and trip make purity-check). Scaffold MUST use `features = ["workbook-embedded", "http"]`.
- Committed golden bundle: crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/ (BUNDLE.lock,
  cell_map.json, executable.ir.json, layout.json, manifest.json, evidence/). Source .xlsx:
  crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx.
- pmcp version = 2.9.0 (root Cargo.toml); pmcp-server-toolkit version = 0.1.0.
- The five generic tool names served by every workbook bundle: calculate, explain, get_manifest,
  diff_version, render_workbook (handler.rs:151/228/334/451/540).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: workbook_server.rs template module + carried bundle/xlsx/pmcp.toml payload</name>
  <files>cargo-pmcp/src/templates/workbook_server.rs, cargo-pmcp/src/templates/workbook_bundle/</files>
  <read_first>
    - cargo-pmcp/src/templates/sql_server.rs (FULL — the orchestrator + per-file generate fns + the
      drift-lock test module to clone; note the sql_server Cargo.toml `features = ["code-mode","sqlite","http"]`
      is the ANTI-PATTERN to NOT copy — Pitfall 4)
    - cargo-pmcp/src/templates/mod.rs (the `pub mod sql_server;` index line to mirror)
    - crates/pmcp-server-toolkit/examples/workbook_server_http.rs (the EmbeddedSource wiring the emitted
      main.rs must match; the `--bundle-dir`/LocalDirSource branch is the harness seam to filter in the drift test)
    - crates/pmcp-workbook-server/Cargo.toml (the `default-features = false` purity posture + the T-95-06 rationale comment)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/ (the committed golden bundle bytes to carry)
    - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx (the source .xlsx to carry)
    - 96-PATTERNS.md sections "cargo-pmcp/src/templates/workbook_server.rs" + "Purity-safe served-tree
      dependency posture" + "Emitted-source drift-lock golden test" + Pitfall 5
  </read_first>
  <action>
    Create cargo-pmcp/src/templates/workbook_server.rs mirroring sql_server.rs's `generate(dir, name)`
    orchestrator: one private `generate_<file>` per emitted file, raw `fs::write` + `format!` (`{{`/`}}`
    escaping), NO template engine. Emit the Shape B payload (D-06, DIVERGING from sql_server's
    schema.sql/deploy.toml set): (a) Cargo.toml pinning `pmcp = { version = "2.9.0", features =
    ["streamable-http"] }` and `pmcp-server-toolkit = { version = "0.1.0", default-features = false,
    features = ["workbook-embedded", "http"] }` (MANDATORY default-features=false — Pitfall 4 purity gate;
    NEVER code-mode), plus clap + tokio; (b) src/main.rs = the workbook_server_http.rs EmbeddedSource
    wiring with `include_dir!("$CARGO_MANIFEST_DIR/bundle/tax-calc@1.1.0")`, importing SOLELY from
    `pmcp_server_toolkit::workbook`; (c) a sample pmcp.toml (mapping the tax-calc workbook -> bundle id,
    mirror the Phase 94 pmcp.toml shape); (d) the source workbook/tax-calc.xlsx (D-07 carried, not
    regenerated); (e) the embedded bundle/tax-calc@1.1.0/* (D-07 carried golden bytes). Carry the bundle
    + xlsx bytes by committing them under a cargo-pmcp template asset dir (e.g.
    cargo-pmcp/src/templates/workbook_bundle/) and `include_bytes!`/`include_dir!`-ing them into the
    emitter, OR by copying from the committed source at generate-time — choose the copy approach that lets
    the bundle-bytes golden test (Task 2) assert byte-equality with the committed source. Add the success
    `println!` gated on PMCP_QUIET like sql_server.
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp</automated>
  </verify>
  <acceptance_criteria>
    - cargo-pmcp/src/templates/workbook_server.rs exists with a `pub fn generate(dir: &Path, name: &str) -> Result<()>`
    - the emitted Cargo.toml string contains `default-features = false` and `features = ["workbook-embedded", "http"]` and does NOT contain `code-mode`
    - the emitted main.rs string contains `EmbeddedSource` and `try_with_workbook_bundle` and imports from `pmcp_server_toolkit::workbook`
    - the carried bundle/tax-calc@1.1.0 includes BUNDLE.lock, cell_map.json, executable.ir.json, layout.json, manifest.json
    - `cargo build -p cargo-pmcp` exits 0
  </acceptance_criteria>
  <done>A purity-safe Shape B template module that emits a runnable workbook-server crate carrying the proven tax-calc golden bundle + source .xlsx + sample pmcp.toml.</done>
</task>

<task type="auto">
  <name>Task 2: dispatch arm + drift-lock & bundle-bytes golden tests</name>
  <files>cargo-pmcp/src/commands/new.rs, cargo-pmcp/src/templates/mod.rs, cargo-pmcp/src/templates/workbook_server.rs</files>
  <read_first>
    - cargo-pmcp/src/commands/new.rs (the `match kind.as_deref()` dispatch at new.rs:70-80, the `bail!`
      supported-list, `validate_crate_name` at new.rs:116-140, and `execute_sql_server` at new.rs:146-156 to mirror)
    - cargo-pmcp/src/templates/mod.rs (the module-index to extend)
    - cargo-pmcp/src/templates/sql_server.rs:285-339 (the `wiring_lines()` normalizer +
      `emitted_main_matches_example_modulo_setup` golden test to clone)
    - cargo-pmcp/src/templates/workbook_server.rs (the emitter from Task 1)
    - crates/pmcp-server-toolkit/examples/workbook_server_http.rs (the EXAMPLE_SRC the drift test compares against)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/ (the committed bytes the bundle-bytes test asserts against)
    - 96-PATTERNS.md sections "cargo-pmcp/src/commands/new.rs" + "cargo-pmcp/src/templates/mod.rs" + Pitfall 5
  </read_first>
  <action>
    Add `Some("workbook-server") => return execute_workbook_server(&workspace_dir, &name, global_flags),`
    to the new.rs dispatch and update the `bail!` supported-list string to include `workbook-server`.
    Add `fn execute_workbook_server(...)` mirroring `execute_sql_server`: call `validate_crate_name(name)?`
    FIRST (path-traversal guard, T-86-03-02), then `fs::create_dir_all(workspace_dir.join("src"))`, then
    `templates::workbook_server::generate(workspace_dir, name)?`, then the not-quiet success println.
    Register `pub mod workbook_server;` in cargo-pmcp/src/templates/mod.rs (alphabetical). Add the two
    golden tests inside workbook_server.rs's `#[cfg(test)] mod tests`: (1) the drift-lock test cloning
    sql_server.rs:285-339 — `EXAMPLE_SRC = include_str!("../../../crates/pmcp-server-toolkit/examples/workbook_server_http.rs")`,
    a `wiring_lines()` normalizer that strips comments + the example's `--bundle-dir`/LocalDirSource
    harness seam, and `emitted_main_matches_example_modulo_setup` asserting equality; (2) a
    bundle-bytes-match test asserting the carried `tax-calc@1.1.0` bytes equal the committed
    crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0 source (Pitfall 5 — no drift / no
    boot-integrity break). Also add a generate-emits-expected-files test (mirror any sql_server
    file-presence test) writing into a tempfile::TempDir and asserting Cargo.toml + src/main.rs +
    pmcp.toml + workbook/tax-calc.xlsx + bundle/tax-calc@1.1.0/BUNDLE.lock all exist.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp workbook_server</automated>
  </verify>
  <acceptance_criteria>
    - cargo-pmcp/src/commands/new.rs contains a `Some("workbook-server")` match arm calling `execute_workbook_server`
    - the `bail!` unknown-kind message lists `workbook-server`
    - cargo-pmcp/src/templates/mod.rs contains `pub mod workbook_server;`
    - `cargo test -p cargo-pmcp workbook_server` exits 0
    - the drift-lock test fails if the emitted main.rs diverges from the example wiring (verifiable by manually perturbing the emitter)
    - the bundle-bytes test fails if the carried bundle differs from the committed golden
  </acceptance_criteria>
  <done>`cargo pmcp new --kind workbook-server` dispatches to the emitter, with golden tests locking the emitted main.rs to the example and the carried bundle to the committed golden.</done>
</task>

<task type="auto">
  <name>Task 3: runnable example demonstrating the scaffold (ALWAYS requirement)</name>
  <files>cargo-pmcp/examples/workbook_server_scaffold.rs</files>
  <read_first>
    - cargo-pmcp/src/templates/workbook_server.rs (the `generate` fn the example drives)
    - cargo-pmcp/Cargo.toml ([dev-dependencies] / [[example]] conventions — confirm tempfile is available
      or add it dev-only)
    - Any existing cargo-pmcp example for the conventional shape
    - 96-PATTERNS.md "Fuzz target + property tests" (the EXAMPLE clause)
    - 96-RESEARCH.md Validation Architecture Wave 0 Gaps (the runnable example bullet)
  </read_first>
  <action>
    Add cargo-pmcp/examples/workbook_server_scaffold.rs: a small runnable example that invokes
    `templates::workbook_server::generate` into a temp dir and prints the generated file tree (Cargo.toml,
    src/main.rs, pmcp.toml, workbook/tax-calc.xlsx, bundle/tax-calc@1.1.0/...), demonstrating the WBCL-05
    output a developer would get from `cargo pmcp new --kind workbook-server`. This satisfies the
    CLAUDE.md ALWAYS EXAMPLE requirement. Keep it dependency-light (tempfile dev-dep if needed); do NOT
    boot a server (the smoke is the file-tree + the drift/bundle golden tests in Task 2).
  </action>
  <verify>
    <automated>cargo run -p cargo-pmcp --example workbook_server_scaffold</automated>
  </verify>
  <acceptance_criteria>
    - cargo-pmcp/examples/workbook_server_scaffold.rs exists
    - `cargo run -p cargo-pmcp --example workbook_server_scaffold` exits 0 and prints the generated file tree including bundle/tax-calc@1.1.0
  </acceptance_criteria>
  <done>A runnable example demonstrates the Shape B scaffold output, satisfying the ALWAYS EXAMPLE requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg (crate name) -> filesystem | `cargo pmcp new --kind workbook-server <name>` writes files under a name-derived dir. |
| scaffolded crate -> served binary tree | The emitted Cargo.toml's feature set determines whether the reader/code-mode leak into the served tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04 | Tampering | crate name path traversal (`new --kind workbook-server ../evil`) | mitigate | `validate_crate_name` called FIRST in execute_workbook_server, BEFORE any fs::write — rejects `/`, `\`, `..`, leading digit (reuse, do not reimplement; new.rs:116-140). |
| T-96-05 | Elevation | code-mode/umya/SWC leaking into the served tree via toolkit defaults | mitigate | Emitted Cargo.toml uses `default-features = false, features = ["workbook-embedded","http"]` (T-95-06); a `make purity-check` over the served cone confirms absence; the emitted-Cargo.toml unit test asserts no `code-mode`. |
| T-96-06 | Tampering | carried embedded bundle drifting from the committed golden (boot-integrity break) | mitigate | Bundle-bytes golden test asserts byte-equality with crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0 (Pitfall 5). |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages (clap/tokio/include_dir already vetted; tempfile is a dev-dep) — no install task -> no legitimacy checkpoint. |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp workbook_server` green (dispatch + drift-lock + bundle-bytes + file-presence).
- `cargo run -p cargo-pmcp --example workbook_server_scaffold` green.
- Manual smoke (optional, captured in SUMMARY): `cargo pmcp new --kind workbook-server /tmp/wbtest && (cd /tmp/wbtest && cargo build)` builds; `cargo run` prints `PMCP_WORKBOOK_SERVER_ADDR=` and serves 5 tools.
- `make purity-check` over the scaffolded served cone shows no pmcp-code-mode/umya/swc/quick-xml.
</verification>

<success_criteria>
- WBCL-05 satisfied: `cargo pmcp new --kind workbook-server` scaffolds a runnable crate (Cargo.toml + main.rs over EmbeddedSource + sample pmcp.toml + sample bundle), a thin shell over the toolkit module, mirroring `--kind sql-server`.
- The scaffold is purity-safe (default-features=false, workbook-embedded+http) and drift-locked to the canonical example + committed golden bundle.
</success_criteria>

<output>
Create `.planning/phases/96-shape-b-scaffold-dialect-version-declaration-generalization-validation/96-02-SUMMARY.md` when done
</output>
