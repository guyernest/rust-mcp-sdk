---
phase: 102-http-task-dispatch
plan: 03
type: execute
wave: 3
depends_on: [102-02]
files_modified:
  - tests/tool_as_task_lifecycle_http.rs
  - examples/s46_http_tool_as_task.rs
  - Cargo.toml
autonomous: true
requirements: [HTASK-03, HTASK-04]
must_haves:
  truths:
    - "A server built with Server::builder().tool(t).task_store(store).build() and served via StreamableHttpServer serves initialize → call(task) → tasks/get (poll) → tasks/result over REAL HTTP"
    - "The live round-trip asserts the three-way id match, non-empty typed content, advertised tasks capability, and the -32002 pending error"
    - "A worked example demonstrates a pmcp.run-shaped HTTP task server with NO ServerCore::handle_request shim"
    - "make quality-gate and make doc-check are green"
  artifacts:
    - path: "tests/tool_as_task_lifecycle_http.rs"
      provides: "Live HTTP loopback round-trip integration test (HTASK-03)"
      contains: "StreamableHttpServer"
      min_lines: 80
    - path: "examples/s46_http_tool_as_task.rs"
      provides: "Worked HTTP tool-as-task example over Server + StreamableHttpServer, no ServerCore shim"
      contains: "StreamableHttpServer"
    - path: "Cargo.toml"
      provides: "[[example]] registration for s46_http_tool_as_task"
      contains: "s46_http_tool_as_task"
  key_links:
    - from: "tests/tool_as_task_lifecycle_http.rs"
      to: "Server::builder().task_store(..)"
      via: "high-level Server built with a task_store, served over StreamableHttpServer"
      pattern: "task_store"
    - from: "examples/s46_http_tool_as_task.rs"
      to: "StreamableHttpServer"
      via: "high-level Server served over HTTP, no ServerCore::handle_request"
      pattern: "StreamableHttpServer"
---

<objective>
Prove the phase end-to-end with the acceptance gate: a LIVE HTTP loopback
round-trip (HTASK-03) driving a store-backed high-level `Server` over
`StreamableHttpServer` + `StreamableHttpTransport`, plus the worked
`s46_http_tool_as_task` example and the doctest/coverage that close HTASK-04.

Purpose: Phase 101 could only test tasks via an in-process duplex shim because
the HTTP path rejected `tasks/*`. With Plan 02 done, this plan replaces that shim
with a real HTTP round-trip — the carried-from-Phase-101 rule that protocol-shape
requirements are "resolved" ONLY via a live round-trip. The example doubles as the
proof that pmcp.run can drop its `ServerCore::handle_request` shim.

Output: `tests/tool_as_task_lifecycle_http.rs`, `examples/s46_http_tool_as_task.rs`,
the `Cargo.toml` `[[example]]` block, and final `make quality-gate` + `make doc-check`
green (PMAT cog-25 CI gate green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-http-task-dispatch/102-RESEARCH.md
@.planning/phases/102-http-task-dispatch/102-PATTERNS.md
@.planning/phases/102-http-task-dispatch/102-02-SUMMARY.md

<interfaces>
<!-- HTTP loopback harness analog (verified): tests/workflow_prompt_e2e_test.rs:6-92 -->
    #![cfg(all(feature = "streamable-http", not(target_arch = "wasm32")))]
    use pmcp::server::streamable_http_server::StreamableHttpServer;
    use pmcp::shared::streamable_http::{StreamableHttpTransport, StreamableHttpTransportConfig};
    use pmcp::{Client, ClientCapabilities, Result, Server};
    // build server via Server::builder() ... ; Arc<Mutex<Server>>;
    // addr "127.0.0.1:18765" (Pitfall 5: prefer :0 + read-back, or distinct high port under --test-threads=1)
    // StreamableHttpServer::new(addr, server); tokio::spawn(http_server.start());
    // sleep(500ms); StreamableHttpTransportConfig { enable_json_response: true, .. };
    // Client::new(transport); client.initialize(ClientCapabilities::default()).await?;

<!-- Invariants to mirror over HTTP: tests/tool_as_task_lifecycle.rs -->
    completing_task_tool() :135-156 (returns taskId+status+nested result.content;
        .with_execution(ToolExecution::new().with_task_support(TaskSupport::Required)))
    pending_task_tool() :161-179 (no result content → tasks/result is -32002 before completion)
    init.capabilities.tasks.is_some() :220 (advertised tasks)
    client.call_tool_with_task(name, args) → wire id != "tool-fabricated" :226-235 (store-minted)
    client.tasks_get(&id) poll until status.is_terminal(); polled id == client id :240-255
    client.tasks_result(&id) → !result.content.is_empty() :259-265
    _meta.relatedTask.taskId == wire task.taskId (three-way) :269-298
    pending tool → tasks_result returns -32002 :303-327

<!-- Example analog: examples/s45_tool_as_task_lifecycle.rs (in-process DuplexTransport :52-123 → replace with HTTP) -->
    task-tool body :127-153; 4 lifecycle steps (initialize/assert tasks → call_tool_with_task
    → poll tasks_get → tasks_result) :165-225.

<!-- Cargo example block analog: Cargo.toml:541-544 (s45) -->
    [[example]]
    name = "s45_tool_as_task_lifecycle"
    path = "examples/s45_tool_as_task_lifecycle.rs"
    required-features = ["full"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live HTTP loopback round-trip integration test (HTASK-03)</name>
  <files>tests/tool_as_task_lifecycle_http.rs</files>
  <read_first>
    - tests/workflow_prompt_e2e_test.rs:1-95 (HTTP loopback harness — cfg gate, StreamableHttpServer + StreamableHttpTransport setup, the 18765 port + 500ms sleep)
    - tests/tool_as_task_lifecycle.rs:128-327 (completing_task_tool/pending_task_tool builders + the full set of invariants/assertions to mirror, incl. the three-way id assert and -32002)
    - .planning/phases/102-http-task-dispatch/102-PATTERNS.md (`tests/tool_as_task_lifecycle_http.rs` section — two composed analogs)
    - .planning/phases/102-http-task-dispatch/102-RESEARCH.md (Pitfall 5 — port choice; Open Question 3 — new file vs extend; HTASK-03 row in Test Map)
  </read_first>
  <action>
    Create `tests/tool_as_task_lifecycle_http.rs` (NEW file, keeping the Phase 101 in-process
    test untouched per HTASK-04 no-regression — research Open Question 3). Header:
    `#![cfg(all(feature = "streamable-http", not(target_arch = "wasm32")))]`.

    Build the server with the HIGH-LEVEL path (the capability this phase adds):
    `Server::builder().name("s46").version("1.0.0").tool("complete_now", completing_task_tool()).tool("stay_pending", pending_task_tool()).task_store(Arc::new(InMemoryTaskStore::new())).build()?`.
    Reuse the `completing_task_tool`/`pending_task_tool` builder shapes from
    tests/tool_as_task_lifecycle.rs:135-179 (TypedTool/ToolHandler returning taskId+status+nested
    result.content, `.with_execution(ToolExecution::new().with_task_support(TaskSupport::Required))`).

    Stand it up over real HTTP using the workflow_prompt_e2e_test harness: `Arc<Mutex<Server>>`,
    `StreamableHttpServer::new(addr, server)`, `tokio::spawn(http.start())`, then a
    `StreamableHttpTransport` client with `enable_json_response: true`. Per Pitfall 5: PREFER binding
    `127.0.0.1:0` and reading back the assigned port; if that is not exposed by the harness, use a
    distinct high port (NOT 18765) and rely on CI's `--test-threads=1`. Document the choice in a comment.

    Drive and assert the SAME invariants as Phase 101, but over HTTP:
    - `client.initialize(ClientCapabilities::default())` → assert `init.capabilities.tasks.is_some()`
      (the advertised-tasks invariant; this is what makes the client's assert_capability pass).
    - `client.call_tool_with_task("complete_now", {})` → assert the wire task id != "tool-fabricated"
      (store-minted).
    - poll `client.tasks_get(&id)` until `status.is_terminal()`; assert polled id == client id.
    - `client.tasks_result(&id)` → assert `!result.content.is_empty()` (non-empty typed content).
    - three-way id: assert `CreateTaskResult.task.taskId == tasks/get id == _meta.relatedTask.taskId`
      (mirror tests/tool_as_task_lifecycle.rs:269-298 against the raw envelope where needed).
    - pending: drive "stay_pending" and assert `tasks/result` returns the specified `-32002` before completion.

    Name the test fn so `cargo test --features full tool_as_task_lifecycle_http` selects it.
  </action>
  <verify>
    <automated>cargo test --features full tool_as_task_lifecycle_http</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tool_as_task_lifecycle_http.rs` exists and contains `StreamableHttpServer` and `task_store`
    - The test builds the server via `Server::builder()....task_store(...)` (NOT ServerCore) — `grep -n "ServerCore" tests/tool_as_task_lifecycle_http.rs` returns nothing
    - The test does NOT hardcode port 18765 (`grep -n "18765" tests/tool_as_task_lifecycle_http.rs` returns nothing)
    - `cargo test --features full tool_as_task_lifecycle_http` exits 0 (three-way id, non-empty content, advertised tasks, -32002 pending all asserted)
    - `cargo test --features full tool_as_task_lifecycle` still exits 0 (Phase 101 in-process test untouched and green)
  </acceptance_criteria>
  <done>A live HTTP loopback round-trip drives the full lifecycle through a high-level store-backed Server and asserts all Phase 101 invariants over real HTTP; the in-process Phase 101 test is untouched and green.</done>
</task>

<task type="auto">
  <name>Task 2: Worked example s46_http_tool_as_task + Cargo.toml registration (HTASK-04)</name>
  <files>examples/s46_http_tool_as_task.rs, Cargo.toml</files>
  <read_first>
    - examples/s45_tool_as_task_lifecycle.rs:1-228 (the example to mirror — header conventions :1-47, task-tool body :127-153, in-process DuplexTransport :52-123 to REPLACE with HTTP, 4 lifecycle steps :165-225)
    - tests/workflow_prompt_e2e_test.rs:6-92 (HTTP harness to compose into the example)
    - Cargo.toml:541-544 (s45 [[example]] block to mirror as s46)
    - .planning/phases/102-http-task-dispatch/102-PATTERNS.md (`examples/s46_http_tool_as_task.rs` + `Cargo.toml` sections)
  </read_first>
  <action>
    Create `examples/s46_http_tool_as_task.rs`. Compose two analogs: the s45 lifecycle
    (task-tool body + the 4 numbered round-trip steps) over the real HTTP transport from
    the workflow_prompt_e2e_test harness, REPLACING s45's in-process `DuplexTransport`
    (s45:52-123) with `StreamableHttpServer` + `StreamableHttpTransport`.

    Header: doc comment "Server example: HTTP tool-as-task lifecycle (Phase 102)" with the run
    line `//! Run with: \`cargo run --example s46_http_tool_as_task --features full\``, and
    `#![cfg(not(target_arch = "wasm32"))]`. In the doc comment, EMPHASIZE: this is a pmcp.run-shaped
    server that serves tasks/* through the HIGH-LEVEL `Server` + `StreamableHttpServer` with NO
    `ServerCore::handle_request` shim.

    Reuse the s45 task-tool body verbatim (s45:127-153): a TypedTool returning
    `{ taskId, status:"completed", ttl, createdAt, lastUpdatedAt, result:{content:[...]} }` with
    `.with_execution(ToolExecution::new().with_task_support(TaskSupport::Required))`. Build the
    server with `Server::builder().name(..).version(..).tool("summarize", task_tool).task_store(Arc::new(InMemoryTaskStore::new())).build()?`,
    serve over `StreamableHttpServer`, then drive the 4 lifecycle steps from a `StreamableHttpTransport`
    client: initialize (assert tasks advertised) → `call_tool_with_task` → poll `tasks_get` → `tasks_result`.
    Print human-readable progress per step (mirror s45).

    Register in Cargo.toml immediately after the s45 block (Cargo.toml:544):
    `[[example]]` / `name = "s46_http_tool_as_task"` / `path = "examples/s46_http_tool_as_task.rs"` /
    `required-features = ["full"]`.
  </action>
  <verify>
    <automated>cargo run --example s46_http_tool_as_task --features full</automated>
  </verify>
  <acceptance_criteria>
    - `examples/s46_http_tool_as_task.rs` exists and contains `StreamableHttpServer` and `task_store`
    - `grep -n "ServerCore::handle_request" examples/s46_http_tool_as_task.rs` returns nothing (no shim — HTASK-04 / SC-6 structural claim)
    - `Cargo.toml` contains an `[[example]]` block with `name = "s46_http_tool_as_task"`
    - `cargo run --example s46_http_tool_as_task --features full` serves, the loopback client sees `initialize` advertising `tasks`, completes the round-trip, and exits 0
  </acceptance_criteria>
  <done>A runnable worked example stands up a pmcp.run-shaped HTTP task server through the high-level Server + StreamableHttpServer with no ServerCore shim, registered in Cargo.toml.</done>
</task>

<task type="auto">
  <name>Task 3: Doctest coverage + final phase gate (HTASK-04)</name>
  <files>src/server/mod.rs, .planning/phases/102-http-task-dispatch/102-VALIDATION.md</files>
  <read_first>
    - src/server/mod.rs (the ServerBuilder::task_store / with_task_store rustdoc added in Plan 02 Task 1 — confirm the doctest is present and re-pathed to ServerBuilder)
    - src/server/builder.rs:798-828 (the original ServerCoreBuilder task_store doctest — the source the ServerBuilder doctest mirrors)
    - CLAUDE.md (make quality-gate scope, make doc-check, PMAT cog-25 CI gate, ALWAYS coverage)
    - .planning/phases/102-http-task-dispatch/102-VALIDATION.md (Per-Task Verification Map — full coverage roster; Sign-Off checklist)
  </read_first>
  <action>
    Confirm the new `ServerBuilder::task_store` (and `with_task_store`) methods carry a working
    `# Examples` doctest mirroring builder.rs:798-828 but using `Server::builder()` / `ServerBuilder`
    (this should have landed in Plan 02 Task 1; if the doctest is missing or still references
    `ServerCoreBuilder`, add/fix it here so `make doc-check` exercises the new high-level surface).
    The doctest must compile-run: build a `Server` with a `task_store` and show it advertises tasks.

    Run the full ALWAYS-coverage roster and the phase gate:
    - `cargo test --features full` (unit + integration incl. the HTTP round-trip)
    - `cargo test --features full proptest_task_branch_gate` (property gate from Plan 02)
    - `cargo run --example s46_http_tool_as_task --features full` (example)
    - `make doc-check` (doctests incl. the new ServerBuilder task_store doctest)
    - `make quality-gate` (fmt --all, clippy pedantic+nursery -D warnings, build, test, audit)
    - PMAT complexity: `pmat analyze complexity --max-cognitive 25` — confirm ZERO violations in the
      touched files (task_dispatch.rs, mod.rs, core.rs, builder.rs); if any new fn exceeds cog 25,
      split it (per-endpoint or extract-helper) — do not mask with an allow.

    Verify the frozen surfaces one final time: `git diff src/types/tasks.rs` empty (wire shapes),
    and the `Server`/`ServerBuilder` public API change is additive only (new setters + fields; no
    removed/renamed public items). Confirm the wasm boundary holds.

    Set `nyquist_compliant: true` and the Validation Sign-Off boxes in 102-VALIDATION.md if all green.
  </action>
  <verify>
    <automated>make quality-gate && make doc-check</automated>
  </verify>
  <acceptance_criteria>
    - `make doc-check` exits 0 and exercises a `ServerBuilder::task_store` doctest (grep the doctest exists in mod.rs and references `Server::builder` not `ServerCoreBuilder`)
    - `make quality-gate` exits 0 (fmt/clippy pedantic+nursery/build/test/audit)
    - `pmat analyze complexity --format json --max-cognitive 25 | jq '[.violations[] | select(.path | test("server/(task_dispatch|mod|core|builder)"))] | length'` returns 0
    - `git diff src/types/tasks.rs` is empty (wire shapes frozen)
    - `cargo run --example s46_http_tool_as_task --features full` exits 0
  </acceptance_criteria>
  <done>The new high-level task surface has a working doctest; the full ALWAYS-coverage roster runs green; make quality-gate, make doc-check, and the PMAT cog-25 gate all pass; wire shapes + public API additive-only + wasm boundary verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real HTTP client → StreamableHttpServer → Server | The live round-trip exercises the actual untrusted-HTTP boundary the phase opened; the test is also the security assertion that owner-scoping + store-minted id hold over real HTTP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-08 | Tampering/Spoofing | HTTP round-trip three-way id assertion | mitigate | The HTTP test asserts `CreateTaskResult.task.taskId == tasks/get id == _meta.relatedTask.taskId == store-minted id` over real HTTP and that the wire id != "tool-fabricated" — proving the store-mints-id control survives HTTP framing. |
| T-102-09 | Information Disclosure | -32002 pending over HTTP | accept (frozen) | The test asserts the specified `-32002` pending error reaches the HTTP client unchanged — the frozen, intentional pending shape. |
| T-102-10 | Information Disclosure | example/test bind address | mitigate | Loopback `127.0.0.1` only (prefer ephemeral `:0`); no external interface bound. No secrets in the example (InMemoryTaskStore, no env/credentials). |
| T-102-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages installed (research §Package Legitimacy Audit: not applicable). No `cargo add`. |
</threat_model>

<verification>
- `cargo test --features full tool_as_task_lifecycle_http` — HTASK-03 live HTTP round-trip
- `cargo test --features full tool_as_task_lifecycle` — Phase 101 in-process untouched
- `cargo run --example s46_http_tool_as_task --features full` — worked example
- `make doc-check` — ServerBuilder task_store doctest
- `make quality-gate` — full gate
- `pmat analyze complexity --max-cognitive 25` — zero violations
- `git diff src/types/tasks.rs` — empty
</verification>

<success_criteria>
- A live HTTP loopback round-trip drives initialize → call(task) → tasks/get → tasks/result green over a high-level store-backed Server
- The round-trip asserts three-way id, non-empty content, advertised tasks, -32002 pending
- A worked HTTP example demonstrates a pmcp.run-shaped server with NO ServerCore::handle_request shim
- make quality-gate AND make doc-check green; PMAT cog-25 green; wire + public API additive-only; wasm boundary intact
</success_criteria>

<output>
Create `.planning/phases/102-http-task-dispatch/102-03-SUMMARY.md` when done. Note the final
verification results and confirm the pmcp.run shim can be removed.
</output>
