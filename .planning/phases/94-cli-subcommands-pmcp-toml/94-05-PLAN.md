---
phase: 94-cli-subcommands-pmcp-toml
plan: 05
type: execute
wave: 4
depends_on: ["94-01", "94-02", "94-03", "94-04"]
files_modified:
  - cargo-pmcp/tests/workbook_cli_integration.rs
  - cargo-pmcp/examples/workbook_cli_demo.rs
autonomous: true
requirements: [WBCL-01, WBCL-02, WBCL-03, WBCL-04]
must_haves:
  truths:
    - "An end-to-end test drives `workbook lint`, `workbook compile`, and `workbook emit` over a real fixture (CLAUDE.md ALWAYS: integration coverage)"
    - "The purity gate still passes after cargo-pmcp links pmcp-workbook-compiler — umya stays out of every SERVED tree (CLAUDE.md offline-cone/purity gate)"
    - "A runnable example demonstrates the three workbook subcommands end-to-end (CLAUDE.md ALWAYS: cargo run --example)"
    - "compile-all over a multi-entry pmcp.toml builds every declared workbook (WBCL-04/D-05 end-to-end)"
  artifacts:
    - path: "cargo-pmcp/tests/workbook_cli_integration.rs"
      provides: "end-to-end CLI integration tests: lint exit codes, compile seed lane, gate block, --accept flow, emit ungated marker, compile-all"
      contains: "workbook"
      min_lines: 120
    - path: "cargo-pmcp/examples/workbook_cli_demo.rs"
      provides: "runnable demonstration of workbook lint/compile/emit over a fixture"
      contains: "workbook"
      min_lines: 40
  key_links:
    - from: "cargo-pmcp/tests/workbook_cli_integration.rs"
      to: "cargo pmcp workbook {lint,compile,emit}"
      via: "assert_cmd / Command spawning the built cargo-pmcp binary"
      pattern: "workbook"
    - from: "make purity-check"
      to: "PURITY_CRATES (served trees only)"
      via: "confirms cargo-pmcp is NOT in the served set"
      pattern: "purity-check"
---

<objective>
Close the phase with the CLAUDE.md ALWAYS requirements that the per-handler plans cannot cover alone:
(1) an end-to-end CLI integration test driving lint/compile/emit over a real fixture, (2) a confirmation
that the purity gate still passes now that cargo-pmcp links the compiler (umya must stay out of every
SERVED tree), and (3) a runnable `cargo run --example` demonstrating the three subcommands.

Purpose: Prove the thin CLI shell works end-to-end as a unit, and prove the new dependency edge did
NOT breach the reader-free served boundary.
Output: `cargo-pmcp/tests/workbook_cli_integration.rs`, `cargo-pmcp/examples/workbook_cli_demo.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md
@.planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md
@cargo-pmcp/src/commands/workbook/config.rs
@cargo-pmcp/src/commands/workbook/lint.rs
@cargo-pmcp/src/commands/workbook/compile.rs
@cargo-pmcp/src/commands/workbook/emit.rs

<interfaces>
Built binary: `cargo pmcp workbook {compile,lint,emit}` (Plans 02/03/04). The integration test spawns the
built `cargo-pmcp` binary (it is a `cargo pmcp` subcommand-style binary — confirm the invocation form from
existing cargo-pmcp integration tests, e.g. whether they use `assert_cmd::Command::cargo_bin("cargo-pmcp")`
plus a leading `pmcp` arg, or invoke a test helper).

Fixtures: the Phase 93 compiler test suite already has at least one valid `.xlsx` golden fixture (find it
under `crates/pmcp-workbook-compiler/tests/` or `crates/pmcp-workbook-compiler/fixtures/`). REUSE an existing
fixture; do NOT author a new workbook by hand. For a lint-error case, reuse a fixture the Phase 93 dialect
tests already mark as having an Error finding (find it the same way), or construct a `pmcp.toml` pointing at
the valid fixture for the compile/emit paths.

Purity gate: `make purity-check` (Makefile :499) scans only `PURITY_CRATES := pmcp-workbook-runtime
pmcp-workbook-dialect` (Makefile :496) — cargo-pmcp is NOT in that set, so linking the compiler into cargo-pmcp
is by-design safe. This plan CONFIRMS that the gate still passes (it should require no Makefile edit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the purity gate still passes after cargo-pmcp links the compiler</name>
  <files>cargo-pmcp/tests/workbook_cli_integration.rs</files>
  <read_first>
    - Makefile (the `purity-check` target at :499-520 + `PURITY_CRATES`/`PURITY_WRITER_CRATES` at :496-497 —
      confirm cargo-pmcp is NOT in PURITY_CRATES so the gate does not scan it)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md (the offline-cone note in the
      "cargo-pmcp/Cargo.toml (modified)" section — cargo-pmcp is OFFLINE tooling, not served)
    - crates/pmcp-workbook-runtime/deny.toml, crates/pmcp-workbook-dialect/deny.toml (the crate-local bans the
      Layer-2 gate enforces — confirm cargo-pmcp has no such deny.toml because it is not a served crate)
  </read_first>
  <action>
    Run `make purity-check` and confirm it passes (exit 0) with the new `cargo-pmcp → pmcp-workbook-compiler`
    edge in place. The gate scans ONLY the served reader-free crates (`pmcp-workbook-runtime`,
    `pmcp-workbook-dialect`) — cargo-pmcp is offline tooling and intentionally OUT of that set, so no Makefile
    edit is expected. Additionally, ASSERT POSITIVELY that umya entered cargo-pmcp's tree (proving the dependency
    actually linked) AND assert it is ABSENT from the served crates' trees: run
    `cargo tree -p pmcp-workbook-runtime` and `cargo tree -p pmcp-workbook-dialect` and confirm `umya`/`quick-xml`
    do NOT appear, while `cargo tree -p cargo-pmcp` DOES show `pmcp-workbook-compiler` (and transitively umya).
    If the purity gate FAILS, do NOT weaken it — stop and report (a failure would mean cargo-pmcp was somehow
    added to a served set, which is a real boundary breach). Record the gate result in the integration test as a
    documented `#[test]` that shells `make purity-check` and asserts success (env-gated/`#[ignore]` if `make` is
    not guaranteed in CI for this crate's test runner — Claude's discretion; the manual `make purity-check`
    confirmation is the load-bearing check, the test is the durable guard).
  </action>
  <verify>
    <automated>make purity-check && cargo tree -p pmcp-workbook-runtime | grep -qiv umya</automated>
  </verify>
  <acceptance_criteria>
    - `make purity-check` exits 0 with the new dependency edge in place.
    - `cargo tree -p pmcp-workbook-runtime` does NOT contain `umya` or `quick-xml` (served tree stays reader-free).
    - `cargo tree -p pmcp-workbook-dialect` does NOT contain `umya` or `quick-xml`.
    - `cargo tree -p cargo-pmcp -i pmcp-workbook-compiler` shows the edge (the link actually happened, non-vacuous).
    - No Makefile edit was required (PURITY_CRATES unchanged).
  </acceptance_criteria>
  <done>The purity gate passes with cargo-pmcp linking the compiler; umya is present in cargo-pmcp's offline tree and absent from every served crate's tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end CLI integration test driving lint/compile/emit + compile-all</name>
  <files>cargo-pmcp/tests/workbook_cli_integration.rs</files>
  <behavior>
    - `workbook lint <error-fixture>` exits non-zero; `workbook lint <clean-or-warning-fixture>` exits 0 (D-10).
    - `workbook compile <fixture> --workflow <id> --approver alice` (seed lane, first version) writes the
      seven-member bundle into `{id}@{version}/` and exits 0 (WBCL-01, D-12).
    - `workbook compile <fixture> --workflow <id>` WITHOUT `--approver` exits non-zero (clap rejects, D-06).
    - `workbook emit <fixture> --workflow <id>` writes a bundle whose `evidence/` carries `gated: false` and the
      output contains `UNGATED` (WBCL-03, D-08).
    - bare `workbook compile` with a two-entry `pmcp.toml` builds BOTH declared workbooks (compile-all, WBCL-04/D-05).
    - `workbook compile <fixture> --format json` emits valid JSON (the library LintReport/changelog serde types, D-09).
  </behavior>
  <read_first>
    - cargo-pmcp/tests/ (an existing cargo-pmcp integration test — learn the binary-spawn convention: `assert_cmd`,
      `cargo_bin`, the leading `pmcp` arg form, and `tempfile`/tmpdir usage; mirror it exactly so the test runner
      and `--test-threads=1` convention hold)
    - crates/pmcp-workbook-compiler/tests/ AND crates/pmcp-workbook-compiler/fixtures/ (locate an existing valid
      `.xlsx` golden fixture to REUSE; and a fixture the dialect tests treat as having an Error finding for the
      lint-error case — do NOT hand-author a workbook)
    - cargo-pmcp/src/commands/workbook/config.rs (Plan 01 — write a two-entry `pmcp.toml` into a tmpdir for the
      compile-all case using the exact `[[workbook]]` schema the parser expects)
    - cargo-pmcp/src/commands/workbook/{compile,lint,emit}.rs (Plans 02/03/04 — the exact flag names + exit-code
      contract the test asserts)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md (D-05/D-06/D-08/D-09/D-10 — the behaviors to assert)
  </read_first>
  <action>
    Create `cargo-pmcp/tests/workbook_cli_integration.rs`. Using the same binary-spawn + tmpdir convention as the
    existing cargo-pmcp integration tests (likely `assert_cmd` + `tempfile`), write tests covering: (1) lint exit
    codes over an error fixture vs a clean/warning fixture (D-10); (2) the seed-lane compile writing the
    seven-member bundle into a tmp out_dir and exiting 0 (WBCL-01); (3) compile WITHOUT `--approver` failing
    (D-06); (4) emit producing a bundle whose `evidence/` carries `gated: false` and output containing `UNGATED`
    (WBCL-03/D-08) — read the emitted evidence marker file and assert it parses to false; (5) bare `compile` over a
    two-entry `pmcp.toml` written into the tmpdir building BOTH workbooks (WBCL-04/D-05); (6) `--format json`
    emitting parseable JSON (D-09). REUSE Phase 93 fixtures; copy them into the tmpdir as needed. Keep tests
    deterministic and runnable under `--test-threads=1`. Do NOT assert on the gate-BLOCK path here if constructing
    a real over-tolerance second version is impractical with the available fixtures — that path is unit-tested in
    Plan 03; instead add a TODO note pointing to the Plan-03 unit coverage (Claude's discretion: if a Phase-93
    two-version fixture exists, DO assert the block + `--accept` round-trip end-to-end).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test workbook_cli_integration -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test workbook_cli_integration -- --test-threads=1` passes.
    - The test asserts a lint error fixture exits non-zero AND a clean/warning fixture exits 0 (D-10).
    - The test asserts the seed-lane compile writes a `{id}@{version}/` dir with the seven bundle members.
    - The test asserts compile-without-`--approver` fails (D-06).
    - The test reads the emitted `evidence/` marker and asserts `gated: false` + the `UNGATED` banner (D-08).
    - The test asserts bare `compile` over a two-entry pmcp.toml builds both workbooks (WBCL-04/D-05).
    - `cargo clippy -p cargo-pmcp --tests` zero warnings.
  </acceptance_criteria>
  <done>An end-to-end integration test drives lint/compile/emit + compile-all over real Phase-93 fixtures, asserting the WBCL-01/02/03/04 behaviors and the D-06/D-08/D-09/D-10 decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Runnable workbook CLI example (CLAUDE.md ALWAYS: cargo run --example)</name>
  <files>cargo-pmcp/examples/workbook_cli_demo.rs</files>
  <read_first>
    - cargo-pmcp/examples/ (an existing cargo-pmcp example, if any, for the example convention; else a workspace
      example under examples/ for the `01_`/`02_` naming + doc-comment style)
    - cargo-pmcp/src/commands/workbook/{compile,lint,emit}.rs (the exact subcommand flags to demonstrate)
    - crates/pmcp-workbook-compiler/fixtures/ (the fixture the example compiles — REUSE, do not author)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md (the BA-lifecycle lens: author → first build →
      fix → build → emit — the narrative the example should walk)
  </read_first>
  <action>
    Create `cargo-pmcp/examples/workbook_cli_demo.rs` — a runnable example that demonstrates the three workbook
    subcommands end-to-end over a REUSED Phase-93 fixture. It should (a) write a tmp `pmcp.toml` declaring the
    fixture → a bundle_id → a tmp out_dir, (b) invoke the lint path, (c) invoke the seed-lane compile path with an
    `--approver`, (d) invoke the emit path showing the UNGATED banner + the `gated: false` evidence marker, and
    print a short narrative tying each step to the BA lifecycle (CONTEXT specifics). It MAY call the handler
    `execute` functions directly (constructing the `Args` structs) OR shell the built binary — Claude's discretion;
    direct handler calls keep it hermetic. The example must compile and run via
    `cargo run -p cargo-pmcp --example workbook_cli_demo` and exit 0 on the happy path. Keep functions cognitive
    complexity ≤25; no SATD comments.
  </action>
  <verify>
    <automated>cargo run -p cargo-pmcp --example workbook_cli_demo</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p cargo-pmcp --example workbook_cli_demo` builds and exits 0.
    - The example exercises lint, compile (seed lane, with --approver), and emit (showing UNGATED) over a reused
      fixture — no hand-authored workbook.
    - `cargo clippy -p cargo-pmcp --examples` zero warnings.
  </acceptance_criteria>
  <done>A runnable example demonstrates the full workbook CLI surface end-to-end, satisfying the CLAUDE.md ALWAYS cargo-run-example requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new dependency edge → served reader-free trees | linking the compiler into cargo-pmcp must NOT pull umya into any served binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-05-PURITY | Tampering / Elevation | umya leaking into a served tree via the new edge | mitigate | `make purity-check` (unchanged, scans served crates only) plus explicit `cargo tree` negative assertions on `pmcp-workbook-runtime`/`pmcp-workbook-dialect` confirm umya/quick-xml are ABSENT from every served tree, while present (by design) in cargo-pmcp's offline tree. Failing closed: a purity-gate failure stops the phase, never weakened. |
| T-94-05-FIXTURE | Tampering | a hand-authored fixture passing a fabricated-identity freshness gate | accept | The integration test REUSES Phase-93 golden fixtures (already provenance-validated) rather than authoring new workbooks; the umya-stamped fabricated-identity gate (WBCO-07) is the compiler's concern, not re-litigated here. |
| T-94-05-SC | Tampering | npm/pip/cargo installs (e.g. `assert_cmd`/`tempfile` dev-deps) | mitigate | If `assert_cmd`/`tempfile`/`predicates` are not already dev-deps of cargo-pmcp, adding them requires a Package Legitimacy check (crates.io verification) before install. Prefer reusing whatever the existing cargo-pmcp integration tests already depend on (no new dev-dep). |
</threat_model>

<verification>
- `make purity-check` passes; served trees reader-free; cargo-pmcp links the compiler.
- `cargo test -p cargo-pmcp --test workbook_cli_integration -- --test-threads=1` passes.
- `cargo run -p cargo-pmcp --example workbook_cli_demo` exits 0.
- `make quality-gate` passes (fmt/clippy/build/test/audit) before commit.
</verification>

<success_criteria>
- End-to-end coverage of WBCL-01/02/03/04 via a real-fixture integration test.
- Purity boundary confirmed intact after the new dependency edge (CLAUDE.md offline-cone requirement).
- Runnable example satisfies the CLAUDE.md ALWAYS cargo-run-example requirement.
</success_criteria>

<output>
Create `.planning/phases/94-cli-subcommands-pmcp-toml/94-05-SUMMARY.md` when done.
</output>
