---
phase: 94-cli-subcommands-pmcp-toml
plan: 03
type: execute
wave: 3
depends_on: ["94-01", "94-02"]
files_modified:
  - cargo-pmcp/src/commands/workbook/compile.rs
autonomous: true
requirements: [WBCL-01, WBCL-04]
must_haves:
  truths:
    - "`cargo pmcp workbook compile <wb.xlsx> --workflow <id> --approver <X>` runs ingest→lint→synth→compile→reconcile→GATE→write, gate BEFORE any write (WBCL-01)"
    - "`--approver <name>` is MANDATORY on compile; --accept cannot be satisfied without it (D-06)"
    - "`workbook compile <bundle-id>` resolves path/out_dir from pmcp.toml; bare `workbook compile` compiles ALL declared workbooks (D-05/WBCL-04)"
    - "On a gate block the CLI prints GateBlock::render() verbatim (deltas + change class + the exact --accept line) and exits with a DISTINCT gate-block code (D-07/D-10)"
    - "`--accept --approver <X> --effective-date <D>` re-runs the same compile, records a fingerprint-bound ApprovalRecord, re-baselines the corpus, and lets the version through (D-07)"
  artifacts:
    - path: "cargo-pmcp/src/commands/workbook/compile.rs"
      provides: "CompileArgs + compile handler (seed lane vs gated-update lane) + gate rendering + --accept flow + compile-all"
      contains: "pub struct CompileArgs"
      min_lines: 160
  key_links:
    - from: "cargo-pmcp/src/commands/workbook/compile.rs"
      to: "pmcp_workbook_compiler::compile_workbook / gate::gate / gate::accept::accept"
      via: "seed-lane / gated-update-lane / accept verb calls"
      pattern: "compile_workbook|gate::gate|accept::accept|::accept\\("
    - from: "cargo-pmcp/src/commands/workbook/compile.rs"
      to: "GateBlock::render() + accept_command"
      via: "print the library's gate-block text verbatim on a block"
      pattern: "\\.render\\(\\)"
---

<objective>
Implement the `workbook compile` handler (WBCL-01) — the BA's primary verb. It orchestrates the
Phase 93 library: first-version seed lane (`compile_workbook`, no gate, D-12) vs gated-update lane
(`gate::gate` → block-or-`accept::promote`). The gate runs BEFORE any write. It surfaces the
`--accept --approver --effective-date` flow (D-07), resolves bundle-ids/compile-all through
`pmcp.toml` (D-05/WBCL-04), and renders the gate block + findings in human/JSON (D-09) with the
errors-fail / warnings-pass / gate-block-distinct exit codes (D-10).

This plan adds NO compiler logic. Every verb already exists in `pmcp-workbook-compiler`.
Purpose: One command the BA learns for both build and approval; the gate literally prints the next action.
Output: `cargo-pmcp/src/commands/workbook/compile.rs` (replaces the Plan-02 stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md
@.planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md
@cargo-pmcp/src/commands/workbook/config.rs
@cargo-pmcp/src/commands/workbook/lint.rs

<interfaces>
From Plan 01 (`workbook/config.rs`): `PmcpToml::load(&Path) -> Result<Option<PmcpToml>>`,
`PmcpToml::resolve(&str) -> Result<&WorkbookEntry>`, `PmcpToml::all_entries() -> &[WorkbookEntry]`,
`WorkbookEntry { path, bundle_id, out_dir }`.
From Plan 02 (`workbook/lint.rs`): `render_lint_report(&LintReport, &str, bool) -> Result<()>`,
`lint_exit_code(&LintReport) -> i32` — REUSE for the lint phase inside compile (do not re-render).

Library verbs (re-exported from `pmcp-workbook-compiler`):
- SEED LANE (first version, NO gate — D-12): `compile_workbook(workbook_path: &Path, out_root: &Path,
  workflow: &str, version: &str, approver: &str) -> Result<BundleLock, CompileError>`. `CompileError`
  variants: `Ingest | Lint | Reconcile | Emit | Gate`.
- GATED UPDATE LANE (prior baseline exists): `gate::gate(case: &ApprovalCase, computed: &BTreeMap<String,f64>,
  candidate_workbook_hash: &str, prev_bundle_hash: &str, change_classes: &[ChangeClass],
  approvals: &[ApprovalRecord]) -> GateDecision`. `GateDecision::{ Pass { fingerprint }, Blocked(Box<GateBlock>) }`.
  On `Pass` → `gate::accept::promote(&EmitLane::GatedUpdate { prior_version }, ...)`. (lib.rs:282-294 shows the
  seed lane internally building `PromoteInputs` + calling `accept::promote(&EmitLane::Seed, ...)` — mirror that
  PromoteInputs assembly for the gated lane.)
- `GateBlock::render() -> String` (gate/mod.rs:79-106) — print VERBATIM on a block; carries deltas + change
  classes + the copy-pasteable `accept_command`. Do NOT reformat the deltas.
- `--accept` flow: `gate::accept::accept(case, out_root, bundle_id, computed, candidate_workbook_hash,
  prev_bundle_hash, change_classes, approver, effective_date) -> Result<(ApprovalCase, ApprovalRecord), AcceptError>`
  (accept.rs:76-116) — re-baselines + writes the fingerprint-bound ApprovalRecord ATOMICALLY.
- On-disk approval I/O (do NOT re-implement): `gate::governed_artifact::{write_approval, read_approvals,
  approvals_dir, atomic_promote_dir}`.
- Corpus: `gate::corpus::{derive_corpus, replay_candidate, ApprovalRecord, candidate_fingerprint, approval_matches}`.

Per CONTEXT D-discretion: whether ONE code path branches on "prior accepted baseline exists" or the CLI
selects the lane is Claude's call. The fingerprint is computed by the library (`candidate_fingerprint`) and
binds prev_bundle_hash + candidate_workbook_hash + region deltas — an approval can NEVER be inherited by a
different transition (T-94-03-REPLAY).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CompileArgs + the seed/gated lane orchestration with gate-before-write</name>
  <files>cargo-pmcp/src/commands/workbook/compile.rs</files>
  <behavior>
    - `compile <wb.xlsx> --workflow quote --approver alice` with NO prior baseline runs the seed lane
      (`compile_workbook`, no gate, D-12) and writes the seven-member bundle into `{workflow}@{version}/`.
    - `--approver` omitted ⇒ clap rejects the invocation (required arg) — compile CANNOT proceed without it (D-06).
    - A re-compile WITH a prior baseline AND an over-tolerance named-output delta + NO covering approval ⇒
      `gate::gate` returns `Blocked`; the CLI prints `block.render()` verbatim and exits with the gate-block code,
      writing NOTHING (gate before write, WBCL-01).
    - The same blocked transition re-run with `--accept --approver alice --effective-date 2026-06-12` ⇒ records a
      fingerprint-bound ApprovalRecord, re-baselines, promotes the new version, exits 0 (D-07).
    - exit-code mapping is a pure function: ok⇒0, compile/lint error⇒1, gate-block⇒2 (distinct, D-10) — unit-tested.
  </behavior>
  <read_first>
    - crates/pmcp-workbook-compiler/src/lib.rs (the `compile_workbook` signature at :188-203 AND the seed-lane
      internal PromoteInputs assembly + `accept::promote(&EmitLane::Seed, ...)` at :282-294 — the exact shape to
      mirror for the gated-update lane; the full re-export surface lines :82-157)
    - crates/pmcp-workbook-compiler/src/gate/mod.rs (`gate()` :132-179, `GateDecision` :48-60, `GateBlock::render()`
      :79-106, `accept_command()` :110-115)
    - crates/pmcp-workbook-compiler/src/gate/accept.rs (`accept()` :76-116, `promote()` + `EmitLane` :118-130,
      `PromoteInputs`, `AcceptError` :37-48)
    - crates/pmcp-workbook-compiler/src/gate/corpus.rs (`derive_corpus`, `replay_candidate`, `candidate_fingerprint`,
      `approval_matches`, `ApprovalRecord`, `ApprovalCase`)
    - crates/pmcp-workbook-compiler/src/gate/governed_artifact.rs (`write_approval`, `read_approvals`,
      `approvals_dir`, `atomic_promote_dir` — the CLI calls these, never re-implements persistence)
    - cargo-pmcp/src/commands/app.rs (the `build_all` handler skeleton + quiet-gated output at :219-255, :77)
    - cargo-pmcp/src/commands/workbook/lint.rs (Plan 02 — REUSE `render_lint_report` + `lint_exit_code` for the
      internal lint pass; do not re-render findings)
    - cargo-pmcp/src/commands/workbook/config.rs (Plan 01 — `PmcpToml::load/resolve/all_entries` for id-resolution)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md (section "compile.rs" — the seed-lane vs gated
      lane discussion, the CompileArgs flag list, and the GateBlock::render() "print verbatim" rule)
  </read_first>
  <action>
    Replace the Plan-02 `compile.rs` stub. Define `#[derive(Debug, clap::Args)] pub struct CompileArgs`:
    positional `bundle_id_or_path: Option<String>` (D-03/D-05: bare path, bundle-id, or no-arg compile-all);
    `#[arg(long)] pub workflow: Option<String>` (required when a bare PATH is given — a bundle-id supplies its own
    workflow from pmcp.toml); `#[arg(long, required = true)] pub approver: String` (MANDATORY — D-06, no
    git-identity fallback); `#[arg(long)] pub accept: bool` and `#[arg(long)] pub effective_date: Option<String>`
    (D-07; `--accept` REQUIRES `--effective-date` — enforce in handler, bail! if accept && effective_date.is_none());
    `#[arg(long)] pub out: Option<PathBuf>` (out-dir override vs toml-declared, Claude's discretion on precedence);
    `#[arg(long, default_value = "text")] pub format: String` (D-09).

    Implement `pub fn execute(args: CompileArgs, gf: &GlobalFlags) -> Result<()>`. Resolve the target set:
    (a) `Some(path-that-is-a-file)` → single ad-hoc compile (workflow from `--workflow`, bail! if absent);
    (b) `Some(bundle-id)` → `PmcpToml::load(cwd)?` then `resolve(id)?` → (path, out_dir, workflow=bundle_id);
    (c) `None` → load pmcp.toml, iterate `all_entries()` (compile-ALL, D-05) — aggregate per-workbook exit status
    (Claude's discretion: worst-status-wins; print a per-workbook summary). For EACH target call a single
    `compile_one(entry_or_path, &args, gf) -> Result<i32>` returning the exit code.

    Inside `compile_one`: run the lint phase first (ingest + `dialect::linter::lint` + `render_lint_report` reusing
    Plan 02; if `lint_exit_code` says error, return 1 WITHOUT compiling). Select the lane: detect whether a prior
    accepted baseline exists under `out_dir/{bundle_id}` via `read_approvals`/baseline dir presence (Claude's
    discretion on the exact probe). If NONE → SEED LANE: call `compile_workbook(path, out_root, workflow, version,
    &args.approver)` (version comes from the workbook — read it via the library's manifest/version surface, NEVER
    from pmcp.toml or a flag — D-02/D-11), map `CompileError` → anyhow with context, return 0 on success. If a
    prior baseline EXISTS → GATED UPDATE LANE: assemble `PromoteInputs` (mirror lib.rs:282-294) + the corpus
    (`derive_corpus`/`replay_candidate`) and call `gate::gate(...)`. On `GateDecision::Blocked(block)`: if
    `args.accept` is false → print `block.render()` verbatim (text mode) or serialize the `GateBlock` (json mode,
    D-09) and return the gate-block code (2). If `args.accept` is true → call `gate::accept::accept(case, out_root,
    bundle_id, computed, candidate_workbook_hash, prev_bundle_hash, change_classes, &args.approver,
    effective_date)` then `accept::promote(&EmitLane::GatedUpdate { prior_version }, ...)`, print a success line,
    return 0. On `GateDecision::Pass { .. }` → `accept::promote(&EmitLane::GatedUpdate { prior_version }, ...)`,
    return 0.

    Add a pure `pub fn compile_exit_code(...)` (or reuse a small enum→i32 map) so ok⇒0 / error⇒1 / gate-block⇒2 is
    unit-testable (D-10 — the gate-block code is DISTINCT from a compile error so CI can tell "broken sheet" from
    "needs approval"). Map the worst per-workbook code to the process exit for compile-all. Gate decorative output
    on `gf.should_output()` / `PMCP_QUIET`. Every function cognitive complexity ≤25 (factor `compile_one`,
    `run_gated_lane`, `resolve_targets` as separate fns to stay under the cap).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --lib workbook::compile</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --lib workbook::compile` passes including the exit-code mapping test
      (ok⇒0, error⇒1, gate-block⇒2 — D-10).
    - `cargo run -p cargo-pmcp -- workbook compile x.xlsx --workflow q 2>&1 | grep -qi approver` — invoking
      WITHOUT `--approver` is rejected by clap citing the required `--approver` arg (D-06).
    - A unit/integration test asserts `--accept` without `--effective-date` `bail!`s (D-07 pairing).
    - A test asserts the gate-block render output contains the substring `--accept` and `--approver` and
      `--effective-date` (the copy-pasteable line from `accept_command`, D-07) AND that no bundle dir is written on
      a block (gate-before-write, WBCL-01).
    - `grep -c 'compile_workbook\|gate::gate\|::accept(' cargo-pmcp/src/commands/workbook/compile.rs` ≥ 3 (calls the
      library verbs, does not reimplement).
    - `grep -v '^#' cargo-pmcp/src/commands/workbook/compile.rs | grep -c 'version.*=.*pmcp.toml\|version.*flag'`
      returns 0 (version is read from the workbook, never from toml/flag — D-02/D-11).
    - `cargo clippy -p cargo-pmcp` zero warnings on compile.rs.
  </acceptance_criteria>
  <done>`workbook compile` orchestrates seed vs gated lane with gate-before-write, requires --approver, resolves bundle-ids + compile-all from pmcp.toml, prints GateBlock::render() verbatim on a block, surfaces the --accept flow, and maps ok/error/gate-block to distinct exit codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BA CLI invocation → recorded ApprovalRecord | the `--approver`/`--accept` act is an audit-bearing governance event |
| candidate workbook output → promoted served baseline | the gate is the trust boundary between a recompiled sheet and served truth |
| pmcp.toml `out_dir` → bundle write target | the resolved write location (shared boundary with Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03-APPROVER | Spoofing / Repudiation | silent self-approval via `--accept` with no approver | mitigate | `--approver` is `required = true` on `CompileArgs`; `--accept` additionally requires `--effective-date`. There is NO git-identity fallback (D-06). The recorded `ApprovalRecord.approved_by` is the explicit flag value — unit-tested that accept-without-approver cannot construct. |
| T-94-03-REPLAY | Tampering | replaying an approval against a DIFFERENT candidate output | mitigate | The CLI passes the candidate's `candidate_workbook_hash` + `prev_bundle_hash` to the library; `candidate_fingerprint` binds them + the region deltas, and `approval_matches` rejects a record whose fingerprint does not reproduce THIS transition (T-93-06-INHERIT). The CLI never hand-builds or relaxes the fingerprint. |
| T-94-03-WRITE | Tampering | a gate block leaking a partial/ungated write | mitigate | On `GateDecision::Blocked` the handler returns BEFORE any `promote`/emit call — gate runs before any write (WBCL-01). Test asserts no bundle dir appears after a block. |
| T-94-03-PATH | Tampering / Elevation | `out_dir`/`--out` resolved write target | mitigate | Write targets resolve through `PmcpToml` (validated in Plan 01 to reject `..` escapes); a `--out` override is resolved under the project root with the same containment rule. |
| T-94-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (the only dep edge was added in Plan 01 and is offline cargo-pmcp tooling). N/A — documented, no install task. |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --lib workbook::compile` passes (exit codes, accept pairing, gate-before-write).
- `--approver` mandatory; `--accept` requires `--effective-date`.
- Gate block prints `GateBlock::render()` verbatim with the copy-pasteable accept line.
- `cargo clippy -p cargo-pmcp` zero warnings.
</verification>

<success_criteria>
- WBCL-01 satisfied: ingest→lint→synth→compile→reconcile→GATE→write with gate before any write.
- WBCL-04 satisfied: bundle-id resolution + bare compile-all through pmcp.toml.
- D-06 (mandatory approver), D-07 (--accept flow), D-09 (human/json), D-10 (distinct exit codes) all met.
</success_criteria>

<output>
Create `.planning/phases/94-cli-subcommands-pmcp-toml/94-03-SUMMARY.md` when done.
</output>
