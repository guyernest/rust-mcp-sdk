---
phase: 94-cli-subcommands-pmcp-toml
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - cargo-pmcp/src/commands/workbook/mod.rs
  - cargo-pmcp/src/commands/workbook/lint.rs
  - cargo-pmcp/src/commands/mod.rs
  - cargo-pmcp/src/main.rs
autonomous: true
requirements: [WBCL-02]
must_haves:
  truths:
    - "`cargo pmcp workbook lint <wb.xlsx>` runs the dialect linter standalone (WBCL-02, D-04)"
    - "Workbook verbs surface as a clap subcommand GROUP `workbook compile|lint|emit`, NOT flat names (D-04)"
    - "lint exits non-zero ONLY on Severity::Error; warnings-only exits 0 (D-10)"
    - "`--format json` emits the library's LintReport serde type; default is rich human text (D-09)"
    - "Workbook commands are NOT target-consuming — excluded from is_target_consuming() (no Phase-77 env injection)"
  artifacts:
    - path: "cargo-pmcp/src/commands/workbook/mod.rs"
      provides: "WorkbookCommand subcommand-group enum + execute(&GlobalFlags) dispatch + module declarations"
      contains: "pub enum WorkbookCommand"
      min_lines: 40
    - path: "cargo-pmcp/src/commands/workbook/lint.rs"
      provides: "lint handler + shared render_lint_report(&LintReport, format) helper + exit-code mapping"
      contains: "fn render_lint_report"
      min_lines: 80
  key_links:
    - from: "cargo-pmcp/src/main.rs"
      to: "commands::workbook::WorkbookCommand"
      via: "Commands::Workbook variant + dispatch arm"
      pattern: "Commands::Workbook"
    - from: "cargo-pmcp/src/commands/workbook/lint.rs"
      to: "pmcp_workbook_compiler::dialect::linter::lint"
      via: "lint verb call on the ingested WorkbookCellSource"
      pattern: "dialect::linter::lint|::lint\\("
---

<objective>
Create the `workbook` clap subcommand GROUP, wire it into `main.rs`, and implement the `lint` verb
(WBCL-02). This plan owns `workbook/mod.rs` (declaring all three handler modules so Plans 03/04 can
fill compile.rs/emit.rs without touching mod.rs), the `lint` handler, and the shared
`render_lint_report` helper that `compile.rs` reuses for its internal lint pass.

Purpose: Surface the dialect linter as `cargo pmcp workbook lint <wb.xlsx>` with the human/JSON
dual rendering (D-09) and the errors-fail / warnings-pass exit-code contract (D-10). Establish the
group skeleton (D-04) the whole phase hangs off.
Output: `workbook/mod.rs`, `workbook/lint.rs` (new); `commands/mod.rs`, `main.rs` (modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md
@.planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md
@cargo-pmcp/src/commands/workbook/config.rs

<interfaces>
From Plan 01 (`workbook/config.rs`): `PmcpToml`, `WorkbookEntry`, `load`, `resolve`, `all_entries`.

Library verbs this plan calls (re-exported from `pmcp-workbook-compiler`):
- `pmcp_workbook_compiler::dialect::linter::lint(src: &dyn CellSource, rules: &DialectRules) -> LintReport`
- `DialectRules::default()` — builds the 13-name WHITELIST + palette.
- `WorkbookCellSource` (re-export at lib.rs:125) — adapts the ingested `WorkbookMap` to `CellSource`.
- The ingest entry to turn a `.xlsx` path into a `WorkbookMap` (read `crates/pmcp-workbook-compiler/src/ingest.rs`
  / `lib.rs` for the exact ingest fn the seed lane uses — the linter runs over the ingested map).
- `LintReport { findings: Vec<LintFinding> }`, `LintReport::has_errors()` (Severity::Error only),
  `LintFinding { severity, rule, sheet, cell: Option<String>, message, repair }`, `Severity` — ALL already
  `Serialize`. Re-render in human form; serialize directly in json form (do NOT define a parallel DTO).

Module declarations this plan MUST emit in `workbook/mod.rs` so Plans 03/04 can fill their bodies:
`pub mod config; pub mod compile; pub mod lint; pub mod emit;`
The `compile`/`emit` modules will exist as Plan-03/Plan-04 deliverables; this plan creates `mod.rs` with
the enum variants `Compile(compile::CompileArgs)` / `Emit(emit::EmitArgs)` referencing types those plans
define. To keep Wave 2 compilable, this plan creates MINIMAL compile.rs/emit.rs stubs ONLY IF needed for
`cargo check` — see Task 1 action for the stub policy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the workbook subcommand group (mod.rs), register the module, wire main.rs</name>
  <files>cargo-pmcp/src/commands/workbook/mod.rs, cargo-pmcp/src/commands/mod.rs, cargo-pmcp/src/main.rs</files>
  <read_first>
    - cargo-pmcp/src/commands/configure/mod.rs (the EXACT group template — `ConfigureCommand` enum at :44-56 +
      `impl ConfigureCommand { pub fn execute(self, global_flags: &GlobalFlags) -> Result<()> { match self {...} } }`
      at :58-68; `Add`/`Use`/`List`/`Show` map 1:1 to `Compile`/`Lint`/`Emit`)
    - cargo-pmcp/src/commands/mod.rs (the `pub mod ...;` list at :1-19 where `pub mod workbook;` is added; GlobalFlags
      + should_output at :30-52)
    - cargo-pmcp/src/main.rs (the `enum Commands` `Configure` variant at :155-158 to mirror for `Workbook`; the
      dispatch arm in `dispatch_trait_based` at :531; `is_target_consuming()` at :360-368 where `Workbook` must be
      LEFT OUT; the `after_long_help` example block style)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md (sections "mod.rs (subcommand-group)" and
      "main.rs (modified)" — the four in-place edits, verbatim)
  </read_first>
  <action>
    Create `cargo-pmcp/src/commands/workbook/mod.rs`. Declare `pub mod config; pub mod compile; pub mod lint;
    pub mod emit;`. Define `#[derive(Debug, Subcommand)] pub enum WorkbookCommand` with three variants:
    `Compile(compile::CompileArgs)`, `Lint(lint::LintArgs)`, `Emit(emit::EmitArgs)` (the `Args`-struct-per-handler
    newtype style, mirroring `Add(add::AddArgs)`). Add `impl WorkbookCommand { pub fn execute(self, global_flags:
    &GlobalFlags) -> Result<()> }` dispatching each variant to its handler `execute(args, global_flags)`. Import
    `use super::GlobalFlags;`. Add doc comments on the enum + variants (cargo-pmcp doc-comment convention).

    To keep Wave 2 compilable BEFORE Plans 03/04 land compile.rs/emit.rs: create MINIMAL `compile.rs` and
    `emit.rs` stub files containing ONLY `#[derive(Debug, clap::Args)] pub struct CompileArgs;` /
    `EmitArgs;` and `pub fn execute(_args: CompileArgs, _gf: &GlobalFlags) -> anyhow::Result<()> { anyhow::bail!("workbook compile not yet implemented") }`
    (likewise emit). Plans 03/04 REPLACE these stub bodies. Mark each stub with a `// STUB: filled by Plan 94-03 (compile) / 94-04 (emit)`
    comment so the executor of those plans knows the file is theirs to own. (This is the only Wave-2 cross-file
    concession; the real handler logic is owned by Plans 03/04.)

    Edit `cargo-pmcp/src/commands/mod.rs`: add `pub mod workbook;` to the existing `pub mod ...;` list.

    Edit `cargo-pmcp/src/main.rs` (four in-place edits): (1) add the `Workbook { #[command(subcommand)] command:
    commands::workbook::WorkbookCommand }` variant to `enum Commands` with an `after_long_help` examples block
    listing `workbook compile/lint/emit` usage; (2) add `Commands::Workbook { command } => command.execute(global_flags),`
    to the `dispatch_trait_based` match; (3) do NOT add `Workbook` to `is_target_consuming()` — leave it falling
    through to the default `false` (CONTEXT: workbook commands are not target-consuming, no Phase-77 env injection);
    (4) confirm no other touch point in main.rs is needed.
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp && cargo run -p cargo-pmcp -- pmcp workbook --help 2>&1 | grep -qi compile</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cargo-pmcp` succeeds with the stubs in place.
    - `cargo run -p cargo-pmcp -- pmcp workbook --help` lists `compile`, `lint`, and `emit` subcommands (D-04 group).
    - `grep -c 'Workbook' cargo-pmcp/src/main.rs` shows the variant AND the dispatch arm (≥2 matches).
    - `Workbook` does NOT appear inside the `is_target_consuming()` `matches!(...)` arm (assert by reading :360-368).
    - `cargo run -p cargo-pmcp -- pmcp workbook compile x.xlsx --help` does not panic (stub args parse).
  </acceptance_criteria>
  <done>The `workbook` group exists, is dispatched from main.rs, is excluded from is_target_consuming(), and `workbook --help` shows the three verbs. compile/emit are bail!-ing stubs owned by Plans 03/04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the lint handler + shared render_lint_report helper + exit-code mapping</name>
  <files>cargo-pmcp/src/commands/workbook/lint.rs</files>
  <behavior>
    - `cargo pmcp workbook lint <fixture.xlsx>` on a fixture WITH a Severity::Error finding exits non-zero (D-10).
    - The same command on a fixture with ONLY Warning/Info findings exits 0, but still PRINTS the warnings (D-10).
    - `--format json` emits valid JSON that deserializes back to a `LintReport` (keys: `findings` each with
      `severity`/`rule`/`sheet`/`cell`/`message`/`repair`).
    - default (text) renders located collect-all findings (sheet + cell + message + repair), the BA surface (D-09).
    - exit-code mapping is a pure function over `LintReport::has_errors()` → testable in isolation (property:
      has_errors=true ⇒ nonzero, false ⇒ 0).
  </behavior>
  <read_first>
    - cargo-pmcp/src/commands/configure/list.rs (the `--format text|json` dispatch at :56-68, `#[arg(long,
      default_value = "text")] pub format: String` at :17-22, `serde_json::to_string_pretty` json path at :124-150,
      and the `data → stdout / status → stderr` convention at :5, :99-100)
    - crates/pmcp-workbook-compiler/src/dialect/linter.rs (the `lint(src: &dyn CellSource, rules: &DialectRules)
      -> LintReport` entry at :190)
    - crates/pmcp-workbook-dialect/src/lib.rs (`DialectRules::default()` building the 13-name WHITELIST + palette
      at :93-112)
    - crates/pmcp-workbook-compiler/src/lib.rs (the re-export surface: `WorkbookCellSource` :125, `LintReport`/
      `LintFinding`/`Severity` :105, and the ingest fn used to turn a `.xlsx` path into the WorkbookMap the linter
      runs over)
    - crates/pmcp-workbook-runtime/src/finding.rs (the `LintFinding`/`LintReport`/`Severity` field shapes +
      `has_errors()` at :43-111 + their serde derives)
    - cargo-pmcp/src/commands/workbook/mod.rs (this plan Task 1 — `LintArgs` is referenced by the enum variant
      `Lint(lint::LintArgs)`)
    - cargo-pmcp/src/commands/app.rs (the quiet-aware output pattern: `let not_quiet = std::env::var("PMCP_QUIET").is_err();`
      at :77, :107-111)
  </read_first>
  <action>
    Implement `cargo-pmcp/src/commands/workbook/lint.rs`. Define `#[derive(Debug, clap::Args)] pub struct LintArgs`
    with: positional `workbook_path: PathBuf` (lint always takes an explicit path — no compile-all for lint this
    phase), and `#[arg(long, default_value = "text")] pub format: String`. Implement `pub fn execute(args: LintArgs,
    gf: &GlobalFlags) -> Result<()>`: ingest the `.xlsx` at `args.workbook_path` into a `WorkbookMap` (call the same
    ingest fn the seed lane uses — see lib.rs read_first), wrap it in `WorkbookCellSource`, call
    `pmcp_workbook_compiler::dialect::linter::lint(&src, &DialectRules::default())` to get a `LintReport`. Render via
    a NEW shared helper `pub fn render_lint_report(report: &LintReport, format: &str, not_quiet: bool) -> Result<()>`
    (factored HERE so compile.rs reuses it — CONTEXT discretion: lint phase inside compile shares this renderer):
    `match format { "json" => println!("{}", serde_json::to_string_pretty(report)?), "text" => print each finding as
    `<severity> <sheet>!<cell>: <message> — fix: <repair>` to stdout, "other" => bail! }`. Add a pure
    `pub fn lint_exit_code(report: &LintReport) -> i32` returning `1` when `report.has_errors()` else `0` (D-10 —
    only Severity::Error blocks; Warning/Info advisory). The handler maps that to process exit: on errors, `bail!`
    with a concise summary so the dispatcher returns non-zero (or call `std::process::exit(lint_exit_code(...))`
    AFTER printing — Claude's discretion, but the printed findings MUST appear before exit). Gate decorative
    headers on `not_quiet` (`PMCP_QUIET` env / `gf.should_output()`). Add unit tests: `lint_exit_code` over a
    report with an Error finding ⇒ 1, warnings-only ⇒ 0; a json-render round-trips back to `LintReport`. Keep
    every function cognitive complexity ≤25.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --lib workbook::lint</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --lib workbook::lint` passes including the exit-code property test (Error⇒1, warnings⇒0).
    - A unit test serializes a `LintReport` via `render_lint_report(.., "json", ..)`-equivalent path and deserializes
      it back to an equal `LintReport` (asserts D-09 reuses the library serde type, no parallel DTO).
    - `grep -c 'lint_exit_code\|render_lint_report' cargo-pmcp/src/commands/workbook/lint.rs` ≥ 2 (both helpers exist,
      so compile.rs in Plan 03 can reuse them).
    - `cargo clippy -p cargo-pmcp` zero warnings on lint.rs.
    - The unknown-`--format` path `bail!`s with a message containing "text" and "json".
  </acceptance_criteria>
  <done>`workbook lint` runs the dialect linter over an ingested workbook, renders findings in text or json, exits non-zero only on Severity::Error, and exposes render_lint_report + lint_exit_code for compile.rs reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → process | a user-supplied `.xlsx` path and `--format` string cross into the ingest + render path |
| lint result → process exit code | CI consumes the exit code to gate a build (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-02-EXIT | Tampering | exit-code mapping `lint_exit_code` | mitigate | Pure function unit-tested: only `Severity::Error` ⇒ nonzero; a warnings-only sheet can NEVER silently fail CI nor silently pass an error sheet. Property-tested. |
| T-94-02-FMT | Spoofing / DoS | unknown `--format` value | mitigate | Unknown format `bail!`s explicitly rather than defaulting to a possibly-wrong renderer. |
| T-94-02-PATH | Tampering | `--workbook_path` ingest target | accept | A lint reads an explicit user-named local file; no escalation beyond reading a path the user already controls. The compiler ingest is the umya-isolated reader (Phase 93 boundary). |
| T-94-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (Plan 01 added the only dep). The Package Legitimacy Gate is N/A here; if any install is introduced, block via a human-verify checkpoint. |
</threat_model>

<verification>
- `cargo build -p cargo-pmcp` succeeds; `cargo pmcp workbook --help` shows the three verbs.
- `cargo test -p cargo-pmcp --lib workbook::lint` passes (exit-code + json round-trip).
- `Workbook` absent from `is_target_consuming()`.
- `cargo clippy -p cargo-pmcp` zero warnings.
</verification>

<success_criteria>
- The `workbook` subcommand group is wired and excluded from target-consuming (D-04, Phase-77 exclusion).
- `workbook lint` satisfies WBCL-02 with text/json output (D-09) and errors-fail/warnings-pass exit codes (D-10).
- `render_lint_report` + `lint_exit_code` are reusable by compile.rs (Plan 03).
</success_criteria>

<output>
Create `.planning/phases/94-cli-subcommands-pmcp-toml/94-02-SUMMARY.md` when done.
</output>
