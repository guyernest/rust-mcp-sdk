---
phase: 94-cli-subcommands-pmcp-toml
plan: 04
type: execute
wave: 3
depends_on: ["94-01", "94-02"]
files_modified:
  - cargo-pmcp/src/commands/workbook/emit.rs
autonomous: true
requirements: [WBCL-03, WBCL-04]
must_haves:
  truths:
    - "`cargo pmcp workbook emit <wb.xlsx>` regenerates a bundle WITHOUT the golden-corpus gate (WBCL-03, D-08)"
    - "emit prints a loud `UNGATED — not regression-checked, do not deploy` banner (D-08)"
    - "emit writes an ungated marker (gated: false) into the bundle's evidence/ so the status travels with the artifact (D-08)"
    - "emit resolves bundle-ids / compile-all through pmcp.toml like compile, but does NOT require --approver (dev/reference) (D-08/WBCL-04)"
    - "CR-02 @<version> non-overwrite means emit cannot clobber a promoted baseline (D-08)"
  artifacts:
    - path: "cargo-pmcp/src/commands/workbook/emit.rs"
      provides: "EmitArgs + emit handler (gate-skipped bundle write) + UNGATED banner + evidence gated:false marker"
      contains: "pub struct EmitArgs"
      min_lines: 90
  key_links:
    - from: "cargo-pmcp/src/commands/workbook/emit.rs"
      to: "pmcp_workbook_compiler::artifact::emit_bundle + EvidenceInputs"
      via: "ungated bundle emit with a gated:false evidence marker"
      pattern: "emit_bundle|EvidenceInputs"
    - from: "cargo-pmcp/src/commands/workbook/emit.rs"
      to: "colored banner"
      via: "loud UNGATED banner via colored .red().bold()"
      pattern: "UNGATED"
---

<objective>
Implement the `workbook emit` handler (WBCL-03): regenerate a bundle WITHOUT the golden-corpus gate
for dev/reference, with a loud `UNGATED` banner AND a persisted `gated: false` marker in the bundle's
`evidence/` so an unvetted bundle can never be mistaken for a promoted one downstream (D-08).

This is `compile.rs` minus the gate. It calls the same emit surface but skips `gate::gate`. It adds
NO compiler logic.
Purpose: A fast dev/reference rebuild path whose ungated status TRAVELS with the artifact.
Output: `cargo-pmcp/src/commands/workbook/emit.rs` (replaces the Plan-02 stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-cli-subcommands-pmcp-toml/94-CONTEXT.md
@.planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md
@cargo-pmcp/src/commands/workbook/config.rs
@cargo-pmcp/src/commands/workbook/lint.rs

<interfaces>
From Plan 01 (`workbook/config.rs`): `PmcpToml::load/resolve/all_entries`, `WorkbookEntry { path, bundle_id, out_dir }`.
From Plan 02 (`workbook/lint.rs`): `render_lint_report` + `lint_exit_code` (reuse for the internal lint pass).

Library verbs (re-exported from `pmcp-workbook-compiler`):
- The seven-member bundle emitter: `artifact::emit_bundle(...)` + `artifact::EvidenceInputs` (re-export
  lib.rs:141-144). The seven-member bundle contract (manifest.json, executable.ir.json, cell_map.json,
  layout.json, BUNDLE.lock, evidence/, ...) is the Phase 92 contract; `evidence/` is the member the
  `gated: false` marker attaches to. READ artifact.rs to learn how `EvidenceInputs` carries evidence members
  and where to inject the `gated: false` marker (an evidence map entry / file).
- CR-02 `@<version>` non-overwrite already protects promoted baselines, so emit cannot clobber a promoted one
  (gate/accept.rs `atomic_promote_dir` semantics) — emit writes its own `{workflow}@{version}/` and refuses to
  overwrite an existing baseline.
- `colored` crate (already a dep, Cargo.toml:43): `"...".red().bold()` for the UNGATED banner.

NOTE: emit does NOT call `gate::gate`. It does NOT require `--approver` (dev/reference, ungated — D-08).
The `version` still comes from the workbook (D-02/D-11), never from toml/flag.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EmitArgs + the ungated emit handler with banner + evidence gated:false marker</name>
  <files>cargo-pmcp/src/commands/workbook/emit.rs</files>
  <behavior>
    - `emit <wb.xlsx> --workflow quote` regenerates the seven-member bundle WITHOUT calling the gate (no
      gate::gate invocation in the code path) — WBCL-03.
    - The bundle's `evidence/` contains a marker indicating `gated: false` after emit (D-08) — assert the marker
      file/entry exists and parses to false.
    - emit prints a banner containing the substring `UNGATED` to stderr/stdout (D-08) — assert the substring.
    - emit does NOT require `--approver` (omitting it succeeds) — emit is dev/reference.
    - `emit <bundle-id>` resolves path/out_dir from pmcp.toml; bare `emit` emits ALL declared workbooks (D-08
      mirrors compile's resolution, WBCL-04).
    - emit cannot overwrite an existing promoted `@<version>` baseline (CR-02) — attempting it errors/refuses
      rather than clobbering.
  </behavior>
  <read_first>
    - cargo-pmcp/src/commands/workbook/compile.rs (Plan 03 — the SIBLING this handler mirrors minus the gate; copy
      the target-resolution (bundle-id / compile-all) shape and the lint-phase reuse, DROP the gate/accept branch)
    - crates/pmcp-workbook-compiler/src/lib.rs (the re-export surface: `emit_bundle`/`EvidenceInputs` :141-144,
      `compile_workbook` :188-203 for the ingest→...→emit shape the seed lane uses)
    - crates/pmcp-workbook-compiler/src/artifact.rs (where `emit_bundle` + `EvidenceInputs` live — learn how
      evidence members are assembled and the exact seam to inject the `gated: false` marker into evidence/)
    - crates/pmcp-workbook-compiler/src/gate/accept.rs (`atomic_promote_dir` / `@<version>` non-overwrite semantics
      at :16-22, :118-130 — confirm emit's write refuses to clobber a promoted baseline, CR-02)
    - cargo-pmcp/src/commands/app.rs (the quiet-aware output pattern at :77, :107-111)
    - cargo-pmcp/Cargo.toml (`colored = "3"` at :43 — already a dep for the banner)
    - cargo-pmcp/src/commands/workbook/lint.rs (Plan 02 — reuse `render_lint_report` + `lint_exit_code`)
    - .planning/phases/94-cli-subcommands-pmcp-toml/94-PATTERNS.md (section "emit.rs" — the "compile minus gate"
      rule, the banner via `colored`, the evidence `gated: false` marker, and CR-02 non-overwrite)
    - .planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md (the seven-member bundle contract
      the `evidence/` member belongs to)
  </read_first>
  <action>
    Replace the Plan-02 `emit.rs` stub. Define `#[derive(Debug, clap::Args)] pub struct EmitArgs`: positional
    `bundle_id_or_path: Option<String>` (bundle-id / bare path / no-arg emit-all, mirroring CompileArgs);
    `#[arg(long)] pub workflow: Option<String>` (required when a bare PATH is given); `#[arg(long)] pub out:
    Option<PathBuf>`; `#[arg(long, default_value = "text")] pub format: String`. DROP `--accept`/`--effective-date`,
    and `--approver` is NOT required (emit is dev/reference, ungated — D-08).

    Implement `pub fn execute(args: EmitArgs, gf: &GlobalFlags) -> Result<()>` mirroring compile's target resolution
    (single ad-hoc path / pmcp.toml bundle-id / bare emit-ALL via `all_entries`). For each target call
    `emit_one(...)`: run the lint phase first (reuse `render_lint_report`/`lint_exit_code`; an Error still blocks
    the emit — Claude's discretion, but a broken sheet should not silently emit a bundle), then run the same
    ingest→synth→compile→reconcile→emit pipeline the seed lane uses BUT call `artifact::emit_bundle` directly with
    an `EvidenceInputs` that carries a `gated: false` marker (inject the marker into the evidence/ member per the
    artifact.rs seam — D-08; the marker is a small JSON/text evidence entry so the status TRAVELS with the
    artifact). Do NOT call `gate::gate`. Read `version` from the workbook (D-02/D-11). After a successful write,
    print the loud `UNGATED — not regression-checked, do not deploy` banner via `colored` (e.g.
    `"UNGATED — not regression-checked, do not deploy".red().bold()`), gated on `gf.should_output()`/non-quiet but
    the banner is a SAFETY warning so prefer emitting it even in quiet to stderr (Claude's discretion; it MUST be
    visible). Rely on CR-02 `@<version>` non-overwrite so emit cannot clobber a promoted baseline; surface the
    refusal as a clear error if the target dir already holds a promoted baseline. Keep every function cognitive
    complexity ≤25.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --lib workbook::emit</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --lib workbook::emit` passes including a test asserting the emitted bundle's
      `evidence/` carries a `gated: false` marker (D-08).
    - A test asserts the emit output contains the substring `UNGATED` (D-08 banner).
    - A test asserts emit succeeds WITHOUT `--approver` (dev/reference — D-08).
    - `grep -c 'gate::gate' cargo-pmcp/src/commands/workbook/emit.rs` returns 0 (emit never calls the gate — WBCL-03).
    - `grep -c 'emit_bundle\|EvidenceInputs' cargo-pmcp/src/commands/workbook/emit.rs` ≥ 1 (uses the library emitter).
    - A test asserts emit refuses to overwrite a pre-existing promoted `@<version>` baseline (CR-02).
    - `cargo clippy -p cargo-pmcp` zero warnings on emit.rs.
  </acceptance_criteria>
  <done>`workbook emit` regenerates a bundle without the gate, prints a loud UNGATED banner, writes a gated:false marker into evidence/ that travels with the artifact, requires no --approver, resolves ids/emit-all from pmcp.toml, and cannot clobber a promoted baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ungated bundle → downstream consumer (server/deploy) | an unvetted bundle must be DISTINGUISHABLE from a gated/promoted one |
| pmcp.toml `out_dir`/`--out` → bundle write target | resolved write location (shared boundary with Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04-UNGATED | Spoofing / Repudiation | an ungated bundle mistaken for a promoted one downstream | mitigate | emit writes a `gated: false` marker INTO the bundle's `evidence/` member so the status travels with the artifact (D-08), AND prints a loud `UNGATED` banner. Test asserts both the marker presence and that it parses to false. A server/downstream can read the marker to refuse deploy. |
| T-94-04-CLOBBER | Tampering | emit overwriting a promoted baseline | mitigate | CR-02 `@<version>` non-overwrite (gate/accept.rs `atomic_promote_dir`) refuses to overwrite an existing promoted dir; the handler surfaces the refusal as an error rather than clobbering. Test asserts the refusal. |
| T-94-04-PATH | Tampering / Elevation | `out_dir`/`--out` write target | mitigate | Write targets resolve through `PmcpToml` (Plan 01 rejects `..` escapes); a `--out` override resolves under the project root with the same containment. |
| T-94-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. N/A — documented, no install task. |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --lib workbook::emit` passes (gated:false marker, UNGATED banner, no-approver, no-clobber).
- emit never calls `gate::gate` (grep == 0).
- `cargo clippy -p cargo-pmcp` zero warnings.
</verification>

<success_criteria>
- WBCL-03 satisfied: ungated dev/reference bundle regeneration.
- D-08 satisfied: loud banner + persisted `gated: false` evidence marker; CR-02 non-overwrite.
- WBCL-04 reuse: bundle-id resolution + emit-all through pmcp.toml.
</success_criteria>

<output>
Create `.planning/phases/94-cli-subcommands-pmcp-toml/94-04-SUMMARY.md` when done.
</output>
