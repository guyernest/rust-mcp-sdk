---
phase: 72.1
slug: finalize-landing-support
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-20
---

# Phase 72.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Phase 72.1 is a template + rustdoc change with no new Rust API surface; validation leans on grep guardrails, `tsc --noEmit`, `next build`, and `make quality-gate`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust: `cargo test` / `cargo clippy` / `cargo doc` (workspace). Template: `tsc --noEmit` + `next build` via `npx`. |
| **Config file** | Root `Cargo.toml` (workspace), `cargo-pmcp/templates/landing/nextjs/tsconfig.json`, `cargo-pmcp/templates/landing/nextjs/package.json` |
| **Quick run command** | `make quality-gate` (matches CI exactly — fmt, clippy pedantic+nursery, build, test, audit) |
| **Full suite command** | `make quality-gate && ( cd cargo-pmcp/templates/landing/nextjs && npx -y -p typescript@5 tsc --noEmit && npx -y next@14 build )` |
| **Estimated runtime** | ~90-180 seconds (make quality-gate ~60-120s; tsc ~10s; next build ~30-60s on cold npm cache) |

---

## Sampling Rate

- **After every task commit:** Run targeted grep assertions for the task's guardrails (G1–G6 matrix below), plus `cargo fmt --all -- --check`.
- **After every plan wave:** Run `make quality-gate`.
- **Before `/gsd-verify-work`:** Full suite must be green, AC-11 manual gate signed off.
- **Max feedback latency:** 180 seconds worst-case (next build on cold cache); 15 seconds for the common path (grep + fmt check).

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------------|-----------|-------------------|-------------|--------|
| 72.1-01-01 | 01 | 1 | CR-03 AC-4 (hook exists) | Hook runs client-side only, aborts on unmount | type | `npx -y -p typescript@5 tsc --noEmit -p cargo-pmcp/templates/landing/nextjs/tsconfig.json` | ⬜ new file | ⬜ pending |
| 72.1-01-02 | 01 | 1 | CR-03 AC-2 / AC-4 / AC-5 / AC-6 | `/signup` renders "Sign-up is not available for this server." or redirects to Cognito | grep | `grep -n "useLandingConfig\|Sign-up is not available" cargo-pmcp/templates/landing/nextjs/app/signup/page.tsx` && `! grep -n "NEXT_PUBLIC_COGNITO\|NEXT_PUBLIC_LANDING_CLIENT_ID" cargo-pmcp/templates/landing/nextjs/app/signup/page.tsx` | ✅ edit existing | ⬜ pending |
| 72.1-01-03 | 01 | 1 | CR-03 AC-4 / AC-7 | Callback redirects to `signupRedirectAfter` or `/connect` | grep | `grep -n "useLandingConfig\|signupRedirectAfter\|/connect" cargo-pmcp/templates/landing/nextjs/app/signup/callback/page.tsx` && `! grep "NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER" cargo-pmcp/templates/landing/nextjs/app/signup/callback/page.tsx` | ✅ edit existing | ⬜ pending |
| 72.1-01-04 | 01 | 1 | CR-03 AC-8 | `/connect` interpolates `cfg.mcpEndpoint` on every snippet; no hand-assembled URL | grep | `grep -c "cfg.mcpEndpoint" cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx` (expect ≥4) && `! grep "\\.us-east\\.true-mcp\\.com" cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx` | ✅ edit existing | ⬜ pending |
| 72.1-01-05 | 01 | 1 | CR-03 AC-9 | Header renders Sign-up button only when `cfg.signup !== null` | grep | `grep -n "signupEnabled\|cfg.signup" cargo-pmcp/templates/landing/nextjs/app/components/Header.tsx` && `grep -n "MCP_SERVER_NAME" cargo-pmcp/templates/landing/nextjs/app/components/Header.tsx` (must still match) | ✅ edit existing | ⬜ pending |
| 72.1-01-06 | 01 | 1 | CR-03 AC-12 | Rustdoc describes `/landing-config` runtime mechanism | grep+cargo | `! grep -n "NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER" cargo-pmcp/src/landing/config.rs` && `cargo doc --no-deps -p cargo-pmcp` | ✅ edit existing | ⬜ pending |
| 72.1-01-07 | 01 | 1 | — | cargo-pmcp version bumped to 0.8.1 | grep | `grep -n '^version = "0.8.1"' cargo-pmcp/Cargo.toml` | ✅ edit existing | ⬜ pending |
| 72.1-01-08 | 01 | 1 | CR-03 AC-2 / AC-3 / AC-4 / AC-10 / AC-11 | Full quality gate green; grep guardrails G1–G6 pass; `next build` succeeds | system | `make quality-gate` + `( cd cargo-pmcp/templates/landing/nextjs && npx -y -p typescript@5 tsc --noEmit && npx -y next@14 build )` + grep guardrails below | ✅ composite | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Grep Guardrail Matrix (lift into PLAN.md `must_haves`)

| Guardrail | Command | Expected | Maps to AC |
|-----------|---------|----------|------------|
| **G1 — No `NEXT_PUBLIC_*` env vars** | `grep -rE 'NEXT_PUBLIC_COGNITO\|NEXT_PUBLIC_LANDING_CLIENT_ID\|NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/templates/landing/nextjs/` | **0 matches** | AC-2 |
| **G2 — `MCP_SERVER_NAME` still consumed** | `grep -rl 'MCP_SERVER_NAME' cargo-pmcp/templates/landing/nextjs/` \| wc -l | **≥1** (scope boundary: branding kept) | AC-3 |
| **G3 — exactly one fetch of `/landing-config`** | `grep -rE "fetch\('/landing-config'" cargo-pmcp/templates/landing/nextjs/` | **exactly 1 match** (inside the hook) | AC-4 |
| **G4 — all four consumers import the hook** | `grep -l "useLandingConfig" cargo-pmcp/templates/landing/nextjs/app/signup/page.tsx cargo-pmcp/templates/landing/nextjs/app/signup/callback/page.tsx cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx cargo-pmcp/templates/landing/nextjs/app/components/Header.tsx` | **4 matches** | AC-4 |
| **G5 — no hand-assembled MCP URL in `connect/page.tsx`** | `grep -E "\\.us-east\\.true-mcp\\.com\|\\$\\{serverName\\}" cargo-pmcp/templates/landing/nextjs/app/connect/page.tsx` | **0 matches** | AC-8 |
| **G6 — rustdoc has no stale `NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER`** | `grep -n 'NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER' cargo-pmcp/src/landing/config.rs` | **0 matches** | AC-12 |

---

## Wave 0 Requirements

No Wave 0 stubs needed. Verification infrastructure for Phase 72.1 is:
- `make quality-gate` already exists and passes on current main.
- `cargo doc --no-deps -p cargo-pmcp` is a standalone invocation; no config changes needed.
- `npx -y -p typescript@5 tsc --noEmit -p cargo-pmcp/templates/landing/nextjs/tsconfig.json` requires no new template files beyond the hook itself (the template's `tsconfig.json` already has path aliases — researcher flagged `@/lib/*` availability as Assumption A5; fallback is relative imports).
- `npx -y next@14 build` in the template dir — no new install scripts needed.

*Existing infrastructure covers all phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| AC-11: `next build && next start` works without network access to `/landing-config` | CR-03 AC-11 | Template has no Playwright / e2e harness; offline runtime behavior is not automatable without introducing test infra explicitly out-of-scope for 0.8.1 | (1) `cd cargo-pmcp/templates/landing/nextjs && unset NEXT_PUBLIC_COGNITO_DOMAIN NEXT_PUBLIC_COGNITO_REGION NEXT_PUBLIC_LANDING_CLIENT_ID NEXT_PUBLIC_SIGNUP_REDIRECT_AFTER && npx -y next@14 build` should succeed. (2) `npx -y next@14 start` (or serve `out/`). (3) Open `/signup` in a browser with network tools set to block `/landing-config` → must render "Sign-up is not available for this server." without hanging or JS errors. (4) Open `/connect` with `/landing-config` blocked → must render "Connection instructions unavailable. Please try again." without errors. (5) Open `/` (Home) with `/landing-config` blocked → Header renders without a Sign-up button and no console errors. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (AC-11 flagged as manual with operator instructions)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (all 8 tasks have automated grep/type/build checks)
- [x] Wave 0 covers all MISSING references (none required — existing infra covers)
- [x] No watch-mode flags
- [x] Feedback latency < 180s (common path ~15s)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — planner must embed these guardrails as `must_haves` in the PLAN.md.
