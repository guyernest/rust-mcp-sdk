---
phase: 116-auth-hardening-seps
plan: 14
type: execute
wave: 9
depends_on: ["116-06", "116-07", "116-09", "116-10", "116-11", "116-12"]
files_modified:
  - tests/v2_bounded_reads_tripwire.rs
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "The four auth-surface files are permanently inside the bounded-read tripwire's scope, and the tripwire reports zero violations"
    - "A future unbounded read added to any of those four files fails the gate rather than passing unnoticed"
    - "The exemption allowlist is still empty — the fence was closed by fixing reads, not by writing exemptions"
    - "The tripwire's own scope justification names AUTH-03/D-15, so it does not read as enforcing something its stated requirement does not cover"
  artifacts:
    - path: "tests/v2_bounded_reads_tripwire.rs"
      provides: "EXTRA_SCOPE and REQUIRED_FILES widened to the four auth files, module doc second-owner note, reqwest-shaped failure guidance"
      contains: "src/client/oauth.rs"
  key_links:
    - from: "tests/v2_bounded_reads_tripwire.rs"
      to: ".planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md"
      via: "D-113-V closure, owner Phase 116"
      pattern: "D-113-V"
---

<objective>
Close D-113-V by permanently widening the Phase-113.1 bounded-read tripwire to the four auth-surface
files, and prove it reports zero.

Purpose: D-113-V is roadmap-assigned to Phase 116 with status OPEN. Its measured population of 31
reviewed-unbounded whole-body reads spans exactly the four files this phase edits —
`providers/generic_oidc.rs`, `providers/cognito.rs`, `client/oauth.rs`, `client/auth.rs`. The scanner
ALREADY finds all of them (it strips whitespace and resolves rustfmt-split chains, pinned by a test at
`:1050`); only the scope fence keeps them unreported. D-113-V's own note says to expect the fence
widening to be the whole job, and 116-01 already OBSERVED the fence reporting them.

**Sequencing matters.** This plan runs LAST among the source-touching plans on purpose: widening the
fence before the reads are bounded would leave `make quality-gate` red for several waves, which the
repo's zero-tolerance rule does not permit.
Output: a closed, permanently enforced fence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/116-auth-hardening-seps/116-CONTEXT.md
@.planning/phases/116-auth-hardening-seps/116-RESEARCH.md
@.planning/phases/116-auth-hardening-seps/116-PATTERNS.md
@.planning/phases/116-auth-hardening-seps/116-BASELINES.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen both scope constants and update the fence's own justification</name>
  <files>tests/v2_bounded_reads_tripwire.rs</files>
  <read_first>
    - tests/v2_bounded_reads_tripwire.rs (READ IN FULL — the module doc at :1-13 quoting HTTP-09's requirement text verbatim as its scope justification, `SHARED_DIR` / `EXTRA_SCOPE` / `REQUIRED_FILES` at :64-82, `WHOLE_BODY_NEEDLES` and `bound_in_scope` around :560-620, the empty `WHOLE_BODY_ALLOWLIST` with its written floor at :580-591, the failure message at :650-658, and the rustfmt-split-chain test at :1050)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md § D-113-V (the four-file table, the counting method, the two named exclusions with line numbers, the fix shape, and the stated reason the fence was not widened then)
    - .planning/phases/116-auth-hardening-seps/116-BASELINES.md § "D-15 Pre-Fix Violation Baseline" (the violation list 116-01 observed with a temporary widening — this task's job is to drive that same list to zero)
    - src/client/auth.rs, src/client/oauth.rs, src/server/auth/providers/generic_oidc.rs, src/server/auth/providers/cognito.rs (all four as plans 116-06, 116-07 and 116-12 left them — confirm each has zero reqwest whole-body needles before widening)
    - .planning/phases/116-auth-hardening-seps/116-PATTERNS.md § "tests/v2_bounded_reads_tripwire.rs — scope widening"
  </read_first>
  <behavior>
    - With the four files in `EXTRA_SCOPE`, the tripwire reports ZERO violations
    - `REQUIRED_FILES` includes all four base names, so a path typo that silently drops one from the discovered scope fails the anti-vacuity guard rather than passing over a shrunken set
    - `WHOLE_BODY_ALLOWLIST` is still EMPTY
    - **Negative control:** reverting ONE bounded read in one of the four files (temporarily, in a scratch working tree) makes the tripwire FAIL and NAME that file and line. Restore afterwards; the committed diff must contain only the tripwire change
    - **Anti-vacuity control:** removing one of the four newly added `REQUIRED_FILES` names while leaving `EXTRA_SCOPE` intact must NOT make the suite pass silently — confirm what the guard actually does and record it
    - The failure message, when it fires on an auth file, names the reqwest `chunk()`-accumulate shape (or the shared helper), not `http_body_util::Limited`
    - Every pre-existing tripwire test still passes, including the rustfmt-split-chain test at `:1050`
  </behavior>
  <action>
Widen the fence and make its own documentation honest about what it now covers.

**Both constants.** Add `src/client/auth.rs`, `src/client/oauth.rs`,
`src/server/auth/providers/generic_oidc.rs` and `src/server/auth/providers/cognito.rs` to
`EXTRA_SCOPE`, AND add their base names (`auth.rs`, `oauth.rs`, `generic_oidc.rs`, `cognito.rs`) to
`REQUIRED_FILES`. Both are required: `REQUIRED_FILES` is the anti-vacuity guard, and widening
`EXTRA_SCOPE` alone would let a future path typo silently drop coverage. Watch for a base-name
collision — `auth.rs` may match more than one path — and if the guard matches by base name, make sure
the entry resolves to the intended file; record what you found.

**Module doc.** The doc at :1-13 quotes HTTP-09's requirement text verbatim as its scope
justification. With auth files in scope, the file would read as enforcing something its own stated
requirement does not cover. Add AUTH-03 / D-15 as the SECOND OWNER, name D-113-V as the deferred item
being closed, and state that the auth files entered scope in Phase 116.

**Failure message.** The current guidance tells the reader to wrap the read in
`http_body_util::Limited` "exactly as `collect_body_within_cap` does" — that is the HYPER shape, and
the four auth files are reqwest. Extend the message so an auth-file violation points at the reqwest
`chunk()`-accumulate shape and at `src/shared/http_body_cap.rs` (116-06). Keep the existing hyper
guidance for the existing hyper files, keep "Deleting the needle is not a fix", and keep the
allowlist's requirement that any entry be a written, reviewed exemption.

**Do not grow the allowlist.** `WHOLE_BODY_ALLOWLIST` is empty and its own doc calls that its FLOOR.
If any violation remains after the four earlier plans, the answer is to fix the read, not to write an
exemption. If a read genuinely cannot be bounded, stop and escalate rather than adding an entry — an
allowlist entry added to close a phase is exactly the failure mode the floor exists to prevent.

**Run both controls.** The negative control (revert one bounded read, observe the failure NAME the
file and line, restore) is what makes this fence evidence rather than decoration — Phase 115 relearned
three times that an unfired fence proves nothing. The anti-vacuity control checks the second silent
failure mode: coverage lost by omission rather than by regression. Record both observations, including
WHICH assertion fired, in the summary. Confirm afterwards that
`git diff --exit-code -- src/client/auth.rs src/client/oauth.rs src/server/auth/providers/` is clean.
  </action>
  <verify>
    <automated>cargo nextest run --features full,oauth -E 'binary(v2_bounded_reads_tripwire)' &amp;&amp; cargo nextest list --features full,oauth -E 'binary(v2_bounded_reads_tripwire)' | tail -3 &amp;&amp; git diff --exit-code -- src/client/auth.rs src/client/oauth.rs src/server/auth/providers/</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --features full,oauth -E 'binary(v2_bounded_reads_tripwire)'` passes with a NON-ZERO test count
    - The same command passes under `--features full` alone (the tripwire is a source-text scanner and must not require `oauth`)
    - `grep -c 'src/client/auth.rs\|src/client/oauth.rs\|generic_oidc.rs\|cognito.rs' tests/v2_bounded_reads_tripwire.rs` shows all four in `EXTRA_SCOPE` and all four base names in `REQUIRED_FILES`
    - `grep -A2 'WHOLE_BODY_ALLOWLIST' tests/v2_bounded_reads_tripwire.rs` shows the list is still `&[]`
    - The module doc names AUTH-03, D-15 and D-113-V
    - The failure message names the reqwest `chunk()`-accumulate shape and `src/shared/http_body_cap.rs` for auth files, and still names `http_body_util::Limited` for the hyper files
    - The negative control was RUN and its output recorded: the assertion that fired is named, and the violation names the reverted file and line
    - The anti-vacuity control was RUN and its outcome recorded
    - `git diff --exit-code -- src/client/auth.rs src/client/oauth.rs src/server/auth/providers/` exits 0 — no source file was changed by this plan
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <done>D-113-V is closed by measurement: the fence that would have reported 31 sites now reports zero, permanently, and was observed to still bite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authorization server / IdP → any response body pmcp reads whole | Body size is entirely peer-controlled, on success and error paths alike |
| future contributor → the four auth files | A new unbounded read reintroduces the DoS surface this phase just closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-55 | Denial of Service | a NEW unbounded whole-body read added to an auth file after this phase | mitigate | The four files are permanently in `EXTRA_SCOPE` and their names in `REQUIRED_FILES`; the tripwire runs inside `make quality-gate`, so a regression fails the gate |
| T-116-56 | Repudiation | closing the fence with exemptions instead of fixes | mitigate | `WHOLE_BODY_ALLOWLIST` asserted still empty; the plan forbids growing it and requires escalation instead |
| T-116-57 | Repudiation | coverage silently lost to a path typo | mitigate | `REQUIRED_FILES` anti-vacuity guard widened in lockstep; the anti-vacuity control is RUN, not assumed |
| T-116-58 | Repudiation | a fence that was never observed to fire | mitigate | The negative control reverts one bounded read and records which assertion fired and what it named |
| T-116-SC | Tampering | cargo installs | mitigate | Zero packages added; this plan touches one test file |
</threat_model>

<verification>
- `cargo nextest run --features full,oauth -E 'binary(v2_bounded_reads_tripwire)'` green with a non-zero count
- The same binary green under `--features full` alone
- Negative control and anti-vacuity control both RUN and recorded, naming the assertion that fired
- `git diff --exit-code` clean for all four source files
- `make quality-gate` exits 0
</verification>

<success_criteria>
- The four auth files are permanently in scope and the tripwire reports zero
- The allowlist is still empty
- The fence's own documentation names its second owner
- Both controls were observed, not assumed
</success_criteria>

<output>
Create `.planning/phases/116-auth-hardening-seps/116-14-SUMMARY.md` when done
</output>
