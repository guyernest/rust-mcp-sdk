---
phase: 116-auth-hardening-seps
plan: 15
type: execute
wave: 10
depends_on: ["116-13", "116-14"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/116-auth-hardening-seps/deferred-items.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Every phase gate has been run at HEAD and its result recorded as a delta against the phase-base baseline"
    - "AUTH-01, AUTH-02 and AUTH-03 are booked on measured evidence, each citing an artifact plus a named test binary and a count"
    - "AUTH-01's booking states plainly that the requirement's 'strict on v2, lenient on v1' wording was realized as the spec's flag-keyed rule, and why that is strictly safer"
    - "Every item this phase did not do has a written home with a named owner"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "AUTH-01/02/03 booked with cited evidence, and the requirement-index rows updated"
      contains: "AUTH-01"
    - path: ".planning/phases/116-auth-hardening-seps/deferred-items.md"
      provides: "every deferral, refinement and named-owner item this phase produced or inherited"
      min_lines: 80
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/116-auth-hardening-seps/116-BASELINES.md"
      via: "every booked number is a delta against a recorded baseline"
      pattern: "b2bf9157"
---

<objective>
Run every phase gate at HEAD, record each as a delta against `116-BASELINES.md`, and only then book
AUTH-01/02/03 — each citing an artifact, a named test binary and a count (D-20).

Purpose: `D-115-G` (booking ahead of evidence) recurred TWICE on a single requirement in Phase 115,
and this project's roadmap warns in as many words against inheriting `[~]` by habit. These three
requirements derive from published RFCs (9207, 7591, 8414) and published spec prose, with NO dependency
on `schema.json` or on the still-unpublished `ext-tasks` repo — so there is no publication hold and
`[x]` is the correct booking, but only after the gates exit 0.
Output: three bookings a future reader can re-derive, and a deferred-items file with no orphans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-auth-hardening-seps/116-CONTEXT.md
@.planning/phases/116-auth-hardening-seps/116-RESEARCH.md
@.planning/phases/116-auth-hardening-seps/116-BASELINES.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run every phase gate at HEAD and record the deltas</name>
  <files>.planning/phases/116-auth-hardening-seps/deferred-items.md</files>
  <read_first>
    - .planning/phases/116-auth-hardening-seps/116-BASELINES.md (READ IN FULL — every number this task produces a delta against, and the RESEARCH assumption A2 item that 116-01 left open)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § Pitfalls 3, 4, 5, 7, 8, 9 (why each obvious gate command is insufficient, and which two `make` targets are no-op stubs)
    - CLAUDE.md § "Quality Gate Enforcement" and § "ALWAYS Requirements for New Features"
    - Makefile (targets `quality-gate`, `lint`, `test`, `doc-check`, `wasm-build`, `check-todos`, `test-examples`)
  </read_first>
  <action>
Run the full phase gate and write a `## Phase-End Gate Results` section into
`deferred-items.md` (creating the file), each entry naming the command, the exit code, the number, and
the delta against `116-BASELINES.md`.

1. `make quality-gate` — exit code and the passed/failed/ignored counts. This closes RESEARCH
   assumption A2, which 116-01 carried rather than measured. Note that if it fails on the known
   macOS-keychain live-HTTP flake (`D-115-AL`: `streamable_http.rs:458` native-roots `.expect` with
   `Os { code: -36 }`), re-run the whole gate and record BOTH runs — do not normalize a failure away
   silently.
2. `cargo nextest run --features full,oauth` — exit code and total count. Then, per binary, record the
   count from
   `cargo nextest list --features full,oauth -E 'binary(<name>)'` for every binary this phase created
   or extended: `oauth_iss_validation`, `oauth_discovery_urls`, `oauth_application_type`,
   `oauth_credential_store`, `oauth_discovery_validation`, `oauth_provider_discovery`,
   `oauth_state_csrf`, `oauth_iss_integration`, `oauth_dcr_integration`, `oauth_store_wiring`,
   `oauth_refresh`, `v2_bounded_reads_tripwire`. Every count must be NON-ZERO — a mis-typed selector
   exits 0 having run nothing, and that defect hit Phase 114 seven times. Use `binary(...)`, never
   `test(/.../)`.
3. `cargo clippy --features full,oauth --lib --tests` with `make lint`'s full flag set
   (`-D clippy::all -W clippy::pedantic -W clippy::nursery -W clippy::cargo`). `make lint` alone uses
   `--features "full"` and compiles ZERO lines of `src/client/oauth.rs`; this run is what actually
   covers the phase's code.
4. `pmat quality-gate --fail-on-violation --checks complexity` — must be 0 violations. Also record
   whether any `#[allow(clippy::cognitive_complexity)]` was added anywhere in the phase; the answer
   should be none.
5. `cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157` — must exit 0 now that 116-13
   bumped the minor. Name the baseline in the recorded command. State separately that the crates.io
   2.17.0 comparison carries a PRE-EXISTING `#[deprecated]` failure on `OptimizedSseTransport` that is
   not this phase's.
6. `make doc-check` — `^error` count and per-file distribution, recorded as a DELTA against the
   baseline (28 at the phase base, none in this phase's files). The phase neither caused nor cleared
   this; say exactly that. `make doc-check` is the ONLY gate whose feature list includes `oauth`, so it
   is the only place this phase's new rustdoc is compiled at all.
7. `make wasm-build` after `rustup target add wasm32-unknown-unknown` — exit code and warning count.
8. `make check-todos` — must pass; `grep -rn 'TODO\|FIXME\|HACK\|XXX' src/` must return nothing.
9. `make test-examples` plus `cargo run --example c11_oauth_iss_state_validation` (both with and
   without `--features full,oauth`).
10. `cargo fuzz` campaign result for the phase's target: run count and artifacts-dir state, carried
    from 116-08. Note that `fuzz` is in the workspace `exclude` array (`D-115-AB`), so
    `make quality-gate` covers nothing under `fuzz/` — the campaign result is separate evidence.
11. The refined dependency fence from 116-13, with its output.

Do NOT cite `make check-unwraps` or `make unused-deps` as evidence for anything: both are no-op stubs
(`Makefile:768-772` and `:202-206`) that print success unconditionally. If either appears in the gate
transcript, note in the record that it proves nothing.

If ANY gate is red, STOP. Do not proceed to Task 2. Booking a requirement on a red gate is precisely
the failure `D-115-G` records twice.
  </action>
  <verify>
    <automated>make quality-gate &amp;&amp; cargo nextest run --features full,oauth &amp;&amp; pmat quality-gate --fail-on-violation --checks complexity &amp;&amp; cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157</automated>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` contains a `## Phase-End Gate Results` section with all eleven numbered entries
    - Every entry names its exact command and its exit code
    - The per-binary table lists all twelve named binaries with a NON-ZERO count each, obtained via `binary(...)` selectors
    - `make quality-gate` exit 0 is recorded, closing RESEARCH assumption A2 explicitly by name
    - `pmat quality-gate --checks complexity` records 0 violations and states whether any cognitive-complexity allow attribute was added (expected: none)
    - `cargo semver-checks ... --baseline-rev b2bf9157` records exit 0, and the crates.io pre-existing failure is named separately as not this phase's
    - `make doc-check` is recorded as a DELTA with the sentence that the phase neither caused nor cleared it
    - `check-unwraps` and `unused-deps` are explicitly noted as no-op stubs that prove nothing
    - No requirement in `.planning/REQUIREMENTS.md` was modified by this task
  </acceptance_criteria>
  <done>Every claim Task 2 will make has a command, an exit code and a number behind it.</done>
</task>

<task type="auto">
  <name>Task 2: Book AUTH-01, AUTH-02 and AUTH-03 on the measured evidence</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 640-680 (the `### Auth Hardening (AUTH)` section at :660-664 with the three unchecked entries, and the Phase-115 booking immediately above it — copy that booking's evidence discipline: cited artifact, named test binary, count, and named limitations)
    - .planning/REQUIREMENTS.md lines 780-830 (the requirement-index rows `AUTH-01 | Phase 116 | Pending` at :789-791, and the phase-count line at :826 — all must be updated in lockstep)
    - .planning/phases/116-auth-hardening-seps/deferred-items.md § "Phase-End Gate Results" (as Task 1 left it)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § "Open Questions 3" (the recommendation for AUTH-01's booking language) and § "Pitfall 6" (the exact scoping AUTH-03's no-oauth2 claim requires)
    - .planning/ROADMAP.md § "Phase 116" (the three success criteria the bookings answer)
  </read_first>
  <action>
Book the three requirements `[x]`, each with an evidence block a future reader can re-derive rather
than trust. Run this task ONLY after Task 1 recorded every gate at exit 0.

**AUTH-01.** The requirement text says "strict on v2, lenient on v1". Book it, and state PLAINLY in the
booking that this wording was realized as the MCP spec's own normative rule — **strict whenever the
authorization server advertises `authorization_response_iss_parameter_supported` or emits `iss`,
lenient otherwise** — because `Client::era()` is not available during the OAuth flow (the flow runs
before any MCP connection exists) and the spec's four-row table keys on the RFC 9207 discovery flag,
not on protocol era. Say explicitly that this is STRICTLY SAFER FOR v1 than the requirement asked for:
a v1 deployment whose AS emits `iss` now gets it validated, where the requirement would have skipped
the check entirely. Do not book against words the code does not implement; state the realization and
why. Cite: `src/shared/oauth_validation.rs`, `src/client/oauth.rs`, `src/error/mod.rs`, and the counts
for `binary(oauth_iss_validation)`, `binary(oauth_state_csrf)`, `binary(oauth_iss_integration)` and
`binary(oauth_discovery_validation)`. Name the anchor-validation finding (RESEARCH Pitfall 1) as work
the requirement did not ask for and got anyway, since without it the `iss` check would have been
decorative.

**AUTH-02.** Cite `src/server/auth/provider.rs`, `src/shared/oauth_validation.rs`,
`src/client/oauth.rs`, and the counts for `binary(oauth_application_type)` and
`binary(oauth_dcr_integration)` (the latter as a before/after: 5 at the phase base). State that
`application_type` is asserted on the WIRE BODY, not on the in-memory struct, and that the semver gate
stayed additive because the value rides the existing `#[serde(flatten)] extra` carrier. Name the one
obligation deliberately NOT adopted: SEP-837's optional retry with an adjusted `application_type` (a
MAY), and why.

**AUTH-03.** Cite `src/shared/credential_store.rs`, `src/shared/oauth_validation.rs`,
`src/client/auth.rs`, both provider files, `src/client/oauth.rs`, the cargo-pmcp `auth_cmd` tree, and
the counts for `binary(oauth_credential_store)`, `binary(oauth_discovery_urls)`,
`binary(oauth_provider_discovery)`, `binary(oauth_store_wiring)`, `binary(oauth_refresh)` and
`binary(v2_bounded_reads_tripwire)`. Scope the no-new-crate claim EXACTLY as RESEARCH Pitfall 6
requires — "no new `oauth2`/`openidconnect` dependency; `pmcp` core remains oauth2-free; cargo-pmcp's
PRE-EXISTING direct `oauth2 = 5.0` is confined to `src/deployment/` and is untouched, with zero
`oauth2::` references under `src/commands/`" — and cite the fence commands. A reviewer WILL grep and
find that line; an unscoped claim reopens the booking.

Also record, inside AUTH-03's booking, the four limitations this phase names rather than buries:
(1) AS-change detection does not use the spec's stated mechanism because RFC 9728 Protected Resource
Metadata is deferred by owner decision; (2) schema-1 cargo-pmcp entries that record no issuer are
dropped rather than guessed; (3) an already-installed cargo-pmcp 0.18.0 hard-errors on
`schema_version: 2` and nothing in this repo can change it; (4) SEP-2350 step-up scope accumulation is
deferred whole.

Update the requirement-index rows at :789-791 from `Pending` to the booked state, and reconcile the
phase-count line at :826. All three must move together — a booked requirement with a `Pending` index
row is the kind of inconsistency that reopens a phase.
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*AUTH-0' .planning/REQUIREMENTS.md | grep -q '^3$' &amp;&amp; test "$(grep -c 'AUTH-0[123] | Phase 116 | Pending' .planning/REQUIREMENTS.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - All three AUTH entries read `- [x]` in `.planning/REQUIREMENTS.md`
    - No `AUTH-0N | Phase 116 | Pending` row remains in the requirement index
    - Each booking cites at least one source artifact path AND at least one `binary(...)` name AND a numeric count
    - AUTH-01's booking contains the sentence explaining the flag-keyed realization and the words "strictly safer"
    - AUTH-02's booking states the wire-body assertion and names the non-adopted SEP-837 retry MAY
    - AUTH-03's booking scopes the no-oauth2 claim naming `cargo-pmcp/Cargo.toml`, `src/deployment/` and the zero-hits grep under `src/commands/`
    - AUTH-03's booking enumerates all four named limitations
    - The phase-count line at :826 is consistent with the three bookings
    - `git diff` shows `.planning/REQUIREMENTS.md` changed only in the AUTH section, the three index rows and the count line
  </acceptance_criteria>
  <done>A reader who distrusts the bookings can re-derive all three from the cited commands without asking anyone.</done>
</task>

<task type="auto">
  <name>Task 3: Give every deferral and refinement a written home with a named owner</name>
  <files>.planning/phases/116-auth-hardening-seps/deferred-items.md</files>
  <read_first>
    - .planning/phases/116-auth-hardening-seps/deferred-items.md (as Task 1 left it)
    - .planning/phases/116-auth-hardening-seps/116-CONTEXT.md § Deferred Ideas (all nine entries, including the two the owner added on 2026-08-02: RFC 9728 Protected Resource Metadata and the RFC 8707 `resource` parameter, both owner: Guy)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § "Decisions Requiring Amendment" (A1-A4), § "Open Questions" (1-5), § "State of the Art" (the DCR deprecation in favor of Client ID Metadata Documents)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md (the house format for a deferred item: identifier, statement, evidence, owner, status)
    - .planning/phases/114-tasks-extension-migration/... deferred items referencing `D-114-V` (the doc-check blocker, owner UNASSIGNED)
  </read_first>
  <action>
Write the phase's deferred-items register. Follow the house format: a stable identifier, a statement of
what was NOT done, the evidence or reasoning, a named owner, and a status. Every entry below must
appear; anything with no owner must say UNASSIGNED rather than being left implicit.

**Owner-deferred, from CONTEXT (2026-08-02):**
- RFC 9728 Protected Resource Metadata discovery — an MCP-spec client MUST that pmcp does not
  implement; it derives the AS from the MCP base URL directly (`src/client/oauth.rs:366-390`). Record
  the NAMED DEPENDENCY: SEP-2352 specifies AS-change detection as "detected via updated protected
  resource metadata", so D-18's detection (116-11) uses the provenance signal available today instead
  of the spec's stated mechanism. Owner: Guy. Needs a roadmap slot.
- RFC 8707 `resource` parameter on authorization and token requests — the other MCP-spec client MUST;
  pmcp sends none (`src/client/oauth.rs:664-672`). Ships together with the RFC 9728 item. Owner: Guy.
- SEP-2350 step-up scope accumulation — deferred WHOLE, both halves together. Record RESEARCH's
  correction to the rationale: the client half CAN be triggered by a non-pmcp server, so the deferral
  stands as a coherence decision (ship both halves as one feature) rather than as "nothing would
  trigger it".
- `UpstreamAuthDecorator` + `HEADER_UPSTREAM_AUTH` SDK extraction; the outbound-OAuth vending core
  (`OutboundOAuthCore`, ~987 lines, and note that `docs/design/agents-teams-sdk-extraction-plan.md`
  Phases A-F contain NO phase for it, so it needs a roadmap slot rather than an assumption that Phase
  117 absorbs it); the Cognito internal/external fallback chain; token-at-rest encryption in core;
  typed accessors for the other RFC 7591 fields `DcrResponse` drops into `extra`.

**Amendments this phase adopted (record the fact, not a deferral):**
- A1 — the RFC 9207 flag did NOT go on `OidcDiscoveryMetadata`; it rides a new `AuthorizationServerExtras`
  sibling type returned by `discover_with_extras`, because a field would be a MAJOR semver break.
- A2 — the markers ride `Error::Protocol`, not `Error::Authentication`, which has no `data` member.
- A3 — SEP-2351 landed as an ORDERED PROBE (not an append-to-insert swap, which was measured to 404
  Microsoft Entra ID), and SEP-2207's actual content (`grant_types` + `offline_access`) landed IN
  ADDITION to D-14's three defects.
- A4 — D-18 branches on credential provenance: warn-and-re-register for DCR credentials, a typed
  `reauth_required` error for pre-registered ones.

**Refinements and known limitations:**
- D-17 refinement: schema-1 cargo-pmcp entries that record NO issuer cannot be re-keyed without
  guessing (which SEP-2352 forbids) and are dropped with a named warning and a reported count. State
  this against D-17's "every existing login is preserved" wording explicitly.
- Forward-compat trap: an already-installed `cargo-pmcp` 0.18.0 hard-errors on `schema_version: 2`
  (`cache.rs:74-80`) with an "upgrade cargo-pmcp" message. The message is already actionable and
  nothing in this repo can change an installed binary. Released-behavior note. Owner: cargo-pmcp
  release notes.
- SEP-837's optional retry with an adjusted `application_type` (a MAY) was deliberately not adopted.
- Client ID Metadata Documents (PR #2858) — the spec now DEPRECATES DCR in favor of CIMD while
  retaining DCR for backcompat. This phase hardens DCR, which remains correct; CIMD support is out of
  scope and needs its own slot. Owner: UNASSIGNED.
- `make doc-check` is RED at HEAD with 28 pre-existing errors, none in this phase's files, and it
  blocks the org-required `gate`. Recorded as `D-113-W` / `D-114-V`, owner UNASSIGNED. This phase
  NEITHER CAUSED NOR CLEARED IT — repeat the measured delta from Task 1 and cross-reference the
  existing identifiers rather than minting a new one.
- `make check-unwraps` and `make unused-deps` are no-op stubs; the two
  `.duration_since(UNIX_EPOCH).unwrap()` calls in `src/client/oauth.rs` production paths are named as
  a real, uncovered item. Owner: UNASSIGNED.

Cross-reference D-113-V as CLOSED by 116-14, citing the tripwire's zero report, so a reader of the
Phase 113 deferred-items file can follow the thread forward.
  </action>
  <verify>
    <automated>test -f .planning/phases/116-auth-hardening-seps/deferred-items.md && grep -q 'RFC 9728' .planning/phases/116-auth-hardening-seps/deferred-items.md && grep -q 'RFC 8707' .planning/phases/116-auth-hardening-seps/deferred-items.md && grep -q 'D-113-V' .planning/phases/116-auth-hardening-seps/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` contains an entry for every item enumerated in the action, each with an identifier, a statement, evidence, an owner and a status
    - The RFC 9728 entry states the NAMED DEPENDENCY on SEP-2352's AS-change detection mechanism and names the owner as Guy
    - The RFC 8707 entry names the owner as Guy and states it ships with the RFC 9728 item
    - The four amendments A1-A4 are each recorded as ADOPTED with a one-line statement of the corrected shape
    - The D-17 refinement is stated explicitly against D-17's "every existing login is preserved" wording
    - The `make doc-check` entry cross-references `D-113-W` / `D-114-V` and does NOT mint a new identifier, and repeats the measured delta showing the phase neither caused nor cleared it
    - D-113-V is recorded as CLOSED by 116-14 with the tripwire's zero report cited
    - Every item with no owner says UNASSIGNED explicitly
    - `grep -rn 'TODO\|FIXME\|HACK\|XXX' src/` returns nothing — deferred work lives in this file, never in a source comment
  </acceptance_criteria>
  <done>No item this phase chose not to do is discoverable only by re-reading the plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor claims → `.planning/REQUIREMENTS.md` `[x]` | A booking is the artifact downstream phases and reviewers trust without re-deriving |
| this phase's scope → the milestone's remaining spec obligations | Unrecorded gaps become false parity claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-59 | Repudiation | booking a requirement ahead of measured evidence (`D-115-G`, twice in Phase 115) | mitigate | Task 2 runs ONLY after Task 1 records every gate at exit 0; each booking cites an artifact, a named `binary(...)` and a count |
| T-116-60 | Repudiation | a verify block that "passes" having selected zero tests (Phase 114, seven times) | mitigate | Every per-binary count is obtained with a `binary(...)` selector and must be NON-ZERO; `test(/.../)` is forbidden for file selection |
| T-116-61 | Repudiation | AUTH-03's "no oauth2 crate" claim falsified by a reviewer's grep | mitigate | The claim is scoped naming `cargo-pmcp/Cargo.toml`, `src/deployment/` and the zero-hit grep under `src/commands/`, with the fence commands cited |
| T-116-62 | Repudiation | booking AUTH-01 against wording ("strict on v2") the code does not implement | mitigate | The booking states the flag-keyed realization, why the era is unavailable pre-connection, and that the result is strictly safer for v1 |
| T-116-63 | Information Disclosure | a deferred spec MUST (RFC 9728 / RFC 8707) read as implemented | mitigate | Both recorded with named owner Guy, with RFC 9728's dependency on SEP-2352's detection mechanism stated in the register AND in 116-11's rustdoc |
| T-116-SC | Tampering | cargo installs | mitigate | Phase installed zero packages; the refined dependency fence is recorded in Task 1 item 11 |
</threat_model>

<verification>
- Every gate in Task 1 recorded with command, exit code and delta; all green
- All three AUTH requirements booked `[x]` with citations, and no `Pending` index row remains
- `deferred-items.md` exists with every enumerated entry and no implicit owner
- `make quality-gate` exits 0 at the final commit
</verification>

<success_criteria>
- AUTH-01, AUTH-02 and AUTH-03 are booked on evidence a stranger can re-derive
- AUTH-01's booking is honest about how "strict on v2, lenient on v1" was realized
- AUTH-03's no-new-crate claim is scoped so a grep confirms rather than contradicts it
- Every deferral, amendment and limitation has a written home with a named owner
</success_criteria>

<output>
Create `.planning/phases/116-auth-hardening-seps/116-15-SUMMARY.md` when done
</output>
