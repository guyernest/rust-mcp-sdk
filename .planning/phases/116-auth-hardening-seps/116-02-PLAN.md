---
phase: 116-auth-hardening-seps
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - src/error/mod.rs
  - src/shared/oauth_validation.rs
  - src/shared/mod.rs
  - src/lib.rs
  - tests/oauth_iss_validation.rs
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "A caller can programmatically distinguish an iss mismatch, a state mismatch and a reauth-required failure without matching on message substrings"
    - "The RFC 9207 four-row decision table is implemented as a pure function callable with no TcpListener, no browser and no reqwest"
    - "Applying any of RFC 3986 §6.2.2-6.2.3's four normalizations to an issuer produces a mismatch, never a match"
    - "The pure tier compiles for wasm32-unknown-unknown without the oauth feature"
  artifacts:
    - path: "src/shared/oauth_validation.rs"
      provides: "AuthorizationRequestRecord, IssPresence, validate_authorization_response, iss_presence_from"
      min_lines: 200
      contains: "pub fn validate_authorization_response"
    - path: "src/error/mod.rs"
      provides: "ISS_MISMATCH_MARKER, STATE_MISMATCH_MARKER, REAUTH_REQUIRED_MARKER + constructors + predicates"
      contains: "ISS_MISMATCH_MARKER"
    - path: "tests/oauth_iss_validation.rs"
      provides: "4-row table unit tests + no-normalization property tests + D-04 precedence tests"
      min_lines: 150
  key_links:
    - from: "src/shared/oauth_validation.rs"
      to: "src/error/mod.rs"
      via: "Error::iss_mismatch / Error::state_mismatch"
      pattern: "Error::(iss_mismatch|state_mismatch)"
    - from: "src/lib.rs"
      to: "src/shared/oauth_validation.rs"
      via: "crate-root re-export"
      pattern: "pub use shared::oauth_validation::"
---

<objective>
Land AUTH-01's two foundations: the stable programmatic error identities (D-03, corrected by
RESEARCH A2) and the wasm-clean pure validation tier (D-01/D-02/D-05/D-06/D-12) implementing the MCP
spec's normative four-row `iss` decision table plus `state` comparison.

Purpose: everything else in AUTH-01 is wiring. This plan is the semantics. Landing it as a pure
function over (query string, per-request record) is what makes a Lambda/Workers redirect handler and
the CLI loopback listener share one implementation instead of two (D-05), and what keeps
`authorization_code_flow_inner` under the PR-blocking cog-25 gate.
Output: `src/shared/oauth_validation.rs`, three error markers, and an ALWAYS-coverage test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/116-auth-hardening-seps/116-CONTEXT.md
@.planning/phases/116-auth-hardening-seps/116-RESEARCH.md
@.planning/phases/116-auth-hardening-seps/116-PATTERNS.md
@.planning/phases/116-auth-hardening-seps/116-BASELINES.md
@CLAUDE.md
</context>

<interfaces>
Contracts this plan CREATES. Downstream plans (116-04, 116-06, 116-09, 116-11, 116-12) consume these
exact names — do not rename them without updating those plans.

`src/error/mod.rs` (public):
- `pub const ISS_MISMATCH_MARKER: &str = "IssMismatch"`
- `pub const STATE_MISMATCH_MARKER: &str = "StateMismatch"`
- `pub const REAUTH_REQUIRED_MARKER: &str = "ReauthRequired"`
- `Error::iss_mismatch(expected: &str, actual: Option<&str>) -> Error`
- `Error::state_mismatch() -> Error`
- `Error::reauth_required(issuer: &str, reason: &str) -> Error`
- `Error::is_iss_mismatch(&self) -> bool`, `Error::is_state_mismatch(&self) -> bool`, `Error::is_reauth_required(&self) -> bool`
- `Error::iss_expected(&self) -> Option<&str>`, `Error::iss_actual(&self) -> Option<&str>`
- `Error::reauth_issuer(&self) -> Option<&str>`

`src/shared/oauth_validation.rs` (public, ungated):
- `#[non_exhaustive] pub enum IssPresence { Required, Optional }`
- `pub struct AuthorizationRequestRecord` — PRIVATE fields, `new(expected_issuer, code_verifier, state, iss_presence)` plus `expected_issuer()`, `code_verifier()`, `state()`, `iss_presence()` accessors
- `pub fn validate_authorization_response(raw_query: &str, record: &AuthorizationRequestRecord) -> Result<String>` — returns the authorization code
- `pub fn iss_presence_from(env_value: Option<&str>, builder: Option<IssPresence>, discovery_flag: Option<bool>) -> IssPresence`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three marker-const error identities on Error::Protocol</name>
  <files>src/error/mod.rs</files>
  <read_first>
    - src/error/mod.rs lines 28-60 (the `Error` enum — confirm `Authentication(String)` is a bare-String tuple variant with no `data` member, and that the enum is NOT `#[non_exhaustive]`)
    - src/error/mod.rs lines 110-150 (the existing marker consts `MRTR_ROUND_LIMIT_MARKER`, `MRTR_INPUT_REQUIRED_MARKER`, `RETIRED_ON_V2_MARKER`, `PMCP_ERROR_KEY`, `RETIRED_METHOD_KEY`)
    - src/error/mod.rs lines 560-660 (`retired_on_v2` constructor with its doctest, `is_retired_on_v2`, `retired_field`, `protocol_data`, `pmcp_error_marker`)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § "A2 — D-03's marker cannot ride Error::Authentication"
  </read_first>
  <behavior>
    - `Error::iss_mismatch("https://as.example", Some("https://evil.example")).is_iss_mismatch()` is true
    - `Error::iss_mismatch(...).iss_expected()` returns `Some("https://as.example")`; `.iss_actual()` returns `Some("https://evil.example")`
    - `Error::iss_mismatch("https://as.example", None).iss_actual()` returns `None` — the row-2 case (AS advertises support, `iss` absent from the response)
    - `Error::state_mismatch().is_state_mismatch()` is true, and `to_string()` contains NEITHER the expected nor the received state value
    - `Error::reauth_required("https://as.example", "refresh failed").is_reauth_required()` is true and `.reauth_issuer()` returns `Some("https://as.example")`
    - Negative controls: `Error::internal("nope")` returns false for all three predicates; `Error::retired_on_v2(...)` returns false for all three; `Error::iss_mismatch(...).is_state_mismatch()` is false
    - `Error::Authentication("x".into()).is_iss_mismatch()` is false — pins A2's finding so a future refactor cannot silently move the carrier back
  </behavior>
  <action>
Add three marker consts, three constructors and their predicates + typed field accessors, following
`retired_on_v2` at `src/error/mod.rs:587-648` EXACTLY in shape.

Carrier variant is `Error::Protocol { code, message, data }` — NOT `Error::Authentication`, per
RESEARCH A2: `Authentication` is a bare-`String` tuple variant with no `data` member, and the private
helpers `protocol_data()` and `pmcp_error_marker()` match only `Self::Protocol`, so a marker on
`Authentication` would make its own predicate return `false`.

Marker consts (values are the crate's compatibility surface — copy the "do not change this string"
rustdoc note verbatim from `RETIRED_ON_V2_MARKER`):
`ISS_MISMATCH_MARKER = "IssMismatch"`, `STATE_MISMATCH_MARKER = "StateMismatch"`,
`REAUTH_REQUIRED_MARKER = "ReauthRequired"`.

Private `data` key consts alongside the existing ones: `ISS_EXPECTED_KEY = "expectedIssuer"`,
`ISS_ACTUAL_KEY = "actualIssuer"`, `REAUTH_ISSUER_KEY = "issuer"`.

`ErrorCode` for all three: `ErrorCode::INVALID_REQUEST`. Justify the choice in an inline comment the
way `retired_on_v2` justifies `METHOD_NOT_FOUND` — these are locally-produced fail-fasts over a
malformed/hostile authorization response, so the code describes the response, not a transport fault.

`Error::state_mismatch()` takes NO arguments and its message must name neither the expected nor the
received state. The expected state is a CSRF secret and the received one is attacker-controlled;
echoing either turns the refusal into a disclosure channel (the same rule
`src/shared/sse_optimized.rs:321-327` states for refused bodies).

`Error::iss_mismatch(expected, actual)` takes `actual: Option<&str>`. `None` means "the AS advertises
`authorization_response_iss_parameter_supported` but sent no `iss`" — spec table row 2. The message
must distinguish the two cases in human-readable prose while both carry the same marker, so one
predicate covers both.

Each constructor gets `#[must_use]` and a rustdoc `# Examples` doctest ending in a NEGATIVE control
line, copying the habit at `src/error/mod.rs:576-586`.

Reuse the existing `protocol_data()` / `pmcp_error_marker()` helpers — add no new private helper
unless a typed field accessor needs one, in which case mirror `retired_field`'s shape.
  </action>
  <verify>
    <automated>cargo nextest run --features full,oauth -E 'binary(pmcp)' -E 'test(iss_mismatch) + test(state_mismatch) + test(reauth_required)' --no-fail-fast; cargo test --features full,oauth --doc error 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ISS_MISMATCH_MARKER\|STATE_MISMATCH_MARKER\|REAUTH_REQUIRED_MARKER' src/error/mod.rs` is at least 9 (three consts, three predicates, three constructor payloads)
    - `grep -n 'Self::Authentication' src/error/mod.rs` shows no occurrence inside any of the three new constructors
    - Every one of the three constructors builds `Self::Protocol { .. }`
    - Each of the three constructor rustdocs contains a doctest whose last assertion is a negative control (`assert!(!...)`)
    - `cargo test --features full,oauth --doc` passes for `src/error/mod.rs`
    - `cargo clippy --features full,oauth --lib --tests -- -D clippy::all -W clippy::pedantic -W clippy::nursery` reports no new warning attributable to `src/error/mod.rs`
    - `make doc-check` `^error` count is <= the number recorded in `116-BASELINES.md` (delta, not zero — Pitfall 7)
  </acceptance_criteria>
  <done>Downstream code and conformance fixtures can branch on `err.is_iss_mismatch()` instead of on message text, and A2's finding is pinned by a test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The wasm-clean pure validation tier and its ALWAYS-coverage suite</name>
  <files>src/shared/oauth_validation.rs, src/shared/mod.rs, src/lib.rs, tests/oauth_iss_validation.rs</files>
  <read_first>
    - src/shared/pkce.rs (READ IN FULL — the exact structural precedent: ungated module doc naming why it is ungated, `use crate::error::{Error, Result}`, `#[must_use]` pure fns with doc examples, inline `#[cfg(test)] mod tests` where each test's doc names the invariant it pins. `generate_state()` at :143-146 already exists — do NOT reimplement it)
    - src/shared/mod.rs lines 14-30 (the `pub mod pkce;` declaration and its load-bearing "Ungated on purpose" rationale comment)
    - src/lib.rs lines 100-112 (the ungated crate-root re-export of the pkce helpers, and the contrasting wasm-gated re-exports below it)
    - tests/pkce_helper.rs (READ IN FULL — the ALWAYS-coverage test file to model: header enumerating the covered rows, RFC-vector-first ordering, crate-root re-export resolution check, `proptest!` block with actionable failure messages)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § "Code Examples → The normative iss decision table" (the 4-row table, the record requirement, the no-normalization enumeration, the error-response clause) and § "A1"
    - src/error/mod.rs (as modified by Task 1)
  </read_first>
  <behavior>
    Four-row table (`tests/oauth_iss_validation.rs`), one test per row:
    - flag=true (IssPresence::Required), `iss` present and equal to `record.expected_issuer()` → Ok(code)
    - flag=true, `iss` absent → Err, `is_iss_mismatch()` true, `iss_actual()` is None
    - flag=false/absent (IssPresence::Optional), `iss` present and equal → Ok(code)
    - flag=false/absent, `iss` absent → Ok(code) (the D-01 floor: lenient only when the AS says nothing and sends nothing)
    Plus: flag=Optional, `iss` present and DIFFERENT → Err (this is the floor's teeth — v1 gets strictly safer)
    No-normalization properties (RFC 3986 §6.2.2-6.2.3, four generators, each asserting MISMATCH):
    - scheme/host case folding (`HTTPS://AS.Example` vs `https://as.example`) → mismatch
    - default-port elision (`https://as.example:443` vs `https://as.example`) → mismatch
    - trailing slash (`https://as.example/` vs `https://as.example`) → mismatch
    - percent-encoding normalization (`https://as.example/%74enant` vs `https://as.example/tenant`) → mismatch
    State (D-12):
    - state present and equal → passes on to iss validation
    - state present and different → Err, `is_state_mismatch()` true, and the error string contains neither state value
    - state absent from the response → Err, `is_state_mismatch()` true
    Error-response non-disclosure:
    - response carrying `error=access_denied&error_description=SECRET&state=<good>&iss=<wrong>` → Err with `is_iss_mismatch()` true, and the returned error's `to_string()` does NOT contain `SECRET`
    - response carrying `error=access_denied&error_description=visible&state=<good>&iss=<right>` → Err that DOES surface the AS error (iss validated first, then the error is actionable)
    D-04 precedence (`iss_presence_from`): env > builder > discovery flag
    - `("strict", Some(Optional), Some(false))` → Required
    - `("lenient", Some(Required), Some(true))` → Optional
    - `(None, Some(Required), Some(false))` → Required
    - `(None, None, Some(true))` → Required
    - `(None, None, Some(false))` and `(None, None, None)` → Optional
    - an unrecognized env value falls through to the next level rather than silently disabling validation
    Public-surface: the crate-root re-export resolves to the same items as the module path
  </behavior>
  <action>
Create `src/shared/oauth_validation.rs` as the ungated, wasm-clean pure tier. Model it on
`src/shared/pkce.rs` wholesale — it is the existing instance of exactly this pattern (D-05/D-06).

Module doc must state, the way `pkce.rs:1-20` does, WHY it is ungated and what it contrasts with:
`src/client/oauth.rs` is `#[cfg(all(not(target_arch = "wasm32"), feature = "oauth"))]` and `oauth`
pulls `webbrowser`/`dirs`/`rand`, so a Cloudflare Workers or Lambda redirect handler gets zero of it;
this module must be callable there. Permitted imports: `crate::error::{Error, Result}` and `url`
(non-optional, already the callback parser). Add nothing else — the phase's dependency delta is
required to be empty.

Declare in `src/shared/mod.rs` with a rationale comment mirroring the one above `pub mod pkce;` so a
future contributor does not "tidy" a `cfg` onto it. Re-export at crate root in `src/lib.rs` alongside
the pkce line: `AuthorizationRequestRecord`, `IssPresence`, `validate_authorization_response`.
`iss_presence_from` stays module-path-only. Do NOT add a wasm32 `cfg` to either declaration.

`IssPresence` is `#[non_exhaustive]` with variants `Required` and `Optional`. There is deliberately
no `Disabled` variant: D-01's floor is unconditional — an `iss` that is present is ALWAYS compared —
so the only thing configurable is whether absence is fatal.

`AuthorizationRequestRecord` holds `expected_issuer`, `code_verifier`, `state` and `iss_presence` as
PRIVATE fields with a `new(..)` constructor and read-only accessors. Private fields are the semver
choice, not a style choice: RESEARCH A1 shows `OAuthConfig`, `DcrRequest` and `OidcDiscoveryMetadata`
are all-pub-field non-`#[non_exhaustive]` structs where adding a field is a MAJOR break; a struct with
private fields is not exhaustively constructible, so future fields stay minor. Its rustdoc must state
the spec's record requirement: issuer, verifier and state MUST live in ONE per-request record — three
separate locals is exactly how `state` became structurally unbindable at `src/client/oauth.rs:712`.

`validate_authorization_response(raw_query, record)` takes the RAW query component (no leading `?`)
so a platform handler that only ever sees a query string can call it. Decode with
`url::form_urlencoded::parse(raw_query.as_bytes())` — RFC 9207 §2.4 REQUIRES the
`application/x-www-form-urlencoded` decode before comparison and this performs it; never hand-roll a
percent-decode. Evaluation order is fixed and load-bearing:
1. `state` — must be present and equal `record.state()`, else `Error::state_mismatch()`.
2. `iss` — the four-row table against `record.expected_issuer()` using `==` on the decoded strings.
   Simple string comparison per RFC 3986 §6.2.1. Apply NO normalization of any kind.
3. `error` — only now may an AS-supplied `error` be surfaced. The spec's MUST NOT is explicit: on an
   `iss` mismatch the client must not act on or display `error`, `error_description` or `error_uri`,
   which is why steps 1 and 2 precede this one.
4. `code` — present ⇒ `Ok(code)`; absent with no `error` ⇒ a protocol error naming the missing
   parameter.
Multiple occurrences of a parameter: take the first and document that choice.

`iss_presence_from(env_value, builder, discovery_flag)` is the D-04 precedence resolver, kept pure so
it is unit-testable without a process environment. Precedence is env > builder > discovery flag.
Accepted env values are `"strict"` ⇒ `Required` and `"lenient"` ⇒ `Optional`, compared
case-insensitively after trimming; any other value falls through to the next level rather than
disabling anything. `discovery_flag` is `Some(true)` ⇒ `Required`, `Some(false)`/`None` ⇒ `Optional`.
The env var NAME (`PMCP_OAUTH_ISS_VALIDATION`) is read at the CALL SITE in 116-09, never inside this
module — D-07's I/O-free discipline applies to the pure tier too.

Write `tests/oauth_iss_validation.rs` modelled on `tests/pkce_helper.rs`: a header enumerating the
rows covered, the four table rows first, then the no-normalization property block, then state, then
error-response non-disclosure, then `iss_presence_from` precedence, then the crate-root re-export
resolution check. Per Phase 115's discipline, derive the property invariants from the RFC's
no-normalization sentence rather than restating the implementation's comparison operator. This file
does NOT need `#![cfg(feature = "oauth")]` — the tier is ungated, which is the point; leaving it
ungated means it also runs under plain `make quality-gate`.
  </action>
  <verify>
    <automated>cargo nextest run --features full,oauth -E 'binary(oauth_iss_validation)' &amp;&amp; cargo nextest list --features full,oauth -E 'binary(oauth_iss_validation)' | tail -3 &amp;&amp; cargo build --target wasm32-unknown-unknown --no-default-features --features wasm</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest list --features full,oauth -E 'binary(oauth_iss_validation)'` reports a NON-ZERO test count (a zero count exits 0 and proves nothing — RESEARCH Pitfall 5)
    - `cargo nextest run --features full -E 'binary(oauth_iss_validation)'` ALSO reports a non-zero count, proving the tier is genuinely ungated
    - `cargo build --target wasm32-unknown-unknown --no-default-features --features wasm` exits 0
    - `grep -n 'reqwest\|webbrowser\|dirs::\|rand::' src/shared/oauth_validation.rs` returns nothing
    - `grep -n 'cfg(.*wasm32\|feature = "oauth"' src/shared/oauth_validation.rs` returns nothing
    - `grep -n 'pub mod oauth_validation' src/shared/mod.rs` returns one line, preceded by a rationale comment naming why the module is ungated
    - `grep -n 'pub use shared::oauth_validation::' src/lib.rs` returns one line
    - The five table tests (four rows plus the Optional-present-and-different case) all exist and pass
    - The four no-normalization properties each assert MISMATCH and each name the RFC 3986 subsection they derive from
    - The error-description non-disclosure test asserts the secret string is absent from `err.to_string()`
    - `git diff --exit-code b2bf9157..HEAD -- Cargo.toml` shows no dependency line added
    - `pmat quality-gate --fail-on-violation --checks complexity` reports 0 violations
  </acceptance_criteria>
  <done>The RFC 9207 table and the CSRF `state` check exist as one pure, wasm-callable function with an RFC-derived property suite, and the CLI is free to become just its first caller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authorization server → OAuth callback query string | Every byte is attacker-influenceable in a mix-up or CSRF attack |
| pmcp error surface → downstream callers / logs | Secrets can leak out through error messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Spoofing | authorization response `iss` | mitigate | `validate_authorization_response` implements the spec's 4-row table with simple string comparison and NO normalization; four property tests assert each RFC 3986 §6.2.2-6.2.3 normalization yields a mismatch |
| T-116-02 | Tampering | authorization response `state` (CSRF / code injection) | mitigate | `state` is compared FIRST, before `iss` and before any AS-supplied `error` is surfaced; absence is a mismatch, not a skip |
| T-116-03 | Information Disclosure | `Error::state_mismatch` message | mitigate | Constructor takes no arguments and names neither value; asserted by a test on `to_string()` |
| T-116-04 | Information Disclosure | AS-supplied `error_description` after an `iss` mismatch | mitigate | Evaluation order puts `error` handling strictly after `state` and `iss`; asserted by the non-disclosure test |
| T-116-05 | Elevation of Privilege | future refactor moving the marker back onto `Error::Authentication` | mitigate | A test asserts `Error::Authentication(..).is_iss_mismatch()` is false, pinning RESEARCH A2 |
| T-116-SC | Tampering | cargo installs | mitigate | Zero packages added; acceptance criterion diffs `Cargo.toml` against `b2bf9157` |
</threat_model>

<verification>
- `cargo nextest run --features full,oauth -E 'binary(oauth_iss_validation)'` green with a non-zero count
- `cargo nextest run --features full -E 'binary(oauth_iss_validation)'` ALSO green with a non-zero count (ungated proof)
- `cargo build --target wasm32-unknown-unknown --no-default-features --features wasm` exits 0
- `make quality-gate` exits 0
- `cargo clippy --features full,oauth --lib --tests -- -D clippy::all -W clippy::pedantic -W clippy::nursery -W clippy::cargo` clean
- `pmat quality-gate --fail-on-violation --checks complexity` 0 violations
- `cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157` reports zero MAJOR/breaking findings (a "minor bump required" outcome is EXPECTED — this plan adds public API; the version bump lands in 116-13)
- `make doc-check` `^error` count <= the `116-BASELINES.md` baseline
</verification>

<success_criteria>
- The four-row table and the `state` check are implemented once, purely, and are callable with no transport
- Three stable error discriminators exist on `Error::Protocol` with negative-control doctests
- The module compiles for wasm32 with neither `oauth` nor `reqwest`
</success_criteria>

<output>
Create `.planning/phases/116-auth-hardening-seps/116-02-SUMMARY.md` when done
</output>

