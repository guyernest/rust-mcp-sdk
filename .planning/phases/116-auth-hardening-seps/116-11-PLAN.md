---
phase: 116-auth-hardening-seps
plan: 11
type: execute
wave: 7
depends_on: ["116-05", "116-10"]
files_modified:
  - src/client/oauth.rs
  - tests/oauth_store_wiring.rs
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "Tokens and DCR-issued client_ids are stored under the issuer that produced them, so a server that switches authorization server misses the cache and re-registers"
    - "The legacy issuer-less token file is never read as credentials, and its presence is announced once rather than silently ignored"
    - "A change of authorization server for a known MCP server is announced by name, and is fatal only where the spec says it should be"
    - "A platform can supply its own credential store without the SDK reaching for a home directory"
  artifacts:
    - path: "src/client/oauth.rs"
      provides: "CredentialStore wiring, with_credential_store / with_account_scope builders, legacy cache discard, D-18 issuer-change detection"
      contains: "CredentialStore"
    - path: "tests/oauth_store_wiring.rs"
      provides: "issuer-keyed round trip, cross-AS miss, legacy discard, issuer-change warn vs error by provenance"
      min_lines: 140
  key_links:
    - from: "src/client/oauth.rs"
      to: "src/shared/credential_store.rs"
      via: "load/save/delete keyed by (issuer, account) plus record_issuer/last_issuer"
      pattern: "(CredentialKey|record_issuer|last_issuer)"
---

<objective>
Replace the flat, issuer-less token cache with the issuer-keyed `CredentialStore` (SEP-2352 /
D-07/D-16/D-17/D-19), and make an authorization-server substitution visible instead of invisible
(D-18, refined by RESEARCH A4).

Purpose: today `~/.pmcp/oauth-tokens.json` holds a single flat token with NO issuer field
(`src/client/oauth.rs:151-156`). It cannot be re-keyed without GUESSING which AS issued it — precisely
what SEP-2352 forbids — so D-17 discards it. Once storage is issuer-keyed, SEP-2352's two mandates
fall out of the key shape rather than out of enforcement code: a different AS is a cache miss, and
since the DCR-issued `client_id` lives in the same record (a client_id issued by AS-A is meaningless
at AS-B), re-registration is automatic. But issuer-keying also makes an AS substitution SAFE and
INVISIBLE — the user is simply walked through a fresh login at an IdP they did not expect. D-18 makes
it loud.
Output: the CLI flow on the store, with the substitution announced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/116-auth-hardening-seps/116-CONTEXT.md
@.planning/phases/116-auth-hardening-seps/116-RESEARCH.md
@.planning/phases/116-auth-hardening-seps/116-PATTERNS.md
@.planning/phases/116-auth-hardening-seps/116-BASELINES.md
@CLAUDE.md
</context>

<interfaces>
CONSUMED (do not redefine):
- `pmcp::shared::credential_store::{CredentialKey, StoredCredentials, CredentialStore, InMemoryCredentialStore, FileCredentialStore, default_credential_path, normalize_server_key}` (116-05)
- `Error::{reauth_required, is_reauth_required, reauth_issuer}` (116-02)

CREATED (all inherent builder methods on `OAuthHelper`, backed by PRIVATE fields — never fields on `OAuthConfig`):
- `OAuthHelper::with_credential_store(self, store: std::sync::Arc<dyn CredentialStore>) -> Self`
- `OAuthHelper::with_account_scope(self, account: impl Into<String>) -> Self`

Consumed downstream by 116-12 (refresh reads `client_id` from the store) and 116-13 (cargo-pmcp).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route credential persistence through the issuer-keyed store and discard the legacy cache</name>
  <files>src/client/oauth.rs, tests/oauth_store_wiring.rs</files>
  <read_first>
    - src/client/oauth.rs lines 126-200 (`AuthorizationResult` with its `issuer` and `client_id` fields, the flat `TokenCache` at :149-156 with NO issuer field, and `OAuthHelper { config, client }` — private fields, so new private fields are semver-free)
    - src/client/oauth.rs lines 420-540 (READ THIS RANGE — `get_access_token`: the cached-token read, the expiry check with its `.duration_since(UNIX_EPOCH).unwrap()` at :434-437, the refresh attempt, and the fall-through into `authorization_code_flow`)
    - src/client/oauth.rs lines 950-1000 (`load_cached_token` / `cache_token` and the `cache_file` resolution — the two functions being replaced, and the `TokenCache` literal at :970-975)
    - src/shared/credential_store.rs (as 116-05 left it — the trait, both impls, `default_credential_path`, `normalize_server_key`, and the `FileCredentialStore` rustdoc's forward-compat note)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § "Code Examples → SEP-2352 — Authorization Server Binding" (the two verbatim MUSTs) and § "Runtime State Inventory"
  </read_first>
  <behavior>
    - A successful authorization stores credentials under `CredentialKey::new(<effective issuer>, <account scope>)`, and the stored record carries the effective `client_id` — DCR-issued when DCR fired, config-supplied otherwise
    - A subsequent call with the SAME issuer and account loads the cached token and performs no browser flow
    - A subsequent call after the issuer changes MISSES the cache, and (for a DCR flow) re-registers — with no enforcement branch in the code doing the checking, only the key shape
    - `~/.pmcp/oauth-tokens.json` is NEVER opened for reading; if it exists, a single `tracing::warn!` names the path, states that it is discarded because it records no issuer and cannot be re-keyed without guessing, and states that one re-login is required. The file is left in place for the user to delete
    - The warning fires at most once per `OAuthHelper` instance, not once per call
    - `OAuthHelper::with_credential_store(Arc::new(InMemoryCredentialStore::new()))` fully replaces the file store — the resulting helper touches no home directory at all
    - `OAuthHelper::with_account_scope("cognito-sub-123")` keys credentials under that account; a helper with a different account scope and the same issuer misses
    - The default account scope is the empty string — the single-user CLI case
    - `OAuthHelper::new(cfg)` performs no filesystem access; the store (and therefore `default_credential_path`) is resolved lazily on first use
    - `OAuthConfig` gains no field
  </behavior>
  <action>
Wire `OAuthHelper` to the credential store.

**The store field.** Add a private, lazily-resolved store to `OAuthHelper`, plus
`with_credential_store(self, Arc<dyn CredentialStore>)` and
`with_account_scope(self, impl Into<String>)` as inherent consuming builders. Both must be builder
methods, not `OAuthConfig` fields: `OAuthConfig` is public, all-pub-field and not
`#[non_exhaustive]`, so a new field is `constructible_struct_adds_field` = MAJOR and would break the
three in-repo literal sites plus every downstream user's.

`OAuthHelper::new` must NOT call `default_credential_path()` — resolve the default
`FileCredentialStore` on FIRST USE, honoring `config.cache_file` when set and
`default_credential_path()` otherwise. D-07's I/O-free construction is a platform-proven constraint,
and a constructor that reads `dirs` makes the type unusable in a Lambda that will inject its own store
a line later.

**Replace the cache functions.** Retire `load_cached_token` / `cache_token` / the `TokenCache` struct
in favor of `store.load(&key)` / `store.save(&key, &creds)` where
`key = CredentialKey::new(effective_issuer, account_scope)`. The effective issuer is the same value
AUTH-01 uses as its anchor: the issuer from the AS's discovery document. Persist the effective
`client_id` in the record — this is not optional and not a convenience: it is what makes SEP-2352's
"MUST re-register with the new authorization server" automatic, and it is what 116-12 needs to fix
D-14's second defect (DCR flows currently cannot refresh at all because `client_id` is read from
`config` where a DCR-issued id never lives).

**Discard the legacy cache (D-17, core half).** `~/.pmcp/oauth-tokens.json` must never be read as
credentials. Emit one `tracing::warn!` per helper instance if the path exists, naming the path, the
reason (no issuer recorded ⇒ cannot be re-keyed without guessing, which SEP-2352 forbids) and the
consequence (one forced re-login). Do not delete or rename the file — leave it for the user. Do not
attempt any migration of it; the cargo-pmcp file, which DOES record an issuer per entry, is the one
that gets a real migration, and that lands in core's `FileCredentialStore` (116-05) and in 116-13.

Write `tests/oauth_store_wiring.rs` against `InMemoryCredentialStore` for the key-shape behaviors and
against a `tempfile::tempdir()`-backed `FileCredentialStore` for the persistence behaviors. The
legacy-discard test must assert the file is NOT read — plant a syntactically valid legacy file with a
distinctive token and assert that token never appears in any result.
  </action>
  <verify>
    <automated>cargo nextest run --features full,oauth -E 'binary(oauth_store_wiring)' &amp;&amp; cargo nextest list --features full,oauth -E 'binary(oauth_store_wiring)' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest list --features full,oauth -E 'binary(oauth_store_wiring)'` reports a NON-ZERO count
    - `grep -n 'struct TokenCache' src/client/oauth.rs` returns nothing — the flat issuer-less cache type is gone
    - `grep -n 'oauth-tokens.json' src/client/oauth.rs` appears only in the discard warning path, never in a read path
    - The legacy-discard test plants a distinctive token in a legacy file and asserts it never surfaces
    - `grep -n 'default_credential_path' src/client/oauth.rs` shows no call inside `OAuthHelper::new`
    - `grep -n 'pub credential_store\|pub account' src/client/oauth.rs` returns nothing — no new public field on `OAuthConfig`
    - The cross-issuer miss test exists and its doc names SEP-2352
    - `cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157` reports ZERO major/breaking findings
    - `pmat quality-gate --fail-on-violation --checks complexity` reports 0 violations
  </acceptance_criteria>
  <done>Credentials are addressed by the AS that issued them, a platform can inject its own store, and the unkeyable legacy file is announced rather than silently trusted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Announce an authorization-server change, fatal by credential provenance</name>
  <files>src/client/oauth.rs, tests/oauth_store_wiring.rs</files>
  <read_first>
    - src/client/oauth.rs (as this plan's Task 1 left it — the store wiring and the point at which the effective issuer becomes known)
    - src/shared/credential_store.rs (as 116-05 left it — `record_issuer` / `last_issuer` with their default impls, and `normalize_server_key`)
    - src/error/mod.rs (as 116-02 left it — `Error::reauth_required(issuer, reason)` and `is_reauth_required` / `reauth_issuer`)
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md § "A4 — D-18's 'never a hard fail' is narrower than the spec" (the two spec sentences and the provenance distinction) and § "Code Examples → SEP-2352"
    - .planning/phases/116-auth-hardening-seps/116-CONTEXT.md § Deferred Ideas, the RFC 9728 entry (the spec specifies AS-change detection as "detected via updated protected resource metadata"; RFC 9728 is DEFERRED by owner decision, so detection here uses the provenance signal available today)
  </read_first>
  <behavior>
    - First connection to a given MCP server URL records the discovered issuer against `normalize_server_key(server_url)`
    - A second connection to the SAME server URL that discovers the SAME issuer produces no warning and no error
    - A second connection that discovers a DIFFERENT issuer, with `config.client_id == None` (DCR-issued credentials): emits a `tracing::warn!` naming BOTH the old and the new issuer and the server URL, then PROCEEDS to re-register and re-authorize — SEP-2352's "MUST NOT reuse ... and MUST re-register" is exactly this
    - A second connection that discovers a DIFFERENT issuer, with `config.client_id == Some(..)` (pre-registered credentials): returns `Err`, `err.is_reauth_required()` is true, `err.reauth_issuer()` names the NEW issuer, and the message names BOTH issuers and the server URL. The browser flow is NOT started
    - After a warned (DCR) change, the newly recorded last-seen issuer is the new one
    - An `OAuthHelper` using a store that does not override `record_issuer`/`last_issuer` (the default impls) still works: no warning, no error, no panic
  </behavior>
  <action>
Detect and announce an authorization-server substitution.

Once the effective issuer is known for a connection, call
`store.last_issuer(&normalize_server_key(server_url)?)`. On a change, branch on CREDENTIAL PROVENANCE —
this is RESEARCH A4's refinement of D-18, and it is what makes the AUTH-03 booking defensible against
the spec text, whose two adjacent sentences prescribe different remedies:

- **DCR-issued** (`config.client_id.is_none()`): warn and proceed. The spec's requirement for DCR
  credentials is "MUST NOT reuse client credentials from a different authorization server and MUST
  re-register with the new authorization server" — which issuer-keyed storage already accomplishes by
  missing the cache. The warning is what makes the automatic re-registration visible. Hard-failing here
  would convert a legitimate operational event (a tenant move, a provider migration) into an outage,
  which D-18 explicitly rejects.
- **Pre-registered** (`config.client_id.is_some()`): return `Error::reauth_required(new_issuer, ..)`.
  The spec says pre-registered credentials are inherently specific to one AS and that clients SHOULD
  surface an error rather than silently attempting to use mismatched ones. Silently re-running a
  browser login against an unexpected IdP with a client_id provisioned for a different one is the exact
  case the spec is warning about.

Both paths must name BOTH issuers and the server URL. Then record the new issuer so the next
connection compares against it.

Write down, in the rustdoc of whatever function performs this check, the DEPENDENCY the owner deferred:
the spec specifies AS-change detection as "detected via updated protected resource metadata" (RFC
9728), which pmcp does not implement — it derives the AS from the MCP base URL directly
(`src/client/oauth.rs:366-390`). RFC 9728 Protected Resource Metadata discovery is DEFERRED by owner
decision (2026-08-02), so this detection uses the provenance signal available today: the issuer that
discovery actually resolved for this server URL. State that plainly rather than implying spec parity.
The deferral and its named dependency are recorded again in 116-15's deferred-items file.

`record_issuer` / `last_issuer` have default impls returning `Ok(())` / `Ok(None)`, so a platform store
that does not implement them degrades to today's behavior rather than breaking. Cover that with a test
using a minimal hand-written implementor.
  </action>
  <verify>
    <automated>cargo nextest run --features full,oauth -E 'binary(oauth_store_wiring)' &amp;&amp; cargo nextest list --features full,oauth -E 'binary(oauth_store_wiring)' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All seven behavior rows above have a corresponding test, all passing
    - The pre-registered case asserts `err.is_reauth_required()` and `err.reauth_issuer()`, not a message substring
    - The pre-registered case asserts the browser/loopback path was NOT entered (no listener bound, or an equivalent observable)
    - The DCR case asserts the flow PROCEEDED and that the newly recorded issuer is the new one
    - Both change paths' messages contain BOTH issuer values and the server URL
    - The default-impl test uses a hand-written `CredentialStore` implementor overriding only `load`/`save`/`delete` and asserts no panic and no error
    - The rustdoc on the detection function names RFC 9728, states it is deferred by owner decision, and states what signal is used instead
    - `cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157` reports ZERO major/breaking findings
    - `pmat quality-gate --fail-on-violation --checks complexity` reports 0 violations
  </acceptance_criteria>
  <done>An authorization-server substitution is impossible to miss, and is fatal exactly where the spec asks for it and nowhere else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authorization server A credentials → authorization server B | The cross-AS replay SEP-2352 exists to prevent |
| MCP server URL → discovered issuer | A server that changes the AS it points at can walk a user through a login at an unexpected IdP |
| legacy on-disk token file → live credentials | An issuer-less token cannot be attributed to an AS without guessing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-39 | Elevation of Privilege | replaying a client_id or token issued by AS-A at AS-B | mitigate | Storage keyed by `(issuer, account)`; a different issuer is a cache miss by construction, and the DCR `client_id` lives in the same record so re-registration is automatic |
| T-116-40 | Spoofing | silent authorization-server substitution walking a user through an unexpected IdP | mitigate | D-18 detection naming both issuers; fatal `Error::reauth_required` for PRE-REGISTERED credentials (A4), warn-and-re-register for DCR credentials |
| T-116-41 | Spoofing | attributing an issuer-less legacy token to an authorization server by guessing | mitigate | The legacy file is never read; its presence is warned once and one re-login is required. Guessing is the exact thing SEP-2352 forbids |
| T-116-42 | Information Disclosure | credential store forced onto a home directory in an environment that has none | mitigate | `with_credential_store` injection plus lazy default resolution; `OAuthHelper::new` performs no filesystem access |
| T-116-43 | Repudiation | AS-change detection not matching the spec's stated mechanism | accept | RFC 9728 Protected Resource Metadata is DEFERRED by owner decision (2026-08-02). Detection uses the provenance signal available today; the gap is written into the detection function's rustdoc and into 116-15's deferred-items with a named owner, not buried |
| T-116-SC | Tampering | cargo installs | mitigate | Zero packages added |
</threat_model>

<verification>
- `cargo nextest run --features full,oauth -E 'binary(oauth_store_wiring)'` green with a non-zero count
- `cargo nextest run --features full,oauth -E 'binary(oauth_iss_integration)'` and `-E 'binary(oauth_dcr_integration)'` still green (116-09 and 116-10 unregressed)
- `make quality-gate` exits 0; `cargo clippy --features full,oauth --lib --tests` with pedantic+nursery clean
- `pmat quality-gate --fail-on-violation --checks complexity` 0 violations
- `cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157` — zero breaking findings
- `make doc-check` `^error` count within the recorded baseline
</verification>

<success_criteria>
- Credentials, including the DCR `client_id`, are keyed by `(issuer, account)` and re-registration on AS change is automatic
- The legacy issuer-less file is never read and its discard is announced once
- An AS change is named in full, and is fatal only for pre-registered credentials
- A platform can inject a store and the helper's constructor touches nothing
</success_criteria>

<output>
Create `.planning/phases/116-auth-hardening-seps/116-11-SUMMARY.md` when done
</output>
