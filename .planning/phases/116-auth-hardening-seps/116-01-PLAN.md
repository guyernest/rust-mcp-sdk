---
phase: 116-auth-hardening-seps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/116-auth-hardening-seps/116-BASELINES.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "A reader can re-derive every phase-end gate claim from a recorded, dated baseline instead of trusting it"
    - "The contract-first obligation is answered with a measured finding, not an assumption"
    - "The D-15 tripwire is OBSERVED to report violations on the four auth files before any read is bounded"
  artifacts:
    - path: ".planning/phases/116-auth-hardening-seps/116-BASELINES.md"
      provides: "Phase-base measurements: semver baseline, doc-check error count, full,oauth nextest A/B, wasm32 target, dependency fence, D-15 pre-fix violation list"
      min_lines: 60
  key_links:
    - from: ".planning/phases/116-auth-hardening-seps/116-BASELINES.md"
      to: "116-15-PLAN.md phase gate"
      via: "delta comparison at phase end"
      pattern: "b2bf9157"
---

<objective>
Establish the measured substrate every later plan in Phase 116 compares against, and answer the
contract-first question before a single source line is edited.

Purpose: RESEARCH Pitfalls 3, 4, 5, 7 and 9 all say the same thing — the obvious gate commands in
this repo either compile none of this phase's code, do not compile at all, silently select zero
tests, are already red, or answer against two disagreeing baselines. Without a dated baseline
recorded up front, the phase-end booking (D-20) cannot cite anything a future reader can re-derive.
Output: `116-BASELINES.md` — the phase's evidence anchor. No source files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-auth-hardening-seps/116-CONTEXT.md
@.planning/phases/116-auth-hardening-seps/116-RESEARCH.md
@.planning/phases/116-auth-hardening-seps/116-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Answer the contract-first question with a measured finding</name>
  <files>.planning/phases/116-auth-hardening-seps/116-BASELINES.md</files>
  <read_first>
    - CLAUDE.md (§ "Contract-First Development" — the mandate this task discharges)
    - contracts/mcp-protocol-sdk-v1.yaml (the in-repo contract this phase's surface would live in)
    - contracts/binding.yaml (the source-resolution bindings gate)
    - Makefile (grep `comply` — `make comply` runs inside `make quality-gate`)
  </read_first>
  <action>
Check BOTH candidate contract locations and record what is actually there.

1. `../provable-contracts/contracts/pmcp/` — the path CLAUDE.md names. Record whether the directory
   exists at all.
2. In-repo `contracts/` — the path `make comply` actually resolves (`contracts/binding.yaml`,
   `contracts/mcp-protocol-sdk-v1.yaml`, `contracts/team-servers-v1.yaml`).

For the in-repo contracts, grep case-insensitively for the auth surface tokens this phase touches:
`oauth`, `dcr`, `issuer`, `credential`, `application_type`, `auth_cmd`. Record the hit count per
file.

Write a `## Contract-First Finding` section into `116-BASELINES.md` stating, as a finding a reader
can re-derive: which directories exist, the per-file grep counts, and the resulting conclusion —
either (a) an auth contract exists and names the equations this phase must keep true, in which case
list them so later plans can cite them, or (b) no contract covers the OAuth client surface, in which
case state that this phase adds no contract equation and that `make comply` remains a pass-through
for it. Do NOT author a new contract; that is a separate decision outside this phase's scope.

Also record the exact `make comply` invocation used by `make quality-gate` (Makefile line numbers),
so a later plan citing "comply passed" names the same command.
  </action>
  <verify>
    <automated>test -f .planning/phases/116-auth-hardening-seps/116-BASELINES.md &amp;&amp; grep -q '^## Contract-First Finding' .planning/phases/116-auth-hardening-seps/116-BASELINES.md</automated>
  </verify>
  <acceptance_criteria>
    - `116-BASELINES.md` contains a `## Contract-First Finding` section
    - The section names both `../provable-contracts/contracts/pmcp/` and in-repo `contracts/` and states, for each, whether it exists
    - The section records a per-file grep count for the six tokens `oauth`, `dcr`, `issuer`, `credential`, `application_type`, `auth_cmd` across the in-repo `contracts/*.yaml` files
    - The section ends in one of the two enumerated conclusions (a) or (b), stated in a single explicit sentence
    - The section names the `make comply` invocation with its Makefile line number
    - No file under `contracts/` is modified: `git status --porcelain contracts/` is empty
  </acceptance_criteria>
  <done>A reader can answer "does a contract govern this phase's surface?" from the recorded finding without re-running anything.</done>
</task>

<task type="auto">
  <name>Task 2: Record the six phase-base gate measurements</name>
  <files>.planning/phases/116-auth-hardening-seps/116-BASELINES.md</files>
  <read_first>
    - .planning/phases/116-auth-hardening-seps/116-RESEARCH.md (Pitfalls 3, 4, 5, 7, 9; § Package Legitimacy Audit; § Environment Availability)
    - Makefile (targets `lint`, `test`, `doc-check`, `wasm-build`, `quality-gate`)
    - Cargo.toml (lines 200-220 — `full` does not contain `oauth`; `oauth = ["http-client", "dep:webbrowser", "dep:dirs", "dep:rand"]`)
  </read_first>
  <action>
Measure and record, under a `## Phase-Base Measurements` section, with the phase-base SHA
`b2bf91571236af2ee9f64c7b14608f3be4edf133` named in every command that takes a baseline. Each entry
records the exact command, the exit code, and the number a later plan will diff against.

1. **semver baseline.** `cargo semver-checks check-release -p pmcp --baseline-rev b2bf9157`. Record
   exit code and the pass/fail counts. State explicitly, per RESEARCH Pitfall 9, that this is the
   phase-base baseline and NOT the crates.io 2.17.0 baseline (which carries a pre-existing
   `#[deprecated]` failure on `OptimizedSseTransport` that is not this phase's).
2. **doc-check baseline.** `make doc-check`; count `^error` lines. RESEARCH measured exit 2 with 28
   errors, none in this phase's files. Record the actual count AND the per-file distribution, since
   `src/error/mod.rs` already carries 1 and this phase adds rustdoc there — the phase-end claim is a
   DELTA against this number, never "zero".
3. **The `full,oauth` A/B.** Run all three and record the selected-test counts:
   `cargo nextest list --features full -E 'binary(oauth_dcr_integration)'`,
   `cargo nextest list --features full,oauth -E 'binary(oauth_dcr_integration)'`,
   `cargo nextest list --features full -E 'binary(/oauth/)'`.
   RESEARCH measured 0, 5 and 0. Record the actual numbers and name the five tests. This is the
   evidence that `make quality-gate` alone proves nothing for this phase.
4. **`--features oauth` alone.** Confirm it still fails to build (RESEARCH measured 4 errors in
   `examples/s51_v2_tasks_agent.rs`). Record the failing example and error count. If it now builds,
   say so — the plans still use `full,oauth`, but the recorded reason changes.
5. **wasm32 target.** `rustup target add wasm32-unknown-unknown` then `make wasm-build`. Record exit
   code and warning count. RESEARCH assumption A5 did not probe this; this task closes it.
6. **Dependency fence.** `git diff --exit-code b2bf9157..HEAD -- Cargo.toml Cargo.lock`, plus
   `grep -rnE '^oauth2\s*=|^openidconnect\s*=' Cargo.toml` and
   `grep -rn 'oauth2::' cargo-pmcp/src/commands/`. Record that pmcp core is oauth2-free and that
   `cargo-pmcp`'s PRE-EXISTING direct `oauth2 = "5.0"` (`cargo-pmcp/Cargo.toml:84`) is confined to
   `cargo-pmcp/src/deployment/` — this is RESEARCH Pitfall 6, and the AUTH-03 booking must scope its
   "no oauth2 crate" claim to exactly this shape. Record whether `Cargo.lock` is tracked by git.

Do NOT run `make quality-gate` as part of this task if it exceeds the plan's time budget; instead
record RESEARCH assumption A2 (carried, not re-measured) verbatim as an open item that plan 116-15
must close.
  </action>
  <verify>
    <automated>grep -c 'b2bf9157' .planning/phases/116-auth-hardening-seps/116-BASELINES.md | grep -qv '^0$' &amp;&amp; grep -q '^## Phase-Base Measurements' .planning/phases/116-auth-hardening-seps/116-BASELINES.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Phase-Base Measurements` contains six numbered subsections matching items 1-6 above
    - Every recorded command string appears verbatim and includes `b2bf9157` where a baseline rev applies
    - The doc-check entry records BOTH a total `^error` count and a per-file distribution
    - The A/B entry records three numbers and names the individual tests selected under `--features full,oauth`
    - The wasm32 entry records the outcome of `rustup target add wasm32-unknown-unknown` and of `make wasm-build`, with exit codes
    - The dependency-fence entry states in one sentence the exact scope of the "no oauth2/openidconnect" claim, naming `cargo-pmcp/Cargo.toml:84` and `cargo-pmcp/src/deployment/`
    - No source file changed: `git status --porcelain src/ tests/ Cargo.toml` is empty
  </acceptance_criteria>
  <done>Every later plan can cite a dated, reproducible number rather than asserting one.</done>
</task>

<task type="auto">
  <name>Task 3: Observe the D-15 tripwire failing on the four auth files, then revert</name>
  <files>.planning/phases/116-auth-hardening-seps/116-BASELINES.md</files>
  <read_first>
    - tests/v2_bounded_reads_tripwire.rs (lines 1-130 for the module doc, `SHARED_DIR`, `EXTRA_SCOPE`, `REQUIRED_FILES`; lines 560-670 for `WHOLE_BODY_ALLOWLIST`, `bound_in_scope`, and the failure message)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/deferred-items.md (§ D-113-V — the four-file table, its counting method, the two named exclusions, and the stated reason the fence was not widened)
    - .planning/phases/116-auth-hardening-seps/116-PATTERNS.md (§ "tests/v2_bounded_reads_tripwire.rs — scope widening")
  </read_first>
  <action>
Phase 115's discipline: a fence that was never observed to fail is not evidence. Produce the
pre-fix violation list that plan 116-14 will later have to drive to zero.

Temporarily add these four paths to `EXTRA_SCOPE` in `tests/v2_bounded_reads_tripwire.rs`:
`src/client/auth.rs`, `src/client/oauth.rs`, `src/server/auth/providers/generic_oidc.rs`,
`src/server/auth/providers/cognito.rs`. Do NOT touch `REQUIRED_FILES`, `WHOLE_BODY_ALLOWLIST`, the
module doc, or the scanner in this task — this is a measurement, not the fix.

Run `cargo nextest run --features full,oauth -E 'binary(v2_bounded_reads_tripwire)'` and capture the
full assertion output. Record, under `## D-15 Pre-Fix Violation Baseline`: the complete violation
list verbatim (file:line plus the needle that matched), the per-file count, and the grand total.
State the counting-method caveat D-113-V records — its 31 is per read-SITE including rustfmt-split
chains, while a naive per-LINE grep measured 19 — and state plainly that neither number is the
closure condition: the closure condition is this same command reporting zero AFTER the fence is
permanently widened in 116-14.

Then `git checkout -- tests/v2_bounded_reads_tripwire.rs` and confirm the working tree diff for that
file is empty. The commit for this plan must contain ZERO bytes of change under `tests/`.
  </action>
  <verify>
    <automated>git diff --exit-code -- tests/v2_bounded_reads_tripwire.rs &amp;&amp; grep -q '^## D-15 Pre-Fix Violation Baseline' .planning/phases/116-auth-hardening-seps/116-BASELINES.md</automated>
  </verify>
  <acceptance_criteria>
    - `## D-15 Pre-Fix Violation Baseline` lists violations grouped by the four files, each entry carrying a `file:line` and the matched needle
    - A per-file count and a grand total are stated
    - The section states in one sentence that the closure condition is a zero report from `cargo nextest run --features full,oauth -E 'binary(v2_bounded_reads_tripwire)'` after 116-14's permanent widening, not any particular count
    - The section records that `WHOLE_BODY_ALLOWLIST` was NOT touched and that growing it is forbidden by its own written floor
    - `git diff --exit-code -- tests/v2_bounded_reads_tripwire.rs` exits 0 (the temporary widening was reverted)
    - `git status --porcelain tests/` is empty
  </acceptance_criteria>
  <done>The four auth files are proven, by observation rather than by transcription, to be reported by the existing scanner the moment the fence admits them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → phase-end booking | Unmeasured claims cross here and become `[x]` in REQUIREMENTS.md |
| repo → crates.io publish | A dependency added silently would cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-EV | Repudiation | D-20 booking evidence | mitigate | Every phase-end number is a delta against a dated, command-recorded baseline written by Task 2; `[x]` without a citable delta is not bookable |
| T-116-GATE | Tampering | `make quality-gate` as a proxy for correctness | mitigate | Task 2 item 3 records the measured A/B proving the gate compiles zero lines of this phase's code; all plans additionally run `--features full,oauth` |
| T-116-SC | Tampering | npm/pip/cargo installs | mitigate | Phase installs ZERO packages (RESEARCH § Package Legitimacy Audit). Task 2 item 6 records the inverse fence proving nothing was added; no `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `116-BASELINES.md` exists with all three sections and is committed
- Working tree carries no change under `src/`, `tests/`, `contracts/`, `Cargo.toml`
- Every recorded measurement names its exact command
</verification>

<success_criteria>
- A future reader can reproduce all six phase-base measurements from the recorded commands
- The contract-first question is answered with a finding, not an assumption
- The D-15 fence has been OBSERVED to report the four auth files, with the list captured
</success_criteria>

<output>
Create `.planning/phases/116-auth-hardening-seps/116-01-SUMMARY.md` when done
</output>

