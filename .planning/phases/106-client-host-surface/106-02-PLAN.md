---
phase: 106-client-host-surface
plan: 02
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - src/client/mod.rs
  - fuzz/fuzz_targets/client_host_dispatch.rs
  - fuzz/Cargo.toml
autonomous: true
requirements: [HOST-04, HOST-05]
must_haves:
  truths:
    - "The sampling approval callback is invoked before a completion is returned; the default (no callback registered) allows the completion"
    - "An approval callback returning Deny causes the sampling request to return a JSON-RPC error instead of a completion, and does not tear down the connection"
    - "ClientCapabilities advertised at initialize include sampling/elicitation/roots exactly when the corresponding host handler is registered, and omit them when it is not"
    - "A caller-supplied ClientCapabilities is merged with the registry, with the registry winning for the three host fields"
    - "Adversarial/malformed inbound sampling and elicitation params do not panic or hang the dispatch path"
  artifacts:
    - path: "src/client/mod.rs"
      provides: "approval invocation in dispatch_host_request + capability derivation in initialize"
      contains: "host_registry.approval"
    - path: "fuzz/fuzz_targets/client_host_dispatch.rs"
      provides: "fuzz target over inbound host-request params deserialization"
      contains: "CreateMessageParams"
    - path: "fuzz/Cargo.toml"
      provides: "registered client_host_dispatch fuzz binary"
      contains: "client_host_dispatch"
  key_links:
    - from: "src/client/mod.rs dispatch_host_request sampling path"
      to: "host_registry.approval"
      via: "invoke approval before returning completion"
      pattern: "approval"
    - from: "src/client/mod.rs initialize"
      to: "ClientCapabilities sampling/elicitation/roots"
      via: "derive from host_registry (registry wins)"
      pattern: "host_registry.(sampling|elicitation|roots)"
---

<objective>
Layer the human-in-the-loop approval hook (HOST-04) and capability derivation (HOST-05) onto the host dispatch built in plan 01, and add fuzz coverage for the inbound host-request deserialization path. Approval defaults to allow, can deny a request before a completion is returned, and denial surfaces as a JSON-RPC error rather than a connection failure. Advertised `ClientCapabilities` become truthful — derived from which host handlers are registered.

Purpose: HOST-04 is the host-side access-control seam consoles/approval flows plug into; HOST-05 makes capability advertisement match actual behavior. Fuzz satisfies the ALWAYS robustness requirement for the new inbound surface.
Output: Approval invocation + capability derivation in `src/client/mod.rs`, plus a registered `client_host_dispatch` fuzz target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-client-host-surface/106-CONTEXT.md
@.planning/phases/106-client-host-surface/106-RESEARCH.md
@.planning/phases/106-client-host-surface/106-PATTERNS.md
@.planning/phases/106-client-host-surface/106-01-SUMMARY.md

<interfaces>
<!-- Contracts from plan 01 + verified working-tree line numbers. -->

From plan 01 (src/client/host):
- `HostSamplingHandler`, `ApprovalCallback = Arc<dyn Fn(&CreateMessageParams) -> BoxFuture<'static, ApprovalDecision> + Send + Sync>`, `enum ApprovalDecision { Allow, Deny(String) }`.
- `ClientHostRegistry { sampling, elicitation, roots, approval }` (pub(crate) Option fields) held on `Client` as `host_registry`.
- `Client::dispatch_host_request(id, request) -> JSONRPCResponse` already routes sampling/elicitation/roots and returns -32601 for unhandled methods and -32603 for handler errors. This plan inserts the approval gate into its sampling path.

initialize (src/client/mod.rs:300-316): signature `pub async fn initialize(&mut self, capabilities: ClientCapabilities) -> Result<InitializeResult>`; line 308 stores `self.capabilities = Some(capabilities.clone())`; the InitializeRequest is built at :311-315 with `capabilities`.

ClientCapabilities (src/types/capabilities.rs:25-36): `#[non_exhaustive]` struct with `sampling: Option<SamplingCapabilities>`, `elicitation: Option<ElicitationCapabilities>`, `roots: Option<RootsCapabilities>` (all skip_serializing_if None; each sub-struct is Default-constructible).

JSONRPCError deny code: for approval Deny, return a JSON-RPC error response (reuse the create_response Error shape used in plan 01). Use -32603 (internal/refused) with the Deny reason string in `message`; do NOT tear down the transport (return a Response, not Err).

Fuzz analog (fuzz/fuzz_targets/fuzz_peer_handle.rs, ~26 lines): serde-boundary fuzz pattern. fuzz/Cargo.toml registers targets as `[[bin]] name=... path=fuzz_targets/... test=false doc=false bench=false` (existing entries at lines ~34-89, e.g. fuzz_peer_handle at :84-88).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke the sampling approval hook (default allow, deny -> JSON-RPC error)</name>
  <read_first>
    - src/client/mod.rs (the `dispatch_host_request` method created in plan 01 — the sampling routing branch is where the approval gate is inserted)
    - src/client/host/sampling.rs (ApprovalCallback + ApprovalDecision definitions from plan 01)
    - src/server/mod.rs (lines 1331-1350 — JSONRPCResponse Error shape to reuse for the Deny path)
    - .planning/phases/106-client-host-surface/106-RESEARCH.md (Pattern 3 + Open Question 3 — approve after the handler produces the CreateMessageResult, before returning it)
  </read_first>
  <action>
    In `dispatch_host_request`'s sampling branch: after the HostSamplingHandler produces a `CreateMessageResult`, if `self.host_registry.approval` is `Some(cb)`, invoke `cb(&params).await` and match the `ApprovalDecision`. On `Allow`, return the completion Result response as before. On `Deny(reason)`, return a JSON-RPC Error response (code -32603, `message` = the deny reason, `data: None`) using the same create_response Error shape — do NOT return Err and do NOT drop the transport. When `approval` is `None`, behave as default-allow (return the completion unchanged). Invoke approval only on the sampling path (not elicitation/roots) per HOST-04 scope. Keep the future boxed via `BoxFuture` (no tokio::spawn — wasm-clean).
    Unit tests: (a) with no approval callback registered, a registered sampling handler's result is returned unchanged (default allow); (b) with an approval callback that returns `Deny("nope")`, the response payload is a JSON-RPC Error with code -32603 and message containing "nope", and the handler's completion is NOT returned; (c) with an approval callback returning `Allow`, the completion is returned.
  </action>
  <verify>
    <automated>cargo test --lib client 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'host_registry.approval' src/client/mod.rs` succeeds.
    - Unit test: default (no callback) returns the handler completion — passes.
    - Unit test: `Deny("nope")` yields a ResponsePayload::Error with code -32603 and message containing "nope", and no completion — passes.
    - Unit test: `Allow` returns the completion — passes.
    - The Deny path returns a JSONRPCResponse (not a Rust Err) — asserted by the test observing a Response payload, and the receive loop is not torn down.
    - Approval is invoked only on the sampling branch (elicitation/roots tests from plan 01 still pass unchanged).
  </acceptance_criteria>
  <done>The sampling path consults the approval callback before returning a completion; default is allow, Deny returns a -32603 error response without dropping the connection.</done>
</task>

<task type="auto">
  <name>Task 2: Derive advertised ClientCapabilities from the host registry in initialize</name>
  <read_first>
    - src/client/mod.rs (lines 300-320 — initialize: the capability store at :308 and the InitializeRequest construction at :311-315)
    - src/types/capabilities.rs (lines 25-40 — ClientCapabilities fields sampling/elicitation/roots and their sub-structs' Default)
    - .planning/phases/106-client-host-surface/106-RESEARCH.md (Pitfall 4 — capability drift; registry wins on merge)
    - .planning/phases/106-client-host-surface/106-PATTERNS.md (Zone D — capability derivation)
  </read_first>
  <action>
    In `initialize`, before building/sending the InitializeRequest, take the caller-supplied `capabilities` as mutable and OR in the three host fields from `self.host_registry`: if `host_registry.sampling.is_some()` then `capabilities.sampling.get_or_insert_with(Default::default)`; same for `elicitation` and `roots`. The registry wins for these three fields (a registered handler always advertises its capability; an unregistered one leaves the caller's value untouched — do NOT remove a caller-set capability, only add when a handler is registered, per "registry reflects which handlers are registered"). Store and send the merged capabilities (update both `self.capabilities = Some(merged.clone())` at :308 and the InitializeRequest body). Do NOT add an independent capability setter and do NOT gate the inbound handler on server capabilities (that is the legacy create_message path's concern).
    Unit tests: (a) a Client built with `.on_sampling(mock)` then `initialize(ClientCapabilities::default())` sends capabilities where `sampling.is_some()`; (b) a Client with no sampling handler and default caps sends `sampling.is_none()`; (c) same coverage for elicitation and roots (registered => present, unregistered => absent). Assert against the capabilities stored on the client or captured from the outgoing InitializeRequest.
  </action>
  <verify>
    <automated>cargo test --lib client 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE 'host_registry.(sampling|elicitation|roots)' src/client/mod.rs` succeeds within the initialize path.
    - Unit test: registered sampling handler => advertised `sampling.is_some()` — passes.
    - Unit test: no sampling handler + default caps => `sampling.is_none()` — passes.
    - Unit tests for elicitation and roots (registered => present, unregistered => absent) — pass.
    - No independent public capability setter for these three fields was added.
    - Existing initialize tests remain green (`cargo test --lib client` overall green).
  </acceptance_criteria>
  <done>Advertised ClientCapabilities truthfully reflect which host handlers are registered; caller-supplied capabilities are merged with the registry winning for sampling/elicitation/roots.</done>
</task>

<task type="auto">
  <name>Task 3: Add the client_host_dispatch fuzz target for inbound host-request params</name>
  <read_first>
    - fuzz/fuzz_targets/fuzz_peer_handle.rs (full — the serde-boundary fuzz pattern to copy verbatim)
    - fuzz/Cargo.toml (lines 30-95 — existing [[bin]] entries; register the new target the same way)
    - src/types/sampling.rs (CreateMessageParams — the inbound sampling params type)
    - src/types/elicitation.rs (ElicitRequestParams — the inbound elicitation params type)
  </read_first>
  <action>
    Create `fuzz/fuzz_targets/client_host_dispatch.rs` mirroring `fuzz_peer_handle.rs`: `#![no_main]`, `use libfuzzer_sys::fuzz_target;`, parse the raw bytes into `serde_json::Value` (return early on failure), then attempt `from_value::<pmcp::types::sampling::CreateMessageParams>` and `from_value::<pmcp::types::elicitation::ElicitRequestParams>` on clones, asserting no panic/hang on adversarial JSON. Register the target in `fuzz/Cargo.toml` as a `[[bin]]` with `name = "client_host_dispatch"`, `path = "fuzz_targets/client_host_dispatch.rs"`, `test = false`, `doc = false`, `bench = false`, matching the existing entries. Confirm the fuzz crate builds the new target (build only; a full fuzz run is not required in CI).
  </action>
  <verify>
    <automated>cd fuzz && cargo build --bin client_host_dispatch 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `fuzz/fuzz_targets/client_host_dispatch.rs` exists and references both `CreateMessageParams` and `ElicitRequestParams`.
    - `fuzz/Cargo.toml` contains a `[[bin]]` entry named `client_host_dispatch` with `test = false`.
    - `cargo build --bin client_host_dispatch` (run inside `fuzz/`) succeeds.
    - A short local fuzz smoke run (`cargo fuzz run client_host_dispatch -- -runs=1000` if `cargo-fuzz` is installed) exits 0, or the executor records that cargo-fuzz was unavailable and only the build was verified.
  </acceptance_criteria>
  <done>A registered client_host_dispatch fuzz target exercises the inbound host-request param deserialization and builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> client (inbound sampling) | A server can push sampling requests that drive the host's LLM. The approval hook is the policy gate before a completion is returned. |
| server -> client (malformed params) | Server-controlled JSON is deserialized into typed params before dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-05 | Elevation of Privilege | server coerces client into an unapproved LLM call | mitigate | HOST-04 approval hook invoked before returning a completion (Task 1); Deny returns a -32603 error, default is allow (documented). This is the host-side access-control seam (ASVS V4). |
| T-106-06 | Spoofing/Repudiation | client advertises a capability it cannot honor (or hides one it can) | mitigate | HOST-05 derives sampling/elicitation/roots capabilities from the registry (Task 2) so advertisement matches actual behavior; no independent setter permitted. |
| T-106-07 | Denial of Service | malformed inbound sampling/elicitation params panic or hang the client | mitigate | Typed serde deserialization + the client_host_dispatch fuzz target (Task 3) proving no panic on adversarial JSON (ASVS V5). |
| T-106-08 | Denial of Service | approval callback denial tears down the connection | mitigate | Deny returns a JSON-RPC error Response (not Err); the receive loop continues — unit-tested in Task 1. |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — additive code within existing pmcp + fuzz crates (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test --lib client` green (approval + capability derivation unit tests).
- `cd fuzz && cargo build --bin client_host_dispatch` succeeds.
- Plan 01's duplex round-trips still pass unchanged (`cargo test --test client_host_roundtrip`).
- `make quality-gate` green before phase verification.
</verification>

<success_criteria>
- Sampling approval is consulted before a completion is returned; default allow, Deny -> -32603 error without dropping the connection.
- Advertised ClientCapabilities reflect exactly the registered host handlers.
- A fuzz target guards the inbound host-request deserialization path.
</success_criteria>

<output>
Create `.planning/phases/106-client-host-surface/106-02-SUMMARY.md` when done.
</output>
