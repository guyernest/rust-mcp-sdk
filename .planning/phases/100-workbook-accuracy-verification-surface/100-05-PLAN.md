---
phase: 100-workbook-accuracy-verification-surface
plan: 05
type: execute
wave: 5
depends_on: ["100-01", "100-02", "100-03", "100-04"]
files_modified:
  - crates/pmcp-server-toolkit/examples/workbook_table_authoring.rs
autonomous: true
requirements: [WBVER-04]
user_setup: []

must_haves:
  truths:
    - "One runnable example demonstrates render_workbook(filled), render_workbook(inputs_only), and verify_accuracy over the text+bool-bearing tax bundle, end-to-end"
    - "make quality-gate, make purity-check, and make doc-check are all green"
    - "PMAT cog-25 reports no new violations across the touched workbook crates"
    - "No regression to the EXISTING tool NAMES or the existing render_workbook / workbook:// wire behavior (the tool COUNT goes 5→6 by design — verify_accuracy is additive; assert names + render wire behavior, NOT an exact tools/list length)"
  artifacts:
    - path: "crates/pmcp-server-toolkit/examples/workbook_table_authoring.rs"
      provides: "ALWAYS example demonstrating all three WBVER capabilities over the tax bundle"
      contains: "verify_accuracy"
  key_links:
    - from: "the example"
      to: "render_workbook(filled) + render_workbook(inputs_only) + verify_accuracy"
      via: "tool calls over the tax-calc@1.1.0 bundle"
      pattern: "inputs_only"
---

<objective>
Final item (WBVER-04): the ALWAYS-coverage example leg plus the phase-level gate verification. Extend ONE existing workbook example (D-06: the tax bundle) to demonstrate all three new capabilities end-to-end — `render_workbook(filled)`, `render_workbook(inputs_only)`, and `verify_accuracy` — over the now text+bool-bearing tax-calc@1.1.0 bundle, in one cohesive narrative. Then confirm `make quality-gate` + `make purity-check` + `make doc-check` + PMAT cog-25 are all green and that no existing wire behavior regressed.

Purpose: Satisfy the CLAUDE.md ALWAYS bar (the `cargo run --example` leg — fuzz/property/unit already landed in Plans 02-04) and gate the phase as Toyota-Way clean.
Output: A runnable example covering the three capabilities + all phase gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-CONTEXT.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-RESEARCH.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-PATTERNS.md
@docs/design/2026-06-22-workbook-accuracy-verification-design.md

<interfaces>
<!-- Existing example: crates/pmcp-server-toolkit/examples/workbook_table_authoring.rs (~4.3K) over the tax-calc@1.1.0 bundle.
     Run with: cargo run --example workbook_table_authoring --features workbook-embedded -p pmcp-server-toolkit. -->
<!-- New surfaces from Plans 02-04: render_workbook accepts mode: "filled"|"inputs_only"; verify_accuracy is the 6th tool returning a ReconcileReport. -->
<!-- The example exercises the served tool handlers (or the in-process builder) the same way the existing example invokes render_workbook today. -->
<!-- No-regression contract: the tool COUNT increases 5→6 by design. Assert that the EXISTING tool NAMES are still present and the render_workbook/workbook:// wire behavior is unchanged — NOT an exact tools/list length, which would false-fail on the additive verify_accuracy. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the tax workbook example to demo filled + inputs_only + verify_accuracy</name>
  <files>crates/pmcp-server-toolkit/examples/workbook_table_authoring.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/examples/workbook_table_authoring.rs (the full existing example — how it loads the tax bundle, builds the server/handlers, and invokes render_workbook today)
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (RenderWorkbookHandler + VerifyAccuracyHandler — the surfaces to call; how `mode` is passed and how verify_accuracy is invoked)
    - crates/pmcp-server-toolkit/src/workbook/mod.rs lines 259-282 (the registered tools the example can drive)
    - 100-PATTERNS.md § "Tax fixture / Example analog (D-06)" (extend workbook_table_authoring.rs to demo the three capabilities over the tax bundle)
    - 100-CONTEXT.md D-06/D-07 (one cohesive narrative over the tax bundle, which now has a text + bool formula output) and § "Claude's Discretion" (honest framing)
    - docs/design/2026-06-22-workbook-accuracy-verification-design.md § 6 (the ALWAYS testing bar)
  </read_first>
  <action>
    Extend the existing `workbook_table_authoring.rs` example (do NOT create a new example file — D-06 says one bundle, one narrative) so its `main` additionally:
    1. Calls `render_workbook` with `mode: "filled"` and shows the returned `workbook://` URI (the existing behavior, now explicitly filled) — narrate that text/bool outputs now carry formula+cached-result (WBVER-01).
    2. Calls `render_workbook` with `mode: "inputs_only"` and shows the URI — narrate the double-entry download (bare formulas, Excel computes; WBVER-02).
    3. Calls `verify_accuracy` (no filter) and prints the ReconcileReport — narrate that all reference outputs (incl. the new text `bracket_label` and bool `is_taxable`) are within TOL, the compile-time penny-reconcile made inspectable (WBVER-03). Optionally show the D-03 unknown-filter Err and/or the per-row `cell` A1 addresses for illustration.

    Keep the example real-world and runnable end-to-end against the embedded tax-calc@1.1.0 bundle; print human-readable output. Use the same invocation pattern the existing example already uses to call render_workbook (do not invent a new harness). No SATD comments. The example must compile under the `workbook-embedded` feature.
  </action>
  <verify>
    <automated>cargo run --example workbook_table_authoring --features workbook-embedded -p pmcp-server-toolkit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the example contains a render_workbook call with `mode` = inputs_only, a render_workbook call with filled, and a verify_accuracy call (`grep -c 'inputs_only' && grep -c 'verify_accuracy'` ≥ 1 each in the example).
    - CLI: `cargo run --example workbook_table_authoring --features workbook-embedded -p pmcp-server-toolkit` exits 0 and prints (a) a filled URI, (b) an inputs_only URI, (c) a ReconcileReport with all_within_tol == true including the text+bool outputs.
    - BEHAVIOR: the verify_accuracy output in the example shows the text (`bracket_label`) and bool (`is_taxable`) reference outputs reconciling within tol (proving D-07 text/bool coverage end-to-end).
    - No SATD; the example builds clean under `workbook-embedded`.
  </acceptance_criteria>
  <done>One runnable example demonstrates render_workbook(filled), render_workbook(inputs_only), and verify_accuracy over the text+bool-bearing tax bundle, satisfying the ALWAYS `cargo run --example` leg.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — quality-gate + purity-check + doc-check + PMAT cog-25 green, no wire-behavior regression</name>
  <files>crates/pmcp-server-toolkit/examples/workbook_table_authoring.rs</files>
  <read_first>
    - CLAUDE.md § "Quality Gate Enforcement" + § "ALWAYS Requirements" (make quality-gate, the ALWAYS bar, cog-25, deny(panic), doctests on new public runtime fns)
    - Makefile targets `purity-check` (~line 505) and `doc-check` (~line 417) — the reader-free gate (PURITY_CRATES / PURITY_WRITER_CRATES) and doc gate
    - 100-RESEARCH.md § "Project Constraints (from CLAUDE.md)" and § "Validation Architecture → Sampling Rate" (phase gate = quality-gate + purity-check + doc-check + cog-25 + doctests)
    - crates/pmcp-server-toolkit/tests (the existing workbook_integration.rs / tools-list tests that assert the wire shapes — confirm no regression; the tool COUNT moves 5→6 by design)
  </read_first>
  <action>
    Run the full phase gate and resolve any failures introduced across Plans 01-05 (this is the consolidation/verification task — no new feature code beyond fixing gate failures):
    - `make quality-gate` (fmt --all, clippy pedantic+nursery, build, test, audit) — must pass. Fix any fmt/clippy/test failure surfaced across the touched crates.
    - `make purity-check` — the served runtime tree must stay reader-free (RenderMode + reconcile.rs + the writer changes introduce NO umya/quick-xml/calamine).
    - `make doc-check` — doctests (incl. the new reconcile_reference / seed_reference_inputs / RenderMode docs) and doc coverage pass.
    - PMAT cog-25: `pmat analyze complexity --format json --max-cognitive 25 | jq '.violations[] | select(.path | startswith("crates/pmcp-workbook-runtime/src") or startswith("crates/pmcp-server-toolkit/src/workbook"))'` reports NO new violation introduced by this phase. If a touched function regressed past cog-25, decompose it (do NOT add a blanket #[allow]).
    - Confirm NO regression to the EXISTING render_workbook / workbook:// wire BEHAVIOR: run the existing workbook integration / tools-list tests and confirm the EXISTING tool NAMES are unchanged and the render_workbook URI payload shape (for a no-mode / filled call) is unchanged. The tool COUNT goes 5→6 by design (verify_accuracy is additive) — assert on tool NAMES + render wire behavior, NOT an exact tools/list length. If an existing snapshot test hard-codes "exactly five tools" or an exact list length, update it to assert the existing five names are still present (subset) plus the additive verify_accuracy, so the additive sixth tool does not false-fail the snapshot.

    If a gate fails, fix the smallest root cause in the appropriate already-touched file (formatting in any of the Plan 01-05 files, a missed doc comment, a cog-25 decomposition, or a brittle exact-count snapshot) — keep changes confined to this phase's surface.
  </action>
  <verify>
    <automated>make quality-gate 2>&1 | tail -15 && make purity-check 2>&1 | tail -8 && make doc-check 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `make quality-gate` exits 0 (fmt/clippy/build/test/audit all pass).
    - CLI: `make purity-check` exits 0 (served runtime reader-free; RenderMode/reconcile/writer changes add no reader dep).
    - CLI: `make doc-check` exits 0 (doctests incl. reconcile_reference + seed_reference_inputs + RenderMode pass; doc coverage green).
    - PMAT: the cog-25 jq filter over the touched workbook crate paths returns an empty set (no new violation).
    - BEHAVIOR (no wire-behavior regression): the existing workbook integration / tools-list tests pass — the EXISTING tool NAMES and the render_workbook URI payload shape (no-mode/filled) are unchanged; mode default = filled and verify_accuracy are purely additive. Any prior "exactly five tools"/exact-length snapshot is updated to a names-subset + additive-sixth assertion (not an exact count).
  </acceptance_criteria>
  <done>make quality-gate + make purity-check + make doc-check are green, PMAT cog-25 shows no new violations across the touched workbook crates, and no existing render_workbook/workbook:// wire BEHAVIOR or tool NAME regressed (the additive 5→6 tool-count change does not false-fail any snapshot) — the phase is Toyota-Way clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example / gate run → built artifacts | The example and gates run trusted local code over the embedded fixture; no new external input boundary. The only adversarial surface (mode string, tool filter, URI) is hardened in Plans 03-04; this plan verifies those gates hold end-to-end. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-11 | Tampering | a reader dep sneaking into the served tree via the new code | mitigate | `make purity-check` (PURITY_CRATES / PURITY_WRITER_CRATES) is run as a phase gate and must be green — RenderMode/reconcile/writer changes are asserted reader-free. |
| T-100-12 | DoS | a cog-25 regression / panic path slipping through | mitigate | PMAT cog-25 + clippy deny(panic/unwrap/expect) run in quality-gate; the phase gate fails closed on any new violation. |
| T-100-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this phase; `make quality-gate` includes `cargo audit` over the unchanged dependency set. Not applicable. |
</threat_model>

<verification>
- `cargo run --example workbook_table_authoring --features workbook-embedded -p pmcp-server-toolkit` exits 0 demonstrating all three capabilities.
- `make quality-gate` + `make purity-check` + `make doc-check` all exit 0.
- PMAT cog-25 jq filter over the touched workbook crate paths is empty.
- Existing workbook integration / tools-list tests green (existing tool NAMES + render wire behavior unchanged; additive 5→6 count does not false-fail).
</verification>

<success_criteria>
- The ALWAYS `cargo run --example` leg is satisfied by one example demonstrating filled / inputs_only / verify_accuracy over the text+bool tax bundle; make quality-gate + purity-check + doc-check + PMAT cog-25 are all green; no existing render_workbook / workbook:// wire BEHAVIOR or tool NAME regressed and the additive 5→6 tool-count change false-fails no snapshot (WBVER-04).
</success_criteria>

<output>
Create `.planning/phases/100-workbook-accuracy-verification-surface/100-05-SUMMARY.md` when done
</output>
