---
phase: 100-workbook-accuracy-verification-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/manifest.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/cell_map.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/layout.json
  - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock
  - crates/pmcp-workbook-runtime/src/manifest_model.rs
  - crates/pmcp-server-toolkit/src/workbook/handler.rs
  - crates/pmcp-server-toolkit/src/workbook/mod.rs
  - crates/pmcp-workbook-runtime/src/render/mod.rs
autonomous: true
requirements: [WBVER-01, WBVER-02, WBVER-03, WBVER-04]
user_setup: []

must_haves:
  truths:
    - "The tax-calc@1.1.0 fixture has at least one text formula output cell and one boolean formula output cell, both with a non-null formula in layout.json"
    - "load_bundle boots the edited fixture without an integrity (hash mismatch) panic"
    - "RESERVED_TOOL_NAMES contains \"verify_accuracy\" and is length 5"
    - "An xlsx-XML extraction test helper exists that unzips an in-memory .xlsx buffer and returns a named sheet's XML string"
  artifacts:
    - path: "crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock"
      provides: "Re-folded integrity lock consistent with the edited 4 artifacts"
    - path: "crates/pmcp-workbook-runtime/src/manifest_model.rs"
      provides: "RESERVED_TOOL_NAMES bumped to [&str; 5] including verify_accuracy"
      contains: "verify_accuracy"
    - path: "crates/pmcp-workbook-runtime/src/render/mod.rs"
      provides: "sheet-XML extraction test helper for asserting <f>/<v> presence"
  key_links:
    - from: "fixture 4 JSON artifacts"
      to: "BUNDLE.lock"
      via: "build_bundle_lock / fold_evidence_hash re-fold"
      pattern: "build_bundle_lock"
    - from: "RESERVED_TOOL_NAMES (manifest_model.rs)"
      to: "handler.rs:672 H3 binding test"
      via: "reserved_tool_names_match_the_registered_meta_tool_names"
      pattern: "verify_accuracy"
---

<objective>
Wave-0 prerequisites for the three additive items. Lands the cross-cutting groundwork that Items 1-3 depend on but which does not itself implement a requirement end-to-end:

1. **Fixture extension (D-07):** add ONE text formula output cell and ONE boolean formula output cell to the `tax-calc@1.1.0` `Calculate_Tax` tool, then re-fold `BUNDLE.lock`. The existing tax oracle is all-numeric; WBVER-01 (text/bool formula re-verification) cannot be demonstrated or unit-tested without these cells.
2. **6th-tool registration prerequisites:** add `"verify_accuracy"` to the runtime-leaf `RESERVED_TOOL_NAMES`, update the H3 binding test, and flip the "five tools" → "six tools" doc/count in `workbook/mod.rs`. (The handler itself is added in Plan 04; this is the const/test/doc groundwork so Plan 04 does not have to re-touch the runtime leaf.)
3. **xlsx-XML test helper:** a `#[cfg(test)]` helper in `render/mod.rs` that unzips an in-memory `.xlsx` buffer and returns a sheet's XML, so WBVER-01/02 unit tests can assert `<f>`/`<v>` presence/absence. Current render tests only check the ZIP magic.

Purpose: De-risk the integrity-locked fixture edit and the load-bearing cross-crate constant up front, in one atomic wave, before the items that consume them run.
Output: An edited+re-folded fixture with text+bool formula outputs, `RESERVED_TOOL_NAMES` of length 5, and a reusable sheet-XML extraction test helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-CONTEXT.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-RESEARCH.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-PATTERNS.md
@docs/design/2026-06-22-workbook-accuracy-verification-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify executor support for chosen text/bool formulas, then extend the tax fixture with a text + a bool formula output and re-fold BUNDLE.lock</name>
  <files>crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/manifest.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/cell_map.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/layout.json, crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock</files>
  <read_first>
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/manifest.json (current output CellRoles; gross_income=60000, deductions=12000 defaults)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/cell_map.json (Calculate_Tax tool: outputs CellEntry list + oracle BTreeMap; taxable_income=48000, tax_owed=4800)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/executable.ir.json (existing output formula cells, e.g. 3_Outputs!B2..B5)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/layout.json (CellLayout shape; addr + formula fields for output cells)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock (current per-artifact + combined hashes)
    - crates/pmcp-workbook-runtime/src/artifact_model.rs lines 33-72 (CellEntry {json_key, seed_coord, unit}; Tool.oracle: BTreeMap<json_key, CellValue>) and lines 149,168 (fold_evidence_hash, build_bundle_lock — the re-fold helpers)
    - crates/pmcp-workbook-runtime/src/semantics.rs and crates/pmcp-workbook-runtime/src/scalar_eval.rs (CONFIRM apply/scalar_eval support IF(...) returning Text and comparison operators returning Bool — research Assumption A1)
    - 100-RESEARCH.md § "D-07 Resolution" (the five hash-linked artifacts + exact edit list) and § "Pitfall 2: Stale BUNDLE.lock"
    - 100-PATTERNS.md § "Tax fixture tax-calc@1.1.0 — text + bool formula output (D-06/D-07)" (the build_bundle_lock / fold_evidence_hash re-fold recipe at artifact_model.rs:149,168)
  </read_first>
  <action>
    FIRST verify (research Assumption A1, pattern-map flag): grep `semantics::apply` / `scalar_eval` to confirm the executor returns `CellValue::Text` for an `IF(cond,"a","b")` and `CellValue::Bool` for a comparison like `taxable_income>0`. If the chosen formula is unsupported, pick a supported equivalent BEFORE authoring cells — do not author a cell whose formula evaluates to `CellValue::Error`.

    Add TWO output cells to the `Calculate_Tax` tool, mirroring the existing 3_Outputs numeric outputs:
    - A **text** formula output, e.g. `bracket_label` at `3_Outputs!B6` with formula `IF(taxable_income>=40000,"bracket_2","bracket_1")` (or the supported equivalent). At the tier defaults taxable_income=48000 ⇒ oracle text = `"bracket_2"`.
    - A **boolean** formula output, e.g. `is_taxable` at `3_Outputs!B7` with formula `taxable_income>0`. At defaults ⇒ oracle bool = `true`.

    Edit all FOUR data artifacts hash-consistently (per 100-RESEARCH.md D-07 Resolution):
    1. `manifest.json` — add the two output `CellRole`s (`role:"output"`, one `dtype:"text"`, one `dtype:"bool"`).
    2. `executable.ir.json` — add the two `"Formula": {...}` cells referencing the existing taxable_income cell.
    3. `cell_map.json` — add the two new `CellEntry` (with `json_key` + `seed_coord` = the A1 addr, e.g. `3_Outputs!B6`) to `Calculate_Tax.outputs`, AND their oracle values to `Calculate_Tax.oracle` (`{"Text":"bracket_2"}` and `{"Bool":true}`).
    4. `layout.json` — add the two `CellLayout` cells with `formula` SET (non-null) — this is precisely what WBVER-01 proves (formula+cached-result emission).

    Then RE-FOLD `BUNDLE.lock`: the evidence hash folds `cell_map.json` via `fold_evidence_hash` (sort-by-path, length-prefixed); `build_bundle_lock(bundle_id, version, workbook_hash, ir_json, manifest_json, evidence_hash)` recomputes `h_exec`, `h_manifest`, `h_evidence`, and the combined hash. Compute the new lock by driving these runtime helpers (e.g. a throwaway `#[ignore]`-gated regenerate test, OR a one-off binary that reads the 4 edited JSONs and prints the folded lock). Do NOT hand-edit the hex by hand — drive the actual hashing helpers so the lock matches byte-for-byte. Keep `workbook_hash` unchanged (no source .xlsx is touched this round).

    Honor `deny(panic)` discipline in any generator code: use `?`, not unwrap/expect, on the value path.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook-embedded -- golden 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `cell_map.json` Calculate_Tax tool has two new outputs whose oracle entries are `{"Text": "bracket_2"}` and `{"Bool": true}` (or the chosen supported equivalents); `layout.json` has matching CellLayout cells with non-null `formula`.
    - BEHAVIOR: the existing "golden bundle boots" path (handler.rs:652 expect) does NOT panic — i.e. `load_bundle` integrity check passes against the re-folded `BUNDLE.lock`. Run: `cargo test -p pmcp-server-toolkit --features workbook-embedded golden` is green (no hash-mismatch panic).
    - BEHAVIOR: at the tier defaults the two new outputs compute to the authored oracle values (text=bracket_2, bool=true) — confirm via the existing executor/golden reconcile path; no `CellValue::Error`.
    - All five artifacts remain in the fixture dir; no source .xlsx added (workbook_hash unchanged).
  </acceptance_criteria>
  <done>The tax-calc@1.1.0 fixture carries one text formula output and one boolean formula output on Calculate_Tax, the BUNDLE.lock is re-folded via build_bundle_lock/fold_evidence_hash, and the bundle boots without an integrity panic.</done>
</task>

<task type="auto">
  <name>Task 2: Add verify_accuracy to RESERVED_TOOL_NAMES, update the H3 binding test, and flip the five→six tools doc/count</name>
  <files>crates/pmcp-workbook-runtime/src/manifest_model.rs, crates/pmcp-server-toolkit/src/workbook/handler.rs, crates/pmcp-server-toolkit/src/workbook/mod.rs</files>
  <read_first>
    - crates/pmcp-workbook-runtime/src/manifest_model.rs line 212 (`pub const RESERVED_TOOL_NAMES: [&str; 4] = ...` — the runtime-LEAF const the compiler reads without a toolkit dep)
    - crates/pmcp-server-toolkit/src/workbook/handler.rs lines 666-685 (the `reserved_tool_names_match_the_registered_meta_tool_names` H3 binding test asserting RESERVED_TOOL_NAMES == the registered meta-tool NAMEs)
    - crates/pmcp-server-toolkit/src/workbook/mod.rs lines 4-5, 152-153, 164-166, 259-282 (the "five served tools" / "all FIVE" / "register all five" doc strings + the tool_arc registration block)
    - 100-PATTERNS.md § "Cross-crate constant (do not hand-copy)" and § "mod.rs — register the 6th tool + fix five tools count"
    - 100-RESEARCH.md § "Doc/count drift to fix (string-level)" (the load-bearing H3 binding warning)
  </read_first>
  <action>
    In `manifest_model.rs:212` change `RESERVED_TOOL_NAMES: [&str; 4]` → `[&str; 5]` and add `"verify_accuracy"` (keep ordering consistent with how the H3 test lists the registered names — verify_accuracy is the 5th meta tool / 6th served tool). This const lives in the runtime leaf so the compiler's reserved-name gate reads it without a toolkit dependency — do NOT hand-copy it into the toolkit.

    In `handler.rs` H3 binding test (line ~672), add the expected name to the `registered` array so it stays equal to the new `RESERVED_TOOL_NAMES`. Do NOT yet reference a `VerifyAccuracyHandler::NAME` symbol (that handler is added in Plan 04) — use the string literal `"verify_accuracy"` here so this plan compiles standalone; Plan 04 will keep the test in sync when it lands the handler.

    In `mod.rs` flip the doc/count strings "five served tools" / "all FIVE served tools" / "register all five workbook tools" → six (lines ~4-5, 152-153, 164-166, 265). Do NOT add a `tool_arc` registration call yet (the handler does not exist until Plan 04); only update the prose/count. (Plan 04 adds the actual `.tool_arc(VerifyAccuracyHandler::NAME, ...)`.)

    Zero SATD comments; do not weaken the binding test.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook-embedded reserved_tool_names_match -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'verify_accuracy' crates/pmcp-workbook-runtime/src/manifest_model.rs` ≥ 1 and the array type is `[&str; 5]`.
    - SOURCE: `manifest_model.rs` RESERVED_TOOL_NAMES literal contains all of explain, get_manifest, diff_version, render_workbook, verify_accuracy.
    - BEHAVIOR: `cargo test -p pmcp-server-toolkit --features workbook-embedded reserved_tool_names_match_the_registered_meta_tool_names` passes (the H3 binding test array equals the 5-element const).
    - SOURCE: `mod.rs` no longer says "five" / "FIVE" for the served-tool count in the doc comments touched; says six.
    - The crate still compiles (`cargo build -p pmcp-server-toolkit --features workbook-embedded` succeeds) — no reference to a not-yet-existing VerifyAccuracyHandler symbol.
  </acceptance_criteria>
  <done>RESERVED_TOOL_NAMES is [&str; 5] including verify_accuracy, the H3 binding test passes against it, and mod.rs's five→six doc/count drift is fixed — without yet registering the handler (Plan 04 does that).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add a #[cfg(test)] sheet-XML extraction helper to render/mod.rs for asserting &lt;f&gt;/&lt;v&gt; presence</name>
  <files>crates/pmcp-workbook-runtime/src/render/mod.rs</files>
  <read_first>
    - crates/pmcp-workbook-runtime/src/render/mod.rs lines 509-525 (render_xlsx_produces_valid_zip_container + render_xlsx_is_deterministic_byte_identical — the current tests that only check the ZIP magic) and lines 632-666 (render_xlsx_writes_text_and_bool_and_falls_back_on_error_value — the existing text/bool VALUE test to which WBVER-01 will add an `<f>`/`<v>` assertion)
    - crates/pmcp-workbook-runtime/src/render/mod.rs lines 11-15 (the deliberate purity-gate relaxation: rust_xlsxwriter/zip are the ONLY writer deps allowed)
    - 100-VALIDATION.md § "Wave 0 Requirements" first bullet (the helper "unzip the in-memory buffer; current tests only check ZIP magic")
    - 100-RESEARCH.md § "Validation Architecture → Wave 0 Gaps" first bullet
  </read_first>
  <behavior>
    - Given a valid in-memory `.xlsx` `Vec<u8>` produced by `render_xlsx`, the helper unzips it and returns the XML string for a requested worksheet (e.g. `xl/worksheets/sheet1.xml`).
    - The returned XML for a Filled render of a formula cell contains both an `<f>` element (the formula) and a `<v>` element (the cached result).
    - The helper is `#[cfg(test)]` only and uses no panics on the value path beyond test-local `expect` (tests may use expect; the helper itself should surface failures as a `Result` or test-local expect — keep it total against well-formed buffers).
  </behavior>
  <action>
    Add a `#[cfg(test)]` helper (e.g. `fn extract_sheet_xml(buf: &[u8], sheet_path: &str) -> String`) that opens the buffer with the `zip` crate (already the writer's transitive dep / purity-allowed in render/mod.rs), reads the named entry (default `xl/worksheets/sheet1.xml`), and returns its UTF-8 string. Keep it inside the existing `#[cfg(test)] mod tests` block. This helper is consumed by Plan 02 (WBVER-01: assert `<f>`+`<v>` for text/bool) and Plan 03 (WBVER-02: assert `<f>` present, `<v>` absent for InputsOnly).

    Add ONE self-test that runs render_xlsx on the existing test layout, extracts sheet1 XML, and asserts a known numeric formula cell already emits `<f>` and `<v>` (proves the helper works against the proven numeric path before Items 1/2 rely on it). Do NOT assert text/bool here — that is Plan 02's job.

    Do NOT add zip/quick-xml to the NON-test dependency surface; this stays test-only so `make purity-check` is unaffected.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-runtime render:: -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: a `#[cfg(test)]` `extract_sheet_xml` (or equivalently named) helper exists in render/mod.rs and reads a worksheet entry from the in-memory buffer.
    - BEHAVIOR: the helper's self-test asserts `<f>` AND `<v>` are present for a numeric formula cell in the Filled render — `cargo test -p pmcp-workbook-runtime render::` is green.
    - PURITY: no new entry added to the runtime crate's non-test/`[dependencies]` surface; `make purity-check` still green (run it: `make purity-check`).
    - All pre-existing render:: tests still pass (no regression).
  </acceptance_criteria>
  <done>render/mod.rs has a test-only sheet-XML extraction helper, self-verified to detect &lt;f&gt;/&lt;v&gt; on a numeric formula cell, ready for Plans 02 and 03 to assert text/bool and InputsOnly behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture → bundle loader | The integrity-locked tax-calc@1.1.0 bundle is verified at boot via BUNDLE.lock; a stale/forged lock fails the load (fail-closed). |
| runtime-leaf const → compiler reserved-name gate | RESERVED_TOOL_NAMES is read cross-crate by the compiler; drift between it and the registered handlers is caught by the H3 binding test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | tax-calc@1.1.0 fixture artifacts vs BUNDLE.lock | mitigate | Re-fold the lock via build_bundle_lock/fold_evidence_hash (not by hand); the boot-time integrity check (handler.rs:652) is the assertion — a mismatched edit fails closed, never silently serves a tampered bundle. |
| T-100-02 | Tampering | RESERVED_TOOL_NAMES drift from registered handlers | mitigate | H3 binding test (handler.rs:672) asserts the const equals the registered meta-tool names; updating the const + test in one change keeps the compiler's reserved-name gate honest. |
| T-100-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (internal-only; zip/rust_xlsxwriter already vetted workspace deps). Not applicable — no [ASSUMED]/[SUS] package surface. |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features workbook-embedded golden` green (fixture boots).
- `cargo test -p pmcp-server-toolkit --features workbook-embedded reserved_tool_names_match` green (H3 binding).
- `cargo test -p pmcp-workbook-runtime render::` green (XML helper self-test).
- `make purity-check` green (no new reader deps).
</verification>

<success_criteria>
- Tax fixture has a text + a bool formula output on Calculate_Tax; BUNDLE.lock re-folded; bundle boots clean.
- RESERVED_TOOL_NAMES is `[&str; 5]` incl. verify_accuracy; H3 binding test green; mod.rs five→six doc/count fixed.
- A reusable test-only sheet-XML extraction helper exists and detects `<f>`/`<v>`.
</success_criteria>

<output>
Create `.planning/phases/100-workbook-accuracy-verification-surface/100-01-SUMMARY.md` when done
</output>
