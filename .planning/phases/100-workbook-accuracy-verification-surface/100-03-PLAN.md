---
phase: 100-workbook-accuracy-verification-surface
plan: 03
type: execute
wave: 3
depends_on: ["100-02"]
files_modified:
  - crates/pmcp-workbook-runtime/src/render/mod.rs
  - crates/pmcp-workbook-runtime/src/lib.rs
  - crates/pmcp-server-toolkit/src/workbook/render_uri.rs
  - crates/pmcp-server-toolkit/src/workbook/render_resource.rs
  - crates/pmcp-server-toolkit/src/workbook/handler.rs
  - crates/pmcp-server-toolkit/src/workbook/schema.rs
autonomous: true
requirements: [WBVER-02]
user_setup: []

must_haves:
  truths:
    - "render_workbook accepts an optional mode: filled | inputs_only (default filled); existing callers (no mode) are unaffected"
    - "InputsOnly renders formula cells as bare formulas with NO cached <v>; Filled keeps <f>+<v>"
    - "An unknown mode value surfaces as Err (never a panic)"
    - "A mode-carrying workbook:// URI round-trips and stays <= MAX_ENCODED_URI_LEN (64 KiB)"
    - "A workbook:// URI minted before this phase (no mode in payload) still decodes, defaulting to Filled"
    - "Each render mode is byte-deterministic across reads (per-mode determinism, not cross-mode equality)"
  artifacts:
    - path: "crates/pmcp-workbook-runtime/src/render/mod.rs"
      provides: "RenderMode { Filled, InputsOnly } enum + render_xlsx(layout, run, mode)"
      contains: "enum RenderMode"
    - path: "crates/pmcp-server-toolkit/src/workbook/render_uri.rs"
      provides: "mode field on DecodedRender/RenderPayload/RenderPayloadRef with #[serde(default)] back-compat"
      contains: "mode"
    - path: "crates/pmcp-server-toolkit/src/workbook/schema.rs"
      provides: "mode property on the render input schema (advertise == accept)"
  key_links:
    - from: "render_workbook handler (handler.rs:602)"
      to: "validate_input"
      via: "parse+strip mode BEFORE delegating (CalculateInput is deny_unknown_fields)"
      pattern: "mode"
    - from: "render_resource::regenerate (render_resource.rs:85)"
      to: "render_xlsx(layout, run, decoded.mode)"
      via: "thread decoded.mode into the writer"
      pattern: "render_xlsx"
---

<objective>
Item 2 (WBVER-02): give `render_workbook` an optional `mode: filled | inputs_only` argument (default `filled`, additive). `inputs_only` produces a clean double-entry copy — input cells seeded with the caller's values, formula cells written as BARE formulas with no `set_result` (the server contributes zero output values; Excel computes everything from scratch). Unknown mode → `Err`; the encoded `workbook://` URI stays ≤ 64 KiB and round-trips the mode; pre-phase URIs still decode (default Filled).

Threads a new `RenderMode { Filled, InputsOnly }` enum (runtime leaf) from the handler arg, through the `workbook://` URI payload, through `regenerate`, into `render_xlsx(layout, run, mode)`.

Purpose: Double-entry verification download — the BA can pull a formula-only workbook and let Excel be the oracle for arbitrary inputs.
Output: `RenderMode` enum + mode-threaded render + URI payload + handler arg + input schema; per-D-05 a clean copy (no highlighting/formatting/comments).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-CONTEXT.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-RESEARCH.md
@.planning/phases/100-workbook-accuracy-verification-surface/100-PATTERNS.md
@docs/design/2026-06-22-workbook-accuracy-verification-design.md

<interfaces>
<!-- render/mod.rs: render_xlsx(layout: &LayoutDescriptor, run: &RunResult) -> Result<Vec<u8>, RenderError> (line 227);
     threads run through render_sheet (252) -> write_cell (347) -> write_computed_value (371) -> write_formula_or_value (Plan 02).
     lib.rs:104 re-exports: pub use render::{CellLayout, LayoutDescriptor, SheetLayout, ...} -- add RenderMode here. -->
<!-- render_uri.rs: pub const MAX_ENCODED_URI_LEN: usize = 64*1024 (line 66); pub struct DecodedRender { dto, provenance } (75);
     struct RenderPayload { dto, provenance } (92, DECODE side); struct RenderPayloadRef<'a> { dto, provenance } (102, ENCODE side);
     pub fn encode(dto, provenance) (121); fn decode(...) checks MAX len FIRST (line 146). Proptests ~248-288: prop_encode_decode_identity, prop_decode_total, encode_is_deterministic (205). -->
<!-- render_resource.rs: fn regenerate(&self, uri) (85): decode -> provenance check -> validate_input -> run_bundle -> render_xlsx(layout, run). -->
<!-- handler.rs: RenderWorkbookHandler::compute (602): validate_input(args, manifest, cell_map) -> render_uri::encode(canonical_dto, stamp). -->
<!-- input.rs: CalculateInput is #[serde(deny_unknown_fields)] (line 47) -- a `mode` key is REJECTED; strip mode BEFORE validate_input. -->
<!-- schema.rs: fn assemble_input_schema (392) builds the top-level additionalProperties:false render envelope. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RenderMode enum + thread mode through render_xlsx (InputsOnly = bare formula, no set_result)</name>
  <files>crates/pmcp-workbook-runtime/src/render/mod.rs, crates/pmcp-workbook-runtime/src/lib.rs</files>
  <read_first>
    - crates/pmcp-workbook-runtime/src/render/mod.rs lines 227-251 (render_xlsx signature + init_workbook fixed-datetime pin at 239-245 — the determinism source) and 252-345 (render_sheet → write_cell threading of `run`)
    - crates/pmcp-workbook-runtime/src/render/mod.rs the write_formula_or_value helper added in Plan 02 (the single place set_result is applied)
    - crates/pmcp-workbook-runtime/src/lib.rs line ~104 (`pub use render::{...}` barrel — where LayoutDescriptor is re-exported)
    - 100-PATTERNS.md § "render/mod.rs Item 2" (add mode: RenderMode as 3rd param; thread it the same way `run` is threaded; InputsOnly = Formula::new(...) WITHOUT .set_result; non-formula cells write their value unchanged in BOTH modes so input seeding falls out for free; enum location beside LayoutDescriptor)
    - 100-RESEARCH.md § "Pattern 2: Mode-threaded render" + § "Pitfall 3" determinism note (init_workbook pins doc props to a fixed datetime; assert PER-MODE determinism, never cross-mode equality)
    - docs/design/2026-06-22-workbook-accuracy-verification-design.md § 4.3 (inputs-only double-entry mode)
  </read_first>
  <behavior>
    - render_xlsx(layout, run, RenderMode::Filled) is byte-identical to the pre-phase render_xlsx(layout, run) for the same inputs (no regression — Filled is the default).
    - render_xlsx(layout, run, RenderMode::InputsOnly): every formula cell emits `<f>` (bare formula) and NO cached `<v>`; input/non-formula cells emit their seeded value exactly as in Filled.
    - Each mode is byte-deterministic: two InputsOnly renders of the same input are byte-identical; two Filled renders are byte-identical. Filled and InputsOnly differ from each other by design (no cross-mode equality assertion).
    - RenderMode derives the serde set needed by the URI payload (Serialize + Deserialize) and is re-exported at the crate root.
  </behavior>
  <action>
    Define `pub enum RenderMode { Filled, InputsOnly }` in render/mod.rs beside LayoutDescriptor. Derive `Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize` and a serde rename so it serializes as `"filled"` / `"inputs_only"` (snake_case enum), and `Default` ⇒ `Filled` (or a manual Default impl) so absent-on-decode yields Filled. Re-export it from lib.rs alongside LayoutDescriptor.

    Add `mode: RenderMode` as the 3rd param of `render_xlsx` (line 227) and thread it through `render_sheet` → `write_cell` → `write_computed_value` → `write_formula_or_value`, mirroring how `run: &RunResult` is already threaded through those four. In `write_formula_or_value` (from Plan 02): when `mode == InputsOnly` and the cell has a formula, write `Formula::new(normalize_formula_for_writer(f))` WITHOUT `.set_result(...)` (bare formula). When `mode == Filled`, keep the set_result(cached) path. Non-formula cells write their value unchanged in BOTH modes — so InputsOnly's "seed inputs, bare formulas for the rest" falls out for free (the fixture's input cells have formula = null, research-verified).

    Per D-05: InputsOnly is a CLEAN copy — NO extra highlighting/formatting/comments. Do not add cell formats or comments; only omit set_result.

    Update ALL in-crate callers of render_xlsx to pass `RenderMode::Filled` (the existing render tests + any internal call), so the default path is byte-unchanged. Keep cog-25: the mode branch is one extra `match`/`if` arm inside the shared helper, not new combinatorics in write_computed_value.

    `deny(panic)`; doc-comment RenderMode and the new param (crate denies missing docs on public items).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-runtime render:: 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'enum RenderMode' crates/pmcp-workbook-runtime/src/render/mod.rs` == 1; lib.rs re-exports RenderMode (grep `RenderMode` in lib.rs ≥ 1).
    - BEHAVIOR (WBVER-02 InputsOnly unit test): a NEW render:: test renders the fixture in InputsOnly, extracts sheet XML via the Plan-01 helper, asserts every formula cell has `<f>` and NO `<v>`; and renders Filled, asserts `<f>`+`<v>`. Green.
    - BEHAVIOR (per-mode determinism): extend `render_xlsx_is_deterministic_byte_identical` (line 522) to assert two InputsOnly renders are byte-equal AND two Filled renders are byte-equal (do NOT assert Filled == InputsOnly). Green.
    - BEHAVIOR (no regression): `render_xlsx(layout, run, RenderMode::Filled)` matches the prior single-mode output — all pre-existing render:: tests pass with the Filled default.
    - COMPLEXITY: no new cog-25 violation on render_xlsx / write_computed_value / write_formula_or_value (`pmat analyze complexity`).
    - PURITY: `make purity-check` green.
  </acceptance_criteria>
  <done>RenderMode { Filled, InputsOnly } exists and is re-exported; render_xlsx threads mode; InputsOnly emits bare formulas (no `<v>`); Filled is byte-unchanged; both modes are per-mode deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry mode through the workbook:// URI payload with back-compat default</name>
  <files>crates/pmcp-server-toolkit/src/workbook/render_uri.rs, crates/pmcp-server-toolkit/src/workbook/render_resource.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/render_uri.rs lines 66-105 (MAX_ENCODED_URI_LEN; DecodedRender; RenderPayload [DECODE]; RenderPayloadRef [ENCODE]) and 121-160 (encode/decode; size guard FIRST at 146) and 205 (encode_is_deterministic) and 248-288 (prop_encode_decode_identity, prop_decode_total)
    - crates/pmcp-server-toolkit/src/workbook/render_resource.rs lines 85-108 (regenerate: decode → provenance check → validate_input → run_bundle → render_xlsx)
    - 100-PATTERNS.md § "render_uri.rs — mode in the URI payload" (add mode to all three structs; DECODE side #[serde(default)]; keep RenderPayloadRef field order stable for encode determinism; extend proptests) and § "render_resource.rs — thread mode into render_xlsx" (single-line change: pass decoded.mode)
    - 100-RESEARCH.md § "Item 2 plumbing detail" URI-size quantification (~+32 base64 bytes, ~0.05% of 64 KiB cap) + § "Pitfall 1" (#[serde(default)] back-compat; keep encode byte-deterministic)
  </read_first>
  <behavior>
    - encode(dto, provenance, mode) → decode(...) round-trips dto, provenance AND mode (per mode).
    - A payload JSON with NO `mode` key decodes to DecodedRender { mode: Filled, .. } (back-compat: pre-phase URIs).
    - encode is byte-deterministic for a fixed (dto, provenance, mode) — RenderPayloadRef field order unchanged.
    - A mode-carrying encoded URI length stays < MAX_ENCODED_URI_LEN.
    - decode stays TOTAL: arbitrary/garbage input never panics (prop_decode_total still holds).
    - regenerate passes decoded.mode into render_xlsx so the regenerated bytes reflect the chosen mode.
  </behavior>
  <action>
    In render_uri.rs add a `mode: RenderMode` field (using the runtime re-export) to `DecodedRender`, `RenderPayload`, and `RenderPayloadRef`. On the DECODE struct (`RenderPayload`) annotate `#[serde(default)]` so an absent `mode` deserializes to `RenderMode::default()` == Filled (Pitfall 1 back-compat). Keep `RenderPayloadRef` field ORDER stable (append `mode` LAST or keep existing order) so `encode_is_deterministic` (line 205) stays byte-identical. Change `encode` to take `mode: RenderMode` and write it into RenderPayloadRef; `decode` lifts it into DecodedRender. The MAX_ENCODED_URI_LEN size guard at line 146 is UNCHANGED and still runs FIRST.

    In render_resource.rs `regenerate` (line ~104) pass `decoded.mode` as the 3rd arg to `render_xlsx`. The provenance verification (90-96) and re-validation (98) are unchanged — `mode` is a render parameter, not a manifest input, so it is NOT re-validated.

    Extend the proptests (248-288): `prop_encode_decode_identity` — generate a RenderMode (both variants) and assert it round-trips; ADD an assertion that the encoded URI len < MAX_ENCODED_URI_LEN. Keep `prop_decode_total` (the fuzz surface) — `#[serde(default)]` keeps it total. Add a unit test that a hand-built payload WITHOUT a `mode` key decodes to Filled (explicit back-compat proof).

    `deny(panic/unwrap/expect)` on the value path.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook-embedded render_uri 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `mode` field present on DecodedRender, RenderPayload (with `#[serde(default)]`), and RenderPayloadRef in render_uri.rs; render_resource.rs regenerate passes `decoded.mode` to render_xlsx.
    - BEHAVIOR (WBVER-02 property): `prop_encode_decode_identity` round-trips mode AND asserts encoded len < MAX_ENCODED_URI_LEN; `prop_decode_total` still passes (totality). Green.
    - BEHAVIOR (back-compat unit): a payload minted without a `mode` key decodes to DecodedRender with mode == Filled. Green.
    - BEHAVIOR (determinism): `encode_is_deterministic` (line 205) still passes (RenderPayloadRef field order stable).
    - `cargo test -p pmcp-server-toolkit --features workbook-embedded render_uri` green; `cargo build -p pmcp-server-toolkit --features workbook-embedded` succeeds.
  </acceptance_criteria>
  <done>mode rides inside the workbook:// payload with #[serde(default)] back-compat; encode stays deterministic and under 64 KiB; regenerate reproduces the chosen mode; decode stays total.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parse the mode arg in render_workbook (strip before validate_input) + advertise it in the input schema</name>
  <files>crates/pmcp-server-toolkit/src/workbook/handler.rs, crates/pmcp-server-toolkit/src/workbook/schema.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/handler.rs lines 582-660 (RenderWorkbookHandler + compute at ~602: validate_input(args, manifest, cell_map) → render_uri::encode(canonical_dto, stamp)) and 114-130 (with_provenance, render_at_boundary — the boundary discipline)
    - crates/pmcp-server-toolkit/src/workbook/input.rs lines 45-58 (CalculateInput #[serde(deny_unknown_fields)] — a `mode` key is REJECTED) and 91+ (validate_input signature)
    - crates/pmcp-server-toolkit/src/workbook/schema.rs lines 392-426 (assemble_input_schema: top-level additionalProperties:false render envelope; override_props advertised at 400-406 — the "advertise == accept" precedent)
    - 100-PATTERNS.md § "handler.rs — mode parse (WBVER-02)" (the GOTCHA: lift args["mode"] out FIRST, default "filled", unknown → Err, then pass remainder to validate_input, then encode(dto, stamp, mode)) and § "schema.rs — mode input prop" (add `"mode": {"type":"string","enum":["filled","inputs_only"]}` so advertise == accept)
    - 100-RESEARCH.md § "Item 2 plumbing detail" (the deny_unknown_fields gotcha + the advertise==accept invariant) and § "Pitfall 4" (deny(panic) — unknown mode → Err, never panic)
  </read_first>
  <behavior>
    - render_workbook with no `mode` arg behaves exactly as before (default Filled) — existing callers unaffected.
    - render_workbook with `mode: "filled"` or `mode: "inputs_only"` encodes the chosen mode into the workbook:// URI.
    - render_workbook with an unknown `mode` (e.g. "foo") returns an Err (isError envelope at the boundary), NOT a panic, and NOT a validate_input deny_unknown_fields rejection of the remaining inputs.
    - The advertised render input schema lists `mode` as an optional string enum ["filled","inputs_only"] (advertise == accept).
  </behavior>
  <action>
    In RenderWorkbookHandler::compute, take `args` as a `Value`; lift `args["mode"]` out FIRST. Map it: absent/null → RenderMode::Filled; `"filled"` → Filled; `"inputs_only"` → InputsOnly; anything else → `Err(WorkbookToolError::invalid_input(...))` listing the valid values (use the existing invalid_input style at handler.rs:77). Remove the `mode` key from the args object, then pass the REMAINING `{inputs, overrides}` object to `validate_input` (so CalculateInput's deny_unknown_fields does not reject it). Then call the new `render_uri::encode(&validated.canonical_dto, &self.stamp, mode)` (3-arg). Keep the success payload + `with_provenance` + `render_at_boundary` boundary wrapping unchanged.

    In schema.rs `assemble_input_schema` (392) add a top-level `mode` property `{"type":"string","enum":["filled","inputs_only"]}` (optional, not in required) so the advertised schema matches what the handler accepts — the same advertise==accept reason override_props is advertised (lines 400-406). Keep `additionalProperties:false` (mode is now a known property).

    `deny(panic/unwrap/expect)`: use `.get`/`as_str`/`ok_or_else`; the parse must be total.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features workbook-embedded workbook::handler 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: render_workbook compute lifts `mode` from args BEFORE validate_input; schema.rs assemble_input_schema advertises `mode` with enum ["filled","inputs_only"].
    - BEHAVIOR (WBVER-02 unknown-mode unit): a NEW handler test calls render_workbook with `{"mode":"bogus", ...}` and asserts the result is an Err / isError envelope (never a panic). Green.
    - BEHAVIOR (happy path): render_workbook with `mode:"inputs_only"` produces a URI whose decoded payload carries InputsOnly; with no mode → Filled. (Assert via decode of the returned URI.) Green.
    - BEHAVIOR (no regression): render_workbook with NO mode arg still succeeds and produces a Filled URI identical in shape to pre-phase (existing render_workbook handler tests green).
    - BEHAVIOR (advertise==accept): the render input schema's `mode` enum equals what the handler accepts.
    - PURITY/COMPLEXITY: `make purity-check` green; no new cog-25 violation on compute (`pmat analyze complexity`).
  </acceptance_criteria>
  <done>render_workbook parses+strips mode before validate_input, encodes it into the URI, rejects unknown modes as Err, and advertises the optional mode enum; default (no-mode) behavior is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → render_workbook args | The `mode` string is attacker-controlled tool input; validated → Err on unknown value, never panic. |
| MCP client → workbook:// URI (resources/read) | The full URI is attacker-controlled; size-guarded FIRST (64 KiB), then totally decoded; the new `mode` field defaults to Filled on absent/garbage and is re-validated for provenance before render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-05 | Tampering / DoS | forged/oversized workbook:// URI carrying a malicious mode | mitigate | MAX_ENCODED_URI_LEN check runs FIRST in decode (render_uri.rs:146); decode is total (prop_decode_total); mode defaults to Filled on absent/garbage; provenance re-verified before render (render_resource.rs:90-96). |
| T-100-06 | Spoofing / Input-validation | unknown `mode` value reaching the writer | mitigate | Handler parses mode BEFORE validate_input and returns Err on any value outside {filled, inputs_only}; deny(panic) upheld (handler.rs:77 invalid_input style). |
| T-100-07 | Tampering | `mode` leaking into CalculateInput (deny_unknown_fields) and corrupting other tools | mitigate | mode is stripped from args before validate_input; it is NOT added to CalculateInput, so calculate/explain are unaffected; mode is a render parameter, not re-validated against the manifest. |
| T-100-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — internal change over already-vetted base64/serde. Not applicable. |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-runtime render::` green (RenderMode + InputsOnly + per-mode determinism).
- `cargo test -p pmcp-server-toolkit --features workbook-embedded render_uri` green (mode round-trip, size bound, back-compat default, decode totality).
- `cargo test -p pmcp-server-toolkit --features workbook-embedded workbook::handler` green (unknown-mode Err, happy path, no-mode regression).
- `make purity-check` green; PMAT cog-25 no new violations.
</verification>

<success_criteria>
- render_workbook accepts an optional, advertised `mode` (default filled); inputs_only emits bare formulas (no `<v>`), deterministically; unknown mode → Err; the URI round-trips mode and stays ≤ 64 KiB; pre-phase URIs still decode (Filled) — WBVER-02 fully delivered with no wire-shape regression.
</success_criteria>

<output>
Create `.planning/phases/100-workbook-accuracy-verification-surface/100-03-SUMMARY.md` when done
</output>
