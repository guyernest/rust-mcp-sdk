---
phase: 46-mcp-bridge-review-and-fixes
task: debugging
total_tasks: N/A
status: in_progress
last_updated: "2026-03-11T03:02:52.145Z"
---

<current_state>
Debugging why open-images MCP server at https://open-images.us-east.true-mcp.com/mcp
doesn't render MCP Apps UI in Claude Desktop. Compared against working server at ngrok.
Made SDK fixes, ran /simplify twice. All 735 tests pass. Changes NOT committed yet.
Need to rebuild open-images lambda and redeploy to test the fix in Claude Desktop.
</current_state>

<completed_work>

- Diagnosed missing flat `ui/resourceUri` key on tool `_meta` — Claude Desktop needs it
- Added `insert_legacy_resource_uri_key()` shared helper in `src/types/ui.rs`
- Updated `build_meta_map()` and `WidgetMeta::to_meta_map()` to emit dual keys
- Fixed `build_uri_to_tool_meta()` in `core.rs` — was producing `_meta: {}` on resources/list when no openai/* keys to propagate; now skips empty maps
- Added `_meta` with `ui.resourceUri` to resources/list in `simple_resources.rs`
- Migrated MIME type: `UIResourceContents::html()` now uses `text/html;profile=mcp-app`
- Added `html_mcp_app()` constructor, deprecated `html_mcp()`
- Added `inline_ext_apps_shim()` — replaces CDN ext-apps imports with inline App class
- Added import map in mcp-preview index.html to redirect CDN imports to local bundle
- Fixed Object.assign bug in shim (silent data loss for early hostContextChanged)
- Bounded rfind("import") to current line to prevent false matches
- Added CDN marker cross-reference comments between Rust and JS
- Fixed stale doc comments in ui.rs
- Updated 8+ tests across ui.rs, protocol.rs, mcp_apps.rs, core.rs
- Two rounds of /simplify — all issues addressed
</completed_work>

<remaining_work>

- Rebuild open-images lambda with updated SDK and redeploy to production
- Test in Claude Desktop to confirm UI renders
- If still not working: check protocol version (open-images uses 2024-11-05 vs ngrok uses 2025-03-26)
- If still not working: check if plain JSON vs SSE transport matters
- Commit the SDK changes once confirmed working
</remaining_work>

<decisions_made>

- Dual-emit ui/resourceUri flat key alongside nested ui.resourceUri — Claude Desktop and ChatGPT both need the flat key
- Shared insert_legacy_resource_uri_key() helper to avoid duplication
- Skip empty propagation maps in build_uri_to_tool_meta() to avoid _meta: {} on resources/list
- inline_ext_apps_shim replaces CDN import inline (server-side) for Claude Desktop; import map redirect (client-side) for mcp-preview
- Changed UIResourceContents::html() to use text/html;profile=mcp-app
</decisions_made>

<blockers>
- Cannot test without rebuilding and redeploying open-images lambda
- The open-images server is in a separate repo/workspace (pmcp-run)
</blockers>

<context>
Root issues found:
1. Missing flat ui/resourceUri key on tools _meta (Claude Desktop lookup)
2. Empty _meta: {} on resources/list (from empty propagation maps in core.rs)
3. text/html+mcp MIME type not recognized by Claude Desktop (now text/html;profile=mcp-app)

The working ngrok server (Node.js SDK) had both flat+nested keys and no _meta on resources/list.
All SDK changes are in working tree, 9 files modified, 735 tests passing.
</context>

<next_action>
1. Rebuild open-images lambda with updated SDK dependency
2. Deploy to AWS Lambda
3. Test in Claude Desktop — call search_images and check if widget renders
4. If working: commit SDK changes
</next_action>
