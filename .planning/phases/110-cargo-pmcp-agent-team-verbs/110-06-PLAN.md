---
phase: 110-cargo-pmcp-agent-team-verbs
plan: 06
type: execute
wave: 3
depends_on: [110-02, 110-05]
files_modified:
  - cargo-pmcp/src/lib.rs
  - cargo-pmcp/examples/agent_scaffold_and_run.rs
  - cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs
  - cargo-pmcp/fuzz/Cargo.toml
autonomous: true
requirements: [CLI-01, CLI-02, CLI-04]

must_haves:
  truths:
    - "cargo run -p cargo-pmcp --example agent_scaffold_and_run scaffolds an agent project AND drives a fixed-source AgentEngine loop to completion, fully offline (the CLAUDE.md ALWAYS EXAMPLE deliverable for the agent verbs)"
    - "cargo +nightly fuzz run fuzz_package_kind feeds arbitrary bytes to package show's kind-detection dispatch and never panics (the CLAUDE.md ALWAYS FUZZ deliverable for the untrusted-package parse path)"
    - "Both deliverables reach production code through narrow public lib seams, never the bin-only command layer"
  artifacts:
    - path: "cargo-pmcp/examples/agent_scaffold_and_run.rs"
      provides: "Runnable agent new + agent dev --source fixed demo over public seams"
      contains: "templates_agent::generate"
    - path: "cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs"
      provides: "libfuzzer target over package-show kind detection (untrusted bytes)"
      contains: "detect_kind"
    - path: "cargo-pmcp/src/lib.rs"
      provides: "templates_agent + package_kind public #[path] seams"
      contains: "templates_agent"
  key_links:
    - from: "cargo-pmcp/examples/agent_scaffold_and_run.rs"
      to: "cargo_pmcp::templates_agent::generate"
      via: "lib #[path] seam onto templates/agent.rs"
      pattern: "templates_agent::generate"
    - from: "cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs"
      to: "cargo_pmcp::package_kind::detect_kind"
      via: "lib #[path] seam onto commands/package/kind.rs"
      pattern: "package_kind::detect_kind"
---

<objective>
Ship the CLAUDE.md "ALWAYS Requirements for New Features" deliverables the Phase 110
verbs still lack: (1) a runnable `cargo run --example` demo for the agent verbs
(`agent new` scaffold + `agent dev --source fixed` offline loop), mirroring the
existing `workbook_server_scaffold.rs` / `workbook_cli_demo.rs` example precedent, and
(2) a fuzz target over `package show`'s kind-detection dispatch — a pure function over
untrusted bytes (the class already covered by `fuzz_iam_config.rs` / `fuzz_config_parse.rs`).
Both reach production code through narrow public lib `#[path]` seams (the established
`templates_workbook_server` / `workbook_explain` convention), never the bin-only
command layer.

Purpose: Close the ALWAYS-deliverable gap (BLOCKER: missing FUZZ/EXAMPLE) without
touching the verb handlers themselves — the example drives the emitter + engine
directly, the fuzz target drives the pure kind-detection function.
Output: One agent example + one fuzz target + the two public seams that expose them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-CONTEXT.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-RESEARCH.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-PATTERNS.md
@cargo-pmcp/examples/workbook_server_scaffold.rs
@cargo-pmcp/fuzz/fuzz_targets/fuzz_iam_config.rs
@crates/pmcp-agent/examples/s50_standalone_vs_sampled.rs

<interfaces>
<!-- Everything the executor needs; no exploration. -->
The two seams to add mirror the VERIFIED existing convention in cargo-pmcp/src/lib.rs:
  - `#[path = "templates/workbook_server.rs"] pub mod templates_workbook_server;`
  - `#[path = "commands/workbook/explain_surface.rs"] pub mod workbook_explain;`
  Add analogously:
  - `#[path = "templates/agent.rs"] pub mod templates_agent;`  (leaf from plan 110-02 —
    std::fs + format! + pmcp_package serialization only; NO `clap`/`GlobalFlags`, so it
    compiles in the lib target like `templates_workbook_server`).
  - `#[path = "commands/package/kind.rs"] pub mod package_kind;` (leaf from plan 110-05 —
    pure `detect_kind` over `&str` against `pmcp_package::oci::media_types::*` constants; NO
    `clap`/`GlobalFlags`/`OciLayout`, so it is lib-safe).

Public pmcp-agent surface the example uses (verified in crates/pmcp-agent/src/lib.rs):
  - `use pmcp_agent::{AgentEngine, InMemoryStore, RunOutcome};`
  - `use pmcp_agent::{CompletionSource, ToolInvoker};` (public seam traits — the example
    defines a tiny inline end-turn `CompletionSource` + a no-op `ToolInvoker`; the s50
    `DemoInvoker`/`ScriptedSource` are example-local and NOT exported, so re-implement the
    minimal shapes inline against the public traits).
  - `use pmcp_agent::config::ResolvedAgentConfig;` (see s50 `run_standalone` for the ctor args).
  - `AgentEngine::new(source, invoker, InMemoryStore::new(), config).run("example-run").await -> RunOutcome`.

The pure kind-detection fn the fuzz target drives (created in plan 110-05, kind.rs):
  - `cargo_pmcp::package_kind::detect_kind(artifact_or_media_type: &str) -> Option<PackageKind>`
    matching `pmcp_package::oci::media_types::{MT_AGENT_CONFIG, MT_TEAM_CONFIG,
    MT_SERVER_ENVELOPE, MT_WORKFLOW_MANIFEST, ARTIFACT_TYPE_AGENT, ARTIFACT_TYPE_TEAM,
    ARTIFACT_TYPE_SERVER, ARTIFACT_TYPE_WORKFLOW}`.

fuzz/Cargo.toml `[[bin]]` block shape (verified — mirror an existing entry exactly):
  `[[bin]]\nname = "fuzz_package_kind"\npath = "fuzz_targets/fuzz_package_kind.rs"\ndoc = false\ntest = false\nbench = false`
  The fuzz crate already depends on `cargo-pmcp = { path = ".." }`; the seam re-export makes
  `detect_kind` reachable with no new fuzz-crate dependency.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add templates_agent + package_kind lib seams and the agent example</name>
  <files>cargo-pmcp/src/lib.rs, cargo-pmcp/examples/agent_scaffold_and_run.rs</files>
  <read_first>
    - cargo-pmcp/src/lib.rs (the `templates_workbook_server` + `workbook_explain` `#[path]` seam blocks to mirror, incl. their doc comments)
    - cargo-pmcp/examples/workbook_server_scaffold.rs (the scaffold-via-public-seam example shape)
    - crates/pmcp-agent/examples/s50_standalone_vs_sampled.rs (the `run_standalone` fixed-source loop body to lift inline)
    - cargo-pmcp/src/templates/agent.rs (the `generate(dir, name)` signature from plan 110-02)
  </read_first>
  <action>
    In lib.rs, add two `#[path]` seams mirroring the existing `templates_workbook_server`
    block (same doc-comment rationale style — leaf module, dependency-light, reached by an
    example/fuzz target NOT the bin-only tree):
    - `#[path = "templates/agent.rs"] pub mod templates_agent;`
    - `#[path = "commands/package/kind.rs"] pub mod package_kind;`
    Create examples/agent_scaffold_and_run.rs mirroring workbook_server_scaffold.rs: in an
    auto-cleaned `tempfile::tempdir()`, `fs::create_dir_all(dir.join("src"))`, then
    `cargo_pmcp::templates_agent::generate(&dir, "demo_agent")?` and print the emitted file
    tree (reuse the `print_tree` walkdir helper shape). THEN demonstrate `agent dev --source
    fixed` fully offline: define a tiny inline end-turn `CompletionSource` (returns an
    end-turn completion, no network) and a no-op `ToolInvoker` (records + returns ok) against
    the PUBLIC pmcp_agent traits, build `ResolvedAgentConfig::new(...)` (s50 args) +
    `AgentEngine::new(source, invoker, InMemoryStore::new(), config)`, `.run("example-run").await`,
    and print the terminal `RunOutcome`. Header doc-comment MUST state it satisfies the
    CLAUDE.md ALWAYS EXAMPLE requirement for CLI-01/CLI-02 and give the run command
    `cargo run -p cargo-pmcp --example agent_scaffold_and_run`. `#[tokio::main]` main (the loop is async).
    Do NOT reference the bin-only `templates::*` or `commands::*` paths — only the public seam + pmcp_agent.
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp --example agent_scaffold_and_run && cargo run -p cargo-pmcp --example agent_scaffold_and_run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs contains `#[path = "templates/agent.rs"] pub mod templates_agent;` and `#[path = "commands/package/kind.rs"] pub mod package_kind;`
    - examples/agent_scaffold_and_run.rs contains `templates_agent::generate` and drives `AgentEngine::...run(`
    - the example performs NO network I/O (inline fixed end-turn source + no-op invoker)
    - `cargo run -p cargo-pmcp --example agent_scaffold_and_run` exits 0 and prints a terminal RunOutcome
  </acceptance_criteria>
  <done>The agent verbs have a runnable end-to-end example (scaffold + offline fixed-source loop) reached through the public lib seam; ALWAYS EXAMPLE requirement satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Add fuzz_package_kind target over the kind-detection dispatch</name>
  <files>cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs, cargo-pmcp/fuzz/Cargo.toml</files>
  <read_first>
    - cargo-pmcp/fuzz/fuzz_targets/fuzz_iam_config.rs (the `#![no_main]` + utf8-guard + never-panic target shape)
    - cargo-pmcp/fuzz/Cargo.toml (the `[[bin]]` block form to append)
    - cargo-pmcp/src/commands/package/kind.rs (the `detect_kind` signature from plan 110-05)
  </read_first>
  <action>
    Create fuzz_targets/fuzz_package_kind.rs mirroring fuzz_iam_config.rs: `#![no_main]`,
    `use libfuzzer_sys::fuzz_target;`, then `fuzz_target!(|data: &[u8]| { let Ok(s) =
    std::str::from_utf8(data) else { return; }; let _ = cargo_pmcp::package_kind::detect_kind(s); })`.
    The invariant under test: `detect_kind` never panics on arbitrary/adversarial media-type
    strings drawn from an untrusted `.pmcp` package's manifest (threat T-110-05-03 — kind
    mis-detection / parser confusion). Header doc-comment names the threat and gives the run
    command `cargo +nightly fuzz run fuzz_package_kind` + the `-max_total_time=60` smoke form,
    and states it satisfies the CLAUDE.md ALWAYS FUZZ requirement for the package-show untrusted
    parse path.
    In fuzz/Cargo.toml, append a `[[bin]]` block for `fuzz_package_kind` mirroring the existing
    entries (`name`/`path`/`doc = false`/`test = false`/`bench = false`). No new dependency — the
    `cargo-pmcp` path dep + the `package_kind` seam already expose `detect_kind`.
  </action>
  <verify>
    <automated>grep -q 'name = "fuzz_package_kind"' cargo-pmcp/fuzz/Cargo.toml && grep -q 'package_kind::detect_kind' cargo-pmcp/fuzz/fuzz_targets/fuzz_package_kind.rs && cargo build --manifest-path cargo-pmcp/fuzz/Cargo.toml --bin fuzz_package_kind 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - fuzz_targets/fuzz_package_kind.rs contains `#![no_main]`, the utf8 guard, and `package_kind::detect_kind`
    - fuzz/Cargo.toml contains a `[[bin]]` block named `fuzz_package_kind`
    - `cargo build --manifest-path cargo-pmcp/fuzz/Cargo.toml --bin fuzz_package_kind` compiles (libfuzzer target links)
  </acceptance_criteria>
  <done>The untrusted package-show kind-detection dispatch has a libfuzzer target that never panics on arbitrary bytes; ALWAYS FUZZ requirement satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted `.pmcp` manifest → detect_kind | Adversarial media-type/artifact-type strings from a foreign package cross into kind detection |
| example scaffold → filesystem | Emitter writes into an auto-cleaned tempdir only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-06-01 | Denial of Service | `detect_kind` on adversarial media-type bytes | mitigate | libfuzzer `fuzz_package_kind` proves the pure dispatch never panics/hangs on arbitrary input (utf8-guarded), covering package show's untrusted-parse boundary (mirrors T-76-03 fuzz precedent) |
| T-110-06-02 | Tampering | example filesystem writes | accept | Example writes only into a `tempfile::tempdir()` that auto-cleans; no user path input, no network |
| T-110-06-SC | Tampering | cargo dependency additions | accept | No new registry packages — both seams re-mount in-repo leaves; the fuzz crate reuses its existing `cargo-pmcp` path dep. No slopcheck applies. |
</threat_model>

<verification>
- `cargo run -p cargo-pmcp --example agent_scaffold_and_run` — scaffold + offline loop demo green.
- `cargo build --manifest-path cargo-pmcp/fuzz/Cargo.toml --bin fuzz_package_kind` — fuzz target links.
- `cargo +nightly fuzz run fuzz_package_kind -- -max_total_time=60` — smoke fuzz finds no panic (native/nightly, optional in CI).
- `make quality-gate` before commit (fmt/clippy/build/test).
</verification>

<success_criteria>
- The agent verbs have a runnable `cargo run --example` demo (scaffold + fixed-source loop), offline.
- `package show` kind detection has a libfuzzer target over untrusted bytes.
- Both reach production code through public lib `#[path]` seams, not the bin-only command tree.
- ALWAYS EXAMPLE + ALWAYS FUZZ deliverables for Phase 110 are satisfied.
</success_criteria>

<output>
Create `.planning/phases/110-cargo-pmcp-agent-team-verbs/110-06-SUMMARY.md` when done
</output>
</content>
</invoke>
