---
phase: 110-cargo-pmcp-agent-team-verbs
plan: 02
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - cargo-pmcp/src/commands/agent/new.rs
  - cargo-pmcp/src/templates/agent.rs
  - cargo-pmcp/src/templates/mod.rs
  - cargo-pmcp/src/commands/new.rs
  - cargo-pmcp/tests/scaffold_agent.rs
autonomous: true
requirements: [CLI-01]

must_haves:
  truths:
    - "cargo pmcp agent new <name> scaffolds an agent project with an AgentPackage JSON manifest, a standalone runner, and a tripwire test"
    - "The emitted manifest round-trips through pmcp_package::AgentPackage"
    - "The scaffold's pmcp-agent pin cannot silently drift from the workspace crate version"
  artifacts:
    - path: "cargo-pmcp/src/templates/agent.rs"
      provides: "Agent scaffold emitter + PMCP_AGENT_VERSION drift-guard"
      contains: "PMCP_AGENT_VERSION"
    - path: "cargo-pmcp/src/commands/agent/new.rs"
      provides: "agent new handler: validate name then emit scaffold"
      contains: "validate_crate_name"
    - path: "cargo-pmcp/tests/scaffold_agent.rs"
      provides: "CLI-01 scaffold integration test against the real binary"
      exports: []
  key_links:
    - from: "cargo-pmcp/src/commands/agent/new.rs"
      to: "templates::agent::generate"
      via: "scaffolder delegation after validate_crate_name"
      pattern: "templates::agent::generate"
    - from: "cargo-pmcp/src/templates/agent.rs"
      to: "crates/pmcp-agent/Cargo.toml"
      via: "include_str! drift-guard test"
      pattern: "include_str!.*pmcp-agent/Cargo.toml"
---

<objective>
Implement `cargo pmcp agent new <name>` (CLI-01): a scaffolder that emits an agent
project — an `AgentPackage` JSON manifest, a standalone runner over `AgentEngine`,
a `Cargo.toml` pinning `pmcp-agent`, and a `tests/pin.rs` tripwire in the generated
project — plus an internal template drift-guard so the emitted pin cannot drift from
the released `pmcp-agent` version (D-05). Reuses the existing scaffolding engine in
`cargo-pmcp/src/templates/` and `validate_crate_name` from `commands/new.rs` (D-01a).
Fills the plan-110-01 stub in agent/new.rs.

Purpose: cargo-pmcp becomes the agent on-ramp, matching its server-scaffold story.
Output: A dedicated `templates/agent.rs` emitter + wired `agent new` handler + scaffold test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-CONTEXT.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-RESEARCH.md
@.planning/phases/110-cargo-pmcp-agent-team-verbs/110-PATTERNS.md
@cargo-pmcp/src/templates/workbook_server.rs
@cargo-pmcp/src/commands/new.rs
@crates/pmcp-agent/examples/s50_standalone_vs_sampled.rs

<interfaces>
<!-- Contracts the executor uses directly — no exploration needed. -->
From cargo-pmcp/src/commands/new.rs (verified):
  - `fn validate_crate_name(name: &str) -> Result<()>` at line 119 — currently PRIVATE.
    Promote to `pub(crate) fn validate_crate_name`. Hardened path-traversal + Cargo-name guard
    (rejects empty, leading digit, `/`, `\`, `..`, illegal chars). REUSE — do not re-implement.
  - `execute_sql_server(workspace_dir, name, global_flags)` (line ~149) is the scaffolder shape:
    validate_crate_name(name)? → fs::create_dir_all(dir.join("src")) → templates::*::generate(dir, name)
    → gated `println!` next-steps behind `global_flags.should_output()`.

From cargo-pmcp/src/templates/workbook_server.rs (the emitter + drift-guard to mirror):
  - `pub fn generate(dir: &Path, name: &str) -> Result<()>` orchestrates per-file `generate_*` fns,
    each a raw `fs::write(dir.join(...), format!(...)).context(...)` (NO template engine; escape braces `{{`/`}}`).
  - Drift-guard: `const PMCP_VERSION: &str = "2.17.0";` + `#[test] fn emitted_pmcp_version_matches_workspace_pin()`
    that `include_str!("../../../Cargo.toml")`, parses `["package"]["version"]`, and `assert_eq!`.

From crates/pmcp-package (AgentPackage struct the emitted manifest MUST round-trip through):
  - `use pmcp_package::{AgentPackage, ConfigSlot, SlotType};`
  - Fields: name:String, version:semver::Version, instructions:String,
    llm: ConfigSlot { slot: SlotType::LlmProvider { name, tested_value } },
    max_tokens:i64, max_iterations:i64, connectors:vec![], tool_selection:None,
    input_schema:None, output_schema:None, importance:None, finalizer_role:None, budget_defaults:vec![].
    (exact literal in s50_standalone_vs_sampled.rs `agent_package()`.)

From crates/pmcp-agent (runner target):
  - `AgentEngine::new(source, invoker, InMemoryStore::new(), config)` + `.run("...").await` (s50 run_standalone path).
  - `OpenAiCompatSource::new(endpoint, model, SecretString::new(key))` (feature "openai-compat").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold integration test against the real binary (RED)</name>
  <files>cargo-pmcp/tests/scaffold_agent.rs</files>
  <behavior>
    - Running the real cargo-pmcp binary with `agent new <name>` in a tempdir creates
      `<name>/Cargo.toml`, `<name>/src/main.rs`, an AgentPackage manifest file, and `<name>/tests/pin.rs`.
    - The emitted manifest file parses as `pmcp_package::AgentPackage` (round-trips).
    - The emitted Cargo.toml pins `pmcp-agent`.
    - Test fails RED now (agent new is still the 110-01 bail! stub).
  </behavior>
  <read_first>
    - cargo-pmcp/tests/scaffold_sql_server.rs (the CLI-acceptance-against-real-binary shape)
    - cargo-pmcp/tests/support/scaffold_patch.rs (append_crates_io_patch, ChildGuard — for resolving unpublished workspace deps)
  </read_first>
  <action>
    Mirror scaffold_sql_server.rs: invoke the REAL built binary via `env!("CARGO_BIN_EXE_cargo-pmcp")`
    (do NOT weaken to an in-process call) inside a `tempfile::tempdir()`, running `agent new <name>`.
    Reuse `tests/support/scaffold_patch.rs::append_crates_io_patch` for the `[patch.crates-io]` block
    that resolves the unpublished workspace deps, and `ChildGuard` for cleanup. Assert the four emitted
    files exist, that the manifest file deserializes via `serde_json::from_str::<pmcp_package::AgentPackage>`,
    and that the emitted Cargo.toml contains `pmcp-agent`. Mark the test module to run under
    `--test-threads=1` semantics (heavy tempdir build) consistent with scaffold_sql_server.rs.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test scaffold_agent -- --test-threads=1 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - cargo-pmcp/tests/scaffold_agent.rs contains `env!("CARGO_BIN_EXE_cargo-pmcp")`
    - Test references `pmcp_package::AgentPackage` for the round-trip assertion
    - Before Task 2/3, `cargo test -p cargo-pmcp --test scaffold_agent` fails (RED)
  </acceptance_criteria>
  <done>A real-binary scaffold test exists and fails because agent new is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: templates/agent.rs emitter with AgentPackage manifest + two-level pin tripwire</name>
  <files>cargo-pmcp/src/templates/agent.rs, cargo-pmcp/src/templates/mod.rs</files>
  <behavior>
    - `generate(dir, name)` writes Cargo.toml (pins pmcp-agent), src/main.rs (AgentEngine runner),
      an AgentPackage JSON manifest, and tests/pin.rs into the scaffold.
    - Unit test `emitted_agent_version_matches_workspace_pin` fails if PMCP_AGENT_VERSION drifts from
      crates/pmcp-agent/Cargo.toml `[package] version`.
    - Unit test asserts the emitted JSON manifest deserializes as `pmcp_package::AgentPackage`.
  </behavior>
  <read_first>
    - cargo-pmcp/src/templates/workbook_server.rs (generate orchestrator + const + include_str! drift-test to mirror exactly)
    - cargo-pmcp/src/templates/mod.rs (barrel — add `pub mod agent;` next to `pub mod workbook_server;`)
    - crates/pmcp-agent/examples/s50_standalone_vs_sampled.rs (the AgentPackage literal + the run_standalone runner body to lift into src/main.rs)
    - crates/pmcp-agent/Cargo.toml (the version the drift-guard targets)
  </read_first>
  <action>
    Add `pub mod agent;` to templates/mod.rs. Create templates/agent.rs mirroring workbook_server.rs:
    a `pub fn generate(dir: &Path, name: &str) -> Result<()>` calling per-file `generate_cargo_toml`,
    `generate_main_rs`, `generate_manifest`, `generate_pin_test`, each a raw `fs::write(...).context(...)`.
    - generate_cargo_toml: emit `[package]` + `[dependencies] pmcp-agent = { version = "{PMCP_AGENT_VERSION}", features = ["openai-compat"] }` and `tokio`, `serde_json`, `pmcp-package = "0.1"` as needed; escape literal braces `{{`/`}}`.
    - generate_manifest: emit an `AgentPackage` serialized to pretty JSON matching the s50 `agent_package()` literal (name = the scaffold name, version "1.0.0", a demo instructions string, llm ConfigSlot LlmProvider, max_tokens 100_000, max_iterations 5, all other fields empty/None). Write to e.g. `agent.package.json`.
    - generate_main_rs: emit a standalone runner lifting s50 `run_standalone` — build `ResolvedAgentConfig::new(...)`, `OpenAiCompatSource::new("http://localhost:11434/v1", ...)`, `AgentEngine::new(...)`, `engine.run(...).await`.
    - generate_pin_test: emit `tests/pin.rs` into the scaffold asserting its own Cargo.toml pins `pmcp-agent` (satisfies the literal "generated tripwire test" reading of CLI-01/D-05, Research Open-Q1 → ship both).
    Add `const PMCP_AGENT_VERSION: &str = "0.1.0";` and, mirroring workbook_server.rs, a
    `#[test] fn emitted_agent_version_matches_workspace_pin()` that `include_str!("../../../crates/pmcp-agent/Cargo.toml")`,
    parses `["package"]["version"]`, and `assert_eq!(PMCP_AGENT_VERSION, v, "agent scaffold pin drifted — bump PMCP_AGENT_VERSION")`.
    Add a second unit test that serializes the manifest and asserts `serde_json::from_str::<pmcp_package::AgentPackage>` succeeds (round-trip lock). Keep each `generate_*` and the handler under cognitive complexity 25.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --lib emitted_agent_version_matches_workspace_pin && cargo test -p cargo-pmcp --lib agent -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - templates/mod.rs contains `pub mod agent;`
    - templates/agent.rs contains `const PMCP_AGENT_VERSION: &str = "0.1.0";`
    - templates/agent.rs contains `include_str!("../../../crates/pmcp-agent/Cargo.toml")`
    - `cargo test -p cargo-pmcp --lib emitted_agent_version_matches_workspace_pin` passes
    - The manifest round-trip unit test passes (`serde_json::from_str::<pmcp_package::AgentPackage>` on the emitted JSON is Ok)
  </acceptance_criteria>
  <done>The emitter produces a valid AgentPackage manifest + runner + in-scaffold pin test, and the internal drift-guard passes against pmcp-agent 0.1.0.</done>
</task>

<task type="auto">
  <name>Task 3: Wire agent new handler and promote validate_crate_name (GREEN)</name>
  <files>cargo-pmcp/src/commands/agent/new.rs, cargo-pmcp/src/commands/new.rs</files>
  <read_first>
    - cargo-pmcp/src/commands/agent/new.rs (the 110-01 stub with NewArgs to replace)
    - cargo-pmcp/src/commands/new.rs (validate_crate_name at line 119 to promote to pub(crate); execute_sql_server scaffolder shape)
  </read_first>
  <action>
    In commands/new.rs, change `fn validate_crate_name` to `pub(crate) fn validate_crate_name`
    (promote, do not copy). In agent/new.rs, replace the stub `execute` body: resolve the target
    dir from `NewArgs.path` (default to `./<name>`), call `crate::commands::new::validate_crate_name(&args.name)?`
    BEFORE any fs write (path-traversal guard, T-86-03-02 precedent), `fs::create_dir_all(dir.join("src"))`,
    then delegate to `crate::templates::agent::generate(&dir, &args.name)?`. Gate a colored next-steps
    `println!` (cd, `cargo run`, run the pin test) behind `global_flags.should_output()`. Return Ok(()).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test scaffold_agent -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - commands/new.rs declares `pub(crate) fn validate_crate_name`
    - agent/new.rs calls `validate_crate_name` before any `fs::` write and calls `templates::agent::generate`
    - agent/new.rs no longer contains the `bail!("agent new: implemented in plan 110-02")` stub
    - `cargo test -p cargo-pmcp --test scaffold_agent` passes (GREEN — the Task 1 test)
  </acceptance_criteria>
  <done>`cargo pmcp agent new <name>` scaffolds a compilable agent project; the scaffold integration test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → agent new | User-supplied crate name + output path (untrusted; may attempt traversal) |
| emitter → filesystem | Scaffolded files written under the resolved project dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-02-01 | Tampering | scaffold name / output path | mitigate | Reuse hardened `validate_crate_name` (path-traversal + Cargo-name guard) BEFORE any fs write; acceptance-gated (Research V5) |
| T-110-02-02 | Tampering | emitted `pmcp-agent` pin drift | mitigate | `emitted_agent_version_matches_workspace_pin` drift-guard test + in-scaffold `tests/pin.rs` (D-05); high severity → covered by Task 2 |
| T-110-02-03 | Integrity | emitted manifest shape drift | mitigate | Round-trip unit test asserts the emitted JSON deserializes as `pmcp_package::AgentPackage` |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --lib emitted_agent_version_matches_workspace_pin` — pin drift-guard green.
- `cargo test -p cargo-pmcp --test scaffold_agent -- --test-threads=1` — end-to-end scaffold green.
- `make quality-gate` before commit (fmt/clippy/build/test; PMAT cog ≤25 on new fns in CI).
</verification>

<success_criteria>
- `cargo pmcp agent new <name>` emits AgentPackage manifest + runner + in-scaffold pin test.
- Emitted manifest round-trips through `pmcp_package::AgentPackage`.
- Two-level pin tripwire (internal drift-guard + emitted tests/pin.rs) present per D-05.
- Crate name validated for path traversal before any write.
</success_criteria>

<output>
Create `.planning/phases/110-cargo-pmcp-agent-team-verbs/110-02-SUMMARY.md` when done
</output>
