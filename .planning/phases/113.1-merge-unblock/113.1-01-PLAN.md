---
phase: 113.1-merge-unblock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/streamable_http_server.rs
autonomous: true
requirements: [HTTP-09]
roadmap_success_criteria: [SC-1]
closes_decisions: [D-06, D-08, D-09, D-11]

must_haves:
  truths:
    - "Every v1 and v2 POST request routes exactly as it did before: the transport, v2, MRTR and subscriptions suites are green with no test edited to accommodate the refactor"
    - "The v2 header gate still runs BEFORE session resolution and BEFORE legacy protocol-version validation on both entrypoints"
    - "A v2 gate Reject on the middleware path still fires report_middleware_error before returning the 4xx"
    - "The fast path's subscriptions/listen arm is still reached only after session + v2 matrix + legacy-version + auth"
    - "Each extraction is proven load-bearing by a recorded negative control naming the test that went RED, or the absence of such a test is recorded as a finding"
  artifacts:
    - path: "src/server/streamable_http_server.rs"
      provides: "resolve_v2_gate / resolve_v2_gate_with_error_hook / dispatch_message_fast"
      contains: "async fn resolve_v2_gate"
  key_links:
    - from: "handle_post_fast_path"
      to: "resolve_v2_gate"
      via: "direct call replacing the inline match &ingress gate block"
      pattern: "resolve_v2_gate\\(&state"
    - from: "handle_post_with_middleware"
      to: "resolve_v2_gate_with_error_hook"
      via: "direct call replacing the inline gate block with its hook arm"
      pattern: "resolve_v2_gate_with_error_hook\\("
    - from: "handle_post_fast_path"
      to: "dispatch_message_fast"
      via: "Box::pin'd call replacing the inline 4-arm match ingress"
      pattern: "Box::pin\\(dispatch_message_fast\\("
---

<objective>
Land D-06's two prescribed extractions on `src/server/streamable_http_server.rs` using the file's
own plain-fn + `*_with_error_hook` sibling convention (D-09), and prove each one preserves the
load-bearing ordering it moves (D-11).

Purpose: `handle_post_fast_path` (cog 30) and `handle_post_with_middleware` (cog 31) fail the
org-required PR gate at ceiling 25. This plan is step 1 of 2 — it delivers the extractions CONTEXT
locks; plan 05 delivers the additional extractions RESEARCH measured as necessary to reach D-10's
`<= 20`.

Output: three new private helpers, both call sites rewired, three recorded negative controls, and a
measured PMAT number. **This plan does not close the gate** — RESEARCH measured D-06 alone at 26/28,
still over 25. That is the planned intermediate state and must be recorded, not treated as failure.

Serves ROADMAP Phase-113.1 Success Criterion 1. It does not itself close HTTP-09 (plans 02/03 do).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md
@.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md
@.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md

<interfaces>
<!-- Extracted from src/server/streamable_http_server.rs at HEAD e8cbd998. `git diff 4ad2ac1f..HEAD
     -- src/` is empty, so every RESEARCH/PATTERNS line number is current. Line numbers shift as
     soon as Task 1 lands — after that, locate by symbol name, not by number. -->

Sibling-pair precedents this plan extends (three instances already in the file):
  fn validate_protocol_version(state, era, session_id, protocol_version)
      -> std::result::Result<(), Response>                                                       :1913
  async fn validate_protocol_version_with_error_hook(state, era, is_init_request, session_id,
      protocol_version, http_middleware, http_context) -> std::result::Result<(), Response>      :3305
  fn resolve_session_for_request(state, era, is_init_request, session_id, protocol_version)
      -> std::result::Result<Option<String>, Response>                                           :2112
  async fn resolve_session_with_error_hook(...) -> std::result::Result<Option<String>, Response>  :3280
  async fn report_middleware_error(http_middleware, context, error_kind: &str)                   :2156

Gate machinery the extraction wraps (unchanged by this plan):
  async fn run_v2_header_gate(state, headers, raw_body: &[u8], method_override: Option<&str>)
      -> (Option<ProtocolContext>, V2GateOutcome)                                                :1307
  enum V2GateOutcome { Reject { code, message, data }, Passthrough, EnforceOk { method, name } }  :866
  fn v2_gate_reject_response(raw_body: &[u8], era, code, message: &str, data) -> Response         :854

Dispatch twin that `dispatch_message_fast` mirrors:
  async fn dispatch_message_with_middleware(state, ingress: HttpIngress,
      dispatch: MiddlewareDispatch, auth_context, http_middleware, http_context) -> Response      :3409
  struct FastPathDispatch { is_init_request, response_session_id, protocol_context,
      v2_outbound, sessions_on }                                                                  :2375
  struct MiddlewareDispatch { <the same five fields> }                                            :3335
  async fn handle_fast_path_request(state, id, request, auth_context,
      dispatch: FastPathDispatch, session_id: Option<&String>) -> Response                        :2388

Call-site body types (both coerce to &[u8] — verified in RESEARCH Pattern 2):
  fast path        body: String                 -> body.as_bytes()
  middleware path  server_request.body: Vec<u8> -> &server_request.body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the v2 header gate into the resolve_v2_gate / resolve_v2_gate_with_error_hook sibling pair</name>

  <files>src/server/streamable_http_server.rs</files>

  <read_first>
    - `src/server/streamable_http_server.rs` `:3084-3239` — `handle_post_fast_path` end to end. Read the WHOLE function before editing.
    - `src/server/streamable_http_server.rs` `:3520-3683` — `handle_post_with_middleware` end to end.
    - `src/server/streamable_http_server.rs` `:1907-1921`, `:2112-2170`, `:3276-3328` — the four existing sibling-pair instances plus `report_middleware_error`. This is the convention D-09 copies; read it before writing a line.
    - `src/server/streamable_http_server.rs` `:854-900` (`v2_gate_reject_response`, `V2GateOutcome`) and `:1307-1400` (`run_v2_header_gate`).
    - `.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md` § "1. `src/server/streamable_http_server.rs` — `resolve_v2_gate` + `resolve_v2_gate_with_error_hook`" — carries both bodies to lift verbatim and both wrapper shapes.
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Pattern 2: Plain fn + `*_with_error_hook` sibling (D-09)".
  </read_first>

  <action>
Add two private functions and rewire both call sites.

**The plain half — `resolve_v2_gate`.** Place it immediately after `resolve_session_for_request`
(currently ends around `:2155`), among the file's other plain per-request resolvers. Signature,
exactly: `async fn resolve_v2_gate(state: &ServerState, headers: &HeaderMap, raw_body: &[u8], ingress: &HttpIngress) -> std::result::Result<(Option<crate::types::protocol::ProtocolContext>, Option<(String, String)>), Response>`.
Note `std::result::Result` fully qualified — the file has its own `Result` alias in scope and every
sibling spells it out this way.

Its body is the fast path's gate block lifted verbatim from `:3125-3151`, with two mechanical
changes: the `V2GateOutcome::Reject` arm's `return v2_gate_reject_response(...)` becomes
`return Err(v2_gate_reject_response(raw_body, era, code, &message, data))`, and the two success arms
produce `Ok((ctx, None))` and `Ok((ctx, Some((method, name))))`. Preserve exactly: the three-variant
match head `HttpIngress::Public(TransportMessage::Request { .. }) | HttpIngress::Discover { .. } | HttpIngress::SubscriptionsListen { .. }`,
the `matches!(ingress, HttpIngress::Discover { .. }).then_some(crate::types::protocol::SERVER_DISCOVER_METHOD)`
method override, the `let era = ctx.as_ref().map(|pc| pc.era);` recovery on the Reject arm, and the
`HttpIngress::Public(_) => Ok((None, None))` fallthrough.

**Its rustdoc must carry the ordering prose now at `:3115-3124`, moved not copied.** That comment
block is today the ONLY thing enforcing "the v2 gate runs BEFORE session resolution (the era decides
whether sessions apply at all — HTTP-01) and BEFORE the legacy protocol-version check (an accepted
v2 request carries `MCP-Protocol-Version: 2026-07-28`, which the static SUPPORTED check would
reject)". Also follow the file's convention of naming the plan that created the helper, as
`validate_protocol_version`'s rustdoc does at `:1910-1912` — name plan 113.1-01 and D-06/D-09.

**The hook half — `resolve_v2_gate_with_error_hook`.** Place it immediately after
`validate_protocol_version_with_error_hook` (ends around `:3328`). Same signature plus
`http_middleware: &ServerHttpMiddlewareChain, http_context: &ServerHttpContext`. It delegates to
`resolve_v2_gate`; on the `Err` arm it calls
`report_middleware_error(http_middleware, http_context, "v2 header gate rejected").await` and then
returns the same `Err`. Use the `match { Ok(v) => Ok(v), Err(r) => { hook; Err(r) } }` shape of
`resolve_session_with_error_hook` (`:3280`). `"v2 header gate rejected"` is the exact string the
middleware path passes today at `:3599-3604` — do not reword it.

**Rewire the fast path.** Replace `:3115-3151` (the ordering comment plus the whole
`let (protocol_context, v2_outbound) = match &ingress { ... };`) with a call to `resolve_v2_gate`
destructured through `match ... { Ok(pair) => pair, Err(response) => return response }`, passing
`&state`, `&headers`, `body.as_bytes()`, `&ingress`. Leave `let is_v2_request = v2_outbound.is_some();`
at `:3152` and everything after it untouched.

**Rewire the middleware path.** Replace `:3571-3619` the same way, calling
`resolve_v2_gate_with_error_hook` with `&state`, `&server_request.headers`, `&server_request.body`,
`&ingress`, `http_middleware`, `&http_context`. Leave `:3620` onward untouched.

**Do NOT** write one function taking an `Option<&ServerHttpMiddlewareChain>` — D-09 and RESEARCH
§ "Don't Hand-Roll" both reject it explicitly.
**Do NOT** touch either legacy protocol-version guard call site. Their asymmetry is deliberate
(D-08): fast gates on `!is_init_request && !is_v2_request` at `:3167`; middleware gates on
`!is_v2_request` only at `:3641` and folds the init check inside the wrapper, whose rustdoc says so
at `:3303-3304`. A refactor that "harmonises" them silently changes behavior.
  </action>

  <verify>
    <automated>cargo nextest run --features full --test v2_required_headers --test v2_stateless_http --test v2_subscriptions --test streamable_http_integration --test http_middleware_integration</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'async fn resolve_v2_gate(' src/server/streamable_http_server.rs` returns 1
    - `grep -c 'async fn resolve_v2_gate_with_error_hook(' src/server/streamable_http_server.rs` returns 1
    - `grep -c 'run_v2_header_gate(' src/server/streamable_http_server.rs` returns 2 — its own definition plus the single call inside `resolve_v2_gate`, proving the copy-paste is gone rather than duplicated
    - `grep -c 'v2 header gate rejected' src/server/streamable_http_server.rs` returns 1, and that line sits inside `resolve_v2_gate_with_error_hook`
    - `grep -c '!is_init_request && !is_v2_request' src/server/streamable_http_server.rs` returns 1 AND `grep -c 'if !is_v2_request {' src/server/streamable_http_server.rs` returns 1 — D-08's asymmetry is intact
    - `git diff --stat -- tests/ | wc -l` returns 0 — no test was edited to accommodate the refactor
    - `cargo nextest run --features full --test v2_required_headers --test v2_stateless_http --test v2_subscriptions --test streamable_http_integration --test http_middleware_integration` exits 0
    - `make lint` exits 0 with zero warnings
  </acceptance_criteria>

  <done>Both entrypoints call one shared gate helper through the file's existing sibling-pair convention; the ordering prose lives in the helper's rustdoc; the five transport/v2 suites are green with no test edited.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the fast path's inline 4-arm ingress dispatch into dispatch_message_fast</name>

  <files>src/server/streamable_http_server.rs</files>

  <read_first>
    - `src/server/streamable_http_server.rs` `:3183-3238` at the pre-Task-1 numbering — the inline `match ingress` being lifted (CONTEXT cites `:3179-3236`; RESEARCH § Corrections measured the true range as `:3183-3238`, 56 lines). Task 1 has already shifted these numbers — locate by content.
    - `src/server/streamable_http_server.rs` `:3404-3510` — `dispatch_message_with_middleware`, the twin that already exists. This is the shape to mirror.
    - `src/server/streamable_http_server.rs` `:2368-2402` — `FastPathDispatch` and `handle_fast_path_request`'s destructure-on-entry idiom.
    - `src/server/streamable_http_server.rs` `:3665-3682` — the caller-side `Box::pin(dispatch_message_with_middleware(...))` and its explanatory comment.
    - `.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md` § "2. ... `dispatch_message_fast`" — names the three security-load-bearing details the extraction must preserve verbatim.
  </read_first>

  <action>
Add `async fn dispatch_message_fast(state: &ServerState, ingress: HttpIngress, dispatch: FastPathDispatch, auth_context: Option<crate::server::auth::AuthContext>, session_id: Option<&String>) -> Response`
immediately before `dispatch_message_with_middleware`, so the twins sit adjacent. Destructure
`FastPathDispatch` into its five fields on entry exactly as `handle_fast_path_request` does at
`:2396-2402`, then hold the four match arms verbatim.

Three details in the lifted body are security-load-bearing and must survive byte-for-byte:

1. **`Box::pin` on both large arms** (currently `:3189` and `:3225`), each with its existing comment
   explaining that the dispatch future crosses clippy's `large_futures` threshold. Dropping either
   re-trips the lint and reds `make lint`.
2. **`assemble_subscriptions_listen` stays LAST in pipeline order.** Its comment at `:3221-3223`
   ("HTTP-04: reached AFTER the same session / v2-matrix / legacy-version / auth pipeline as every
   other ingress — a held-open stream must not be a way around auth") is a V4 access-control
   statement. Moving that arm earlier is a real auth bypass.
3. **The `Discover` arm's comment** at `:3216-3218` ("reached AFTER session, the v2 matrix,
   legacy-version validation, and auth — never an early return").

At the fast-path call site, replace the inline match with
`Box::pin(dispatch_message_fast(&state, ingress, FastPathDispatch { is_init_request, response_session_id, protocol_context, v2_outbound, sessions_on }, auth_context, session_id.as_ref())).await`,
carrying over the boxing comment in the shape used at `:3665-3667`. `auth_context` and `session_id`
are already bound above the current match; do not re-derive them.

Give the helper a rustdoc in the shape of `dispatch_message_with_middleware`'s at `:3404-3408` (one
sentence per arm), plus the "Extracted in plan 113.1-01 (D-06)" line the file's convention uses.

**Do NOT** merge `FastPathDispatch` and `MiddlewareDispatch`. They are field-for-field identical, but
merging them is explicitly out of scope (D-07) — it reduces no cognitive complexity and is recorded
as a deferred idea.
**Do NOT** share one dispatch function between the two paths — that is the pipeline unification D-06
rejects.
  </action>

  <verify>
    <automated>cargo nextest run --features full --test v2_stateless_http --test v2_subscriptions --test v2_mrtr --test streamable_http_spec_compliance --test streamable_http_integration</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'async fn dispatch_message_fast(' src/server/streamable_http_server.rs` returns 1
    - `grep -c 'Box::pin(dispatch_message_fast(' src/server/streamable_http_server.rs` returns 1
    - `awk '/^async fn dispatch_message_fast/,/^}/' src/server/streamable_http_server.rs | grep -c 'HttpIngress::'` returns at least 4 — all four arms moved
    - `awk '/^async fn handle_post_fast_path/,/^}/' src/server/streamable_http_server.rs | grep -c 'match ingress {'` returns 0 — the inline match is gone from the handler
    - `awk '/^async fn dispatch_message_fast/,/^}/' src/server/streamable_http_server.rs | grep -c 'Box::pin('` returns 2 — both large arms kept their boxing
    - `awk '/^async fn dispatch_message_fast/,/^}/' src/server/streamable_http_server.rs | grep -n 'assemble_subscriptions_listen'` shows it after the `TransportMessage::Request` arm, not before it
    - `git diff --stat -- tests/ | wc -l` returns 0
    - `cargo nextest run --features full --test v2_stateless_http --test v2_subscriptions --test v2_mrtr --test streamable_http_spec_compliance --test streamable_http_integration` exits 0
    - `make lint` exits 0 with zero warnings — proves `large_futures` did not re-trip
  </acceptance_criteria>

  <done>The fast path delegates its ingress dispatch to a named helper mirroring the middleware twin; the boxing, the auth-last ordering of `subscriptions/listen`, and every explanatory comment survived the move; the v2/MRTR/subscription suites are green.</done>
</task>

<task type="auto">
  <name>Task 3: Run and record the three D-11 negative controls, then measure PMAT</name>

  <files>src/server/streamable_http_server.rs</files>

  <read_first>
    - `src/server/streamable_http_server.rs` — the post-Task-2 state of both handlers and all three new helpers.
    - `.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md` § D-11 — why each control exists and what it proves.
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Pitfall 4" (three PMAT invocations that report wrong numbers) and § "Code Examples → The scratch-tree PMAT sizing probe".
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-29-SUMMARY.md` — the house negative-control recording style ("each control failed exactly ONE test").
  </read_first>

  <action>
Both handlers carry ordering constraints enforced today only by prose comments. This task supplies
the compensating evidence D-11 requires: three deliberate breaks, each shown to make a NAMED test go
RED, each reverted.

**Control A — ordering.** Move the `resolve_v2_gate` call in `handle_post_fast_path` to AFTER the
`resolve_session_for_request` block and after the legacy protocol-version guard, keeping everything
else identical. Run `cargo nextest run --features full --test v2_required_headers --test v2_stateless_http`.
Record the exact failing test name(s) and the assertion text. Revert.

**Control B — the hook.** Delete the
`report_middleware_error(http_middleware, http_context, "v2 header gate rejected").await` call from
`resolve_v2_gate_with_error_hook`'s `Err` arm, leaving the refusal itself intact. Run
`cargo nextest run --features full --test http_middleware_integration --test enhanced_middleware_tests --test sse_middleware_integration`.
Record the failing test name. Revert.

**Control C — dispatch.** In `dispatch_message_fast`, remove the `Box::pin` from the
`HttpIngress::SubscriptionsListen` arm, then run `make lint` and
`cargo nextest run --features full --test v2_subscriptions --test v2_subscriptions_client`. Record
the clippy lint name or failing test name. Revert.

**If a control does NOT go RED, record that fact rather than hunting for a control that does.** A
green control is a real finding — it means the suite does not constrain that property, and the
honest record is the point (plan 113-24 recorded exactly such a result for its second enforcement
point, and 113-31 recorded a fifth control because a fourth short-circuited). Do not weaken an
existing test, and do not add a test solely to make a control fail.

After all three are reverted, confirm the source file is byte-identical to its post-Task-2 state
(`git diff --stat -- src/server/streamable_http_server.rs` empty relative to the Task-2 commit),
then measure:

- The gate itself: `pmat quality-gate --fail-on-violation --checks complexity` from the repo root.
  **Expect it to still FAIL**, at approximately `handle_post_fast_path` 26 and
  `handle_post_with_middleware` 28 (RESEARCH's measured value for exactly this configuration). That
  is the planned intermediate state; plan 05 closes it. Record the literal output.
- The per-function numbers via `pmat analyze complexity -p . --format json` from the repo root
  (RESEARCH verified this reproduces the gate; the `Cargo.toml` at the analysis root is what selects
  the real AST engine).

**Do NOT** use `pmat analyze complexity --file src/server/streamable_http_server.rs` — RESEARCH
measured it reporting 14 / 18 on the identical unmodified file. It is not the gate.
**Do NOT** use CLAUDE.md's documented `jq '.violations[]'` recipe — RESEARCH verified the
whole-project JSON has no `violations` key, so it silently emits nothing. (This documentation defect
is recorded as a deferred item by plan 04; do not fix CLAUDE.md here.)
**Do NOT** add `#[allow(clippy::cognitive_complexity)]` to either handler. D-113-F ruled them
reducible and RESEARCH confirmed 16/16 is reachable; the escape hatch is not available.

Write all three control outcomes and both PMAT measurements into the plan SUMMARY verbatim.
  </action>

  <verify>
    <automated>pmat analyze complexity -p . --format json | python3 -c "import json,sys; fs=json.load(sys.stdin).get('files',[]); rows=sorted((fn['name'], fn['metrics']['cognitive']) for f in fs for fn in f.get('functions',[]) if fn['name'].startswith('handle_post_')); print(rows); assert len(rows)>=2 and all(c <= 28 for _,c in rows), f'expected both handlers at or under the measured 26/28 intermediate, got {rows}'"</automated>
  </verify>

  <acceptance_criteria>
    - `git status --porcelain src/server/streamable_http_server.rs` shows no uncommitted change after the three reverts (each control was reverted, not left in the tree)
    - The SUMMARY records, for each of controls A, B and C: the exact edit made, the literal command run, and either the NAMED test that went RED with its assertion text, or an explicit "control stayed GREEN — the suite does not constrain this property" finding
    - The SUMMARY contains the literal output of `pmat quality-gate --fail-on-violation --checks complexity`, showing the run still FAILS with the two handler violations and their measured cognitive values
    - `pmat analyze complexity -p . --format json` reports `handle_post_fast_path` and `handle_post_with_middleware` both at 28 or below (down from the 30/31 baseline)
    - `grep -c 'cognitive_complexity' src/server/streamable_http_server.rs` returns 0 — no `#[allow]` escape hatch was added
    - `cargo nextest run --features full` exits 0 across the full suite
    - `make quality-gate` exits 0
  </acceptance_criteria>

  <done>All three extractions have recorded, reverted negative controls; the measured PMAT numbers for both handlers are in the SUMMARY; the tree is clean and the full suite plus `make quality-gate` are green at the 26/28 intermediate state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote HTTP peer → `handle_post_fast_path` / `handle_post_with_middleware` | Every v1 and v2 request body, header and ingress classification crosses here. This is the SDK's single POST admission point. |
| peer-chosen `MCP-Protocol-Version` / `params._meta` → `run_v2_header_gate` | The era classification that decides whether v1 session semantics apply at all. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113.1-01 | Elevation of Privilege | `resolve_v2_gate` call ordering in both handlers | mitigate | The gate MUST resolve before `resolve_session_for_request`. The ordering prose moves into the helper's rustdoc (Task 1) and D-11 negative control A (Task 3) proves a named test constrains it. |
| T-113.1-02 | Spoofing | legacy `validate_protocol_version` ordering | mitigate | The gate MUST also precede the legacy version check, or an accepted v2 request carrying `MCP-Protocol-Version: 2026-07-28` is rejected by the static SUPPORTED set. Same rustdoc + same negative control. |
| T-113.1-03 | Repudiation | `resolve_v2_gate_with_error_hook` `Err` arm | mitigate | `report_middleware_error(.., "v2 header gate rejected")` must survive the extraction — dropping it silently stops middleware observing gate refusals. D-11 negative control B. |
| T-113.1-04 | Elevation of Privilege | `dispatch_message_fast`'s `SubscriptionsListen` arm | mitigate | The arm stays LAST, after session + v2 matrix + legacy-version + auth. Its V4 access-control comment moves with it verbatim (Task 2); reordering it is a real auth bypass. |
| T-113.1-05 | Denial of Service | `dispatch_message_fast`'s two large arms | mitigate | `Box::pin` preserved on both arms and at the call site; `make lint` (clippy `large_futures`) is the enforcing check, asserted in Task 2's acceptance criteria and exercised by D-11 negative control C. |
| T-113.1-06 | Spoofing | response id derivation (HTTP-05) | accept | `live_id = id.clone()` is captured inside `handle_fast_path_request`, which this plan does not modify; the extraction moves the call, not the capture. Covered by the existing `v2_stateless_http` id-replay regression, run in Task 2. |
| T-113.1-SC | Tampering | cargo installs | accept | This plan installs no package. RESEARCH § "Package Legitimacy Audit" records the phase as N/A. A plan proposing a new crate is a scope deviation to escalate, not to add silently. |
</threat_model>

<verification>
Run from the repo root, in this order:

1. `cargo nextest run --features full` — full suite, zero failures, zero test files edited.
2. `make lint` — zero warnings (this is what catches a dropped `Box::pin`).
3. `make quality-gate` — exits 0. Note it does NOT run PMAT (Phase 75 D-07, verified at `Makefile:673-690`).
4. `pmat quality-gate --fail-on-violation --checks complexity` — **expected to still FAIL** with the two handler violations at approximately 26 and 28. Record the literal output; do not treat it as a blocker for this plan.
5. `pmat analyze complexity -p . --format json` — per-function numbers for both handlers, recorded in the SUMMARY.

Do not use `pmat analyze complexity --file <path>` (reports 14/18 on the unmodified file) and do not
use CLAUDE.md's `jq '.violations[]'` recipe (emits nothing against pmat 3.15.0).
</verification>

<success_criteria>
- `resolve_v2_gate`, `resolve_v2_gate_with_error_hook` and `dispatch_message_fast` exist as private helpers following the file's own conventions
- `run_v2_header_gate` has exactly one call site in the crate; the copy-paste between the handlers is gone
- D-08's legacy-version guard asymmetry is byte-unchanged at both call sites
- `FastPathDispatch` and `MiddlewareDispatch` remain two separate structs (D-07 not silently absorbed)
- Full suite, `make lint` and `make quality-gate` all green, with zero test-file edits
- Three negative controls recorded with named outcomes and reverted
- Both handlers measured at or below 28 cognitive, down from 30/31, with the number in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/113.1-merge-unblock/113.1-01-SUMMARY.md` when done.

It MUST contain: the literal `pmat quality-gate` output before and after; the per-function
`pmat analyze complexity -p .` numbers; and a verbatim section per negative control (edit made,
command run, outcome — named RED test or a recorded GREEN finding).
</output>
