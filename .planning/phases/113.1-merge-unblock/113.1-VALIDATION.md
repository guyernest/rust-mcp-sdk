---
phase: 113.1
slug: merge-unblock
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-27
---

# Phase 113.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `113.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` / `cargo nextest` (`Makefile:32` installs it; `Makefile:212` uses it) |
| **Config file** | none — standard cargo layout; `fuzz/` is a separate workspace |
| **Quick run command** | `cargo test --test v2_bounded_reads_tripwire --features full` (0.04 s) · `cargo test --lib --features full sse_parser` |
| **Full suite command** | `make test-all` (= `test-unit test-doc test-property test-examples test-integration`) |
| **Estimated runtime** | quick ~1 s · full suite minutes-scale |

**Gate command NOT covered by `make quality-gate`:** `pmat quality-gate --fail-on-violation --checks complexity` (0.5 s). Verified: `make quality-gate` (`Makefile:673-690`) runs fmt-check, lint, build, test-all, pmcp-package-gate, audit, unused-deps, check-todos, check-unwraps, validate-always, purity-check, comply — **PMAT appears nowhere**. Every verification step in this phase's plans must invoke it explicitly.

---

## Sampling Rate

- **After every task commit:** Run `pmat quality-gate --fail-on-violation --checks complexity` + `cargo test --test v2_bounded_reads_tripwire --features full` + the touched module's unit tests
- **After every plan wave:** Run `cargo nextest run --features full` + `make lint`
- **Before `/gsd:verify-work`:** `make quality-gate` AND the explicit PMAT invocation AND the per-function probe confirming ≤ 20 on both handlers AND the nightly fuzz run with output inspected
- **Max feedback latency:** 60 seconds (quick loop); full suite only at wave boundaries

---

## Per-Task Verification Map

*(To be filled by the planner — task IDs assigned at plan time. Requirement→test mapping below is the pre-plan contract from RESEARCH.md.)*

| Req / Decision | Behavior | Test Type | Automated Command | File Exists | Status |
|----------------|----------|-----------|-------------------|-------------|--------|
| HTTP-09 cl.1 | No unbounded whole-body read in scope; allowlist EMPTY | tripwire | `cargo test --test v2_bounded_reads_tripwire --features full no_unbounded_whole_body_read_over_peer_supplied_bytes` | ✅ (D-05 edits length pin) | ⬜ pending |
| HTTP-09 cl.2 | No unreviewed accumulation over peer bytes | tripwire | `cargo test --test v2_bounded_reads_tripwire --features full every_peer_byte_accumulation_is_reviewed` | ✅ (must stay green through `chunk()` fix) | ⬜ pending |
| HTTP-09 cl.3 | `feed` O(n) at adversarial 1-byte chunking — absolute budget | unit (perf) | `cargo test --lib --features full sse_parser_feed_1b_chunks_stays_within_its_linear_time_budget` | ❌ W0 (D-13.1, RED-first) | ⬜ pending |
| HTTP-09 cl.3 | `feed` cost growth linear — machine-independent ratio | unit (perf) | `cargo test --lib --features full sse_parser_feed_cost_grows_linearly_not_quadratically` | ❌ W0 (D-16, RED-first) | ⬜ pending |
| HTTP-09 cl.3 | Event sequence + retained tail invariant under re-chunking | property | `PROPTEST_CASES=1000 cargo test --features full -- --ignored property_feed_chunking_invariance` | ❌ W0 (D-13.2) | ⬜ pending |
| HTTP-09 cl.3 | Buffer newline-free after feeds of every shape | unit | `cargo test --lib --features full drain_leaves_no_newline_in_the_retained_buffer` | ❌ W0 (D-15) | ⬜ pending |
| HTTP-09 cl.3 | No panic on arbitrary remote bytes | fuzz | `RUSTUP_TOOLCHAIN=nightly cargo fuzz run subscription_listen_frames -- -runs=20000` | ✅ target exists (D-13.3) | ⬜ pending |
| D-113-Q | `connect_sse` refuses an over-cap body | unit | new test (`src/shared/sse_optimized.rs` or `tests/sse_optimization_tests.rs`) | ❌ W0 | ⬜ pending |
| D-113-Q | Example still compiles under `#[deprecated]` | build | `cargo build --example t03_sse_optimized --features sse` | ✅ (explicit — `make test-examples` cannot fail this) | ⬜ pending |
| D-113-U | Both handlers ≤ 20 | gate | `pmat quality-gate --fail-on-violation --checks complexity` + per-fn probe | ✅ | ⬜ pending |
| D-113-U | Behavior preserved across both extractions | integration | `cargo nextest run --features full` | ✅ | ⬜ pending |
| D-19 | Conformance predicate pin is current (arm 2, record-only) | manual + test | `gh api …stateless.ts?ref=main` diff; `cargo nextest run --test v2_conformance_pin` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `sse_parser_feed_1b_chunks_stays_within_its_linear_time_budget` — HTTP-09 cl.3, RED-first
- [ ] `sse_parser_feed_cost_grows_linearly_not_quadratically` — HTTP-09 cl.3, RED-first
- [ ] `property_feed_chunking_invariance` — HTTP-09 cl.3 / D-13.2
- [ ] `drain_leaves_no_newline_in_the_retained_buffer` — D-15's replacement for the `debug_assert`
- [ ] over-cap refusal test for the new `connect_sse` read — D-113-Q
- [ ] accumulation `ALLOWLIST` entry for the `chunk()` loop — RESEARCH § Pitfall 3 (gate prerequisite, not a test)
- [ ] nightly toolchain availability check before relying on fuzz evidence (D-113-G: `make test-fuzz` green proves nothing)

Framework install: **none needed** — existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Org-required `gate` status check green on branch | HTTP-09 / D-113-U | CI-side aggregate check; only observable after push | `gh api repos/paiml/rust-mcp-sdk/commits/<sha>/check-runs` after push |
| D-11 negative controls (3×): each deliberate break makes a NAMED test go RED, then revert | D-113-U | Deliberate temporary breakage — recorded in SUMMARY, not left in tree | Reorder gate past session resolution; drop `report_middleware_error` from `Err` arm; reorder a `dispatch_message_fast` arm — record failing test name each time |
| D-19 arm 2 result written to `113-SPEC-RECHECK.md` | HTTP-08 rolled-forward obligation | Record-only doc write against upstream repo state | Compare live conformance predicate at upstream `main` to pin `a865118206d4d8cc8dbc5f5201607839281d0c3b`; write result |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
