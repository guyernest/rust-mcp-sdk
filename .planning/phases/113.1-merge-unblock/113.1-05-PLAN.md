---
phase: 113.1-merge-unblock
plan: 05
type: execute
wave: 2
depends_on: ["113.1-01"]
files_modified:
  - src/server/streamable_http_server.rs
autonomous: true
requirements: [HTTP-09]
roadmap_success_criteria: [SC-1]
closes_decisions: [D-08, D-10]

must_haves:
  truths:
    - "pmat quality-gate --fail-on-violation --checks complexity passes with no src/ function over cognitive 25"
    - "handle_post_fast_path and handle_post_with_middleware are BOTH at cognitive 20 or below, measured per-function and recorded"
    - "The legacy protocol-version guard asymmetry between the two paths is preserved exactly, not harmonised"
    - "Every v1 and v2 POST request routes exactly as before: the full suite is green with no test edited"
    - "No #[allow(clippy::cognitive_complexity)] was added to either handler"
  artifacts:
    - path: "src/server/streamable_http_server.rs"
      provides: "per-path read/classify preamble helpers and per-path legacy-version guard helpers"
      contains: "struct FastIngress"
  key_links:
    - from: "handle_post_fast_path"
      to: "the fast read/classify preamble helper"
      via: "one call returning Result<FastIngress, Response>"
      pattern: "FastIngress"
    - from: "handle_post_with_middleware"
      to: "the middleware read/classify preamble helper"
      via: "one call returning Result<MwIngress, Response>"
      pattern: "MwIngress"
---

<objective>
Finish D-113-U: bring both POST entrypoints to cognitive complexity `<= 20`, closing the org-required
`gate` check.

Purpose: RESEARCH's single most important finding is that **D-06's two prescribed extractions are not
sufficient**. Measured with pmat 3.15.0 in a scratch tree that reproduces the gate's exact baseline
(30/31), plan 01's configuration lands at **26 / 28** — both still over the hard ceiling of 25. The
reason is mechanical: PMAT's cognitive score for these two functions tracks `nesting_max` almost
exactly (empirically cog ≈ 3 × nest), so an extraction that does not remove the *deepest* nested block
buys zero points. Two of the candidates RESEARCH measured — fast session resolve and fast auth — score
a **literal zero delta**.

CONTEXT's Claude's-Discretion block already authorises this: *"if `resolve_v2_gate` +
`dispatch_message_fast` land short of 20, choose the next extraction by the same P2
extract-named-stages technique … staying inside D-06's fence."*

Target row, from RESEARCH's measured cumulative table: **`D-06 + preamble + legacy guards` → 16 / 16.**
That is four points of margin on each handler, which is what D-10's own rationale asks for — Phase 113
burned 8-9 points on these two entrypoints in a single phase, and Phases 114-119 add to the same
functions. The `20 / 19` row technically satisfies D-10 but leaves the fast path with zero margin: one
`if` added by Phase 114 puts it back over.

Output: two read/classify preamble helpers with their result structs, two legacy-version guard
helpers preserving D-08's asymmetry, updated handler rustdoc, and a green PMAT gate with the
per-function numbers recorded.

Serves ROADMAP Phase-113.1 Success Criterion 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md
@.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md
@.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md
@.planning/phases/113.1-merge-unblock/113.1-01-SUMMARY.md

<interfaces>
<!-- Line numbers below are the PRE-plan-01 numbering. Plan 01 has already shifted them — locate by
     symbol and by content, and re-derive before citing anything in a comment. -->

The fast preamble to lift (:3088-3113):
  let (parts, body) = request.into_parts();  /  let headers = parts.headers;
  read_body_with_limit(body, state.config.max_request_bytes).await   -> Err(response) => return response
  validate_headers(&headers, "POST")                                  -> Err(error_response) => return
  parse_transport_message_fast(body.as_bytes())
      -> Err(response) => return map_unparsed_body_for_v2(&state, body.as_bytes(), response).await
  extract_session_and_protocol_headers(&headers) -> (session_id, protocol_version)
  is_init_request = match &ingress { Public(msg) => is_initialize_request(msg),
                                     Discover{..} | SubscriptionsListen{..} => false }

The middleware preamble to lift (:3530-3569):
  convert_axum_to_middleware_request(request, state.config.max_request_bytes).await
  build_middleware_context(&server_request)
  run_request_middleware(http_middleware, &mut server_request, &http_context).await
  validate_headers(&server_request.headers, "POST")  + report_middleware_error("Header validation failed")
  parse_transport_message_with_middleware(&server_request.body, http_middleware, &http_context).await
      -> Err(response) => return map_unparsed_body_for_v2(&state, &server_request.body, response).await
  extract_session_and_protocol_headers(&server_request.headers)
  is_init_request (same match)

The two legacy-version guard call sites — ASYMMETRIC BY DESIGN (D-08):
  fast  (:3167-3176)  if !is_init_request && !is_v2_request {
                          validate_protocol_version(&state, era, session_id.as_ref(),
                                                    protocol_version.as_ref()) }
  mw    (:3641-3655)  if !is_v2_request {
                          validate_protocol_version_with_error_hook(&state, era, is_init_request,
                              session_id.as_ref(), protocol_version.as_ref(),
                              http_middleware, &http_context).await }
  The wrapper's own rustdoc at :3303-3304 reads "A no-op for init requests" — the init check is folded
  inside it BY DESIGN.

Return-struct convention to mirror:
  struct FastPathDispatch { .. }    :2375   — five fields, each with a rustdoc naming the requirement it serves
  struct MiddlewareDispatch { .. }  :3335
  handle_fast_path_request destructures on entry at :2396-2402

RESEARCH's measured single-extraction deltas (fast / mw), each applied ALONE to the unmodified file:
  baseline                                     30 (nest 10) / 31 (nest 10)
  fast read+classify preamble  (:3091-3105)    23 (nest 7)  / 31
  fast legacy-version guard    (:3167-3176)    26 (nest 8)  / 31
  fast session resolve         (:3156-3165)    30 ZERO DELTA / 31
  fast auth                    (:3178-3181)    30 ZERO DELTA / 31
  mw read+classify preamble    (:3530-3562)    30 / 21 (nest 7)
  mw legacy-version guard      (:3641-3655)    30 / 28 (nest 9)
  mw session resolve           (:3624-3637)    30 / 31 ZERO DELTA
  mw auth                      (:3657-3663)    30 / 31 ZERO DELTA
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the read/classify preamble on each path into FastIngress / MwIngress helpers</name>

  <files>src/server/streamable_http_server.rs</files>

  <read_first>
    - `src/server/streamable_http_server.rs` — `handle_post_fast_path` and `handle_post_with_middleware` end to end at their current post-plan-01 state. Re-derive line numbers; plan 01 moved them.
    - `src/server/streamable_http_server.rs` `:2368-2402` (pre-plan-01 numbering) — `FastPathDispatch` / `MiddlewareDispatch` and `handle_fast_path_request`'s destructure-on-entry idiom. This is the "bundle of per-request values threaded onward" convention the new structs mirror, including per-field rustdoc naming the requirement each field serves.
    - `.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md` § "3. ... `FastIngress` / `MwIngress` preamble structs" — carries both bodies to lift and the struct convention.
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Architecture Patterns → Pattern 1" — the measured delta menu and the `nesting_max` explanation, and § "Open Questions" item 2 (one helper per path returning a named struct, NOT one shared helper).
    - `.planning/phases/113.1-merge-unblock/113.1-01-SUMMARY.md` — the measured starting point this task builds on.
  </read_first>

  <action>
Extract the body-read + header-validate + parse + classify preamble on each path into its own helper.
**Two helpers, one per path — never one shared helper.** A shared preamble is the pipeline
unification D-06 explicitly rejects, and the two preambles genuinely differ (the middleware path runs
`convert_axum_to_middleware_request`, `build_middleware_context` and `run_request_middleware` first,
and hooks `report_middleware_error` on header-validation failure).

**Fast path.** Add `struct FastIngress` bundling the five values the handler needs downstream:
`headers`, `body`, `ingress`, `session_id` and `protocol_version`, plus `is_init_request`. Give each
field a rustdoc line in the style of `FastPathDispatch`'s at `:2375` naming what it is for. Add
`async fn read_and_classify_fast(state: &ServerState, request: axum::extract::Request<Body>) -> std::result::Result<FastIngress, Response>`
holding the lifted preamble. The parse-error arm returns a `Response` via `map_unparsed_body_for_v2`,
so a single `Result<FastIngress, Response>` return type works — RESEARCH Open Question 2 confirms it.
In the handler, replace the preamble with one call plus a destructure of the struct.

**Middleware path.** Add `struct MwIngress` and
`async fn read_and_classify_with_middleware(state: &ServerState, request: axum::extract::Request<Body>, http_middleware: &ServerHttpMiddlewareChain) -> std::result::Result<MwIngress, Response>`.
`MwIngress` must also carry the `server_request` and the `http_context`, since the handler uses both
downstream. Preserve the `report_middleware_error(http_middleware, &http_context, "Header validation failed")`
call on the header-validation failure arm — that string is exactly what the path emits today; do not
reword it.

**Re-measure after EACH helper, not once at the end.** Run
`pmat analyze complexity -p . --format json` from the repo root and read the two handlers' `cognitive`
and `nesting_max` values. RESEARCH's caveat A1 applies: the deltas were measured on syntactically
valid but uncompiled textual sketches, so treat them as sizing rather than as a promise. The expected
landing point after this task, combined with plan 01, is around **20 / 19**; Task 2 takes it to 16/16.

Record the measured numbers in the SUMMARY. If a helper lands materially off its predicted delta, say
so — the prediction is the thing that was wrong, not the measurement.

**Do NOT** touch the legacy-version guard call sites in this task (Task 2 owns them), and do not
"harmonise" them at any point — D-08.
**Do NOT** extract the session-resolve or auth blocks. RESEARCH measured both at a literal zero delta
on both paths; extracting them adds indirection for no gate movement.
**Do NOT** merge `FastPathDispatch` and `MiddlewareDispatch` (D-07, out of scope), and do not fold the
new `FastIngress` into `FastPathDispatch` — they carry different values at different pipeline stages.
  </action>

  <verify>
    <automated>cargo nextest run --features full --test v2_required_headers --test v2_stateless_http --test v2_subscriptions --test v2_mrtr --test streamable_http_integration --test http_middleware_integration --test streamable_http_spec_compliance</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'struct FastIngress' src/server/streamable_http_server.rs` returns 1 and `grep -c 'struct MwIngress' src/server/streamable_http_server.rs` returns 1
    - `grep -c 'async fn read_and_classify_fast' src/server/streamable_http_server.rs` returns 1 and `grep -c 'async fn read_and_classify_with_middleware' src/server/streamable_http_server.rs` returns 1
    - `grep -c 'Header validation failed' src/server/streamable_http_server.rs` returns 1 — the hook string survived the move unchanged
    - `grep -c 'map_unparsed_body_for_v2' src/server/streamable_http_server.rs` returns 3 — its definition plus one call in each preamble helper (unchanged from the pre-plan count)
    - `grep -c 'struct FastPathDispatch' src/server/streamable_http_server.rs` returns 1 and `grep -c 'struct MiddlewareDispatch' src/server/streamable_http_server.rs` returns 1 — D-07 was not silently absorbed
    - `grep -c '!is_init_request && !is_v2_request' src/server/streamable_http_server.rs` returns 1 AND `grep -c 'if !is_v2_request {' src/server/streamable_http_server.rs` returns 1 — the guards are untouched by this task
    - `git diff --stat -- tests/ | wc -l` returns 0
    - The named test suites exit 0
    - `make lint` exits 0 with zero warnings
    - The SUMMARY records `pmat analyze complexity -p .` cognitive and nesting_max for both handlers after each helper landed
  </acceptance_criteria>

  <done>Each path reads and classifies its request through one named helper returning a documented struct; the two preambles remain separate; both handlers measured and recorded, expected around 20/19.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the legacy protocol-version guard per path, preserving D-08's asymmetry, and land both handlers at 20 or below</name>

  <files>src/server/streamable_http_server.rs</files>

  <read_first>
    - `src/server/streamable_http_server.rs` — both legacy-version guard call sites at their current state (pre-plan-01 they were `:3167-3176` and `:3641-3655`).
    - `src/server/streamable_http_server.rs` `:1907-1921` and `:3303-3328` (pre-plan-01) — `validate_protocol_version` and `validate_protocol_version_with_error_hook`, including the wrapper's rustdoc line "A no-op for init requests".
    - `.planning/phases/113.1-merge-unblock/113.1-CONTEXT.md` § D-08 — states plainly that a refactor which "harmonises" these two call sites silently changes behavior.
    - `.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md` § 3, closing paragraph — "If the ≤20 target needs a per-path legacy-guard helper, write TWO helpers preserving each call site's own condition."
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Pitfall 5: Landing the handlers at 22-25 satisfies the gate and defeats the phase".
  </read_first>

  <action>
Extract each legacy protocol-version guard into its own named helper. **TWO helpers, each preserving
its own call site's condition verbatim.** The asymmetry is load-bearing:

- The fast path's condition is `!is_init_request && !is_v2_request`, and it calls the PLAIN
  `validate_protocol_version`.
- The middleware path's condition is `!is_v2_request` ONLY, and it calls
  `validate_protocol_version_with_error_hook`, passing `is_init_request` INTO the wrapper — whose own
  rustdoc documents that it is a no-op for init requests.

A single helper covering both, or "unifying" the conditions so they read the same, silently changes
behavior on one of the paths. Each new helper's rustdoc must state which condition it carries and why
it differs from its twin, citing D-08 — so the next reader does not re-litigate it.

Each helper takes the values its condition needs and returns
`std::result::Result<(), Response>`; the handler reduces to a single `?`-style
`if let Err(response) = ... { return response; }`. Follow the file's plain-fn naming and the
`std::result::Result<_, Response>` spelling used by every sibling.

**Then measure and land the target.** Run `pmat quality-gate --fail-on-violation --checks complexity`
from the repo root — 0.5 s — and `pmat analyze complexity -p . --format json` for the per-function
numbers. The gate must now PASS. The blocking assertion for this plan is stricter than the gate:
**both handlers must be at cognitive 20 or below** (D-10). RESEARCH's measured target row for exactly
this configuration is 16 / 16.

If either handler lands above 20, do NOT stop at "the gate is green". Pick the next extraction from
RESEARCH's measured menu — the remaining candidate with a non-zero delta is the fast path's
`is_init_request` match, which the full-preamble row measured at 15/15 — and stay inside D-06's fence
(extract named stages; no pipeline unification, no struct merge, no guard harmonisation). Re-measure
after each addition.

**Do NOT** add `#[allow(clippy::cognitive_complexity)]` to either handler. CLAUDE.md forbids
disabling, weakening or removing the gate without Phase-level approval; D-113-F already ruled these
two functions reducible; and RESEARCH confirmed 16/16 is reachable, so the annotated escape hatch
(hard cap 50) is not available here.
**Do NOT** accept 22-25 as done. RESEARCH § Pitfall 5 measured why: the `D-06 + legacy guards` row
lands the middleware handler at exactly 25, which passes the gate with zero margin, and Phase 113
consumed 8-9 points on these functions in one phase.
**Do NOT** use `pmat analyze complexity --file <path>` (reports 14/18 on the identical unmodified
file) or CLAUDE.md's `jq '.violations[]'` recipe (emits nothing against pmat 3.15.0).
  </action>

  <verify>
    <automated>pmat quality-gate --fail-on-violation --checks complexity &amp;&amp; pmat analyze complexity -p . --format json | python3 -c "import json,sys; fs=json.load(sys.stdin).get('files',[]); rows=sorted((fn['name'], fn['metrics']['cognitive']) for f in fs for fn in f.get('functions',[]) if fn['name'].startswith('handle_post_')); print(rows); assert len(rows)>=2 and all(c <= 20 for _,c in rows), f'D-10 requires <= 20 on BOTH handlers, measured {rows}'"</automated>
  </verify>

  <acceptance_criteria>
    - Two distinct legacy-version guard helpers exist, one per path, and each rustdoc names its own condition and cites D-08
    - `grep -c '!is_init_request && !is_v2_request' src/server/streamable_http_server.rs` returns 1 — the fast condition is preserved exactly once, now inside its helper
    - `grep -c 'validate_protocol_version_with_error_hook(' src/server/streamable_http_server.rs` returns 2 — its definition plus exactly one call, from the middleware guard helper
    - `pmat quality-gate --fail-on-violation --checks complexity` exits 0 and reports zero complexity violations
    - `pmat analyze complexity -p . --format json` reports `handle_post_fast_path` and `handle_post_with_middleware` both at cognitive 20 or below; both numbers are recorded verbatim in the SUMMARY
    - `grep -c 'cognitive_complexity' src/server/streamable_http_server.rs` returns 0 — no `#[allow]` escape hatch anywhere in the file
    - `git diff --stat -- tests/ | wc -l` returns 0
    - `cargo nextest run --features full` exits 0 across the full suite
    - `make lint` exits 0 with zero warnings
    - `make quality-gate` exits 0
  </acceptance_criteria>

  <done>Each path validates the legacy protocol version through its own named helper carrying its own condition; the PR-blocking gate passes; both handlers are measured at 20 or below with the numbers on the record.</done>
</task>

<task type="auto">
  <name>Task 3: Update both handlers' rustdoc to name this phase's extractions, and confirm the full gate</name>

  <files>src/server/streamable_http_server.rs</files>

  <read_first>
    - `src/server/streamable_http_server.rs` — the rustdoc immediately above `handle_post_fast_path` (pre-plan-01 `:3078-3083`) and above `handle_post_with_middleware` (`:3514-3519`). Both currently enumerate the 75-01 extractions.
    - `src/server/streamable_http_server.rs` `:1910-1912` — the file's convention for naming the refactor plan that created a helper ("Refactored in 75-01 Task 1a-A (P2): extracted … as early-return chains").
    - `.planning/phases/113.1-merge-unblock/113.1-PATTERNS.md` § "Shared Patterns → Rustdoc naming the refactor plan that created the helper" and § "Ordering-constraint comments as the only enforcement".
    - `.planning/phases/113.1-merge-unblock/113.1-RESEARCH.md` § "Validation Architecture → Sampling Rate" — the phase-gate command list.
  </read_first>

  <action>
**Part 1 — the handler rustdoc.** Both handlers' rustdoc currently list the 75-01 extractions. Extend
each to name the extractions plans 113.1-01 and 113.1-05 added, in the file's established form, so the
next person reading either handler can find every stage without grepping: for the fast path,
`read_and_classify_fast`, `resolve_v2_gate`, the fast legacy-version guard helper and
`dispatch_message_fast`; for the middleware path, `read_and_classify_with_middleware`,
`resolve_v2_gate_with_error_hook`, the middleware legacy-version guard helper and
`dispatch_message_with_middleware`. Record the measured cognitive complexity each handler now carries,
as a durable marker of the budget Phases 114-119 are spending from.

Verify while you are there that the **ordering constraint** survived both plans: every helper that
sits between the gate and dispatch must make plain, in its own rustdoc, that the v2 gate resolves
BEFORE session resolution (HTTP-01: the era decides whether sessions apply at all) and BEFORE the
legacy version check (an accepted v2 request carries `MCP-Protocol-Version: 2026-07-28`, which the
static SUPPORTED set would reject). Those two sentences are the ONLY enforcement of that ordering in
the tree; a permanent ordering tripwire was considered under D-11 and deliberately deferred.

**Part 2 — the full gate.** Run, from the repo root, and record each result verbatim:
- `pmat quality-gate --fail-on-violation --checks complexity` — must PASS
- `pmat analyze complexity -p . --format json` — both handlers at 20 or below
- `cargo nextest run --features full` — full suite green
- `make lint` — zero warnings
- `make quality-gate` — exits 0
- `cargo semver-checks` — no update required; every helper this phase added is private, so nothing
  should move

**Part 3 — the before/after record.** The SUMMARY must carry a single table: `handle_post_fast_path`
and `handle_post_with_middleware` at the pre-phase baseline (30 / 31), after plan 01, after Task 1 of
this plan, and after Task 2 — each number a real measurement, none copied from RESEARCH's predictions.
Where a real extraction diverged from its predicted delta, say so. RESEARCH's caveat A1 says plainly
that its menu was measured on uncompiled sketches; this table is what replaces it with fact.

**Do NOT** weaken any gate, and do not re-run PMAT after `rm -rf .pmat/context.db*` unless a result
looks impossible — RESEARCH verified the cache re-derives correctly, and clearing it is a debugging
step, not a routine one.
  </action>

  <verify>
    <automated>make quality-gate &amp;&amp; pmat quality-gate --fail-on-violation --checks complexity &amp;&amp; cargo semver-checks check-release 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `handle_post_fast_path`'s rustdoc names `read_and_classify_fast`, `resolve_v2_gate` and `dispatch_message_fast`
    - `handle_post_with_middleware`'s rustdoc names `read_and_classify_with_middleware` and `resolve_v2_gate_with_error_hook`
    - `grep -c 'sessions apply' src/server/streamable_http_server.rs` returns at least 1 — the HTTP-01 ordering statement survives somewhere in a helper rustdoc
    - `grep -c '2026-07-28' src/server/streamable_http_server.rs` returns at least 1 in the ordering-rationale context — the legacy-check ordering statement survives
    - `pmat quality-gate --fail-on-violation --checks complexity` exits 0
    - Both handlers measure at cognitive 20 or below
    - `cargo nextest run --features full` exits 0
    - `make lint` exits 0 with zero warnings
    - `make quality-gate` exits 0
    - `cargo semver-checks` reports no update required
    - The SUMMARY contains the four-column before/after complexity table with MEASURED values at every step
  </acceptance_criteria>

  <done>Both handlers document the full extracted pipeline and their current complexity budget; the PR-blocking gate is green with margin; the complete measured trajectory from 30/31 is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote HTTP peer → `handle_post_fast_path` / `handle_post_with_middleware` | Unchanged from plan 01: the SDK's single POST admission point for every v1 and v2 request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113.1-40 | Elevation of Privilege | the ordering "v2 gate before session resolution" surviving a second round of extraction | mitigate | The ordering statement is asserted present in a helper rustdoc (Task 3 criteria), and plan 01's recorded negative control A already demonstrated which named test constrains it. The preamble helpers sit BEFORE the gate and cannot reorder it. |
| T-113.1-41 | Spoofing | "harmonising" the two legacy-version guard conditions during extraction | mitigate | Two separate helpers, each preserving its own condition verbatim; asserted by exact-string greps in Task 2's criteria. Each rustdoc cites D-08 so the asymmetry is not re-litigated. |
| T-113.1-42 | Tampering | the middleware header-validation error hook being dropped when the preamble moves | mitigate | `report_middleware_error(.., "Header validation failed")` is asserted present exactly once by grep (Task 1 criteria) and exercised by the middleware integration suites. |
| T-113.1-43 | Information Disclosure | the v2 parse-error path losing `map_unparsed_body_for_v2`, which recovers the ORIGINAL id for a 404 + -32601 | mitigate | Its call count is pinned at 3 (definition plus one per preamble helper) in Task 1's criteria. |
| T-113.1-44 | Repudiation | closing the phase at 22-25 "because the gate is green" | mitigate | The blocking automated verify asserts `<= 20` on BOTH handlers, not the gate's 25. RESEARCH § Pitfall 5 measured why 25 has zero margin. |
| T-113.1-45 | Tampering | weakening the gate with an annotated `#[allow(clippy::cognitive_complexity)]` | mitigate | `grep -c cognitive_complexity` must return 0 for the file. CLAUDE.md forbids weakening the gate without Phase-level approval, and D-113-F ruled these functions reducible. |
| T-113.1-SC | Tampering | cargo installs | accept | No package is installed. `pmat` is pinned at 3.15.0 and must not be upgraded inside this phase. |
</threat_model>

<verification>
Run from the repo root:

1. `pmat quality-gate --fail-on-violation --checks complexity` — **must PASS**. This is the exact
   PR-blocking invocation, it costs 0.5 s, and `make quality-gate` does NOT cover it
   (verified at `Makefile:673-690`).
2. `pmat analyze complexity -p . --format json` — both handlers at cognitive 20 or below.
3. `cargo nextest run --features full` — full suite green, zero test files edited.
4. `make lint` — zero warnings.
5. `make quality-gate` — exits 0.
6. `cargo semver-checks` — no update required (every helper added is private).

Do not use `pmat analyze complexity --file <path>` and do not use CLAUDE.md's `jq '.violations[]'`
recipe; both are measured-wrong and the second is recorded as a deferred defect by plan 04.
</verification>

<success_criteria>
- `pmat quality-gate --fail-on-violation --checks complexity` exits 0 with no `src/` function over 25
- BOTH handlers measure at cognitive 20 or below, with the numbers recorded, not merely under the gate
- Two preamble helpers and two legacy-version guard helpers exist, one of each per path
- D-08's guard asymmetry is preserved verbatim and documented at both new helpers
- `FastPathDispatch` and `MiddlewareDispatch` are still two separate structs
- No `#[allow(clippy::cognitive_complexity)]` anywhere in the file
- Full suite, `make lint`, `make quality-gate` and `cargo semver-checks` all green, with zero test edits
- The SUMMARY carries the measured complexity trajectory from the 30/31 baseline
</success_criteria>

<output>
Create `.planning/phases/113.1-merge-unblock/113.1-05-SUMMARY.md` when done.

It MUST contain the literal passing output of `pmat quality-gate --fail-on-violation --checks complexity`
and the four-column measured complexity table (baseline / after plan 01 / after Task 1 / after Task 2)
for both handlers.
</output>
