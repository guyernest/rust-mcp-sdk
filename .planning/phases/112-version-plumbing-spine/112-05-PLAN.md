---
phase: 112-version-plumbing-spine
plan: 05
type: execute
wave: 4
depends_on: [112-03, 112-04]
files_modified:
  - src/server/core.rs
  - src/server/mod.rs
  - src/server/wasm_core.rs
autonomous: true
requirements: [VERS-03, VERS-04, VERS-07, VERS-08]
must_haves:
  truths:
    - "A v2 client calling server/discover receives a read-only projection of the already-computed ServerCore capabilities, including the extensions map"
    - "A v1-era request for server/discover receives standard -32601 method-not-found"
    - "Every v2 response carries a resultType discriminator that defaults to complete"
    - "v1 responses are byte-identical to today — no resultType envelope, no serverInfo injection"
    - "v2 results carry serverInfo"
  artifacts:
    - path: "src/server/core.rs"
      provides: "server/discover era-gated arm + resultType(v2-only) + serverInfo(v2) injection at serialization"
      contains: "server/discover"
    - path: "src/server/mod.rs"
      provides: "twin-site discover/resultType/serverInfo parity"
      contains: "resultType"
  key_links:
    - from: "src/server/core.rs"
      to: "ServerCore.capabilities"
      via: "discover projects already-computed capabilities read-only (no recompute)"
      pattern: "self\\.capabilities"
---

<objective>
Add the era-gated dispatch behaviors that ride on the spine from Plan 04: `server/discover` as a read-only capability projection (V2 only; V1 gets `-32601`), the `resultType` envelope injected at serialization (v2-only, defaulting to `complete`), and `serverInfo` on v2 results — all without touching v1 wire output.

Purpose: `server/discover` is the stateless replacement for initialize's capability exchange (D-09); `resultType` is the discriminator Phases 113 (`input_required`) and 114 (`task`) will set. Keeping all of it v2-only (D-07/D-08) means zero v1 fixture churn and no risk to strict v1 consumers.
Output: `server/discover` handler projecting `ServerCore.capabilities` (incl. `extensions`); internal `ResultType` enum injected at the era-gated serialization boundary; `serverInfo` attached on the v2 path; V1 `server/discover` → `-32601`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-version-plumbing-spine/112-CONTEXT.md
@.planning/phases/112-version-plumbing-spine/112-RESEARCH.md
@.planning/phases/112-version-plumbing-spine/112-PATTERNS.md
@.planning/phases/112-version-plumbing-spine/112-03-PLAN.md
@.planning/phases/112-version-plumbing-spine/112-04-PLAN.md

<interfaces>
<!-- Dispatch + capability anchors. -->

Capability source (read-only projection — do NOT recompute):
  src/server/core.rs:461   capabilities: self.capabilities.clone(),   // build-time projection
  src/server/core.rs:1045  &self.capabilities                          // accessor
  ServerCapabilities.extensions: Option<HashMap<String, Value>>  (src/types/capabilities.rs:109)

Era-gate arm precedent (src/server/core.rs:1125-1148):
  match on request → Initialize arm, is_initialized() -32002 guard, success_response at ~1134
  handle_request_internal now carries protocol_context (Plan 04)

serverInfo shape (src/types/protocol/mod.rs):
  InitializeResult.server_info: Implementation (line 266)

From Plan 03: ClientRequest::ServerDiscover(ServerDiscoverRequest); error_codes::METHOD_NOT_FOUND (-32601)
From Plan 04: extra/protocol_context era available at dispatch; is_v2_opted_in()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server/discover era-gated arm — v2 capability projection incl. extensions; v1 → -32601</name>
  <files>src/server/core.rs, src/server/mod.rs, src/server/wasm_core.rs</files>
  <read_first>
    - src/server/core.rs (dispatch match around 1125-1148; capabilities at 461/1045; success_response boundary at ~1134)
    - src/server/mod.rs (dispatch around 1262; InitializeResult construction)
    - src/server/wasm_core.rs (handle_request method-string match at 50+)
    - src/types/capabilities.rs (ServerCapabilities incl. extensions:109)
    - .planning/phases/112-version-plumbing-spine/112-CONTEXT.md (D-09, D-10)
  </read_first>
  <behavior>
    - A v2 (era==V2) server/discover request returns a result carrying the same capabilities projection the server computed at build (tools/prompts/resources/extensions), read-only, WITHOUT initialize side effects (no is_initialized mutation)
    - The discover result includes the extensions map when the server populated it via .with_extension
    - A v1-era (or non-opted-in) server/discover request returns JSON-RPC error -32601 method-not-found (D-10)
    - discover does not recompute capabilities — it projects self.capabilities
  </behavior>
  <action>
    Add a `handle_discover()` (read-only) that projects `self.capabilities` (the already-computed `ServerCapabilities`, incl. `extensions`) into a discover result — do NOT recompute capabilities and do NOT trigger any initialize-style side effect (no `is_initialized()` mutation). Add a dispatch arm for `ClientRequest::ServerDiscover` (from Plan 03) that is reached ONLY when `era == Era::V2`; when the request arrives under `era == Era::V1` (or on a non-opted-in server), fall through to standard `-32601` method-not-found using `error_codes::METHOD_NOT_FOUND` (D-10). Mirror the arm at the mod.rs dispatch twin and provide the same method-string branch in `wasm_core.rs::handle_request` for parity.

    Define the discover result shape as an additive serde struct (camelCase, `#[non_exhaustive]`), carrying at minimum the capability projection + serverInfo. Reuse existing `ServerCapabilities`/`Implementation` types — do NOT invent parallel capability types.

    Tests: a v2 server/discover returns capabilities including a `.with_extension`-populated key; a v1 server/discover returns `-32601`; assert discover does not flip initialization state.
  </action>
  <verify>
    <automated>cargo test --lib server_discover</automated>
  </verify>
  <acceptance_criteria>
    - core.rs contains a `server/discover` dispatch arm gated on `Era::V2` and a `handle_discover`-style projection reading `self.capabilities`
    - A test proves v2 server/discover returns the extensions map populated via `.with_extension`
    - A test proves v1/non-opted-in server/discover returns error code -32601
    - A test proves discover does NOT mutate initialization state (no side effects)
    - `cargo build --lib` and `cargo build --lib --target wasm32-unknown-unknown` exit 0
  </acceptance_criteria>
  <done>server/discover projects computed capabilities incl. extensions on v2 only; v1 gets -32601; no recomputation, no side effects; parity across sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resultType envelope (v2-only) + serverInfo on v2 results; v1 byte-identical</name>
  <files>src/server/core.rs, src/server/mod.rs</files>
  <read_first>
    - src/server/core.rs (success_response serialization boundary ~1134; extra chain has protocol_context from Plan 04)
    - src/server/mod.rs (InitializeResult construction at 1262; serverInfo field)
    - .planning/phases/112-version-plumbing-spine/112-CONTEXT.md (D-07, D-08)
    - .planning/phases/112-version-plumbing-spine/112-RESEARCH.md (Pitfall 7 — v1 fixture churn)
  </read_first>
  <behavior>
    - A v2 response JSON carries "resultType":"complete"
    - A v1 response JSON carries NO resultType key and is byte-identical to today's output
    - A missing resultType is interpreted as complete by readers (default)
    - v2 results carry serverInfo (io.modelcontextprotocol serverInfo); v1 results do not gain it
  </behavior>
  <action>
    Define an INTERNAL `ResultType` enum (`Complete`/`InputRequired`/`Task`) — NOT a public field on any Result struct (D-08). Only `Complete` is used this phase; `InputRequired`/`Task` exist so Phases 113/114 can set the discriminator. Inject the `resultType` value at the era-gated serialization boundary (where `success_response` builds the outgoing JSON) ONLY when `era == Era::V2` (D-07). On the v1 path, emit nothing new — v1 wire output must stay byte-identical (Pitfall 7). Likewise attach `serverInfo` on v2 results only (VERS-03), reusing the existing `Implementation` server-info value the server already holds.

    Do NOT add `resultType` to handler-facing Result types; handlers keep returning today's types. The discriminator is a serialization-layer concern.

    Tests: serialize a v2 response and assert the JSON has `resultType == "complete"` and a `serverInfo` object; serialize the equivalent v1 response and assert NO `resultType` key and NO added serverInfo (byte-identity against a pinned expectation / existing snapshot).
  </action>
  <verify>
    <automated>cargo test --lib result_type_envelope</automated>
  </verify>
  <acceptance_criteria>
    - An internal `ResultType` enum exists with `Complete`/`InputRequired`/`Task`; it is NOT a public field on any Result struct (grep shows no `pub result_type:` added to Result structs)
    - A test asserts a v2 response JSON contains `"resultType":"complete"` and a serverInfo object
    - A test asserts a v1 response JSON contains NO `resultType` key (byte-identity preserved)
    - Existing v1 response/snapshot tests remain green: `cargo test --lib server::` exits 0
  </acceptance_criteria>
  <done>resultType + serverInfo are injected v2-only at serialization via an internal ResultType enum; v1 output is byte-identical; handler Result types are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v2 client → server/discover | Read-only capability projection; must not leak initialize side effects |
| serialization layer → v1 consumers | v1 wire output must not change (strict v1 consumers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-10 | Information Disclosure | `server/discover` capability projection | mitigate | Projection is read-only over the SAME capabilities computed at build (no recompute → no drift/over-disclosure); no auth-gated data added; discover has no side effects |
| T-112-07 | Tampering (compat) | v1 response serialization | mitigate | resultType + serverInfo are injected ONLY when era==V2 (D-07); v1 output byte-identical; byte-identity test guards regression (Pitfall 7) |
| T-112-04b | Spoofing | v1 request reaching a v2-only method | mitigate | server/discover era-gated to V2; V1/non-opted-in → -32601, clean era separation (D-10) |
</threat_model>

<verification>
- `cargo test --lib server_discover` and `cargo test --lib result_type_envelope` green
- `cargo test --lib server::` green (v1 snapshots unchanged)
- `cargo build --lib` and `cargo build --lib --target wasm32-unknown-unknown` clean
- Phase-gate: `cargo semver-checks check-release` classifies minor; `make quality-gate` green
</verification>

<success_criteria>
- v2 server/discover projects capabilities incl. extensions; v1 → -32601
- v2 responses carry resultType:"complete" + serverInfo; v1 responses byte-identical
</success_criteria>

<output>
Create `.planning/phases/112-version-plumbing-spine/112-05-SUMMARY.md` when done
</output>
