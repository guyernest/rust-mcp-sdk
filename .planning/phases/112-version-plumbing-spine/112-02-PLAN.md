---
phase: 112-version-plumbing-spine
plan: 02
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - src/server/cancellation.rs
  - src/shared/cancellation.rs
autonomous: true
requirements: [VERS-01, VERS-03, VERS-09]
must_haves:
  truths:
    - "A handler can read the resolved protocol era via extra.era()"
    - "A handler can read per-request client identity via extra.client_info() and extra.protocol_version()"
    - "A handler can read W3C trace-context via extra.trace_context()"
    - "The native and wasm RequestHandlerExtra expose the identical protocol_context surface (parity)"
    - "Existing RequestHandlerExtra construction (new/with_*) still compiles unchanged for every current caller"
  artifacts:
    - path: "src/server/cancellation.rs"
      provides: "native RequestHandlerExtra.protocol_context field + with_protocol_context + accessors"
      contains: "protocol_context"
    - path: "src/shared/cancellation.rs"
      provides: "wasm RequestHandlerExtra mirror of the same surface"
      contains: "protocol_context"
  key_links:
    - from: "src/server/cancellation.rs"
      to: "src/types/protocol/context.rs"
      via: "protocol_context field holds Option<ProtocolContext>"
      pattern: "protocol_context: Option<.*ProtocolContext>"
---

<objective>
Add the per-request `protocol_context` carrier and its typed accessors to `RequestHandlerExtra` — the handler-facing surface for VERS-01/03/09 — on BOTH the native and wasm structs, following the Phase-109 `request_meta` additive-field precedent exactly.

Purpose: Handlers must read the resolved era and client identity via typed accessors, never via ambient session state. This is the "handler surface" tier; Plan 04 wires the dispatch layer that populates the field.
Output: `protocol_context` field + `with_protocol_context()` builder + `era()/protocol_version()/client_info()/client_capabilities()/trace_context()` accessors, mirrored across `src/server/cancellation.rs` and `src/shared/cancellation.rs`, with a parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-version-plumbing-spine/112-CONTEXT.md
@.planning/phases/112-version-plumbing-spine/112-PATTERNS.md
@.planning/phases/112-version-plumbing-spine/112-01-PLAN.md

<interfaces>
<!-- The additive-field triplet to replicate, from the Phase-109 request_meta precedent. -->

Native RequestHandlerExtra (src/server/cancellation.rs):
  line 178  #[non_exhaustive]
  line 179  pub struct RequestHandlerExtra { ... }
  line 189  pub auth_context: Option<crate::server::auth::AuthContext>,
  line 214  pub request_meta: Option<serde_json::Value>,
  line 254  pub fn new(request_id: String, cancellation_token: CancellationToken) -> Self { ... request_meta: None, ... }
  line 319  pub fn with_request_meta(mut self, meta: Option<serde_json::Value>) -> Self { self.request_meta = meta; self }

Wasm RequestHandlerExtra (src/shared/cancellation.rs):
  line 49   #[non_exhaustive]
  line 50   pub struct RequestHandlerExtra { ... }
  line 69   pub request_meta: Option<serde_json::Value>,
  line 116  pub fn with_request_meta(mut self, meta: Option<serde_json::Value>) -> Self { ... }
  line 158  fn test_shared_extensions_parity() { ... }   // parity-test analog

New types from Plan 01 (crate::types::protocol):
  ProtocolContext { era, negotiated_version, client_info, client_capabilities }
  TraceContext::from_meta(&serde_json::Value) -> Option<TraceContext>
  Era { V1, V2 }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add protocol_context field + builder + era/identity accessors (native + wasm)</name>
  <files>src/server/cancellation.rs, src/shared/cancellation.rs</files>
  <read_first>
    - src/server/cancellation.rs (struct at 178-215; new() default block around 254; with_request_meta at 319; with_auth_context accessor pattern)
    - src/shared/cancellation.rs (struct at 49-70; new() around 86; with_request_meta at 116; #[cfg(not(target_arch = "wasm32"))] gating around auth_context; parity test at 158)
    - src/types/protocol/context.rs (ProtocolContext shape from Plan 01)
  </read_first>
  <behavior>
    - RequestHandlerExtra::new(...) defaults protocol_context to None
    - .with_protocol_context(Some(ctx)) sets the field and returns Self (builder chain compatible with existing .with_* calls)
    - extra.era() returns Some(Era::V2) when a v2 ProtocolContext is attached, None when the field is None
    - extra.protocol_version() returns the negotiated ProtocolVersion when a context is attached, None otherwise
    - extra.client_info() returns Some(&Implementation) when the context carries clientInfo, None otherwise
    - extra.client_capabilities() returns Some(&ClientCapabilities) when present, None otherwise
    - The native and wasm structs expose the identical accessor signatures (parity)
  </behavior>
  <action>
    In BOTH `src/server/cancellation.rs` (native) and `src/shared/cancellation.rs` (wasm): add an additive public field `pub protocol_context: Option<crate::types::protocol::ProtocolContext>` to the `#[non_exhaustive]` `RequestHandlerExtra` struct, default it to `None` in every `new()` constructor, and add `#[must_use] pub fn with_protocol_context(mut self, ctx: Option<ProtocolContext>) -> Self`. Add typed accessors: `era() -> Option<Era>`, `protocol_version() -> Option<&ProtocolVersion>` (or owned clone — match the existing accessor style), `client_info() -> Option<&Implementation>`, `client_capabilities() -> Option<&ClientCapabilities>`. Each accessor reads through `self.protocol_context.as_ref()`.

    Do NOT change the type of any existing field (auth_context/request_meta) — overloading an existing field is an accidental-3.0 break (Pitfall 8). This is purely additive. Follow the wasm file's `#[cfg(not(target_arch = "wasm32"))]` discipline where the native-only fields are gated; `protocol_context` itself is available on BOTH builds (it carries no native-only types — ProtocolContext's field types are all wasm-safe protocol types).

    Add a parity test mirroring `test_shared_extensions_parity`: construct a RequestHandlerExtra with a v2 ProtocolContext via new()+with_protocol_context and assert `era()==Some(Era::V2)`, `client_info()` readback, on both the native and shared modules.
  </action>
  <verify>
    <automated>cargo test --lib cancellation::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'protocol_context' src/server/cancellation.rs` ≥ 3 (field, default, accessor)
    - `grep -c 'protocol_context' src/shared/cancellation.rs` ≥ 3
    - Both files contain `pub fn with_protocol_context` and `pub fn era(`
    - `cargo build --lib` exits 0 AND `cargo build --lib --target wasm32-unknown-unknown` exits 0 (wasm parity)
    - `cargo test --lib cancellation::` exits 0 including the new parity test
    - `grep -n 'pub auth_context' src/server/cancellation.rs` shows the field type unchanged (no existing field mutated)
  </acceptance_criteria>
  <done>Both RequestHandlerExtra structs carry protocol_context + builder + era/identity accessors with byte-parity signatures; existing callers still compile; wasm build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add trace_context() accessor over request_meta (VERS-09)</name>
  <files>src/server/cancellation.rs, src/shared/cancellation.rs</files>
  <read_first>
    - src/server/cancellation.rs (request_meta field at 214; the accessors added in Task 1)
    - src/shared/cancellation.rs (request_meta at 69; Task 1 accessors)
    - src/types/protocol/context.rs (TraceContext::from_meta from Plan 01)
  </read_first>
  <behavior>
    - extra.trace_context() returns Some(TraceContext) when request_meta carries a traceparent key
    - extra.trace_context() returns None when request_meta is None or lacks traceparent
    - traceparent/tracestate/baggage values round-trip from request_meta JSON into the returned TraceContext
  </behavior>
  <action>
    In BOTH cancellation files, add `pub fn trace_context(&self) -> Option<crate::types::protocol::TraceContext>` that reads the existing `self.request_meta` (`Option<serde_json::Value>`) and delegates to `TraceContext::from_meta(...)`. Do NOT add a new field for trace context — VERS-09 keys live in the existing `_meta` flatten-map JSON already carried by `request_meta` (Pattern 3). Keep the accessor identical on native and wasm.

    Add unit tests: build a RequestHandlerExtra via new()+with_request_meta(Some(json!({"traceparent":"00-abc-01","tracestate":"a=1","baggage":"k=v"}))) and assert `trace_context()` returns the exact traceparent/tracestate/baggage; build one with request_meta=None and assert `trace_context()` is None.
  </action>
  <verify>
    <automated>cargo test --lib cancellation::</automated>
  </verify>
  <acceptance_criteria>
    - Both cancellation files contain `pub fn trace_context`
    - No new struct field was added for trace context (grep shows trace_context only as a method, reading request_meta)
    - `cargo test --lib cancellation::` exits 0 including the trace_context tests
    - `cargo build --lib --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>trace_context() surfaces W3C keys from the existing request_meta on both builds, tested, with no new field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client _meta → handler | Self-reported clientInfo / trace keys surfaced to handler code as read-only accessors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02 | Spoofing | `RequestHandlerExtra::client_info()` | accept (documented) | `client_info()` surfaces SELF-REPORTED identity from `_meta` — document in the rustdoc that it is informational only and MUST NOT be used as an authorization anchor; real identity binds to the OAuth token, enforced in Phase 114 (TASK-05). No auth decision is made in this phase from this value |
| T-112-08 | Elevation (semver) | additive field on `#[non_exhaustive]` structs | mitigate | Additive field + `with_*` builder + accessor keeps `new()`/`with_*` callers source-compatible; `cargo semver-checks` at the phase gate confirms minor. Never change an existing field's type |
</threat_model>

<verification>
- `cargo test --lib cancellation::` green (parity + trace_context tests)
- `cargo build --lib` and `cargo build --lib --target wasm32-unknown-unknown` both clean (native+wasm parity)
- `grep -c protocol_context src/server/cancellation.rs src/shared/cancellation.rs` shows the field present in both
</verification>

<success_criteria>
- Handlers can call `extra.era()`, `extra.protocol_version()`, `extra.client_info()`, `extra.client_capabilities()`, `extra.trace_context()` on both builds
- No existing RequestHandlerExtra field type changed; all current callers still compile
</success_criteria>

<output>
Create `.planning/phases/112-version-plumbing-spine/112-02-SUMMARY.md` when done
</output>
