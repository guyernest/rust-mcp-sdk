---
phase: 112-version-plumbing-spine
plan: 08
type: execute
wave: 5
depends_on: [112-03, 112-06]
files_modified:
  - src/server/streamable_http_server.rs
autonomous: true
requirements: [VERS-06]
must_haves:
  truths:
    - "Every production JSON-RPC error-code literal in the streamable-HTTP transport resolves from an error_codes:: constant, not a bare integer"
    - "The transport's parse/invalid-request/internal/method-not-found/auth-required codes emit byte-identical wire values after the literal→constant swap"
    - "Test-module (#[cfg(test)]) and doc-comment literals stay bare — they remain the independent oracle for the constant values"
    - "No production create_error_response call in the file passes a bare -326xx / -32700 / -320xx literal"
  artifacts:
    - path: "src/server/streamable_http_server.rs"
      provides: "all 25 pre-existing transport error-code literals migrated to error_codes:: constants"
      contains: "error_codes::"
  key_links:
    - from: "src/server/streamable_http_server.rs"
      to: "src/types/protocol/error_codes.rs"
      via: "create_error_response / JSON-RPC error construction reads error_codes::{PARSE_ERROR, INVALID_REQUEST, INTERNAL_ERROR, METHOD_NOT_FOUND, AUTHENTICATION_REQUIRED} instead of bare literals; wire values unchanged"
      pattern: "error_codes::(PARSE_ERROR|INVALID_REQUEST|INTERNAL_ERROR|METHOD_NOT_FOUND|AUTHENTICATION_REQUIRED)"
---

<objective>
Close the last VERS-06 gap the checker flagged: migrate the 25 pre-existing bare error-code literals in `src/server/streamable_http_server.rs` (the streamable-HTTP transport — the file Plan 06 also touches) onto Plan 03's centralized `error_codes::` table, so no production error-emission site in the transport carries a raw `-326xx`/`-32700`/`-320xx` literal. This is the streamable-HTTP counterpart of Plan 07 (which migrates core/mod/task_dispatch/jsonrpc).

Purpose: The checker's Blocker showed `streamable_http_server.rs` had ~20+ bare literals outside Plan 07's file list, so VERS-06 ("ALL error codes resolve from one centralized table") and ROADMAP success criterion #5 would be false on completion. This plan (wave 5, after Plan 06's wave-4 header-enforcement edits land in the same file — sequential, no same-wave overlap) performs a pure name-for-value swap. Combined with Plan 03's `error::ErrorCode` delegation (the 210-site dominant surface), Plan 06's error_codes:: new paths, and Plan 07's dispatch/jsonrpc migration, the phase leaves ONE source of truth.
Output: All 25 production literals in `streamable_http_server.rs` read from `error_codes::*`; wire values byte-identical; test/doc literals preserved as the value oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-version-plumbing-spine/112-CONTEXT.md
@.planning/phases/112-version-plumbing-spine/112-RESEARCH.md
@.planning/phases/112-version-plumbing-spine/112-PATTERNS.md
@.planning/phases/112-version-plumbing-spine/112-03-PLAN.md
@.planning/phases/112-version-plumbing-spine/112-06-PLAN.md

<interfaces>
<!-- Plan 03 constants + the exact literal call sites to migrate (verified against the tree; test module begins at line 1720). -->

From Plan 03 (src/types/protocol/error_codes.rs — import as `crate::types::protocol::error_codes`):
  pub const PARSE_ERROR:             i32 = -32700;
  pub const INVALID_REQUEST:         i32 = -32600;
  pub const METHOD_NOT_FOUND:        i32 = -32601;
  pub const INTERNAL_ERROR:          i32 = -32603;
  pub const AUTHENTICATION_REQUIRED: i32 = -32003;

Production literal call sites to migrate in src/server/streamable_http_server.rs (ALL before the #[cfg(test)] module at line 1720 → all production):
  -32700  → error_codes::PARSE_ERROR             lines 395, 403, 415, 423, 435, 443, 1153
  -32600  → error_codes::INVALID_REQUEST         lines 482, 523, 533, 682, 715, 1140, 1317, 1540, 1651, 1699, 1716
  -32603  → error_codes::INTERNAL_ERROR          lines 585, 597, 998, 1084
  -32601  → error_codes::METHOD_NOT_FOUND        line 1549
  -32003  → error_codes::AUTHENTICATION_REQUIRED lines 771, 1055
  (25 sites total; most are `create_error_response(StatusCode::…, -326xx, "…")` — the numeric arg is the JSON-RPC error code, NOT the HTTP StatusCode, which stays as-is)

DO NOT touch: the #[cfg(test)] module (line 1720+) and any `///` doc-comment example literals — they are the independent oracle proving each constant still equals its wire value.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 25 streamable-HTTP transport error-code literals to error_codes:: constants</name>
  <files>src/server/streamable_http_server.rs</files>
  <read_first>
    - src/types/protocol/error_codes.rs (Plan 03 constants — confirm PARSE_ERROR/INVALID_REQUEST/INTERNAL_ERROR/METHOD_NOT_FOUND/AUTHENTICATION_REQUIRED names/values)
    - src/server/streamable_http_server.rs (the create_error_response helper signature — confirm which positional arg is the JSON-RPC code vs the HTTP StatusCode; the literal sites listed in <interfaces>; the test module boundary = first `^#[cfg(test)]` line, which was 1720 at plan time but WILL have shifted after Plan 06's edits — recompute it, do not trust the plan-time number)
    - .planning/phases/112-version-plumbing-spine/112-RESEARCH.md (Pitfall 6 frozen-code discipline; there is no frozen -32002 in THIS file, but keep the name-for-value swap byte-identical)
  </read_first>
  <action>
    Add `use crate::types::protocol::error_codes;` (or reference the fully-qualified path) and replace every PRODUCTION JSON-RPC error-code integer literal with its named constant per the <interfaces> map: `-32700` → `error_codes::PARSE_ERROR`, `-32600` → `error_codes::INVALID_REQUEST`, `-32603` → `error_codes::INTERNAL_ERROR`, `-32601` → `error_codes::METHOD_NOT_FOUND`, `-32003` → `error_codes::AUTHENTICATION_REQUIRED`.

    CRITICAL: this is a NAME-for-VALUE swap only — the emitted wire code stays byte-identical (each constant equals the literal it replaces, guarded by Plan 03's consistency test). Do NOT change any `StatusCode::…` HTTP status argument, message string, or control flow. In `create_error_response(StatusCode::X, -326yy, "msg")` only the middle numeric (the JSON-RPC code) changes.

    LEAVE test-module literals AS-IS: everything inside the `#[cfg(test)]` module (line 1720+) and any `///` doc-comment examples keep bare literals — they are the independent oracle that proves the constant still equals the wire value. Migrating them would make the assertions tautological.
  </action>
  <verify>
    <automated>cargo build --lib && cargo test --lib streamable_http && cargo test --test '*' v2_required_headers</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'error_codes::' src/server/streamable_http_server.rs` ≥ 25 (all production sites migrated)
    - No production error-code literal remains before the test module (boundary computed dynamically — Plan 06 adds code to this file first, so the plan-time line 1720 will have shifted): `TEST_LINE=$(grep -n '^#\[cfg(test)\]' src/server/streamable_http_server.rs | head -1 | cut -d: -f1); awk -v n="$TEST_LINE" 'NR<n' src/server/streamable_http_server.rs | grep -vE '^\s*//|^\s*///' | grep -cE '(-32700|-32600|-32603|-32601|-32003)\b'` == 0
    - The `#[cfg(test)]` module still contains bare literals (oracle preserved): `TEST_LINE=$(grep -n '^#\[cfg(test)\]' src/server/streamable_http_server.rs | head -1 | cut -d: -f1); awk -v n="$TEST_LINE" 'NR>=n' src/server/streamable_http_server.rs | grep -cE '\-32[0-9]{3}'` ≥ 1
    - No HTTP StatusCode or message string changed: `git diff src/server/streamable_http_server.rs` shows only the numeric-code argument swapped to `error_codes::…` (spot-check that `StatusCode::` occurrences are unchanged)
    - `cargo build --lib` exits 0; `cargo test --lib streamable_http` and `cargo test --test '*' v2_required_headers` exit 0 (transport behavior + Plan 06 header gate unchanged)
  </acceptance_criteria>
  <done>All 25 production transport error-code literals resolve from error_codes:: constants; wire values byte-identical; test/doc literals preserved as the oracle; the crate builds and transport/header tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| centralized table ↔ transport wire output | The migrated create_error_response sites must emit the SAME numeric code the table defines — a name/value drift would silently change the HTTP transport's error contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-06e | Tampering | transport error-code wire value during literal→constant migration | mitigate | Name-for-value swap only; each error_codes:: const equals the literal it replaces (Plan 03 consistency test); #[cfg(test)] oracle literals left bare and re-run to prove byte-identity |
| T-112-06f | Tampering | accidental StatusCode / message change during bulk edit | mitigate | Only the JSON-RPC code arg changes; acceptance grep confirms StatusCode:: and message strings untouched; transport tests re-run |
</threat_model>

<verification>
- `cargo build --lib` clean; `cargo test --lib streamable_http` green (transport behavior unchanged)
- `cargo test --test '*' v2_required_headers` green (Plan 06 header gate still passes after migration)
- `grep -c 'error_codes::' src/server/streamable_http_server.rs` ≥ 25; no production `-326xx`/`-32700`/`-320xx` literal before the first `^#[cfg(test)]` line (recomputed at execution time)
- Phase-gate: `make quality-gate` green; `cargo semver-checks check-release` classifies minor
</verification>

<success_criteria>
- The streamable-HTTP transport's 25 production error-code literals are sourced from the centralized error_codes:: table
- Wire values byte-identical; #[cfg(test)] / doc literals preserved as the value oracle
</success_criteria>

<output>
Create `.planning/phases/112-version-plumbing-spine/112-08-SUMMARY.md` when done
</output>
</content>
</invoke>
