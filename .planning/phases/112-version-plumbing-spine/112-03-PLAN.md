---
phase: 112-version-plumbing-spine
plan: 03
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - src/types/protocol/error_codes.rs
  - src/types/protocol/mod.rs
autonomous: true
requirements: [VERS-06, VERS-04]
must_haves:
  truths:
    - "One centralized module holds every protocol error code as a versioned constant"
    - "The frozen v1 task-pending code -32002 is re-exported verbatim and its semantics are unchanged"
    - "v2 error-code values are explicit TODO placeholders (unfilled) pending the final 2026-07-28 schema.json"
    - "ClientRequest carries a ServerDiscover variant that deserializes from method \"server/discover\""
    - "The existing frozen locking test pending_tasks_result_preserves_minus_32002 still passes untouched"
  artifacts:
    - path: "src/types/protocol/error_codes.rs"
      provides: "centralized version-gated error-code constants (v1 real, v2 TODO)"
      contains: "pub const V1_TASK_PENDING: i32 = -32002"
    - path: "src/types/protocol/mod.rs"
      provides: "ClientRequest::ServerDiscover variant + module wiring"
      contains: "server/discover"
  key_links:
    - from: "src/types/protocol/error_codes.rs"
      to: "src/server/task_dispatch.rs"
      via: "V1_TASK_PENDING re-exports the frozen -32002 used at task_dispatch.rs:576 / core.rs:1145"
      pattern: "V1_TASK_PENDING.*-32002"
---

<objective>
Consolidate the scattered error-code integer literals into ONE centralized version-gated module (VERS-06, structure-first) and add the additive `ServerDiscover` variant to `ClientRequest` (VERS-04) — both without touching the frozen `-32002` task-pending semantics or its locking test.

Purpose: VERS-06's "one table" makes the eventual `-32002`/`-32602` reconciliation a single-place fix, and separates pmcp's frozen *task-pending* `-32002` from the spec's *resource-not-found* rename (a different semantic). The `ServerDiscover` variant is the wire hook Plan 05 dispatches on. Both are additive; the phase must stay a 2.x minor.
Output: New `error_codes.rs` module (v1 real, v2 TODO); `ClientRequest::ServerDiscover` variant with an `#[non_exhaustive]` decision gated on `cargo-semver-checks`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-version-plumbing-spine/112-CONTEXT.md
@.planning/phases/112-version-plumbing-spine/112-RESEARCH.md
@.planning/phases/112-version-plumbing-spine/112-PATTERNS.md

<interfaces>
<!-- Anchors for the error table + variant work. -->

Existing standard codes (src/types/protocol/mod.rs:129-138):
  pub enum ProtocolErrorCode {
    InvalidRequest = -32600, MethodNotFound = -32601,
    InvalidParams = -32602, InternalError = -32603,
  }   // NOTE: -32602 is already InvalidParams in this codebase

Frozen task-pending literal (DO NOT edit / reconcile):
  src/server/core.rs:1145            -32002
  src/server/task_dispatch.rs:576    -32002
  src/server/task_dispatch_tests.rs:355  fn pending_tasks_result_preserves_minus_32002 (asserts -32002)

ClientRequest additive-variant precedent (src/types/protocol/mod.rs:478 — NOT #[non_exhaustive]):
  #[serde(rename = "tasks/get")]  TasksGet(crate::types::tasks::GetTaskRequest),
  #[serde(rename = "tasks/list")] TasksList(...),   // variant round-trip tests ~mod.rs:640-657
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create centralized version-gated error-code table (structure-first)</name>
  <files>src/types/protocol/error_codes.rs, src/types/protocol/mod.rs</files>
  <read_first>
    - src/types/protocol/mod.rs (ProtocolErrorCode enum at 128-138; module declarations near top)
    - src/server/core.rs (frozen -32002 usage at 1145)
    - src/server/task_dispatch.rs (frozen -32002 at 576)
    - .planning/phases/112-version-plumbing-spine/112-RESEARCH.md (Pitfall 6; Code Examples "Centralized version-gated error-code table"; Open Question 2 — prefer pub const i32 over new enum variants)
  </read_first>
  <action>
    Create `src/types/protocol/error_codes.rs` as a module of `pub const i32` values (NOT new `ProtocolErrorCode` enum variants — keeping the C-style discriminant surface untouched avoids the Pitfall-4 semver risk). Include the standard JSON-RPC codes as named consts mirroring the existing enum values (`INVALID_REQUEST = -32600`, `METHOD_NOT_FOUND = -32601`, `INVALID_PARAMS = -32602`, `INTERNAL_ERROR = -32603`) and re-export the frozen `pub const V1_TASK_PENDING: i32 = -32002;` with a doc comment naming its call sites (core.rs:1145, task_dispatch.rs:576) and its locking test.

    For v2 values, add COMMENTED-OUT placeholders only, e.g. `// pub const V2_RESOURCE_NOT_FOUND: i32 = /* TODO: fill from final 2026-07-28 schema.json — SEP-2164 maps the spec's resource-not-found -32002 → -32602, a DIFFERENT semantic from pmcp's frozen task-pending -32002 above. Do NOT hard-code before final publication. */;`. Do NOT assign any v2 numeric value in this phase.

    CRITICAL (Pitfall 6 hard rule): do NOT redefine or change the meaning of `-32002`; do NOT edit `pending_tasks_result_preserves_minus_32002`; do NOT reconcile pmcp's task-pending `-32002` with the spec's resource-not-found rename. Add `pub mod error_codes;` to mod.rs and re-export the module.

    Add a compile/consistency test in error_codes.rs: assert `V1_TASK_PENDING == -32002` and that the standard consts equal their `ProtocolErrorCode` enum counterparts (proves the table stays in sync with the existing enum).
  </action>
  <verify>
    <automated>cargo test --lib protocol::error_codes && cargo test --lib pending_tasks_result_preserves_minus_32002</automated>
  </verify>
  <acceptance_criteria>
    - src/types/protocol/error_codes.rs contains `pub const V1_TASK_PENDING: i32 = -32002`
    - error_codes.rs contains NO uncommented v2 numeric error constant (grep for `V2_` returns only commented lines: `grep -n 'V2_' src/types/protocol/error_codes.rs` shows only lines beginning with `//`)
    - src/types/protocol/mod.rs contains `pub mod error_codes`
    - `cargo test --lib pending_tasks_result_preserves_minus_32002` exits 0 and that test file is unmodified (`git diff --stat src/server/task_dispatch_tests.rs` shows no change)
    - `cargo test --lib protocol::error_codes` exits 0
  </acceptance_criteria>
  <done>The centralized table exists with real v1 values, the frozen -32002 re-exported verbatim, and v2 values left as commented TODO; the frozen locking test is untouched and green.</done>
</task>

<task type="auto">
  <name>Task 2: Add ClientRequest::ServerDiscover variant + resolve the #[non_exhaustive] question</name>
  <files>src/types/protocol/mod.rs</files>
  <read_first>
    - src/types/protocol/mod.rs (ClientRequest enum declaration at 475-478 and the TasksGet/TasksList variants ~525-533; variant round-trip tests ~640-657)
    - .planning/phases/112-version-plumbing-spine/112-RESEARCH.md (Pitfall 4; Open Question 1 — run semver-checks both ways, prefer adding #[non_exhaustive] now if minor)
  </read_first>
  <action>
    Add a `ServerDiscover` variant to `ClientRequest` following the TasksGet variant precedent: `#[serde(rename = "server/discover")] ServerDiscover(...)`. Give it a request payload type — a new lightweight `ServerDiscoverRequest` struct (empty-but-extensible, `#[non_exhaustive]`, `Debug/Clone/Serialize/Deserialize`, camelCase) since `server/discover` takes no required params; define it near the other request structs in mod.rs. Do NOT add `TasksUpdate` here (that is Phase 114 territory).

    Resolve Open Question 1: run `cargo semver-checks check-release` first WITHOUT `#[non_exhaustive]` on `ClientRequest`, then WITH it. If adding `#[non_exhaustive]` to `ClientRequest` classifies as a minor change, add it now (it de-risks every later v2.5 variant addition — Phases 113/114). If it classifies major, leave `ClientRequest` as-is and rely on the additive-variant-is-minor classification. Record which path you took and the semver-checks classification in the SUMMARY.

    Extend the existing ClientRequest variant round-trip tests: assert a JSON-RPC request with `"method":"server/discover"` deserializes into `ClientRequest::ServerDiscover` and re-serializes with the `server/discover` method string.
  </action>
  <verify>
    <automated>cargo test --lib protocol:: && cargo semver-checks check-release</automated>
  </verify>
  <acceptance_criteria>
    - src/types/protocol/mod.rs contains `ServerDiscover` and `#[serde(rename = "server/discover")]`
    - A round-trip test proves `{"method":"server/discover",...}` ↔ `ClientRequest::ServerDiscover`
    - `cargo semver-checks check-release` reports NO `major`-classified item (the ServerDiscover addition, and any `#[non_exhaustive]` addition, classify minor); the SUMMARY records the classification
    - `cargo test --lib protocol::` exits 0
  </acceptance_criteria>
  <done>ClientRequest deserializes server/discover into ServerDiscover; the #[non_exhaustive] decision is made and semver-verified minor; round-trip tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final spec schema → error-code values | v2 numeric codes must come only from the published 2026-07-28 schema.json |
| downstream crate `match` → ClientRequest | Adding a variant can break exhaustive matches without a wildcard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-06 | Tampering | frozen `-32002` task-pending code / its locking test | mitigate | V1_TASK_PENDING re-exports -32002 verbatim; v2 values are commented TODO only; do NOT edit `pending_tasks_result_preserves_minus_32002`; a single code never carries two meanings — the -32002/-32602 rename is NOT applied to the task-pending path |
| T-112-08 | Elevation (semver) | `ClientRequest` variant addition (not `#[non_exhaustive]`) | mitigate | Run `cargo semver-checks` both ways; add `#[non_exhaustive]` now iff it classifies minor; otherwise rely on additive-variant-is-minor. Gate blocks any `major` classification |
</threat_model>

<verification>
- `cargo test --lib protocol::error_codes` and `cargo test --lib protocol::` green
- `cargo test --lib pending_tasks_result_preserves_minus_32002` green with `git diff` clean on task_dispatch_tests.rs
- `cargo semver-checks check-release` classifies minor (no major items)
- `grep -n 'V2_' src/types/protocol/error_codes.rs` returns only commented lines
</verification>

<success_criteria>
- One centralized error-code module with real v1 values + commented v2 TODOs; frozen -32002 untouched
- `ClientRequest::ServerDiscover` exists and round-trips; #[non_exhaustive] decision made and semver-verified
</success_criteria>

<output>
Create `.planning/phases/112-version-plumbing-spine/112-03-SUMMARY.md` when done
</output>
