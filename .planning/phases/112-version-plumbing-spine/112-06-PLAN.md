---
phase: 112-version-plumbing-spine
plan: 06
type: execute
wave: 4
depends_on: [112-01, 112-04]
files_modified:
  - src/shared/http_constants.rs
  - src/server/streamable_http_server.rs
autonomous: true
requirements: [VERS-05]
must_haves:
  truths:
    - "On the v2 HTTP path, a request missing Mcp-Method or Mcp-Name is rejected with a 4xx + structured JSON-RPC error"
    - "On the v2 HTTP path, an Mcp-Method header that disagrees with the JSON-RPC body method is rejected (fail closed)"
    - "v2 HTTP responses emit the required Mcp-Method/Mcp-Name/MCP-Protocol-Version headers outbound"
    - "v1 HTTP requests are untouched — no header enforcement runs when the server is not v2-opted-in"
  artifacts:
    - path: "src/shared/http_constants.rs"
      provides: "MCP_METHOD and MCP_NAME header-name constants"
      contains: "MCP_METHOD"
    - path: "src/server/streamable_http_server.rs"
      provides: "v2 strict header reject + header/body method cross-check + outbound header emission"
      contains: "MCP_METHOD"
  key_links:
    - from: "src/server/streamable_http_server.rs"
      to: "src/shared/http_constants.rs"
      via: "uses MCP_METHOD/MCP_NAME constants for inbound validation + outbound emission"
      pattern: "MCP_METHOD"
---

<objective>
Enforce the required v2 HTTP headers (`Mcp-Method`/`Mcp-Name`, alongside `MCP-Protocol-Version`) on the streamable-HTTP path: strict 4xx reject when absent on a v2 request (D-05), header/body method cross-check as an anti-smuggling fail-closed gate (D-06), and outbound emission — all HTTP-only and v1-untouched.

Purpose: VERS-05 is exactly what the official conformance suite (Phase 118) will test; lenient-now would mean a breaking tightening later. The header/body cross-check closes the WAF-desync smuggling class. Headers exist only on HTTP; stdio v2 is header-free (D-11).
Output: `MCP_METHOD`/`MCP_NAME` constants; strict inbound validation + body cross-check + outbound emission in `streamable_http_server.rs`, routed and tested through the HTTP path (not in-memory transport).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-version-plumbing-spine/112-CONTEXT.md
@.planning/phases/112-version-plumbing-spine/112-RESEARCH.md
@.planning/phases/112-version-plumbing-spine/112-PATTERNS.md
@.planning/phases/112-version-plumbing-spine/112-01-PLAN.md
@.planning/phases/112-version-plumbing-spine/112-04-PLAN.md

<interfaces>
<!-- Header-constant + transport anchors. -->

Existing header constants (src/shared/http_constants.rs:4-8):
  pub const MCP_SESSION_ID: &str = "mcp-session-id";
  pub const MCP_PROTOCOL_VERSION: &str = "mcp-protocol-version";

From Plan 01: PROTOCOL_VERSION_2026_07_28 = "2026-07-28"; protocol_era(&str)->Era
From Plan 04: ServerCore.is_v2_opted_in()  (whether to enforce at all — D-04)

Transport: src/server/streamable_http_server.rs — existing header-parse + stateless() branch (the request path Phase 113 later gates onto). This plan adds only the validation gate + outbound headers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCP_METHOD / MCP_NAME header-name constants</name>
  <files>src/shared/http_constants.rs</files>
  <read_first>
    - src/shared/http_constants.rs (existing MCP_SESSION_ID / MCP_PROTOCOL_VERSION const style at 4-8)
  </read_first>
  <action>
    Add `pub const MCP_METHOD: &str = "mcp-method";` and `pub const MCP_NAME: &str = "mcp-name";` next to the existing mcp-* constants, using the same lowercase style (HTTP header names are case-insensitive; the file already uses lowercase for the mcp-* family). Doc-comment each with its VERS-05 purpose.
  </action>
  <verify>
    <automated>cargo build --lib && grep -n 'MCP_METHOD\|MCP_NAME' src/shared/http_constants.rs</automated>
  </verify>
  <acceptance_criteria>
    - http_constants.rs contains `pub const MCP_METHOD: &str = "mcp-method"` and `pub const MCP_NAME: &str = "mcp-name"`
    - `cargo build --lib` exits 0
  </acceptance_criteria>
  <done>The two v2 header-name constants exist in http_constants.rs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strict v2 header reject + header/body method cross-check + outbound emission</name>
  <files>src/server/streamable_http_server.rs</files>
  <read_first>
    - src/server/streamable_http_server.rs (existing header-parse path + stateless() branch; where the request body method is available)
    - src/shared/http_constants.rs (MCP_METHOD/MCP_NAME from Task 1; MCP_PROTOCOL_VERSION)
    - .planning/phases/112-version-plumbing-spine/112-CONTEXT.md (D-05, D-06, D-11)
    - .planning/phases/112-version-plumbing-spine/112-RESEARCH.md (Wave-0 Pitfall 11 — HTTP target, not in-memory transport; Security Domain threat table)
  </read_first>
  <behavior>
    - A v2 HTTP request (MCP-Protocol-Version: 2026-07-28) on an opted-in server with Mcp-Method/Mcp-Name present and matching the body method → processed normally
    - A v2 HTTP request missing Mcp-Method OR Mcp-Name → 4xx response with a structured JSON-RPC error body (D-05)
    - A v2 HTTP request whose Mcp-Method header != the JSON-RPC body `method` → rejected, fail closed (D-06)
    - A v2 HTTP response emits Mcp-Method, Mcp-Name, and MCP-Protocol-Version headers outbound
    - A v1 HTTP request (or any request to a non-opted-in server) → NO header enforcement, behaves exactly as today (D-04/D-11)
  </behavior>
  <action>
    In the streamable-HTTP inbound path, when the server `is_v2_opted_in()` AND the request self-identifies as v2 (via `MCP-Protocol-Version: 2026-07-28`), enforce: (1) reject with a 4xx + structured JSON-RPC error when `Mcp-Method` or `Mcp-Name` are absent (D-05); (2) parse the JSON-RPC body `method` and reject when it does not equal the `Mcp-Method` header value — fail closed, this is the anti-smuggling gate (D-06). Use the `MCP_METHOD`/`MCP_NAME` constants. On the v2 outbound response, set `Mcp-Method`, `Mcp-Name`, and `MCP-Protocol-Version` headers.

    v1 requests and non-opted-in servers get ZERO header enforcement — do not add any era-detection to the v1 path (D-04). Headers apply only where they exist (HTTP); stdio is untouched (D-11).

    Tests MUST route through the real HTTP path (the streamable-HTTP server / ConformanceTarget), NOT in-memory transport, because in-memory transport cannot exercise headers (Pitfall 11). Cover: missing-header reject (4xx + JSON-RPC error shape), header/body mismatch reject, happy-path pass-through, outbound-header presence, and a v1 request untouched.
  </action>
  <verify>
    <automated>cargo test --test '*' v2_required_headers</automated>
  </verify>
  <acceptance_criteria>
    - streamable_http_server.rs references `MCP_METHOD` and `MCP_NAME` for both inbound validation and outbound emission
    - A test over the HTTP path proves a v2 request missing Mcp-Method/Mcp-Name gets a 4xx with a structured JSON-RPC error body
    - A test proves Mcp-Method vs body-method mismatch is rejected (fail closed)
    - A test proves v2 responses carry Mcp-Method/Mcp-Name/MCP-Protocol-Version headers
    - A test proves a v1 request (or non-opted-in server) is not subject to header enforcement
    - Tests use the HTTP transport, not in-memory (no in-memory transport in the header tests)
    - `cargo build --lib` exits 0
  </acceptance_criteria>
  <done>v2 HTTP header enforcement (strict reject + body cross-check + outbound emission) is wired and HTTP-path tested; v1 is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/WAF → HTTP server | Untrusted headers + body cross the transport boundary; a WAF may inspect headers while the server executes the body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03 | Tampering / Elevation | `Mcp-Method` header vs JSON-RPC body `method` desync | mitigate | D-06: cross-check header against body method; reject on mismatch (fail closed) — closes the WAF-sees-one-method/server-runs-another smuggling class |
| T-112-04 | Spoofing / Tampering | missing required v2 headers | mitigate | D-05: strict 4xx reject with structured JSON-RPC error when Mcp-Method/Mcp-Name absent on the v2 path; no lenient window |
| T-112-11 | Tampering (compat) | header enforcement leaking onto v1 | mitigate | Enforcement gated on `is_v2_opted_in()` AND v2 self-identification; v1 requests and non-opted-in servers get zero enforcement (D-04/D-11) |
</threat_model>

<verification>
- `cargo test --test '*' v2_required_headers` green over the HTTP path (not in-memory)
- v1 HTTP regression: existing streamable-HTTP tests unchanged and green
- `cargo build --lib` clean
- Phase-gate: `make quality-gate` green; `cargo semver-checks check-release` classifies minor
</verification>

<success_criteria>
- v2 HTTP requests enforce Mcp-Method/Mcp-Name inbound (strict) + body cross-check + emit them outbound
- v1 HTTP path untouched
</success_criteria>

<output>
Create `.planning/phases/112-version-plumbing-spine/112-06-SUMMARY.md` when done
</output>
