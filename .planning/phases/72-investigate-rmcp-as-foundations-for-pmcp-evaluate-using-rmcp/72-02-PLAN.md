---
phase: 72
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01-PLAN
files_modified:
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md
autonomous: true
requirements:
  - RMCP-EVAL-03
  - RMCP-EVAL-04
tags:
  - research
  - decision
  - rmcp
  - foundations
  - poc
  - rubric

must_haves:
  truths:
    - "72-POC-PROPOSAL.md contains at least 2 PoC slices, each with LOC/Files/Pass/Fail sections, each <=500 LOC touched"
    - "At least one PoC slice is executable in <=3 days (time-box stated explicitly)"
    - "72-DECISION-RUBRIC.md contains at least 5 falsifiable thresholds, each numeric or boolean, each followed by a Data source: line"
    - "Every slice name in 72-POC-PROPOSAL.md appears as a Data source reference in 72-DECISION-RUBRIC.md"
  artifacts:
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md"
      provides: "2-3 candidate PoC slices with LOC/Files/Pass/Fail/time-box"
      min_lines: 80
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md"
      provides: ">=5 falsifiable thresholds each citing a named data source"
      min_lines: 60
  key_links:
    - from: "72-POC-PROPOSAL.md"
      to: "72-INVENTORY.md rows 1-10"
      via: "Slice 1 re-export test surface draws from EXACT rows"
      pattern: "72-INVENTORY"
    - from: "72-DECISION-RUBRIC.md"
      to: "72-POC-PROPOSAL.md slice names"
      via: "Slice 1 / Slice 2 / Slice 3 outcomes used as data sources for thresholds"
      pattern: "Slice [123]"
    - from: "72-DECISION-RUBRIC.md"
      to: "72-STRATEGY-MATRIX.md"
      via: "thresholds map to matrix criteria (maintenance reduction / migration cost / breaking-change surface / etc.)"
      pattern: "72-STRATEGY-MATRIX|Maintenance reduction|Migration cost"
---

<objective>
Produce Phase 72's two mid-phase deliverables — the PoC proposal and the decision rubric — that bridge the inventory/matrix (Plan 01) to the final recommendation (Plan 03).

Purpose: the PoC proposal translates abstract strategy-matrix options into concrete, falsifiable code experiments a future migration-scoping phase could run; the decision rubric sets numerical/boolean thresholds so the Plan 03 recommendation is derivable from data, not from taste.

Output:
  - 72-POC-PROPOSAL.md — 2-3 PoC slices, each <=500 LOC touched, with exact files-to-change lists, hypothesis-tested statements, pass criteria, disqualifying outcomes, and time-box estimates.
  - 72-DECISION-RUBRIC.md — at least 5 falsifiable thresholds covering (at minimum): protocol-types LOC delta, rmcp issue-response-time, enterprise extension-point coverage percent, SemVer break count, PoC time-box. Each threshold cites a named data source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-01-SUMMARY.md

<interfaces>
PoC slice block shape (one per slice, required fields — mandated by 72-VALIDATION.md row 3):
- H3 heading starting with `### PoC Slice N:` (grep -c '^### PoC Slice' is the count gate)
- `LOC: <N>` (numeric, less than 500)
- `Files:` list (pmcp paths to touch)
- `Hypothesis tested:` one-sentence falsifiable statement
- `Pass:` criterion (exact numeric or boolean outcome)
- `Fail:` criterion (the disqualifying outcome)
- `Time-box:` days (at least one slice must be at most 3 days)

Threshold block shape (mandated by 72-VALIDATION.md row 4):
- Line starting with two asterisks wrapping Threshold (a bullet like `- **Threshold:**` — grep is `^- \*\*Threshold:\*\*`)
- Immediately followed by a line containing `Data source:` (named command, deliverable, or CONTEXT.md entry)

At minimum include thresholds for:
1. Protocol-types LOC delta (citing 72-INVENTORY.md Totals)
2. rmcp issue response time / governance responsiveness (citing `gh issue list -R modelcontextprotocol/rust-sdk --state closed`)
3. Enterprise extension-point coverage percent (citing PoC Slice 2 mcp-tester outcome)
4. SemVer break count / v3.0 necessity (citing PoC Slice 1 compile-error count)
5. PoC time-box (citing slice time-box declarations in 72-POC-PROPOSAL.md)
</interfaces>

<read_first>
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 334-378 (§"PoC Scope Sizing" — authoritative 3-slice seed)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 381-419 (§"Decision Framework" — seed rubric table + decision tree)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 421-443 (§"Rmcp Trajectory Risk" — governance responsiveness data)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md lines 48-49 (grep gates for both deliverables)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md (Plan 01 output — Totals table feeds Rubric LOC threshold)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md (Plan 01 output — criteria map to rubric thresholds)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-01-SUMMARY.md (Plan 01 summary — UNVERIFIED rows carried into PoC slice design)
- examples/s01_basic_server.rs (cited by PoC Slice 2; verify exists via Glob)
- src/server/workflow/task_prompt_handler.rs (cited by PoC Slice 3; verify exists and current LOC)
- src/types/jsonrpc.rs (cited by PoC Slice 1)
</read_first>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (markdown only) | Phase 72 Plan 02 produces documentation; no runtime code paths introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03 | Tampering (misrepresented PoC pass criteria could bias the recommendation) | 72-POC-PROPOSAL.md Pass/Fail blocks | mitigate | Pass/Fail criteria are explicitly numeric or boolean AND cite a reproducible command (`cargo check --message-format=json` or `mcp-tester run`); plan-checker verifies falsifiability |
| T-72-04 | Information Disclosure | Decision rubric cites internal git log + gh CLI output | accept | Output is repo-public; no secrets or private issue data included |

This plan modifies markdown only. The PoCs themselves are not executed in Phase 72 — they are proposals for a future migration-scoping phase.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write 72-POC-PROPOSAL.md (three slices, each at most 500 LOC, at least one at most 3 days)</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md</files>
  <read_first>
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 334-378 (§"PoC Scope Sizing" — Slice 1, 2, 3 verbatim seed)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md (row references for target files)
    - src/types/jsonrpc.rs (confirm exists; Slice 1 touches it)
    - examples/s01_basic_server.rs (confirm exists; Slice 2 touches a one-off variant)
    - src/server/workflow/task_prompt_handler.rs (confirm exists; Slice 3 touches it)
  </read_first>
  <action>
Create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md` with EXACTLY the structure below (H3 heading `### PoC Slice N:` for each slice; each slice MUST contain the literal lines `LOC:`, `Files:`, `Hypothesis tested:`, `Pass:`, `Fail:`, `Time-box:`). Content is seeded verbatim from 72-RESEARCH.md §"PoC Scope Sizing" with filename verification.

File contents to write:

```markdown
# Phase 72 — PoC Proposal (RMCP-EVAL-03)

**Baseline pin:** rmcp 1.5.0 / pmcp 2.4.0.
**Scope rule:** every slice at most 500 LOC touched; at least one slice executable in at most 3 days.
**Execution posture:** these slices are PROPOSALS scoped to a FUTURE migration phase (likely v3.0). Phase 72 itself does not execute them — it evaluates their design.
**Data feeds:** slice outcomes are the named data sources for 72-DECISION-RUBRIC.md threshold rows.

---

### PoC Slice 1: Types re-export feasibility

LOC: 100

Files:
- src/types/jsonrpc.rs (delete body, replace with `pub use rmcp::model::{JsonRpcMessage, JsonRpcRequest, JsonRpcResponse, JsonRpcNotification, JsonRpcError, RequestId};`)
- Cargo.toml (add `rmcp = "=1.5.0"` dependency)
- Workspace Cargo.toml dev-dependencies (rmcp for integration test)

Hypothesis tested: pmcp internal code can tolerate rmcp types being re-exported in place of pmcp types without losing spec conformance or serde round-trip identity.

Pass:
- `cargo check --workspace --all-features --message-format=json 2>&1 | grep -c '"level":"error"'` returns at most 50
- `cargo test --workspace -p pmcp --features full types::jsonrpc` passes
- `serde_json::to_value` roundtrip on a canonical `JsonRpcRequest` produces bit-identical JSON vs the pre-swap snapshot (property test)

Fail:
- `cargo check` returns more than 200 errors OR JSON round-trip diverges OR any required trait impl (e.g., a pmcp-specific `From<serde_json::Value>`) is missing AND cannot be added with a blanket impl

Time-box: 0.5 days

Maps to strategy-matrix option: disambiguates A (Full adopt) vs B (Hybrid). Fails means A is off the table, B is the only adopt option.

Cites 72-INVENTORY.md rows: 1 (jsonrpc, EXACT).

---

### PoC Slice 2: One typed tool on rmcp service layer

LOC: 400

Files:
- examples/z01_rmcp_foundation_poc.rs (NEW, one-off — not committed to main)
- Cargo.toml dev-dependencies (rmcp service feature)
- A small adapter module (in-file, not a library crate) that wraps pmcp's `ToolHandler` in an rmcp `ServerHandler` and translates `CallToolRequest` and `CallToolRequestParams` between the two dialects

Hypothesis tested: pmcp's TypedTool and `#[mcp_tool]` macro-generated surface composes on top of rmcp's `ServerHandler`/`Service` without losing its ergonomics (same call sites, same trait impls, same macro output).

Pass:
- Example compiles and runs under stdio transport via `rmcp::service::serve_server(stdio_transport, adapter)`
- `mcp-tester conformance run --target-example examples/z01_rmcp_foundation_poc.rs` returns 0 failures
- Adapter LOC at most 400 total touched

Fail:
- Conformance suite fails OR adapter grows past 400 LOC (integration friction too high for Hybrid)

Time-box: 2 days

Maps to strategy-matrix option: validates Option B (Hybrid wrapper).

Cites 72-INVENTORY.md rows: 4 (tools, EXACT), 21 (protocol framing helpers, Partial).

---

### PoC Slice 3: Workflow engine on rmcp Peer

LOC: 500

Files:
- src/server/workflow/task_prompt_handler.rs (add feature-gated `#[cfg(feature = "rmcp-foundation")]` alternate impl)
- Cargo.toml (new optional feature `rmcp-foundation`)
- examples/s31_workflow_minimal.rs (unchanged, used as end-to-end test)
- examples/s33_workflow_dsl_cookbook.rs (unchanged, used as end-to-end test)

Hypothesis tested: pmcp's workflow engine (Phase 70 `PeerHandle`) runs on rmcp's `Peer<RoleServer>` without workflow DSL regressions and with identical observable semantics.

Pass:
- `cargo run --example s31_workflow_minimal --features rmcp-foundation` emits identical event sequence vs default build (diff-able transcript)
- `cargo run --example s33_workflow_dsl_cookbook --features rmcp-foundation` passes all in-example assertions

Fail:
- DSL semantics drift (event ordering changes, missing event variants) OR any workflow-specific `Peer` method is absent upstream (would require upstream rmcp PR first)

Time-box: 3 days

Maps to strategy-matrix option: validates Option B preserves pmcp workflow engine — a critical pmcp-exclusive asset.

Cites 72-INVENTORY.md rows: 14 (mcp_apps — workflow touches these), 22 (middleware), 24 (cancellation/RequestHandlerExtra).

---

## Slice Selection Recommendation

For the FUTURE migration-scoping phase (not Phase 72 itself):
1. Run Slice 1 first — cheapest signal on whether A (Full adopt) is viable. If fail, stop.
2. Run Slice 2 second — confirms Option B ergonomics hold for the typical pmcp user path.
3. Run Slice 3 only if workflow engine migration is on the critical path (Option B full migration).

## Workspace Ripple Caveat

Every slice must be executed with `--workspace`, not just on the pmcp root crate. Phase 72 Pitfall 5 names workspace ripple (mcp-tester, cargo-pmcp, pmcp-code-mode, pmcp-tasks) as the dominant migration cost. A slice that passes on pmcp alone but fails on downstream crates is a misleading pass.

## Data Feeds Into

- 72-DECISION-RUBRIC.md: Slice 1 compile-error count becomes the "SemVer break count" threshold data source. Slice 2 mcp-tester conformance outcome becomes the "Enterprise extension-point coverage percent" threshold data source. All time-boxes feed the "PoC time-box" threshold.
- 72-RECOMMENDATION.md: if slices are not yet executed at Plan 03 time, rubric falls back to "unresolved, defer decision OR recommend Option D".
```

After writing, run these gates locally (they are the same gates declared in `<acceptance_criteria>` below):

```bash
POC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md
grep -c '^### PoC Slice' "$POC"        # expect 3
grep -c '^LOC:' "$POC"                 # expect 3
grep -c '^Files:' "$POC"               # expect 3
grep -c '^Pass:' "$POC"                # expect 3
grep -c '^Fail:' "$POC"                # expect 3
grep -c '^Time-box:' "$POC"            # expect 3
```
  </action>
  <verify>
    <automated>POC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md; test $(grep -c '^### PoC Slice' $POC) -ge 2 && test $(grep -c '^LOC:' $POC) -ge 2 && test $(grep -c '^Files:' $POC) -ge 2 && test $(grep -c '^Pass:' $POC) -ge 2 && test $(grep -c '^Fail:' $POC) -ge 2 && test $(grep -c '^Time-box:' $POC) -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md`
    - `wc -l` at least 80
    - `grep -c '^### PoC Slice' 72-POC-PROPOSAL.md` at least 2 (we ship 3)
    - For each slice block: grep for `^LOC:`, `^Files:`, `^Pass:`, `^Fail:`, `^Time-box:` must each match at least once per slice (cumulative counts at least 3 each — validation gate)
    - At least one slice has `Time-box:` with a value matching the regex `([0-3](\.[0-9]+)?) day` (at-most-3-day requirement)
    - Every slice's `LOC:` value is an integer at most 500 — verify `grep '^LOC:' 72-POC-PROPOSAL.md | awk '{print $2}' | awk 'BEGIN{max=0} {if ($1>max) max=$1} END{exit !(max<=500)}'`
    - File cites `72-INVENTORY.md` at least 3 times (one per slice)
    - File cites `rmcp 1.5.0` at least once
    - `! grep -iE '\bTBD\b|\bFIXME\b' 72-POC-PROPOSAL.md`
  </acceptance_criteria>
  <done>72-POC-PROPOSAL.md exists with 3 slices satisfying all field requirements; at least one slice has Time-box at most 3 days.</done>
</task>

<task type="auto">
  <name>Task 2: Write 72-DECISION-RUBRIC.md (at least 5 falsifiable thresholds, each with Data source)</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md</files>
  <read_first>
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 381-419 (§"Decision Framework" — seed thresholds with illustrative numbers + decision tree)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 421-443 (§"Rmcp Trajectory Risk" — governance data for responsiveness threshold)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md line 49 (grep gate: `grep -c '^- \*\*Threshold:\*\*'` at least 5, every Threshold followed by Data source)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md (Task 1 output — slice names become data sources)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md (Plan 01 output — LOC totals become threshold data sources)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md (Plan 01 output — criteria map 1:1 onto rubric threshold categories)
  </read_first>
  <action>
Create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md` with EXACTLY this structure. Produce 7 threshold blocks (well above the minimum 5). Each threshold is a bullet starting `- **Threshold:**` with content on the same line; the next line is indented with two spaces and starts `Data source:`.

File contents to write:

```markdown
# Phase 72 — Decision Rubric (RMCP-EVAL-04)

**Baseline pin:** rmcp 1.5.0 / pmcp 2.4.0.
**Purpose:** provide falsifiable thresholds so the Plan 03 recommendation is derivable from measurable inputs rather than from taste.
**Rule:** every threshold is numeric or boolean, and every threshold line is immediately followed by a `Data source:` line naming a command, deliverable, or CONTEXT.md entry.

---

## Thresholds

### T1 — Protocol-types LOC delta

- **Threshold:** If Full-adopt would delete at least 5,000 LOC from `src/types/` + `src/shared/` (per 72-INVENTORY.md Totals), and Hybrid would delete at least 3,000 LOC, then maintenance savings is material enough to accept migration cost.
  Data source: 72-INVENTORY.md §"Totals" (Strong-overlap / deletable under Full adopt row); corroborate with `wc -l src/types/*.rs src/shared/*.rs` executed at Plan 03 time.

### T2 — rmcp governance responsiveness (median issue close time)

- **Threshold:** If rmcp's median time-to-close on closed issues over the last 50 issues is less than 14 days, rmcp governance is responsive enough to depend on. If it is at least 14 days, prefer Option D (Status quo) OR require Option E (Fork) as an escape hatch attached to Option A or B.
  Data source: `gh issue list -R modelcontextprotocol/rust-sdk --state closed --limit 50 --json closedAt,createdAt` then compute median `closedAt - createdAt`. If `gh` is unavailable for that org, fall back to WebFetch on `https://github.com/modelcontextprotocol/rust-sdk/issues?q=is%3Aissue+is%3Aclosed`.

### T3 — Enterprise extension-point coverage percent

- **Threshold:** PoC Slice 2 (see 72-POC-PROPOSAL.md) must produce an adapter of at most 400 LOC AND pass the mcp-tester conformance suite with 0 failures for Option B (Hybrid) to be declared viable. Coverage percent is defined as (passing conformance checks / total conformance checks) * 100 and must be at least 95 percent.
  Data source: 72-POC-PROPOSAL.md PoC Slice 2 execution output (`mcp-tester conformance run --target-example examples/z01_rmcp_foundation_poc.rs`). If slice not executed at Plan 03 time, mark threshold UNRESOLVED.

### T4 — SemVer break count / v3.0 necessity

- **Threshold:** PoC Slice 1 (see 72-POC-PROPOSAL.md) must produce at most 50 compile errors on `cargo check --workspace --all-features` for Option A (Full adopt) to be declared feasible. More than 200 errors disqualifies Option A and forces Option B or below.
  Data source: 72-POC-PROPOSAL.md PoC Slice 1 execution output; command: `cargo check --workspace --all-features --message-format=json 2>&1 | grep -c '"level":"error"'`. If slice not executed at Plan 03 time, treat Option A as conditionally viable pending PoC.

### T5 — PoC time-box

- **Threshold:** At least one PoC slice must have a declared Time-box of at most 3 days, and the aggregate of all PoC slices chosen for execution must be at most 6 days. This caps Phase 72 (or its migration-scoping successor) from drifting into implementation.
  Data source: 72-POC-PROPOSAL.md `Time-box:` declarations per slice. Aggregate is the sum of Time-box values for slices selected in the §"Slice Selection Recommendation" ordering.

### T6 — pmcp v2.x breaking-change window

- **Threshold:** pmcp v2.x must still be in a declared breaking-change window (per PROJECT.md / MEMORY.md / CONTEXT.md) for Option A or B to be recommendable. If window is closed, recommend Option C or D and defer foundation adoption to the next major window.
  Data source: CONTEXT.md entry (if present) OR MEMORY.md `v2.0 cleanup philosophy` reference OR PROJECT.md SemVer policy line. Absence of an explicit CONTEXT.md statement means this threshold is UNRESOLVED and forces a conservative recommendation.

### T7 — Downstream pmcp-user tolerance for v3.0

- **Threshold:** If at least 3 known production pmcp users exist AND at least 2 of them tolerate a v3.0 breaking change, Option A or B is recommendable. If 0 or 1 of 3 tolerate, recommend Option D; if user count is unknown, defer.
  Data source: CONTEXT.md user-survey entry OR MEMORY.md production-user log (pmcp-run is a confirmed major consumer; Lambda feedback note implies at least one more). If CONTEXT.md has no entry, mark UNRESOLVED and surface to 72-RECOMMENDATION.md as an open question.

---

## Decision Tree (from 72-RESEARCH.md §"Decision Framework", verbatim)

```
1. Run Slice 1. Does it pass (T4)?
   YES -> continue to step 2
   NO  -> Option A (Full adopt) disqualified. Consider B/C/D.

2. Run Slice 2. Does it pass within budget (T3)?
   YES -> continue to step 3
   NO  -> Option B (Hybrid) needs deeper scope. Consider C (Selective borrow) or D (Status quo).

3. Is pmcp's v2.x breaking-change window still open (T6)?
   YES -> Option A or B is in play; decide on savings (T1) vs migration cost tradeoff
   NO  -> Option D or C preferred; defer foundation-adoption to next major window

4. Is rmcp's governance responsiveness acceptable (T2)?
   YES -> Option A or B acceptable
   NO  -> Reject A, reject B without an escape hatch; consider E (Fork) only as insurance

5. If A or B selected: publish v3.0 roadmap sketch with Phase 72 as the precursor document.
   If D selected: publish upstream-contribution plan and close Phase 72.
```

## Mapping to Strategy Matrix Criteria (cross-reference)

| Rubric threshold | Strategy matrix criterion (from 72-STRATEGY-MATRIX.md) |
|------------------|---------------------------------------------------------|
| T1 | Maintenance reduction |
| T2 | Spec-upgrade agility (governance is the gating risk) |
| T3 | Enterprise feature preservation |
| T4 | Breaking-change surface |
| T5 | Migration cost |
| T6 | Breaking-change surface (SemVer window gating) |
| T7 | Migration cost (user-borne) |

## Feeds Into

- 72-RECOMMENDATION.md: the Plan 03 recommendation cites these thresholds by ID (T1..T7) in its per-criterion justification subsections. Any UNRESOLVED threshold becomes an open question in the recommendation.
```

After writing, run these gates locally (mirror of `<acceptance_criteria>` below):

```bash
RUBRIC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md
grep -c '^- \*\*Threshold:\*\*' "$RUBRIC"                    # expect 7 (at least 5)
# Every Threshold bullet is immediately followed by a Data source line:
awk '/^- \*\*Threshold:/{need=1; next} need && /Data source:/{hit++; need=0} END{print hit}' "$RUBRIC"
# expect 7
```
  </action>
  <verify>
    <automated>RUBRIC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md; test $(grep -c '^- \*\*Threshold:\*\*' $RUBRIC) -ge 5 && test $(awk '/^- \*\*Threshold:/{need=1; next} need && /Data source:/{hit++; need=0} END{print hit}' $RUBRIC) -ge 5 && grep -q 'PoC Slice 1' $RUBRIC && grep -q 'PoC Slice 2' $RUBRIC && grep -q '72-INVENTORY.md' $RUBRIC && grep -q 'rmcp 1.5.0' $RUBRIC</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md`
    - `wc -l` at least 60
    - `grep -c '^- \*\*Threshold:\*\*' 72-DECISION-RUBRIC.md` at least 5 (we ship 7)
    - Every `- **Threshold:**` bullet is immediately followed by a `Data source:` line (awk check in verify passes; count equals threshold count)
    - `grep -c 'PoC Slice' 72-DECISION-RUBRIC.md` at least 2 (Slice 1 and Slice 2 cited as data sources)
    - `grep -c '72-INVENTORY.md' 72-DECISION-RUBRIC.md` at least 1 (Totals cited as T1 data source)
    - `grep -q 'Decision Tree' 72-DECISION-RUBRIC.md` (decision tree section present)
    - `grep -c 'Mapping to Strategy Matrix' 72-DECISION-RUBRIC.md` at least 1 (cross-ref table present)
    - `! grep -iE '\bTBD\b|\bFIXME\b' 72-DECISION-RUBRIC.md`
  </acceptance_criteria>
  <done>72-DECISION-RUBRIC.md exists with at least 5 threshold bullets, each followed by Data source, cross-referenced to PoC slices and inventory/matrix.</done>
</task>

<task type="auto">
  <name>Task 3: Commit Plan 02 deliverables</name>
  <files>
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md,
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md
  </files>
  <read_first>
    - CLAUDE.md §"Quality Gate Enforcement" (markdown-only commit; quality gates don't apply to this plan)
  </read_first>
  <action>
Commit the two produced files:

```bash
gsd-sdk query commit "docs(72): add PoC proposal and decision rubric" \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md
```

Fallback if `gsd-sdk query commit` fails because of unrelated staged files:

```bash
git add .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md \
        .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md
git commit -m "docs(72): add PoC proposal and decision rubric"
```

Do NOT use `--no-verify`. Do NOT run `make quality-gate` (markdown-only change).
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -q '72-POC-PROPOSAL.md' && git log -1 --name-only | grep -q '72-DECISION-RUBRIC.md' && git log -1 --pretty=%s | grep -qE '^docs\(72\):'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` matches `docs(72):`
    - `git log -1 --name-only` lists both POC-PROPOSAL.md and DECISION-RUBRIC.md
    - `git status --porcelain` returns empty for both files (clean)
  </acceptance_criteria>
  <done>Plan 02 deliverables committed; POC-PROPOSAL.md and DECISION-RUBRIC.md on current branch.</done>
</task>

</tasks>

<verification>
Plan 02 is complete when:
1. 72-POC-PROPOSAL.md has at least 2 `### PoC Slice` H3 blocks, each with all 6 required fields, and at least one Time-box at most 3 days.
2. 72-DECISION-RUBRIC.md has at least 5 `- **Threshold:**` bullets, each immediately followed by `Data source:`.
3. Every PoC slice name in 72-POC-PROPOSAL.md (Slice 1, Slice 2, Slice 3) appears as a data source reference in 72-DECISION-RUBRIC.md (at least Slice 1 and Slice 2).
4. Plan 02's commit is on `HEAD`.

Cross-document consistency check (manual, logged in SUMMARY):
- Every `LOC:` value in POC-PROPOSAL is at most 500 (bounded by the scope rule).
- Every strategy-matrix criterion (5 columns) has at least one threshold in the rubric mapping.
- T1 LOC delta matches 72-INVENTORY.md Totals within 10 percent.
</verification>

<success_criteria>
- 72-POC-PROPOSAL.md exists with 3 slices, each with LOC/Files/Pass/Fail/Time-box fields satisfying validation gates.
- 72-DECISION-RUBRIC.md exists with at least 5 falsifiable thresholds, each followed by Data source, covering the 5 mandated categories.
- One git commit on HEAD with message `docs(72): add PoC proposal and decision rubric`.
</success_criteria>

<output>
After completion, create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-02-SUMMARY.md` documenting:
- Number of PoC slices shipped (target 3; minimum 2) and their Time-box distribution.
- Number of rubric thresholds shipped (target 7; minimum 5) and which strategy-matrix criteria each maps to.
- Count of UNRESOLVED thresholds that Plan 03 will need CONTEXT.md input to close (expected: at least T6 and T7 pending CONTEXT.md).
- Cross-doc consistency: every slice cited as a data source in the rubric; every inventory Totals row referenced by at least one threshold.
</output>
