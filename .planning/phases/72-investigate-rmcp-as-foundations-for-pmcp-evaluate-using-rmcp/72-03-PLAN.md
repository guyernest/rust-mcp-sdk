---
phase: 72
plan: 03
type: execute
wave: 3
depends_on:
  - 72-01-PLAN
  - 72-02-PLAN
files_modified:
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
autonomous: true
requirements:
  - RMCP-EVAL-05
tags:
  - research
  - decision
  - rmcp
  - foundations
  - recommendation
  - synthesis

must_haves:
  truths:
    - "72-RECOMMENDATION.md starts with `**Recommendation:** <A|B|C|D|E>` in the first 5 lines"
    - "72-RECOMMENDATION.md contains at least 5 `### Criterion` subsections, one per strategy-matrix criterion"
    - "Each `### Criterion` subsection cites at least one threshold (T1..T7) from 72-DECISION-RUBRIC.md and at least one row or Option cell from 72-INVENTORY.md / 72-STRATEGY-MATRIX.md"
    - "The recommendation either picks a concrete option (A/B/C/D/E) or explicitly defers with a named rationale (UNRESOLVED thresholds listed)"
  artifacts:
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md"
      provides: "Final go/no-go recommendation with per-criterion justification — closes RMCP-EVAL-05"
      min_lines: 100
  key_links:
    - from: "72-RECOMMENDATION.md"
      to: "72-DECISION-RUBRIC.md thresholds T1..T7"
      via: "per-criterion subsections cite threshold IDs"
      pattern: "T[1-7]"
    - from: "72-RECOMMENDATION.md"
      to: "72-STRATEGY-MATRIX.md option rows"
      via: "recommendation cites the winning option's matrix cells for each criterion"
      pattern: "Option [A-E]|A\\. Full adopt|B\\. Hybrid|C\\. Selective|D\\. Status quo|E\\. Fork"
    - from: "72-RECOMMENDATION.md"
      to: "72-INVENTORY.md rows"
      via: "per-criterion justifications cite inventory rows by number"
      pattern: "72-INVENTORY|row [0-9]"
---

<objective>
Synthesize the four prior deliverables (72-INVENTORY.md, 72-STRATEGY-MATRIX.md, 72-POC-PROPOSAL.md, 72-DECISION-RUBRIC.md) into Phase 72's single final output: the go/no-go recommendation.

Purpose: close RMCP-EVAL-05. Convert measured evidence into a single answer to the phase question — should pmcp refactor its protocol layer onto rmcp 1.5.0, and if so, under which of the five options? The recommendation must be traceable (every justification cites a threshold ID and an inventory/matrix row), falsifiable (if a threshold's data is UNRESOLVED, that is stated, not hidden), and contained (no migration-planning scope creep).

Output:
  - 72-RECOMMENDATION.md — opens with a one-line `**Recommendation:** <A|B|C|D|E>` declaration, followed by per-criterion justification subsections (one `### Criterion: ...` H3 per strategy-matrix column), a UNRESOLVED-thresholds section if any, and a "Next Steps" pointer to a future v3.0 migration-scoping phase (if A or B selected) or an upstream-contribution plan (if D selected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-01-SUMMARY.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-02-SUMMARY.md

<interfaces>
Recommendation file shape (mandated by 72-VALIDATION.md row 5):
- First 5 lines MUST contain a line matching regex `^\*\*Recommendation:\*\* [A-E]` (header declaration).
- At least 5 `### Criterion` H3 subsections, one per strategy-matrix criterion (Maintenance reduction, Migration cost, Breaking-change surface, Enterprise feature preservation, Spec-upgrade agility).

Validation gate commands (from 72-VALIDATION.md):
- `head -5 72-RECOMMENDATION.md | grep -E '^\*\*Recommendation:\*\* [A-E]'`
- `grep -c '^### Criterion' 72-RECOMMENDATION.md` at least 5

Recommendation derivation (from 72-DECISION-RUBRIC.md §"Decision Tree"):
1. If T4 passes (Slice 1 less than 50 errors) AND T3 passes (Slice 2 adapter at most 400 LOC AND conformance green) AND T6 open AND T2 responsive -> Option A or B eligible
2. If T4 fails but T3 passes -> Option B (not A)
3. If T3 fails -> Option C or D
4. If T6 closed -> Option C or D regardless
5. If T2 unresponsive -> Option E as insurance attached to A or B, or outright Option D

Because Phase 72 does not EXECUTE the PoCs (they are proposals for a future phase), Slice outcomes are UNRESOLVED at synthesis time. The default posture when multiple thresholds are UNRESOLVED is: recommend Option B (Hybrid) conditionally, with an explicit "gate pending PoC execution" annotation — OR recommend Option D if CONTEXT.md says v2.x window is closed or user tolerance is zero. The executor MUST consult 72-02-SUMMARY.md for the UNRESOLVED threshold list and CONTEXT.md (if present) for T6/T7 answers before finalizing the pick.
</interfaces>

<read_first>
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md (Plan 01 — row references for per-criterion justification)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md (Plan 01 — cite winning option's cells per criterion)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-POC-PROPOSAL.md (Plan 02 — Slice names cited in Criterion justifications)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md (Plan 02 — T1..T7 IDs cited in every criterion subsection)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-01-SUMMARY.md (Plan 01 summary)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-02-SUMMARY.md (Plan 02 summary — source of UNRESOLVED threshold list)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 263-283 (§"Comparable Precedents" — tonic/prost is the Option B analogue)
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md (per-REQ validation criteria)
- Optionally: any `.planning/phases/72-.../72-CONTEXT.md` if it exists (provides user locks on T6 v2.x window and T7 user tolerance); if absent, executor documents absence and treats T6/T7 as UNRESOLVED
</read_first>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (markdown only) | Phase 72 Plan 03 produces the final recommendation document; no runtime code paths introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05 | Repudiation (recommendation without cited evidence could be later disowned) | 72-RECOMMENDATION.md per-criterion subsections | mitigate | Every criterion subsection MUST cite a threshold ID (T1..T7) AND an inventory/matrix row; missing citations fail the acceptance check |
| T-72-06 | Tampering (aesthetic adoption of rmcp despite rubric failure — Pitfall 3 from 72-RESEARCH.md) | recommendation pick | mitigate | Executor MUST follow the decision tree in 72-DECISION-RUBRIC.md; if thresholds disqualify A or B, the recommendation MUST pick C or D even if that feels like "less ambitious"; plan-checker verifies decision-tree traversal in SUMMARY |

This plan modifies markdown only. The recommendation is a decision document; acting on it (i.e., migration) is a separate future phase.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Derive the recommendation from the decision tree and UNRESOLVED thresholds</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md</files>
  <read_first>
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-DECISION-RUBRIC.md (§"Decision Tree" — walk it using current UNRESOLVED state)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-02-SUMMARY.md (UNRESOLVED threshold list authoritative from Plan 02)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md (per-option per-criterion cells feeding justifications)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md (row citations for Maintenance-reduction + Enterprise-preservation justifications)
    - If `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-CONTEXT.md` exists, read it for T6/T7 user locks
  </read_first>
  <action>
Walk the 72-DECISION-RUBRIC.md decision tree using current evidence. Since Phase 72 does NOT execute PoCs, T3 and T4 are UNRESOLVED at synthesis time. The executor MUST:

1. Check for `72-CONTEXT.md` in the phase directory.
   - If present and it locks T6 (v2.x window state) and T7 (user tolerance), use those locks.
   - If absent, mark T6 and T7 as UNRESOLVED in the recommendation.

2. Apply this decision logic given the expected UNRESOLVED set (T3, T4, T6, T7):

   - If T6 locked "closed" OR T7 locked "zero tolerance" -> **Recommendation: D** (Status quo + upstream PRs).
   - If T2 (rmcp governance responsiveness) has NOT been measured yet AND no CONTEXT.md override -> add a "Prerequisite Measurements" subsection listing the `gh issue list` query the next phase must run.
   - Otherwise (T6/T7 open or locked "open", T3/T4 pending PoC, T2 pending measurement) -> **Recommendation: B (conditional)** — Hybrid / wrapper pattern, conditional on Slice 1 passing T4 (at most 50 compile errors) and Slice 2 passing T3 (at most 400 LOC adapter + 0 conformance failures) in a follow-on PoC phase. Precedent: tonic over prost, axum over hyper (72-RESEARCH.md §"Comparable Precedents").

3. Write `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md` with EXACTLY the structure below. Fill the recommendation letter per step 2. If step 2 yields "B (conditional)", the first 5 lines still carry `**Recommendation:** B` — the word "conditional" and its gating thresholds belong in the body.

File structure (produce this verbatim, filling the letter and letting the Criterion cells reflect the chosen option):

```markdown
# Phase 72 — Final Recommendation (RMCP-EVAL-05)

**Recommendation:** <A|B|C|D|E>
**Baseline pin:** rmcp 1.5.0 / pmcp 2.4.0.
**Synthesis date:** 2026-04-19.

## Summary

One-paragraph statement naming the chosen option, the precedent it follows (tonic/prost for B; reqwest/hyper for A; sqlx for C; none for D; vendored-fork for E), and the headline consequence (v3.0 required / stays on 2.x / etc.). Must NOT introduce new evidence — only synthesize Plan 01 and Plan 02 outputs.

## Per-Criterion Justification

Each subsection cites at least one threshold ID (T1..T7) from 72-DECISION-RUBRIC.md AND at least one 72-INVENTORY.md row number OR 72-STRATEGY-MATRIX.md cell.

### Criterion: Maintenance reduction

Justification: <two-to-four sentences citing T1 (protocol-types LOC delta) and the matrix row for the chosen option, and at least one inventory row (e.g., row 1 jsonrpc, row 4 tools) showing what gets deleted vs retained>

### Criterion: Migration cost

Justification: <two-to-four sentences citing T5 (PoC time-box) and T4 (Slice 1 SemVer break count, UNRESOLVED pending PoC), the matrix "Migration cost" cell for the chosen option, and the workspace-ripple caveat from 72-POC-PROPOSAL.md §"Workspace Ripple Caveat">

### Criterion: Breaking-change surface

Justification: <two-to-four sentences citing T4 (SemVer break count) and T6 (v2.x window), the matrix "Breaking-change surface" cell for the chosen option, and stating whether v3.0 is required>

### Criterion: Enterprise feature preservation

Justification: <two-to-four sentences citing T3 (extension-point coverage percent) and inventory rows 14 (mcp_apps), 16 (websocket), 22 (middleware), 23 (simd_parsing) as pmcp-exclusive assets preserved; state what mcp-tester verifies in Slice 2>

### Criterion: Spec-upgrade agility

Justification: <two-to-four sentences citing T2 (rmcp governance responsiveness) and the matrix "Spec-upgrade agility" cell for the chosen option; note pin + patch-fork strategy from 72-RESEARCH.md §"Rmcp Trajectory Risk" if Option A or B chosen>

## UNRESOLVED Thresholds

List every threshold Plan 02 tagged UNRESOLVED. For each, state what the next phase (migration-scoping, likely v3.0) must produce to close it. Expected entries:
- T2 — `gh issue list` analysis on modelcontextprotocol/rust-sdk closed issues, last 50
- T3 — PoC Slice 2 execution and mcp-tester conformance run
- T4 — PoC Slice 1 execution and `cargo check --workspace` error count
- T6 — CONTEXT.md entry on v2.x breaking-change window state (if not already locked)
- T7 — CONTEXT.md entry OR lightweight pmcp-user survey on v3.0 tolerance (if not already locked)

If any threshold has a CONTEXT.md lock, move it from UNRESOLVED to "Resolved via CONTEXT.md" with the lock quoted verbatim.

## Next Steps

Exactly one of these, matching the Recommendation letter:

- If A or B: "Open a v3.0 migration-scoping phase. Kickoff deliverable: a PoC execution report running PoC Slice 1, PoC Slice 2 (and optionally Slice 3). Do NOT migrate production code until the PoC report closes T3 and T4." Cite 72-POC-PROPOSAL.md as the scoping input.
- If C: "Open a selective-borrow phase limited to the single rmcp subsystem chosen (e.g., Unix-socket transport OR task_manager). Scope at most 1,500 LOC touched."
- If D: "Open an upstream-contribution phase. Identify the top 3 rmcp ergonomic gaps pmcp can upstream (seed from 72-INVENTORY.md rows 9 ElicitationSchemaBuilder, and from 69-PROPOSALS.md unshipped rows). Do NOT migrate; keep pmcp's protocol layer first-party."
- If E: "Escape hatch only. Do not select E as a primary strategy. Document the conditions under which Fork would be activated (e.g., rmcp critical bug unresponded for more than 14 days, per T2)."

## Provenance

- 72-INVENTORY.md (RMCP-EVAL-01)
- 72-STRATEGY-MATRIX.md (RMCP-EVAL-02)
- 72-POC-PROPOSAL.md (RMCP-EVAL-03)
- 72-DECISION-RUBRIC.md (RMCP-EVAL-04)
- 72-RESEARCH.md (phase brief)

## Closes

- RMCP-EVAL-05 (this document IS the deliverable).
```

Verify after writing (same as `<acceptance_criteria>` below):
```bash
REC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
head -5 "$REC" | grep -E '^\*\*Recommendation:\*\* [A-E]'
grep -c '^### Criterion' "$REC"    # expect 5
```
  </action>
  <verify>
    <automated>REC=.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md; head -5 $REC | grep -qE '^\*\*Recommendation:\*\* [A-E]' && test $(grep -c '^### Criterion' $REC) -ge 5 && grep -qE 'T[1-7]' $REC && grep -q '72-INVENTORY.md' $REC && grep -q '72-DECISION-RUBRIC.md' $REC && grep -q '72-POC-PROPOSAL.md' $REC && grep -q '72-STRATEGY-MATRIX.md' $REC</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md`
    - `wc -l` at least 100
    - `head -5 72-RECOMMENDATION.md | grep -E '^\*\*Recommendation:\*\* [A-E]'` matches exactly one line (validation gate row 5)
    - `grep -c '^### Criterion' 72-RECOMMENDATION.md` returns at least 5 (one per strategy-matrix column)
    - Every `### Criterion` block cites at least one threshold ID from T1..T7: `grep -A 6 '^### Criterion' 72-RECOMMENDATION.md | grep -cE 'T[1-7]'` at least 5
    - All four prior deliverables cited at least once: `grep -c '72-INVENTORY.md\|72-STRATEGY-MATRIX.md\|72-POC-PROPOSAL.md\|72-DECISION-RUBRIC.md' 72-RECOMMENDATION.md` at least 4
    - `grep -q '^## UNRESOLVED Thresholds' 72-RECOMMENDATION.md` (or `## Resolved via CONTEXT.md` if all thresholds are locked)
    - `grep -q '^## Next Steps' 72-RECOMMENDATION.md`
    - `grep -q 'Closes' 72-RECOMMENDATION.md` AND `grep -q 'RMCP-EVAL-05' 72-RECOMMENDATION.md`
    - `! grep -iE '\bTBD\b|\bFIXME\b' 72-RECOMMENDATION.md` (no placeholders; UNRESOLVED is allowed as the explicit status marker)
    - The chosen option letter in the header matches the letter referenced in `## Next Steps` (consistency check: `head -5 $REC | grep -oE 'Recommendation:\*\* [A-E]'` letter == bullet activated in Next Steps)
  </acceptance_criteria>
  <done>72-RECOMMENDATION.md exists with a single `**Recommendation:** <letter>` header, 5 Criterion subsections each citing T-IDs and inventory/matrix rows, an UNRESOLVED (or Resolved-via-CONTEXT) section, and a Next Steps subsection consistent with the chosen letter.</done>
</task>

<task type="auto">
  <name>Task 2: Commit Plan 03 deliverable and close Phase 72</name>
  <files>
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
  </files>
  <read_first>
    - CLAUDE.md §"Quality Gate Enforcement" (markdown-only commit)
  </read_first>
  <action>
Commit the recommendation:

```bash
gsd-sdk query commit "docs(72): final recommendation on rmcp foundation adoption (closes RMCP-EVAL-05)" \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
```

Fallback:

```bash
git add .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RECOMMENDATION.md
git commit -m "docs(72): final recommendation on rmcp foundation adoption (closes RMCP-EVAL-05)"
```

Do NOT run `make quality-gate` (markdown-only). Do NOT use `--no-verify`.

Do NOT in this task:
- Mark any RMCP-EVAL-## row as `[x]` complete in REQUIREMENTS.md — that is the job of a dedicated close-out pass (via `/gsd-verify-work` or a follow-on roadmap-update step), not this plan.
- Update ROADMAP.md `Plans: N plans complete` counter — that is automated elsewhere.
- Edit STATE.md — session-continuity updates happen outside this plan.
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -q '72-RECOMMENDATION.md' && git log -1 --pretty=%s | grep -qE 'docs\(72\):.*recommendation' && git log -1 --pretty=%s | grep -q 'RMCP-EVAL-05'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` contains `docs(72):` AND `recommendation` AND `RMCP-EVAL-05`
    - `git log -1 --name-only` lists `72-RECOMMENDATION.md`
    - `git status --porcelain` is empty for the recommendation file (clean)
    - No unrelated files appear in the commit (`git log -1 --stat` shows exactly 1 file changed)
  </acceptance_criteria>
  <done>Phase 72 final deliverable committed on HEAD; recommendation closes RMCP-EVAL-05; phase ready for `/gsd-verify-work`.</done>
</task>

</tasks>

<verification>
Plan 03 is complete (AND Phase 72 is ready for verification) when:
1. 72-RECOMMENDATION.md opens with `**Recommendation:** <A|B|C|D|E>` in the first 5 lines.
2. It contains at least 5 `### Criterion` H3 subsections, each citing a T-ID and an inventory/matrix row.
3. UNRESOLVED (or Resolved-via-CONTEXT) section enumerates every threshold status.
4. Next Steps section is consistent with the chosen letter.
5. Plan 03's commit is on `HEAD`.
6. All five Phase 72 deliverables exist:
   - 72-INVENTORY.md
   - 72-STRATEGY-MATRIX.md
   - 72-POC-PROPOSAL.md
   - 72-DECISION-RUBRIC.md
   - 72-RECOMMENDATION.md

Cross-document consistency check (for plan-checker):
- Every strategy-matrix criterion (5 columns) appears as a `### Criterion` subsection in the recommendation.
- Every threshold (T1..T7) referenced in the recommendation exists in 72-DECISION-RUBRIC.md.
- Every slice cited in the recommendation exists in 72-POC-PROPOSAL.md.
- The winning option's strategy-matrix row justifies the same claims the recommendation makes.

Per 72-VALIDATION.md Falsifiability Checklist: reviewer fills the phase-gate checklist; this plan's SUMMARY must report PASS/FAIL on each item.
</verification>

<success_criteria>
- 72-RECOMMENDATION.md exists with the mandated header, 5 Criterion subsections each citing T-IDs + inventory/matrix rows, an UNRESOLVED section, and a Next Steps subsection consistent with the chosen option letter.
- One git commit on HEAD with message matching `docs(72): final recommendation on rmcp foundation adoption (closes RMCP-EVAL-05)`.
- Phase 72 now has all 5 deliverables present under `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/`.
</success_criteria>

<output>
After completion, create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-03-SUMMARY.md` documenting:
- Chosen recommendation letter (A/B/C/D/E) and one-sentence rationale.
- Count of UNRESOLVED thresholds at recommendation time (expected non-zero because Phase 72 does not execute PoCs).
- Decision-tree traversal log: which branch was taken at each step (steps 1–5 of 72-DECISION-RUBRIC.md §"Decision Tree").
- Whether 72-CONTEXT.md was present and which thresholds it locked.
- Falsifiability Checklist status (each of the 5 phase-gate items PASS/FAIL).
- Next-Steps handoff: whether the successor is a v3.0 migration-scoping phase, a selective-borrow phase, or an upstream-contribution phase.
</output>
