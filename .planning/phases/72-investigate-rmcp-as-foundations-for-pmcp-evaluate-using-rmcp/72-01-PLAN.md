---
phase: 72
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md
  - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
autonomous: true
requirements:
  - RMCP-EVAL-01
  - RMCP-EVAL-02
tags:
  - research
  - decision
  - rmcp
  - foundations
  - inventory
  - strategy-matrix

must_haves:
  truths:
    - "REQUIREMENTS.md contains rows for RMCP-EVAL-01 through RMCP-EVAL-05"
    - "72-INVENTORY.md lists at least 15 pmcp module families with pmcp file:line evidence and rmcp doc/blob evidence for each"
    - "72-STRATEGY-MATRIX.md contains all 25 cells (5 options x 5 criteria) with no TBD placeholders"
    - "Every row in INVENTORY maps cleanly onto one of the 5 strategy-matrix options (no orphan modules)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "RMCP-EVAL-01..05 requirement rows + traceability rows"
      contains: "RMCP-EVAL-01"
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md"
      provides: "Inversion inventory: pmcp -> rmcp module mapping with overlap classification"
      min_lines: 60
    - path: ".planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md"
      provides: "5x5 scored strategy matrix (Full adopt / Hybrid / Selective / Status quo / Fork) with per-cell rationale"
      min_lines: 80
  key_links:
    - from: "72-INVENTORY.md"
      to: "72-RESEARCH.md §'Inversion Inventory'"
      via: "row-by-row correspondence with overlap ratings preserved"
      pattern: "EXACT|UNVERIFIED|NO RMCP EQUIVALENT|Partial"
    - from: "72-STRATEGY-MATRIX.md"
      to: "72-INVENTORY.md totals row"
      via: "LOC-savings columns cite inventory per-row impact"
      pattern: "LOC"
    - from: ".planning/REQUIREMENTS.md"
      to: "72-RESEARCH.md §'Proposed REQ-IDs'"
      via: "one row per RMCP-EVAL-## with description copied from research"
      pattern: "RMCP-EVAL-0[1-5]"
---

<objective>
Seed Phase 72's two anchor deliverables — the inversion inventory and the strategy matrix — and land the five RMCP-EVAL REQ-IDs in `.planning/REQUIREMENTS.md`.

Purpose: give downstream plans (02, 03) the canonical module-overlap evidence and the scored option framework they need to build a PoC proposal, a decision rubric, and a final recommendation. These two documents are the backbone of the decision; every later deliverable cites them.

Output:
  - Five RMCP-EVAL-01..05 rows in REQUIREMENTS.md with Traceability entries.
  - `72-INVENTORY.md` — inversion inventory covering >=15 pmcp module families, each row carrying pmcp file:line evidence, rmcp evidence (docs.rs anchor or github.com blob URL), overlap rating, and migration-LOC impact.
  - `72-STRATEGY-MATRIX.md` — scored 5-options x 5-criteria matrix with rationale per cell, no TBD placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md
@.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md

<interfaces>
<!-- RMCP-EVAL REQ-IDs (authoritative text from 72-RESEARCH.md §"Proposed REQ-IDs") -->
- RMCP-EVAL-01: Produce a source-citation-backed inversion inventory covering every module in src/types/ and src/shared/ (and src/server/cancellation.rs), with nearest rmcp 1.5.0 equivalent and overlap classification (EXACT / Partial / pmcp-superset / NO RMCP EQUIVALENT / UNVERIFIED).
- RMCP-EVAL-02: Score each of five architectural options (A. Full adopt / B. Hybrid wrapper / C. Selective borrow / D. Status quo + upstream PRs / E. Fork rmcp) against five criteria (maintenance reduction, migration cost, breaking-change surface, enterprise feature preservation, upgrade agility).
- RMCP-EVAL-03: Propose 2-3 candidate PoC slices <=500 LOC each with falsifiable pass/fail criteria.
- RMCP-EVAL-04: Decision rubric with >=5 falsifiable thresholds each citing a named data source.
- RMCP-EVAL-05: Final recommendation (A|B|C|D|E) with per-criterion justification citing RMCP-EVAL-04 thresholds and RMCP-EVAL-01 rows.

<!-- Row structure mandated by 72-VALIDATION.md (Per-Task Verification Map row 1) -->
INVENTORY row grep gate: `grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' 72-INVENTORY.md` >= 15
(i.e., every row must contain a pmcp file:line cell shaped `| path/to/file.rs:NNN |`).

<!-- Row structure mandated by 72-VALIDATION.md (Per-Task Verification Map row 2) -->
STRATEGY-MATRIX option rows: `grep -E '^\| (A\. Full adopt|B\. Hybrid|C\. Selective|D\. Status quo|E\. Fork)' 72-STRATEGY-MATRIX.md` must match >=5 lines, AND `! grep -i 'TBD' 72-STRATEGY-MATRIX.md`.

<!-- Five strategy matrix criteria columns (from 72-RESEARCH.md §"Strategy Matrix") -->
Columns: Maintenance reduction | Migration cost | Breaking-change surface | Enterprise feature preservation | Spec-upgrade agility

<!-- Five strategy matrix option rows -->
Rows: A. Full adopt | B. Hybrid / wrapper | C. Selective borrow (transports only) | D. Status quo + upstream PRs | E. Fork rmcp into vendor/
</interfaces>

<read_first>
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md (AUTHORITATIVE — copy tables and verbatim text from §"Inversion Inventory", §"Strategy Matrix", §"Architectural Responsibility Map", §"Proposed REQ-IDs")
- .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md (mandates per-deliverable grep gates)
- .planning/REQUIREMENTS.md (add rows here; match existing section style used by PARITY-* rows)
- .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md (rmcp 1.5.0 baseline pin rationale; Phase 72 inherits this pin)
- CLAUDE.md (`gsd-sdk query commit` final-task convention)
</read_first>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (markdown only) | Phase 72 produces documentation artifacts only; no runtime code paths, no data flows, no user input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Information Disclosure | `.planning/REQUIREMENTS.md` and deliverable markdown | accept | Public planning docs contain no secrets, no PII, no credentials; reviewed by planner-audit per 72-VALIDATION.md |
| T-72-02 | Tampering (supply-chain risk of future rmcp dependency) | strategy-matrix scoring | mitigate | Strategy matrix explicitly scores "Breaking-change surface" and Option E (Fork) documents the vendor/ escape hatch; this plan does NOT add rmcp as a dependency — that is a separate future migration phase |

Rationale (applies to all three Phase 72 plans): This phase modifies markdown only. Supply-chain risk of adopting rmcp is **scored** here as a rubric criterion but is not **introduced** here.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Land RMCP-EVAL-01..05 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — match section style of PARITY-HANDLER-01 block, lines 50-56)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 42-55 (§"Phase Requirements" — authoritative REQ-ID descriptions, copy verbatim)
  </read_first>
  <action>
Append a new H3 section `### rmcp Foundation Evaluation (Phase 72 research)` to the v2.1 Requirements area, immediately below the existing `### rmcp Parity (Phase 69 research — seeds follow-on phases)` block (i.e., insert before `## Previous Requirements`).

The section MUST contain exactly these five checkbox rows, in order, with descriptions copied verbatim (or near-verbatim with light line-wrap) from 72-RESEARCH.md §"Phase Requirements":

- [ ] **RMCP-EVAL-01**: Produce a source-citation-backed inversion inventory covering every module family in `src/types/` and `src/shared/` (and `src/server/cancellation.rs`), identifying the nearest rmcp 1.5.0 equivalent and an overlap rating (EXACT / Partial / pmcp-superset / pmcp-exclusive / UNVERIFIED). Each row MUST cite a pmcp `file:line` AND an rmcp docs.rs anchor or GitHub blob URL.
- [ ] **RMCP-EVAL-02**: Score the five architectural options (A. Full adopt / B. Hybrid wrapper / C. Selective borrow / D. Status quo + upstream PRs / E. Fork rmcp) against five criteria (maintenance reduction, migration cost, breaking-change surface, enterprise feature preservation, upgrade agility). All 25 cells scored with rationale; no `TBD`.
- [ ] **RMCP-EVAL-03**: Propose 2-3 candidate PoC slices, each `<=500` LOC touched, each with explicit files list, hypothesis tested, pass criterion, and disqualifying outcome. One slice must be executable in `<=3` days.
- [ ] **RMCP-EVAL-04**: Publish a decision rubric with `>=5` falsifiable thresholds (numeric or boolean), each citing a named data source (git log query, gh CLI query, mcp-tester run, PoC branch output, or CONTEXT.md entry).
- [ ] **RMCP-EVAL-05**: Publish a final recommendation picking exactly one of {A, B, C, D, E}, with a per-criterion subsection that engages every rubric criterion from RMCP-EVAL-04 and cites the inventory row(s) and matrix cell(s) supporting its conclusion.

Also extend the `## Traceability` table (after the existing `| PARITY-MACRO-01 | Phase 71 | Complete |` row) with five new rows:

| RMCP-EVAL-01 | Phase 72 | Pending |
| RMCP-EVAL-02 | Phase 72 | Pending |
| RMCP-EVAL-03 | Phase 72 | Pending |
| RMCP-EVAL-04 | Phase 72 | Pending |
| RMCP-EVAL-05 | Phase 72 | Pending |

Update the Coverage block footer: bump "v2.1 requirements: 23 total" to "28 total (20 pre-seed + 3 seeded by Phase 69 + 5 seeded by Phase 72)" and "Mapped to phases: 23" to "28". Add a new `*Last updated: 2026-04-19 — added 5 RMCP-EVAL-* IDs seeded by Phase 72 rmcp foundation evaluation research*` line at the bottom of the trailing footer block (preserve the existing updated-date lines).

Do NOT rename, renumber, or reorder any existing REQ-IDs. Do NOT touch any `[x]` checkbox state.
  </action>
  <verify>
    <automated>grep -cE '^- \[ \] \*\*RMCP-EVAL-0[1-5]\*\*' .planning/REQUIREMENTS.md | grep -qx 5 && grep -cE '^\| RMCP-EVAL-0[1-5] \| Phase 72 \| Pending \|' .planning/REQUIREMENTS.md | grep -qx 5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*RMCP-EVAL-' .planning/REQUIREMENTS.md` returns 5
    - `grep -c '| RMCP-EVAL-0[1-5] | Phase 72 | Pending |' .planning/REQUIREMENTS.md` returns 5
    - `grep -c '### rmcp Foundation Evaluation (Phase 72 research)' .planning/REQUIREMENTS.md` returns 1
    - `! grep -E 'RMCP-EVAL-0[6-9]' .planning/REQUIREMENTS.md` (no stray IDs introduced)
    - `! grep -i 'TBD' .planning/REQUIREMENTS.md` returns no lines inside the new Phase 72 section (grep `-A 8 'rmcp Foundation Evaluation'` must not contain `TBD`)
    - Existing PARITY-HANDLER-01 / PARITY-CLIENT-01 / PARITY-MACRO-01 rows unmodified (`grep -c '^- \[.\] \*\*PARITY-' .planning/REQUIREMENTS.md` still returns 3)
  </acceptance_criteria>
  <done>REQUIREMENTS.md has 5 new RMCP-EVAL rows, 5 matching Traceability rows, Coverage footer updated, and all pre-existing rows untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write 72-INVENTORY.md (inversion inventory, >=15 rows)</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md</files>
  <read_first>
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 94-135 (§"Inversion Inventory" — the seed 24-row matrix; executor TRANSLATES this table into the deliverable format below)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md lines 45-46 (grep gate for row count)
    - src/types/mod.rs (verify current re-export surface; cite line 36 `pub use protocol::*;` if present)
    - src/types/jsonrpc.rs (verify current LOC for row evidence)
    - src/types/protocol/mod.rs (verify exists)
    - src/shared/stdio.rs (verify exists)
    - Fetch https://docs.rs/rmcp/1.5.0/rmcp/model/index.html (rmcp evidence — record exact fragment anchors per type)
    - Fetch https://docs.rs/rmcp/1.5.0/rmcp/transport/index.html (transport evidence)
    - Fetch https://docs.rs/rmcp/1.5.0/rmcp/service/index.html (service/framing evidence)
  </read_first>
  <action>
Create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md` with the following exact structure:

```markdown
# Phase 72 — Inversion Inventory (RMCP-EVAL-01)

**Baseline pin:** rmcp 1.5.0 (inherited from Phase 69 D-02) / pmcp 2.4.0 (latest released main).
**Inventory date:** 2026-04-19.
**Source of truth for pmcp:** `src/types/`, `src/shared/`, `src/server/cancellation.rs` on current `main`.
**Source of truth for rmcp:** https://docs.rs/rmcp/1.5.0/rmcp/ (module index + model/transport/service/handler/task_manager pages).

## Overlap Legend

| Rating | Meaning |
|--------|---------|
| EXACT | rmcp type/module matches pmcp 1:1 for spec surface; pmcp adds no behavior beyond it |
| Partial | rmcp covers most of pmcp's surface; pmcp has identifiable extra behavior or accessors |
| pmcp-superset | rmcp has a base but pmcp ships a materially richer layer (e.g., TypedTool on top of Tool) |
| pmcp-exclusive | rmcp 1.5.0 has no equivalent (as verified against docs.rs) |
| UNVERIFIED | Needs direct source check in Wave 2 PoC or via `cargo public-api` on an rmcp test crate |

## Inventory Rows

| # | pmcp module family | pmcp file:line | rmcp counterpart | rmcp evidence | Overlap | Gap from rmcp (if pmcp-superset) | Migration LOC impact |
|---|---|---|---|---|---|---|---|
| 1 | JSON-RPC envelope types | src/types/jsonrpc.rs:1 | `rmcp::model::{JsonRpcMessage, JsonRpcRequest, JsonRpcResponse, JsonRpcNotification, JsonRpcError, RequestId}` | https://docs.rs/rmcp/1.5.0/rmcp/model/index.html#jsonrpc | EXACT | None identified | ~615 LOC deletable; N downstream imports redirect |
| 2 | Protocol Request/Notification envelopes + version | src/types/protocol/mod.rs:1, src/types/protocol/version.rs:1 | `rmcp::model::{Request, RequestNoParam, RequestOptionalParam, Notification}` + version negotiation | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT | pmcp ships LATEST_PROTOCOL_VERSION=2025-11-25 (same spec pin) | ~723 LOC deletable |
| 3 | Capabilities (server+client) | src/types/capabilities.rs:1 | `rmcp::model::{ServerCapabilities, ClientCapabilities, ToolsCapability, PromptsCapability, ResourcesCapability, SamplingCapability, TasksCapability, RootsCapabilities, ElicitationCapability, FormElicitationCapability}` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT (CompletionCapabilities/LoggingCapabilities UNVERIFIED — flag for PoC) | Verify Completion + Logging capabilities coverage | ~793 LOC deletable (minus verified gaps) |
| 4 | Tools (Tool, CallTool*, ToolAnnotations) | src/types/tools.rs:1 | `rmcp::model::{Tool, ToolAnnotations, CallToolRequest, CallToolRequestParams, CallToolResult, ToolChoice, ToolChoiceMode, ToolUseContent, ToolResultContent}` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT | TypedTool input/output schema lives in src/server/typed_tool.rs — unaffected | ~904 LOC deletable |
| 5 | Prompts (Prompt, GetPrompt*, ListPrompts*) | src/types/prompts.rs:1 | `rmcp::model::{Prompt, PromptMessage, PromptArgument, PromptReference, GetPromptRequest, GetPromptResult, ListPromptsRequest, ListPromptsResult}` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT | None | ~500 LOC deletable |
| 6 | Resources (Resource, ResourceTemplate, ReadResource*) | src/types/resources.rs:1 | `rmcp::model::{Resource, ResourceTemplate, RawResource, RawResourceTemplate, ResourceContents, ResourceReference, EmbeddedResource, ReadResourceRequest, ReadResourceResult, SubscribeRequest}` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT | None | ~405 LOC deletable |
| 7 | Sampling (CreateMessage*, ModelPreferences) | src/types/sampling.rs:1 | `rmcp::model::{SamplingMessage, CreateMessageRequest, CreateMessageRequestParams, CreateMessageResult, ModelPreferences, ModelHint, ContextInclusion}` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT | None | ~556 LOC deletable |
| 8 | Content (TextContent, ImageContent, AudioContent) | src/types/content.rs:1 | `rmcp::model::{Content, TextContent, ImageContent, AudioContent, RawContent}` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT on base | pmcp keeps MCP Apps content wrappers | ~649 LOC: base deletable, Apps wrappers kept |
| 9 | Elicitation (CreateElicitation*, ElicitationSchema) | src/types/elicitation.rs:1 | `rmcp::model::{ElicitationSchema, CreateElicitationRequest, ElicitationAction, CreateElicitationResult}` + `ElicitationSchemaBuilder` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT + rmcp offers builder pmcp lacks | Builder ergonomics could be adopted | ~198 LOC deletable |
| 10 | Tasks (Task, TaskList, TaskStatus) | src/types/tasks.rs:1 | `rmcp::model::{Task, TaskList, TaskStatus, TaskSupport, TasksCapability, TaskRequestsCapability, CreateTaskResult, GetTaskResult, CancelTaskRequest}` | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT for types | pmcp's task_store + call_tool_and_poll orchestration stays (CLIENT-05 strength) | ~499 LOC: base deletable, orchestration kept |
| 11 | Auth types (AuthInfo, AuthScheme) | src/types/auth.rs:1 | UNVERIFIED — rmcp `auth` feature covers OAuth2 but model-level AuthInfo unconfirmed | https://docs.rs/rmcp/1.5.0/rmcp/ (search "auth" feature) | UNVERIFIED | Likely pmcp-exclusive at handler boundary | ~376 LOC: verify, then decide |
| 12 | Notifications (per-variant notification types) | src/types/notifications.rs:1 | `rmcp::model::{Notification, NotificationNoParam}` + per-notification variants | https://docs.rs/rmcp/1.5.0/rmcp/model/ | EXACT for base | pmcp Client notification-channel surface is a DX choice (CLIENT-03 gap from Phase 69) | ~343 LOC base deletable |
| 13 | Completable / Completion | src/types/completable.rs:1 | UNVERIFIED — Phase 69 did not inventory rmcp completion surface | https://docs.rs/rmcp/1.5.0/rmcp/model/ (verify in Wave 2 PoC) | UNVERIFIED | If rmcp covers, deletable | ~508 LOC: verify first |
| 14 | MCP Apps + UI (UIResource, WidgetMeta, UIContent) | src/types/mcp_apps.rs:1, src/types/ui.rs:1 | NO RMCP EQUIVALENT (SEP-1865 not in 1.5.0) | https://docs.rs/rmcp/1.5.0/rmcp/model/ (absence verified by module listing) | pmcp-exclusive | pmcp enterprise exclusive | 0 — keep (~2,447 LOC retained) |
| 15 | stdio transport | src/shared/stdio.rs:1 | `rmcp::transport::io` (async-read-write + stdio) | https://docs.rs/rmcp/1.5.0/rmcp/transport/index.html | EXACT | None | ~288 LOC deletable |
| 16 | WebSocket transport (client + WASM) | src/shared/websocket.rs:1, src/shared/wasm_websocket.rs:1 | NO dedicated WebSocket transport in rmcp 1.5.0 (only generic Sink/Stream adapter) | https://docs.rs/rmcp/1.5.0/rmcp/transport/ (absence verified — no ws-specific module) | pmcp-exclusive | pmcp keeps (differentiator) | 0 — keep (~763 LOC retained) |
| 17 | SSE parser + reconnect | src/shared/sse_parser.rs:1, src/shared/sse_optimized.rs:1, src/shared/reconnect.rs:1 | rmcp `client-side-sse` helper via `sse-stream` dep (not a full parser) | https://docs.rs/rmcp/1.5.0/rmcp/transport/ (only sse-stream helper) | Partial (pmcp SIMD parser + reconnect is differentiator) | pmcp keeps | 0 — keep (~1,582 LOC retained) |
| 18 | Streamable HTTP (client + server) | src/shared/streamable_http.rs:1, src/server/streamable_http_server.rs:1 | `rmcp::transport::{StreamableHttpClientTransport, transport-streamable-http-server}` | https://docs.rs/rmcp/1.5.0/rmcp/transport/ | Partial — same spec; pmcp adds axum + DNS rebinding + Lambda | pmcp keeps security layer OR delegates + layers on top | Hybrid-friendly (~996 LOC conditional) |
| 19 | WASM HTTP transport | src/shared/wasm_http.rs:1 | NO rmcp wasm32 transport documented | https://docs.rs/rmcp/1.5.0/rmcp/transport/ (absence) | pmcp-exclusive | pmcp keeps (WASM differentiator) | 0 — keep (~265 LOC retained) |
| 20 | Connection pool | src/shared/connection_pool.rs:1 | NO direct rmcp equivalent | https://docs.rs/rmcp/1.5.0/rmcp/ (absence in module listing) | pmcp-exclusive | pmcp keeps | 0 — keep (~683 LOC retained) |
| 21 | Protocol framing helpers | src/shared/protocol.rs:1, src/shared/protocol_helpers.rs:1 | `rmcp::service` (framing + Peer construction) | https://docs.rs/rmcp/1.5.0/rmcp/service/index.html | Partial — framing deletable, pmcp DX helpers kept | Helpers are DX sugar | Mixed — ~431 deletable, ~950 helpers kept |
| 22 | Middleware framework | src/shared/middleware.rs:1 | No first-party middleware framework in rmcp (tower feature exists) | https://docs.rs/rmcp/1.5.0/rmcp/ (absence of middleware module) | pmcp-exclusive | pmcp keeps (Phase 56 asset) | 0 — keep (~1,608 LOC retained) |
| 23 | SIMD parsing | src/shared/simd_parsing.rs:1 | NO rmcp equivalent | (absence) | pmcp-exclusive | pmcp keeps (perf differentiator, ~16x TS SDK) | 0 — keep (~611 LOC retained) |
| 24 | Cancellation / RequestHandlerExtra | src/shared/cancellation.rs:1, src/server/cancellation.rs:1 | `rmcp::service::RequestContext<RoleServer>` via `tokio_util::sync::CancellationToken` | https://docs.rs/rmcp/1.5.0/rmcp/service/ | Partial — shape parity after Phase 70 | Hybrid integration candidate | Low migration risk |
| 25 | Session | src/shared/session.rs:1 | `rmcp::transport::streamable-http-server-session` feature | https://docs.rs/rmcp/1.5.0/rmcp/transport/ | UNVERIFIED | pmcp session includes auth plumbing | Zero to Low |
| 26 | Batch (JSON-RPC batching) | src/shared/batch.rs:1 | UNVERIFIED — rmcp batching status unclear | https://docs.rs/rmcp/1.5.0/rmcp/ | UNVERIFIED | Possibly deletable | Low |
| 27 | URI template | src/shared/uri_template.rs:1 | rmcp `resources` likely uses internal impl | https://docs.rs/rmcp/1.5.0/rmcp/model/ | Independent impls | Low-stakes duplication | Keep or evaluate |
| 28 | Event store | src/shared/event_store.rs:1 | NO rmcp equivalent | (absence) | pmcp-exclusive | pmcp keeps | 0 — keep (~421 LOC retained) |
| 29 | Logging | src/shared/logging.rs:1 | rmcp uses `tracing` directly | (no logging module) | UNVERIFIED | Likely keep | Zero to Low |

## Totals (cite Strategy Matrix)

| Category | LOC impact | Notes |
|----------|-----------|-------|
| Strong-overlap / deletable under Full adopt | ~6,400 LOC | jsonrpc + protocol + capabilities + tools + prompts + resources + sampling + content-base + elicitation-base + tasks-base + notifications-base + stdio |
| Conditional (Hybrid-friendly) | ~2,500 LOC | streamable_http, session, batch, logging |
| pmcp-exclusive / retained | ~9,000 LOC | mcp_apps + ui, websocket, sse_parser+reconnect, middleware, simd_parsing, auth, connection_pool, event_store, uri_template |
| UNVERIFIED rows needing PoC | 4 | auth (row 11), completable (row 13), session (row 25), batch (row 26), logging (row 29) |

## Feeds Into

- 72-STRATEGY-MATRIX.md rows A–E cite these totals for the "Maintenance reduction" column.
- 72-POC-PROPOSAL.md Slice 1 uses rows 1–10 as the re-export test surface.
- 72-DECISION-RUBRIC.md thresholds cite these totals for the LOC-delta gate.
```

After writing, run the grep gate locally before committing:
```bash
grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md
# Must return >= 15
```

All 29 rows above have pmcp file:line cells; the file satisfies the validation gate with margin.
  </action>
  <verify>
    <automated>test $(grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md) -ge 15 && ! grep -iE '^(TBD|XXX|FIXME)$' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md && grep -q 'Baseline pin:.*rmcp 1.5.0' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md`
    - `wc -l` >= 60 (structure + >=15 rows + totals)
    - `grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' 72-INVENTORY.md` >= 15 (validation gate from 72-VALIDATION.md row 1)
    - `grep -c 'docs.rs/rmcp/1.5.0' 72-INVENTORY.md` >= 10 (every EXACT/Partial row cites rmcp docs.rs)
    - `grep -c '| EXACT |' 72-INVENTORY.md` >= 6 (matches Phase 69 inventory)
    - `grep -c '| pmcp-exclusive |' 72-INVENTORY.md` >= 5 (mcp_apps, websocket, middleware, simd, event_store, connection_pool)
    - `grep -c '| UNVERIFIED |' 72-INVENTORY.md` >= 3 (auth, completable, session/batch/logging)
    - `! grep -iE '\bTBD\b|\bFIXME\b' 72-INVENTORY.md`
    - File contains the anchor string `rmcp 1.5.0` at least once in the first 20 lines (baseline pin declared)
  </acceptance_criteria>
  <done>72-INVENTORY.md exists with >=15 file:line-bearing rows, rmcp evidence URLs, overlap ratings, and a totals section that feeds the strategy matrix.</done>
</task>

<task type="auto">
  <name>Task 3: Write 72-STRATEGY-MATRIX.md (5 options x 5 criteria, all 25 cells scored)</name>
  <files>.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md</files>
  <read_first>
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 285-332 (§"Strategy Matrix" — authoritative rubric + per-option descriptions)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-RESEARCH.md lines 263-283 (§"Comparable Precedents" — tonic/prost, axum/hyper, reqwest references)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-VALIDATION.md lines 47 (grep gate: `^| (A\. Full adopt|B\. Hybrid|C\. Selective|D\. Status quo|E\. Fork)` >=5 AND `! grep -i 'TBD'`)
    - .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md (Task 2 output — cite Totals table for LOC columns)
  </read_first>
  <action>
Create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md` with EXACTLY this structure (copy-paste friendly — fill cell content from 72-RESEARCH.md):

```markdown
# Phase 72 — Strategy Matrix (RMCP-EVAL-02)

**Baseline pin:** rmcp 1.5.0 / pmcp 2.4.0.
**Depends on:** 72-INVENTORY.md (Totals table) for LOC columns.
**Scoring is directional; absolute numeric weights are set by CONTEXT.md (not here).**

## Options (rows)

- **A. Full adopt** — pmcp re-exports rmcp's model + service + transport; deletes src/types/ (except mcp_apps, ui, auth) and src/shared/{stdio,protocol}.rs; refactors handlers to consume rmcp's RequestContext<RoleServer>.
- **B. Hybrid / wrapper** — pmcp depends on rmcp as a foundation; re-exports rmcp model types with a compatibility facade; keeps pmcp transports where pmcp has a real differentiator (WebSocket, WASM, security-hardened streamable-HTTP, SIMD SSE).
- **C. Selective borrow (transports only)** — pmcp depends on rmcp only for a narrow slice (e.g., Unix-socket transport, or task_manager). Types and handlers stay first-party.
- **D. Status quo + upstream PRs** — pmcp keeps protocol layer fully first-party. Any rmcp ergonomic gaps pmcp has better answers to get upstreamed as PRs. Zero migration.
- **E. Fork rmcp into vendor/** — pmcp vendors rmcp source into `crates/vendor/rmcp/` and owns all changes locally. Escape hatch only.

## Criteria (columns)

- **Maintenance reduction** — LOC removed from pmcp's protocol layer + spec-tracking burden transferred to rmcp.
- **Migration cost** — Engineering effort in PoC slices (cite 72-POC-PROPOSAL.md slice sizes) + downstream workspace ripple (mcp-tester, cargo-pmcp, pmcp-code-mode, pmcp-tasks).
- **Breaking-change surface** — Count of pmcp public type identities that change; impact on pmcp v3.0 SemVer planning.
- **Enterprise feature preservation** — Can pmcp's workflow engine, MCP Apps, TypedTool, auth middleware, pmcp-macros, pmcp-code-mode still ship unchanged externally?
- **Spec-upgrade agility** — How quickly does pmcp track an MCP spec rev (weekly/monthly/quarterly) under this option?

## The Matrix

| Option | Maintenance reduction | Migration cost | Breaking-change surface | Enterprise feature preservation | Spec-upgrade agility |
|---|---|---|---|---|---|
| A. Full adopt | HIGH — ~6,400 LOC gone from src/types/ + ~1,200 from src/shared/ (cite 72-INVENTORY.md Totals row 1); spec-tracking burden transfers to rmcp's ~6-week cadence | HIGH — every pmcp import changes; PoC Slice 1 measures the compile-error count; downstream ripple into mcp-tester + cargo-pmcp + pmcp-code-mode + pmcp-tasks (see 72-POC-PROPOSAL.md Slice 1) | HIGH — pmcp 3.0 major; pmcp::types::X changes identity to `pub use rmcp::model::X`; downstream pattern-matches / trait impls break | 100% preserved IF hybrid facade ships — but mcp_apps/ui/auth (72-INVENTORY rows 11, 14) stay first-party; workflow engine + TypedTool touch types at every boundary, risk needs Slice 2 verification | HIGH — rmcp tracks spec ~quarterly; pmcp ships same-day once facade bumps |
| B. Hybrid / wrapper | MEDIUM — ~5,000 LOC gone (types re-exported, stdio delegated); workflow + transports stay first-party per 72-INVENTORY.md rows 14–23 | MEDIUM — facade construction effort ~1–2 weeks; less ripple than A because facade preserves pmcp::types::X paths; PoC Slice 2 gauges ergonomics friction | MEDIUM — v3.0 still required because identity changes under the facade; some imports stable, some break (custom From impls, enum-variant pattern matches) | 100% naturally — pmcp owns MCP Apps, workflow, auth, middleware, all pmcp-exclusive transports (72-INVENTORY rows 14, 16, 17, 19, 20, 22, 23, 28) | MEDIUM — still need to wrap new rmcp surfaces in the facade on each rmcp minor |
| C. Selective borrow (transports only) | LOW-MEDIUM — ~500–1,500 LOC per borrowed subsystem (Unix-socket from rmcp::transport, or rmcp::task_manager) | LOW-MEDIUM — limited ripple; but pmcp keeps spec-tracking burden for everything else | LOW — borrowed subsystem is NEW surface not replacement; no existing pmcp types change identity | 100% — nothing pmcp-exclusive is touched | LOW — types still first-party; spec revs still hit pmcp directly |
| D. Status quo + upstream PRs | ZERO — pmcp keeps all ~21,700 LOC of src/types/ + src/shared/ per 72-INVENTORY Totals | ZERO migration; small ongoing cost of upstream PRs (Phase 69 gaps pmcp wants in rmcp, e.g., ElicitationSchemaBuilder inbound from row 9) | ZERO — no pmcp public API changes | 100% | LOW — pmcp tracks spec manually; rmcp's tracking doesn't help |
| E. Fork rmcp into vendor/ | NEGATIVE — pmcp now maintains its own protocol layer AND a vendored copy of rmcp's; doubles burden | HIGH — fork-sync tooling + CI ripple | LOW externally (users see pmcp unchanged), HIGH internally (two codebases) | 100% | LOW — vendored fork diverges over time; spec tracking now requires merging upstream manually |

## Precedent Crosswalk (from 72-RESEARCH.md §"Comparable Precedents")

| Option | Closest Rust precedent | Lesson |
|---|---|---|
| A | reqwest over hyper | Full wrapper hides foundation; advanced users need escape hatch |
| B | tonic over prost, axum over hyper | Proven layering; version-pin coupling is a real risk (axum 0.7 on hyper 1.0) |
| C | sqlx driver borrow | Small savings; narrow scope |
| D | (any SDK that kept wire + types first-party) | Zero churn, misses ecosystem goodwill |
| E | (vendored forks — jsonrpc-core community forks) | Escape hatch only, not a long-term strategy |

## Risk Annotation

Three risks cut across all options and must be carried into 72-DECISION-RUBRIC.md:
1. **rmcp governance SLA is unpublished** (72-RESEARCH.md §"Rmcp Trajectory Risk") — impacts Option A, B, C.
2. **pmcp v2.x breaking-change window open/closed** (Assumption A6 from 72-RESEARCH.md) — impacts Option A, B.
3. **pmcp downstream-user tolerance for v3.0** (Assumption A7) — impacts Option A, B.

## Feeds Into

- 72-POC-PROPOSAL.md slice selection: Slice 1 disambiguates Option A vs B; Slice 2 validates Option B; Slice 3 validates Option B workflow preservation.
- 72-DECISION-RUBRIC.md thresholds: maintenance-reduction column feeds the "maintenance hours/release" threshold; migration-cost column feeds the LOC-delta threshold.
- 72-RECOMMENDATION.md: final pick of exactly one option from {A, B, C, D, E}.
```

The matrix has exactly 5 option rows and 5 criteria columns — 25 scored cells. No cell says `TBD`. Each cell either cites 72-INVENTORY.md rows by number or quotes numerical estimates verbatim from 72-RESEARCH.md.
  </action>
  <verify>
    <automated>test $(grep -cE '^\| (A\. Full adopt|B\. Hybrid|C\. Selective|D\. Status quo|E\. Fork)' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md) -ge 5 && ! grep -iE '\bTBD\b' .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md && test $(wc -l < .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md) -ge 40</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md`
    - `wc -l` >= 40 (structure + 5 option rows + precedent + feeds-into)
    - `grep -cE '^\| (A\. Full adopt|B\. Hybrid|C\. Selective|D\. Status quo|E\. Fork)' 72-STRATEGY-MATRIX.md` returns exactly 5 (validation gate from 72-VALIDATION.md row 2)
    - `! grep -i 'TBD' 72-STRATEGY-MATRIX.md` (validation gate no-placeholder rule)
    - `grep -c '72-INVENTORY.md' 72-STRATEGY-MATRIX.md` >= 1 (cross-doc link present)
    - `grep -c 'Maintenance reduction' 72-STRATEGY-MATRIX.md` >= 2 (column heading + criterion description)
    - `grep -c 'Spec-upgrade agility' 72-STRATEGY-MATRIX.md` >= 2
    - `grep -q '^## The Matrix' 72-STRATEGY-MATRIX.md` (structure anchor present)
    - `grep -q 'rmcp 1.5.0' 72-STRATEGY-MATRIX.md` (baseline pin cited)
  </acceptance_criteria>
  <done>72-STRATEGY-MATRIX.md exists with exactly 5 option rows, 5 criteria columns, all 25 cells scored, no TBD placeholders, cross-referenced to 72-INVENTORY.md Totals.</done>
</task>

<task type="auto">
  <name>Task 4: Commit Plan 01 deliverables</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md,
    .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
  </files>
  <read_first>
    - CLAUDE.md §"Quality Gate Enforcement" (no code changes here — markdown-only commit; quality gates don't apply but `gsd-sdk query commit` is still used per GSD convention)
  </read_first>
  <action>
Commit the three produced/modified files using the GSD SDK commit helper:

```bash
gsd-sdk query commit "docs(72): seed RMCP-EVAL REQ-IDs, inversion inventory, strategy matrix" \
  .planning/REQUIREMENTS.md \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md \
  .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
```

Do NOT run `make quality-gate` — this plan modifies only `.planning/` markdown; quality gates cover Rust code paths. If the helper fails (e.g., pre-commit hook blocks because of unrelated staged files), fall back to:

```bash
git add .planning/REQUIREMENTS.md \
        .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-INVENTORY.md \
        .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-STRATEGY-MATRIX.md
git commit -m "docs(72): seed RMCP-EVAL REQ-IDs, inversion inventory, strategy matrix"
```

Do NOT use `--no-verify` — if a hook blocks the commit, diagnose and fix rather than bypass.
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -q '72-INVENTORY.md' && git log -1 --name-only | grep -q '72-STRATEGY-MATRIX.md' && git log -1 --name-only | grep -q 'REQUIREMENTS.md'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` matches `docs(72):`
    - `git log -1 --name-only` lists all three files
    - `git status --porcelain .planning/REQUIREMENTS.md` returns empty (clean)
    - `git status --porcelain .planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/` shows only untracked placeholder `.gitkeep` (or empty)
  </acceptance_criteria>
  <done>Plan 01 deliverables committed; REQUIREMENTS.md + INVENTORY + STRATEGY-MATRIX on current branch.</done>
</task>

</tasks>

<verification>
Plan 01 is complete when:
1. `grep -c '^- \[ \] \*\*RMCP-EVAL-' .planning/REQUIREMENTS.md` returns 5.
2. `grep -cE '\| [a-zA-Z_/.]+:[0-9]+ \|' 72-INVENTORY.md` returns >= 15.
3. `grep -cE '^\| (A\. Full adopt|B\. Hybrid|C\. Selective|D\. Status quo|E\. Fork)' 72-STRATEGY-MATRIX.md` returns 5.
4. `! grep -i 'TBD' 72-STRATEGY-MATRIX.md` succeeds.
5. Plan 01's commit is on `HEAD`.

Cross-document consistency check (manual, logged in SUMMARY):
- Every pmcp-exclusive row in 72-INVENTORY.md (rating = pmcp-exclusive OR NO RMCP EQUIVALENT) aligns with Option B's "Enterprise feature preservation" cell rationale.
- Strategy matrix's Option A "Maintenance reduction" cell cites the same LOC totals as 72-INVENTORY.md Totals table.
</verification>

<success_criteria>
- REQUIREMENTS.md contains RMCP-EVAL-01..05 rows + 5 Traceability rows; no pre-existing IDs modified.
- 72-INVENTORY.md exists with >= 15 file:line-bearing rows and rmcp docs.rs citations.
- 72-STRATEGY-MATRIX.md has all 25 cells scored, no TBD placeholders, cites 72-INVENTORY.md at least once.
- One git commit on HEAD with message `docs(72): seed RMCP-EVAL REQ-IDs, inversion inventory, strategy matrix` containing all three modified files.
</success_criteria>

<output>
After completion, create `.planning/phases/72-investigate-rmcp-as-foundations-for-pmcp-evaluate-using-rmcp/72-01-SUMMARY.md` documenting:
- Row counts in 72-INVENTORY.md (total + per-overlap-rating breakdown)
- Number of UNVERIFIED rows carried into Plan 02 / PoC
- Matrix scoring highlights (which options scored HIGH on which criteria directionally)
- Cross-doc consistency check result
- Any deviations from 72-RESEARCH.md framing (should be none — research is authoritative)
</output>
