---
phase: 113-stateless-http-multi-round-trip-elicitation
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/sse_parser.rs
  - src/shared/streamable_http.rs
  - src/shared/http.rs
  - src/client/subscriptions.rs
autonomous: true
requirements: [HTTP-04]
gap_closure: true
closes:
  - "GAP-A (Blocker) — unbounded SSE client buffer: the bound is bypassed by any newline-carrying chunk and `EventBuilder::data` has no bound at all (113-VERIFICATION.md gap item 3; 113-REVIEW.md CR-01 + CR-02)"
  - "113-REVIEW.md WR-01 (residual) — the bound's doc comments claim a guarantee the code does not provide"
  - "113-REVIEW.md IN-02 — the flood test re-types the 1 MiB literal it claims to source from `SseConfig`"
  - "113-REVIEW.md IN-03 — every bounding test and doctest feeds only newline-free chunks"

must_haves:
  truths:
    - "A peer that streams ordinary newline-terminated `data:` lines forever cannot grow a pmcp client's heap: `overflowed()` latches and both incremental feeders end their stream"
    - "A single SSE line larger than the parser's bound is REFUSED, not parsed and emitted — `overflowed()` observes the condition it exists to detect"
    - "After every `feed` return, the parser retains at most `max_buffer_size` bytes across BOTH accumulators (`buffer` + the in-progress event's `data`)"
    - "The two whole-body transport call sites still accept a complete collected body larger than the incremental bound"
    - "No incremental (chunk-at-a-time) feeder uses the whole-body bypass"
  artifacts:
    - path: "src/shared/sse_parser.rs"
      provides: "unconditional in-flight bound in `feed`, `buffered_bytes()` accessor, `feed_complete_body` bypass, corrected contract docs, newline-carrying regression tests"
      contains: "feed_complete_body"
    - path: "src/shared/streamable_http.rs"
      provides: "both whole-body call sites migrated to the explicit bypass"
      contains: "feed_complete_body"
    - path: "src/shared/http.rs"
      provides: "`connect_sse`'s parser bound named by a constant with headroom for large JSON-RPC results, and an overflow report that describes buffered bytes rather than one line"
    - path: "src/client/subscriptions.rs"
      provides: "`listen_overflow`'s message describes the real bound; a newline-carrying flood test drives the production predicate"
  key_links:
    - from: "src/shared/sse_parser.rs::SseParser::feed"
      to: "self.max_buffer_size"
      via: "unconditional pre-check over buffer + current_event.data + data"
      pattern: "buffered_bytes\\(\\)"
    - from: "src/shared/streamable_http.rs"
      to: "SseParser::feed_complete_body"
      via: "the two collected-body call sites"
      pattern: "feed_complete_body"
    - from: "src/client/subscriptions.rs::read_next_frame"
      to: "listen_overflow"
      via: "per-chunk poll that ends the stream"
      pattern: "listen_overflow\\(&"
    - from: "src/shared/http.rs::connect_sse"
      to: "report_sse_line_overflow"
      via: "per-frame poll that breaks the reader task"
      pattern: "report_sse_"
---

<objective>
Make `SseParser`'s `max_buffer_size` an actual upper bound on everything the parser
retains, so a hostile or merely broken v2 server can no longer grow a pmcp client's heap
by streaming ordinary newline-terminated `data:` lines on a long-lived
`subscriptions/listen` stream.

Purpose: `113-VERIFICATION.md` gap item 3 is still OPEN and was independently reproduced
against the built crate — `feed("data: AAAAAAAA\n")` x100,000 under
`with_max_buffer_size(64)` accumulates **899,999 bytes** with `overflowed() == false`, and
a single `data:` line of **1,000,000 bytes** delivered with its terminating newline is
accepted whole with `overflowed() == false`. Plan 113-15 bounded only the case where the
peer never sends a newline at all, which is the artificial edge case rather than the
realistic one. HTTP-04 / Success Criterion 3 ("memory-bounded long-lived stream") cannot
be verified until this closes.

Output: an unconditional in-flight bound in `SseParser::feed`, a `buffered_bytes()`
accessor that makes the bound observable, an explicit `feed_complete_body` bypass for the
two collected-body transport call sites, corrected contract documentation, and regression
tests that drive NEWLINE-CARRYING floods (the input class every existing bounding test
and doctest avoids — review IN-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-VERIFICATION.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-REVIEW.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-15-SUMMARY.md

<interfaces>
<!-- Contracts the executor works against. Extracted from the tree at HEAD 5cb340a2. -->
<!-- Line numbers in 113-VERIFICATION.md and 113-REVIEW.md are STALE: commit 4d3aeadc -->
<!-- ("refactor(113): tighten the SSE/subscriptions gap-closure code") moved them. -->
<!-- Navigate by IDENTIFIER, never by the line numbers quoted in those documents. -->

`src/shared/sse_parser.rs` — current state, by identifier:
  - `pub struct SseParser` — private fields `buffer: String`, `current_event: EventBuilder`,
    `last_event_id: Option<String>`, `max_buffer_size: usize`, `overflowed: bool`
  - `pub const DEFAULT_MAX_BUFFER_SIZE: usize = 1024 * 1024`
  - `pub fn new() -> Self` (delegates to `with_max_buffer_size(DEFAULT_MAX_BUFFER_SIZE)`)
  - `pub fn with_max_buffer_size(max_buffer_size: usize) -> Self`
  - `pub fn overflowed(&self) -> bool` (latching; `reset()` deliberately does not clear it)
  - `pub fn max_buffer_size(&self) -> usize`
  - `pub fn feed(&mut self, data: &str) -> Vec<SseEvent>` — a PRE-check gated on
    `!data.contains('\n')`, then a drain loop over `self.buffer.find('\n')`, then a POST
    residual check on `self.buffer.len()` alone
  - `fn process_line(&mut self, line: &str) -> Option<SseEvent>` — its `"data"` arm is the
    UNBOUNDED accumulator: it appends into `self.current_event.data` and nothing bounds it
  - `fn dispatch_event(&mut self) -> Option<SseEvent>` — only a BLANK line dispatches
  - `pub fn reset(&mut self)`, `pub fn last_event_id(&self) -> Option<&str>`
  - `struct EventBuilder { id, event, data: String, retry }` (private)

`feed` call sites across the tree (`grep -rn "\.feed(" --include='*.rs' src`):
  - `src/shared/streamable_http.rs:528` — WHOLE collected body (POST response)
  - `src/shared/streamable_http.rs:1150` — WHOLE collected body (`start_sse` GET)
  - `src/shared/http.rs:198` — INCREMENTAL, inside `connect_sse`'s spawned reader task
  - `src/client/subscriptions.rs:261` — INCREMENTAL, `drain_sse_payloads`, the
    `subscriptions/listen` consumer

Overflow observers already in place (both are free functions over the parser, because
their call sites own a live `hyper::body::Incoming` that cannot be constructed in a test):
  - `src/client/subscriptions.rs::listen_overflow(parser: &SseParser) -> Option<Error>`
  - `src/shared/http.rs::report_sse_line_overflow(...) -> bool`
  - `src/client/subscriptions.rs::MAX_LISTEN_LINE_BYTES: usize = 256 * 1024`
  - `src/shared/http.rs::sse_reader_parser() -> SseParser` (currently `SseParser::new()`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bound the accumulated event, not only the line — and give the whole-body call sites an explicit bypass</name>
  <files>src/shared/sse_parser.rs, src/shared/streamable_http.rs</files>

  <read_first>
    - `src/shared/sse_parser.rs` IN FULL (922 lines) — the struct, `feed`, `process_line`'s
      `"data"` arm, `dispatch_event`, `reset`, and the whole `#[cfg(test)] mod tests`
      including the two proptests. You are changing the contract this file documents in
      four separate doc blocks; you must see all of them.
    - `src/shared/streamable_http.rs` lines 500-560 and 1120-1180 — the two whole-body
      `feed` call sites, so you can see that each builds a FRESH per-body parser and feeds
      one already-collected `String`.
    - `.planning/.../113-REVIEW.md` sections CR-01, CR-02, WR-01, IN-02, IN-03 — CR-01 and
      CR-02 carry the exact measurements this task must invert into passing assertions.
    - `.planning/.../113-VERIFICATION.md` frontmatter `gaps[].missing` — the first two
      entries are the acceptance contract for this task.
  </read_first>

  <action>
Make `max_buffer_size` bound EVERYTHING the parser retains, and give the two legitimate
whole-body callers an explicit, documented escape hatch instead of the peer-controlled
`contains('\n')` escape that exists today.

In `src/shared/sse_parser.rs`:

1. Add `pub(crate) fn buffered_bytes(&self) -> usize` returning
   `self.buffer.len() + self.current_event.data.len()` — the parser's TOTAL in-flight
   retention. `pub(crate)` deliberately, not `pub`: its only consumers are this file's
   tests, `src/client/subscriptions.rs`, and (via that module) the fuzz seam, so it must
   not widen the shipped public API. Document it as the single quantity `max_buffer_size`
   bounds.

2. Replace `feed`'s pre-check. Today it reads
   `self.buffer.len().saturating_add(data.len()) > self.max_buffer_size && !data.contains('\n')`.
   DROP the `!data.contains('\n')` term entirely — that term is precisely what makes the
   bound unenforceable (CR-02), and its accompanying comment (the paragraph claiming the
   `data` condition "is what keeps a single legitimately large COMPLETE body working")
   must go with it. The new condition is
   `self.buffered_bytes().saturating_add(data.len()) > self.max_buffer_size`, unconditional.
   Keep the existing discard behaviour on trip: latch `self.overflowed = true`, clear
   `self.buffer`, reset `self.current_event` to `EventBuilder::new()`, leave
   `self.last_event_id` untouched (it is stream-level resumption state), and return an
   empty `Vec`. Never truncate-and-emit.

3. Change the POST-drain residual check from `self.buffer.len() > self.max_buffer_size` to
   `self.buffered_bytes() > self.max_buffer_size`, so the invariant that holds on RETURN
   from `feed` is over both accumulators. Its existing comment about a chunk that begins
   with a newline stays accurate and should be extended to name the event accumulator.

4. Add `pub fn feed_complete_body(&mut self, body: &str) -> Vec<SseEvent>` — the explicit
   bypass the verification's `missing` list requires. It performs the append-and-drain
   with NO bound check and NO latch. Its rustdoc must state, in these terms: it is for a
   body that has ALREADY been read into memory in full under a separate size cap, so the
   bytes are bounded by that cap rather than by stream age; an INCREMENTAL feeder must
   never call it, because incremental retention across chunks is exactly the unbounded
   condition `feed` exists to refuse. Cross-reference `feed` from it and it from `feed`.

5. To keep both entry points sharing one implementation and to stay under the PMAT cog-25
   gate, extract the append-plus-drain body of `feed` into one private helper (e.g.
   `fn drain_complete_lines(&mut self, data: &str) -> Vec<SseEvent>`) that `feed` calls
   after its bound check and `feed_complete_body` calls directly. The CRLF byte-index
   handling and the `debug_assert!(!self.buffer.contains('\n'), ...)` invariant move with
   it unchanged — that char-boundary logic is a 113-13 regression guard (T-113-67) and
   must not be rewritten.

6. Correct every doc block that now states something false:
   - the `max_buffer_size` FIELD doc ("Upper bound on `buffer`, i.e. on ONE unterminated
     SSE line") must say it bounds the parser's total in-flight bytes — the unterminated
     line in `buffer` PLUS the `data:` lines already accumulated into the in-progress
     event, which is only dispatched by a BLANK line;
   - `with_max_buffer_size`'s contract paragraph ("A chunk that would push the buffer past
     it while completing no line at all is DISCARDED") must drop "while completing no line
     at all";
   - `overflowed`'s doc must say it latches when the parser discarded oversized IN-FLIGHT
     data, not "an oversized line";
   - `DEFAULT_MAX_BUFFER_SIZE` and the `SseConfig::max_buffer_size` field doc must match.
   Add one doctest to `with_max_buffer_size` that feeds NEWLINE-CARRYING data and asserts
   `overflowed()` — review IN-03 records that every existing doctest feeds only
   newline-free chunks, which is exactly why a green suite coexisted with the defect.

7. Migrate both whole-body call sites in `src/shared/streamable_http.rs` (the POST-response
   site near line 528 and the `start_sse` GET site near line 1150) from `feed` to
   `feed_complete_body`. Nothing else at those sites changes.

8. Tests in `src/shared/sse_parser.rs`'s `#[cfg(test)] mod tests`, using the verifier's own
   reproductions inverted into passing assertions:
   - a newline-carrying flood test: `SseParser::with_max_buffer_size(64)`, feed
     `"data: AAAAAAAA\n"` 100,000 times, then assert `overflowed()` is true and
     `buffered_bytes() <= 64`. Name it so it reads as the twin of
     `a_newlineless_flood_cannot_grow_the_buffer_past_the_bound`.
   - an oversized-complete-line test: under a 64-byte bound, feed
     `"data: " + "B".repeat(1_000_000) + "\n\n"` in ONE call and assert the returned event
     vector is EMPTY and `overflowed()` is true (today it returns one 1,000,000-byte event
     with the flag clear).
   - a bypass test: the SAME 1,000,000-byte body through `feed_complete_body` on a
     64-bounded parser yields exactly one event whose `data.len() == 1_000_000` with
     `overflowed()` still false — this is the property that keeps the two transport call
     sites working and must fail loudly if the bypass is ever removed.
   - tighten the existing `a_bounded_feed_never_panics_on_arbitrary_text` proptest: replace
     its loose `parser.buffer.len() <= std::cmp::max(8, chunk.len())` assertion with
     `parser.buffered_bytes() <= parser.max_buffer_size()`. CR-02 names that looseness as
     the reason the property test could not see the defect.
   - fix review IN-02 while you are in the file: have
     `a_newlineless_flood_cannot_grow_the_buffer_past_the_bound` read its expected bound
     from `SseConfig::default().max_buffer_size` (or `DEFAULT_MAX_BUFFER_SIZE`) in BOTH the
     assertion and its message instead of re-typing `1024 * 1024`.

   Every existing test in the module must still pass unmodified except the proptest
   assertion above — in particular `events_completed_before_an_oversized_line_are_still_returned`,
   `the_overflow_flag_latches`, `a_newline_prefixed_flood_still_trips_the_bound` and
   `with_max_buffer_size_bounds_at_the_value_given`. If any of them breaks, the change is
   wrong; do not weaken the test to fit the code.

Do NOT touch `src/server/subscriptions.rs` (plan 113-18 owns it) and do NOT touch the fuzz
target or `decode_listen_chunks_for_fuzz` (plan 113-19 owns those).
  </action>

  <acceptance_criteria>
    - BEHAVIOR: `SseParser::with_max_buffer_size(64)` fed `"data: AAAAAAAA\n"` 100,000
      times ends with `overflowed() == true` and `buffered_bytes() <= 64`. (Before this
      task the same input reaches 899,999 accumulated bytes with `overflowed() == false` —
      the verifier's independently reproduced measurement.)
    - BEHAVIOR: `SseParser::with_max_buffer_size(64).feed("data: " + "B"x1_000_000 + "\n\n")`
      returns an EMPTY event vector and `overflowed() == true`. (Before this task it returns
      one event with `data.len() == 1_000_000` and the flag clear.)
    - BEHAVIOR: `SseParser::with_max_buffer_size(64).feed_complete_body("data: " + "B"x1_000_000 + "\n\n")`
      returns exactly one event with `data.len() == 1_000_000` and `overflowed() == false`.
    - SOURCE: `grep -n "contains('\\\\n')" src/shared/sse_parser.rs` returns NO match inside
      the bound-enforcement block (the `debug_assert!` on `self.buffer` may remain).
    - SOURCE: `grep -c "feed_complete_body" src/shared/streamable_http.rs` is exactly 2.
    - SOURCE (tripwire): `grep -n "feed_complete_body" src/shared/http.rs src/client/subscriptions.rs`
      returns no match — no INCREMENTAL feeder uses the bypass.
    - SOURCE: `grep -c "buffered_bytes" src/shared/sse_parser.rs` is >= 6 (accessor,
      pre-check, post-check, and the tests).
    - TEST: `cargo test --lib --features full -- sse_parser` passes with a count STRICTLY
      GREATER than the 20 recorded by the verifier (>= 23), 0 failed.
    - TEST: `cargo test --doc --features full -- sse_parser` passes (>= 8, 0 failed).
    - TEST: `cargo test --test sse_parser_tests --features full` and
      `cargo test --test streamable_http_integration --features full` and
      `cargo test --test streamable_http_properties --features full` all pass — these feed
      whole SSE streams through `feed` and prove the bound does not break ordinary traffic.
    - TEST: `cargo test --test v2_stateless_http --features full` = 23 passed, 0 failed
      (the whole-body POST path).
    - TEST: `cargo test --test v2_mrtr --features full` = 27 passed, 0 failed.
    - NEGATIVE CONTROL: with the pre-check condition temporarily forced to `false`, the
      newline-carrying flood test and the oversized-complete-line test both FAIL; restore
      and re-run green. Record both outputs in the SUMMARY.
    - SEMVER: `cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp`
      reports no semver update required (`feed_complete_body` is additive; `buffered_bytes`
      is `pub(crate)` and invisible).
    - BUILD: `cargo build --lib --target wasm32-unknown-unknown` succeeds.
    - SUPPLY CHAIN: `git diff --name-only -- Cargo.toml Cargo.lock` is empty.
    - NO SATD: `grep -nE "TODO|FIXME|XXX|TBD" src/shared/sse_parser.rs src/shared/streamable_http.rs`
      returns no match.
  </acceptance_criteria>

  <verify>
    <automated>cargo test --lib --features full -- sse_parser && cargo test --doc --features full -- sse_parser && cargo test --test v2_stateless_http --features full && cargo test --test streamable_http_properties --features full</automated>
  </verify>

  <done>
    `max_buffer_size` is an unconditional upper bound on `buffer + current_event.data`;
    both of the verifier's reproductions now latch `overflowed()`; the two whole-body
    transport call sites go through the documented `feed_complete_body` bypass and no
    incremental feeder does; every pre-existing sse_parser test still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make both incremental feeders report the bound they actually enforce</name>
  <files>src/client/subscriptions.rs, src/shared/http.rs</files>

  <read_first>
    - `src/client/subscriptions.rs` lines 60-300 — `MAX_LISTEN_LINE_BYTES`,
      `sse_payload_stream`'s parser construction, `read_next_frame`'s per-chunk
      `listen_overflow` poll, `drain_sse_payloads`, and `listen_overflow` itself with its
      message text.
    - `src/client/subscriptions.rs` lines 980-1130 — the existing bound tests in the
      `#[cfg(test)] mod tests` block, so the new flood test matches their shape and the
      `MAX_LISTEN_LINE_BYTES < shared` tightness assertion is updated coherently.
    - `src/shared/http.rs` lines 60-210 — `sse_reader_parser`, `report_sse_line_overflow`
      and its doc block, and `connect_sse`'s reader loop at line 198.
    - `src/shared/http.rs` lines 420-500 — the two existing overflow tests.
    - `src/shared/sse_parser.rs` as changed by Task 1 — you need the new contract wording
      so these two modules do not restate a guarantee that no longer matches.
  </read_first>

  <action>
Task 1 changed WHAT the bound covers, so both incremental observers must now describe the
real condition, and `connect_sse`'s bound must be chosen deliberately rather than inherited.

In `src/client/subscriptions.rs`:

1. `listen_overflow`'s error message currently reads "the server sent a single
   subscriptions/listen line exceeding {N} bytes". That is now wrong in the common case —
   the bound trips on ACCUMULATED in-flight bytes, which may be many complete `data:` lines
   of an event the peer never terminates. Reword it to name buffered/in-flight bytes for
   ONE undelivered event rather than "a single line", keeping the two properties the
   message already has: it names the limit via `parser.max_buffer_size()` (never a
   re-derived constant), and it echoes NO frame content.
2. Update `MAX_LISTEN_LINE_BYTES`'s doc comment the same way. Renaming the constant to
   reflect buffered-bytes semantics is at your discretion; what is NOT discretionary is
   that no doc or message in this module may claim the bound is per-LINE. If you rename,
   update all 9 references and the test that asserts it is tighter than the shared default.
3. Add a test that drives the PRODUCTION predicate with newline-carrying input: build a
   parser via `SseParser::with_max_buffer_size(64)`, push `"data: AAAAAAAA\n"` repeatedly
   through `drain_sse_payloads`, and assert `listen_overflow(&parser)` becomes `Some(_)`
   whose message names 64. This is the client-side half of review IN-03 — every existing
   bound test in this module feeds `"x".repeat(N)` with no newline.

In `src/shared/http.rs`:

4. `sse_reader_parser` currently returns `SseParser::new()` (the shared 1 MiB default) with
   a comment saying tightening this transport's payload ceiling is out of scope. After
   Task 1 that ceiling is REAL for the first time: an accumulating JSON-RPC result larger
   than the bound now ends the reader task, where before it was accepted. Choose the bound
   explicitly instead of inheriting it: introduce a named module constant (e.g.
   `MAX_HTTP_SSE_BUFFERED_BYTES`) set to 16 MiB and build the parser with
   `SseParser::with_max_buffer_size(...)`. Document the reasoning in the constant's doc:
   this transport carries arbitrary JSON-RPC results including base64 `image`/`audio`
   content, so 1 MiB would reject legitimate traffic, while 16 MiB keeps memory a function
   of ONE payload rather than of stream age — which is the property the bound exists for.
   Replace the "out of scope" comment with that statement.
5. Reword `report_sse_line_overflow`'s doc and its log message the same way as
   `listen_overflow` (buffered in-flight bytes, not "a single line"), and have it report
   the parser's own `max_buffer_size()` rather than a re-derived default. Renaming the
   function to drop "line" is at your discretion; the accuracy of the doc and message is
   not.
6. Add a newline-carrying flood test alongside the two existing overflow tests, driving
   `report_sse_line_overflow` through a deliberately tiny parser exactly as they do.

Do NOT change `read_next_frame`'s or `connect_sse`'s control flow — the per-chunk poll and
the stream-ending / break-the-loop behaviour they already have are correct and are what
turns Task 1's latch into an actual stream termination.
  </action>

  <acceptance_criteria>
    - BEHAVIOR: a parser built at `with_max_buffer_size(64)` and fed `"data: AAAAAAAA\n"`
      repeatedly through `drain_sse_payloads` eventually yields `listen_overflow(&parser)
      == Some(_)`, and the error message contains `64` and contains none of the fed
      payload bytes.
    - BEHAVIOR: `report_sse_line_overflow` returns its end-the-task verdict for the same
      newline-carrying flood shape.
    - SOURCE: `grep -niE "a single (subscriptions/listen )?line" src/client/subscriptions.rs src/shared/http.rs`
      returns no match — no message or doc still claims the bound is per-line.
    - SOURCE: `src/shared/http.rs` contains a named constant for the reader bound and
      `sse_reader_parser` calls `SseParser::with_max_buffer_size` (not `SseParser::new`);
      `grep -c "SseParser::new()" src/shared/http.rs` is 0 outside `#[cfg(test)]`.
    - TEST: `cargo test --lib --features full -- client::subscriptions` passes with a count
      >= 30 (the 113-16 baseline) and 0 failed.
    - TEST: `cargo test --lib --features full -- shared::http` passes with a count >= 21
      (the 113-15 baseline) and 0 failed.
    - TEST: `cargo test --test v2_subscriptions_client --features full` = 7 passed, 0 failed.
    - TEST: `cargo test --test v2_subscriptions --features full` >= 10 passed, 0 failed
      (server side untouched by this plan).
    - TEST: `cargo build -p pmcp-team-servers --all-features` exits 0 — the in-repo
      consumer of the exported `HttpTransport` still builds against the new bound.
    - EXAMPLE: `cargo run --example s49_v2_subscriptions_client --features full` exits 0.
    - EXAMPLE: `cargo run --example t08_simd_parsing_performance --features full` exits 0
      — it feeds a ~780 KB whole SSE body through `feed`, the closest in-tree case to the
      new unconditional bound.
    - SEMVER: `cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp`
      reports no semver update required.
    - NO SATD: `grep -nE "TODO|FIXME|XXX|TBD" src/client/subscriptions.rs src/shared/http.rs`
      returns no match.
  </acceptance_criteria>

  <verify>
    <automated>cargo test --lib --features full -- client::subscriptions && cargo test --lib --features full -- shared::http && cargo test --test v2_subscriptions_client --features full && cargo build -p pmcp-team-servers --all-features</automated>
  </verify>

  <done>
    Both incremental feeders bound accumulated in-flight bytes, name the bound they
    actually enforce in doc and message, and each has a newline-carrying regression test
    driving the production predicate; `connect_sse`'s ceiling is a named, justified
    constant rather than an inherited default.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote v2 server → pmcp client `SubscriptionStream` | A long-lived `subscriptions/listen` SSE body. Every byte, every framing decision and the stream's LIFETIME are chosen by the peer. |
| remote HTTP server → `HttpTransport::connect_sse` reader task | The same, for the general-purpose exported transport carrying arbitrary JSON-RPC results. |
| collected HTTP response body → `feed_complete_body` | INSIDE the boundary: the bytes were already read into memory in full under a separate size cap, so their size is bounded by that cap rather than by stream age. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-79 | Denial of Service | `SseParser::process_line`'s `"data"` accumulator reached from `feed` | mitigate | Bound `buffer.len() + current_event.data.len() + data.len()` against `max_buffer_size` UNCONDITIONALLY in `feed` (Task 1.2); on trip, discard both accumulators and latch. Proven by the 100,000-iteration newline-carrying flood test and its negative control. |
| T-113-80 | Denial of Service | `SseParser::feed`'s `contains('\n')` escape | mitigate | Delete the escape (Task 1.2). A single 1,000,000-byte line under a 64-byte bound is now refused instead of parsed and emitted. |
| T-113-81 | Tampering | the new `feed_complete_body` bypass | mitigate | The bypass is restricted by construction to the two collected-body call sites; the tripwire grep in Task 1's acceptance criteria asserts neither incremental feeder (`src/shared/http.rs`, `src/client/subscriptions.rs`) references it, and its rustdoc states the precondition. |
| T-113-82 | Information Disclosure | the two overflow messages | accept | Both name only the limit and the peer's behaviour; neither echoes frame content, which is the untrusted input `MAX_ECHOED_FRAME` exists to keep out of client logs. Task 2's acceptance criteria assert no fed bytes appear in the message. |
| T-113-83 | Denial of Service (residual) | in-flight retention WITHIN the bound | accept | An attacker can still pin `max_buffer_size` bytes per stream: 256 KiB per `subscriptions/listen` stream and 16 MiB per `connect_sse` reader task. Bounded by the CLIENT's own concurrency (it chooses how many streams to open), and memory is now a function of one payload rather than of stream age — which is the property Success Criterion 3 requires. |
| T-113-84 | Denial of Service (accepted behaviour change) | `HttpTransport::connect_sse` payload ceiling | accept | A legitimate JSON-RPC result accumulating past 16 MiB now ends the reader task where it previously did not. Deliberate, documented on the constant (Task 2.4), and set high enough that base64 `image`/`audio` content is unaffected. |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No package is installed and no manifest is touched; `git diff --name-only -- Cargo.toml Cargo.lock` must be empty (acceptance criterion). No package-legitimacy checkpoint is required because no dependency is added. |
</threat_model>

<verification>
Run in this order; each must pass before the next.

1. `cargo test --lib --features full -- sse_parser` — >= 23 passed, 0 failed
2. `cargo test --doc --features full -- sse_parser` — >= 8 passed, 0 failed
3. `cargo test --lib --features full -- client::subscriptions` — >= 30 passed, 0 failed
4. `cargo test --lib --features full -- shared::http` — >= 21 passed, 0 failed
5. `cargo test --test sse_parser_tests --features full` — 0 failed
6. `cargo test --test streamable_http_integration --features full` — 0 failed
7. `cargo test --test streamable_http_properties --features full` — 0 failed
8. `cargo test --test v2_subscriptions_client --features full` — 7 passed, 0 failed
9. `cargo test --test v2_subscriptions --features full` — >= 10 passed, 0 failed
10. `cargo test --test v2_stateless_http --features full` — 23 passed, 0 failed
11. `cargo test --test v2_mrtr --features full` — 27 passed, 0 failed
12. `cargo build -p pmcp-team-servers --all-features` — exit 0
13. `cargo run --example s49_v2_subscriptions_client --features full` — exit 0
14. `cargo run --example t08_simd_parsing_performance --features full` — exit 0
15. `cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp` — no semver
    update required
16. `cargo build --lib --target wasm32-unknown-unknown` — exit 0
17. `git diff --name-only -- Cargo.toml Cargo.lock` — empty
18. `make quality-gate` — exit 0 (fmt --all, clippy pedantic+nursery zero warnings, build,
    test, audit). Bare `cargo clippy -- -D warnings` is WEAKER than this gate and does not
    substitute for it. Note the known pre-existing D-113-G behaviour: the fuzz stage
    reports ~17 nightly-only build failures and the gate still exits 0; that is not a
    regression from this plan.
19. Contract-first check: `ls ../provable-contracts/contracts/` — no such directory exists
    in this environment, so there is no contract YAML to update for these surfaces. Record
    the check and its result in the SUMMARY rather than skipping it silently.
</verification>

<success_criteria>
- The verifier's two independently reproduced measurements are inverted: 100,000
  newline-carrying feeds under a 64-byte bound latch `overflowed()` with
  `buffered_bytes() <= 64`, and a 1,000,000-byte complete line under the same bound is
  refused rather than emitted.
- `buffered_bytes() <= max_buffer_size` holds on return from every `feed`.
- `feed_complete_body` exists, is documented with its precondition, is used by exactly the
  two whole-body transport call sites, and is used by no incremental feeder.
- No doc comment, rustdoc contract or error message in `sse_parser.rs`,
  `client/subscriptions.rs` or `shared/http.rs` still claims the bound is per-LINE.
- At least one bound test and one doctest per feeder drives NEWLINE-CARRYING input
  (review IN-03 closed).
- Every pre-existing test in the touched modules still passes, unweakened.
- `make quality-gate` exits 0 and the milestone is still semver-additive against 2.17.0.
</success_criteria>

<output>
Create `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-17-SUMMARY.md`
when done. It MUST record: the before/after byte measurements for both reproductions, the
negative-control run (pre-check disabled → which tests fail → restored → green), the
`connect_sse` bound decision and its rationale, the contract-first check result, and the
threat-register dispositions.
</output>
