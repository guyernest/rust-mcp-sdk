---
phase: 113-stateless-http-multi-round-trip-elicitation
plan: 12
type: execute
wave: 6
depends_on: ["113-01", "113-02", "113-03", "113-04", "113-05", "113-06", "113-07", "113-08", "113-09", "113-10", "113-11"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md
autonomous: true
requirements: [HTTP-01, HTTP-02, HTTP-03, HTTP-04, HTTP-05, CLNT-01, CLNT-02]

must_haves:
  truths:
    - "The whole phase compiles and passes on every feature combination the repo ships, not just --all-features"
    - "The milestone is still additive — cargo semver-checks requires no major bump against 2.17.0"
    - "make quality-gate passes, which is the repo's mandatory pre-commit and pre-push gate"
    - "HTTP-04's roadmap wording matches what shipped, and the SEP-2243 gap research surfaced is recorded rather than silently absorbed"
  artifacts:
    - path: ".planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md"
      provides: "recorded evidence of the feature/target build matrix"
      contains: "wasm32-unknown-unknown"
    - path: ".planning/ROADMAP.md"
      provides: "HTTP-04 reworded to the capability-gated opt-in that shipped"
      contains: "subscriptions/listen"
  key_links:
    - from: ".planning/REQUIREMENTS.md HTTP-04"
      to: ".planning/ROADMAP.md Phase 113 success criteria"
      via: "both reworded consistently to the capability-gated opt-in"
      pattern: "capability"
---

<objective>
Close the phase: prove the feature/target build matrix (the repo's repeat false-green
offender), prove the milestone is still additive, run the mandatory quality gate, update the
provable contract, and reconcile the roadmap wording with what actually shipped.

Purpose: `cargo test --all-features` unifies the dev-dependency `pmcp` `full` feature with
the crate under test and has masked real feature-flag gaps twice in this repo's history.
This phase adds `ring` behind `streamable-http` and a whole cfg-gated module, which is
exactly the shape that burns. Separately, RESEARCH resolved D-12 in a way that changes what
HTTP-04 means, and surfaced a genuine milestone-level requirements gap (SEP-2243
`x-mcp-header`) that must be recorded rather than quietly absorbed into this phase.
Output: a recorded build matrix, a green quality gate, an updated contract, and reconciled
planning documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONTEXT.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature/target build matrix and the additive-semver gate</name>
  <files>.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md</files>
  <read_first>
    - Cargo.toml (the `[features]` block, especially `streamable-http`, `http-client`, `full`, and the default feature set; plus the `ring` entry plan 01 added)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (Pitfall 9 and Pitfall 10, and the "Environment Availability" row for `cargo semver-checks`)
    - .planning/phases/112-version-plumbing-spine/112-VERIFICATION.md (the Phase-112 semver baseline: 223 checks pass, no major bump required, cargo-semver-checks 0.49.0 / cargo-public-api 0.52.0 pinned)
    - src/server/request_state.rs and src/server/core.rs (the cfg gates whose correctness this matrix proves)
  </read_first>
  <action>
Run and RECORD the full matrix. Use the absolute rustup cargo path (not a shell alias or
proxy) so tool-proxy output corruption cannot silently alter results, and capture the exact
command and its exit status for each row.

Matrix rows (each must exit 0):
1. `cargo build --lib --no-default-features` — proves `request_state.rs` and the MRTR
   dispatch arms are genuinely cfg-gated and not accidentally required.
2. `cargo build --lib --features streamable-http` — the phase's target deployment.
3. `cargo build --lib --features full`.
4. `cargo build --lib --target wasm32-unknown-unknown` — proves no `ring` and no MRTR server
   code leaked onto wasm (D-14). Also run it with `--features` sets the repo supports on
   wasm.
5. `cargo build --all-features` at the WORKSPACE level, run in a way that does NOT unify
   dev-dependency features into the crate under test — this is the dev-dep-free check the
   Phase-109 gap taught. Record exactly how it was invoked.
6. `cargo build --examples --features full`.
7. `cargo test --lib --features full`, `cargo test --features full` (all integration tests),
   `make test-doc`.
8. `cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp` — must report NO
   required major bump and must NOT report `enum_variant_added`,
   `constructible_struct_adds_field`, `struct_marked_non_exhaustive`, or any other major
   lint. Compare the pass count against Phase 112's recorded 223 and note any delta.
9. `cargo public-api` diff against the 2.17.0 baseline — record the added public items and
   confirm every one is an ADDITION.

Write `113-FEATURE-MATRIX.md` with one row per command: the exact command, exit status, and
for rows 8-9 the relevant summary line. If any row fails, FIX the code (do not weaken the
row) and record what was fixed.

Explicitly enumerate in the record the new public API surface this phase adds, so the
reviewer can confirm additivity by reading rather than by trusting the tool: the `mrtr` and
`subscriptions` type modules and their re-exports, the `ProtocolContext::with_mrtr_params`
builder, the `RequestHandlerExtra` MRTR accessors, `ReadResourceResult._meta`, the three
error-code constants, `ClientBuilder::with_protocol_version` and `mrtr_round_limit`,
`Client::server_discover`, and the `Error` MRTR round-limit constructor/predicate trio.
  </action>
  <acceptance_criteria>
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md` exists
    - The file contains the literal strings `--no-default-features`, `wasm32-unknown-unknown`, `semver-checks`, `public-api`
    - Every matrix row in the file records an exit status of 0
    - The file contains a list of the new public API items with each marked as an addition
    - `cargo build --lib --no-default-features` exits 0
    - `cargo build --lib --target wasm32-unknown-unknown` exits 0
    - `cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp` exits 0 with no required major bump
  </acceptance_criteria>
  <verify>
    <automated>cargo build --lib --no-default-features &amp;&amp; cargo build --lib --target wasm32-unknown-unknown &amp;&amp; cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp</automated>
  </verify>
  <done>Every feature and target combination the repo ships builds and tests green, the additive-semver gate passes against the 2.17.0 baseline, and the new public surface is enumerated and recorded as additions.</done>
</task>

<task type="auto">
  <name>Task 2: Quality gate, complexity budget, and the provable contract</name>
  <files>.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md</files>
  <read_first>
    - CLAUDE.md (the Toyota Way quality gates section, the CI PMAT cognitive-complexity gate at cog ≤ 25 with its hard cap of 50, the zero-SATD rule, the check-unwraps rule, and the contract-first workflow)
    - Makefile (the `quality-gate` target and what it chains: fmt-check → lint → build → test-all → package gate → audit → unused-deps → check-todos → check-unwraps → validate-always → purity-check → comply)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (the six refactor techniques P1-P6, if a complexity violation needs fixing)
    - src/server/request_state.rs, src/server/core.rs, src/server/streamable_http_server.rs, src/client/mod.rs (the four files most likely to carry a complexity hotspot from this phase)
  </read_first>
  <action>
1. **`make quality-gate`** — the repo's mandatory pre-commit and pre-push gate, and the only
   invocation that matches CI exactly. Run it and make it pass. Do NOT substitute individual
   `cargo fmt` / `cargo clippy` calls: the gate runs clippy with pedantic + nursery lint
   groups and workspace-wide `cargo fmt --all`, and a bare `cargo clippy -- -D warnings` is
   strictly weaker.
2. **Complexity budget.** Run
   `pmat analyze complexity --format json --max-cognitive 25 | jq '.violations[] | select(.path | startswith("src/"))'`
   and confirm this phase introduced no new violation. The likely hotspots by design are
   `request_state::verify`, `mrtr_ingest`, `mrtr_egress`, the `sessions_active`/status-mapper
   composition, and the client MRTR loop — every one of those was planned as a decomposed
   composition, so a violation means the decomposition was skipped and should be fixed by
   applying one of the P1-P6 techniques, not by adding an `#[allow]`. If a function is
   genuinely irreducible, the `// Why:`-annotated `#[allow(clippy::cognitive_complexity)]`
   template is the documented escape hatch and the hard cap is 50. If `pmat` is unavailable
   locally, record that and note that CI's `quality-gate` job is the binding check.
3. **Zero SATD.** Confirm no `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` was introduced, and
   confirm `#[allow(dead_code)]` was REMOVED from `ResponseDisposition::InputRequired`
   (it existed solely for this phase).
4. **Contract-first.** Per CLAUDE.md, update the contract YAML under
   `../provable-contracts/contracts/pmcp/` to cover the new v2 stateless-HTTP and MRTR
   behavior, then run `pmat comply check --path .`. If the `../provable-contracts` checkout
   is not present in this environment, record that fact explicitly in the matrix file with
   the exact path checked, and note that `make quality-gate`'s `comply` step runs as an
   informational report on this repo (Phase 109 D-07 alignment) — do not silently skip.

Append a `## Quality Gate` section to `113-FEATURE-MATRIX.md` recording each command, its
exit status, and any fix applied.
  </action>
  <acceptance_criteria>
    - `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-FEATURE-MATRIX.md` contains a `## Quality Gate` heading
    - The file records `make quality-gate` with exit status 0
    - The file records the `pmat analyze complexity` result or an explicit unavailable-locally note naming CI as the binding check
    - `grep -rnE '\b(TODO|FIXME|XXX|HACK|PLACEHOLDER)\b' src/ --include='*.rs' | wc -l` is unchanged from the pre-phase baseline (record both numbers)
    - `grep -c '#\[allow(dead_code)\]' src/server/core.rs` reflects the removal of the `InputRequired` allow
    - `make quality-gate` exits 0
  </acceptance_criteria>
  <verify>
    <automated>make quality-gate</automated>
  </verify>
  <done>The mandatory quality gate is green, no new cognitive-complexity violation or SATD was introduced, the Phase-112 dead-code allow is gone, and the contract state is either updated or its absence explicitly recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP and REQUIREMENTS with what shipped</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 113 section: goal, requirements line, the five success criteria, and the Plans list)
    - .planning/REQUIREMENTS.md (the HTTP-01..05 and CLNT-01..02 entries and the traceability table)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (the "HTTP-04 must be reworded" suggested text; Assumption A8 and Open Question 4 on SEP-2243 `x-mcp-header`; the `-32002` resolution)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONTEXT.md (D-11 and D-13 — the wording must reflect the owner's decision, not the research's first reading)
  </read_first>
  <action>
1. **Reword HTTP-04** in BOTH `.planning/REQUIREMENTS.md` and the Phase-113 success criteria
   in `.planning/ROADMAP.md`, to the capability-gated opt-in that actually shipped. Use the
   substance of RESEARCH's suggested text: on the v2 path `resources/subscribe`/`unsubscribe`
   and the HTTP GET stream endpoint are removed; v2 change notifications are delivered via a
   `subscriptions/listen` long-lived stream with `toolsListChanged`/`promptsListChanged`/
   `resourcesListChanged`/`resourceSubscriptions` opt-ins, `subscriptionId` tagging and an
   `acknowledged`-first frame; the stream is OPT-IN, pmcp's stateless enterprise default
   advertises no subscription-delivered capability, and answering `subscriptions/listen` with
   method-not-found in that configuration is conformant; a tripwire test enforces that
   advertising any subscription capability requires serving the stream. Add one sentence
   recording D-11: polling over the Tasks mechanism remains pmcp's RECOMMENDED enterprise
   mechanism and is documented as a pmcp extension, not as a conformant substitute.
2. **Mark the seven requirements complete** — flip HTTP-01..05 and CLNT-01..02 from `[ ]` to
   `[x]` in `.planning/REQUIREMENTS.md` and set their traceability-table Status to the
   repo's completed marker, matching how the VERS-* rows were marked after Phase 112.
3. **Record the SEP-2243 `x-mcp-header` / `Mcp-Param-{Name}` gap** as a milestone-level
   requirements gap rather than absorbing it into Phase 113. Research found that clients MUST
   support `x-mcp-header` mirroring and that the header-mismatch validation table covers
   `Mcp-Param-*`, but no current requirement — VERS-05, HTTP-01..05, CLNT-01 — mentions it.
   Add it to `.planning/REQUIREMENTS.md` as a new, explicitly UNASSIGNED requirement with a
   note that it is closest to CLNT-01's header work, and add a line under the v2.5 milestone
   section of `.planning/ROADMAP.md` flagging it for phase assignment. Do not assign it to a
   phase yourself.
4. **Record the `-32002` resolution.** The draft schema states that `-32002` (resource not
   found, 2025-11-25 and earlier) is reserved and replaced by `-32602`; the rename targets
   resource-not-found, NOT task-pending, so pmcp's proprietary `V1_TASK_PENDING` squat is
   unaffected and stays frozen. This closes the research pack's Open Verification Item that
   cross-cuts Phases 112 and 114 — record the resolution where that item is tracked in
   `.planning/ROADMAP.md` (the final-spec checkpoint note) so Phase 114 does not re-litigate it.
5. **Update the Phase-113 Plans list** in `.planning/ROADMAP.md` to the twelve plans that
   shipped, grouped by wave, matching the format used for Phase 112.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` HTTP-04 text contains both `opt-in` and `subscriptions/listen`
    - `.planning/ROADMAP.md` Phase 113 success criterion 3 contains `opt-in`
    - `grep -c '^\- \[x\] \*\*HTTP-0' .planning/REQUIREMENTS.md` is `5`
    - `grep -c '^\- \[x\] \*\*CLNT-0[12]' .planning/REQUIREMENTS.md` is `2`
    - `.planning/REQUIREMENTS.md` contains `x-mcp-header` marked as unassigned
    - `.planning/ROADMAP.md` contains `x-mcp-header`
    - `.planning/ROADMAP.md` records that the `-32002` rename targets resource-not-found and leaves `V1_TASK_PENDING` frozen
    - `.planning/ROADMAP.md` Phase 113 Plans list names all twelve plan files
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^- \[x\] \*\*HTTP-0' .planning/REQUIREMENTS.md)" = "5" &amp;&amp; grep -q 'x-mcp-header' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'x-mcp-header' .planning/ROADMAP.md</automated>
  </verify>
  <done>HTTP-04's wording matches the capability-gated opt-in that shipped and records D-11's polling stance; the seven phase requirements are marked complete; the SEP-2243 gap is recorded as unassigned rather than absorbed; the -32002 resolution is written down so Phase 114 does not re-litigate it; the Plans list names all twelve plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build configuration → shipped artifact | A feature combination that is never built is a combination that is never verified |
| planning documents → future phases | An unrecorded gap or an unreconciled requirement becomes a silent omission downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-38 | Tampering | crypto or MRTR code leaking onto a target where it is unsafe or unbuildable | mitigate | Explicit `--no-default-features` and `wasm32-unknown-unknown` build rows prove `ring` and the MRTR server path are genuinely cfg-gated (D-14), recorded with exit statuses rather than asserted |
| T-113-39 | Tampering | feature-unification false green hiding a real gap | mitigate | A dev-dependency-free workspace `--all-features` build is a distinct recorded row, with the exact invocation written down so it can be reproduced (this exact hazard burned Phase 109) |
| T-113-40 | Repudiation | an accidental major-version break shipping as a minor | mitigate | `cargo semver-checks` against the 2.17.0 baseline plus a `cargo public-api` diff, with the new public surface enumerated by hand so additivity is confirmable by reading |
| T-113-41 | Information Disclosure | a security-relevant complexity hotspot going unreviewed | mitigate | `pmat analyze complexity` over `src/` with the four expected hotspots named; a violation must be fixed by decomposition (P1-P6), not by an `#[allow]` |
| T-113-42 | Repudiation | a known spec gap silently absorbed into a phase that did not implement it | mitigate | SEP-2243 `x-mcp-header` is recorded as an explicitly UNASSIGNED milestone requirement rather than folded into CLNT-01 |
</threat_model>

<verification>
- Every row of `113-FEATURE-MATRIX.md` records exit status 0
- `make quality-gate` exits 0
- `cargo semver-checks check-release --baseline-version 2.17.0 -p pmcp` requires no major bump
- `cargo build --lib --no-default-features` and `cargo build --lib --target wasm32-unknown-unknown` succeed
- The full integration suite passes: `v2_required_headers`, `v2_stateless_http`, `v2_client`, `v2_mrtr_ingress`, `v2_mrtr`, `v2_subscriptions`, `server_subscriptions`, `common_harness_smoke`
- `.planning/REQUIREMENTS.md` shows HTTP-01..05 and CLNT-01..02 complete
</verification>

<success_criteria>
- The phase builds and tests green across every feature and target combination the repo ships
- The milestone is provably still additive against the 2.17.0 baseline
- The mandatory quality gate passes with no new complexity violation and no SATD
- HTTP-04's wording matches what shipped, the SEP-2243 gap is recorded as unassigned, and the -32002 question is closed for Phase 114
</success_criteria>

<output>
Create `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-12-SUMMARY.md` when done
</output>
