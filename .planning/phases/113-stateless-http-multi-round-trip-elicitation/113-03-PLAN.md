---
phase: 113-stateless-http-multi-round-trip-elicitation
plan: 03
type: execute
wave: 2
depends_on: ["113-01", "113-02"]
files_modified:
  - src/server/request_state.rs
  - src/server/mod.rs
  - fuzz/fuzz_targets/fuzz_request_state.rs
  - fuzz/Cargo.toml
autonomous: true
requirements: [HTTP-02]

must_haves:
  truths:
    - "A continuation state minted into a requestState token is opaque to the client and decrypts back to the same value on any instance sharing the key"
    - "A tampered token, a token minted for another principal, and a token replayed onto a different method or different arguments are all rejected"
    - "A token minted by an instance the verifier does not share a key with is distinguishable from a tampered token"
    - "An unset PMCP_REQUEST_STATE_KEY yields a working per-process key and a loud startup warning"
  artifacts:
    - path: "src/server/request_state.rs"
      provides: "AEAD mint/verify, key resolution, key-id, TTL, AAD composition, Verdict"
      min_lines: 300
      contains: "pub(crate) enum Verdict"
    - path: "fuzz/fuzz_targets/fuzz_request_state.rs"
      provides: "arbitrary-bytes-as-token never panics, always rejects"
      min_lines: 20
  key_links:
    - from: "src/server/request_state.rs"
      to: "ring::aead"
      via: "seal_in_place_append_tag / open_in_place with Aad"
      pattern: "ring::aead"
    - from: "src/server/request_state.rs"
      to: "crate::types::mrtr::salient_param_digest"
      via: "AAD composition binds the originating request"
      pattern: "salient_param_digest"
    - from: "src/server/mod.rs"
      to: "src/server/request_state.rs"
      via: "feature+target gated module declaration"
      pattern: "mod request_state"
---

<objective>
Build the `requestState` token: a self-contained, AEAD-encrypted, principal-bound,
originating-request-bound, TTL'd blob that lets any instance behind a load balancer resume
a multi-round-trip operation while the server holds nothing between round trips.

Purpose: This is the one place in the SDK where a cryptographic mistake is directly
exploitable by a remote party — the spec says servers MUST treat `requestState` as
attacker-controlled input. D-01/D-02/D-03/D-04/D-05/D-15 all land here, and every other
MRTR plan consumes this module's `mint`/`verify` pair.
Output: `src/server/request_state.rs` plus its property tests and fuzz target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONTEXT.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-PATTERNS.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-01-SUMMARY.md
@.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. -->

From src/server/auth/traits.rs:61 — the ONLY legitimate principal source:
  pub struct AuthContext { pub subject: String, pub scopes: Vec<String>, pub claims: HashMap<String, Value>, .. }

From src/types/mrtr.rs (plan 02):
  pub fn salient_param_digest(method: &str, params: &serde_json::Value) -> [u8; 32]
  pub const MAX_REQUEST_STATE_LEN: usize = 8192;

From src/shared/pkce.rs:54 — the CSPRNG pattern to copy (NO unwrap/expect; `make check-unwraps` gates):
  fn random_bytes() -> Result<[u8; 32]> {
      let mut buf = [0u8; 32];
      getrandom::fill(&mut buf).map_err(|e| Error::internal(format!("CSPRNG (getrandom) failed: {e}")))?;
      Ok(buf)
  }

Already-present deps usable here (no new crates): ring 0.17 (plan 01), getrandom 0.4,
base64 0.22 (URL_SAFE_NO_PAD), sha2 0.11, chrono 0.4, zeroize 1.8, subtle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Key resolution, key-id, and the token codec skeleton</name>
  <files>src/server/request_state.rs, src/server/mod.rs</files>
  <read_first>
    - src/shared/pkce.rs (whole file — module-doc shape, the centralized `random_bytes()` CSPRNG helper, the known-answer/determinism/distinctness test triad; this is the module shape to copy)
    - src/server/observability/config.rs (lines 120-160: the `if let Ok(v) = std::env::var("PMCP_*")` house style for env reads)
    - src/server/mod.rs (the `mod`/`pub mod` declaration block near the top, and the `#[cfg(not(target_arch = "wasm32"))]` gating convention at ~line 49)
    - Cargo.toml (confirm plan 01 landed `ring` under `streamable-http`)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (section "Pattern 3: requestState token design")
  </read_first>
  <behavior>
    - With `PMCP_REQUEST_STATE_KEY` set to a valid 32-byte base64url value, `RequestStateCodec::from_env()` succeeds and its `key_id()` is deterministic for that key
    - With `PMCP_REQUEST_STATE_KEY` unset, `from_env()` still succeeds, uses a fresh random key, and a `tracing` WARN event is emitted exactly once
    - With `PMCP_REQUEST_STATE_KEY` set to a value that is not 32 bytes after decoding, `from_env()` returns an `Err` naming the expected length (fail loudly on a misconfigured key — this is NOT the D-04 fallback case)
    - Two `from_env()` calls with no env var produce DIFFERENT key ids (entropy sanity)
    - `PMCP_REQUEST_STATE_KEY_PREVIOUS`, when set, is accepted for verification but never for minting
    - `PMCP_REQUEST_STATE_TTL_SECS` overrides the 300-second default; an unparseable value falls back to 300 without erroring
  </behavior>
  <action>
Create `src/server/request_state.rs` and declare it in `src/server/mod.rs` as
`#[cfg(all(feature = "streamable-http", not(target_arch = "wasm32")))] pub(crate) mod request_state;`.
D-14 locks MRTR AEAD to native + `streamable-http`; `ring` is only enabled by that feature
and the wasm server (`WasmServerCore`) gets no MRTR this phase. Document that cfg on the
declaration line.

Module doc: state what the token is (self-contained continuation state, D-01), why it is
AEAD not sign-only (confidentiality — the client and any proxy must not read partial args
or the principal, D-02), and the multi-instance thesis (a shared key means any instance
behind the LB can resume, D-03).

Define:
- `const KEY_LEN: usize = 32;`, `const KEY_ID_LEN: usize = 8;`, `const NONCE_LEN: usize = 12;`
  (use `ring::aead::NONCE_LEN` where available), `const DEFAULT_TTL_SECS: u64 = 300;` (D-05).
- `pub(crate) struct KeyId([u8; KEY_ID_LEN])` with `Debug`/`Display` rendering hex, plus
  `fn key_id_of(key: &[u8]) -> KeyId` = first 8 bytes of `sha2::Sha256(key)`. Document
  that the key-id is NON-SECRET, is carried in cleartext on the token, and leaks nothing
  about the key — and that it is load-bearing: it is the ONLY thing that distinguishes
  "another instance's per-process key" (D-04 → re-elicit) from "tampered token"
  (conformance `sep-2322-reject-tampered-state` → JSON-RPC error).
- `pub(crate) struct RequestStateCodec { minting: (KeyId, ring::aead::LessSafeKey), accepting: Vec<(KeyId, ring::aead::LessSafeKey)>, ttl: std::time::Duration }`
  with a manual `Debug` impl that prints ONLY the key ids and the ttl — never key material.
- `RequestStateCodec::from_env() -> crate::error::Result<Self>` implementing D-03/D-04:
  read `PMCP_REQUEST_STATE_KEY`; accept base64url-no-pad or hex, require exactly 32 bytes
  after decode, error otherwise. When unset, generate a key with the `pkce.rs`
  `getrandom::fill(...).map_err(Error::internal)` pattern (never `unwrap`/`expect`) and emit
  a `tracing::warn!` whose text names the env var and states the consequence: retries that
  land on a different instance cannot be resumed and will re-elicit. Also read the
  optional `PMCP_REQUEST_STATE_PREVIOUS_KEY` into `accepting` (verify-only) so key rotation
  does not strand in-flight tokens (RESEARCH Open Question 6). Read
  `PMCP_REQUEST_STATE_TTL_SECS` for the ttl, defaulting to `DEFAULT_TTL_SECS` on absent or
  unparseable. Zeroize the decoded key source buffer with `zeroize::Zeroize` after building
  the `UnboundKey`; never log key bytes.
- `RequestStateCodec::new(key: &[u8; KEY_LEN], ttl: std::time::Duration) -> Result<Self>` —
  a deterministic constructor for tests, property tests, and the fuzz target.
- `pub(crate) fn codec() -> &'static RequestStateCodec` — a `std::sync::OnceLock`-backed
  process-global initialized from `from_env()` on first use, so the key is resolved ONCE
  per process rather than per request (D-03/D-04 "resolve once at server build"). If
  `from_env()` errors on a malformed key, log the error and fall back to a fresh random
  key so the process still serves (degraded → re-elicit), matching D-04's "no silent
  hard-error" posture; the error itself must still be surfaced via `tracing::error!`.

Choose `ring::aead::CHACHA20_POLY1305` and document the choice (no AES-NI timing
dependence; identical API cost to AES-256-GCM here).

Write `#[cfg(test)] mod tests` covering every `<behavior>` bullet. Use a serialized-env
guard (a `static ENV_LOCK: Mutex<()>`) around the env-var tests since `cargo test` runs
threads in parallel and the repo's CI uses `--test-threads=1` only for some suites.
  </action>
  <acceptance_criteria>
    - `src/server/request_state.rs` exists and contains `pub(crate) struct RequestStateCodec`
    - `src/server/mod.rs` contains `mod request_state;` guarded by `#[cfg(all(feature = "streamable-http", not(target_arch = "wasm32")))]`
    - `src/server/request_state.rs` contains `PMCP_REQUEST_STATE_KEY`, `PMCP_REQUEST_STATE_PREVIOUS_KEY`, `PMCP_REQUEST_STATE_TTL_SECS`
    - `grep -E 'unwrap\(\)|expect\(' src/server/request_state.rs | grep -v '#\[cfg(test)\]' | grep -vc 'mod tests'` — no `unwrap`/`expect` outside the test module (`make check-unwraps` must pass)
    - `cargo test --lib --features full -- request_state` exits 0
    - `cargo build --lib --no-default-features` exits 0 (module is genuinely feature-gated)
    - `cargo build --lib --target wasm32-unknown-unknown` exits 0 (no `ring` on wasm)
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib --features full -- request_state &amp;&amp; cargo build --lib --no-default-features &amp;&amp; cargo build --lib --target wasm32-unknown-unknown</automated>
  </verify>
  <done>Key resolution honors D-03/D-04/D-05 with a loud warning on fallback, a non-secret key-id, verify-only previous-key support, and a process-global codec resolved once; the module compiles only under `streamable-http` on native and the wasm/no-default builds are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mint / verify with AAD binding and the D-15 verdict table</name>
  <files>src/server/request_state.rs</files>
  <read_first>
    - src/server/request_state.rs (the codec from Task 1)
    - src/types/mrtr.rs (the `salient_param_digest` signature and its whitelist semantics)
    - src/server/auth/traits.rs (lines 55-95: `AuthContext.subject`)
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-RESEARCH.md (sections "Pattern 3" verification decision table and "Code Examples → Verify a requestState token")
    - .planning/phases/113-stateless-http-multi-round-trip-elicitation/113-CONTEXT.md (D-15 — the owner's expired-token decision, which OVERRIDES the research recommendation)
  </read_first>
  <behavior>
    - `verify(mint(state, binding), same_binding)` returns `Verdict::Ok(state)` for arbitrary JSON state
    - Two mints of identical input produce DIFFERENT tokens (fresh nonce per mint)
    - Flipping any byte of a valid token's ciphertext or tag yields `Verdict::AuthFailed`
    - Appending `-TAMPERED` to a valid token yields `Verdict::AuthFailed` (the exact conformance `sep-2322-reject-tampered-state` mutation)
    - A token minted with principal `alice` verified with principal `bob` yields `Verdict::AuthFailed`
    - A token minted for `tools/call`+`{"name":"read_file","arguments":{"path":"/safe"}}` verified against `{"name":"read_file","arguments":{"path":"/etc/shadow"}}` yields `Verdict::AuthFailed`
    - A token minted for `tools/call` verified against `prompts/get` yields `Verdict::AuthFailed`
    - A token whose `exp` is in the past yields `Verdict::Expired`
    - A token whose key-id is not in the codec's accepting set yields `Verdict::UnknownKey` WITHOUT attempting decryption
    - A token accepted under the previous (verify-only) key yields `Verdict::Ok`
    - Arbitrary non-base64 input, empty input, and input shorter than `1 + KEY_ID_LEN + NONCE_LEN` all yield `Verdict::AuthFailed` and never panic
  </behavior>
  <action>
Add the token layout, `mint`, `verify`, and the verdict enum to `src/server/request_state.rs`.

**Token layout** (document it as an ASCII diagram in the module doc):
`base64url_nopad( key_id_len:u8 || key_id[key_id_len] || nonce[12] || AEAD_seal(plaintext, aad) )`

**Plaintext**: `serde_json` of
`struct Continuation { state: serde_json::Value, exp: i64 /* unix seconds */, round: u8 }`.

**AAD**: `principal_bytes || 0x00 || method_bytes || 0x00 || salient_param_digest`.
Define `pub(crate) struct RequestBinding<'a> { pub principal: &'a str, pub method: &'a str, pub param_digest: [u8; 32] }`
with a constructor `RequestBinding::from_request(principal, method, params)` that calls
`crate::types::mrtr::salient_param_digest`. This single struct is the ONLY way an AAD is
composed, so mint and verify can never disagree. Document the three bindings and cite the
spec's replay-prevention clauses: (a) authenticated principal, (b) short expiry,
(c) identifier for the originating request.

**`pub(crate) fn mint(&self, state: &serde_json::Value, binding: &RequestBinding<'_>, round: u8) -> Result<String>`**
— fresh 12-byte nonce per call from `getrandom` via the no-unwrap helper; `exp = now + ttl`;
seal with the MINTING key only.

**`pub(crate) enum Verdict { Ok(Continuation), Expired, UnknownKey, AuthFailed }`** and
**`pub(crate) fn verify(&self, token: &str, binding: &RequestBinding<'_>) -> Verdict`**.
Never returns `Result` — every failure is a verdict, so no caller can accidentally `?` a
security decision into a 500.

**The verdict table is LOCKED by D-15** (this overrides 113-RESEARCH.md Assumption A5,
which recommended erroring on expiry — the owner decided after research):

| Condition | Verdict | Caller behavior (plan 06 wires it) |
|-----------|---------|-------------------------------------|
| token longer than `MAX_REQUEST_STATE_LEN`, not base64url, or too short to hold key-id+nonce | `AuthFailed` | JSON-RPC error |
| key-id absent from the accepting set | `UnknownKey` | re-elicit with a fresh `input_required` (D-04 degraded path) |
| key-id known, AEAD tag verification fails | `AuthFailed` | JSON-RPC error (conformance `sep-2322-reject-tampered-state`) |
| decrypts, `exp` in the past | `Expired` | re-elicit cleanly with a fresh token (D-15/D-05) |
| decrypts, `exp` in the future | `Ok(continuation)` | resume |

Note in the doc comment that a wrong-principal token and a token replayed onto a different
method or different salient arguments BOTH surface as `AuthFailed` rather than as distinct
verdicts, because those values live in the AAD and therefore fail the constant-time tag
check — this is deliberate: it removes a discrimination oracle, and it means no auxiliary
`==` comparison on principal or digest is needed anywhere.

Keep `verify` under cognitive complexity 25 (PMAT CI gate) by decomposing into small
single-responsibility private fns — `decode_token`, `split_key_id`, `open_sealed`,
`check_expiry` — composed by a thin `verify`, mirroring the `V2Classification` /
`V2GateOutcome` decomposition in `streamable_http_server.rs:379-397`.

Write `#[cfg(test)] mod tests` covering every `<behavior>` bullet, including a
known-answer style test that pins the token's structural layout (decoded prefix length and
key-id equality) so a future layout change is caught.
  </action>
  <acceptance_criteria>
    - `src/server/request_state.rs` contains `pub(crate) enum Verdict` with variants `Ok`, `Expired`, `UnknownKey`, `AuthFailed`
    - `src/server/request_state.rs` contains `pub(crate) struct RequestBinding`
    - A test named for the `-TAMPERED` suffix mutation exists and asserts `Verdict::AuthFailed`
    - A test asserting an unknown key-id yields `Verdict::UnknownKey` exists and passes
    - A test asserting expiry yields `Verdict::Expired` (NOT `AuthFailed`) exists and passes
    - A test replaying a token across differing `arguments` asserts `Verdict::AuthFailed`
    - `cargo test --lib --features full -- request_state` exits 0
    - `pmat analyze complexity --format json --max-cognitive 25 | jq '[.violations[] | select(.path | contains("request_state"))] | length'` reports `0`, or the run is skipped with a note if `pmat` is unavailable locally
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib --features full -- request_state</automated>
  </verify>
  <done>`mint`/`verify` bind principal, method and a salient-param digest through the AEAD AAD; the four-verdict table matches D-15 exactly; every behavior bullet including the exact conformance tamper mutation has a passing test; no function exceeds cognitive complexity 25.</done>
</task>

<task type="auto">
  <name>Task 3: Property tests and the fuzz target</name>
  <files>src/server/request_state.rs, fuzz/fuzz_targets/fuzz_request_state.rs, fuzz/Cargo.toml</files>
  <read_first>
    - src/server/request_state.rs (the finished mint/verify pair and its test module)
    - fuzz/fuzz_targets/pkce_helper.rs (whole file — the crypto-helper fuzz target shape, the plain `cargo fuzz run` invocation convention, the no-`+nightly` note)
    - fuzz/fuzz_targets/client_host_routing.rs (the "drive a `#[doc(hidden)] pub` classifier from a fuzz target" precedent for reaching a `pub(crate)` item)
    - fuzz/Cargo.toml (the `[[bin]]` target list and the feature set the fuzz crate builds pmcp with)
    - src/server/streamable_http_server.rs (lines 2693-2729: the in-repo `proptest::proptest!` block style)
  </read_first>
  <action>
CLAUDE.md's ALWAYS requirements demand property AND fuzz coverage for every new feature.
`requestState` is the phase's obvious target: it is remote-attacker-controlled by spec.

**Property tests** — add a `proptest::proptest!` block to `request_state.rs`'s test module:
1. `property_request_state_roundtrip`: for arbitrary JSON-ish state values, arbitrary
   principal/method strings, and arbitrary ttl in 1..3600 seconds,
   `verify(mint(state, b), b) == Verdict::Ok(state)` — mint→verify is identity.
2. `property_request_state_binding_is_total`: for arbitrary pairs of DIFFERENT bindings
   `b1 != b2`, `verify(mint(state, b1), b2)` is never `Verdict::Ok`.
3. `property_request_state_never_panics`: for arbitrary `String` tokens, `verify` returns a
   verdict and never panics.
Gate the expensive ones behind `#[ignore]` if runtime exceeds the repo's per-test budget,
and record the `PROPTEST_CASES=1000 cargo test --features full -- --ignored property_request_state`
invocation in the module doc so it is discoverable.

**Fuzz target** — create `fuzz/fuzz_targets/fuzz_request_state.rs` modelled on
`fuzz/fuzz_targets/pkce_helper.rs`: build one deterministic `RequestStateCodec` from a
fixed 32-byte key (NOT `from_env`, so the target is reproducible), then for arbitrary
input bytes drive `verify` with the bytes interpreted as a token string
(`String::from_utf8_lossy`) against a fixed binding. Invariant: never panic, and never
return `Verdict::Ok` for input that was not produced by `mint`. Register the target in
`fuzz/Cargo.toml` with a `[[bin]]` entry named `fuzz_request_state` matching the existing
entries' shape, and ensure the fuzz crate enables the `streamable-http` feature on `pmcp`
so the module is compiled in.

Because `RequestStateCodec`/`verify` are `pub(crate)`, expose the minimum needed surface
for the fuzz target using the in-repo precedent from `client_host_routing.rs`: a
`#[doc(hidden)] pub` re-export (e.g. `pmcp::server::request_state_fuzz_support`) that is
documented as internal support surface for examples/fuzz only and NOT stable API. Do not
widen `RequestStateCodec`'s own visibility.

Add a `#[test]` asserting the fuzz support seam exists and returns a rejecting verdict for
`b"garbage"`, so the seam cannot rot without a unit-test failure.
  </action>
  <acceptance_criteria>
    - `fuzz/fuzz_targets/fuzz_request_state.rs` exists and contains `fuzz_target!`
    - `fuzz/Cargo.toml` contains a `[[bin]]` entry with `name = "fuzz_request_state"`
    - `src/server/request_state.rs` contains `proptest!` with tests named `property_request_state_roundtrip`, `property_request_state_binding_is_total`, `property_request_state_never_panics`
    - `cargo fuzz build fuzz_request_state` exits 0
    - `cargo fuzz run fuzz_request_state -- -runs=20000` exits 0 with no crash artifact written
    - `PROPTEST_CASES=256 cargo test --lib --features full -- property_request_state` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PROPTEST_CASES=256 cargo test --lib --features full -- property_request_state &amp;&amp; cargo fuzz build fuzz_request_state</automated>
  </verify>
  <done>Mint→verify identity, binding totality, and no-panic are proven as properties; a registered fuzz target drives `verify` with arbitrary bytes through a documented internal-only seam and survives 20k runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (`params.requestState`) | Remote, unauthenticated-at-parse-time, attacker-controlled bytes enter the crypto path |
| process environment → server | `PMCP_REQUEST_STATE_KEY` carries the long-lived secret |
| instance A → instance B (via load balancer) | A token minted on one instance is verified on another |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | `verify` on a mutated token | mitigate | `ring::aead` CHACHA20_POLY1305 constant-time tag verification; any mutation → `Verdict::AuthFailed` → JSON-RPC error; exact conformance `-TAMPERED` mutation has a dedicated test |
| T-113-02 | Spoofing | cross-principal token replay | mitigate | `AuthContext.subject` is the first AAD component via `RequestBinding`; a wrong principal fails the tag check, not a comparison — no oracle, no `==` |
| T-113-03 | Elevation of Privilege | same-principal replay onto a different tool/arguments | mitigate | `method` + `salient_param_digest` are the second and third AAD components (spec replay clause 5c); dedicated test replays a token across differing `arguments` |
| T-113-04 | Repudiation | long-window replay | mitigate | `exp` baked into the sealed plaintext, default 300s (D-05), `PMCP_REQUEST_STATE_TTL_SECS` configurable; documented that TTL bounds but does not prevent reuse — single-use is explicitly out of scope for a self-contained token |
| T-113-05 | Information Disclosure | key material in logs, `Debug` output, or process listings | mitigate | manual `Debug` prints only key ids and ttl; decoded key buffer zeroized after `UnboundKey` construction; no `tracing` field ever carries key bytes; key-id documented as non-secret |
| T-113-10 | Information Disclosure | verdict discrimination oracle | mitigate | principal/method/digest mismatches all collapse into `AuthFailed` because they live in the AAD; only `UnknownKey` is distinguishable, and it carries no secret (it means "not my key") |
| T-113-14 | Denial of Service | oversized or malformed token parsing | mitigate | `MAX_REQUEST_STATE_LEN` (8192) checked before decode; every malformed shape returns a verdict rather than erroring; fuzz target proves no panic over arbitrary bytes |
| T-113-17 | Spoofing | misconfigured key silently degrading security | accept (with detection) | An unset key falls back to per-process random WITH a `tracing::warn!` naming the consequence (D-04, explicitly chosen over mandatory-key); a malformed key errors from `from_env()` and is logged at `error!` before the degraded fallback |
</threat_model>

<verification>
- `cargo test --lib --features full -- request_state` passes
- `PROPTEST_CASES=256 cargo test --lib --features full -- property_request_state` passes
- `cargo fuzz build fuzz_request_state` succeeds; `cargo fuzz run fuzz_request_state -- -runs=20000` finds no crash
- `cargo build --lib --no-default-features` and `cargo build --lib --target wasm32-unknown-unknown` both succeed
- `make check-unwraps` reports no new `unwrap`/`expect` in production code
</verification>

<success_criteria>
- A minted token round-trips to the same continuation state on any instance sharing the key
- Tamper, wrong principal, and cross-request replay are all rejected as `AuthFailed`
- Unknown key-id is distinguishable from tamper, and expiry re-elicits (D-15)
- Unset `PMCP_REQUEST_STATE_KEY` warns loudly and still serves
- Property + fuzz coverage exists per CLAUDE.md ALWAYS requirements
</success_criteria>

<output>
Create `.planning/phases/113-stateless-http-multi-round-trip-elicitation/113-03-SUMMARY.md` when done
</output>
