---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-book/src/ch12-7-tasks.md
autonomous: true
requirements: []
tags: [docs, book, tasks, refresh]
must_haves:
  truths:
    - "Every protocol-version string in the chapter matches the current MCP protocol version pinned in the codebase (no stale 2025-XX-YY dates that contradict the live protocol)."
    - "Every type name, method signature, and import path in the chapter matches the current `pmcp-tasks` and `pmcp` crate API surface (no references to renamed/removed types)."
    - "The structural narrative of the chapter is preserved — no full rewrite, no section reordering, no pedagogy change. Only mechanical drift correction."
  artifacts:
    - path: pmcp-book/src/ch12-7-tasks.md
      provides: "Tasks book chapter — targeted refresh, current-API-accurate."
      min_lines: 470
      contains: "tasks/get"
  key_links:
    - from: pmcp-book/src/ch12-7-tasks.md
      to: crates/pmcp-tasks/src/lib.rs
      via: "Type names + method signatures match the live source"
      pattern: "TaskStore|TaskContext|InMemoryTaskStore"
---

<objective>
Targeted refresh of `pmcp-book/src/ch12-7-tasks.md` (currently 587 lines) for v2 drift only — protocol version strings, current Tasks API surface (type names, method signatures, import paths). NO structural rewrite, NO pedagogy change, NO section reordering. Per CONTEXT.md D-03 and 81-CONTEXT.md `<specifics>`: "Tasks refresh — narrow audit, not narrative restructuring. The existing Tasks chapters are good. The refresh is mechanical drift correction (protocol version strings, current type names, current method signatures), not a re-think of pedagogy."

Purpose: The Tasks chapter is structurally sound but predates the v2 protocol version bump and the current `pmcp-tasks` crate API surface. Drift accumulates silently — a reader following the chapter today may encounter `TaskStore` method signatures that no longer exist, import paths that have moved, or protocol-version JSON examples (2025-11-25 strings) that need updating. The refresh closes the drift without re-litigating the chapter's structure.

Output: `pmcp-book/src/ch12-7-tasks.md` is patched in place. Sections affected are surface-level (specific lines, specific types) — not whole sections. Total file size stays roughly 470-700 lines (±20% of the original 587 lines).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-CONTEXT.md
@pmcp-book/src/ch12-7-tasks.md
@crates/pmcp-tasks/src/lib.rs
@crates/pmcp-tasks/tests/lifecycle_integration.rs
@crates/pmcp-tasks/tests/workflow_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit ch12-7-tasks.md for v2 drift — enumerate every issue</name>
  <files>(read-only — produces an internal drift list for Task 2)</files>
  <read_first>
    - `pmcp-book/src/ch12-7-tasks.md` — read in full (~587 lines). Identify every occurrence of:
      * Protocol version strings (e.g., `"2025-11-25"` JSON fields, prose mentions of MCP protocol version). Cross-check against the current value used in PMCP source — `grep -rn "2025-" src/types/` to find the current pinned protocol version constant. Drift: any chapter version string that does not match the codebase's current constant.
      * Type names from `pmcp-tasks` crate. Cross-check each against `crates/pmcp-tasks/src/lib.rs` and submodules. Examples to verify: `TaskStore`, `InMemoryTaskStore`, `TaskContext`, `TaskSupport`, `ToolExecution`, `CreateTaskResult`, `CallToolResult` (lives in `pmcp`, not pmcp-tasks). Drift: any name that no longer exists or has been renamed.
      * Method signatures shown in `rust,ignore` blocks. Cross-check each method signature against the live source — `grep "pub fn <method_name>" crates/pmcp-tasks/src/` or in `pmcp` source for cross-crate types. Drift: any signature that does not match (wrong return type, missing/added parameter, different generic bound).
      * Import paths shown in `use` lines inside snippets. Verify each path resolves in the current crate structure. Drift: any path that does not match (module moved/renamed).
      * JSON examples claiming to be wire-level payloads. Cross-check shape against `crates/pmcp-tasks/tests/lifecycle_integration.rs` and `crates/pmcp-tasks/tests/workflow_integration.rs` which exercise the real wire format. Drift: any JSON field name or value that does not match.
      * Capability response examples (the chapter shows the server `tasks` capability JSON). Cross-check against the current `pmcp` source — `grep -rn "tasks" src/types/capabilities.rs` for the current shape. Drift: any field name/value that does not match.
    - `crates/pmcp-tasks/src/lib.rs` — read for the pub re-export list (which types are reachable as `pmcp_tasks::Foo`).
    - `crates/pmcp-tasks/src/router.rs` — light scan for any router-level types used in the chapter (the chapter discusses `tasks/get`, `tasks/result`, `tasks/list`, `tasks/cancel` request handlers).
    - `crates/pmcp-tasks/tests/lifecycle_integration.rs` and `crates/pmcp-tasks/tests/workflow_integration.rs` — the chapter currently cites these as "useful quoting sources" per 81-CONTEXT.md canonical_refs. Check whether any new chapter content should pull from these (out of scope — Task 1 is an audit, not an expansion). The check is: do these tests use API surfaces the chapter omits? If yes, log as a NOTE in the drift list — do NOT add new content in this plan.

    Produce no file output. Internally build a drift list with entries shaped like:
    ```
    LINE 70-84: JSON example uses "2025-11-25" timestamps. Current protocol pins X. Update timestamps.
    LINE 134-155: snippet imports `pmcp::server::task_store::InMemoryTaskStore` — verify still exists; if moved to `pmcp::server::tasks::InMemoryTaskStore` (or wherever), patch the import.
    LINE 200-220: capability JSON shows `tasks.requests.tools.call` — verify field is unchanged.
    ```
  </read_first>
  <action>
    Produce no file output. Build internal drift list with line numbers and required corrections. The drift list must be exhaustive — Task 2 will execute it mechanically. If the drift list is empty (chapter has no drift), say so explicitly in the Task 1 finding and skip Task 2's body (just produce a SUMMARY noting "no drift detected").

    Discipline: do NOT enumerate "things that could be expanded" or "sections that would benefit from rewriting." Per CONTEXT.md D-03, full rewrite is forbidden. The audit is strictly for incorrect facts that contradict the live codebase.

    A reasonable drift list size is 5-20 entries. Zero entries is possible (the chapter may be fully current). More than 30 entries strongly suggests the chapter needs a rewrite — but the rewrite is out of scope. If the drift exceeds 30 entries, escalate as a "Phase 81 audit gap" note in the SUMMARY rather than expanding scope here.
  </action>
  <verify>
    <automated>test -f pmcp-book/src/ch12-7-tasks.md &amp;&amp; test -f crates/pmcp-tasks/src/lib.rs &amp;&amp; test -f crates/pmcp-tasks/tests/lifecycle_integration.rs &amp;&amp; test -f crates/pmcp-tasks/tests/workflow_integration.rs</automated>
  </verify>
  <done>
    Drift list exists internally to the executor (will be reflected in the Task 2 patch + Task 2's SUMMARY). All four source files referenced exist and were read.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply targeted drift corrections to ch12-7-tasks.md</name>
  <files>pmcp-book/src/ch12-7-tasks.md</files>
  <read_first>
    - Drift list from Task 1 — non-negotiable input.
    - `pmcp-book/src/ch12-7-tasks.md` — read each line range identified in the drift list before editing.
  </read_first>
  <action>
    For each entry in the drift list from Task 1, apply the minimal patch that corrects the drift. Examples:

    - **Protocol version strings**: replace stale `"2025-XX-YY"` ISO timestamps with the current value. If the protocol version is referenced symbolically in code as a constant (e.g., `PROTOCOL_VERSION` from `pmcp/src/types/`), use the literal string the constant resolves to today.
    - **Type names**: if a type has been renamed (e.g., `OldName → NewName`), update every occurrence in the chapter. If a type has been removed and replaced with a different abstraction, the chapter MUST be updated to reflect the new abstraction — but do this with the minimum prose edit (preserve surrounding narrative, change only the type name + the immediate sentence/paragraph that explains it). DO NOT restructure the section.
    - **Method signatures**: if a method signature shown in a `rust,ignore` snippet does not match the live source, update the snippet. Preserve everything else (variable names, surrounding code, comments).
    - **Import paths**: update `use` lines in snippets to match the current module structure.
    - **JSON examples**: update field names/values to match the wire format produced by the integration tests (`crates/pmcp-tasks/tests/lifecycle_integration.rs`, `crates/pmcp-tasks/tests/workflow_integration.rs`). Preserve JSON formatting (indentation, key order) where possible; the goal is correctness, not reflow.
    - **Capability response examples**: update field names/values to match `crates/pmcp-tasks/src/router.rs` or `src/types/capabilities.rs` current shape.

    Discipline rules:
    - NO section deletions.
    - NO section reorderings.
    - NO new H2 or H3 sections.
    - NO new diagrams.
    - NO new code examples (only updates to existing snippets).
    - NO pedagogy changes (don't rewrite explanations for clarity; only fix factual errors).
    - Preserve the chapter's existing voice and pacing.

    If a section is so drifted that minimal patches would leave the surrounding narrative incoherent, document the section in the SUMMARY as "needs rewrite — out of scope for Phase 81; logged as follow-on item." Do NOT rewrite it here.

    If Task 1 found zero drift, this task is a no-op. Write a SUMMARY noting "no drift detected; chapter is current as of YYYY-MM-DD." This is a valid outcome; the targeted refresh has nothing to do.
  </action>
  <verify>
    <automated>test -f pmcp-book/src/ch12-7-tasks.md &amp;&amp; [ "$(wc -l &lt; pmcp-book/src/ch12-7-tasks.md)" -ge 470 ] &amp;&amp; [ "$(wc -l &lt; pmcp-book/src/ch12-7-tasks.md)" -le 700 ] &amp;&amp; grep -q '^# Chapter 12.7' pmcp-book/src/ch12-7-tasks.md &amp;&amp; grep -q 'TaskStore' pmcp-book/src/ch12-7-tasks.md &amp;&amp; grep -q 'tasks/get' pmcp-book/src/ch12-7-tasks.md</automated>
  </verify>
  <done>
    Every drift item from Task 1 is corrected with a minimal patch. Chapter length stays within ±20% of the original 587 lines — concretely, between 470 and 700 lines (matching the verify floor of -ge 470). H1 title preserved, all existing H2 section headers preserved, no new H2/H3 sections added.
  </done>
</task>

</tasks>

<threat_model>
Documentation-only change. No new authentication, authorization, input validation, or trust-boundary surface introduced. Threats considered:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03-01 | Misleading | ch12-7-tasks.md drift correction | mitigate | Task 1 enumerates every drift item against the live `pmcp-tasks` crate source. Task 2 applies minimal patches without restructuring. Plan 81-07's grep-stable excerpt audit double-checks the final state. |
| T-81-03-02 | Scope creep | Tasks refresh | mitigate | The plan explicitly forbids section reordering, new sections, new code examples, and pedagogy changes. If drift is so extensive that minimal patches break coherence, the SUMMARY logs a follow-on phase item rather than expanding scope here. |
</threat_model>

<verification>
- `pmcp-book/src/ch12-7-tasks.md` exists, line count between 470 and 700.
- Every drift item from Task 1's audit has a matching patch in the final file (verifiable by spot-checking specific line ranges against the audit).
- No section reordering: H1 title preserved, H2 section order preserved (compare git diff to confirm no `## ` line moved relative to its neighbors).
- No new doctests (`grep -c 'rust,no_run' pmcp-book/src/ch12-7-tasks.md` should equal whatever the pre-patch count was — Phase 81 only adds a doctest to Skills, per D-10).
- mdBook build (plan 81-07 verifies) succeeds.
</verification>

<success_criteria>
- Every type name, method signature, import path, JSON example, and protocol version string in `ch12-7-tasks.md` matches the live codebase.
- Chapter structure unchanged (section order preserved, no new sections, no removed sections).
- ROADMAP.md Phase 81 progress increments by 1 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-03-SUMMARY.md` recording:
- Total drift items found by Task 1 (count + brief categorization: protocol-version / type-rename / signature / import / JSON / capability).
- Per-item resolution: line range edited + before/after snippet (one line each, just enough to show the diff intent).
- Final line count of `ch12-7-tasks.md` and delta vs. original 587 lines.
- Any "needs rewrite" sections logged as follow-on items (should be zero in a healthy outcome; non-zero is a flag).
</output>
