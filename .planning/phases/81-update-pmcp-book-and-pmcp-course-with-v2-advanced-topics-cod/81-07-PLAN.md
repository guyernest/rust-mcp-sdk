---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 07
type: execute
wave: 2
depends_on: ["81-01", "81-02", "81-03", "81-04", "81-05", "81-06"]
files_modified: []
autonomous: true
requirements: []
tags: [docs, audit, mdbook, verification]
must_haves:
  truths:
    - "`mdbook build` succeeds for both `pmcp-book/` and `pmcp-course/` with zero errors (verified by EXIT CODE, not stderr substring grep — revision B-4 fix)."
    - "`cargo test --doc -p pmcp --features skills,full` exits 0 and includes the new Skills chapter doctest in the passing set."
    - "Every inline excerpt in the new/rewritten chapters still matches the live source file byte-for-byte (grep-stable audit)."
    - "Every `https://github.com/paiml/rust-mcp-sdk/blob/main/...` cross-link points to a file that exists in this repo."
    - "Every new SUMMARY.md entry resolves to an existing file."
    - "The `pmcp-code-mode` and `pmcp-code-mode-derive` version pins are byte-equal between the book and course chapters (revision W-4 — Audit E)."
  artifacts:
    - path: .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md
      provides: "Cross-property consistency audit report — drift findings, broken links, build output, doctest pass/fail, version-pin consistency."
      min_lines: 50
      contains: "## mdBook Build Results"
  key_links:
    - from: 81-07-AUDIT.md
      to: pmcp-book/src/ch12-8-skills.md
      via: "Audit report cites every chapter touched by Phase 81"
      pattern: "ch12-8-skills.md|ch12-9-code-mode.md|ch12-7-tasks.md|ch23-skills.md|ch22-code-mode.md|ch21-tasks.md"
---

<objective>
Cross-property consistency audit and build verification for Phase 81. After all chapter plans (81-01 through 81-06) have landed, this plan verifies:

1. **mdBook builds:** `cd pmcp-book && mdbook build` and `cd pmcp-course && mdbook build` both exit 0 (verified by EXIT CODE, per revision B-4 — substring grep on "error" false-positives on benign content like `ch08-error-handling.md`, `appendix-d-errors.md`, "0 errors" progress lines).
2. **Doctest passes:** `cargo test --doc -p pmcp --features skills,full` runs the new Skills doctest and reports pass (revision B-2 — `skills,full`, not bare `skills`).
3. **Inline excerpt drift audit:** Every inline excerpt in the new/rewritten chapters (Skills book + course, Code Mode book + course) matches the live source file. Method: extract each fenced code block's content, grep for the same content in the cited example file, confirm presence.
4. **Cross-link audit:** Every `(https://github.com/paiml/rust-mcp-sdk/blob/main/...)` GitHub permalink cited in the new chapters resolves to a path that exists in this repo.
5. **SUMMARY.md audit:** Both `pmcp-book/src/SUMMARY.md` and `pmcp-course/src/SUMMARY.md` reference files that exist at the linked paths.
6. **Version-pin consistency (revision W-4 — new Audit E):** The `pmcp-code-mode = "..."` and `pmcp-code-mode-derive = "..."` lines in book ch12-9 and course ch22 dependency snippets must be byte-equal. Log mismatch.

Purpose: This plan is the safety net for Phase 81. It catches drift between chapter prose and live source code (the most common documentation failure mode), broken cross-links (the second most common failure), and mdBook build errors that would not surface until a downstream consumer rebuilds the documentation. Without this audit, Phase 81 ships with no automated check that the chapters are consistent with the codebase.

Output:
- `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md` (the audit report, ~50-200 lines depending on findings).
- No source file modifications. If the audit finds issues, the audit report logs them as follow-on findings — the planner explicitly does NOT plan auto-fix here because (a) the issues would all be in chapter content (not code), so are non-blocking; (b) the audit is the verification step, not the remediation step. Remediation goes to /gsd-plan-phase 81 --gaps if needed.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-CONTEXT.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-01-SUMMARY.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-02-SUMMARY.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-03-SUMMARY.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-04-SUMMARY.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-05-SUMMARY.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mdBook builds for both properties (revision B-4 — rely on exit code)</name>
  <files>(no edits — produces stdout/stderr captured into the audit report)</files>
  <read_first>
    - Confirm `mdbook` is available: `which mdbook` (PMCP CI uses mdbook per 81-CONTEXT.md `<code_context>` integration_points; if not installed locally, install with `cargo install mdbook --locked` or `cargo install mdbook`).
    - `pmcp-book/book.toml` and `pmcp-course/book.toml` — confirm both exist and are not customized to require extra plugins beyond core mdbook. If either book.toml references a preprocessor like `mdbook-mermaid` or `mdbook-katex`, install it before running build.
  </read_first>
  <action>
    Run mdBook builds for both properties. Capture output to files so the audit report can quote them:

    ```bash
    (cd pmcp-book &amp;&amp; mdbook build) 2>&amp;1 | tee /tmp/mdbook-book-build.log
    (cd pmcp-course &amp;&amp; mdbook build) 2>&amp;1 | tee /tmp/mdbook-course-build.log
    ```

    Verification relies on the EXIT CODE of `mdbook build`, NOT on grepping stderr for the substring "error" (revision B-4 — the substring grep false-positives on benign content: chapter filenames like `ch08-error-handling.md`, appendix filenames like `appendix-d-errors.md`, progress lines that say "0 errors", and section titles that include the word "error").

    Common failure modes mdbook signals with a non-zero exit:
    - SUMMARY.md references a file that does not exist → mdbook exits non-zero with "Chapter file not found".
    - A code-block fence is mismatched (e.g., ` ```rust` without closing ` ``` `) → mdbook exits non-zero.
    - Frontmatter parsing issues → typically warnings (exit 0); they show up in the log but do not fail the build.

    Cd back to repo root after each build so subsequent tasks have a clean working dir.

    If finer-grained detection is needed in the future, grep for the specific mdbook error prefix `^error\[` (mdbook's actual error format, NOT bare `error`). This is logged as a follow-on improvement in the audit report but not required by this task.
  </action>
  <verify>
    <automated>(cd pmcp-book &amp;&amp; mdbook build) &amp;&amp; (cd pmcp-course &amp;&amp; mdbook build)</automated>
  </verify>
  <done>
    Both `mdbook build` invocations exit 0. Per revision B-4: verify relies on the build exit code, not on stderr substring grep. Build logs captured to `/tmp/mdbook-{book,course}-build.log` for the audit report; the logs are informational, not the pass/fail signal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run cargo test --doc for the new Skills doctest (revision B-2 — skills,full)</name>
  <files>(no edits — captures doctest output)</files>
  <read_first>
    - Plan 81-01 SUMMARY.md — confirm the doctest was added to `src/server/skills.rs`. The audit confirms it is reachable via `cargo test --doc -p pmcp --features skills,full`.
    - `Cargo.toml` at the repo root — confirms `[[example]] s44_server_skills` requires `["skills", "full"]`. Same feature surface drives the doctest body.
  </read_first>
  <action>
    Run the doctest suite (revision B-2 — features are `skills,full` not bare `skills`):

    ```bash
    cargo test --doc -p pmcp --features skills,full 2>&amp;1 | tee /tmp/cargo-test-doc-skills.log
    ```

    Confirm: exit 0, output contains at least one passing doctest, output does NOT contain "FAILED" or "test result: FAILED".

    Also run with no features to confirm the doctest gating is correct:

    ```bash
    cargo build -p pmcp 2>&amp;1 | tee /tmp/cargo-build-no-features.log
    ```

    Confirm: exit 0 (the doctest is feature-gated; no-features build should not try to compile it).
  </action>
  <verify>
    <automated>cargo test --doc -p pmcp --features skills,full 2>&amp;1 | tee /tmp/cargo-test-doc-skills.log &amp;&amp; grep -E 'test result.*ok' /tmp/cargo-test-doc-skills.log &amp;&amp; ! grep -E 'test result.*FAILED' /tmp/cargo-test-doc-skills.log &amp;&amp; cargo build -p pmcp 2>&amp;1 | tee /tmp/cargo-build-no-features.log &amp;&amp; tail -3 /tmp/cargo-build-no-features.log | grep -E 'Compiling|Finished'</automated>
  </verify>
  <done>
    `cargo test --doc -p pmcp --features skills,full` exits 0 with at least one passing test. No FAILED lines. `cargo build -p pmcp` (no features) also exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Inline excerpt drift audit + cross-link audit + SUMMARY.md audit + version-pin consistency audit (revision W-4, W-7, W-8)</name>
  <files>.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md</files>
  <read_first>
    - All six new/rewritten chapter files (`pmcp-book/src/ch12-8-skills.md`, `pmcp-book/src/ch12-9-code-mode.md`, `pmcp-book/src/ch12-7-tasks.md`, `pmcp-course/src/part8-advanced/ch23-skills.md`, `pmcp-course/src/part8-advanced/ch22-code-mode.md`, `pmcp-course/src/part8-advanced/ch21-tasks.md`).
    - The new ch22-exercises.md from plan 81-05 (revision B-1 — exists after 81-05 lands).
    - SUMMARY.md files (`pmcp-book/src/SUMMARY.md`, `pmcp-course/src/SUMMARY.md`).
    - Source files cited by the chapters: `examples/s44_server_skills.rs`, `examples/c10_client_skills.rs`, `examples/skills/{hello-world,refunds,code-mode}/SKILL.md`, `examples/s41_code_mode_graphql.rs`, `tests/skills_integration.rs`, `crates/pmcp-tasks/src/lib.rs`, `crates/pmcp-tasks/tests/{lifecycle,workflow}_integration.rs`, `crates/pmcp-code-mode/src/lib.rs`, `crates/pmcp-code-mode-derive/src/lib.rs`, `src/server/skills.rs`.
  </read_first>
  <action>
    Five audits in one task; produce one consolidated audit report at `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md`.

    **Audit A — Inline excerpt drift:** For each new/rewritten chapter, extract every fenced code block (``` ... ``` pairs) that is annotated with `rust,ignore`, `rust,no_run`, `toml`, or `json`. For each block:
    1. Determine which source file the block was excerpted from. Heuristic: the chapter's nearest preceding "Full example:" GitHub permalink, OR the chapter's nearest preceding sentence naming a file path (e.g., "from examples/s44_server_skills.rs").
    2. If the block is identifiable as an excerpt (not a synthetic example or a `config.toml` template), grep the source file for a stable substring from the block (a function signature, a struct name + first field, a unique string literal). Confirm the substring exists in the source file.
    3. If the substring is NOT found, log: `DRIFT: <chapter>:<line> excerpt does not match <source file>`. Quote the chapter excerpt + cite what was searched for.

    Heuristic threshold for "stable substring": pick a line from the middle of the excerpt that contains a non-trivial identifier (function name, struct field with type, distinctive string literal). Avoid generic lines like `}` or `Ok(())`. If the excerpt is too generic to have a stable substring (rare — for prose-style explanation embedded in fenced blocks), skip that block.

    Record the running count `Total excerpts checked: N`. The audit report MUST display this count, and the verify block below asserts `N >= 10` (revision W-7 — prevents a degenerate audit that skips every excerpt from passing).

    Expected outcome: zero drift findings in a healthy state. Each finding is a flag for follow-up via `/gsd-plan-phase 81 --gaps`. Phase 81 does not auto-fix these — the audit reports them.

    **Audit B — Cross-link audit:** For each chapter, extract every URL matching `https://github.com/paiml/rust-mcp-sdk/blob/main/<path>`. Convert each `<path>` to a local repo path (strip the URL prefix) and verify the file exists via `test -f <path>` from the repo root. Log every broken link.

    Run as a single grep + loop:
    ```bash
    for chapter in pmcp-book/src/ch12-{7-tasks,8-skills,9-code-mode}.md \
                   pmcp-course/src/part8-advanced/{ch21-tasks,ch22-code-mode,ch23-skills}.md; do
      grep -oE 'https://github\.com/paiml/rust-mcp-sdk/blob/main/[^)]+' "$chapter" 2>/dev/null \
        | sed 's|https://github.com/paiml/rust-mcp-sdk/blob/main/||' \
        | while read path; do
            if [ ! -f "$path" ]; then
              echo "BROKEN LINK: $chapter -> $path"
            fi
          done
    done
    ```

    **Audit C — SUMMARY.md audit:** For each SUMMARY.md file (`pmcp-book/src/SUMMARY.md`, `pmcp-course/src/SUMMARY.md`), extract every relative link `(filename.md)` or `(./path/to/file.md)`. For each, confirm the referenced file exists (relative to the SUMMARY.md's directory).

    Specifically verify the four Phase 81 course-side additions resolve (per plan 81-04 Task 4): `ch22-exercises.md`, `ch23-skills.md`, `ch23-exercises.md`. The first of those is created by plan 81-05; the latter two are created by plan 81-04. All three must resolve to existing files.

    **Audit D — Doctest byte-equality (Skills only):** Confirm the `rust,no_run` block in `pmcp-book/src/ch12-8-skills.md` (the Skills chapter doctest) has the same body as the doctest in `src/server/skills.rs`. Extract both, normalize trailing whitespace, diff. Log any mismatch as `DOCTEST DRIFT: pmcp-book/src/ch12-8-skills.md vs src/server/skills.rs`.

    Per revision W-8, ALSO assert the "no doctest in course" invariant survives revisions to plan 81-04 / 81-05:
    ```bash
    ! grep -q 'rust,no_run' pmcp-course/src/part8-advanced/ch23-skills.md \
      &amp;&amp; ! grep -q 'rust,no_run' pmcp-course/src/part8-advanced/ch22-code-mode.md
    ```
    If either course-side file contains a `rust,no_run` fence, log: `COURSE DOCTEST VIOLATION: <file> contains rust,no_run (D-10 forbids doctests in course chapters)`.

    **Audit E — Version-pin consistency (revision W-4 — new):** Extract the `pmcp-code-mode = "..."` and `pmcp-code-mode-derive = "..."` lines from book ch12-9 and course ch22 chapter dependency snippets, and assert byte equality.

    Implementation:
    ```bash
    BOOK_PMCP_CODE_MODE=$(grep -oE 'pmcp-code-mode = "[^"]+"' pmcp-book/src/ch12-9-code-mode.md | head -1)
    COURSE_PMCP_CODE_MODE=$(grep -oE 'pmcp-code-mode = "[^"]+"' pmcp-course/src/part8-advanced/ch22-code-mode.md | head -1)
    BOOK_DERIVE=$(grep -oE 'pmcp-code-mode-derive = "[^"]+"' pmcp-book/src/ch12-9-code-mode.md | head -1)
    COURSE_DERIVE=$(grep -oE 'pmcp-code-mode-derive = "[^"]+"' pmcp-course/src/part8-advanced/ch22-code-mode.md | head -1)

    if [ "$BOOK_PMCP_CODE_MODE" != "$COURSE_PMCP_CODE_MODE" ]; then
      echo "VERSION-PIN DRIFT: pmcp-code-mode book='$BOOK_PMCP_CODE_MODE' course='$COURSE_PMCP_CODE_MODE'"
    fi
    if [ "$BOOK_DERIVE" != "$COURSE_DERIVE" ]; then
      echo "VERSION-PIN DRIFT: pmcp-code-mode-derive book='$BOOK_DERIVE' course='$COURSE_DERIVE'"
    fi
    ```
    Mismatch is a finding (`VERSION-PIN DRIFT: ...`) logged in the audit report. Healthy state: zero version-pin drift findings.

    Write the consolidated audit report to `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md` with these H2 sections:

    ```markdown
    # Phase 81 Cross-Property Consistency Audit

    Run at: <timestamp>
    Plans audited: 81-01, 81-02, 81-03, 81-04, 81-05, 81-06

    ## mdBook Build Results
    - pmcp-book: PASS|FAIL (with build exit code; logs at /tmp/mdbook-book-build.log)
    - pmcp-course: PASS|FAIL (with build exit code; logs at /tmp/mdbook-course-build.log)

    ## Doctest Results
    - cargo test --doc -p pmcp --features skills,full: PASS|FAIL (with test count)
    - cargo build -p pmcp (no features): PASS|FAIL

    ## Inline Excerpt Drift Audit
    - Total excerpts checked: N
    - Drift findings: N (list each)

    ## Cross-Link Audit
    - Total GitHub permalinks checked: N
    - Broken links: N (list each)

    ## SUMMARY.md Audit
    - pmcp-book SUMMARY entries checked: N, broken: N
    - pmcp-course SUMMARY entries checked: N, broken: N
    - Phase 81 new entries resolved: ch22-exercises.md, ch23-skills.md, ch23-exercises.md (each PASS|FAIL)

    ## Doctest Byte-Equality (Skills) + Course-Doctest Invariant
    - Skills book vs skills.rs doctest body: PASS|FAIL with diff if FAIL
    - "No rust,no_run in course chapters" invariant: PASS|FAIL (per W-8)

    ## Version-Pin Consistency (Book vs Course Code Mode chapters)
    - pmcp-code-mode pin: PASS|FAIL (book='...' course='...')
    - pmcp-code-mode-derive pin: PASS|FAIL (book='...' course='...')

    ## Overall Verdict
    - PASS (zero findings) | PASS WITH FINDINGS (N findings logged, listed below) | FAIL (build/doctest broke)

    ## Findings (if any)
    1. <finding 1>
    2. <finding 2>
    ...
    ```

    Discipline: the audit is INFORMATIONAL. Do NOT modify the chapter files based on findings. Each finding is a follow-up item for `/gsd-plan-phase 81 --gaps` (if any). The audit's job is to surface drift, not fix it.
  </action>
  <verify>
    <automated>test -f .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md &amp;&amp; grep -q '## mdBook Build Results' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md &amp;&amp; grep -q '## Doctest Results' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md &amp;&amp; grep -q '## Inline Excerpt Drift Audit' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md &amp;&amp; grep -q '## Cross-Link Audit' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md &amp;&amp; grep -q '## SUMMARY.md Audit' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md &amp;&amp; grep -q '## Version-Pin Consistency' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md &amp;&amp; grep -q '## Overall Verdict' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md &amp;&amp; EXCERPT_COUNT=$(grep -oE 'Total excerpts checked: [0-9]+' .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md | grep -oE '[0-9]+' | head -1) &amp;&amp; [ "${EXCERPT_COUNT:-0}" -ge 10 ] &amp;&amp; [ "$(wc -l &lt; .planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-AUDIT.md)" -ge 50 ]</automated>
  </verify>
  <done>
    `81-07-AUDIT.md` exists with all required H2 sections (including the new `## Version-Pin Consistency` from revision W-4). The audit's `Total excerpts checked: N` value is >= 10 per revision W-7 (prevents degenerate-skip audits from passing). The course-doctest invariant assertion from revision W-8 is recorded in the Doctest Byte-Equality section. Audit covers all six chapter files + both SUMMARYs + the three Phase-81 new course-side entries. mdBook build results, doctest results, per-audit counts, and version-pin comparison are documented. Findings (if any) are listed with chapter + line + finding type.
  </done>
</task>

</tasks>

<threat_model>
Documentation-only audit. No new authentication, authorization, input validation, or trust-boundary surface introduced. Threats considered:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-07-01 | Misleading | Audit completeness | mitigate | The audit script checks every chapter touched by Phase 81 (6 chapter files + ch22-exercises.md created by 81-05), both SUMMARYs, the Skills doctest specifically, and the new version-pin consistency audit (revision W-4). The check methods (grep-stable substrings, file existence, mdBook build exit codes) are deterministic. |
| T-81-07-02 | Misleading | Audit-as-fix temptation | mitigate | The plan explicitly forbids auto-fixing findings. The audit's job is to surface drift, not silently rewrite content. Remediation is operator-driven via `/gsd-plan-phase 81 --gaps`. |
| T-81-07-03 | False negative | Stable-substring heuristic | accept | If an excerpt is so generic that no stable substring exists, the drift check skips it. This is acceptable for prose-style code blocks; the inline-excerpt audit's value is catching obvious drift (function rename, API change), not exhaustive byte-equality. Revision W-7 caps the false-negative blast radius by requiring `Total excerpts checked >= 10` — a degenerate skip-everything audit cannot pass. |
| T-81-07-04 | False positive (revision B-4 fix) | mdBook build verify | mitigate | Verify relies on `mdbook build` exit code, not on substring-grep of stderr. Benign content like `ch08-error-handling.md`, `appendix-d-errors.md`, "0 errors" progress lines no longer trigger false failures. |
</threat_model>

<verification>
- `mdbook build` exits 0 for both `pmcp-book/` and `pmcp-course/` (revision B-4 — exit code, not substring grep).
- `cargo test --doc -p pmcp --features skills,full` exits 0 with the new Skills doctest passing (revision B-2 — `skills,full`).
- `cargo build -p pmcp` (no features) exits 0.
- `81-07-AUDIT.md` exists with all required sections including the new `## Version-Pin Consistency` (revision W-4).
- `Total excerpts checked: N` value with N >= 10 (revision W-7).
- The course-doctest invariant (`! grep -q 'rust,no_run' pmcp-course/src/part8-advanced/ch23-skills.md` AND `... ch22-code-mode.md`) is recorded as PASS/FAIL in the Doctest Byte-Equality section (revision W-8).
- If audit findings exist, they are documented for follow-up.
- If audit findings are zero, the report records `Overall Verdict: PASS (zero findings)`.
</verification>

<success_criteria>
- Both mdBooks build cleanly (exit-code-based verification).
- The Skills doctest is compile-verified and passing under `--features skills,full`.
- All cross-links resolve to existing files.
- All SUMMARY.md entries resolve to existing files — including the three Phase 81 course-side additions (ch22-exercises.md, ch23-skills.md, ch23-exercises.md).
- Inline excerpts in the new/rewritten chapters match the live source files (with `Total excerpts checked >= 10` guarding against degenerate audits).
- Book and course Code Mode chapters cite byte-equal version pins for `pmcp-code-mode` and `pmcp-code-mode-derive` (revision W-4).
- Course chapters contain zero `rust,no_run` fences (revision W-8).
- ROADMAP.md Phase 81 progress reaches 7/7 plans complete.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-07-SUMMARY.md` recording:
- Reference to the `81-07-AUDIT.md` report.
- One-line summary verdict: `Audit PASS (N findings)` or `Audit FAIL (build/doctest broke — see report)`.
- Per-audit counts (mdBook builds, doctest pass/fail, drift findings, broken links, SUMMARY misses, doctest byte-equality, version-pin consistency).
- Confirmation that `Total excerpts checked >= 10` (revision W-7 guard fired and passed).
- Recommended next step: if zero findings, mark Phase 81 ready for `/gsd-verify-work`. If findings exist, recommend `/gsd-plan-phase 81 --gaps` to plan remediation.
</output>
