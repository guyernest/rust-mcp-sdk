---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-course/src/part8-advanced/ch23-skills.md
  - pmcp-course/src/part8-advanced/ch23-exercises.md
  - pmcp-course/src/quizzes/ch23-skills.toml
  - pmcp-course/src/SUMMARY.md
autonomous: true
requirements: []
tags: [docs, course, skills, sep-2640, exercises, quiz]
must_haves:
  truths:
    - "Course reader encounters the dual-surface invariant within the first screen of the Skills chapter — same load-bearing framing as the book chapter."
    - "Course reader gets a Learning Objectives section up front (course-style) listing 5-7 measurable outcomes."
    - "Course reader has a runnable exercise set targeting the three tiers (hello-world / refunds / code-mode) at three difficulty levels."
    - "Course reader takes a quiz (.ai.toml) verifying chapter comprehension — same format as ch20-mcp-apps.toml."
    - "Course chapter cross-links to the same working examples the book chapter cites — same examples, same code presentation per D-08."
    - "The SUMMARY.md edit ALSO slots ch22-exercises.md (created by plan 81-05) between ch22-code-mode.md and ch23-skills.md so a single SUMMARY edit owns all of Phase 81's course-side additions."
  artifacts:
    - path: pmcp-course/src/part8-advanced/ch23-skills.md
      provides: "Skills (SEP-2640) course chapter — three-tier walkthrough, dual-surface invariant up front, course-style Learning Objectives section."
      min_lines: 400
      contains: "## Learning Objectives"
    - path: pmcp-course/src/part8-advanced/ch23-exercises.md
      provides: "Skills course exercises page — three exercises spanning intro/intermediate/advanced difficulty."
      min_lines: 80
      contains: "## Exercise 1"
    - path: pmcp-course/src/quizzes/ch23-skills.toml
      provides: "Skills course quiz — .ai.toml format matching ch20-mcp-apps.toml structure."
      min_lines: 40
      contains: "id = \"ch23-skills\""
    - path: pmcp-course/src/SUMMARY.md
      provides: "Index entries for ch22-exercises.md (created by plan 81-05) + ch23-skills.md + ch23-exercises.md appended after ch22-code-mode.md."
      contains: "ch23-skills.md"
  key_links:
    - from: pmcp-course/src/SUMMARY.md
      to: pmcp-course/src/part8-advanced/ch23-skills.md
      via: "mdBook SUMMARY index entry"
      pattern: "ch23-skills.md"
    - from: pmcp-course/src/SUMMARY.md
      to: pmcp-course/src/part8-advanced/ch22-exercises.md
      via: "mdBook SUMMARY index entry — created by plan 81-05, slotted by 81-04 Task 4 to keep SUMMARY edits in one place"
      pattern: "ch22-exercises.md"
    - from: pmcp-course/src/part8-advanced/ch23-skills.md
      to: examples/s44_server_skills.rs
      via: "Inline excerpt + 'Full example:' GitHub permalink"
      pattern: "examples/s44_server_skills.rs"
    - from: pmcp-course/src/part8-advanced/ch23-exercises.md
      to: pmcp-course/src/part8-advanced/ch23-skills.md
      via: "Each exercise references skills from the chapter"
      pattern: "ch23-skills.md"
---

<objective>
Author the NEW Skills course chapter `pmcp-course/src/part8-advanced/ch23-skills.md`, its exercises page `ch23-exercises.md`, and its quiz file `ch23-skills.toml`. Append entries to `SUMMARY.md` after `ch22-code-mode.md`. The chapter follows the same three-tier walkthrough as `ch12-8-skills.md` from the book (D-06) — same examples, same code presentation (D-08). The course's distinct voice lives in its Learning Objectives framing, exercises, and quiz (D-09). Per CONTEXT.md D-05, accept the book↔course ordering asymmetry — course chapter is `ch23`, slotted after `ch22-code-mode.md`. Do NOT renumber existing course chapters.

NOTE on SUMMARY ownership (revision-mode B-1 fix): Plan 81-05 creates `pmcp-course/src/part8-advanced/ch22-exercises.md`. To keep SUMMARY.md edits in one place (avoid two plans both editing SUMMARY.md and risking conflict), THIS plan's Task 4 also slots `ch22-exercises.md` into SUMMARY.md right after the `ch22-code-mode.md` entry and before the new `ch23-skills.md` entry. Plan 81-05 therefore does NOT touch SUMMARY.md. The exercises file itself is still created by plan 81-05 — only the SUMMARY index entry lives here.

Purpose: PMCP shipped Skills (Phase 80) but the course has zero coverage. Course readers need both the conceptual depth (matching the book chapter) AND the hands-on framing (Learning Objectives, exercises, quiz) that distinguishes the course property. The course-style framing makes the chapter accessible to enterprise-MCP audiences who learn by doing exercises and validating comprehension via quizzes.

Output:
- `pmcp-course/src/part8-advanced/ch23-skills.md` (~400-600 lines, six H2 sections matching the book + course-style Learning Objectives intro).
- `pmcp-course/src/part8-advanced/ch23-exercises.md` (~80-160 lines, three exercises).
- `pmcp-course/src/quizzes/ch23-skills.toml` (~40-100 lines, 5-8 questions).
- Three inserted lines in `pmcp-course/src/SUMMARY.md` (ch22-exercises.md + ch23-skills.md + ch23-exercises.md).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-CONTEXT.md
@.planning/phases/80-sep-2640-skills-support/80-CONTEXT.md
@.claude/skills/spike-findings-rust-mcp-sdk/SKILL.md
@.claude/skills/spike-findings-rust-mcp-sdk/references/skills-dx-layer.md
@.claude/skills/spike-findings-rust-mcp-sdk/references/skills-wire-protocol.md
@pmcp-course/src/SUMMARY.md
@pmcp-course/src/part8-advanced/ch21-tasks.md
@pmcp-course/src/part8-advanced/ch21-exercises.md
@pmcp-course/src/part8-advanced/ch20-mcp-apps.md
@pmcp-course/src/part8-advanced/ch20-exercises.md
@pmcp-course/src/quizzes/ch20-mcp-apps.toml
@examples/s44_server_skills.rs
@examples/c10_client_skills.rs
@examples/skills/hello-world/SKILL.md
@examples/skills/refunds/SKILL.md
@examples/skills/code-mode/SKILL.md
@tests/skills_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ch23-skills.md (Skills course chapter)</name>
  <files>pmcp-course/src/part8-advanced/ch23-skills.md</files>
  <read_first>
    - `pmcp-course/src/part8-advanced/ch21-tasks.md` — structural model for course-style chapter shape (Title → Learning Objectives → Why X Matters for Enterprise → conceptual sections → "Try this" tips → Chapter Contents → Knowledge Check). This is the course voice the Skills chapter must mirror.
    - `pmcp-course/src/part8-advanced/ch20-mcp-apps.md` — second course-style reference for tone (the chapter that just shipped under Phase 48). Read for cadence of Learning Objectives bullets and "Knowledge Check" exit questions.
    - Plan 81-01's task description for `ch12-8-skills.md` — the book chapter's content. The course chapter has the SAME six H2 sections (per D-06 "same shape in book and course") with the SAME inline excerpts (per D-08 "same reference style in book and course"). The course chapter ADDS a Learning Objectives section + a Knowledge Check section + cross-links to ch23-exercises.md.
    - `.planning/phases/80-sep-2640-skills-support/80-CONTEXT.md` — Phase 80's non-negotiable decisions (same as plan 81-01).
    - `.claude/skills/spike-findings-rust-mcp-sdk/SKILL.md` + both references — source of truth for HOW Skills work (same as plan 81-01).
    - `examples/s44_server_skills.rs`, `examples/c10_client_skills.rs`, the three SKILL.md files, `tests/skills_integration.rs` — same source-of-truth examples as plan 81-01.
  </read_first>
  <action>
    Write `pmcp-course/src/part8-advanced/ch23-skills.md`. Structure mirrors `ch12-8-skills.md` from plan 81-01 PLUS course-specific framing.

    **H1 title:** `# Skills: Agent Workflow Instructions (SEP-2640)` (course style — no "Chapter 23:" prefix, just the topic title; matches ch20-mcp-apps.md / ch21-tasks.md voice).

    **Lead paragraph (before any H2):** ~3-5 sentences. Enterprise-MCP framing: agent-workflow instructions need to be discoverable, versionable, and decoupled from individual tools. Skills (SEP-2640) provides this via the existing `resources/*` primitives — no new RPC methods, just a small additive change to capabilities and three new builder methods. PMCP's Skills support is feature-gated behind `skills`. This chapter walks the same three-tier example shipped at `examples/s44_server_skills.rs`, then hands you exercises in `ch23-exercises.md` and a comprehension quiz.

    **## Learning Objectives**
    Course-style — 5-7 measurable bullets (mirror ch21-tasks.md lines 8-16). State that after this chapter, the reader will be able to:
    - Explain the dual-surface invariant and why a pointer-style prompt body silent-fails on SEP-2640-blind hosts.
    - Register a single-file Skill using `ServerCoreBuilder::skill(...)`.
    - Register a multi-file Skill (SKILL.md + references) using `Skill::with_reference(...)` and understand SEP-2640 §9 visibility filtering.
    - Use `bootstrap_skill_and_prompt(skill, prompt_name)` to publish a Skill that also exposes a prompt fallback for SEP-2640-blind hosts.
    - Identify which working examples in the PMCP repo demonstrate each skill tier (`hello-world` / `refunds` / `code-mode`).
    - Cite the SEP-2640 reference implementations the PMCP example structure deliberately matches (TS SDK / gemini-cli / fast-agent / goose / codex).

    **## Why Skills Matter for Enterprise MCP**
    Course-style framing — 1-2 paragraphs + an ASCII table comparing approaches. Pull from spike-findings + 80-CONTEXT.md:
    - Without Skills, agent-workflow instructions live as system prompts in client code (hard to version, hard to ship cross-host, drift between teams).
    - With Skills, instructions are server-published resources discoverable via `resources/list`, readable via `resources/read`, and version-controllable as files in the server's repo.
    - The dual-surface bootstrap makes Skills work on hosts that don't yet support SEP-2640 — zero "wait for the host ecosystem" friction.

    **## The Dual-Surface Invariant** (within the first 150 lines — non-negotiable, same as the book chapter per D-06)
    - State the load-bearing design property: skill surface and prompt surface are byte-equal by construction, derived from one `Skill` value via `Skill::as_prompt_text()`.
    - Explain WHY pointer-style prompt bodies silent-fail on SEP-2640-blind hosts.
    - Show the `as_prompt_text()` implementation as an inline excerpt (same 10-15 lines as the book chapter pulls from `src/server/skills.rs`).
    - State that `tests/skills_integration.rs` asserts byte-equality at runtime; the invariant is enforced in CI, not aspirational.
    - End with one paragraph framing: "the next three tiers build on this invariant."

    **## Tier 1: Hello-World Skill** — Same content as the book chapter Tier 1 (plan 81-01 Task 1). Per D-08, same inline excerpts, same prose, same "Full example:" cross-link. ADD one course-style "Try this" tip at the end: "Run `cargo run --example s44_server_skills --features skills,full` and observe the printed URI list. Note that no `references/...` URI appears — that's §9 visibility filtering, not a bug."

    **## Tier 2: Refunds Skill with References (SEP-2640 §9 Visibility Filtering)** — Same content as the book chapter Tier 2. ADD one course-style "Try this" tip: "After running the example, manually call `resources/list` and compare against the URIs you'd expect from the file system. The supporting files exist but are not listed — confirm this matches your reading of the chapter."

    **## Tier 3: Code-Mode Skill (Composition with Another Advanced Feature)** — Same content as the book chapter Tier 3. ADD one course-style "Try this" tip pointing at `examples/c10_client_skills.rs`: "Run the client example. Observe the byte-equality assertion at the end of main() — note that the example panics if the invariant breaks. Why is panicking better than silently passing?"

    **## Cross-SDK Compatibility (Why Three Tiers Match Other Reference Implementations)** — Same content as the book chapter, but ADD an enterprise framing paragraph: "Cross-SDK compatibility matters for enterprise deployments that target multiple agent platforms. A skill that works in one platform's MCP host should work in another's — Phase 80 deliberately chose the three-tier shape to keep PMCP comparable against the TS SDK, gemini-cli, fast-agent, goose, and codex reference implementations."

    **## Future Work (Deferred from Phase 80)** — Same content as the book chapter.

    **## Chapter Contents** (course-style — point reader at the exercises and quiz)
    - Mirror `ch21-tasks.md` lines 105-115 structure. Roughly:
      "This chapter has two hands-on continuations:
      1. **[Chapter 23 Exercises](./ch23-exercises.md)** -- Practice registering single-file skills, multi-file skills, and dual-surface bootstrapping.
      2. **Knowledge check below** -- Quick comprehension questions before continuing."

    **## Knowledge Check** (course-style exit framing)
    - 3-4 questions. Examples:
      - What does the dual-surface invariant guarantee, and why does it require byte-equality between the skill surface and prompt surface (not just semantic equivalence)?
      - Why are reference files (`skill://<name>/references/...`) addressable via `resources/read` but absent from `resources/list`?
      - What deferred Phase 80 item (mentioned in the "Future Work" section) would change how skill bodies are embedded into a server binary if implemented?

    **Style invariants (enforce throughout — same as plan 81-01):**
    - Same inline-excerpt + cross-link style as the book chapter (D-07, D-08).
    - Same grep-stable excerpt boundaries (whole functions / whole SKILL.md sections).
    - All Rust snippets are `rust,ignore`. NO `rust,no_run` blocks in the course chapter — the doctest mandate (D-10) is book-only.
    - Cross-link format verbatim: ``Full example: [`examples/<file>.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/<file>.rs)``.
    - Total chapter length: target 400-600 lines.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## Learning Objectives' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## Why Skills Matter for Enterprise MCP' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## The Dual-Surface Invariant' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## Tier 1' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## Tier 2' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## Tier 3' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## Cross-SDK Compatibility' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## Future Work' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; grep -q '## Knowledge Check' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; ! grep -q 'rust,no_run' pmcp-course/src/part8-advanced/ch23-skills.md &amp;&amp; [ "$(wc -l &lt; pmcp-course/src/part8-advanced/ch23-skills.md)" -ge 350 ]</automated>
  </verify>
  <done>
    `ch23-skills.md` exists with all course-style sections present (Learning Objectives, Why X Matters, Dual-Surface Invariant, Tier 1-3, Cross-SDK Compatibility, Future Work, Chapter Contents, Knowledge Check), file length ≥ 350 lines, no `rust,no_run` blocks (doctest is book-only).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write ch23-exercises.md (Skills course exercises)</name>
  <files>pmcp-course/src/part8-advanced/ch23-exercises.md</files>
  <read_first>
    - `pmcp-course/src/part8-advanced/ch21-exercises.md` — structural reference for course exercise page format (H1, intro paragraph, exercises numbered with **Difficulty:** annotations + minutes, **Steps**, **Verify your solution**, **Questions to answer**, --- separators). The Skills exercises follow this exact shape.
    - `pmcp-course/src/part8-advanced/ch20-exercises.md` — second reference (smaller, ~1.8K) for tone calibration.
    - `examples/s44_server_skills.rs` and `examples/skills/*` — the working examples the exercises build on.
  </read_first>
  <action>
    Write `pmcp-course/src/part8-advanced/ch23-exercises.md`. Mirror the structure of `ch21-exercises.md` (intro paragraph + 3 exercises with consistent fields).

    **H1 title:** `# Chapter 23 Exercises`

    **Intro paragraph (~2 sentences):** "These exercises build your fluency with PMCP Skills (SEP-2640). Each one targets a specific skill from the chapter, ordered from mechanical setup (Tier 1) to composition with another advanced feature (Tier 3)."

    **## Exercise 1: Register a Single-File Skill (`hello-world`)**
    - **Difficulty:** Introductory (10 min)
    - Framing: practice the mechanical steps of wiring up a single-file skill, no references. Reader builds a binary that registers `skill://hello-world/SKILL.md` and prints the registered URIs.
    - **Steps:**
      1. Create a new binary project (or add to an existing test crate).
      2. Add `pmcp = { version = "...", features = ["skills", "full"] }` to Cargo.toml.
      3. Create a skill body string (e.g., `"# Hello\nGreet the user.\n"`).
      4. Call `pmcp::Server::builder().name(...).version(...).skill(Skill::new("hello-world", body)).build()?`.
      5. Print confirmation including the registered URI list.
    - **Verify your solution:** Run the binary; expect printed output naming `skill://hello-world/SKILL.md` plus auto-synthesized `skill://index.json`.
    - **Questions to answer:**
      - What is the default URI for a skill registered with `Skill::new("foo", body)` and no `.with_path(...)`?
      - What happens if you call `.skill(Skill::new("foo", ...))` twice with the same name? (Hint: read 80-CONTEXT.md D-5.)

    **## Exercise 2: Register a Multi-File Skill with References (`refunds` Tier)**
    - **Difficulty:** Intermediate (25 min)
    - Framing: build a refunds skill with a SKILL.md body and one or two `references/*.md` supporting files. Demonstrate that the references appear in `resources/read` but NOT in `resources/list`.
    - **Steps:**
      1. Create three files in your project: `skills/refunds/SKILL.md`, `skills/refunds/references/policy.md`, `skills/refunds/references/examples.md`.
      2. Use `include_str!` to embed each at compile time.
      3. Construct `Skill::new("refunds", REFUNDS).with_path("acme/billing/refunds").with_reference(SkillReference::new("references/policy.md", "text/markdown", POLICY)).with_reference(SkillReference::new("references/examples.md", "text/markdown", EXAMPLES))`.
      4. Register via `.skill(refunds)` and build the server.
      5. Use the Skills handler (extract it from the example pattern in `examples/c10_client_skills.rs`) to call `list(None, ...)` and `read("skill://acme/billing/refunds/references/policy.md", ...)`.
      6. Assert the list contains the SKILL.md URI + the index URI but NOT either of the references.
      7. Assert the read of `references/policy.md` returns the file's body.
    - **Verify your solution:** Provide a `cargo test` (or `cargo run` with `assert!` calls in main) that demonstrates the §9 filtering. The exercise solution is verified when both assertions pass.
    - **Questions to answer:**
      - Why does SEP-2640 §9 require this filtering? (What attack/UX problem would arise if references were listed?)
      - What MIME type would you set for a `.graphql` reference file? (Hint: look at `examples/s44_server_skills.rs` line 49.)

    **## Exercise 3: Dual-Surface Bootstrap for a Code-Mode Skill (`code-mode` Tier)**
    - **Difficulty:** Advanced (45 min)
    - Framing: bring it all together. Register the `code-mode` skill from `examples/skills/code-mode/` and also publish a prompt fallback via `bootstrap_skill_and_prompt`. Build a tiny client that exercises both flows and asserts byte-equality (the runtime proof of the dual-surface invariant).
    - **Steps:**
      1. Copy or include `examples/skills/code-mode/SKILL.md` + references into your project.
      2. Construct the `Skill` with all three references (`schema.graphql`, `examples.md`, `policies.md`), matching `build_code_mode_skill()` from `examples/s44_server_skills.rs`.
      3. Call `.bootstrap_skill_and_prompt(skill.clone(), "start_code_mode")` on the server builder.
      4. Build the server, then call `server.get_prompt("start_code_mode")` and invoke `.handle(HashMap::new(), extra)` — this is the legacy host flow.
      5. Separately, use `Skills::new().add(skill.clone()).into_handler()?` to construct the SEP-2640 handler. Walk `list` + `read` for each URI (SKILL.md + 3 references) and concatenate with `\n--- {path} ---\n` separators. This is the SEP-2640 host flow.
      6. Assert byte-equality between the two concatenated results.
    - **Verify your solution:** The `assert_eq!` passes. If it fails, the dual-surface invariant is broken — your `as_prompt_text()` understanding is wrong. (This is the same assertion `examples/c10_client_skills.rs` makes; you have a reference implementation if you get stuck.)
    - **Questions to answer:**
      - Why does the chapter (and Phase 80) call the byte-equality property "load-bearing"? What silent-failure mode does it prevent?
      - If you wanted to register the code-mode skill but did NOT call `bootstrap_skill_and_prompt`, what would change for a host that doesn't support SEP-2640? (Hint: 80-CONTEXT.md D-7.)

    **Final note:** Reference `examples/c10_client_skills.rs` as the canonical reference impl. Use the same `cargo run --example c10_client_skills --features skills,full` command in the chapter's "Try this" tips so reader has a working solution they can copy patterns from.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/part8-advanced/ch23-exercises.md &amp;&amp; grep -q '## Exercise 1' pmcp-course/src/part8-advanced/ch23-exercises.md &amp;&amp; grep -q '## Exercise 2' pmcp-course/src/part8-advanced/ch23-exercises.md &amp;&amp; grep -q '## Exercise 3' pmcp-course/src/part8-advanced/ch23-exercises.md &amp;&amp; grep -q '\*\*Difficulty:\*\* Introductory' pmcp-course/src/part8-advanced/ch23-exercises.md &amp;&amp; grep -q '\*\*Difficulty:\*\* Intermediate' pmcp-course/src/part8-advanced/ch23-exercises.md &amp;&amp; grep -q '\*\*Difficulty:\*\* Advanced' pmcp-course/src/part8-advanced/ch23-exercises.md &amp;&amp; [ "$(wc -l &lt; pmcp-course/src/part8-advanced/ch23-exercises.md)" -ge 80 ]</automated>
  </verify>
  <done>
    `ch23-exercises.md` contains three exercises spanning Introductory / Intermediate / Advanced. Each has Steps, Verify your solution, Questions to answer. File length ≥ 80 lines. Structure mirrors `ch21-exercises.md`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write ch23-skills.toml (Skills course quiz)</name>
  <files>pmcp-course/src/quizzes/ch23-skills.toml</files>
  <read_first>
    - `pmcp-course/src/quizzes/ch20-mcp-apps.toml` — structural reference for `.ai.toml` quiz format (id, title, lesson_id, pass_threshold, [[questions]] blocks with type/prompt/answer/context/id). Read the first 5-8 question blocks to internalize the format (MultipleChoice with distractors list, ShortAnswer with alternatives, both with explanatory `context` string).
    - The Skills chapter content (ch23-skills.md from Task 1) — questions must be answerable from chapter content alone, not external knowledge.
  </read_first>
  <action>
    Write `pmcp-course/src/quizzes/ch23-skills.toml`. Structure mirrors `ch20-mcp-apps.toml`.

    **Header block (top of file):**
    ```toml
    # Quiz: Skills - Agent Workflow Instructions (SEP-2640)

    id = "ch23-skills"
    title = "Skills: SEP-2640 Agent Workflow Instructions"
    lesson_id = "ch23"
    pass_threshold = 0.7
    ```

    **Question 1 — MultipleChoice (the dual-surface invariant):**
    Prompt: "What does the dual-surface invariant guarantee about a PMCP Skill registered via `bootstrap_skill_and_prompt`?"
    Distractors (wrong answers; mark each with a comment about why it's plausible-but-wrong):
    - "The skill URI and the prompt name are the same string."
    - "The prompt body contains a `skill://` URI that the host can fetch on demand."
    - "The skill and prompt are advertised in separate `tools/list` and `prompts/list` responses."
    Correct answer: "The skill surface (concatenated `resources/read` results) and the prompt surface (`prompts/get` body) are byte-equal by construction."
    Context: explain that pointer-style prompt bodies silent-fail on SEP-2640-blind hosts; byte-equality is enforced via `Skill::as_prompt_text()` and asserted in `tests/skills_integration.rs`.

    **Question 2 — MultipleChoice (SEP-2640 §9 visibility filtering):**
    Prompt: "Why does SEP-2640 §9 require that supporting files (`skill://<name>/references/...`) be readable via `resources/read` but NOT enumerated in `resources/list`?"
    Distractors:
    - "Listing them would exceed the JSON-RPC payload size limit on most transports."
    - "Supporting files use a different MIME type than SKILL.md and break legacy clients."
    - "Listing them would require a new RPC method (`references/list`)."
    Correct answer: "It keeps the discovery surface focused on top-level skills (one entry per SKILL.md + the index) so agents pick skills cleanly without scanning every supporting file."
    Context: explain that the SkillsHandler::list() filters explicitly to preserve a clean discovery experience.

    **Question 3 — ShortAnswer (capability declaration):**
    Prompt: "Which `ServerCapabilities` field gained an additive entry in Phase 80 to declare SEP-2640 support? (Field name only.)"
    Answer: "extensions"
    Alternatives: ["extensions", "capabilities.extensions"]
    Context: explain GAP #1 from spike 001 — `ServerCapabilities` needed an `extensions: Option<HashMap<String, Value>>` field parallel to `experimental` so SEP-2640 support could be declared wire-correctly at `capabilities.extensions["io.modelcontextprotocol/skills"] = {}`.

    **Question 4 — MultipleChoice (three-tier example structure):**
    Prompt: "Why does PMCP's Skills example structure deliberately include the `hello-world` and `refunds` skills in addition to `code-mode`?"
    Distractors:
    - "To exercise feature flags `skills`, `code-mode`, and `full` separately."
    - "To demonstrate three different MIME types: text/plain, text/markdown, and application/graphql."
    - "Because SEP-2640 §6 requires at least three skills be registered for the capability to be advertised."
    Correct answer: "To enable side-by-side developer-experience comparison against other SEP-2640 reference implementations (TS SDK, gemini-cli, fast-agent, goose, codex), which all use the same `hello-world` + `refunds` tier shape."
    Context: explain that code-mode is the *advanced* tier showing composition with another PMCP feature; the canonical demo for cross-SDK comparison is `hello-world` + `refunds`.

    **Question 5 — MultipleChoice (which builder method):**
    Prompt: "Which `ServerCoreBuilder` method registers a single Skill AND publishes a parallel prompt whose body is byte-equal to the skill content, from one `Skill` value?"
    Distractors:
    - "`.skill(skill)` followed by `.prompt(name, handler)`."
    - "`.skills(Skills::new().add(skill)).prompt(name, SkillPromptHandler::new(skill))`."
    - "`.skill(skill).dual_surface_prompt(name)`."
    Correct answer: "`.bootstrap_skill_and_prompt(skill, prompt_name)`"
    Context: explain that the chained alternatives risk drift — `bootstrap_skill_and_prompt` derives both surfaces from one input, structurally preventing drift.

    **Question 6 — ShortAnswer (feature flag):**
    Prompt: "PMCP's Skills support is feature-gated. Which Cargo feature flag must be enabled to use `pmcp::server::skills::Skill` and the builder methods? (Feature name only.)"
    Answer: "skills"
    Alternatives: ["skills"]
    Context: explain that `skills` is opt-in (not in `default-features`) so users who don't need SEP-2640 don't pull the surface.

    **Question 7 — MultipleChoice (deferred items):**
    Prompt: "Which item from Phase 80's deferred-items list would allow a server author to write `Skill::from_disk(\"skills/hello-world/SKILL.md\")` and have the SKILL.md parsed at compile time?"
    Distractors:
    - "SEP-2640 §4 archive distribution (application/gzip blob mode)."
    - "Skill subscription via `resources/subscribe` for hot-reload."
    - "Multi-tenant skill scoping for host-side disambiguation."
    Correct answer: "The `#[pmcp::skill]` procedural macro (deferred from Phase 80)."
    Context: explain that until the macro lands, server authors use `Skill::new(name, include_str!(...))` to embed SKILL.md at compile time.

    Format each question following the `ch20-mcp-apps.toml` exact structure (use `[[questions]]` blocks; `prompt.prompt`, `prompt.distractors`, `answer.answer`, `answer.position`, `context`, `id` keys). Use UUID-like `id = "..."` strings — format: `"f23a1b2c-d3e4-5678-abcd-{question_number padded}"` (e.g. `"f23a1b2c-d3e4-5678-abcd-123456800001"` through `"...000007"`). For MultipleChoice questions, choose `answer.position = 0` (correct answer goes first in your distractors order or use a different position consistently — match `ch20-mcp-apps.toml` conventions; verify by reading its pattern).

    Total: 7 questions, ~80-100 lines including TOML comments and IDs.
  </action>
  <verify>
    <automated>test -f pmcp-course/src/quizzes/ch23-skills.toml &amp;&amp; grep -q 'id = "ch23-skills"' pmcp-course/src/quizzes/ch23-skills.toml &amp;&amp; grep -q 'lesson_id = "ch23"' pmcp-course/src/quizzes/ch23-skills.toml &amp;&amp; [ "$(grep -c '^\[\[questions\]\]' pmcp-course/src/quizzes/ch23-skills.toml)" -ge 5 ] &amp;&amp; [ "$(wc -l &lt; pmcp-course/src/quizzes/ch23-skills.toml)" -ge 40 ]</automated>
  </verify>
  <done>
    `ch23-skills.toml` exists with `id = "ch23-skills"`, `lesson_id = "ch23"`, at least 5 `[[questions]]` blocks (target 7), file length ≥ 40 lines. TOML parses cleanly (no syntax errors).
  </done>
</task>

<task type="auto">
  <name>Task 4: Add SUMMARY.md entries for ch22-exercises (from 81-05), ch23-skills, ch23-exercises</name>
  <files>pmcp-course/src/SUMMARY.md</files>
  <read_first>
    - `pmcp-course/src/SUMMARY.md` lines 149-170 — the "Part VIII: Advanced Patterns" block. Confirm structure: each chapter is a top-level bullet, sub-pages (exercises, sub-chapters) are nested two-space-indented children. Confirm that `ch22-code-mode.md` is the last top-level bullet currently (no exercises sub-page exists for ch22 — verify, and slot ch22-exercises.md as a 2-space-indented child after it).
    - Verify ordering: plan 81-05 owns the CREATION of `pmcp-course/src/part8-advanced/ch22-exercises.md`. Plan 81-04 (this plan) owns the SUMMARY index entry for that file. Both plans are in Wave 1 and have no inter-dependency on each other for execution order — the file existence check in the verify command tolerates the SUMMARY entry being slotted before the file is written (mdbook build in plan 81-07 is what catches missing files, and 81-07 runs in Wave 2 after both 81-04 and 81-05 land).
  </read_first>
  <action>
    Insert THREE new lines into `pmcp-course/src/SUMMARY.md`, in the "Part VIII: Advanced Patterns" block. The insertion sequence (in order, immediately AFTER the existing `- [Code Mode: Validated LLM Code Execution](./part8-advanced/ch22-code-mode.md)` line):

    ```
      - [Chapter 22 Exercises](./part8-advanced/ch22-exercises.md)
    - [Skills: Agent Workflow Instructions](./part8-advanced/ch23-skills.md)
      - [Chapter 23 Exercises](./part8-advanced/ch23-exercises.md)
    ```

    Indentation rules:
    - `ch22-exercises.md` is 2-space-indented (child of ch22-code-mode.md, mirrors the ch20/ch21 exercises pattern in the same SUMMARY block).
    - `ch23-skills.md` is at file root (top-level bullet, sibling of ch22-code-mode.md).
    - `ch23-exercises.md` is 2-space-indented (child of ch23-skills.md, same pattern).

    Insertion point: directly after `- [Code Mode: Validated LLM Code Execution](./part8-advanced/ch22-code-mode.md)` and before the closing `---` of Part VIII (or before the next `# Appendices` block, whichever comes first — check the actual structure of SUMMARY.md lines 166-172).

    Do NOT modify any other lines. Do NOT renumber any existing chapter entries (per CONTEXT.md D-05 — accept the asymmetric ordering).

    Do NOT add a `ch23-skills.toml` reference — quiz files are NOT in SUMMARY.md; they are referenced by the quiz embed JS (per the existence of `pmcp-course/src/quiz/quiz-embed.iife.js` per the earlier directory listing). Same for `ch22-code-mode.toml` (created by 81-05, not slotted in SUMMARY).

    Coordination note (revision B-1 fix): plan 81-05 does NOT edit SUMMARY.md — that responsibility lives here so a single SUMMARY edit owns all three new course-side index entries for Phase 81. The ch22-exercises.md FILE is still created by plan 81-05; this task only owns its SUMMARY slot.
  </action>
  <verify>
    <automated>grep -q 'ch22-exercises.md' pmcp-course/src/SUMMARY.md &amp;&amp; grep -q 'ch23-skills.md' pmcp-course/src/SUMMARY.md &amp;&amp; grep -q 'ch23-exercises.md' pmcp-course/src/SUMMARY.md &amp;&amp; awk '/ch22-code-mode.md/{a=NR} /ch22-exercises.md/{b=NR} /ch23-skills.md/{c=NR} /ch23-exercises.md/{d=NR} END{exit !(a&lt;b &amp;&amp; b&lt;c &amp;&amp; c&lt;d)}' pmcp-course/src/SUMMARY.md</automated>
  </verify>
  <done>
    `SUMMARY.md` contains exactly three new lines: `ch22-exercises.md` (indented child of ch22-code-mode.md), `ch23-skills.md` (top-level), and `ch23-exercises.md` (indented child of ch23-skills.md). All four entries appear in the order: ch22-code-mode → ch22-exercises → ch23-skills → ch23-exercises. No other lines modified.
  </done>
</task>

</tasks>

<threat_model>
Documentation-only change. No new authentication, authorization, input validation, or trust-boundary surface introduced. Threats considered:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-04-01 | Misleading | ch23-skills.md inline excerpts | mitigate | Same content as book chapter ch12-8-skills.md (plan 81-01). Plan 81-07 audits both for grep-stable excerpt drift. |
| T-81-04-02 | Misleading | ch23-skills.md security framing | mitigate | Same as plan 81-01 — chapter explicitly states Skills are pure context-loading, authorization remains with the tools the skill references. |
| T-81-04-03 | Incorrect quiz answers | ch23-skills.toml | mitigate | Every quiz answer is sourced from chapter content + 80-CONTEXT.md + spike-findings SKILL.md. The `context` field in each [[questions]] block explains the reasoning, giving readers a citation back to the source. |
| T-81-04-04 | Cross-plan coordination | SUMMARY.md ownership for ch22-exercises | mitigate | Plan 81-05 creates the FILE; plan 81-04 (this plan) owns the SUMMARY entry. Single-edit ownership of SUMMARY.md prevents merge conflicts and double-edits. Plan 81-07's SUMMARY audit (Audit C) verifies each entry resolves to an existing file after both plans land. |
</threat_model>

<verification>
- `pmcp-course/src/part8-advanced/ch23-skills.md` exists with all 9 mandatory H2 sections (`## Learning Objectives`, `## Why Skills Matter for Enterprise MCP`, `## The Dual-Surface Invariant`, `## Tier 1`, `## Tier 2`, `## Tier 3`, `## Cross-SDK Compatibility`, `## Future Work`, `## Knowledge Check`).
- `pmcp-course/src/part8-advanced/ch23-exercises.md` exists with three exercises spanning Introductory / Intermediate / Advanced.
- `pmcp-course/src/quizzes/ch23-skills.toml` exists with valid TOML and ≥ 5 questions.
- `pmcp-course/src/SUMMARY.md` contains three new entries (ch22-exercises.md + ch23-skills.md + ch23-exercises.md) positioned after `ch22-code-mode.md` in the documented order.
- No `rust,no_run` blocks in the course chapter (per D-10 — doctest is book-only).
- mdBook build (plan 81-07 verifies) succeeds for `pmcp-course/`.
</verification>

<success_criteria>
- Course reader of `ch23-skills.md` follows the same three-tier walkthrough as the book chapter (same examples, same code presentation per D-08), plus course-specific Learning Objectives and Knowledge Check framing.
- Course reader has 3 exercises (intro/intermediate/advanced) and a 7-question quiz for comprehension validation.
- SUMMARY.md indexes ch22-exercises.md (created by plan 81-05) alongside ch23-skills + ch23-exercises so all three new course pages are reachable from the table of contents.
- mdBook build succeeds for `pmcp-course/`.
- ROADMAP.md Phase 81 progress increments by 1 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-04-SUMMARY.md` recording:
- Paths to all four files created/modified with line counts (ch23-skills.md, ch23-exercises.md, ch23-skills.toml, SUMMARY.md).
- The three SUMMARY.md insertions (line numbers) — including the ch22-exercises.md slot that plan 81-05's file creation depends on.
- Question count + total quiz file line count.
- Confirmation that no `rust,no_run` blocks were added (course chapter is ignore-only per D-10).
- Cross-reference list: every "Full example:" link in the chapter pointing back to actual repo paths.
- Coordination note (revision B-1 fix): explicit record that plan 81-05's SUMMARY responsibility was absorbed into this task.
</output>
