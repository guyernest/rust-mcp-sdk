---
phase: 81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-book/src/ch12-8-skills.md
  - pmcp-book/src/SUMMARY.md
  - src/server/skills.rs
autonomous: true
requirements: []
tags: [docs, book, skills, sep-2640]
must_haves:
  truths:
    - "Book reader encounters the dual-surface invariant within the first screen of the Skills chapter."
    - "Reader can walk three tiers (hello-world / refunds / code-mode) by reading inline excerpts that match the live examples byte-for-byte."
    - "Reader has a runnable, compile-verified code snippet at the end of the chapter (rust,no_run doctest) demonstrating .skill() and .bootstrap_skill_and_prompt()."
    - "Cross-SDK reference implementations are cited so reader can compare PMCP against TS SDK / gemini-cli / fast-agent / goose / codex."
    - "Deferred items (#[pmcp::skill] macro, SEP-2640 §4 archive distribution) are surfaced as forward-looking notes so reader is not misled into thinking they exist."
  artifacts:
    - path: pmcp-book/src/ch12-8-skills.md
      provides: "Skills (SEP-2640) book chapter — single file, three-tier walkthrough, dual-surface invariant up front, ends with rust,no_run doctest."
      min_lines: 400
      contains: "## The Dual-Surface Invariant"
    - path: pmcp-book/src/SUMMARY.md
      provides: "Index entry for the new Skills chapter slotted between Tasks (ch12-7) and Code Mode (ch12-9)."
      contains: "ch12-8-skills.md"
    - path: src/server/skills.rs
      provides: "A rust,no_run doctest item (function or module-level doc comment) demonstrating .skill() + .bootstrap_skill_and_prompt() — verifiable via `cargo test --doc -p pmcp --features skills,full`."
      contains: "```rust,no_run"
  key_links:
    - from: pmcp-book/src/SUMMARY.md
      to: pmcp-book/src/ch12-8-skills.md
      via: "mdBook SUMMARY index entry"
      pattern: "ch12-8-skills.md"
    - from: pmcp-book/src/ch12-8-skills.md
      to: examples/s44_server_skills.rs
      via: "Inline excerpt + 'Full example:' GitHub permalink"
      pattern: "examples/s44_server_skills.rs"
    - from: pmcp-book/src/ch12-8-skills.md
      to: tests/skills_integration.rs
      via: "Inline citation as the byte-equality enforcement test"
      pattern: "tests/skills_integration.rs"
---

<objective>
Author the NEW book chapter `pmcp-book/src/ch12-8-skills.md` covering SEP-2640 Skills support, slot it into `SUMMARY.md` between Tasks (ch12-7) and Code Mode (ch12-9), and embed a runnable `rust,no_run` doctest demonstrating `.skill()` and `.bootstrap_skill_and_prompt()` in `src/server/skills.rs` so the example is compile-verified via `cargo test --doc`.

Purpose: PMCP shipped Skills (Phase 80, SEP-2640) but the book has zero coverage of the feature. Without this chapter, readers cannot discover that PMCP supports Skills, do not learn the dual-surface invariant (the load-bearing design decision), and have no path to compose Skills with code-mode. The chapter lifts Phase 80's three-tier example structure (hello-world / refunds / code-mode) verbatim so the chapter narrative tracks the working examples; the chapter end-to-end teaches the dual-surface invariant first, demonstrates it across three tiers, and ends with a doctest the reader can copy-paste.

Output:
- `pmcp-book/src/ch12-8-skills.md` (~500-700 lines, six H2 sections).
- One inserted line in `pmcp-book/src/SUMMARY.md`.
- One `rust,no_run` doctest added to `src/server/skills.rs` (module-level or a dedicated `doc_test_chapter_example` item), feature-gated behind `skills`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-CONTEXT.md
@.planning/phases/80-sep-2640-skills-support/80-CONTEXT.md
@.claude/skills/spike-findings-rust-mcp-sdk/SKILL.md
@.claude/skills/spike-findings-rust-mcp-sdk/references/skills-dx-layer.md
@.claude/skills/spike-findings-rust-mcp-sdk/references/skills-wire-protocol.md
@pmcp-book/src/ch12-7-tasks.md
@pmcp-book/src/ch12-9-code-mode.md
@pmcp-book/src/SUMMARY.md
@examples/s44_server_skills.rs
@examples/c10_client_skills.rs
@examples/skills/hello-world/SKILL.md
@examples/skills/refunds/SKILL.md
@examples/skills/code-mode/SKILL.md
@tests/skills_integration.rs
@src/server/skills.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ch12-8-skills.md (Skills book chapter)</name>
  <files>pmcp-book/src/ch12-8-skills.md</files>
  <read_first>
    - `pmcp-book/src/ch12-7-tasks.md` — the structural model the Skills chapter mirrors (Title → "Why" → ASCII diagram → narrative with inline excerpts → "Full example" cross-link → doctest). The Skills chapter must NOT invent a new chapter shape; it follows ch12-7 verbatim except for the dual-surface section up front.
    - `pmcp-book/src/ch12-9-code-mode.md` — the existing Code Mode chapter (will be rewritten in plan 81-02). The Skills Tier 3 section cross-links to it; do NOT duplicate its content.
    - `.planning/phases/80-sep-2640-skills-support/80-CONTEXT.md` — Phase 80's 10 non-negotiable implementation decisions. The chapter narrative MUST NOT contradict these; in particular D-1 (no new traits), D-7 (dual-surface rule, byte-equal inlining, not pointer-redirect), D-8 (three-tier example is non-negotiable, code-mode is the advanced tier not the canonical demo).
    - `.claude/skills/spike-findings-rust-mcp-sdk/SKILL.md` and both reference files (`skills-dx-layer.md`, `skills-wire-protocol.md`) — these are the source of truth for HOW Skills work. Excerpts and explanations in the chapter MUST match these.
    - `examples/s44_server_skills.rs` — the canonical server example. The chapter inline-quotes the three tiers from this file. Read it end-to-end so you can produce grep-stable 10-40 line excerpts.
    - `examples/c10_client_skills.rs` — the client example. Tier 1/Tier 2/Tier 3 sections may quote the `sep_2640_flow` / `legacy_prompt_flow_via_get_prompt` blocks; the byte-equality assertion at the bottom of main() is the chapter's anchor for "this isn't aspirational — Phase 80's CI enforces it."
    - `examples/skills/hello-world/SKILL.md`, `examples/skills/refunds/SKILL.md`, `examples/skills/code-mode/SKILL.md` — the actual skill bodies. Tier sections quote these.
    - `tests/skills_integration.rs` — cited (not quoted at length) as the byte-equality enforcement test. Read enough to write one accurate sentence about what it asserts.
    - `src/server/skills.rs` — the production module the chapter teaches against. Read the public surface (Skill, SkillReference, Skills, .skill(), .skills(), .bootstrap_skill_and_prompt(), .as_prompt_text()) so prose matches the real types.
  </read_first>
  <action>
    Write `pmcp-book/src/ch12-8-skills.md` with these six H2 sections in this order. Each section's content directive is non-negotiable.

    **H1 title:** `# Chapter 12.8: Skills — Agent Workflow Instructions (SEP-2640)`

    **Lead paragraph (before any H2):** ~3-5 sentences. Frame Skills as "rich, structured agent-workflow instructions discovered via existing `resources/*` primitives — no new RPC methods, but a small additive change to `ServerCapabilities` and three new builder methods." State that PMCP's Skills support is feature-gated behind `skills` in `Cargo.toml`. State that this chapter walks the same three-tier example shipped at `examples/s44_server_skills.rs`.

    **## The Dual-Surface Invariant** (appears within the first 100 lines — non-negotiable, see 80-CONTEXT.md D-7 and spike-findings SKILL.md):
    - State the load-bearing design property: the skill surface (one or more `resources/read` results concatenated) and the prompt surface (`prompts/get` body) are byte-equal by construction, derived from one `Skill` value via `Skill::as_prompt_text()`.
    - Explain WHY: hosts that don't yet support SEP-2640 fall back to `prompts/get`; a pointer-style prompt body silent-fails on those hosts because the LLM gets a literal URI string but the host has no mechanism to fetch it.
    - Show the `as_prompt_text()` implementation as a small inline excerpt (10-15 lines from `src/server/skills.rs` — find it via grep "fn as_prompt_text"). Reader sees how concatenation produces the labelled-rule format and how `\n--- relative_path ---\n` separators work.
    - State that `tests/skills_integration.rs` asserts byte-equality at runtime — this invariant is enforced in CI, not aspirational.
    - End the section with: "Tiers 1–3 below build on this invariant. The bootstrap helper `bootstrap_skill_and_prompt(skill, prompt_name)` is what makes drift between the two surfaces structurally impossible."

    **## Tier 1: Hello-World Skill**
    - Tier 1 framing: smallest possible skill. One SKILL.md, no references, default URI path.
    - Inline excerpt: the `examples/skills/hello-world/SKILL.md` body in full (it is short, ~10 lines per the file size).
    - Inline excerpt from `examples/s44_server_skills.rs` lines covering `.skill(Skill::new("hello-world", HELLO_WORLD))` registration — grep-stable, 5-10 lines including the surrounding context.
    - Explain: `Skill::new(name, body)` is the minimum. Default URI is `skill://<name>/SKILL.md`. Builder method `.skill(Skill)` is convenience over `.skills(Skills::new().add(skill))`.
    - State what reader sees in `resources/list` after this single skill is registered (the URI + auto-synthesized `skill://index.json`).
    - "Full example: [`examples/s44_server_skills.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/s44_server_skills.rs)"

    **## Tier 2: Refunds Skill with References (SEP-2640 §9 Visibility Filtering)**
    - Tier 2 framing: real-world skill — SKILL.md body plus optional supporting files (references). Demonstrates `.with_path("acme/billing/refunds")` for namespaced URIs.
    - Inline excerpt: `examples/skills/refunds/SKILL.md` body (the file is ~1.1K per ls — quote it in full or quote the first 30 lines and elide the rest with "...").
    - Inline excerpt from `examples/s44_server_skills.rs` showing the Tier 2 registration with `.with_path(...)` — grep-stable, 3-6 lines.
    - SEP-2640 §9 explanation: supporting files in the `references/` subdirectory ARE addressable via `resources/read` but MUST NOT appear in `resources/list` or `skill://index.json`. This is the spec-defined "readable but not listable" behavior. The `SkillsHandler::list()` filters explicitly.
    - State that `examples/s44_server_skills.rs` notes this in its module doc comment (lines 24-29) and `tests/skills_integration.rs` locks the behavior in CI.
    - "Full example: [`examples/s44_server_skills.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/s44_server_skills.rs)"

    **## Tier 3: Code-Mode Skill (Composition with Another Advanced Feature)**
    - Tier 3 framing: Skills compose with another PMCP advanced feature without modifying that feature. The validate_code / execute_code tools from Code Mode (Chapter 12.9) are unchanged; only the bootstrap layer moves from a hand-rolled prompt + scattered resources into a single SEP-2640 skill that ALSO exposes a prompt fallback.
    - Inline excerpt: `examples/s44_server_skills.rs` `build_code_mode_skill()` function (lines 45-62 per the file read above) — multi-reference skill construction.
    - Inline excerpt: the `.bootstrap_skill_and_prompt(code_mode_skill.clone(), "start_code_mode")` line from `main()` of `s44_server_skills.rs` (~3 lines with surrounding builder context).
    - Cross-link to Chapter 12.9 Code Mode for the validate_code / execute_code tool surface. Do NOT duplicate that chapter's content here — this section's job is to show how the same data flows through both the skill surface (SEP-2640 host) and the prompt surface (legacy host).
    - Inline excerpt: the byte-equality assertion at the bottom of `examples/c10_client_skills.rs` main() (the `assert_eq!(sep_2640_text, prompt_text, ...)` call and the println showing equal byte counts — ~4-6 lines). Frame it: "this is the runtime proof the chapter's dual-surface invariant section described."
    - "Full example: [`examples/s44_server_skills.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/s44_server_skills.rs) + [`examples/c10_client_skills.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/c10_client_skills.rs)"

    **## Cross-SDK Compatibility (Why Three Tiers Match Other Reference Implementations)**
    - Framing per 80-CONTEXT.md D-8: hello-world and refunds appear in TS SDK, gemini-cli, fast-agent, goose, and codex SEP-2640 reference implementations. Keeping the same example shape in PMCP enables side-by-side DX comparison.
    - URL: `https://github.com/modelcontextprotocol/experimental-ext-skills`
    - One paragraph framing: PMCP's Skills surface (5-line server-author code per skill, dual-surface bootstrap as a single builder call, byte-equal invariant enforced in CI) is the differentiator. Compare against other SDKs by running their reference servers + PMCP's `s44_server_skills.rs` side-by-side.
    - Cite the SEP itself: `https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2640` (as-of commit `ade2a58`, April 2026).

    **## Future Work (Deferred from Phase 80)**
    - One paragraph per item, NO subsections per item.
    - **`#[pmcp::skill]` procedural macro:** would read SKILL.md from disk at compile time and emit a `Skill` constant. Deferred from Phase 80 as a follow-on spike. Until it ships, server authors use `Skill::new(name, include_str!("skills/.../SKILL.md"))`.
    - **SEP-2640 §4 archive distribution (`application/gzip` blob):** blocked by GAP #2 from spike 001 — `Content::Resource` has no `blob` field. SEP marks archive mode as optional, and text-mode skills work fully without it.
    - State that both are explicit non-goals for Phase 80 and Phase 81 — chapters surface them so readers are not misled into thinking they exist.

    **Doctest at the end of the chapter:** Append (after the Future Work section) a brief `### Try It Yourself` section with one short paragraph: "The doctest below registers a single skill plus a dual-surface prompt, then asserts on the prompt text length. It is compile-verified via `cargo test --doc -p pmcp --features skills,full`. The same doctest is embedded in `src/server/skills.rs` so it stays in sync with the production code."
    - Inline a `rust,no_run` fenced block (NOT `rust,ignore`) with this content:
      ```rust,no_run
      use pmcp::server::skills::Skill;

      fn main() -> Result<(), Box<dyn std::error::Error>> {
          let greeting = Skill::new("hello-world", "# Hello\nThis is a minimal skill.\n");
          let prompt_text = greeting.as_prompt_text();
          assert!(prompt_text.starts_with("# Hello"));

          let _server = pmcp::Server::builder()
              .name("doctest-skills-demo")
              .version("0.1.0")
              .skill(greeting.clone())
              .bootstrap_skill_and_prompt(greeting, "hello_prompt")
              .build()?;
          Ok(())
      }
      ```
    - This is the *content* the chapter displays. The matching `rust,no_run` block that `cargo test --doc` runs is added by Task 3 below in `src/server/skills.rs`.

    **Style invariants (enforce throughout):**
    - Same inline-excerpt + cross-link style as `ch12-7-tasks.md` (D-07, D-08).
    - Every inline excerpt has a clear function/struct/section boundary (grep-stable) so a future drift audit can verify it still matches the live file. Prefer quoting whole functions or whole H1/H2 sections of a SKILL.md over arbitrary line ranges.
    - Use `rust,ignore` for all OTHER snippets in the chapter (Skill::new, Skill::with_reference, etc. that don't need to compile). The only `rust,no_run` block is the doctest at the end.
    - Cross-link format verbatim: ``Full example: [`examples/<file>.rs`](https://github.com/paiml/rust-mcp-sdk/blob/main/examples/<file>.rs)``.
    - Total chapter length: target 500-700 lines (CONTEXT.md D-01 budget). If approaching 700, prefer fewer/shorter inline excerpts over deleting a section — the six H2 sections are mandatory.
  </action>
  <verify>
    <automated>test -f pmcp-book/src/ch12-8-skills.md &amp;&amp; grep -q '## The Dual-Surface Invariant' pmcp-book/src/ch12-8-skills.md &amp;&amp; grep -q '## Tier 1' pmcp-book/src/ch12-8-skills.md &amp;&amp; grep -q '## Tier 2' pmcp-book/src/ch12-8-skills.md &amp;&amp; grep -q '## Tier 3' pmcp-book/src/ch12-8-skills.md &amp;&amp; grep -q '## Cross-SDK Compatibility' pmcp-book/src/ch12-8-skills.md &amp;&amp; grep -q '## Future Work' pmcp-book/src/ch12-8-skills.md &amp;&amp; grep -q 'rust,no_run' pmcp-book/src/ch12-8-skills.md &amp;&amp; awk '/## The Dual-Surface Invariant/{print NR; exit}' pmcp-book/src/ch12-8-skills.md | awk '$1 &lt; 100 {exit 0} {exit 1}' &amp;&amp; [ "$(wc -l &lt; pmcp-book/src/ch12-8-skills.md)" -ge 400 ]</automated>
  </verify>
  <done>
    `ch12-8-skills.md` exists with all six mandatory H2 sections present, "## The Dual-Surface Invariant" appears within the first 100 lines, one `rust,no_run` fenced block exists (the chapter doctest body), file length ≥ 400 lines. All inline excerpts come from grep-stable boundaries (whole functions or whole sections) in the source files cited.
  </done>
</task>

<task type="auto">
  <name>Task 2: Slot ch12-8-skills.md into pmcp-book/src/SUMMARY.md</name>
  <files>pmcp-book/src/SUMMARY.md</files>
  <read_first>
    - `pmcp-book/src/SUMMARY.md` lines 24-35 (the "Part III: Advanced Features" block including the existing `ch12-7-tasks.md` and `ch12-9-code-mode.md` entries between which the new entry slots).
  </read_first>
  <action>
    Insert one new line into `pmcp-book/src/SUMMARY.md` between the existing `ch12-7-tasks.md` and `ch12-9-code-mode.md` lines (Part III: Advanced Features). The new line, exactly matching the indentation of its neighbors (no leading whitespace at file root level since `ch12-7-tasks.md` itself has no leading whitespace):

    `- [Chapter 12.8: Skills — Agent Workflow Instructions](ch12-8-skills.md)`

    Insertion point: directly after the `- [Chapter 12.7: MCP Tasks — Long-Running Operations](ch12-7-tasks.md)` line and directly before the `- [Chapter 12.9: Code Mode — LLM Code Validation &amp; Execution](ch12-9-code-mode.md)` line.

    Do NOT modify any other lines in `SUMMARY.md`. Do NOT renumber any existing chapter entries.

    Use the em-dash character `—` (U+2014) in the chapter title, matching the existing `ch12-9-code-mode.md` entry style (which uses an `—` in its title per the SUMMARY.md read). The chapter file's H1 (created in Task 1) must use the same em-dash so the SUMMARY title visually matches the chapter.
  </action>
  <verify>
    <automated>grep -q 'ch12-8-skills.md' pmcp-book/src/SUMMARY.md &amp;&amp; awk '/ch12-7-tasks.md/{a=NR} /ch12-8-skills.md/{b=NR} /ch12-9-code-mode.md/{c=NR} END{exit !(a&lt;b &amp;&amp; b&lt;c)}' pmcp-book/src/SUMMARY.md</automated>
  </verify>
  <done>
    `SUMMARY.md` contains exactly one new line referencing `ch12-8-skills.md`, positioned between the existing `ch12-7-tasks.md` and `ch12-9-code-mode.md` lines. No other lines modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Embed rust,no_run doctest in src/server/skills.rs (compile-verified)</name>
  <files>src/server/skills.rs</files>
  <behavior>
    - Behavior 1: `cargo test --doc -p pmcp --features skills,full` runs the new doctest and exits 0. (RED before this task: no such doctest exists in skills.rs. GREEN after: doctest compiles and runs.) Why `skills,full` (not bare `skills`): the doctest body invokes `pmcp::Server::builder().name(...).version(...).skill(...).bootstrap_skill_and_prompt(...).build()`, and `Server::builder()` and its returned type pull in transitive runtime dependencies that are only fully wired under the `full` feature set (confirmed via `Cargo.toml`'s `[[example]] s44_server_skills` entry: `required-features = ["skills", "full"]`). Bare `--features skills` is insufficient to compile the doctest body.
    - Behavior 2: The doctest body is byte-identical to the `rust,no_run` fenced block in `pmcp-book/src/ch12-8-skills.md` (the chapter doctest at the end of the "Try It Yourself" section). This is the drift-prevention property — chapter content and compile-verified content are the same string.
    - Behavior 3: The doctest is feature-gated; running `cargo test --doc -p pmcp` (without `--features skills,full`) compiles cleanly and skips the doctest (because the surrounding module is `#[cfg(feature = "skills")]` per existing skills.rs structure). It MUST NOT fail to compile under the no-features build.
  </behavior>
  <read_first>
    - `src/server/skills.rs` — read the module-level doc comment (top of file) and any existing pub items' doc comments. Identify the cleanest insertion point. Two viable options:
      (a) Append to the module-level `//!` block at the top of the file (cleanest — the doctest then runs as part of `cargo test --doc` for the module's doc comment).
      (b) Attach to a new public `#[doc = ...]` zero-cost helper item (more verbose, only needed if option (a) breaks the existing module doc structure).
      Prefer option (a) unless reading shows the module doc is structured as a multi-section explanation where appending would break the prose flow.
    - The crate already has `skills` as a feature flag (per 80-CONTEXT.md and confirmed in the skills.rs file existing with `#[cfg(feature = "skills")]` gating elsewhere). The doctest does NOT need an explicit `#[cfg]` attribute — the surrounding module is already gated, so the doctest is only built/run when the `skills` feature is enabled.
    - `Cargo.toml` at the repo root — confirm the `[[example]] s44_server_skills` entry sets `required-features = ["skills", "full"]`. This is the load-bearing reason the doctest's verify command uses `--features skills,full` (NOT bare `skills`): `Server::builder()` chain requires the full feature surface.
    - `pmcp-book/src/ch12-8-skills.md` — the `rust,no_run` fenced block at the end of the "## Try It Yourself" section. Read it after Task 1 lands. The body in `skills.rs` must be byte-identical to the body inside that fenced block.
  </read_first>
  <action>
    Append a `rust,no_run` doctest to the module-level `//!` doc comment in `src/server/skills.rs`. Use this exact pattern (the body must match the chapter's doctest from Task 1 byte-for-byte):

    ```rust
    //! # Book Chapter Doctest
    //!
    //! Compile-verified mirror of the doctest at the end of
    //! `pmcp-book/src/ch12-8-skills.md`. Keeping these byte-equal is
    //! enforced by Phase 81 plan 81-07 (cross-property consistency audit).
    //!
    //! ```rust,no_run
    //! use pmcp::server::skills::Skill;
    //!
    //! fn main() -> Result<(), Box<dyn std::error::Error>> {
    //!     let greeting = Skill::new("hello-world", "# Hello\nThis is a minimal skill.\n");
    //!     let prompt_text = greeting.as_prompt_text();
    //!     assert!(prompt_text.starts_with("# Hello"));
    //!
    //!     let _server = pmcp::Server::builder()
    //!         .name("doctest-skills-demo")
    //!         .version("0.1.0")
    //!         .skill(greeting.clone())
    //!         .bootstrap_skill_and_prompt(greeting, "hello_prompt")
    //!         .build()?;
    //!     Ok(())
    //! }
    //! ```
    ```

    Place this block at the end of the existing module-level `//!` comment (immediately before the first non-doc-comment line, typically a `use` or `#[cfg(...)]` attribute). Do NOT modify any existing module doc text.

    If the file does not currently have a module-level `//!` doc comment block (read first to confirm), add one minimally — but inspect first; per the file size (~51KB) and known-good Phase 80 patterns, a module doc comment almost certainly already exists.

    After writing, run `make quality-gate` to confirm the file still passes formatting + clippy (`cargo fmt --all -- --check` + clippy --all-features --all-targets -- -D warnings); doctest blocks inside `//!` comments are standard rustdoc and trigger no clippy lints. If `make quality-gate` flags the addition, the most likely cause is a trailing-whitespace issue in the doctest — fix via re-write with no trailing whitespace.
  </action>
  <verify>
    <automated>cargo test --doc -p pmcp --features skills,full 2>&amp;1 | tee /tmp/skills_doctest.log &amp;&amp; grep -E 'test result.*[0-9]+ passed' /tmp/skills_doctest.log &amp;&amp; ! grep -E 'test result.*FAILED' /tmp/skills_doctest.log &amp;&amp; cargo build -p pmcp 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    `cargo test --doc -p pmcp --features skills,full` exits 0 and reports at least one passing doctest (the new one); `cargo build -p pmcp` without `--features skills,full` succeeds (verifying the doctest does not break the no-features build); the doctest body is byte-identical to the `rust,no_run` block in `pmcp-book/src/ch12-8-skills.md`.
  </done>
</task>

</tasks>

<threat_model>
Documentation-only change. No new authentication, authorization, input validation, or trust-boundary surface introduced. Threats considered:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01-01 | Misleading | ch12-8-skills.md inline excerpts | mitigate | All excerpts are grep-stable (whole functions / whole SKILL.md sections); plan 81-07 audits that every excerpt still matches the live source file. The rust,no_run doctest at the end of the chapter is compile-verified by `cargo test --doc`, eliminating drift on that specific snippet. |
| T-81-01-02 | Misleading | ch12-8-skills.md security framing | mitigate | The chapter explicitly states (per Phase 80 D-7 and the skills-wire-protocol.md "Skills do not grant authority" note) that Skills are pure context-loading; authorization and execution remain the responsibility of the tools the skill references (e.g., validate_code / execute_code for code-mode skills). |
| T-81-01-03 | Misleading | Dual-surface invariant explanation | mitigate | The chapter cites `tests/skills_integration.rs` as the runtime enforcement test; readers can verify the claim by reading that test, not just trusting the chapter prose. |
</threat_model>

<verification>
- `pmcp-book/src/ch12-8-skills.md` exists and contains all six mandatory H2 sections (`## The Dual-Surface Invariant`, `## Tier 1`, `## Tier 2`, `## Tier 3`, `## Cross-SDK Compatibility`, `## Future Work`).
- `## The Dual-Surface Invariant` appears within the first 100 lines.
- The chapter contains exactly one `rust,no_run` fenced block at the end (the "Try It Yourself" doctest body).
- `pmcp-book/src/SUMMARY.md` contains a new line referencing `ch12-8-skills.md` positioned between the existing `ch12-7-tasks.md` and `ch12-9-code-mode.md` lines.
- `src/server/skills.rs` contains a `rust,no_run` doctest in its module-level `//!` doc comment, byte-identical to the chapter doctest.
- `cargo test --doc -p pmcp --features skills,full` exits 0 with at least one new passing doctest.
- `cargo build -p pmcp` (no features) still succeeds — the new doctest does not break the default build.
- `make quality-gate` exits 0 (verifies fmt + clippy + tests pass with the source-file edit).
</verification>

<success_criteria>
- Reader of `ch12-8-skills.md` can answer "what is the dual-surface invariant" from the first H2 section alone.
- Reader can walk hello-world / refunds / code-mode tiers using inline excerpts that match `examples/s44_server_skills.rs` byte-for-byte.
- The chapter doctest at the end of `ch12-8-skills.md` is identical to the doctest in `src/server/skills.rs` and both compile-verify via `cargo test --doc -p pmcp --features skills,full`.
- mdBook build (plan 81-07 verifies this) succeeds for `pmcp-book/`.
- ROADMAP.md Phase 81 progress increments by 1 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/81-update-pmcp-book-and-pmcp-course-with-v2-advanced-topics-cod/81-01-SUMMARY.md` recording:
- Path to the new chapter file with line count.
- Path to the SUMMARY.md edit (one line inserted).
- Path to the skills.rs doctest insertion (line range).
- Output of `cargo test --doc -p pmcp --features skills,full` (the relevant "test result: ok. N passed" line).
- Confirmation that `make quality-gate` exits 0.
</output>
