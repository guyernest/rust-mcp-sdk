---
phase: 65-examples-cleanup-protocol-accuracy
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - Cargo.toml
  - examples/*.rs
autonomous: true
requirements:
  - EXMP-03

must_haves:
  truths:
    - "Every example file uses the role-prefix numbering scheme: {role}{nn}_{name}.rs where role is s, c, t, or m"
    - "No two example files share the same prefix (role+number combination)"
    - "Every [[example]] entry in Cargo.toml references the new filename"
    - "No old-named files remain in examples/"
  artifacts:
    - path: "Cargo.toml"
      provides: "Updated [[example]] entries with new names and paths"
      contains: "[[example]]"
    - path: "examples/"
      provides: "Renamed example files with role-prefix scheme"
  key_links:
    - from: "Cargo.toml [[example]] entries"
      to: "examples/*.rs files"
      via: "path field matches actual filename on disk"
      pattern: 'path = "examples/[scmt][0-9]'
---

<objective>
Renumber ALL example files using the role-prefixed numbering scheme (s/c/t/m prefix) with sequential numbers within each role, and update all Cargo.toml [[example]] entries to match.

Purpose: Resolve EXMP-03 (no duplicate prefixes) by doing a full clean-slate renumber per D-05 and D-06. The role-prefix scheme provides at-a-glance organization (like rmcp's subdirectories) without directory nesting.
Output: All example files renamed, all Cargo.toml entries updated, zero duplicate prefixes.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-examples-cleanup-protocol-accuracy/65-CONTEXT.md
@.planning/phases/65-examples-cleanup-protocol-accuracy/65-01-SUMMARY.md
@Cargo.toml
</context>

<threat_model>
This is a file renaming and Cargo.toml update phase. No runtime code changes. No new dependencies. The only risk is breaking references to old example names in documentation files -- mitigated by the fact that the examples/README.md is currently wrong anyway (will be replaced in Plan 03), and the root README.md does not reference individual examples by filename. The executor should search for old example names in .md files to verify no references break.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rename all example files to role-prefix scheme and update Cargo.toml</name>
  <read_first>
    - Cargo.toml (all [[example]] entries, lines ~220-434)
    - .planning/phases/65-examples-cleanup-protocol-accuracy/65-01-SUMMARY.md (to know which files survived the audit)
  </read_first>
  <files>
    Cargo.toml,
    examples/*.rs (all files renamed)
  </files>
  <action>
    Per D-05 and D-06: Full sequential renumber of ALL examples. Clean slate with role-prefixed numbering:

    **Naming scheme:** `{role}{nn}_{descriptive_name}.rs`
    - `s` = Server examples (tools, resources, prompts, sampling, tasks, apps, workflow, auth, middleware on server-side)
    - `c` = Client examples (client initialization, client tools, client resources, client prompts, sampling from client perspective)
    - `t` = Transport examples (stdio, HTTP, WebSocket, SSE, streamable-http)
    - `m` = Middleware examples (auth middleware, error recovery, observability, enhanced middleware)

    **Numbering:** Sequential within each role starting at 01, ordered by complexity (basic -> advanced) within each MCP capability group.

    **Process:**
    1. First, list all surviving .rs files in examples/ (after Plan 01 cleanup)
    2. Categorize each file by role (s/c/t/m) based on its content:
       - Files implementing MCP server handlers, tools, resources, prompts, workflow, apps -> `s`
       - Files implementing MCP client operations -> `c`
       - Files focused on transport layer (WebSocket server, SSE, streamable HTTP, connection pooling) -> `t`
       - Files focused on middleware patterns (auth middleware, error recovery, observability) -> `m`
    3. Within each role category, order by MCP capability then complexity:
       - Server: basic server -> tools -> resources -> prompts -> sampling -> typed tools -> macros -> workflow -> apps -> auth
       - Client: initialization -> tools -> resources -> prompts -> parallel clients
       - Transport: WebSocket -> SSE -> streamable HTTP (stateful) -> streamable HTTP (stateless) -> streamable HTTP client
       - Middleware: basic middleware -> enhanced middleware -> auth middleware -> error recovery -> observability
    4. Assign sequential numbers: s01, s02, ..., c01, c02, ..., t01, t02, ..., m01, m02, ...
    5. Use `git mv` for each rename to preserve history: `git mv examples/OLD_NAME.rs examples/NEW_NAME.rs`
    6. Update ALL [[example]] entries in Cargo.toml to use the new names:
       - Update `name = "new_name"` (without .rs extension)
       - Update `path = "examples/new_name.rs"`
       - Keep `required-features` unchanged
    7. Sort the [[example]] entries in Cargo.toml by name (all s entries first, then c, then t, then m, each sorted by number)

    **Important:** Use `git mv` (not plain `mv`) to preserve file history. Run all renames first, then update Cargo.toml in one edit pass.

    **Example mapping (indicative, actual assignment based on file content):**
    - `02_server_basic.rs` -> `s01_basic_server.rs`
    - `04_server_resources.rs` -> `s02_server_resources.rs`
    - `06_server_prompts.rs` -> `s03_server_prompts.rs`
    - `01_client_initialize.rs` -> `c01_client_initialize.rs`
    - `03_client_tools.rs` -> `c02_client_tools.rs`
    - `13_websocket_transport.rs` -> `t01_websocket_transport.rs`
    - `15_middleware.rs` -> `m01_basic_middleware.rs`
    etc.

    After all renames, also search for old example names referenced in any .md files:
    ```bash
    grep -rn "cargo run --example" *.md docs/ .planning/ 2>/dev/null | grep -v "examples/README.md"
    ```
    Update any references found to use the new names.
  </action>
  <verify>
    <automated>
      bash -c 'DUPES=$(ls examples/*.rs 2>/dev/null | xargs -I{} basename {} .rs | sed "s/_.*$//" | sort | uniq -d | wc -l | tr -d " "); echo "Duplicate prefix count: $DUPES"; test "$DUPES" -eq 0 && echo "PASS: No duplicate prefixes" || echo "FAIL: Duplicate prefixes found"'
    </automated>
  </verify>
  <acceptance_criteria>
    - Every .rs file in examples/ matches pattern `^[scmt][0-9][0-9]_`: `ls examples/*.rs | xargs -I{} basename {} | grep -v "^[scmt][0-9][0-9]_" | wc -l` returns 0
    - No two files share a prefix: `ls examples/*.rs | xargs -I{} basename {} .rs | sed 's/_.*//' | sort | uniq -d` returns empty
    - No old-style numbered files remain: `ls examples/*.rs | xargs -I{} basename {} | grep "^[0-9]" | wc -l` returns 0
    - No unnumbered files remain: `ls examples/*.rs | xargs -I{} basename {} | grep -v "^[scmt][0-9]" | wc -l` returns 0
    - Every [[example]] entry in Cargo.toml has a path starting with `examples/[scmt]`: `grep 'path = "examples/' Cargo.toml | grep -v 'examples/[scmt]' | wc -l` returns 0
    - File count in examples/ matches [[example]] count in Cargo.toml: the two numbers are equal
    - `cargo check --features full` exits 0
  </acceptance_criteria>
  <done>All example files use the role-prefix naming scheme (s/c/t/m + sequential number), no duplicate prefixes exist, all Cargo.toml entries reference the new filenames, and the workspace compiles.</done>
</task>

</tasks>

<verification>
1. `ls examples/*.rs | xargs -I{} basename {} .rs | sed 's/_.*//' | sort | uniq -d` returns empty (no duplicate prefixes)
2. `ls examples/*.rs | xargs -I{} basename {} | grep -v "^[scmt][0-9][0-9]_"` returns empty (all files use role-prefix scheme)
3. Count of .rs files equals count of [[example]] entries in Cargo.toml
4. `cargo check --features full` exits 0
5. No old-style numbered example names referenced in .md files
</verification>

<success_criteria>
- Zero duplicate prefixes across all example files
- Every example file matches `[scmt][0-9][0-9]_*.rs` pattern
- Every file has a matching [[example]] entry in Cargo.toml
- `cargo check --features full` passes
</success_criteria>

<output>
After completion, create `.planning/phases/65-examples-cleanup-protocol-accuracy/65-02-SUMMARY.md`

The SUMMARY must include the full mapping table of old names -> new names, as Plan 03 (examples/README.md) needs this to build the index.
</output>
