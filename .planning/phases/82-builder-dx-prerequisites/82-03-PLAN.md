---
phase: 82-builder-dx-prerequisites
plan: 03
type: execute
wave: 2
depends_on:
  - "82-01"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - pmcp-book/src/ch15-testing.md
autonomous: true
requirements:
  - BLDR-03
  - BLDR-04
tags:
  - documentation
  - requirements-update
  - book-chapter
user_setup: []

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` contains a `BLDR-04` umbrella requirement covering the four extra `_arc` lifts (resources_arc, sampling_arc, auth_provider_arc, tool_authorizer_arc)"
    - "`.planning/REQUIREMENTS.md` BLDR-03 wording explicitly mentions `Server::get_tool(name) accessor symmetric with get_prompt`"
    - "`.planning/REQUIREMENTS.md` traceability table at line 339+ has a row for BLDR-04 → Phase 82 → Pending"
    - "`.planning/ROADMAP.md` Phase 82 block lists BLDR-04 in its Requirements line, and the Success Criteria section gains two new bullets (5 and 6) covering the four extra `_arc` lifts (D-06) and the `Server::get_tool` accessor (D-07)"
    - "`pmcp-book/src/ch15-testing.md` gains a new section under the chapter that demonstrates the handler-level testing pattern with the explicit D-03 'what this skips' callout naming `auth_provider`, `tool_authorizer`, `tool_middleware`, and `pmcp::Client` over stdio/streamable-http as the full-pipeline alternative"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "BLDR-04 umbrella requirement + reworded BLDR-03 + traceability row"
      contains: "BLDR-04"
    - path: .planning/ROADMAP.md
      provides: "Phase 82 block updated with BLDR-04 in Requirements line and two new Success Criteria bullets"
      contains: "BLDR-04"
    - path: pmcp-book/src/ch15-testing.md
      provides: "New section on the handler-level testing pattern for MCP servers"
      contains: "tool_arc"
  key_links:
    - from: ".planning/REQUIREMENTS.md BLDR-03 wording"
      to: "Server::get_tool implementation in Plan 01"
      via: "explicit accessor reference in requirement text"
      pattern: "Server::get_tool"
    - from: ".planning/REQUIREMENTS.md BLDR-04"
      to: "the four lifts in Plan 01 Task 3"
      via: "umbrella requirement enumerating the four method names"
      pattern: "resources_arc.*sampling_arc.*auth_provider_arc.*tool_authorizer_arc"
---

<objective>
Update the planning artifacts and book chapter to formalize the BLDR-04 umbrella requirement, reword BLDR-03 to mention `Server::get_tool` accessor symmetry, extend ROADMAP.md Phase 82 success criteria with bullets 5 and 6, and add a book section to `pmcp-book/src/ch15-testing.md` demonstrating the handler-level testing pattern with the D-03 callout.

Purpose (per CONTEXT.md D-06, D-08, and D-02 part (c)): closes the requirements/spec gap opened by D-05 (lifting all six `_arc` methods instead of just two) and D-07 (adding `get_tool` accessor), and lands the third documentation surface (book section) named in D-02 alongside the doctests (Plan 01) and the integration test (Plan 02). After this plan, every locked decision in CONTEXT.md has either landing code (Plan 01), landing tests (Plan 02), or landing docs (this plan).

Output: three edited markdown files. No source code changes. No new files except an inline section addition to the existing `ch15-testing.md`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-builder-dx-prerequisites/82-CONTEXT.md
@.planning/REQUIREMENTS.md
@pmcp-book/src/ch15-testing.md
@pmcp-book/src/SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update REQUIREMENTS.md — add BLDR-04, reword BLDR-03, add traceability row</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 114-125 (the `### Builder DX Prerequisites` block with BLDR-01, BLDR-02, BLDR-03 — exact wording to preserve and the line numbers for inserting BLDR-04)
    - .planning/REQUIREMENTS.md lines 335-345 (the traceability table around BLDR-01/02/03 rows — the BLDR-04 row goes immediately after BLDR-03)
    - .planning/phases/82-builder-dx-prerequisites/82-CONTEXT.md §decisions D-06 (the exact umbrella-requirement wording for BLDR-04: *"`pmcp::ServerBuilder` gains `_arc` variants for the remaining four handler types (`resources_arc`, `sampling_arc`, `auth_provider_arc`, `tool_authorizer_arc`) so all impl-or-Arc handler-registration paths reach parity with `ServerCoreBuilder`."*)
    - .planning/phases/82-builder-dx-prerequisites/82-CONTEXT.md §decisions D-08 (the BLDR-03 reword guidance: must explicitly mention `Server::get_tool(name) accessor symmetric with get_prompt`)
  </read_first>
  <behavior>
    - Behavior 1: After edits, `grep -E '^\- \[ \] \*\*BLDR-04\*\*:' .planning/REQUIREMENTS.md` returns exactly one match.
    - Behavior 2: After edits, the BLDR-03 line contains the literal substring `Server::get_tool(name) accessor symmetric with get_prompt`.
    - Behavior 3: After edits, the traceability table has a row `| BLDR-04 | Phase 82 | Pending |` immediately after the `BLDR-03` row at line 341.
    - Behavior 4: BLDR-01 and BLDR-02 wording is unchanged (only BLDR-03 is reworded; BLDR-04 is added).
  </behavior>
  <action>
    Three edits to `.planning/REQUIREMENTS.md`:

    1. **Reword BLDR-03 (line 122).** Change its content to read:

       `- [ ] **BLDR-03**: Officially documented handler-level testing pattern so external toolkit integration tests can drive request flow without poking at private \`Server::handle_request\` — delivered via \`Server::get_tool(name)\` accessor symmetric with \`get_prompt\`, plus a comprehensive doctest on each accessor and a reference integration test under \`tests/in_process_handler_pattern.rs\` (rejecting the \`Server::dispatch\`/\`MemoryTransport\` alternative as out-of-scope for this phase per CONTEXT.md D-01).`

       Use this exact text. Implements CONTEXT.md D-08.

    2. **Insert BLDR-04 immediately after BLDR-03.** Add a new line at what is currently line 123:

       `- [ ] **BLDR-04**: \`pmcp::ServerBuilder\` gains \`_arc\` variants for the remaining four handler types (\`resources_arc\`, \`sampling_arc\`, \`auth_provider_arc\`, \`tool_authorizer_arc\`) so all impl-or-Arc handler-registration paths reach parity with \`ServerCoreBuilder\`. This closes the umbrella Arc-symmetry gap surfaced in spike 004's "API surface discovery 1" — no external toolkit author should need a 20-line delegating wrapper shim for any handler type.`

       Use this exact text. Implements CONTEXT.md D-06.

    3. **Add traceability row.** In the traceability table around line 341 (immediately after the `| BLDR-03 | Phase 82 | Pending |` row), insert:

       `| BLDR-04 | Phase 82 | Pending |`

    Do NOT modify BLDR-01 or BLDR-02 wording. Do NOT modify any other requirement's traceability row. Do NOT update the milestone totals (the milestone-totals line at the top of REQUIREMENTS.md, if present, is updated when STATE.md/PROJECT.md velocity rolls forward — that's a separate plan, not this one).
  </action>
  <verify>
    <automated>grep -E '^\- \[ \] \*\*BLDR-04\*\*:' .planning/REQUIREMENTS.md && grep -E 'Server::get_tool\(name\) accessor symmetric with get_prompt' .planning/REQUIREMENTS.md && grep -E '^\| BLDR-04 \| Phase 82 \| Pending \|' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c -E '^\- \[ \] \*\*BLDR-04\*\*:' .planning/REQUIREMENTS.md` returns exactly 1.
    - Source assertion: `grep -c -E 'Server::get_tool\(name\) accessor symmetric with get_prompt' .planning/REQUIREMENTS.md` returns ≥1 (in the reworded BLDR-03).
    - Source assertion: the BLDR-04 entry contains all four method names: `grep -E 'BLDR-04' .planning/REQUIREMENTS.md | grep -c -E 'resources_arc.*sampling_arc.*auth_provider_arc.*tool_authorizer_arc'` returns ≥1 (single line containing all four in order).
    - Source assertion: `grep -c '^| BLDR-04 | Phase 82 | Pending |' .planning/REQUIREMENTS.md` returns exactly 1.
    - Negative assertion: BLDR-01 and BLDR-02 lines are byte-identical to their pre-edit form: `git diff .planning/REQUIREMENTS.md` only shows changes on the BLDR-03 line, the BLDR-04 insertion, and the traceability-table BLDR-04 row insertion — no other lines touched.
    - Negative assertion: the milestone-totals line (if present) is unchanged in this plan.
  </acceptance_criteria>
  <done>REQUIREMENTS.md contains the reworded BLDR-03, the new BLDR-04, and the BLDR-04 traceability row. No other lines were touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md — Phase 82 Requirements line and Success Criteria extension</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 1238-1248 (the Phase 82 block — current Requirements line at 1241 lists `BLDR-01, BLDR-02, BLDR-03`; current Success Criteria has bullets 1-4 at lines 1243-1246; "Plans: TBD" at line 1247)
    - .planning/phases/82-builder-dx-prerequisites/82-CONTEXT.md §decisions D-06 (the ROADMAP success-criteria bullet 5 must cover the four extras' parity claim)
    - .planning/phases/82-builder-dx-prerequisites/82-CONTEXT.md §decisions D-07 (the bullet 6 must cover `Server::get_tool` accessor symmetry)
  </read_first>
  <behavior>
    - Behavior 1: The Phase 82 `**Requirements**:` line (currently line 1241) lists all four IDs: `BLDR-01, BLDR-02, BLDR-03, BLDR-04`.
    - Behavior 2: The Phase 82 `**Success Criteria**` section (currently lines 1242-1246) has six bullets (was four — bullets 5 and 6 are added).
    - Behavior 3: The Phase 82 `**Plans**:` line (currently `TBD` at line 1247) is updated to enumerate the three plan files created in this phase.
    - Behavior 4: Phase 83-89 blocks are byte-identical pre/post — no neighbouring-phase damage.
  </behavior>
  <action>
    Three edits to `.planning/ROADMAP.md`, all within lines 1238-1247 of the Phase 82 block:

    1. **Update Requirements line (line 1241).** Change from:
       `**Requirements**: BLDR-01, BLDR-02, BLDR-03`
       to:
       `**Requirements**: BLDR-01, BLDR-02, BLDR-03, BLDR-04`

    2. **Add Success Criteria bullets 5 and 6.** Immediately after bullet 4 (line 1246), insert two new bullets:

       `  5. All six \`_arc\` handler-registration methods (\`tool_arc\`, \`prompt_arc\`, \`resources_arc\`, \`sampling_arc\`, \`auth_provider_arc\`, \`tool_authorizer_arc\`) are publicly callable on \`pmcp::ServerBuilder\` and reach parity with \`ServerCoreBuilder\`, closing the umbrella Arc-symmetry gap (BLDR-04)`
       `  6. \`pmcp::Server::get_tool(&str) -> Option<&Arc<dyn ToolHandler>>\` is publicly callable, symmetric with the existing \`pmcp::Server::get_prompt\`, so the documented handler-level testing pattern functions for tools as well as prompts (BLDR-03 / D-07)`

       Match the existing bullets' indentation (two leading spaces before the number).

    3. **Update Plans line (line 1247).** Change from:
       `**Plans**: TBD`
       to:
       `**Plans**: 3 plans — 82-01 (six \`_arc\` lifts + \`get_tool\` accessor + doctests), 82-02 (\`tests/in_process_handler_pattern.rs\` regression anchor + equivalence property test), 82-03 (REQUIREMENTS.md/ROADMAP.md/book-section updates)`

    Implements CONTEXT.md D-06 (ROADMAP success-criteria bullet 5) and D-07 (bullet 6).

    Do NOT touch the Phase 82 Goal line (line 1239), Depends-on line (line 1240), or the Phase 83+ blocks below line 1248.
  </action>
  <verify>
    <automated>grep -E '^\*\*Requirements\*\*: BLDR-01, BLDR-02, BLDR-03, BLDR-04$' .planning/ROADMAP.md && awk '/^### Phase 82:/,/^### Phase 83:/' .planning/ROADMAP.md | grep -cE '^\s+[1-6]\.' </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c -E '^\*\*Requirements\*\*: BLDR-01, BLDR-02, BLDR-03, BLDR-04$' .planning/ROADMAP.md` returns exactly 1.
    - Source assertion: extracting the Phase 82 block via `awk '/^### Phase 82:/,/^### Phase 83:/' .planning/ROADMAP.md | grep -cE '^\s+[1-6]\.'` returns 6 (six numbered success-criteria bullets).
    - Source assertion: the Phase 82 block contains all six method names: `awk '/^### Phase 82:/,/^### Phase 83:/' .planning/ROADMAP.md | grep -cE 'tool_arc.*prompt_arc.*resources_arc.*sampling_arc.*auth_provider_arc.*tool_authorizer_arc'` returns ≥1.
    - Source assertion: the Phase 82 block contains the literal substring `get_tool(&str) -> Option<&Arc<dyn ToolHandler>>`.
    - Source assertion: the Phase 82 block contains the literal substring `82-01` and `82-02` and `82-03` in its Plans line.
    - Negative assertion: `git diff .planning/ROADMAP.md` shows changes ONLY within lines 1238-1250 (no neighbouring-phase damage). Concretely: `git diff -U0 .planning/ROADMAP.md | grep -E '^@@' | grep -vE '@@ -12[3-5][0-9]'` returns no output.
  </acceptance_criteria>
  <done>ROADMAP.md Phase 82 block has the updated Requirements line (4 IDs), 6 success-criteria bullets, and a concrete Plans line. No other phase blocks were touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add handler-level testing pattern section to `pmcp-book/src/ch15-testing.md`</name>
  <files>pmcp-book/src/ch15-testing.md</files>
  <read_first>
    - pmcp-book/src/ch15-testing.md — full chapter outline (existing sections via `awk '/^##/||/^###/'`); identify the best insertion point. Recommended: insert a new top-level section `## Handler-Level Testing Pattern (In-Process)` between `## Testing Philosophy` (line 15) and `## Official MCP Inspector` (line 48) since handler-level testing is foundational and should appear before the external-tooling sections. Confirm by reading lines 1-60.
    - pmcp-book/src/SUMMARY.md line 42 (the existing entry for ch15 — confirms the chapter file path; no SUMMARY.md change needed since this is a same-file addition, not a new chapter)
    - .planning/phases/82-builder-dx-prerequisites/82-CONTEXT.md §decisions D-02 part (c) and D-03 (book-section requirements: must contain the same five literal callout tokens — `auth_provider`, `tool_authorizer`, `tool_middleware`, `pmcp::Client`, `stdio` or `streamable-http`)
    - .planning/phases/82-builder-dx-prerequisites/82-CONTEXT.md §specifics ("Make sure this distinction is loud in the book section") — the book section must be more verbose than the doctest in framing the production vs. testing distinction
  </read_first>
  <behavior>
    - Behavior 1: A new section titled exactly `## Handler-Level Testing Pattern (In-Process)` exists in `pmcp-book/src/ch15-testing.md`.
    - Behavior 2: The section contains a copy-paste-ready Rust code example (fenced ` ```rust` block) that builds a server with `tool_arc`, retrieves via `get_tool`, calls `handle(args, RequestHandlerExtra::default()).await`, and asserts on the result.
    - Behavior 3: The section's prose contains a "What this pattern skips" subsection (e.g., `### When NOT to use this pattern`) that literally names `auth_provider`, `tool_authorizer`, `tool_middleware`, and points readers at `pmcp::Client` over `stdio` or `streamable-http` for full-pipeline tests.
    - Behavior 4: The section explicitly states that in **production**, real MCP clients hit `Server::handle_request` and the full auth/authorizer/middleware pipeline runs — the handler-level pattern is a deliberate *testing* shortcut (CONTEXT.md §specifics).
    - Behavior 5: The section references both `tool_arc` and `prompt_arc` so toolkit authors find the pattern under either keyword search.
  </behavior>
  <action>
    Edit `pmcp-book/src/ch15-testing.md` to add a new section.

    **Insertion point**: between the existing `## Testing Philosophy` section (which ends before line 48 at the start of `## Official MCP Inspector`) and the `## Official MCP Inspector` section. Pick the precise line by reading the file and locating the blank line before `## Official MCP Inspector`.

    **New section structure** (use this skeleton — fill in concrete prose; the only mechanical requirements are the literal D-03 tokens and the production-vs-testing distinction):

    ```
    ## Handler-Level Testing Pattern (In-Process)

    When you author an MCP server with `pmcp`, integration tests don't have to spin up a transport, a JSONRPC parser, and a full request-dispatch pipeline. The SDK exposes two accessors — `Server::get_tool(name)` and `Server::get_prompt(name)` — that hand you back the registered `Arc<dyn Handler>` directly, so you can invoke `handler.handle(args, RequestHandlerExtra::default()).await` and assert on the result.

    This is the pattern external toolkit authors should reach for first: it's fast, deterministic, and isolates the handler logic from anything the transport stack might do.

    ### Example

    \`\`\`rust
    // (Concrete example: build with .tool_arc("echo", Arc::new(EchoTool))
    //                    .prompt_arc("greet", Arc::new(GreetPrompt))
    //                    .build()?;
    //  Then server.get_tool("echo").unwrap().handle(json!({"msg": "hi"}),
    //                                                RequestHandlerExtra::default()).await?;
    //  Assert on the returned Value.
    //  Symmetric for get_prompt.)
    \`\`\`

    See `tests/in_process_handler_pattern.rs` in the `rust-mcp-sdk` repo for the executable reference.

    ### What this pattern skips

    This pattern exercises **only** the handler implementation. The JSONRPC dispatch path (`Server::handle_request`) is bypassed, so the following do **not** run:

    - `auth_provider` — no authentication check is performed; the test runs as an unauthenticated caller.
    - `tool_authorizer` — no per-tool authorization (e.g., scope checks via `ScopeBasedAuthorizer`) is invoked.
    - `tool_middleware` — no middleware chain (logging, metrics, OAuth token injection, etc.) runs around the handler.

    In **production**, real MCP clients hit `Server::handle_request` over a transport (stdio, streamable-http, websocket, SSE), and the full pipeline runs every request. The handler-level pattern is a deliberate testing shortcut to keep handler unit/integration tests fast and self-contained.

    ### When to use a full transport instead

    If your integration test needs to verify the full security pipeline (auth, authorization, middleware), drive a real transport with `pmcp::Client`:

    - **stdio** — pair `Server::run_stdio()` with `pmcp::Client` over `StdioTransport`.
    - **streamable-http** — boot the server with `pmcp::StreamableHttpServer` and drive it with `pmcp::Client::connect_streamable_http(...)`.

    Use whichever transport matches your deployment target. Full-pipeline tests are slower and have more moving parts; reach for them when the test is specifically about the pipeline, not the handler logic.

    ### Pattern summary

    | Step | Code |
    |------|------|
    | Register handler with an `Arc` | `.tool_arc(name, Arc::new(MyTool))` or `.prompt_arc(name, Arc::new(MyPrompt))` |
    | Build the server | `.build()?` |
    | Retrieve the handler | `server.get_tool(name)` or `server.get_prompt(name)` |
    | Invoke at handler level | `handler.handle(args, RequestHandlerExtra::default()).await?` |
    | Assert on result | `assert_eq!(...)` against `serde_json::Value` (tools) or `GetPromptResult` (prompts) |
    ```

    Implements CONTEXT.md D-02 part (c). Implements D-03 (the explicit "what this skips" callout in book form). Implements the §specifics requirement that the production/testing distinction be "loud".

    Do NOT modify any other section of `ch15-testing.md`. Do NOT modify `SUMMARY.md` (no new chapter, just a section addition within the existing chapter).
  </action>
  <verify>
    <automated>awk '/^## Handler-Level Testing Pattern \(In-Process\)/,/^## /' pmcp-book/src/ch15-testing.md | grep -c -E 'auth_provider|tool_authorizer|tool_middleware|pmcp::Client|stdio|streamable-http'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^## Handler-Level Testing Pattern (In-Process)$' pmcp-book/src/ch15-testing.md` returns exactly 1.
    - Source assertion: within the new section (delimited by `awk '/^## Handler-Level Testing Pattern/,/^## /'`), all five literal tokens appear: `auth_provider`, `tool_authorizer`, `tool_middleware`, `pmcp::Client`, and at least one of `stdio`/`streamable-http`. Concretely: the awk-piped grep above returns ≥5 distinct matches.
    - Source assertion: the section references both `tool_arc` and `prompt_arc` (toolkit authors find it under either search).
    - Source assertion: the section explicitly contains the substring "production" AND "testing shortcut" (CONTEXT.md §specifics — production-vs-testing distinction "loud").
    - Source assertion: the section references `tests/in_process_handler_pattern.rs` (links the book to Plan 02's regression anchor).
    - Build sanity: if `mdbook` is installed locally, `cd pmcp-book && mdbook build` exits 0. If not installed, skip (CI builds the book; local mdbook is optional per the workspace conventions).
    - Negative assertion: no other section of `ch15-testing.md` is touched. `git diff pmcp-book/src/ch15-testing.md` shows only additions, no deletions outside the insertion point.
    - Negative assertion: `pmcp-book/src/SUMMARY.md` is unchanged (`git diff pmcp-book/src/SUMMARY.md` is empty).
  </acceptance_criteria>
  <done>The new `## Handler-Level Testing Pattern (In-Process)` section exists in ch15-testing.md, contains the D-03 callout (all five literal tokens), references both `_arc` registration methods, points readers at the integration test file, and makes the production-vs-testing distinction explicit. No other book file is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. No runtime code, no public API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-05 | T (Tampering — informational) | Book section + REQUIREMENTS.md BLDR-03 wording | mitigate | The D-03 concrete-wording callout in both the book section and the doctests (Plan 01 Task 4) explicitly names `auth_provider`, `tool_authorizer`, `tool_middleware` as bypassed by the handler-level pattern. This pre-empts the future support ticket where a toolkit author writes an integration test that "looks secure" but actually skips the security pipeline. The traceability between REQUIREMENTS.md BLDR-03 and the doctests/book section ensures the callout cannot drift silently from the requirement. |

**No new attack surface.** This plan edits planning documents and one book chapter. STRIDE categories S/R/I/D/E do not apply. The lone T-class consideration (informational tampering — i.e., users drawing incorrect security conclusions from incomplete documentation) is mitigated by the literal-token D-03 callout enforced by source assertions in both this plan and Plan 01.
</threat_model>

<verification>
1. `grep -E '^\- \[ \] \*\*BLDR-04\*\*:' .planning/REQUIREMENTS.md` exits 0.
2. `grep -E '^\*\*Requirements\*\*: BLDR-01, BLDR-02, BLDR-03, BLDR-04$' .planning/ROADMAP.md` exits 0.
3. `grep -c '^## Handler-Level Testing Pattern (In-Process)$' pmcp-book/src/ch15-testing.md` returns 1.
4. The Phase 82 block in ROADMAP.md has exactly 6 numbered success-criteria bullets.
5. `git diff .planning/REQUIREMENTS.md .planning/ROADMAP.md pmcp-book/src/ch15-testing.md` shows targeted changes only (no neighbouring damage).
6. `make quality-gate` exits 0 (documentation changes never fail the gate, but run it for completeness).
7. If `mdbook` is installed: `cd pmcp-book && mdbook build` exits 0 (the new section is well-formed markdown).
</verification>

<success_criteria>
- REQUIREMENTS.md: BLDR-03 reworded per D-08, BLDR-04 added per D-06, traceability row added.
- ROADMAP.md: Phase 82 Requirements line lists all 4 IDs, Success Criteria has 6 bullets (4 original + 2 new per D-06 and D-07), Plans line concrete (no longer "TBD").
- pmcp-book/src/ch15-testing.md: new `## Handler-Level Testing Pattern (In-Process)` section with the D-03 concrete callout, both `_arc` references, and the production-vs-testing distinction.
- No neighbouring-section damage anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/82-builder-dx-prerequisites/82-03-SUMMARY.md` summarizing:
  - The three exact edits to REQUIREMENTS.md (line ranges + content).
  - The three exact edits to ROADMAP.md (line ranges + content).
  - The one new book section (title, insertion point, the five literal D-03 tokens it contains).
  - Confirmation that BLDR-03 wording now explicitly mentions `Server::get_tool` accessor symmetry (D-08).
  - Confirmation that BLDR-04 covers all four extra `_arc` methods (D-06).
  - The requirement IDs this plan contributes to: BLDR-03 (book-section portion of D-02 part (c) + reword per D-08), BLDR-04 (full requirement-spec creation per D-06).
</output>
