---
phase: 103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas
plan: 06
type: execute
wave: 4
depends_on: [103-01, 103-02, 103-04, 103-05]
files_modified:
  - Cargo.toml
  - CHANGELOG.md
autonomous: true
requirements: [WEBCH-08, WEBCH-09]
must_haves:
  truths:
    - "pmcp version is bumped to 2.11.0 (minor — additive public API: PKCE helper + functional WasmHttpTransport)"
    - "CHANGELOG.md documents the new PKCE helper and the repaired WasmHttpTransport under a 2.11.0 entry"
    - "Both make quality-gate AND make wasm-build pass (SC-4: native gate + wasm build)"
    - "Non-wasm builds are NOT regressed and the wasm boundary is preserved"
  artifacts:
    - path: "Cargo.toml"
      provides: "pmcp version 2.11.0"
      contains: "version = \"2.11.0\""
    - path: "CHANGELOG.md"
      provides: "2.11.0 entry documenting PKCE helper + WasmHttpTransport fix"
      contains: "2.11.0"
  key_links:
    - from: "Cargo.toml version"
      to: "CHANGELOG.md 2.11.0 entry"
      via: "release version bump"
      pattern: "2.11.0"
---

<objective>
Cut the new pmcp release for the two reusable public-API pieces (D-02 PKCE helper + D-08 WasmHttpTransport
fix) and run the full phase verification gate: BOTH make quality-gate AND make wasm-build (SC-4). This is
the final wave — it depends on every prior plan and proves the WASM boundary is preserved with no non-wasm
regression.

Purpose: D-02 and D-08 add public pmcp API, so per CLAUDE.md Release & Publish Workflow the phase must
include a version bump + CHANGELOG entry. RESEARCH "Release Impact" specifies a minor bump 2.10.0 -> 2.11.0
(additive). Critically, make quality-gate does NOT build wasm (Pitfall 5) — SC-4 requires BOTH gates, so
the phase verification must run wasm-build explicitly.
Output: pmcp Cargo.toml version 2.11.0, a CHANGELOG.md 2.11.0 entry, and a green BOTH-gates verification.
NOTE: this plan performs the version bump + CHANGELOG only; tagging/publishing is a separate release-branch
action per CLAUDE.md (NOT done here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-CONTEXT.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md

<interfaces>
<!-- Release Impact (RESEARCH §Release Impact): pmcp 2.10.0 -> 2.11.0 (minor, additive).
     New public API: pmcp::shared::pkce::{generate_code_verifier, code_challenge_s256, generate_state}
     + the now-functional WasmHttpTransport (behavior change, symbol already exported lib.rs:139). -->
<!-- Downstream pins (RESEARCH): mcp-tester pins pmcp = "2.8.1"; cargo-pmcp pins pmcp. Per CLAUDE.md,
     downstream crates need bumping ONLY IF their pin is updated to consume the new API. For a pure
     additive release they MAY stay; decide at release time (this plan documents, does not force). -->
<!-- CHANGELOG format: Keep a Changelog; latest entry header "## [2.10.0] - 2026-06-21". -->
<!-- make quality-gate (Makefile:660-679): fmt/lint/build/test/audit/check-unwraps/validate-always/purity.
     make wasm-build (Makefile:59): cargo build --target wasm32-unknown-unknown --no-default-features --features wasm. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump pmcp to 2.11.0 + add CHANGELOG entry</name>
  <read_first>
    - Cargo.toml:1-5 (root pmcp version = "2.10.0")
    - CHANGELOG.md:1-40 (header + the latest "## [2.10.0]" entry format to mirror)
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md (Release Impact: minor bump rationale, public API surface, downstream pin guidance)
    - CLAUDE.md (Release & Publish Workflow: version bump rules; downstream crates pinning a bumped dep must also be bumped IF the pin is updated)
  </read_first>
  <action>
    Bump the root Cargo.toml pmcp `version` from 2.10.0 to 2.11.0 (minor — additive per RESEARCH Release
    Impact). Add a new CHANGELOG.md entry "## [2.11.0] - 2026-06-30" ABOVE the 2.10.0 entry, mirroring the
    Keep-a-Changelog format, with an "### Added" section documenting: (a) the wasm-safe PKCE crypto helper
    (pmcp::shared::pkce::{generate_code_verifier, code_challenge_s256, generate_state}) — target-agnostic,
    getrandom/sha2/base64-backed, RFC 7636 (D-02/D-03); and a "### Fixed" section documenting (b) the
    WasmHttpTransport Transport impl now correlates send()/receive() (one-slot response buffer) so the
    high-level Client and its typed task helpers work over browser Fetch (D-08). Reference the new
    examples/web-channel-client reference example. Do NOT bump mcp-tester or cargo-pmcp versions or their pmcp
    pins UNLESS they are updated to consume the new API (RESEARCH: pure additive release means they MAY stay);
    record the decision in the SUMMARY. Do NOT tag or publish — that is a separate release-branch action per
    CLAUDE.md.
  </action>
  <verify>
    <automated>grep -m1 '^version' Cargo.toml; grep -c "2.11.0" CHANGELOG.md; cargo build -p pmcp --features full 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -m1 '^version' Cargo.toml` shows `version = "2.11.0"`
    - CHANGELOG.md contains a `## [2.11.0]` entry positioned above `## [2.10.0]`
    - The 2.11.0 entry documents BOTH the PKCE helper (Added) and the WasmHttpTransport fix (Fixed)
    - `cargo build -p pmcp --features full` exits 0 with the new version
    - No tag is created and nothing is published (release-branch action deferred per CLAUDE.md)
  </acceptance_criteria>
  <done>pmcp is at 2.11.0 with a CHANGELOG entry documenting the two new public-API pieces; no publish performed.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase verification — make quality-gate AND make wasm-build (SC-4)</name>
  <read_first>
    - Makefile:57-67 (wasm-build / wasm-release targets), :660-679 (quality-gate composition)
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md (Pitfall 5: quality-gate does NOT build wasm — BOTH are required)
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-VALIDATION.md (Phase gate: quality-gate green + wasm-build/wasm-release green)
    - CLAUDE.md (quality gate must pass before commit/PR; cognitive complexity <= 25; zero SATD; no unwrap/expect in src)
  </read_first>
  <action>
    Run the full phase verification gate. First `make quality-gate` (fmt-check, lint with pedantic+nursery,
    build, test-all, audit, unused-deps, check-todos, check-unwraps, validate-always, purity-check) — fix any
    failure (notably: no unwrap/expect in the new src/ pkce module per Pitfall 7; cognitive complexity <= 25 in
    the demo server tool closure + background updater; zero SATD). Then `make wasm-build` (and `make wasm-release`)
    to prove the wasm target compiles with the PKCE helper + fixed transport + example (Pitfall 5 — wasm is NOT
    in quality-gate, so SC-4 requires this explicitly). Confirm non-wasm is not regressed (quality-gate's host
    build/test). Record the BOTH-green result in the SUMMARY. If wasm-build fails on getrandom, apply the
    103-SPIKE getrandom cfg verdict (the .cargo/config.toml from plan 05) and re-run. Do NOT weaken or skip any
    gate check.
  </action>
  <verify>
    <automated>make quality-gate 2>&1 | tail -25 && make wasm-build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gate` exits 0 (all checks: fmt/lint/build/test/audit/check-unwraps/validate-always/purity)
    - `make wasm-build` exits 0 (wasm target compiles with the new PKCE module + fixed transport + example)
    - No unwrap/expect introduced in src/ (check-unwraps passes); no SATD; cognitive complexity <= 25 (PMAT/lint clean)
    - examples/wasm-client/* unchanged (LOCKED fence verified by git diff)
    - tests/tool_as_task_lifecycle_http.rs and src/server/task_dispatch.rs unchanged (frozen tasks/* contract)
  </acceptance_criteria>
  <done>BOTH make quality-gate AND make wasm-build pass — SC-4 satisfied, wasm boundary preserved, no non-wasm regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release artifact → crates.io consumers | The new public API (PKCE helper + WasmHttpTransport) becomes consumable; version semantics must be correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-SEMVER | Tampering | pmcp version bump | mitigate | Minor bump (2.10.0 -> 2.11.0) reflects purely additive public API (RESEARCH Release Impact); no breaking change introduced, so downstream pins remain valid |
| T-103-GATE | Repudiation | quality/wasm gates | mitigate | BOTH make quality-gate AND make wasm-build run before phase close (Pitfall 5); no gate weakened — the check-unwraps/SATD/complexity gates stay enforced |
| T-103-SC | Tampering | crate installs | accept | No new external packages added this phase; release ships only in-tree-dep code |
</threat_model>

<verification>
- `grep -m1 '^version' Cargo.toml` == `version = "2.11.0"`
- CHANGELOG.md has a 2.11.0 entry above 2.10.0 documenting PKCE helper + WasmHttpTransport fix
- `make quality-gate` exits 0
- `make wasm-build` exits 0
- LOCKED fences intact: examples/wasm-client/* unchanged; frozen tasks/* contract files unchanged
</verification>

<success_criteria>
pmcp 2.11.0 ships the two reusable public-API pieces with a CHANGELOG entry, and BOTH the native quality
gate and the wasm build pass — the WASM boundary is preserved with no non-wasm regression. (SC-4; WEBCH-08/09.)
</success_criteria>

<output>
Create `.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-06-SUMMARY.md` when done
</output>
