---
phase: 103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas
plan: 06
type: execute
wave: 4
depends_on: [103-01, 103-02, 103-04, 103-05]
files_modified:
  - Cargo.toml
  - CHANGELOG.md
autonomous: false
requirements: [WEBCH-08, WEBCH-09]
must_haves:
  truths:
    - "pmcp version is bumped to 2.11.0 (minor — additive public API: PKCE helper + functional WasmHttpTransport)"
    - "CHANGELOG.md documents the new PKCE helper and the repaired WasmHttpTransport under a 2.11.0 entry"
    - "The developer has explicitly decided-and-recorded whether mcp-tester/cargo-pmcp pmcp pins need bumping BEFORE the release branch is cut"
    - "Both make quality-gate AND make wasm-build (and make wasm-release) pass (SC-4: native gate + wasm build/release)"
    - "The split example (client/ wasm crate + server/ native crate) does NOT regress either gate — the wasm build stays free of native deps (HIGH-2)"
    - "Non-wasm builds are NOT regressed and the wasm boundary is preserved"
  artifacts:
    - path: "Cargo.toml"
      provides: "pmcp version 2.11.0"
      contains: "version = \"2.11.0\""
    - path: "CHANGELOG.md"
      provides: "2.11.0 entry documenting PKCE helper + WasmHttpTransport fix"
      contains: "2.11.0"
  key_links:
    - from: "Cargo.toml version"
      to: "CHANGELOG.md 2.11.0 entry"
      via: "release version bump"
      pattern: "2.11.0"
---

<objective>
Cut the new pmcp release for the two reusable public-API pieces (D-02 PKCE helper + D-08 WasmHttpTransport
fix) and run the full phase verification gate: BOTH make quality-gate AND make wasm-build / wasm-release
(SC-4). This is the final wave — it depends on every prior plan and proves the WASM boundary is preserved
with no non-wasm regression, including that the split example (client/ wasm + server/ native crates — HIGH-2)
keeps the wasm build free of native deps. It also forces an explicit, recorded downstream-pin decision
(mcp-tester/cargo-pmcp) before the release branch is cut, per CLAUDE.md Release & Publish Workflow.

Purpose: D-02 and D-08 add public pmcp API, so per CLAUDE.md Release & Publish Workflow the phase must
include a version bump + CHANGELOG entry. RESEARCH "Release Impact" specifies a minor bump 2.10.0 -> 2.11.0
(additive). Critically, make quality-gate does NOT build wasm (Pitfall 5) — SC-4 requires BOTH gates, so
the phase verification must run wasm-build (and wasm-release) explicitly. CLAUDE.md also says downstream
crates that pin a bumped dep must also be bumped IF their pin is updated — for a purely additive release they
MAY stay, but that call must be made and RECORDED (not left implicit) before release. This plan is
`autonomous: false` because of the Task 2 blocking checkpoint; the prior plans (01-05) are autonomous, so the
phase is autonomous up to this final gate — the workflow tooling must understand this single blocking
checkpoint sits at the end (reviewer note acknowledged).
Output: pmcp Cargo.toml version 2.11.0, a CHANGELOG.md 2.11.0 entry, a recorded downstream-pin decision, and
a green BOTH-gates verification.
NOTE: this plan performs the version bump + CHANGELOG only; tagging/publishing is a separate release-branch
action per CLAUDE.md (NOT done here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-CONTEXT.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md

<interfaces>
<!-- Release Impact (RESEARCH §Release Impact): pmcp 2.10.0 -> 2.11.0 (minor, additive).
     New public API: pmcp::shared::pkce::{generate_code_verifier, code_challenge_s256, generate_state}
     + the now-functional WasmHttpTransport (behavior change, symbol already exported lib.rs:139).
     NOTE: plan 01 also relocates getrandom into the cross-target [dependencies] table (HIGH-1) — a
     manifest change, not a new public symbol; mention it in the CHANGELOG if noting dependency hygiene. -->
<!-- Downstream pins (RESEARCH): mcp-tester pins pmcp = "2.8.1"; cargo-pmcp pins pmcp. Per CLAUDE.md,
     downstream crates need bumping ONLY IF their pin is updated to consume the new API. For a pure
     additive release they MAY stay; the decision MUST be made and recorded (Task 2 checkpoint). -->
<!-- CHANGELOG format: Keep a Changelog; latest entry header "## [2.10.0] - 2026-06-21". -->
<!-- make quality-gate (Makefile:660-679): fmt/lint/build/test/audit/check-unwraps/validate-always/purity.
     make wasm-build (Makefile:59): cargo build --target wasm32-unknown-unknown --no-default-features --features wasm.
     make wasm-release (Makefile:57-67): the release-profile wasm build — run it too if it is part of phase validation. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump pmcp to 2.11.0 + add CHANGELOG entry</name>
  <read_first>
    - Cargo.toml:1-5 (root pmcp version = "2.10.0")
    - CHANGELOG.md:1-40 (header + the latest "## [2.10.0]" entry format to mirror)
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md (Release Impact: minor bump rationale, public API surface, downstream pin guidance)
    - CLAUDE.md (Release & Publish Workflow: version bump rules; downstream crates pinning a bumped dep must also be bumped IF the pin is updated)
  </read_first>
  <action>
    Bump the root Cargo.toml pmcp `version` from 2.10.0 to 2.11.0 (minor — additive per RESEARCH Release
    Impact). Add a new CHANGELOG.md entry "## [2.11.0] - 2026-06-30" ABOVE the 2.10.0 entry, mirroring the
    Keep-a-Changelog format, with an "### Added" section documenting: (a) the wasm-safe PKCE crypto helper
    (pmcp::shared::pkce::{generate_code_verifier, code_challenge_s256, generate_state}) — target-agnostic,
    getrandom/sha2/base64-backed, RFC 7636 (D-02/D-03); and a "### Fixed" section documenting (b) the
    WasmHttpTransport Transport impl now correlates send()/receive() (one-slot response buffer, with a
    double-send guard) so the high-level Client and its typed task helpers work over browser Fetch (D-08).
    Optionally note under "### Changed" that getrandom was relocated to the cross-target [dependencies] table so
    the PKCE helper links on host (HIGH-1, dependency hygiene). Reference the new examples/web-channel-client
    reference example (client/ wasm + server/ native split). Do NOT bump mcp-tester or cargo-pmcp here — that
    decision is made and recorded in Task 2. Do NOT tag or publish — that is a separate release-branch action per
    CLAUDE.md.
  </action>
  <verify>
    <automated>grep -m1 '^version' Cargo.toml; grep -c "2.11.0" CHANGELOG.md; cargo build -p pmcp --features full 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -m1 '^version' Cargo.toml` shows `version = "2.11.0"`
    - CHANGELOG.md contains a `## [2.11.0]` entry positioned above `## [2.10.0]`
    - The 2.11.0 entry documents BOTH the PKCE helper (Added) and the WasmHttpTransport fix (Fixed)
    - `cargo build -p pmcp --features full` exits 0 with the new version
    - No tag is created and nothing is published (release-branch action deferred per CLAUDE.md)
  </acceptance_criteria>
  <done>pmcp is at 2.11.0 with a CHANGELOG entry documenting the two new public-API pieces; no publish performed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Decide-and-record downstream pmcp pin bumps (mcp-tester / cargo-pmcp) before release branch</name>
  <what-built>
    The pmcp version is at 2.11.0 with a documented CHANGELOG entry (Task 1). Per CLAUDE.md Release & Publish
    Workflow, downstream crates that pin a bumped dep must also be bumped IF their pin is updated to consume the
    new API. The 2.10.0 -> 2.11.0 bump is purely additive (PKCE helper + WasmHttpTransport fix), so
    mcp-tester (pins pmcp = "2.8.1") and cargo-pmcp (pins pmcp) MAY stay — but this call must be made and recorded
    BEFORE the release branch is cut, not left implicit. Claude has gathered the current pins for you below.
  </what-built>
  <action>
    PAUSE for the developer. Surface the current downstream pins (mcp-tester pins pmcp; cargo-pmcp pins pmcp)
    and force an explicit decision: KEEP all pins (pure additive release, downstream does not yet consume the
    new PKCE helper / WasmHttpTransport API) OR BUMP the named downstream crates (their pin is updated to
    consume the new API, so per CLAUDE.md they also get a version bump). Record the verdict so Task 3 / the
    release branch acts on an explicit decision, not an implicit one. Do not auto-decide — this is a blocking
    human-verify gate.
  </action>
  <how-to-verify>
    1. Confirm the current downstream pins:
       `grep -n 'pmcp' crates/mcp-tester/Cargo.toml | grep -i version`
       `grep -rn 'pmcp' cargo-pmcp/Cargo.toml | grep -i version`
    2. Decide and STATE explicitly, for THIS release:
       - Do mcp-tester / cargo-pmcp need their `pmcp` pin bumped (i.e., are they being updated to consume the
         new PKCE helper / WasmHttpTransport API in this release)?  → if YES, they must also get a version bump
         per CLAUDE.md, and that work is added before the release branch.
       - Or do they stay as-is because this is a pure additive release they do not yet consume?  → record the
         rationale.
    3. Record the verdict (BUMP which crates / KEEP all pins) so the release-branch step has an explicit,
       non-implicit decision to act on. Claude will write your verdict into the 103-06-SUMMARY.
  </how-to-verify>
  <resume-signal>Reply "keep pins" (no downstream bump this release) OR "bump: &lt;crate list&gt;" with the crates whose pmcp pin/version must move, then Claude records the decision and proceeds.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full phase verification — make quality-gate AND make wasm-build / wasm-release (SC-4)</name>
  <read_first>
    - Makefile:57-67 (wasm-build / wasm-release targets), :660-679 (quality-gate composition)
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md (Pitfall 5: quality-gate does NOT build wasm — BOTH are required)
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-VALIDATION.md (Phase gate: quality-gate green + wasm-build/wasm-release green)
    - CLAUDE.md (quality gate must pass before commit/PR; cognitive complexity <= 25; zero SATD; no unwrap/expect in src)
  </read_first>
  <action>
    Run the full phase verification gate. First `make quality-gate` (fmt-check, lint with pedantic+nursery,
    build, test-all, audit, unused-deps, check-todos, check-unwraps, validate-always, purity-check) — fix any
    failure (notably: no unwrap/expect in the new src/ pkce module per Pitfall 7; cognitive complexity <= 25 in
    the demo server tool closure + background updater; zero SATD). Then `make wasm-build` AND `make wasm-release`
    to prove the wasm target compiles (release profile too) with the PKCE helper + fixed transport + the split
    example client crate (Pitfall 5 — wasm is NOT in quality-gate, so SC-4 requires this explicitly; the
    reviewer flagged that acceptance must actually exercise wasm-release, not just wasm-build). Confirm the split
    example keeps the wasm build free of native deps (HIGH-2): the client/ cdylib crate must NOT pull
    hyper/tokio. Apply the Task 2 downstream-pin verdict: if the developer chose to bump mcp-tester/cargo-pmcp,
    make those version+pin edits now and re-run quality-gate; if they chose "keep pins", record that in the
    SUMMARY and make no downstream edits. Confirm non-wasm is not regressed (quality-gate's host build/test).
    Record the BOTH-green result + the downstream-pin verdict in the SUMMARY. If wasm-build fails on getrandom,
    apply the 103-SPIKE getrandom cfg verdict (the .cargo/config.toml in the plan-05 client crate) and re-run.
    Do NOT weaken or skip any gate check.
  </action>
  <verify>
    <automated>make quality-gate 2>&1 | tail -25 && make wasm-build 2>&1 | tail -15 && make wasm-release 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gate` exits 0 (all checks: fmt/lint/build/test/audit/check-unwraps/validate-always/purity)
    - `make wasm-build` exits 0 (wasm target compiles with the new PKCE module + fixed transport + split example client crate)
    - `make wasm-release` exits 0 (release-profile wasm build — reviewer note: acceptance must exercise wasm-release, not only wasm-build)
    - The Task 2 downstream-pin verdict is applied (downstream bumps made if "bump", none if "keep pins") and recorded in the SUMMARY
    - No unwrap/expect introduced in src/ (check-unwraps passes); no SATD; cognitive complexity <= 25 (PMAT/lint clean)
    - The split example wasm client crate pulls NO native HTTP deps (HIGH-2)
    - examples/wasm-client/* unchanged (LOCKED fence verified by git diff)
    - tests/tool_as_task_lifecycle_http.rs and src/server/task_dispatch.rs unchanged (frozen tasks/* contract)
  </acceptance_criteria>
  <done>BOTH make quality-gate AND make wasm-build/wasm-release pass — SC-4 satisfied, downstream-pin verdict applied + recorded, wasm boundary preserved (split example free of native deps), no non-wasm regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release artifact → crates.io consumers | The new public API (PKCE helper + WasmHttpTransport) becomes consumable; version semantics must be correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-SEMVER | Tampering | pmcp version bump | mitigate | Minor bump (2.10.0 -> 2.11.0) reflects purely additive public API (RESEARCH Release Impact); no breaking change introduced, so downstream pins remain valid — and the downstream-pin call is made explicit via the Task 2 blocking checkpoint, not left implicit |
| T-103-GATE | Repudiation | quality/wasm gates | mitigate | BOTH make quality-gate AND make wasm-build/wasm-release run before phase close (Pitfall 5); no gate weakened — the check-unwraps/SATD/complexity gates stay enforced |
| T-103-SC | Tampering | crate installs | accept | No new external packages added this phase; getrandom is only relocated within the manifest (HIGH-1); release ships only in-tree-dep code |
</threat_model>

<verification>
- `grep -m1 '^version' Cargo.toml` == `version = "2.11.0"`
- CHANGELOG.md has a 2.11.0 entry above 2.10.0 documenting PKCE helper + WasmHttpTransport fix
- Downstream-pin verdict (keep/bump) recorded in 103-06-SUMMARY before the release branch
- `make quality-gate` exits 0
- `make wasm-build` AND `make wasm-release` exit 0
- LOCKED fences intact: examples/wasm-client/* unchanged; frozen tasks/* contract files unchanged
</verification>

<success_criteria>
pmcp 2.11.0 ships the two reusable public-API pieces with a CHANGELOG entry, the downstream-pin decision is
made explicitly and recorded before the release branch, and BOTH the native quality gate AND the wasm
build/release pass — the WASM boundary is preserved with no non-wasm regression and the split example keeps
the wasm build free of native deps. (SC-4; WEBCH-08/09.)
</success_criteria>

<output>
Create `.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-06-SUMMARY.md` when done
</output>
</content>
</invoke>
