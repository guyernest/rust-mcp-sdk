---
phase: 103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md
  - tests/web_channel_oauth_route_merge_spike.rs
  - examples/web-channel-client/server/.cargo/config.toml
autonomous: true
requirements: [WEBCH-04, WEBCH-05, WEBCH-07]
must_haves:
  truths:
    - "The mechanism to serve /oauth2/authorize + /oauth2/token alongside the MCP router on ONE origin is PROVEN against the real public StreamableHttpServer API — the EXACT public builder/method (or merge seam) is named, OR the spike escalates that an SDK change is required and adds that task (HIGH-3, Open Question 1)"
    - "A DURABLE artifact survives the spike — a minimized committed test (tests/web_channel_oauth_route_merge_spike.rs) or helper proving the route-merge + owner resolution, NOT just prose in 103-SPIKE.md (HIGH-3)"
    - "The AuthContext.subject value from the bundled IdP bearer is known, and whether store.list(owner) finds the create-path-minted task is confirmed (Open Question 2)"
    - "The chosen example PACKAGE SPLIT (separate wasm-client crate + native server crate) is validated to keep the wasm build free of native HTTP deps — feature unification cannot pull hyper/tokio into the cdylib (HIGH-2, SC-4)"
    - "Whether the wasm build needs --cfg getrandom_backend wasm_js is determined empirically (Open Question 4 / Pitfall 2)"
  artifacts:
    - path: ".planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md"
      provides: "Decisions for IdP route-merge (exact public API), package-split layout, owner resolution, getrandom backend cfg — consumed by plans 04 and 05"
      contains: "Open Question 1"
    - path: "tests/web_channel_oauth_route_merge_spike.rs"
      provides: "DURABLE proof: an #[ignore]/runnable test standing up the merged MCP+OAuth router on one origin via the named public API, asserting both endpoints respond (survives the spike — HIGH-3)"
      contains: "oauth2"
  key_links:
    - from: "103-SPIKE.md"
      to: "103-04-PLAN demo server"
      via: "IdP route-merge (named public API) + package-split + owner-resolution decisions"
      pattern: "route"
    - from: "tests/web_channel_oauth_route_merge_spike.rs"
      to: "the merged MCP+OAuth router"
      via: "durable committed test proving route composition on one origin"
      pattern: "oauth2"
    - from: "103-SPIKE.md"
      to: "103-05-PLAN example wasm build"
      via: "getrandom backend cfg + package-split decision"
      pattern: "getrandom"
---

<objective>
Resolve the highest-risk unknowns BEFORE the demo server (plan 04) and the browser example
(plan 05) are built, and record the decisions in `103-SPIKE.md` — PLUS leave a DURABLE committed
artifact proving the route-merge so the result cannot silently regress (HIGH-3). This de-risks the
MEDIUM-confidence seams (IdP HTTP route-merge AGAINST THE REAL PUBLIC API, the native/wasm example
PACKAGE SPLIT, background-updater owner resolution) and the wasm build gotcha (getrandom backend cfg)
so the downstream plans implement against decided, proven facts, not assumptions.

Purpose: RESEARCH flagged Open Questions 1 (merge OAuth2 IdP routes with the MCP router on one origin to
avoid CORS), 2 (does AuthContext.subject from the bundled IdP bearer match what store.list(owner) sees
for the D-05 background updater), and Pitfall 2/A2 (getrandom 0.4 on wasm32 may need the backend cfg).
The cross-AI review (HIGH-3) additionally requires the route-merge to be PROVEN implementable against the
real public `StreamableHttpServer` API — naming the exact builder/method, or escalating that it needs an
SDK change (and adding that task) — and to leave a durable test/helper, not just prose. The review (HIGH-2)
also requires validating the example PACKAGE SPLIT (separate wasm + native crates) so per-package feature
unification can't pull native HTTP stacks into the wasm cdylib. Each blocks a specific downstream task.
Output: 103-SPIKE.md with a decision per question (including the NAMED public route-merge API and the
package-split layout), a DURABLE committed test `tests/web_channel_oauth_route_merge_spike.rs` proving the
route composition, plus (only if the wasm build proves it necessary)
examples/web-channel-client/server/.cargo/config.toml — note: the getrandom cfg for the WASM cdylib, if
needed, belongs to the wasm client crate's .cargo/config.toml created by plan 05; this plan only records the
verdict and, if a native-side cfg is somehow required, places the server-crate config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-CONTEXT.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-PATTERNS.md

<interfaces>
<!-- IdP route-merge candidates (RESEARCH Open Question 1):
     (a) second axum listener for /oauth2/* on the same host (CORS implications),
     (b) compose ONE Router via the lower-level pmcp::axum builder (build_mcp_router, streamable_http_server.rs:286) — PREFERRED, single origin avoids CORS,
     (c) two ports (CORS).
     HIGH-3: the spike MUST confirm whether the merge seam (e.g. build_mcp_router, or any axum::Router-returning
     builder) is PUBLICLY exposed from `pmcp` (not a pub(crate) internal). If it is NOT public, "merge onto one
     origin" needs an SDK-internal change — escalate and add an explicit SDK task (make the router builder pub, or
     add a documented compose seam) rather than silently assuming plan 04 can do it. -->
<!-- HIGH-2: example PACKAGE SPLIT. One example package cannot carry both a wasm cdylib (pmcp default-features=false,
     features=["wasm"]) and a native server (pmcp full/streamable-http) — Cargo unifies dep features per package
     build, so the wasm build would pull native HTTP stacks. CONTEXT "Claude's Discretion" allows a better example
     layout than mirroring wasm-client. Adopt TWO separate manifests under examples/web-channel-client/: a wasm
     client crate (examples/web-channel-client/client/) and a native server crate
     (examples/web-channel-client/server/). The spike VALIDATES this split keeps the wasm build dependency-clean.
     Scope fence intact: still ONE example directory, still no published crates/ library. -->
<!-- InMemoryOAuthProvider methods (oauth2.rs): new(base_url) :405; validate_authorization :473;
     create_authorization_code :507; exchange_code :536; validate_token :699; metadata :713. -->
<!-- Owner resolution (RESEARCH Open Question 2 / task_dispatch.rs:168-186): subject-first, "local" fallback;
     create-path scopes minted task to same owner (mod.rs:1361-1365). -->
<!-- getrandom backend cfg (Pitfall 2): rustflags = ['--cfg','getrandom_backend="wasm_js"'] under [target.wasm32-unknown-unknown] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROVE IdP route-merge against the real public API + leave a durable test; decide package split + owner resolution (Open Questions 1 and 2, HIGH-2/HIGH-3)</name>
  <read_first>
    - src/server/streamable_http_server.rs:286 (router build / build_mcp_router seam — CHECK its visibility: is it `pub` or `pub(crate)`? HIGH-3), :758-770 (bearer validation — no IdP routes served)
    - src/server/auth/oauth2.rs:405,473,507,536,699,713 (InMemoryOAuthProvider methods + metadata/exchange/validate)
    - src/server/auth/mock.rs:57-82,258-259 (MockValidator alternative bearer validator)
    - src/server/task_dispatch.rs:168-186 (resolve_owner: subject-first, "local" fallback)
    - src/server/mod.rs:1361-1365 (create-path owner scoping)
    - examples/s29_oauth_server.rs:125-225 (proves IdP runs over stdio + only LOGS URLs — no HTTP routes)
    - examples/s46_http_tool_as_task.rs:49-119 (server stand-up the demo will copy)
    - tests/tool_as_task_lifecycle_http.rs:1-60 (the durable integration-test style the spike test mirrors)
  </read_first>
  <action>
    Spike the OAuth2 IdP HTTP wiring AND prove it durably (HIGH-3). First determine whether the route-merge
    seam is PUBLIC: inspect `build_mcp_router` (streamable_http_server.rs:286) and any axum::Router-returning
    builder on `StreamableHttpServer` — confirm whether a public API lets you compose the MCP router with
    hand-wired GET /oauth2/authorize + POST /oauth2/token routes (driving InMemoryOAuthProvider) into ONE axum
    Router on a single origin (Open Question 1 option b, preferred to avoid CORS). NAME the exact public
    builder/method in 103-SPIKE.md. If NO public seam exists, ESCALATE: record that an SDK-internal change is
    required (e.g. make the router builder `pub`, or add a documented compose method) and add that as an explicit
    SDK task note in 103-SPIKE.md for plan 04 — do NOT let plan 04 silently assume composability.

    Then leave a DURABLE artifact (HIGH-3): create `tests/web_channel_oauth_route_merge_spike.rs` — a committed
    integration test (mark `#[ignore]` if it needs a running listener, otherwise a plain `#[tokio::test]`) that
    stands up the MCP server (StreamableHttpServer per s46) merged with the two OAuth routes via the NAMED
    mechanism, and asserts BOTH the MCP POST endpoint AND the two /oauth2/* routes respond on one listener. This
    test survives into plan 04 so the route-merge result cannot silently regress (it replaces the throwaway probe).
    If composition genuinely requires the escalated SDK change and that change is out of this task's scope, leave
    the test `#[ignore]`d with a doc-comment stating the exact blocking SDK task — still committed, still durable.

    SEPARATELY validate the example PACKAGE SPLIT (HIGH-2): confirm the two-manifest layout
    (examples/web-channel-client/client/ wasm cdylib with pmcp features=["wasm"] + examples/web-channel-client/server/
    native crate with pmcp full/streamable-http) keeps the wasm build free of native deps. Record the exact
    directory + manifest layout the downstream plans (04 server, 05 client) must use in 103-SPIKE.md.

    SEPARATELY resolve Open Question 2: stand up the demo server with the bundled IdP bearer path, log the
    resolved owner (AuthContext.subject vs the "local" fallback) on the create-path, and confirm
    store.list(owner, None) returns the freshly-minted Working task for that owner (the background updater's
    discovery heuristic, Pattern 3). Record in 103-SPIKE.md the EXACT decision plan 04 must implement: the NAMED
    router-merge public API (or the escalated SDK task); the package-split layout; what owner string the updater
    must list for; whether to use InMemoryOAuthProvider.validate_token or MockValidator as the server-side bearer
    check. The durable test (not a throwaway probe binary) is the artifact that survives; any scratch probe binary
    is removed.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md && grep -c "Open Question" .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md && test -f tests/web_channel_oauth_route_merge_spike.rs && grep -c "oauth2" tests/web_channel_oauth_route_merge_spike.rs</automated>
  </verify>
  <acceptance_criteria>
    - 103-SPIKE.md exists and answers Open Question 1 by NAMING the exact public StreamableHttpServer builder/method that composes the MCP + /oauth2/* routes on one origin — OR explicitly escalating that an SDK-internal change is required, with the precise SDK task plan 04 must include (HIGH-3)
    - A DURABLE committed test `tests/web_channel_oauth_route_merge_spike.rs` exists, stands up the merged MCP+OAuth router via the named mechanism, and asserts both endpoints respond (or is `#[ignore]`d with a doc-comment naming the blocking SDK task) — `grep -c "oauth2" tests/web_channel_oauth_route_merge_spike.rs` >= 1 (HIGH-3)
    - 103-SPIKE.md records the validated example PACKAGE SPLIT layout: examples/web-channel-client/client/ (wasm) + examples/web-channel-client/server/ (native), confirming the wasm build stays free of native HTTP deps (HIGH-2)
    - 103-SPIKE.md answers Open Question 2 with the EXACT owner string the create-path scopes to under the bundled IdP bearer, and confirms store.list(owner) finds the minted task (or documents the "local" fallback path)
    - 103-SPIKE.md states which bearer validator plan 04 uses (InMemoryOAuthProvider.validate_token vs MockValidator)
    - No throwaway probe BINARY remains under examples/web-channel-client/ (the durable artifact is the committed test, not a scratch binary)
  </acceptance_criteria>
  <done>The IdP route-merge is proven against the real public API (or escalated as a named SDK task) with a DURABLE committed test; the package split + owner-resolution decisions are recorded as implementable facts for plan 04.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm getrandom wasm backend cfg requirement (Pitfall 2 / Open Question 4)</name>
  <read_first>
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md (Pitfall 2 + A2: getrandom 0.4 on wasm32-unknown-unknown may need the backend cfg)
    - Cargo.toml (getrandom dep — note plan 01 moves it to the cross-target [dependencies] table; the wasm32-target entry keeps features=["wasm_js"])
    - Makefile:59-67 (wasm-build target: cargo build --target wasm32-unknown-unknown --no-default-features --features wasm)
    - examples/wasm-client/build.sh (the build harness the example mirrors)
  </read_first>
  <action>
    Empirically determine whether the wasm build needs the getrandom backend cfg. First run
    `cargo tree -p getrandom` to capture the EXACT resolved getrandom version (and confirm plan 01's move of
    getrandom into the cross-target [dependencies] table did not change resolution). Then build the pmcp wasm
    target (cargo build --target wasm32-unknown-unknown --no-default-features --features wasm) — the bare wasm
    feature already pulls getrandom via uuid/js, and plan 01's PKCE module (if landed in this worktree) calls
    getrandom::fill directly. If the build FAILS with a getrandom "no backend" / "unsupported target" error,
    note that the WASM CLIENT crate (examples/web-channel-client/client/, created in plan 05) will need a
    `.cargo/config.toml` with `[target.wasm32-unknown-unknown]` `rustflags = ['--cfg', 'getrandom_backend="wasm_js"']`,
    and record the exact rustflags in 103-SPIKE.md so plan 05's build is deterministic. (Do NOT create the wasm
    client's .cargo/config.toml here — that file belongs to plan 05's client crate; this task only records the
    verdict. If a native-server-side getrandom cfg is somehow needed, place
    examples/web-channel-client/server/.cargo/config.toml accordingly, else leave it absent.) If the build
    SUCCEEDS without the cfg, record cfg-needed = no and create no config file.
  </action>
  <verify>
    <automated>cargo tree -p getrandom 2>&1 | head -3; cargo build --target wasm32-unknown-unknown --no-default-features --features wasm 2>&1 | tail -8; grep -ci getrandom .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md</automated>
  </verify>
  <acceptance_criteria>
    - 103-SPIKE.md records the EXACT resolved getrandom version (from cargo tree -p getrandom)
    - 103-SPIKE.md states cfg-needed = yes/no for the wasm build, with the exact rustflags line if yes (to be applied in the plan-05 wasm client crate's .cargo/config.toml)
    - If a native-side cfg is required, examples/web-channel-client/server/.cargo/config.toml exists with the rustflags; otherwise the file is absent
    - `cargo build --target wasm32-unknown-unknown --no-default-features --features wasm` exits 0 (with the cfg applied if required)
  </acceptance_criteria>
  <done>The wasm getrandom backend cfg requirement is settled empirically and recorded for plan 05's wasm client crate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → IdP/MCP origin | Authorization redirect, token exchange, and MCP requests cross this; CORS posture decided here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-CORS | Information Disclosure / Spoofing | IdP routes vs MCP router origin | mitigate | Spike decides single-origin router-merge (preferred) so the browser hits ONE origin — no cross-origin token leakage; the route-merge is PROVEN against the real public API and a durable committed test (HIGH-3); if two-port fallback, the CORS allow-list is documented and constrained in plan 04 |
| T-103-IDOR | Elevation of Privilege | task owner resolution | mitigate | Confirm AuthContext.subject (not a client param) is the owner the store scopes to, so a client cannot list/cancel another owner's task (cross-owner isolation proven in lifecycle_http test:289-325) |
| T-103-SC | Tampering | crate installs | accept | No new packages introduced by the spike; the durable artifact is a committed in-tree test, not an installed package; any scratch probe binary is removed |
</threat_model>

<verification>
- 103-SPIKE.md present with answers to Open Questions 1 (NAMED public route-merge API or escalated SDK task), 2, the package-split layout, and the getrandom cfg verdict
- tests/web_channel_oauth_route_merge_spike.rs present as the durable route-merge proof (HIGH-3)
- `cargo build --target wasm32-unknown-unknown --no-default-features --features wasm` exits 0
- No throwaway probe BINARY remains in examples/web-channel-client/
</verification>

<success_criteria>
The route-merge is PROVEN against the real public StreamableHttpServer API (or escalated as a named SDK task)
with a DURABLE committed test (HIGH-3); the example package split (wasm client + native server crates) is
validated dependency-clean (HIGH-2); owner resolution and the wasm getrandom cfg are decided and recorded as
implementable facts, unblocking plans 04 and 05. (De-risks SC-2/SC-3/SC-4; WEBCH-04/05/07.)
</success_criteria>

<output>
Create `.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-03-SUMMARY.md` when done
</output>
</content>
</invoke>
