---
phase: 103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md
  - examples/web-channel-client/.cargo/config.toml
autonomous: true
requirements: [WEBCH-04, WEBCH-05, WEBCH-07]
must_haves:
  truths:
    - "The mechanism to serve /oauth2/authorize + /oauth2/token alongside the MCP router on ONE origin is decided and recorded (Open Question 1)"
    - "The AuthContext.subject value from the bundled IdP bearer is known, and whether store.list(owner) finds the create-path-minted task is confirmed (Open Question 2)"
    - "Whether the wasm build needs --cfg getrandom_backend wasm_js is determined empirically (Open Question 4 / Pitfall 2)"
  artifacts:
    - path: ".planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md"
      provides: "Decisions for IdP route-merge, owner resolution, getrandom backend cfg — consumed by plans 04 and 05"
      contains: "Open Question 1"
  key_links:
    - from: "103-SPIKE.md"
      to: "103-04-PLAN demo server"
      via: "IdP route-merge + owner-resolution decisions"
      pattern: "route"
    - from: "103-SPIKE.md"
      to: "103-05-PLAN example wasm build"
      via: "getrandom backend cfg decision"
      pattern: "getrandom"
---

<objective>
Resolve the three highest-risk unknowns BEFORE the demo server (plan 04) and the browser example
(plan 05) are built, and record the decisions in `103-SPIKE.md`. This de-risks the two MEDIUM-confidence
seams (IdP HTTP route-merge, background-updater owner resolution) and the wasm build gotcha (getrandom
backend cfg) so the downstream plans implement against decided facts, not assumptions.

Purpose: RESEARCH flagged Open Questions 1 (merge OAuth2 IdP routes with the MCP router on one origin to
avoid CORS), 2 (does AuthContext.subject from the bundled IdP bearer match what store.list(owner) sees
for the D-05 background updater), and Pitfall 2/A2 (getrandom 0.4 on wasm32 may need the backend cfg).
Each blocks a specific downstream task; resolving them now keeps waves 2-3 deterministic.
Output: 103-SPIKE.md with a decision per question, plus (only if the wasm build proves it necessary)
examples/web-channel-client/.cargo/config.toml with the getrandom backend cfg.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-CONTEXT.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-PATTERNS.md

<interfaces>
<!-- IdP route-merge candidates (RESEARCH Open Question 1):
     (a) second axum listener for /oauth2/* on the same host (CORS implications),
     (b) compose ONE Router via the lower-level pmcp::axum builder (build_mcp_router, streamable_http_server.rs:286) — PREFERRED, single origin avoids CORS,
     (c) two ports (CORS). -->
<!-- InMemoryOAuthProvider methods (oauth2.rs): new(base_url) :405; validate_authorization :473;
     create_authorization_code :507; exchange_code :536; validate_token :699; metadata :713. -->
<!-- Owner resolution (RESEARCH Open Question 2 / task_dispatch.rs:168-186): subject-first, "local" fallback;
     create-path scopes minted task to same owner (mod.rs:1361-1365). -->
<!-- getrandom backend cfg (Pitfall 2): rustflags = ['--cfg','getrandom_backend="wasm_js"'] under [target.wasm32-unknown-unknown] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decide IdP route-merge + owner resolution (Open Questions 1 and 2)</name>
  <read_first>
    - src/server/streamable_http_server.rs:286 (router build / build_mcp_router seam), :758-770 (bearer validation — no IdP routes served)
    - src/server/auth/oauth2.rs:405,473,507,536,699,713 (InMemoryOAuthProvider methods + metadata/exchange/validate)
    - src/server/auth/mock.rs:57-82,258-259 (MockValidator alternative bearer validator)
    - src/server/task_dispatch.rs:168-186 (resolve_owner: subject-first, "local" fallback)
    - src/server/mod.rs:1361-1365 (create-path owner scoping)
    - examples/s29_oauth_server.rs:125-225 (proves IdP runs over stdio + only LOGS URLs — no HTTP routes)
    - examples/s46_http_tool_as_task.rs:49-119 (server stand-up the demo will copy)
  </read_first>
  <action>
    Spike the OAuth2 IdP HTTP wiring: determine whether the pmcp::axum lower-level router builder
    (build_mcp_router, streamable_http_server.rs:286) can be composed with hand-wired GET /oauth2/authorize
    plus POST /oauth2/token routes (driving InMemoryOAuthProvider) into ONE axum Router served on a single
    origin (RESEARCH Open Question 1 option b, preferred to avoid CORS). Write a throwaway probe binary or an
    `#[ignore]` test that stands up the MCP server (StreamableHttpServer per s46) and confirms the chosen merge
    mechanism actually serves both the MCP POST and the two OAuth routes on one listener; if composition is
    awkward, fall back to a second listener / two ports and record the CORS consequence. SEPARATELY resolve
    Open Question 2: stand up the demo server with the bundled IdP bearer path, log the resolved owner
    (AuthContext.subject vs the "local" fallback) on the create-path, and confirm store.list(owner, None)
    returns the freshly-minted Working task for that owner (the background updater's discovery heuristic,
    Pattern 3). Record both findings in 103-SPIKE.md with the EXACT decision plan 04 must implement: which
    router-merge option; what owner string the updater must list for; whether to use
    InMemoryOAuthProvider.validate_token or MockValidator as the server-side bearer check. Do NOT leave the
    probe binary in the example tree — it is throwaway; the decision lives in the spike doc.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md && grep -c "Open Question" .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md</automated>
  </verify>
  <acceptance_criteria>
    - 103-SPIKE.md exists and answers Open Question 1 with a concrete chosen mechanism (router-merge option a/b/c) plus the route paths to wire
    - 103-SPIKE.md answers Open Question 2 with the EXACT owner string the create-path scopes to under the bundled IdP bearer, and confirms store.list(owner) finds the minted task (or documents the "local" fallback path)
    - 103-SPIKE.md states which bearer validator plan 04 uses (InMemoryOAuthProvider.validate_token vs MockValidator)
    - No throwaway probe binary remains under examples/web-channel-client/
  </acceptance_criteria>
  <done>IdP route-merge and owner-resolution decisions are recorded as implementable facts for plan 04.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm getrandom wasm backend cfg requirement (Pitfall 2 / Open Question 4)</name>
  <read_first>
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md (Pitfall 2 + A2: getrandom 0.4 on wasm32-unknown-unknown may need the backend cfg)
    - Cargo.toml:126 (getrandom wasm dep with features wasm_js)
    - Makefile:59-67 (wasm-build target: cargo build --target wasm32-unknown-unknown --no-default-features --features wasm)
    - examples/wasm-client/build.sh (the build harness the example mirrors)
  </read_first>
  <action>
    Empirically determine whether the wasm build needs the getrandom backend cfg. First run
    `cargo tree -p getrandom` to capture the EXACT resolved getrandom version. Then build the pmcp wasm target
    (cargo build --target wasm32-unknown-unknown --no-default-features --features wasm) — the bare wasm feature
    already pulls getrandom via uuid/js, and plan 01's PKCE module (if landed in this worktree) calls
    getrandom::fill directly. If the build FAILS with a getrandom "no backend" / "unsupported target" error,
    create examples/web-channel-client/.cargo/config.toml containing
    `[target.wasm32-unknown-unknown]` with `rustflags = ['--cfg', 'getrandom_backend="wasm_js"']` and record
    that the example build needs it. If the build SUCCEEDS without the cfg, record that the file is NOT needed
    and do NOT create it. Either way, write the verdict (resolved getrandom version + cfg-needed yes/no + exact
    rustflags if needed) into 103-SPIKE.md so plan 05's build.sh / Cargo.toml setup is deterministic.
  </action>
  <verify>
    <automated>cargo tree -p getrandom 2>&1 | head -3; cargo build --target wasm32-unknown-unknown --no-default-features --features wasm 2>&1 | tail -8; grep -ci getrandom .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md</automated>
  </verify>
  <acceptance_criteria>
    - 103-SPIKE.md records the EXACT resolved getrandom version (from cargo tree -p getrandom)
    - 103-SPIKE.md states cfg-needed = yes/no for the wasm build, with the exact rustflags line if yes
    - If cfg is needed, examples/web-channel-client/.cargo/config.toml exists with the wasm32-unknown-unknown getrandom_backend rustflags; if not needed, the file is absent
    - `cargo build --target wasm32-unknown-unknown --no-default-features --features wasm` exits 0 (with the cfg applied if required)
  </acceptance_criteria>
  <done>The wasm getrandom backend cfg requirement is settled empirically and recorded for plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → IdP/MCP origin | Authorization redirect, token exchange, and MCP requests cross this; CORS posture decided here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-CORS | Information Disclosure / Spoofing | IdP routes vs MCP router origin | mitigate | Spike decides single-origin router-merge (preferred) so the browser hits ONE origin — no cross-origin token leakage; if two-port fallback, the CORS allow-list is documented and constrained in plan 04 |
| T-103-IDOR | Elevation of Privilege | task owner resolution | mitigate | Confirm AuthContext.subject (not a client param) is the owner the store scopes to, so a client cannot list/cancel another owner's task (cross-owner isolation proven in lifecycle_http test:289-325) |
| T-103-SC | Tampering | crate installs | accept | No new packages introduced by the spike; probe binary is throwaway and removed |
</threat_model>

<verification>
- 103-SPIKE.md present with answers to Open Questions 1, 2, and the getrandom cfg verdict
- `cargo build --target wasm32-unknown-unknown --no-default-features --features wasm` exits 0
- No throwaway probe artifacts remain in examples/web-channel-client/
</verification>

<success_criteria>
The two MEDIUM-confidence seams (IdP route-merge, owner resolution) and the wasm getrandom cfg are
decided and recorded as implementable facts, unblocking plans 04 and 05. (De-risks SC-2/SC-3; WEBCH-04/05/07.)
</success_criteria>

<output>
Create `.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-03-SUMMARY.md` when done
</output>
