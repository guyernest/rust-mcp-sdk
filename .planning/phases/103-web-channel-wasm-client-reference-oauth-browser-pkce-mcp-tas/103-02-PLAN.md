---
phase: 103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/wasm_http.rs
autonomous: true
requirements: [WEBCH-02]
must_haves:
  truths:
    - "WasmHttpTransport::send() POSTs the message and buffers the parsed response"
    - "WasmHttpTransport::receive() returns the buffered response (not a hard error)"
    - "The high-level Client (send→loop-receive correlation) works over Fetch via WasmHttpTransport"
    - "extra_headers (bearer) injection continues to work for free (do_http_request already injects it)"
    - "Non-wasm host build is NOT regressed (file is wasm-only) and the wasm build compiles"
  artifacts:
    - path: "src/shared/wasm_http.rs"
      provides: "Functional Transport impl for WasmHttpTransport via one-slot pending_response buffer"
      contains: "pending_response"
  key_links:
    - from: "WasmHttpTransport::send"
      to: "WasmHttpTransport::receive"
      via: "self.pending_response one-slot buffer"
      pattern: "pending_response"
    - from: "WasmHttpTransport::send"
      to: "do_request"
      via: "POST + parse TransportMessage"
      pattern: "do_request"
---

<objective>
FIX the existing, broken `WasmHttpTransport::{send,receive}` so the high-level `Client` (and all
four typed task helpers) work over browser Fetch (D-08). This is the central SDK fix and the second
reusable public-API piece in the new release — it is NOT a greenfield transport.

Purpose: Today `send()` POSTs and DISCARDS the response, and `receive()` returns a hard error
("HTTP transport requires send() before receive()"). The high-level `Client::send_request` calls
`send(msg)` once then LOOPS on `receive()` to correlate the response — so `Client.initialize()` over
Fetch fails immediately. Buffering the one-shot POST response in `send()` and popping it in
`receive()` makes the whole typed `Client` API ("call MCP from a web app") work over Fetch.
Output: a repaired `Transport` impl with a one-slot `pending_response` buffer + a correlation unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-CONTEXT.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-PATTERNS.md

<interfaces>
<!-- The trait to satisfy (mirror the working WasmWebSocketTransport at wasm_websocket.rs:126-162) -->
```rust
#[async_trait(?Send)]
impl Transport for T {
    async fn send(&mut self, message: TransportMessage) -> Result<()>;
    async fn receive(&mut self) -> Result<TransportMessage>;
    async fn close(&mut self) -> Result<()>;
}
```
<!-- Existing reusable helper (do NOT rewrite) — wasm_http.rs:72-82:
     do_request(&message) already POSTs via do_http_request and returns a PARSED TransportMessage.
     do_http_request (wasm_http.rs:85-187) already injects extra_headers (117-121, the bearer),
     mcp-session-id (103-107), and updates session_id from the response (167-169). -->
<!-- Current struct (wasm_http.rs:44-49): { config, session_id, protocol_version }.
     Add: pending_response: Option<TransportMessage>. -->
<!-- Correlation contract — src/client/mod.rs:1981-1998: send(msg) once, then loop receive()
     until a TransportMessage::Response is returned. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair send/receive correlation with a one-slot pending_response buffer</name>
  <read_first>
    - src/shared/wasm_http.rs:6 (file-level `#![cfg(target_arch = "wasm32")]`), :44-49 (struct), :72-82 (do_request — reuse), :85-187 (do_http_request — extra_headers/session-id injection), :190-217 (the BROKEN Transport impl to replace), :226-265 (WasmHttpClient raw wrapper — leave untouched)
    - src/shared/wasm_websocket.rs:126-162 (the working Transport impl to mirror — channel-based receive vs our one-slot buffer)
    - src/client/mod.rs:1981-1998 (the send→loop-receive correlation the fix must satisfy)
    - src/shared/transport.rs (TransportMessage enum + Transport trait definition)
  </read_first>
  <behavior>
    - send(msg): calls do_request(&msg) (POST + parse) and stores the result in self.pending_response; returns Ok(())
    - receive(): self.pending_response.take(), returning the buffered TransportMessage; Err only if called before send
    - close(): Ok(()) (HTTP is stateless)
    - WasmHttpClient (raw wrapper, lines 226-265) is unchanged — backward compat preserved
  </behavior>
  <action>
    Add a `pending_response: Option<TransportMessage>` field to the `WasmHttpTransport` struct
    (wasm_http.rs:44-49) and initialize it `None` in `WasmHttpTransport::new`. Replace the broken
    `Transport` impl (wasm_http.rs:190-217): `send()` must `self.pending_response = Some(self.do_request(&message).await?)`
    then `Ok(())`; `receive()` must `self.pending_response.take().ok_or_else(|| Error::internal("receive() called before send() on HTTP transport"))`;
    `close()` stays `Ok(())`. Reuse the existing `do_request` helper (do NOT rewrite the Fetch plumbing — it
    already injects `extra_headers` for the bearer, manages `mcp-session-id`, and parses the response into a
    `TransportMessage`). Keep `#[async_trait(?Send)]` and the file-level `#![cfg(target_arch = "wasm32")]` gate
    intact — this file is wasm-only so the host build cannot regress, but it MUST still compile under
    `make wasm-build` (Pitfall 5). Leave the `WasmHttpClient` raw wrapper (lines 226-265) untouched for the
    old example's backward compatibility. Keep each fn under cognitive complexity 25 (trivial).
  </action>
  <verify>
    <automated>cargo build -p pmcp --target wasm32-unknown-unknown --no-default-features --features wasm 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pending_response" src/shared/wasm_http.rs` >= 3 (field decl, init, send, receive)
    - `src/shared/wasm_http.rs` `receive()` returns `self.pending_response.take()...` (NOT the old hard error string "HTTP transport requires send() before receive()")
    - `grep -c "HTTP transport requires send() before receive()" src/shared/wasm_http.rs` == 0
    - `cargo build --target wasm32-unknown-unknown --no-default-features --features wasm` exits 0
    - `WasmHttpClient` raw wrapper block (lines ~226-265) is unchanged
  </acceptance_criteria>
  <done>Transport impl buffers the POST response in send() and returns it from receive(); wasm build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correlation unit test for send→receive</name>
  <read_first>
    - src/shared/wasm_http.rs (the repaired impl from Task 1)
    - src/shared/wasm_websocket.rs (existing wasm-test patterns, if any, for the test-gating style)
    - examples/wasm-client/Cargo.toml (wasm-bindgen-test 0.3 dev-dep — the wasm unit test framework)
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md (Pitfall 1: warning sign is `Client.initialize()` returning the old error string)
  </read_first>
  <action>
    Add a `#[cfg(test)]` unit test for the correlation invariant. Because the Fetch path requires a real
    browser `window`, structure the test to assert the buffer semantics WITHOUT a live network: add a small
    test-only seam — either a `#[cfg(test)] fn set_pending_for_test(&mut self, msg: TransportMessage)` (or
    construct via a test helper) — then assert that after a `pending_response` is set, `receive()` returns it
    and a second `receive()` returns the "called before send" `Error::internal`. Gate the test so it runs
    under the wasm test harness where applicable (`wasm-bindgen-test`) and/or compiles as a host-target unit
    test if the seam avoids `web_sys`. The behavioral contract to prove: ONE buffered message is popped exactly
    once (one-slot semantics), matching `Client::send_request`'s send-once-then-loop-receive correlation. Keep
    the seam minimal and clearly test-only; do not expose it on the public API.
  </action>
  <verify>
    <automated>cargo build -p pmcp --target wasm32-unknown-unknown --no-default-features --features wasm 2>&1 | tail -5 && echo "WASM-UNIT: run via 'wasm-pack test --headless --firefox examples/web-channel-client' in the example plan / phase verification"</automated>
  </verify>
  <acceptance_criteria>
    - A `#[cfg(test)]` correlation test exists in `src/shared/wasm_http.rs` asserting: buffered message is returned by `receive()`, and a subsequent `receive()` (empty buffer) returns an `Error`
    - The test seam is `#[cfg(test)]`-only (not on the public surface): `grep -c "cfg(test)" src/shared/wasm_http.rs` >= 1
    - wasm build still compiles after adding the test
  </acceptance_criteria>
  <done>One-slot buffer correlation is proven by a test; the warning-sign error path (empty receive) is asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → MCP server | JSON-RPC + Authorization: Bearer header crosses this boundary over Fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-CORR | Tampering | send/receive response correlation | mitigate | One-slot buffer returns exactly the response parsed from the matching POST; each Client request mints a unique JSON-RPC id (Uuid::new_v4, client/mod.rs:522), and the one-shot transport shares no cache — avoids the cross-cache replayed-id bug from MEMORY |
| T-103-BEARER | Information Disclosure | extra_headers bearer injection | accept | The bearer is injected by the existing do_http_request (wasm_http.rs:117-121); this plan does not change header handling — token storage/scoping is addressed in plan 05 (sessionStorage, origin-scoped) |
| T-103-SC | Tampering | crate installs | accept | No new packages; wasm transport deps already in-tree |
</threat_model>

<verification>
- `cargo build --target wasm32-unknown-unknown --no-default-features --features wasm` exits 0
- `grep -c "HTTP transport requires send() before receive()" src/shared/wasm_http.rs` == 0
- Host build not regressed (file is wasm-only; verified by `cargo build -p pmcp --features full` at wave merge)
- Correlation test present and `#[cfg(test)]`-gated
</verification>

<success_criteria>
The high-level `Client` works over browser Fetch: send() buffers, receive() returns the buffered
response, bearer injection continues to work, wasm build green, host build unregressed. (SC-1/SC-2 transport; WEBCH-02.)
</success_criteria>

<output>
Create `.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-02-SUMMARY.md` when done
</output>
