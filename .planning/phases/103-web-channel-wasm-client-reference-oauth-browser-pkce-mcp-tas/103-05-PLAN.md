---
phase: 103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas
plan: 05
type: execute
wave: 3
depends_on: [103-01, 103-02, 103-03, 103-04]
files_modified:
  - examples/web-channel-client/client/src/lib.rs
  - examples/web-channel-client/client/Cargo.toml
  - examples/web-channel-client/client/.cargo/config.toml
  - examples/web-channel-client/client/build.sh
  - examples/web-channel-client/client/index.html
  - examples/web-channel-client/client/main.js
  - examples/web-channel-client/client/style.css
  - examples/web-channel-client/README.md
autonomous: true
requirements: [WEBCH-03, WEBCH-06, WEBCH-07]
must_haves:
  truths:
    - "The browser WasmClient lives in its OWN wasm-only crate (examples/web-channel-client/client/), separate from the native server crate, so the cdylib build pulls NO native HTTP deps (HIGH-2, SC-4)"
    - "The browser WasmClient uses the high-level Client<WasmHttpTransport> (not raw JSON-RPC) so typed task helpers work over Fetch"
    - "The example performs a full-page-redirect PKCE flow using the pmcp pkce helper; verifier+state+token live in sessionStorage"
    - "The Fetch token-exchange request format is specified exactly (content-type, fields, expected JSON response) so the browser demo does not fail on form-vs-JSON mismatch"
    - "The bearer token is threaded into the transport via extra_headers"
    - "The browser drives call(task) -> poll tasks/get @~500ms -> tasks/result, plus a Cancel button (tasks/cancel)"
    - "The README documents the single-user/no-concurrency limitation if the demo updater could not use a unique task marker (MEDIUM-6)"
    - "The example wasm crate builds via make wasm-build / build.sh and is documented for pmcp.run adaptation"
  artifacts:
    - path: "examples/web-channel-client/client/src/lib.rs"
      provides: "#[wasm_bindgen] WasmClient: PKCE flow + high-level Client task helpers"
      contains: "WasmHttpTransport"
    - path: "examples/web-channel-client/client/Cargo.toml"
      provides: "wasm cdylib crate (pmcp default-features=false features=[wasm]) — separate from the native server crate (HIGH-2)"
      contains: "cdylib"
    - path: "examples/web-channel-client/client/main.js"
      provides: "?code=&state= redirect detection + explicit 500ms setTimeout poll loop"
      contains: "setTimeout"
    - path: "examples/web-channel-client/client/index.html"
      provides: "Login UI + task status line + Cancel button"
      contains: "Cancel"
    - path: "examples/web-channel-client/README.md"
      provides: "How pmcp.run adapts this as a web-app channel + single-user concurrency note"
      contains: "channel"
  key_links:
    - from: "examples/web-channel-client/client/src/lib.rs"
      to: "pmcp pkce helper"
      via: "generate_code_verifier / code_challenge_s256 / generate_state"
      pattern: "code_challenge_s256|generate_code_verifier"
    - from: "examples/web-channel-client/client/src/lib.rs"
      to: "Client<WasmHttpTransport>"
      via: "Client::new(WasmHttpTransport::new(config)) with bearer in extra_headers"
      pattern: "WasmHttpTransport"
    - from: "examples/web-channel-client/client/main.js"
      to: "wasm tasks_get"
      via: "500ms setTimeout poll loop until terminal"
      pattern: "setTimeout"
---

<objective>
Build the browser WASM client and demo harness — the EXAMPLE deliverable (SC-3) — in its OWN wasm-only crate
(examples/web-channel-client/client/, HIGH-2). It performs the full-page redirect PKCE flow using the new
pmcp pkce helper, threads the bearer into the fixed WasmHttpTransport, and drives the full tasks lifecycle
over Fetch with an explicit visible 500ms poll loop and a Cancel button.

Purpose: This is the "strong reference" sibling to examples/wasm-client that the pmcp.run team lifts to build
a web-app channel. The browser-specific OAuth orchestration lives HERE (D-01/D-03), not in pmcp: full-page
redirect (D-07), reading ?code=&state= on return, sessionStorage for verifier/state/token (D-06), Fetch
token exchange, and the explicit poll loop + Cancel button (D-09). The key divergence from the old example:
use the HIGH-LEVEL Client<WasmHttpTransport> so all four typed task helpers work over Fetch (the D-08 win).
The client lives in its OWN crate, separate from the native server crate (HIGH-2), so the wasm cdylib build
pulls NO native HTTP deps (SC-4). Output: the full examples/web-channel-client/client/ crate (lib.rs,
Cargo.toml, .cargo/config.toml if the spike said getrandom cfg is needed, build.sh, index.html, main.js,
style.css) plus the shared examples/web-channel-client/README.md. Does NOT modify examples/wasm-client
(LOCKED fence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-CONTEXT.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-PATTERNS.md
@.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-SPIKE.md

<interfaces>
<!-- HIGH-2: PACKAGE SPLIT. The wasm client is its OWN crate at examples/web-channel-client/client/
     (a cdylib crate). The native demo server is a SEPARATE crate at examples/web-channel-client/server/
     (plan 04). They do NOT share one feature-unified package — this keeps pmcp full/streamable-http
     (hyper/tokio) OUT of the wasm cdylib build (SC-4). README.md is the ONE shared doc at the example root. -->
<!-- pmcp pkce helper (from plan 01): pmcp::shared::pkce::{generate_code_verifier() -> Result<String>,
     code_challenge_s256(&str) -> String, generate_state() -> Result<String>}. -->
<!-- Fixed transport (plan 02): Client::new(WasmHttpTransport::new(WasmHttpConfig{ url,
     extra_headers: vec![("Authorization".into(), format!("Bearer {token}"))] })) then client.initialize(...). -->
<!-- High-level task helpers (client/mod.rs:508-620): call_tool_with_task(name, args) -> ToolCallResponse::Task(t).task_id;
     tasks_get(&id) -> Task (Task.status.is_terminal()); tasks_result(&id) -> CallToolResult; tasks_cancel(&id). -->
<!-- wasm boilerplate to copy verbatim — wasm-client/lib.rs:25-105 (JsError, StructuredError, to_js_error,
     #[wasm_bindgen(constructor)] with console_error_panic_hook + tracing-wasm). -->
<!-- Connect pattern model — wasm-client/lib.rs:123-133 (WebSocket high-level branch); REPLACE the raw HTTP
     branch (134-164) with the high-level Client<WasmHttpTransport>. -->
<!-- Auth URL params reference (do NOT cfg-port the module) — oauth.rs:660-672: append code_challenge,
     code_challenge_method=S256, state. -->
<!-- Token exchange request format: POST /oauth2/token MUST match what InMemoryOAuthProvider.exchange_code
     expects — confirm against oauth2.rs:536 + the demo server (plan 04) whether the endpoint expects an
     application/x-www-form-urlencoded body (grant_type=authorization_code, code, code_verifier, redirect_uri,
     client_id) vs application/json, and what the JSON response shape is (access_token field). Specify the EXACT
     content-type + fields in lib.rs so the Fetch does not fail on form-vs-JSON mismatch. -->
<!-- web-sys features needed (client Cargo.toml): Storage, Window, Location, UrlSearchParams (D-06/D-07). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: #[wasm_bindgen] WasmClient (own client crate) — PKCE flow + high-level Client task helpers over Fetch</name>
  <read_first>
    - 103-SPIKE.md (the package-split client-crate layout + the getrandom cfg verdict — informs whether client/.cargo/config.toml is needed)
    - examples/wasm-client/src/lib.rs:25-105 (wasm boilerplate to copy verbatim), :123-133 (WS high-level branch to model the connect on), :134-164 (the RAW HTTP branch to REPLACE)
    - src/shared/pkce.rs (plan 01 helper — call via its public re-export path)
    - src/shared/wasm_http.rs (plan 02 fixed WasmHttpTransport + WasmHttpConfig.extra_headers)
    - src/client/mod.rs:508-620 (typed task helpers: call_tool_with_task/tasks_get/tasks_result/tasks_cancel)
    - src/client/oauth.rs:660-672 (auth-URL param reference — code_challenge + code_challenge_method=S256 + state; do NOT cfg-port this module, D-01)
    - src/server/auth/oauth2.rs:536 (exchange_code — the EXACT token-exchange request shape the Fetch POST must match)
    - examples/web-channel-client/server/src/main.rs (plan 04 — the actual /oauth2/token route the client posts to)
  </read_first>
  <behavior>
    - new(): console_error_panic_hook + tracing-wasm init (copy wasm-client boilerplate)
    - begin_login(authorize_base, client_id, redirect_uri) -> builds verifier+challenge+state via pmcp pkce, stores verifier+state in sessionStorage, returns the authorize_url for JS to navigate to (full-page redirect)
    - complete_login(code, state) -> verifies state matches sessionStorage, Fetch POST /oauth2/token with the EXACT content-type + fields the demo server expects (form-encoded vs JSON per oauth2.rs:536), parses the access_token from the JSON response, stores bearer in sessionStorage
    - connect(url): build Client::new(WasmHttpTransport::new(config-with-bearer)); client.initialize(...)
    - invoke_task(name) -> task_id via call_tool_with_task; poll_task(id) -> status/result via tasks_get/tasks_result; cancel_task(id) via tasks_cancel
  </behavior>
  <action>
    Create the wasm client crate at examples/web-channel-client/client/ (HIGH-2 package split): a cdylib crate
    SEPARATE from the native server crate (plan 04). Create examples/web-channel-client/client/src/lib.rs gated
    `#![cfg(target_arch = "wasm32")]` mirroring examples/wasm-client/src/lib.rs structure. Copy the wasm
    boilerplate verbatim (JsError, StructuredError, to_js_error, the #[wasm_bindgen(constructor)] with
    console_error_panic_hook::set_once + tracing-wasm — wasm-client/lib.rs:25-105). REPLACE the raw HTTP branch
    (wasm-client/lib.rs:134-164) with the high-level path (model on the WS branch :123-133): build WasmHttpConfig
    { url, extra_headers: vec![("Authorization".into(), format!("Bearer {token}"))] }, then
    Client::new(WasmHttpTransport::new(config)), then client.initialize(ClientCapabilities::default()). Expose
    #[wasm_bindgen] methods for the example-level PKCE orchestration (D-01): a login-begin method that calls the
    pmcp pkce helper (generate_code_verifier/code_challenge_s256/generate_state), stores verifier+state in
    sessionStorage via web-sys Storage (D-06), and builds the authorize_url (append code_challenge,
    code_challenge_method=S256, state per oauth.rs:660-672 — but build it HERE, do NOT cfg-port oauth.rs); a
    login-complete method that reads code+state, verifies state == sessionStorage state (CSRF check), and does a
    Fetch POST to /oauth2/token. Specify the token-exchange request EXACTLY to match what
    InMemoryOAuthProvider.exchange_code (oauth2.rs:536) + the plan-04 server route expect: the precise
    content-type (application/x-www-form-urlencoded vs application/json), the field set (grant_type,
    code, code_verifier from sessionStorage, redirect_uri, client_id), and parse the access_token from the
    documented JSON response shape — so the Fetch does not fail on a form-vs-JSON mismatch. Store the bearer in
    sessionStorage. Expose task-helper methods to JS — invoke (call_tool_with_task -> task_id), poll (tasks_get ->
    status; tasks_result when terminal), cancel (tasks_cancel). Map all pmcp::Error via to_js_error. If 103-SPIKE
    said the getrandom backend cfg is needed, create examples/web-channel-client/client/.cargo/config.toml with
    the recorded rustflags. Keep methods small (cognitive complexity <= 25). Do NOT modify examples/wasm-client
    (LOCKED fence).
  </action>
  <verify>
    <automated>cargo build --manifest-path examples/web-channel-client/client/Cargo.toml --target wasm32-unknown-unknown --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - examples/web-channel-client/client/src/lib.rs exists in its OWN crate, gated `#![cfg(target_arch = "wasm32")]` (HIGH-2 split)
    - `grep -cE "code_challenge_s256|generate_code_verifier" examples/web-channel-client/client/src/lib.rs` >= 1 (uses the pmcp pkce helper, not hand-rolled)
    - `grep -c "WasmHttpTransport" examples/web-channel-client/client/src/lib.rs` >= 1 and `grep -c "Client::new" examples/web-channel-client/client/src/lib.rs` >= 1 (high-level Client, NOT raw WasmHttpClient)
    - `grep -cE "call_tool_with_task|tasks_get|tasks_result|tasks_cancel" examples/web-channel-client/client/src/lib.rs` >= 3 (typed task helpers exposed)
    - The token-exchange Fetch specifies an explicit content-type and the field set matching oauth2.rs:536: `grep -ciE "x-www-form-urlencoded|application/json|grant_type" examples/web-channel-client/client/src/lib.rs` >= 1
    - `grep -c "sessionStorage\|session_storage\|Storage" examples/web-channel-client/client/src/lib.rs` >= 1 (D-06)
    - examples/wasm-client/src/lib.rs is UNCHANGED (git diff empty for that file)
    - The wasm lib build exits 0
  </acceptance_criteria>
  <done>The WasmClient (own crate) performs the PKCE flow via the pmcp helper, exchanges the token with the exact server-matched request format, and drives tasks over the high-level Client<WasmHttpTransport>; wasm build green.</done>
</task>

<task type="auto">
  <name>Task 2: Build harness — client Cargo.toml, build.sh, index.html, main.js, style.css, shared README</name>
  <read_first>
    - 103-SPIKE.md (the package-split layout + getrandom cfg verdict + any single-user/concurrency limitation flagged by plan 04 for the README — MEDIUM-6)
    - examples/wasm-client/Cargo.toml (base; add web-sys Storage/Window/Location/UrlSearchParams; pmcp default-features=false features=["wasm"] — NOT http/streamable-http, Pitfall 6)
    - examples/wasm-client/build.sh (copy verbatim, change --out-name + paths for the client/ subdir)
    - examples/wasm-client/index.html, main.js, style.css (mirror for init glue + UI)
    - .planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-RESEARCH.md (Code Examples: Browser PKCE flow shape; Driving the task lifecycle over Fetch — for main.js poll loop)
  </read_first>
  <action>
    Complete the example harness inside the client crate (and the shared root README). (1)
    examples/web-channel-client/client/Cargo.toml: the wasm cdylib config (crate-type=["cdylib"], wasm-pack
    metadata wasm-opt=false) plus pmcp { path="../../..", default-features=false, features=["wasm"] } (NOT
    http/streamable-http — Pitfall 6) and the example wasm deps (wasm-bindgen, wasm-bindgen-futures,
    serde-wasm-bindgen, console_error_panic_hook, tracing-wasm), with web-sys features
    ["console","WebSocket","Storage","Window","Location","UrlSearchParams"] (NEW vs wasm-client for D-06/D-07),
    and wasm-bindgen-test dev-dep. Because the server is a SEPARATE crate (plan 04, HIGH-2), this client crate
    has NO native bin and NO hyper/tokio — there is no feature unification with the native server. (2) build.sh:
    copy from wasm-client/build.sh changing --out-name and output paths for the client/ subdir; if 103-SPIKE said
    the getrandom backend cfg is needed, ensure examples/web-channel-client/client/.cargo/config.toml exists with
    the recorded rustflags. (3) index.html: mirror wasm-client/index.html and ADD a Login button, a task status
    line (Working -> Completed), and a Cancel button (D-09). (4) main.js: mirror wasm-client/main.js for the
    wasm-init glue and ADD (no direct analog): on load, read window.location.search for ?code=&state= and call the
    wasm login-complete method to resume the flow (D-07); and a fixed ~500ms setTimeout poll loop that calls the
    wasm poll method until terminal then shows the result (D-09 — keep it explicit/visible, do NOT hide in a
    helper); wire the Cancel button to the wasm cancel method. (5) style.css: copy from wasm-client/style.css.
    (6) examples/web-channel-client/README.md (the ONE shared root doc): document how the pmcp.run team adapts
    this as a web-app channel (SC-3) — the package split (client/ wasm + server/ native), the PKCE flow, the
    bearer-in-Fetch transport, the tasks poll loop, how to run the demo server crate, and how to swap the bundled
    IdP for a real provider (noting the IndexedDB durability upgrade path from D-06). If plan 04 flagged that the
    background updater could NOT use a unique task marker, DOCUMENT the single-user/no-concurrency limitation here
    explicitly (MEDIUM-6). Run build.sh / make wasm-build to confirm the client crate compiles to wasm.
  </action>
  <verify>
    <automated>cd examples/web-channel-client/client && bash build.sh 2>&1 | tail -12 || (cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && make wasm-build 2>&1 | tail -12)</automated>
  </verify>
  <acceptance_criteria>
    - The client harness files exist: examples/web-channel-client/client/{Cargo.toml,build.sh,index.html,main.js,style.css} + examples/web-channel-client/README.md (shared root)
    - client/Cargo.toml: `grep -cE "Storage|Location|UrlSearchParams" examples/web-channel-client/client/Cargo.toml` >= 3 (web-sys features for D-06/D-07); pmcp dep uses features=["wasm"] and NOT http/streamable-http; crate-type=["cdylib"]; NO hyper/tokio native deps (separate from server crate — HIGH-2)
    - main.js: `grep -c "setTimeout" examples/web-channel-client/client/main.js` >= 1 (explicit poll loop) and `grep -ciE "code=|state=|searchParams|location.search" examples/web-channel-client/client/main.js` >= 1 (redirect detection)
    - index.html: `grep -ci "cancel" examples/web-channel-client/client/index.html` >= 1 (Cancel button) and a status line present
    - README.md: `grep -ci "channel" examples/web-channel-client/README.md` >= 1; if plan 04 flagged it, the single-user/concurrency limitation is documented (MEDIUM-6)
    - The client crate builds to wasm (build.sh OR make wasm-build exits 0)
  </acceptance_criteria>
  <done>The full browser harness (own client crate) builds to wasm with the redirect-detect + 500ms poll loop + Cancel button, and a shared adaptation README (incl. the single-user concurrency note if applicable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser JS ↔ wasm | OAuth orchestration crosses the JS/wasm boundary; tokens live in browser storage |
| browser → IdP/MCP | Authorization redirect, token exchange, and bearer-authenticated MCP requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-CSRF | Tampering/Spoofing | OAuth state on redirect return | mitigate | state generated by the pmcp pkce helper, stored in sessionStorage, and verified == returned state in the login-complete method before token exchange |
| T-103-XSS-TOKEN | Information Disclosure | bearer in sessionStorage | accept (documented) | sessionStorage is origin-scoped + cleared on tab close — the demo default (D-06); README documents IndexedDB/httpOnly as the production durability/hardening upgrade |
| T-103-OPENREDIR | Tampering | authorize_url / redirect_uri | mitigate | redirect_uri is the example's own registered URI; the bundled IdP validates it (oauth2.rs:473); the example does not navigate to an attacker-controlled href |
| T-103-RNG | Tampering | verifier/state generation | mitigate | Uses the pmcp pkce helper (getrandom CSPRNG), never Math.random |
| T-103-REPLAY | Spoofing | JSON-RPC id reuse | mitigate | The high-level Client mints unique ids (Uuid::new_v4); the one-shot WasmHttpTransport shares no cache (avoids the MEMORY cross-cache id bug) |
| T-103-RACE | Tampering | demo task concurrency | accept (documented) | The demo updater's discovery is race-safe via the unique marker from plan 04; if that marker could not be carried, the single-user/no-concurrency limitation is documented in this README (MEDIUM-6) |
| T-103-SC | Tampering | crate installs | accept | Client crate deps mirror examples/wasm-client (already vetted); no new external packages; gloo-timers avoided in favor of JS setTimeout |
</threat_model>

<verification>
- `cargo build --manifest-path examples/web-channel-client/client/Cargo.toml --target wasm32-unknown-unknown --lib` exits 0
- build.sh OR `make wasm-build` exits 0
- The client crate pulls NO native HTTP deps (separate from the server crate — HIGH-2): `cargo tree --manifest-path examples/web-channel-client/client/Cargo.toml --target wasm32-unknown-unknown` shows no hyper/tokio native HTTP stack
- examples/wasm-client/* unchanged (LOCKED scope fence) — git diff empty for that directory
- Manual E2E (per 103-VALIDATION Manual-Only): serve index.html, Login through the bundled IdP, observe authenticated tools, invoke the long task, watch the 500ms polls flip Working->Completed, click Cancel mid-run
</verification>

<success_criteria>
The browser example (its OWN client crate, separate from the native server — HIGH-2) runs the full-page-redirect
PKCE flow via the pmcp helper, exchanges the token with the exact server-matched request format, threads the
bearer into the fixed transport, and drives call(task) -> poll @500ms -> tasks/result + Cancel over Fetch with
the high-level Client; it builds to wasm with NO native HTTP deps and is documented for pmcp.run adaptation
(incl. the single-user concurrency note if applicable). (SC-1/SC-2/SC-3/SC-4; WEBCH-03/06/07.)
</success_criteria>

<output>
Create `.planning/phases/103-web-channel-wasm-client-reference-oauth-browser-pkce-mcp-tas/103-05-SUMMARY.md` when done
</output>
</content>
</invoke>
