---
phase: 90-openapi-built-in-server
plan: 04
type: execute
wave: 2
depends_on: [90-01, 90-02]
files_modified:
  - crates/pmcp-server-toolkit/src/code_mode.rs
autonomous: true
requirements: [OAPI-05, OAPI-10]
must_haves:
  truths:
    - "HttpCodeExecutor impls pmcp_code_mode::HttpExecutor and round-trips a request through reqwest + auth"
    - "code_mode_tools_from_executor accepts Arc<dyn CodeExecutor> (not Arc<SqlCodeExecutor>)"
    - "The existing SQL code-mode path still wires + passes its tests after the generalization"
    - "validate_code flavor is parameterizable (sql vs openapi)"
    - "HttpCodeExecutor ExecutionError never echoes URL or Authorization"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/code_mode.rs"
      provides: "HttpCodeExecutor (impl HttpExecutor) + generalized code_mode_tools_from_executor + flavor param"
      contains: "impl pmcp_code_mode::HttpExecutor for HttpCodeExecutor"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/code_mode.rs HttpCodeExecutor"
      to: "crates/pmcp-server-toolkit/src/http/auth.rs"
      via: "self.auth.apply(&mut headers, &mut query)"
      pattern: "auth\\.apply"
    - from: "code_mode_tools_from_executor"
      to: "pmcp_code_mode::CodeExecutor"
      via: "Arc<dyn CodeExecutor> parameter"
      pattern: "Arc<dyn .*CodeExecutor>"
---

<objective>
Lift the `HttpCodeExecutor` seam (impl `pmcp_code_mode::HttpExecutor`, OAPI-05) and
perform the one real refactor of the phase: generalize the toolkit's
`code_mode_tools_from_executor` + its two handlers from hardcoded `Arc<SqlCodeExecutor>`
+ `"sql"` flavor to `Arc<dyn CodeExecutor>` + a flavor parameter (OAPI-10 / D-02), so
SQL and OpenAPI share ONE wiring function. The existing SQL path MUST stay green.

Purpose: This single seam feeds BOTH Code Mode (via JsCodeExecutor) and script tools
(Plan 05). The generalization unblocks the OpenAPI code-mode wiring without breaking SQL.
Output: HttpCodeExecutor + a backend-agnostic `code_mode_tools_from_executor`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
@.planning/phases/90-openapi-built-in-server/90-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HttpCodeExecutor — impl HttpExecutor (OAPI-05)</name>
  <files>crates/pmcp-server-toolkit/src/code_mode.rs</files>
  <read_first>
    - MODIFY crates/pmcp-server-toolkit/src/code_mode.rs (ADD a new type alongside SqlCodeExecutor).
    - ANALOG (shape): `SqlCodeExecutor` at code_mode.rs:428-542 (struct-holds-deps + `#[pmcp_code_mode::async_trait] impl CodeExecutor for SqlCodeExecutor` at :510). NOTE: HttpCodeExecutor impls the LOW-LEVEL `HttpExecutor` trait, NOT `CodeExecutor` (it is wrapped by `JsCodeExecutor` for Code Mode and called directly by script tools).
    - REFERENCE BODY to lift (near-verbatim): `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/crates/mcp-openapi-server-core/src/code_mode.rs:95-204` — `HttpClientExecutor` (the `Clone` impl :117-126; `execute_request` body :128-169: GET-like detection, `{placeholder}` substitution from body keys, `format!("{}{}", base_url.trim_end_matches('/'), resolved_path)`, `auth.apply()`, GET→query / else→body).
    - SDK trait: `pmcp_code_mode::HttpExecutor` `async fn execute_request(method, path, body) -> Result<Value, ExecutionError>` (executor.rs:2425-2434).
    - DECISION: Pitfall 5 (redaction — ExecutionError must not echo URL/token), Pitfall 4 (gate behind `code-mode` + the `http`/`js-runtime` features), Pitfall 2 (path concat).
  </read_first>
  <action>
    Add `#[cfg(all(feature = "http", feature = "code-mode"))] #[derive(Clone)] pub struct HttpCodeExecutor { client: reqwest::Client, base_url: String, auth: Arc<dyn crate::http::auth::HttpAuthProvider> }` to code_mode.rs. Implement `#[pmcp_code_mode::async_trait] impl pmcp_code_mode::HttpExecutor for HttpCodeExecutor` by lifting the reference `execute_request` body verbatim, swapping `crate::auth::AuthProvider` → `crate::http::auth::HttpAuthProvider`, the error type to `pmcp_code_mode::ExecutionError`, and using the explicit `format!("{}{}", base_url.trim_end_matches('/'), resolved_path)` concat (Pitfall 2). Map auth failures to `ExecutionError::RuntimeError { message }` WITHOUT echoing the URL/token (Pitfall 5 — the reference already avoids the URL; keep it).
    Add a constructor `pub fn new(client, base_url, auth) -> Self`. The `openapi-code-mode` toolkit feature (which forwards `pmcp-code-mode/js-runtime` so `HttpExecutor`/`PlanExecutor`/`PlanCompiler` are in scope) is ALREADY defined by Plan 01 Task 1 — do NOT edit Cargo.toml here; gate `HttpCodeExecutor` behind `#[cfg(all(feature = "http", feature = "code-mode"))]` and run its tests with `--features openapi-code-mode`.
    Per ALWAYS coverage: `http_executor` integration test — wiremock backend, drive `execute_request("GET", "/Line/{id}/Status", body={id:"victoria"})` and assert path substitution + returned JSON; `test_http_code_executor_display_no_secret` (redaction).
  </action>
  <behavior>
    - Test (wiremock): `execute_request("GET", "/users/{id}", {id:"7"})` hits `/users/7` and returns the mock JSON.
    - Test (wiremock): POST sends the JSON body and applies auth.
    - Test: an auth/connect failure surfaces an ExecutionError whose message contains no URL/token.
  </behavior>
  <acceptance_criteria>
    - `grep -n "impl pmcp_code_mode::HttpExecutor for HttpCodeExecutor" crates/pmcp-server-toolkit/src/code_mode.rs` matches.
    - `grep -c "Url::join" crates/pmcp-server-toolkit/src/code_mode.rs` returns 0 (path concat).
    - `cargo test -p pmcp-server-toolkit --features "http code-mode" http_executor -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features "http code-mode" http_executor -- --test-threads=1</automated>
  </verify>
  <done>HttpCodeExecutor round-trips through reqwest + auth with path substitution; ExecutionError redacted; the single shared seam exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize code_mode_tools_from_executor to Arc&lt;dyn CodeExecutor&gt; + flavor (OAPI-10)</name>
  <files>crates/pmcp-server-toolkit/src/code_mode.rs</files>
  <read_first>
    - MODIFY crates/pmcp-server-toolkit/src/code_mode.rs — THREE coupled changes (the analog is the current hardcoded code itself):
      1. `code_mode_tools_from_executor` signature at code_mode.rs:155-187 — param `executor: Arc<SqlCodeExecutor>` (line 158).
      2. `ExecuteCodeHandler.executor` field at code_mode.rs:318-321 (line 320 `Arc<SqlCodeExecutor>`); the `handle` body at :370-374 already calls `self.executor.execute(code, input.variables.as_ref())` through the trait — no body change.
      3. Validation flavor hardcoded `"sql"`: `ValidateCodeHandler::metadata` at code_mode.rs:311 (`CodeModeToolBuilder::new("sql")`), `ValidateCodeHandler::handle` at :273 (`self.pipeline.validate_sql_query(...)`), `ExecuteCodeHandler::metadata` at :379 (`new("sql")`).
    - ANCHOR (must NOT break): the SQL path — `SqlCodeExecutor: CodeExecutor` at :510, and every existing caller of `code_mode_tools_from_executor` (search the workspace: pmcp-sql-server `assemble.rs` `try_code_mode_from_config_with_connector`, and any toolkit tests).
    - RESEARCH OQ-1 + Assumption A2/A3: parameterize executor + flavor in ONE refactor; coupling is local + parameterizable [VERIFIED code_mode.rs:155-381]. SDK exposes `CodeModeToolBuilder::new("openapi")` and a JS validation surface (`JavaScriptValidator`).
    - DECISION: D-02 (one engine, one wiring fn), D-06 (backend-agnostic toolkit).
  </read_first>
  <action>
    Widen `code_mode_tools_from_executor`: change the `executor` param from `Arc<SqlCodeExecutor>` to `Arc<dyn pmcp_code_mode::CodeExecutor>`. Add a `flavor: &str` (or a small `CodeFlavor` enum `Sql | OpenApi`) parameter. Widen `ExecuteCodeHandler.executor` to `Arc<dyn pmcp_code_mode::CodeExecutor>` (the `handle` body is unchanged — it already calls through the `CodeExecutor` trait). Parameterize the validation flavor in BOTH handlers: `CodeModeToolBuilder::new(flavor)` in the two `metadata()` sites, and route validation in `ValidateCodeHandler::handle` to `validate_sql_query` when flavor is SQL and to the JS validation surface (`JavaScriptValidator` / the openapi validate path) when flavor is OpenApi. Keep `ValidationPipeline` construction shared.
    Keep ALL existing SQL call sites compiling: update the SQL caller(s) to pass `Arc::new(sql_code_executor) as Arc<dyn CodeExecutor>` and `flavor = Sql`. Per Phase 85 LOCKED decision, the connector-aware `try_code_mode_from_config_with_connector` stays the production entry point — update it to call the generalized fn with the SQL flavor; the connectorless validation-only path is unchanged.
    Per ALWAYS coverage: keep the existing SQL code-mode tests green (run them); add a unit test `code_mode_generalized_sql_still_works` that wires `code_mode_tools_from_executor` with a `SqlCodeExecutor` + Sql flavor and asserts `validate_code`/`execute_code` tools register; add `code_mode_openapi_flavor_registers` that wires a `JsCodeExecutor<HttpCodeExecutor>` + OpenApi flavor and asserts the two tools register with the openapi-flavored ToolInfo. (The full byte-equality engine-parity proof is Plan 05.)
  </action>
  <behavior>
    - Test: SQL flavor wiring registers `validate_code`/`execute_code` and rejects a write on a read-only config (existing SQL anchor SC-3 behavior preserved).
    - Test: OpenApi flavor wiring with a `JsCodeExecutor<HttpCodeExecutor>` registers the two tools with the `"openapi"` builder flavor.
    - Test: `ExecuteCodeHandler` dispatches through `Arc<dyn CodeExecutor>` regardless of backend.
  </behavior>
  <acceptance_criteria>
    - `grep -n "executor: Arc<dyn .*CodeExecutor>" crates/pmcp-server-toolkit/src/code_mode.rs` matches (no remaining `Arc<SqlCodeExecutor>` in the wiring fn / ExecuteCodeHandler field).
    - `grep -c "CodeModeToolBuilder::new(\"sql\")" crates/pmcp-server-toolkit/src/code_mode.rs` is 0 (flavor parameterized).
    - `cargo test -p pmcp-server-toolkit --features "code-mode" code_mode -- --test-threads=1` exits 0 (SQL tests still green).
    - `cargo test -p pmcp-sql-server -- --test-threads=1` exits 0 (downstream SQL binary path unbroken).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features "http code-mode" code_mode -- --test-threads=1 && cargo test -p pmcp-sql-server -- --test-threads=1</automated>
  </verify>
  <done>One backend-agnostic wiring fn serves SQL + OpenAPI; flavor parameterized; the SQL code-mode path stays green end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code-mode seam → REST backend | LLM/script-driven api.* calls cross to the backend via the shared seam |
| MCP client → ExecutionError | execution errors reach the MCP client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-04-01 | Information Disclosure | `HttpCodeExecutor` ExecutionError `Display` | mitigate | RuntimeError message names operation/status only, never URL/token; `test_http_code_executor_display_no_secret` (Pitfall 5) |
| T-90-04-02 | Malicious Code / EoP | LLM-generated long-tail JS | mitigate | Code Mode path keeps validate_code→token→execute_code gate (`validation_pipeline_from_config`); only the flavor/executor are generalized, NOT the gate |
| T-90-04-03 | DoS | unbounded api.* calls / loops | mitigate | `ExecutionConfig` caps (`max_api_calls`, `max_loop_iterations`, `timeout_seconds`) enforced by PlanExecutor; carried through unchanged |
| T-90-04-04 | Regression | SQL path broken by generalization | mitigate | SQL flavor call sites updated; `cargo test -p pmcp-sql-server` + toolkit SQL code-mode tests gate the refactor |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features "http code-mode" -- --test-threads=1` green (http_executor + code_mode).
- `cargo test -p pmcp-sql-server -- --test-threads=1` green (SQL path unbroken — the OAPI-10 non-regression gate).
- No URL/token leakage; no `Url::join`; no remaining hardcoded `"sql"` flavor.
</verification>

<success_criteria>
- HttpCodeExecutor impls HttpExecutor and round-trips through reqwest + auth (OAPI-05).
- `code_mode_tools_from_executor` is backend-agnostic (`Arc<dyn CodeExecutor>` + flavor) (OAPI-10 / D-02).
- The existing SQL code-mode path stays green (non-regression proven).
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-04-SUMMARY.md`
</output>
