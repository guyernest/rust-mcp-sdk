---
phase: 90-openapi-built-in-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-server-toolkit/src/http/mod.rs
  - crates/pmcp-server-toolkit/src/http/client.rs
  - crates/pmcp-server-toolkit/src/http/auth.rs
  - crates/pmcp-server-toolkit/src/lib.rs
  - crates/pmcp-server-toolkit/Cargo.toml
autonomous: true
requirements: [OAPI-01, OAPI-03]
must_haves:
  truths:
    - "An HttpConnector executes a GET/POST against a mock backend and returns JSON"
    - "All five outgoing-auth variants apply credentials to the outgoing request only"
    - "base_url + path concatenation preserves an API-Gateway stage prefix (/v1)"
    - "HttpConnectorError Display never echoes URL, Authorization, bearer token, or app_key"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/http/mod.rs"
      provides: "HttpConnector trait + HttpConnectorError + Operation re-export"
      contains: "trait HttpConnector"
    - path: "crates/pmcp-server-toolkit/src/http/client.rs"
      provides: "reqwest-backed HttpClient impl with path-concat + retry"
      contains: "impl HttpConnector for HttpClient"
    - path: "crates/pmcp-server-toolkit/src/http/auth.rs"
      provides: "HttpAuthProvider trait + 5 variants + create_auth_provider"
      contains: "trait HttpAuthProvider"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/http/client.rs"
      to: "crates/pmcp-server-toolkit/src/http/auth.rs"
      via: "self.auth.apply(&mut headers, &mut query)"
      pattern: "auth\\.apply"
    - from: "crates/pmcp-server-toolkit/src/lib.rs"
      to: "crates/pmcp-server-toolkit/src/http"
      via: "pub mod http"
      pattern: "pub mod http"
---

<objective>
Lift the genuinely-new HTTP backend primitives from the pmcp-run reference into the
toolkit behind a feature-gated `http` module: the `HttpConnector` trait + reqwest
`HttpClient` impl (OAPI-01) and the outgoing-HTTP `HttpAuthProvider` with all five
auth variants (OAPI-03 / D-05). This is a structural lift — SHAPE from the SQL
connector analog, BODY from the reference HTTP client + auth.

Purpose: Establishes the backend seam the single-call synthesizer (Plan 03), the
code-mode executor (Plan 04), and the binary dispatch (Plan 06) all build on.
Output: `crates/pmcp-server-toolkit/src/http/{mod,client,auth}.rs` + `http` feature.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HttpConnector trait + HttpConnectorError + http feature</name>
  <files>crates/pmcp-server-toolkit/src/http/mod.rs, crates/pmcp-server-toolkit/Cargo.toml, crates/pmcp-server-toolkit/src/lib.rs</files>
  <read_first>
    - CREATE crates/pmcp-server-toolkit/src/http/mod.rs
    - ANALOG (shape — read fully): crates/pmcp-server-toolkit/src/sql/mod.rs — the `#[async_trait] Send + Sync + 'static` trait (`SqlConnector`, lines ~100-142), the `#[non_exhaustive]` thiserror `ConnectorError` enum (lines ~218-272 incl. the Connection Security doc-comment :262-272), and the redaction test `test_connection_display_does_not_echo_password` (:417-426).
    - MODIFY crates/pmcp-server-toolkit/Cargo.toml — `[features]` block at line 57 (`default = ["code-mode"]` :58, `code-mode = [...]` :64, `sqlite = [...]` :68) and `[dependencies]` at line 22.
    - MODIFY crates/pmcp-server-toolkit/src/lib.rs — module decls at lines 29-40 (`pub mod sql;` :36) and re-export block (`pub use crate::sql::{ConnectorError, Dialect, SqlConnector};` :111).
    - SOURCE-OF-TRUTH (Operation model): reference `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/crates/mcp-openapi-server-core/src/schema/parser.rs` for the `Operation` struct shape with `path_parameters()`/`query_parameters()`/`header_parameters()` accessors. (Schema PARSER itself is Plan 03; here define/re-export only the `Operation` type the trait signature needs — define a minimal `Operation { path, method, params }` here if the parser type is not yet present, to be unified by Plan 03.)
    - DECISION: D-05 (5 auth variants), Pitfall 5 (redaction), CONTEXT Claude's-Discretion (HttpConnector as a trait — recommended).
  </read_first>
  <action>
    Own ALL toolkit Cargo.toml dependency + feature changes for this phase IN THIS TASK (so the toolkit Cargo.toml is touched exactly once, in Wave 1 — Plans 03/04/05 treat these deps as already-present). Add a new feature `http = ["dep:reqwest", "dep:url", "dep:openapiv3", "dep:serde_yaml"]` (`async-trait`/`thiserror`/`base64` already present — confirm `base64` is available, add if not, gated under `http`). Add a feature `openapi-code-mode = ["http", "code-mode", "pmcp-code-mode/js-runtime"]` so the HttpCodeExecutor + script-tool + JsCodeExecutor surface (Plan 04/05) has `PlanCompiler`/`PlanExecutor`/`HttpExecutor` in scope; add a `# Why:` comment per the existing convention (line ~59). Add optional deps: `reqwest = { version = "0.13", default-features = false, features = ["json", "rustls-tls", "query"], optional = true }`, `url = { version = "2.5", optional = true }`, `openapiv3 = { <workspace version>, optional = true }`, `serde_yaml = { version = "0.9", optional = true }`, and `regex` (optional, `http`-gated) if the path-param extractor needs it. Add `wiremock = "0.6"` to `[dev-dependencies]` (used by Plans 01/03/04/05 wiremock tests). Do NOT enable `http` or `openapi-code-mode` in `default` (Pitfall 4 — opt-in; curated-only builds stay light).
    Create `src/http/mod.rs` declaring `pub mod client; pub mod auth;` (and a forward `pub mod schema;` stub left for Plan 03 — create an empty `schema.rs` with a module doc-comment so the module tree compiles; Plan 03 fills it). Define `#[async_trait] pub trait HttpConnector: Send + Sync + 'static` with `async fn execute(&self, operation: &Operation, args: &serde_json::Value) -> Result<serde_json::Value, HttpConnectorError>;` and `fn base_url(&self) -> &str;` (analog of `dialect()`). Define a minimal `pub struct Operation { pub path: String, pub method: String, pub parameters: Vec<...> }` with `path_parameters()/query_parameters()/header_parameters()` accessors mirroring the reference; mark with a doc-comment that Plan 03 may extend it from the openapiv3 parse.
    Define `#[non_exhaustive] #[derive(Debug, thiserror::Error)] pub enum HttpConnectorError` with at least `Request`, `Status { status: u16 }`, `Auth`, `InvalidHeader`, `Backend` variants. Copy the SQL Security doc-comment discipline verbatim (adapt wording): implementors MUST NOT include URL, `Authorization`, bearer token, or `app_key` in any inner `String` — the inner `String` reaches MCP clients via `Display`.
    Gate the whole `http` module behind `#[cfg(feature = "http")]` in `lib.rs`: add `#[cfg(feature = "http")] pub mod http;` after `pub mod sql;` and `#[cfg(feature = "http")] pub use crate::http::{HttpConnector, HttpConnectorError, Operation};` after the sql re-export at line 111.
    Per CLAUDE.md ALWAYS coverage: include a unit test `test_http_error_display_does_not_echo_secret` (mirror the SQL redaction test) asserting the rendered `Display` of each error variant contains no `Bearer`, no `Authorization`, no `app_key`, no `https://`.
  </action>
  <behavior>
    - Test: HttpConnectorError::Status{status:401}.to_string() contains "401" but NOT "Bearer"/"Authorization"/"app_key"/"https://".
    - Test: the `http` module compiles only under `--features http` (cfg gate).
    - Test: `Operation::path_parameters()` returns the `{...}` segments of the path.
  </behavior>
  <acceptance_criteria>
    - `crates/pmcp-server-toolkit/src/http/mod.rs` exists and contains `trait HttpConnector` and `enum HttpConnectorError`.
    - `grep -n "http = \[" crates/pmcp-server-toolkit/Cargo.toml` shows the new feature; `default` does NOT contain `http`.
    - `cargo test -p pmcp-server-toolkit --features http http::mod -- --test-threads=1` exits 0 (incl. `test_http_error_display_does_not_echo_secret`).
    - `cargo build -p pmcp-server-toolkit` (no http feature) still compiles (gate is clean).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features http display_no_secret -- --test-threads=1</automated>
  </verify>
  <done>HttpConnector trait + redacting HttpConnectorError + http feature land; default build unaffected; redaction test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Outgoing HttpAuthProvider — all 5 variants (OAPI-03 / D-05)</name>
  <files>crates/pmcp-server-toolkit/src/http/auth.rs</files>
  <read_first>
    - CREATE crates/pmcp-server-toolkit/src/http/auth.rs
    - CONTRAST analog (DO NOT copy — Pitfall 1): crates/pmcp-server-toolkit/src/auth.rs — the INBOUND `AuthProvider::validate_request`. The new provider is OUTBOUND `apply(&mut HeaderMap, &mut query)`.
    - REFERENCE BODY to lift (5-variant enum, near-verbatim): `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/crates/mcp-openapi-server-core/src/config.rs:164-235` (the `AuthConfig` enum: `None | ApiKey{query_params,headers,required} | Bearer{token,required} | Basic{username,password,required} | OAuth2ClientCredentials{...} | OAuthPassthrough{...}`, plus `default_true`/`default_auth_header`).
    - REFERENCE BODY (apply impls + constructors): `.../mcp-openapi-server-core/src/auth/mod.rs` — `create_auth_provider(&AuthConfig) -> Arc<dyn AuthProvider>` (:26-30), `create_passthrough_auth_provider(config, incoming_token)` (:47-53), and the 5 `apply()` impls re-exported from `mcp_server_common::auth` (INLINE those impls here — the lift replaces the `mcp_server_common` path-dep). The 5 unit tests at `.../auth/mod.rs:104-152` are directly liftable.
    - DECISION: D-05 (all 5 variants now), Pitfall 1 (name distinctly), Pitfall 5 (redaction).
  </read_first>
  <action>
    Create `src/http/auth.rs` gated behind `#[cfg(feature = "http")]`. Define `#[async_trait] pub trait HttpAuthProvider: Send + Sync + 'static` with `async fn apply(&self, headers: &mut reqwest::header::HeaderMap, query: &mut std::collections::HashMap<String, String>) -> Result<(), HttpConnectorError>;` — DISTINCT name from the inbound `AuthProvider` to avoid conflation (Pitfall 1).
    Lift the 5 concrete providers from the reference, inlining the `mcp_server_common::auth` `apply()` bodies: `NoAuth`, `ApiKeyAuth { query_params, headers, required }`, `BearerAuth { token, required }`, `BasicAuth { username, password, required }` (base64-encode `username:password` via the workspace `base64`), `OAuth2ClientCredentialsAuth { token_url, client_id, client_secret, scopes }`, and `OAuthPassthroughAuth { target_header, incoming_token }`. Provide `pub fn create_auth_provider(cfg: &AuthConfig) -> Result<Arc<dyn HttpAuthProvider>, HttpConnectorError>` and `pub fn create_passthrough_auth_provider(cfg: &AuthConfig, incoming_token: Option<String>) -> Result<Arc<dyn HttpAuthProvider>, HttpConnectorError>`.
    Use the `AuthConfig` enum type — it is OWNED by Plan 02 (config.rs). To keep Plan 01 and Plan 02 parallel without a file conflict, DEFINE `AuthConfig` in THIS file (`http::auth`) with the reference `#[serde(tag="type", rename_all="snake_case")]` shape + `Default = None`; Plan 02's `[backend.auth]` config will re-export `pmcp_server_toolkit::http::auth::AuthConfig` rather than redefine it. Document this ownership in a module doc-comment.
    Reuse `HttpConnectorError` (from Task 1) for all error paths; never echo the token/secret (Pitfall 5).
    Per ALWAYS coverage: lift the reference unit tests (`test_no_auth`, `test_bearer_auth`, `test_basic_auth`, plus add `test_api_key_query_param` for the london-tube D-04 path and `test_passthrough_forwards_token`). Add `test_auth_error_display_no_secret`.
  </action>
  <behavior>
    - Test: ApiKeyAuth with `query_params={app_key: X}` puts `app_key=X` into the query map and NOTHING into headers.
    - Test: BearerAuth sets `Authorization: Bearer <token>` header; query untouched.
    - Test: BasicAuth sets `Authorization: Basic <b64(user:pass)>`.
    - Test: OAuthPassthrough forwards the captured incoming token into `target_header`.
    - Test: error Display contains no token/app_key substring.
  </behavior>
  <acceptance_criteria>
    - `crates/pmcp-server-toolkit/src/http/auth.rs` contains `trait HttpAuthProvider`, `enum AuthConfig`, `create_auth_provider`, `create_passthrough_auth_provider`, and all 5 variant structs.
    - `cargo test -p pmcp-server-toolkit --features http http_auth -- --test-threads=1` exits 0 (≥5 variant tests + redaction test).
    - `grep -c "validate_request" crates/pmcp-server-toolkit/src/http/auth.rs` returns 0 (no inbound-auth conflation, Pitfall 1).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features http http_auth -- --test-threads=1</automated>
  </verify>
  <done>Five outgoing-auth variants apply to the outgoing request only; AuthConfig owned here for Plan 02 re-export; redaction proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: reqwest HttpClient impl with path-concat + retry (OAPI-01)</name>
  <files>crates/pmcp-server-toolkit/src/http/client.rs, crates/pmcp-server-toolkit/tests/http_connector_props.rs</files>
  <read_first>
    - CREATE crates/pmcp-server-toolkit/src/http/client.rs
    - ANALOG (shape — a concrete connector impl): crates/pmcp-server-toolkit/src/sql/sqlite.rs (the `impl SqlConnector for SqliteConnector` + constructor + spawn pattern).
    - REFERENCE BODY to lift (read fully — 554 lines, liftable): `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/crates/mcp-openapi-server-core/src/http/mod.rs` — `HttpClient` / `execute_with_options`. KEY ranges: path-concat (NOT `Url::join`) at :212-218; header injection guards (`HeaderName/HeaderValue::try_from`) at :137-143; auth application at :146; retry/backoff `execute_with_retries` at :329-384; the regression test `test_build_url_with_path_prefix` at :507-527.
    - DECISION: Pitfall 2 (path concat preserves `/v1`), Pitfall 5 (redaction), CF-2 (lazy startup — client constructed without contacting backend).
  </read_first>
  <action>
    Create `src/http/client.rs` gated `#[cfg(feature = "http")]`. Define `pub struct HttpClient { client: reqwest::Client, base_url: url::Url, auth: Arc<dyn HttpAuthProvider>, http_config: HttpConfig }` with `pub fn new(client: reqwest::Client, base_url: String, auth: Arc<dyn HttpAuthProvider>) -> Result<Self, HttpConnectorError>` (parse base_url to `url::Url`; lazy — no network, CF-2). `HttpConfig` is owned by Plan 02 — DEFINE it here in `http` (mirror `AuthConfig` ownership decision in Task 2) with `{ timeout_seconds, retries, retry_backoff_ms, user_agent, default_headers }` + serde defaults, so Plan 02 re-exports it.
    Lift the reference `execute_with_options` body into `impl HttpConnector for HttpClient::execute`: build the URL via explicit path-concat (lift :212-218 verbatim — `base_path.trim_end_matches('/')` + `tool_path.trim_start_matches('/')`, NOT `Url::join`); extract path params from the `Operation` and substitute from `args`; split remaining args into query/body by method; guard header names/values with `HeaderName::try_from`/`HeaderValue::try_from` (:137-143); call `self.auth.apply(&mut headers, &mut query).await?` (:146); send via `execute_with_retries` (lift :329-384 — exponential backoff on 5xx/connect/timeout). Map non-2xx to `HttpConnectorError::Status { status }` and reqwest errors to `Request` WITHOUT echoing the URL (Pitfall 5). Implement `fn base_url(&self) -> &str`.
    Per ALWAYS coverage: lift `test_build_url_with_path_prefix` (proves `/v1` survives), add a wiremock integration test `http_connector` that does a GET and a POST against a `wiremock::MockServer` and asserts the returned JSON (add `wiremock = "0.6"` to `[dev-dependencies]` of the toolkit). Add a property test seam noted for Plan 03 (the proptest URL-building target lives in `tests/http_connector_props.rs`, created in Plan 03's Wave-0 step or here — create the file here with a proptest over `(base_path, tool_path)` asserting no double-slash and prefix preservation).
  </action>
  <behavior>
    - Test (regression): base_url `https://x/v1` + path `/users` builds `https://x/v1/users` (prefix preserved, NOT dropped).
    - Test (wiremock): GET against mock returns the mocked JSON body as a Value.
    - Test (wiremock): POST sends the body and applies auth headers/query.
    - Property: for any `(base, leaf)` the joined path has exactly one slash between segments and never drops `base`'s non-root path.
  </behavior>
  <acceptance_criteria>
    - `crates/pmcp-server-toolkit/src/http/client.rs` contains `impl HttpConnector for HttpClient` and the explicit path-concat (NOT `Url::join`).
    - `grep -c "Url::join\|\.join(" crates/pmcp-server-toolkit/src/http/client.rs` returns 0.
    - `cargo test -p pmcp-server-toolkit --features http http_connector -- --test-threads=1` exits 0 (wiremock GET+POST + prefix regression test).
    - `wiremock` present in toolkit `[dev-dependencies]`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features http http_connector -- --test-threads=1</automated>
  </verify>
  <done>HttpClient executes GET/POST against a wiremock backend; stage-prefix preserved; no URL/token leakage; lazy construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toolkit → REST backend | outgoing HTTP request carries operator-configured credentials |
| MCP client → toolkit error surface | `HttpConnectorError` Display reaches the MCP client |
| tool args → outgoing request | client-supplied args become path/query/header/body values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01-01 | Information Disclosure | `HttpConnectorError`/auth errors `Display` | mitigate | Security doc-comment + `test_*_display_no_secret`: never echo URL, `Authorization`, bearer token, `app_key` (Pitfall 5, mirrors sql/mod.rs:262-272) |
| T-90-01-02 | Tampering | inbound vs outbound auth conflation | mitigate | Name the outbound trait `HttpAuthProvider` (`apply`), keep inbound `AuthProvider::validate_request` separate; grep asserts no `validate_request` in http/auth.rs (Pitfall 1) |
| T-90-01-03 | Tampering | header injection via tool args | mitigate | `HeaderName::try_from`/`HeaderValue::try_from` reject invalid header names/values (ref http/mod.rs:138) |
| T-90-01-04 | Tampering/EoP | SSRF via attacker-controlled base_url | accept | `base_url` is operator-config only, never client-supplied; documented invariant. Low risk for v1. |
| T-90-01-05 | correctness boundary | API-Gateway stage-prefix drop | mitigate | Explicit path-concat, not `Url::join` (Pitfall 2); regression test `test_build_url_with_path_prefix` |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features http -- --test-threads=1` green (mod + http_auth + http_connector).
- `cargo build -p pmcp-server-toolkit` (default, no http) still compiles.
- No `Url::join`, no `validate_request`, no credential echo in the http module.
</verification>

<success_criteria>
- HttpConnector trait + HttpClient impl execute GET/POST against a wiremock backend (OAPI-01).
- All five outgoing-auth variants apply to the outgoing request only (OAPI-03 / D-05).
- Redaction proven across all new error variants; stage-prefix preserved.
- `http` is an opt-in feature; default toolkit build is unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-01-SUMMARY.md`
</output>
