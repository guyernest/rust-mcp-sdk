---
phase: 90-openapi-built-in-server
plan: 03
type: execute
wave: 2
depends_on: [90-01, 90-02]
files_modified:
  - crates/pmcp-server-toolkit/src/http/schema.rs
  - crates/pmcp-server-toolkit/src/tools.rs
  - crates/pmcp-server-toolkit/src/lib.rs
autonomous: true
requirements: [OAPI-04, OAPI-02a]
must_haves:
  truths:
    - "An OpenAPI 3.0/3.1 spec parses from JSON and from YAML into an Operation set"
    - "Spec parsing is optional — a curated-only server needs no spec (D-03)"
    - "A single-call [[tools]] (path/method) synthesizes into a ToolInfo + handler"
    - "The single-call handler calls HttpConnector::execute and returns JSON"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/http/schema.rs"
      provides: "OpenApiSchema parser (openapiv3 + serde_yaml fallback) + Operation accessors"
      contains: "OpenApiSchema"
    - path: "crates/pmcp-server-toolkit/src/tools.rs"
      provides: "synthesize_from_config_with_http_connector + single-call handler"
      contains: "synthesize_from_config_with_http_connector"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/tools.rs"
      to: "crates/pmcp-server-toolkit/src/http/mod.rs"
      via: "HttpConnector::execute(&operation, &args)"
      pattern: "\\.execute\\("
---

<objective>
Lift the openapiv3 `--spec` parser (OAPI-04 / D-03 — optional at runtime) and add
the single-call tool synthesizer (OAPI-02a) that maps a `path`/`method` `[[tools]]`
entry to a `ToolInfo` + a handler calling `HttpConnector::execute`. SHAPE from the
SQL synth analog, BODY from the reference schema parser + `create_tool_from_config`.

Purpose: Completes the curated single-call surface and the spec-as-resource input.
Output: `http/schema.rs` parser + the single-call branch in `tools.rs`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
@.planning/phases/90-openapi-built-in-server/90-01-SUMMARY.md
@.planning/phases/90-openapi-built-in-server/90-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenApiSchema parser — openapiv3 + YAML, optional at runtime (OAPI-04 / D-03)</name>
  <files>crates/pmcp-server-toolkit/src/http/schema.rs</files>
  <read_first>
    - CREATE/FILL crates/pmcp-server-toolkit/src/http/schema.rs (stub created in Plan 01).
    - ROLE analog: crates/pmcp-server-toolkit/src/sql/mod.rs::schema_text (the thing that produces the code-mode schema resource).
    - REFERENCE BODY to lift: `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/crates/mcp-openapi-server-core/src/schema/parser.rs` — openapiv3 parse (`parser.rs:9`), `serde_yaml` YAML fallback (`parser.rs:30`), and the `Operation` type with `path_parameters()`/`query_parameters()`/`header_parameters()` accessors (consumed by the client at ref http/mod.rs:188,230,264).
    - DECISION: D-03 (spec OPTIONAL at runtime — `OpenApiSchema::parse` returns into an `Option<OpenApiSchema>` threaded by the binary; required only at scaffold time). Contrast SQL `--schema` which is effectively required (cli.rs:46-47).
    - DEPENDS: Plan 01 defined a minimal `Operation` in `http/mod.rs` — UNIFY it here (the parser is the authoritative producer of `Operation`; reconcile the minimal stub with the openapiv3-derived shape, keeping the trait signature in `http/mod.rs` stable).
  </read_first>
  <action>
    Fill `src/http/schema.rs` (gated `#[cfg(feature = "http")]`). `openapiv3` + `serde_yaml` are ALREADY `http`-gated deps (added by Plan 01 Task 1 — do NOT edit Cargo.toml here). Define `pub struct OpenApiSchema` wrapping the parsed `openapiv3::OpenAPI` plus an indexed operation map. Provide `pub fn parse(text: &str) -> Result<OpenApiSchema, HttpConnectorError>` that tries JSON then falls back to YAML (`serde_yaml`), and `pub fn parse_path(path: &Path) -> Result<OpenApiSchema, HttpConnectorError>`. Provide `operations() -> &[Operation]` / `operation_for(path, method) -> Option<&Operation>` and the spec text accessor for the code-mode `api_schema` resource (D-03 surface (a)).
    Reconcile `Operation`: keep the `Operation` type and its `path_parameters()`/`query_parameters()`/`header_parameters()` accessors authoritative HERE; re-export from `http::mod` so the trait signature in Plan 01 stays valid. Path params are the `{...}` segments; query/header params come from the openapiv3 parameter locations.
    Thread optionality: the parser is never called unless a spec is provided — document that `OpenApiSchema` is always wrapped `Option<>` by the binary (D-03). A curated-only server boots with `None`.
    Per ALWAYS coverage: lift the reference parser tests; add `schema_parse` unit tests parsing a minimal OpenAPI doc from BOTH JSON and YAML and asserting an operation's path/method/params; add `test_schema_parse_error_display_no_secret` (error redaction, though specs carry no creds, keep the discipline consistent).
  </action>
  <behavior>
    - Test: a minimal OpenAPI 3.0 JSON doc parses; `operation_for("/users/{id}", "GET")` returns an Operation whose `path_parameters()` == ["id"].
    - Test: the same doc in YAML parses identically.
    - Test: malformed spec returns a typed `HttpConnectorError`, no panic.
  </behavior>
  <acceptance_criteria>
    - `crates/pmcp-server-toolkit/src/http/schema.rs` contains `OpenApiSchema` and `pub fn parse`.
    - `openapiv3` + `serde_yaml` are `http`-gated deps.
    - `cargo test -p pmcp-server-toolkit --features http schema_parse -- --test-threads=1` exits 0 (JSON + YAML + params).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features http schema_parse -- --test-threads=1</automated>
  </verify>
  <done>OpenAPI specs parse from JSON+YAML; Operation accessors land; spec is runtime-optional (D-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-call tool synthesizer + handler (OAPI-02a)</name>
  <files>crates/pmcp-server-toolkit/src/tools.rs, crates/pmcp-server-toolkit/src/lib.rs, crates/pmcp-server-toolkit/tests/http_connector_props.rs</files>
  <read_first>
    - MODIFY crates/pmcp-server-toolkit/src/tools.rs ; MODIFY crates/pmcp-server-toolkit/src/lib.rs (re-export).
    - ANALOG (shape): `synthesize_from_config_with_connector` at tools.rs:112-117 + the shared `synthesize_inner` it delegates to, and `SynthesizedToolHandler` (the `#[async_trait] impl ToolHandler { handle; metadata }` pattern). Invariants at tools.rs:11-28; helpers `build_input_schema`/`build_param_property`/`build_annotations`.
    - REFERENCE BODY to lift (single-call operation building): `.../mcp-openapi-server-core/src/tools/mod.rs::create_tool_from_config` (single-call ONLY — reference has no script tools). Path params from `"{...}"` segments; inputs → query/path schema; POST/PUT/PATCH → request_body.
    - DECISION: D-01 (detection — THIS task handles `path`+`method` only; the `script` branch is Plan 05), Pitfall — `ToolInfo`/`ToolAnnotations` are `#[non_exhaustive]`: use constructors / the synth path, never struct-literals.
  </read_first>
  <action>
    Add `pub fn synthesize_from_config_with_http_connector(cfg: &ServerConfig, connector: Arc<dyn HttpConnector>) -> Result<Vec<SynthesizedTool>, ToolkitError>` (gated `#[cfg(feature = "http")]`), mirroring the SQL `synthesize_from_config_with_connector` shape. For each `[[tools]]` where `!tool_cfg.is_script_tool()` and `path`+`method` are present, build an `Operation` from `path`/`method`/`base_url` + `[[tools.parameters]]` (lift the reference `create_tool_from_config` operation-building body), build the `ToolInfo` via the EXISTING `build_input_schema` (object envelope + `additionalProperties:false`) and `build_annotations` helpers, and produce a handler that calls `connector.execute(&operation, &args)` and returns the JSON.
    Leave a clearly-marked detection point for the `script` branch (Plan 05 fills the `is_script_tool()` arm). For THIS plan, a `script` tool encountered here returns a clear "script tools require Plan 05 wiring" path OR is simply skipped with the branch stubbed — prefer: detect both, route `script` to a `todo!`-free explicit `ToolkitError` until Plan 05, and document the seam. (Plan 05 replaces that arm with `ScriptToolHandler`.)
    Re-export `pub use crate::tools::synthesize_from_config_with_http_connector;` from lib.rs (gated `http`), mirroring the existing `synthesize_from_config_with_connector` re-export at lib.rs:97.
    Per ALWAYS coverage: `synth_http` unit tests — a single-call `[[tools]]` with `{id}` path param synthesizes a `ToolInfo` whose input schema marks `id` required (object envelope), and the handler (wired to a mock `HttpConnector`) returns the mocked JSON. Add a proptest over path-param substitution (declared `{params}` only) in `tests/http_connector_props.rs` (extend the file from Plan 01).
  </action>
  <behavior>
    - Test: a `[[tools]]` `path="/Line/{id}/Status"` + `method="GET"` synthesizes a tool requiring `id`; calling it with a mock connector returns the mock JSON.
    - Test: POST tool routes non-path args to the request body.
    - Property: only declared `{params}` are substituted into the path; undeclared keys never reach the URL path.
  </behavior>
  <acceptance_criteria>
    - `grep -n "synthesize_from_config_with_http_connector" crates/pmcp-server-toolkit/src/tools.rs crates/pmcp-server-toolkit/src/lib.rs` matches in both.
    - `grep -c "ToolInfo {" crates/pmcp-server-toolkit/src/tools.rs` shows no new struct-literal (uses constructors/synth helpers).
    - `cargo test -p pmcp-server-toolkit --features http synth_http -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features http synth_http -- --test-threads=1</automated>
  </verify>
  <done>Single-call [[tools]] synthesize to ToolInfo + handler over HttpConnector; script arm seam left for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator OpenAPI spec → parser | admin-authored spec text deserialized |
| MCP client tool args → path/query | client args become path-param / query values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-03-01 | Tampering | arg injection into path/query | mitigate | JSON-Schema object envelope + `additionalProperties:false`; path substitution only for declared `{params}` (proptest) |
| T-90-03-02 | DoS | maliciously huge / cyclic spec | accept | openapiv3 handles `$ref` resolution; spec is admin-authored, parsed once at startup; low risk |
| T-90-03-03 | Information Disclosure | parser error echoing spec internals | mitigate | typed `HttpConnectorError`; `test_schema_parse_error_display_no_secret` keeps redaction discipline |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features http schema_parse synth_http -- --test-threads=1` green.
- No struct-literal `ToolInfo`/`ToolAnnotations`; no undeclared path-param substitution (proptest).
</verification>

<success_criteria>
- OpenAPI specs parse from JSON+YAML, runtime-optional (OAPI-04 / D-03).
- Single-call `[[tools]]` synthesize to ToolInfo + handler over HttpConnector (OAPI-02a).
- Script-tool detection seam left explicitly for Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-03-SUMMARY.md`
</output>
