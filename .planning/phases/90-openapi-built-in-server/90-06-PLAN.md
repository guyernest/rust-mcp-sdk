---
phase: 90-openapi-built-in-server
plan: 06
type: execute
wave: 4
depends_on: [90-03, 90-04, 90-05]
files_modified:
  - crates/pmcp-openapi-server/Cargo.toml
  - crates/pmcp-openapi-server/src/main.rs
  - crates/pmcp-openapi-server/src/lib.rs
  - crates/pmcp-openapi-server/src/cli.rs
  - crates/pmcp-openapi-server/src/dispatch.rs
  - crates/pmcp-openapi-server/src/assemble.rs
  - Cargo.toml
autonomous: true
requirements: [OAPI-06]
must_haves:
  truths:
    - "The pmcp-openapi-server binary serves a config-driven OpenAPI MCP server over streamable HTTP"
    - "--spec is optional at runtime; a curated-only config boots without a spec (D-03)"
    - "dispatch builds the HttpConnector + HttpCodeExecutor pair lazily (no backend contact at startup, CF-2)"
    - "build_server wires single-call tools, script tools, and Code Mode via the toolkit"
  artifacts:
    - path: "crates/pmcp-openapi-server/src/lib.rs"
      provides: "run/run_serving/load_config_and_spec/serve pipeline"
      contains: "load_config_and_spec"
    - path: "crates/pmcp-openapi-server/src/dispatch.rs"
      provides: "dispatch → (Arc<dyn HttpConnector>, HttpCodeExecutor)"
      contains: "HttpCodeExecutor"
    - path: "crates/pmcp-openapi-server/src/assemble.rs"
      provides: "build_server (toolkit assemble pattern, NOT reference pmcp_server.rs)"
      contains: "build_server"
  key_links:
    - from: "crates/pmcp-openapi-server/src/lib.rs"
      to: "pmcp::StreamableHttpServer"
      via: "serve over streamable HTTP (CF-1)"
      pattern: "StreamableHttpServer"
    - from: "crates/pmcp-openapi-server/src/assemble.rs"
      to: "pmcp-server-toolkit"
      via: "ServerBuilderExt + synthesize_from_config_with_http_connector + code_mode_tools_from_executor"
      pattern: "ServerBuilderExt"
---

<objective>
Build the `pmcp-openapi-server` Shape A binary (OAPI-06) — a structural copy of
`pmcp-sql-server`: `run → load_config_and_spec → dispatch → build_server → serve`
over streamable HTTP (CF-1), with `--spec` OPTIONAL at runtime (D-03) and lazy
startup (CF-2). Do NOT lift the reference `pmcp_server.rs` (Pitfall 6) — assemble
via the toolkit's `assemble.rs` pattern + `ServerBuilderExt`.

Purpose: The pure-config binary — a non-developer points it at config.toml (+ optional
spec) and gets a live MCP server with zero Rust written.
Output: `crates/pmcp-openapi-server` (5-file crate) added to the workspace.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
@.planning/phases/90-openapi-built-in-server/90-03-SUMMARY.md
@.planning/phases/90-openapi-built-in-server/90-04-SUMMARY.md
@.planning/phases/90-openapi-built-in-server/90-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the crate — Cargo.toml + cli.rs + main.rs (workspace member)</name>
  <files>crates/pmcp-openapi-server/Cargo.toml, crates/pmcp-openapi-server/src/cli.rs, crates/pmcp-openapi-server/src/main.rs, Cargo.toml</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/{Cargo.toml, src/cli.rs, src/main.rs}; MODIFY root Cargo.toml `[workspace.members]`.
    - ANALOG (exact): crates/pmcp-sql-server/Cargo.toml (deps + features + `exclude`), crates/pmcp-sql-server/src/cli.rs (`Args` struct + tests — `config: PathBuf` required :44, `schema: PathBuf` required :46-47, `http: String` default `"127.0.0.1:8080"` :50-51), crates/pmcp-sql-server/src/main.rs (492-byte `#[tokio::main]` shim → `run(Args::parse())`).
    - RESEARCH "Installation" deps: `pmcp` w/ `streamable-http`, `pmcp-server-toolkit` w/ `["code-mode","http"]`, `clap` (`derive`,`env`), `tokio` (`macros`,`rt-multi-thread`), `thiserror`, `tracing`+`tracing-subscriber`; dev: `mcp-tester` 0.7.0, `wiremock` 0.6.
    - DECISION: D-03 (`--spec` OPTIONAL — `Option<PathBuf>`, drop requiredness), CF-1 (streamable HTTP), CLAUDE.md publish slot (after toolkit + connectors).
  </read_first>
  <action>
    Create `crates/pmcp-openapi-server/Cargo.toml` mirroring `pmcp-sql-server/Cargo.toml`: enable `pmcp-server-toolkit` features `["code-mode", "http"]` (drop `sqlite`/connector features). Default-on code-mode (Pitfall 4 — opt-out via `--no-default-features`). Add `exclude = [tests/, .planning/, .pmat/, fuzz/]` to keep fixtures out of the published crate (mirror sql-server). Add `crates/pmcp-openapi-server` to root `Cargo.toml` `[workspace.members]`.
    Create `src/cli.rs` copying the sql-server `Args` struct + its tests, changing ONLY: `pub schema: PathBuf` (required) → `pub spec: Option<PathBuf>` (OPTIONAL, D-03 — doc "required at scaffold time, optional at runtime"); `name`/`about` strings → openapi-server. Keep `config: PathBuf` (required) and `http: String` default `"127.0.0.1:8080"` unchanged.
    Create `src/main.rs` as the thin `#[tokio::main]` shim calling `pmcp_openapi_server::run(Args::parse())` (copy sql-server main verbatim, rename crate).
    Coverage: keep the cli arg-parse tests (mirror sql-server's).
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/Cargo.toml` lists `pmcp-server-toolkit` with `["code-mode","http"]` and `crates/pmcp-openapi-server` is in root `[workspace.members]`.
    - `grep -n "pub spec: Option<PathBuf>" crates/pmcp-openapi-server/src/cli.rs` matches (OPTIONAL, D-03).
    - `cargo build -p pmcp-openapi-server` compiles (after Tasks 2-3 land the lib).
    - `cargo test -p pmcp-openapi-server cli -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-openapi-server cli -- --test-threads=1</automated>
  </verify>
  <done>Crate scaffolded as a workspace member; --spec optional; deps mirror sql-server.</done>
</task>

<task type="auto">
  <name>Task 2: dispatch.rs — build the (HttpConnector, HttpCodeExecutor) pair lazily (CF-2)</name>
  <files>crates/pmcp-openapi-server/src/dispatch.rs</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/src/dispatch.rs.
    - ANALOG (shape + redaction): crates/pmcp-sql-server/src/dispatch.rs (read fully) — the `DispatchError` `#[non_exhaustive]` enum + Security doc-comment (dispatch.rs:42-103, Display names backend/field ONLY), and the lazy-startup discipline (no `schema_text()`/`execute()` at dispatch time, dispatch.rs:113-115). The SQL version returns a single `Arc<dyn SqlConnector>`; the OpenAPI version returns BOTH (partial-analog, the one "No Analog Found" item).
    - SOURCE (the seam shape): 90-RESEARCH.md "Dispatch seam" code block (lines ~660-674) + 90-PATTERNS.md dispatch.rs section.
    - DECISION: CF-2 (lazy — reqwest client built without contacting the backend), D-05 (5 auth variants via `create_auth_provider`), Pitfall 5 (redaction).
  </read_first>
  <action>
    Implement `pub async fn dispatch(cfg: &ServerConfig) -> Result<(Arc<dyn HttpConnector>, HttpCodeExecutor), DispatchError>`: read `cfg.backend` (error `DispatchError::MissingBackend` if absent), build `auth = toolkit::http::auth::create_auth_provider(&backend.auth)?` (5 variants), build a shared `reqwest::Client` (lazy — no network, CF-2), construct `HttpClient::new(client.clone(), backend.base_url.clone(), auth.clone())?` and `HttpCodeExecutor::new(client, backend.base_url.clone(), auth)`, return the pair.
    Copy the SQL `DispatchError` `#[non_exhaustive]` enum + its Security doc-comment (V7 / Pitfall 5 — Display names backend/field ONLY, never URL/credential). Add a `display_no_secret` test for `DispatchError`.
    Coverage: a unit test asserting `dispatch` on a london-tube-shaped `[backend]` config returns the pair WITHOUT any network call (offline / fast — CF-2), and `MissingBackend` when `[backend]` is absent.
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/src/dispatch.rs` returns `(Arc<dyn HttpConnector>, HttpCodeExecutor)`.
    - `grep -c "schema_text\|\.execute(" crates/pmcp-openapi-server/src/dispatch.rs` returns 0 (lazy, CF-2).
    - `cargo test -p pmcp-openapi-server dispatch -- --test-threads=1` exits 0 (offline + MissingBackend + display_no_secret).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-openapi-server dispatch -- --test-threads=1</automated>
  </verify>
  <done>dispatch builds the connector+executor pair lazily; DispatchError redacts; MissingBackend handled.</done>
</task>

<task type="auto">
  <name>Task 3: assemble.rs + lib.rs — build_server + run pipeline over streamable HTTP (D-03/CF-1)</name>
  <files>crates/pmcp-openapi-server/src/assemble.rs, crates/pmcp-openapi-server/src/lib.rs, crates/pmcp-openapi-server/examples/openapi_server_min.rs</files>
  <read_first>
    - CREATE crates/pmcp-openapi-server/src/{assemble.rs, lib.rs}.
    - ANALOG (assemble): crates/pmcp-sql-server/src/assemble.rs (read first 200 lines) — `AssembleError` (:71-83, `Toolkit(#[from])` + `Build(#[from] pmcp::Error)`), the `ServerBuilderExt` single crate-root import (:42-44), `merge_schema_resource` (:98-152, the `/schema`-suffix override at :111), the synthesized `code-mode://instructions`/`code-mode://policies` resources (:155-200). PITFALL 6: do NOT lift the reference `pmcp_server.rs`.
    - ANALOG (lib): crates/pmcp-sql-server/src/lib.rs (read fully) — `RunError` (:52-97; rename `schema`→`spec`), `load_config_and_schema` (:108-119) → `load_config_and_spec` returning `(ServerConfig, Option<OpenApiSchema>)` (D-03 — `args.spec.as_ref().map(parse)` not an unconditional read), `serve` (:156-164, copy VERBATIM — `StreamableHttpServer::with_config(default())`, CF-1), `run_serving` (:200-211), `run` (:245-265, copy VERBATIM incl. `handle.await.map_err(RunError::Serving)`, threat T-85-10-02).
    - DEPENDS: Plan 03 (`synthesize_from_config_with_http_connector` + single-call), Plan 04 (`HttpCodeExecutor` + generalized `code_mode_tools_from_executor` + `JsCodeExecutor`), Plan 05 (`ScriptToolHandler` branch).
    - DECISION: D-03 (spec optional — when `None` skip the spec-resource override; when `Some` merge it as the `api_schema` resource), CF-1 (streamable HTTP only), D-02 (one engine — the SAME `HttpCodeExecutor` feeds the synthesizer's script branch AND the JsCodeExecutor Code Mode wiring).
  </read_first>
  <action>
    Implement `assemble.rs::build_server(cfg, connector, http_exec, spec: Option<OpenApiSchema>) -> Result<pmcp::Server, AssembleError>`: via `ServerBuilderExt`, register single-call + script tools through `synthesize_from_config_with_http_connector(cfg, connector, http_exec.clone(), exec_config)` (Plan 03/05 — the synthesizer routes `is_script_tool()` to `ScriptToolHandler` over the shared `http_exec`), wire Code Mode via the generalized `code_mode_tools_from_executor(builder, cfg, Arc::new(JsCodeExecutor::new(http_exec.clone(), exec_config)), OpenApi-flavor)` (Plan 04 — the SAME `http_exec` instance, D-02), preserve configured resources/prompts (mirror sql-server `merge_schema_resource` + `StaticPromptHandler::from_configs`), and when `spec.is_some()` merge the OpenAPI spec as the `api_schema`/`/schema` resource (D-03 surface (a); skip the override when `None`).
    Implement `lib.rs`: `RunError` (rename schema→spec), `load_config_and_spec(args)` → `(ServerConfig::from_toml_strict_validated, Option<OpenApiSchema>)` with the spec parsed only when `args.spec` is `Some` (D-03), `serve` copied VERBATIM from sql-server (`StreamableHttpServer`, CF-1), `run_serving` (load → dispatch → build_server → parse addr → serve), `run` copied VERBATIM (tracing init + `handle.await.map_err(RunError::Serving)`). Expose `run_serving` as a testable seam returning `(bound_addr, handle)` (mirror sql-server's Codex-HIGH-#5 pattern) so the smoke test drives the real binary path.
    Per ALWAYS coverage: an `http_smoke` integration test (mirror SQL SC-1) that drives `run_serving` against a curated-only config (NO spec — D-03 proof) over a wiremock backend, confirms the server binds + serves over streamable HTTP, and that a tools/list reflects the configured tools. Add a runnable example `examples/openapi_server_min.rs` (≤15-line wiring shape, CF-5) for the ALWAYS "cargo run --example" requirement.
  </action>
  <acceptance_criteria>
    - `crates/pmcp-openapi-server/src/lib.rs` contains `load_config_and_spec` returning an `Option<OpenApiSchema>` and `serve` using `StreamableHttpServer`.
    - `grep -c "mcp_server_common::\|OpenApiPmcpBuilder" crates/pmcp-openapi-server/src/assemble.rs` returns 0 (Pitfall 6 — toolkit assemble, not reference builder).
    - `cargo test -p pmcp-openapi-server http_smoke -- --test-threads=1` exits 0 (curated-only / no-spec boot + serve).
    - `cargo run -p pmcp-openapi-server --example openapi_server_min` builds/runs (ALWAYS example).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-openapi-server http_smoke -- --test-threads=1</automated>
  </verify>
  <done>The binary serves a config-driven OpenAPI MCP server over streamable HTTP; spec optional; one engine feeds tools+code-mode; runnable example present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → binary transport | streamable-HTTP requests (CORS/DNS-rebinding handled by the SDK) |
| binary → REST backend | outgoing requests via the dispatched connector/executor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-06-01 | Information Disclosure | `DispatchError`/`RunError`/`AssembleError` Display | mitigate | Copy SQL redaction discipline; `display_no_secret` for DispatchError; names backend/field only |
| T-90-06-02 | Spoofing/DoS | inbound MCP transport (CORS, DNS rebinding) | mitigate | Reuse `pmcp::StreamableHttpServer` security layers (Phase 56) — not re-implemented |
| T-90-06-03 | Availability | serve-task panic exits silently | mitigate | `handle.await.map_err(RunError::Serving)` propagates a serve panic non-zero (threat T-85-10-02, copied verbatim) |
| T-90-06-04 | Information Disclosure | spec/config as deploy assets leaking secrets | accept | spec/config are admin-authored read-only assets; secret refs resolved via toolkit secrets (CF-8); deploy posture handled in Plan 08 |
</threat_model>

<verification>
- `cargo test -p pmcp-openapi-server -- --test-threads=1` green (cli + dispatch + http_smoke).
- Curated-only (no-spec) boot proven (D-03); streamable HTTP only (CF-1); lazy startup (CF-2).
- No `mcp_server_common::`/reference `pmcp_server.rs` survivors (Pitfall 6).
</verification>

<success_criteria>
- `pmcp-openapi-server` serves a config-driven OpenAPI MCP server over streamable HTTP (OAPI-06).
- `--spec` optional at runtime; lazy startup; one engine feeds single-call + script tools + Code Mode.
- Runnable example + smoke test present (ALWAYS coverage).
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-06-SUMMARY.md`
</output>
