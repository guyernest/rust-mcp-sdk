---
phase: 90-openapi-built-in-server
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-server-toolkit/src/config.rs
autonomous: true
requirements: [OAPI-02a, OAPI-03]
must_haves:
  truths:
    - "A config.toml with [backend] / [backend.auth] / [backend.http] parses on the shared ServerConfig"
    - "Existing SQL configs (no [backend]) still parse unchanged (additive)"
    - "A [[tools]] entry accepts path/method/base_url OR a script field"
    - "Unknown keys under [backend.*] are rejected (deny_unknown_fields preserved)"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/config.rs"
      provides: "BackendSection + AuthConfig/HttpConfig re-exports + ToolDecl path/method/base_url/script fields"
      contains: "pub backend: Option<BackendSection>"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/config.rs"
      to: "ServerConfig"
      via: "additive backend field"
      pattern: "backend: Option<BackendSection>"
---

<objective>
Extend the Phase 83 backend-agnostic `ServerConfig` additively with `[backend]` /
`[backend.auth]` / `[backend.http]` and add the D-01 two-kind tool detection fields
(`path`/`method`/`base_url`/`script`) to `ToolDecl` (D-06). Existing SQL configs
must keep parsing; `#[serde(deny_unknown_fields)]` is preserved everywhere.

Purpose: One config type spans SQL + OpenAPI — the explicit "backend-agnostic
toolkit" goal. The single-call/script detection branch (Plan 03/05) reads these fields.
Output: extended `config.rs` with the new sections + fuzz-corpus seed.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/90-openapi-built-in-server/90-CONTEXT.md
@.planning/phases/90-openapi-built-in-server/90-PATTERNS.md
@.planning/phases/90-openapi-built-in-server/90-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive [backend] / [backend.auth] / [backend.http] on ServerConfig (D-06)</name>
  <files>crates/pmcp-server-toolkit/src/config.rs</files>
  <read_first>
    - MODIFY crates/pmcp-server-toolkit/src/config.rs
    - ANALOG (shape): `DatabaseSection` at config.rs:307-345 + how it hangs off `ServerConfig` (`pub database: DatabaseSection` at config.rs:112-113). The whole-file `#[serde(deny_unknown_fields)]` invariant lives at config.rs:8-24 + every section derive.
    - REFERENCE BODY to lift: `~/Development/mcp/sdk/pmcp-run/built-in/openapi-api/crates/mcp-openapi-server-core/src/config.rs` — `BackendConfig` (:140-159), `AuthConfig` (:164-245 incl. `Default → None`, `default_true`, `default_auth_header`), `HttpConfig`.
    - CROSS-PLAN OWNERSHIP: `AuthConfig` and `HttpConfig` are DEFINED in Plan 01 `http::auth` / `http::client` (to keep Plans 01 & 02 file-parallel). THIS task re-exports/references them from `crate::http` rather than redefining. If `http` feature is off, `BackendSection` still needs to parse — gate the `auth`/`http` field TYPES so the section deserializes regardless (use the `http`-feature types when on; a transparent stub when off — OR simply gate the whole `BackendSection` behind `#[cfg(feature = "http")]` since a no-http build has no OpenAPI backend; choose the cfg-gate to avoid a stub type, and document it).
    - DECISION: D-06 (additive, deny_unknown_fields preserved), REF-01 superset (no renames; additive Option keys only).
  </read_first>
  <action>
    Add to `ServerConfig` (mirror the `pub database: DatabaseSection` line at :112-113): `#[cfg(feature = "http")] #[serde(default)] pub backend: Option<BackendSection>,`. Define `#[cfg(feature = "http")]` structs, all `#[serde(deny_unknown_fields)]` (mirror `DatabaseSection`'s derive line at :306-307): `BackendSection { base_url: String, #[serde(default)] auth: crate::http::auth::AuthConfig, #[serde(default)] http: crate::http::client::HttpConfig }`. Re-export `pub use crate::http::auth::AuthConfig;` and `pub use crate::http::client::HttpConfig;` from config (or document that callers use the `http` module paths). Preserve the whole-file deny_unknown_fields invariant on the new structs.
    Per ALWAYS coverage: add unit tests `test_backend_section_parses` (full `[backend]`+`[backend.auth]`+`[backend.http]` toml round-trips), `test_sql_config_unaffected` (a SQL config with NO `[backend]` still parses — additive proof, REF-01 superset), and `test_backend_unknown_field_rejected` (an unknown key under `[backend.http]` fails — deny_unknown_fields).
    Extend the existing config-parser fuzz target corpus (the toolkit's config-parser fuzz target, per CONTEXT ALWAYS coverage) with a named seed `seed-backend.toml` containing a `[backend]` block — mirror the Phase 84 named-seed convention (track only the named seed file).
  </action>
  <behavior>
    - Test: a london-tube-style `[backend]` + `[backend.auth] type="api_key"` + `[backend.http]` toml parses into `ServerConfig` with `backend.is_some()`.
    - Test: a pure-SQL config (no `[backend]`) parses with `backend == None`.
    - Test: `[backend.http]` with `foo = 1` (unknown) fails to parse.
  </behavior>
  <acceptance_criteria>
    - `grep -n "pub backend: Option<BackendSection>" crates/pmcp-server-toolkit/src/config.rs` matches.
    - `cargo test -p pmcp-server-toolkit --features http config::backend -- --test-threads=1` exits 0 (parse + additive + deny_unknown_fields tests).
    - `cargo test -p pmcp-server-toolkit -- --test-threads=1` (default features) still green — SQL configs unaffected.
    - A named `seed-backend.toml` exists in the config-parser fuzz corpus dir.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features http config -- --test-threads=1</automated>
  </verify>
  <done>[backend]/[backend.auth]/[backend.http] parse additively; SQL configs untouched; deny_unknown_fields preserved; fuzz seed added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToolDecl path/method/base_url/script fields (D-01 two-kind detection)</name>
  <files>crates/pmcp-server-toolkit/src/config.rs</files>
  <read_first>
    - MODIFY crates/pmcp-server-toolkit/src/config.rs
    - ANALOG (exact pattern): the existing `ToolDecl.sql` field at config.rs:499 (`#[serde(default)] pub sql: Option<String>`) inside `ToolDecl` (config.rs:490-509). `ParamDecl` at config.rs:518-546 is REUSED unchanged.
    - REFERENCE (config shape): `.../mcp-openapi-server-core/src/tools/mod.rs` config shape (path/method) + `OPENAPI_SCRIPT_TOOLS.md` (`script` + `[[tools.parameters]]` → `args`).
    - DECISION: D-01 (detection rule: `script.is_some()` ⇒ script tool; `path`+`method` ⇒ single-call), REF-01 superset (additive Option fields only, deny_unknown_fields preserved).
  </read_first>
  <action>
    Add to `ToolDecl`, mirroring the `sql` field pattern exactly (all `#[serde(default)] pub <name>: Option<String>`): `path`, `method`, `base_url`, `script`. Keep `deny_unknown_fields` on `ToolDecl`. Do NOT gate these behind `http` — they live on the shared `ToolDecl` and are simply `None` for SQL configs (additive). `ParamDecl` is reused: for single-call it maps `{path}`+query/body; for script tools it binds to `args` (D-01) — no `ParamDecl` change.
    Add a small inherent helper or doc-comment recording the detection rule (`script.is_some()` first, else `path`+`method`) so Plan 03/05 synthesizers branch consistently — implement it as `impl ToolDecl { pub fn is_script_tool(&self) -> bool { self.script.is_some() } }` (a single small method, cog ≤25).
    Per ALWAYS coverage: unit tests `test_tooldecl_single_call_parses` (`path`+`method`), `test_tooldecl_script_parses` (`script = """..."""` + `[[tools.parameters]]`), `test_tooldecl_detection` (`is_script_tool()` true/false), and `test_tooldecl_sql_still_parses` (a `sql=` tool unaffected, REF-01 superset).
  </action>
  <behavior>
    - Test: a `[[tools]]` with `path="/Line/Mode/tube/Status"` + `method="GET"` parses; `is_script_tool()==false`.
    - Test: a `[[tools]]` with a multi-line `script` + a `[[tools.parameters]]` parses; `is_script_tool()==true`.
    - Test: an existing `sql=` tool still parses (no regression).
  </behavior>
  <acceptance_criteria>
    - `grep -n "pub script: Option<String>" crates/pmcp-server-toolkit/src/config.rs` and the same for `path`/`method`/`base_url` all match.
    - `cargo test -p pmcp-server-toolkit tooldecl -- --test-threads=1` exits 0 (single-call + script + detection + sql-regression).
    - `cargo test -p pmcp-server-toolkit -- --test-threads=1` default features green.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit tooldecl -- --test-threads=1</automated>
  </verify>
  <done>ToolDecl accepts path/method/base_url/script additively; is_script_tool() detection lands; sql tools unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config file → ServerConfig | operator-authored TOML deserialized into typed config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-02-01 | Tampering | unknown/typo config keys silently ignored | mitigate | `#[serde(deny_unknown_fields)]` on every new struct; `test_backend_unknown_field_rejected` (REF-01 superset) |
| T-90-02-02 | Information Disclosure | inline secrets in `[backend.auth]` token fields | accept | secrets reference resolution (`${ENV}`/`provider="auto"`) is reused from Phase 83 toolkit `secrets.rs`; config holds references, not resolved values. Documented; no new code here. |
| T-90-02-03 | Tampering | malformed `[backend]` TOML crashing the parser | mitigate | config-parser fuzz target extended with `seed-backend.toml`; parse errors surface as typed errors, never panics |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features http config -- --test-threads=1` green.
- `cargo test -p pmcp-server-toolkit -- --test-threads=1` (default) green — additive proof.
- deny_unknown_fields verified by a negative test.
</verification>

<success_criteria>
- `[backend]`/`[backend.auth]`/`[backend.http]` parse on the shared ServerConfig additively (D-06).
- `ToolDecl` accepts the two-kind fields with `is_script_tool()` detection (D-01).
- Existing SQL configs parse unchanged; deny_unknown_fields preserved; fuzz seed added.
</success_criteria>

<output>
After completion, create `.planning/phases/90-openapi-built-in-server/90-02-SUMMARY.md`
</output>
