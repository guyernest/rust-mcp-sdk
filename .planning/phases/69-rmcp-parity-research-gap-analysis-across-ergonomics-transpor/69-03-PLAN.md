---
phase: 69
plan: 03
type: execute
wave: 3
depends_on: [69-01, 69-02]
files_modified:
  - .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md
  - .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
estimated_tasks: 3
requirements: []  # Phase 69 is a seed phase — PARITY-<SURFACE>-NN IDs land via Plan 03 into REQUIREMENTS.md
user_setup: []

must_haves:
  truths:
    - "Every claim in 69-RESEARCH.md has an evidence citation (file:line or URL with line fragment/anchor)"
    - "Every proposal in 69-PROPOSALS.md has all six required template subsections"
    - "PARITY-* requirement IDs from 69-PROPOSALS.md are landed in REQUIREMENTS.md"
    - "STATE.md reflects Phase 69 completion and points at the two deliverable documents"
    - "PROJECT.md Key Decisions (if the table exists) reflects the Phase 69 research outcome"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Landed PARITY-* requirement IDs traceable to proposals"
      contains: "PARITY-"
    - path: ".planning/STATE.md"
      provides: "Session continuity with Phase 69 complete"
      contains: "Phase 69"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md"
      via: "PARITY-* ID reference"
      pattern: "PARITY-(MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)"

---

<objective>
Final quality gate for Phase 69. Cross-check that `69-RESEARCH.md` and `69-PROPOSALS.md` satisfy every CONTEXT.md MUST (D-05..D-19) and the CONTEXT.md evidence standards, then land the seeded `PARITY-*` requirement IDs in `REQUIREMENTS.md`, and update `STATE.md` + `PROJECT.md` to reflect phase completion.

Purpose: Without this plan, Phase 69's deliverables would sit in the phase directory unreferenced from the roadmap/requirements/state. This plan does the integration work that makes the research actionable for downstream planning (e.g., `/gsd-plan-phase` invocations for the follow-on phases).

Output: Verified and integrated Phase 69 deliverables. No new SDK code. The integration touches `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, and `.planning/PROJECT.md` only; the two research documents get corrective edits only if quality issues are found.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md

# Plan 01 and Plan 02 outputs — the subjects of this quality gate
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md

<interfaces>
**REQUIREMENTS.md landing shape.** Under the v2.1 Requirements section (existing file), add a new subsection:

```markdown
### rmcp Parity (Phase 69 research — seeds follow-on phases)

Seeded by Phase 69 research (`.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md`). Each ID corresponds to one Success Criterion in a proposal. Status remains pending until the follow-on phase ships.

- [ ] **PARITY-<SURFACE>-NN**: [one-line description copied verbatim from the proposal's Suggested Requirement IDs section]
- [ ] **PARITY-<SURFACE>-NN**: [...]
[...]
```

This subsection appears AFTER the existing "Code Mode Support" subsection and BEFORE "Previous Requirements".

**Traceability table landing shape.** In the `## Traceability` table at the bottom of REQUIREMENTS.md, append one row per PARITY-* ID:

```markdown
| PARITY-<SURFACE>-NN | Phase [suggested-number from proposal] | Pending |
```

**STATE.md updates.** In the `## Accumulated Context` > `### Decisions` > "v2.1 decisions" bullet list, append a new bullet:

```markdown
- [Phase 69]: rmcp parity research complete — 69-RESEARCH.md (gap matrix, N rows total, M High-severity) + 69-PROPOSALS.md (K proposals). PARITY-* requirement IDs landed in REQUIREMENTS.md; follow-on phases not yet scheduled.
```

In the `## Session Continuity` block, update `Stopped at:` to `Phase 69 complete — follow-on proposals ready for ROADMAP slotting` and `Resume:` to `Run /gsd:add-phase to slot a 69-PROPOSALS.md entry into the roadmap.`

Update the progress counters in the STATE.md frontmatter:
- `completed_phases` increments by 1
- `completed_plans` increments by 3 (this plan + Plan 01 + Plan 02)

**PROJECT.md updates.** If `PROJECT.md` contains a "Key Decisions" table, append one row:

```markdown
| 2026-04-16 | Phase 69 rmcp ergonomics parity research | K proposals seeded; follow-on phases to slot into late v2.1 / v2.2 | .planning/phases/69-.../69-PROPOSALS.md |
```

If PROJECT.md does not have such a table, skip this step — Plan 03 does not introduce a new table structure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quality-gate 69-RESEARCH.md and 69-PROPOSALS.md</name>
  <files>
    .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md
    .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md
  </files>
  <read_first>
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md` — in full.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md` — in full.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` D-05 through D-19 — the full decision set to check against.
  </read_first>
  <action>
    Execute the following quality-gate checks. For each check, log PASS or FAIL with specifics. If any FAIL, fix inline (edit the relevant file) before proceeding.

    **A. MUST surface coverage (D-05..D-10):**
    - A1: 69-RESEARCH.md has `## Tool Macros` H2 with a populated table (≥5 data rows). [MUST D-05]
    - A2: 69-RESEARCH.md has `## Builder APIs` H2 with a populated table (≥4 data rows). [MUST D-06]
    - A3: 69-RESEARCH.md has `## Typed Tool / Prompt Wrappers` H2 with a populated table (≥4 data rows). [MUST D-07]
    - A4: 69-RESEARCH.md has `## Handler Signatures + State / Extra Injection` H2 with a populated table (≥4 data rows). [MUST D-08]
    - A5: 69-RESEARCH.md has `## Client-Side Ergonomics` H2 with a populated table (≥4 data rows). [INCLUDE D-09]
    - A6: 69-RESEARCH.md has `## Error Types + Result Wrappers` H2 with a populated table (≥4 data rows). [INCLUDE D-10]

    **B. Baseline pinning (D-01..D-04):**
    - B1: 69-RESEARCH.md header contains `**rmcp version pinned:**` with a concrete version string (regex `[0-9]+\.[0-9]+\.[0-9]+`). [D-01]
    - B2: 69-RESEARCH.md header contains `**rmcp source tag:**` with a GitHub release URL (regex `github\.com/modelcontextprotocol/rust-sdk/releases/tag/`). [D-01]
    - B3: 69-RESEARCH.md header contains `**pmcp baseline:**` with a short git SHA (7-char hex). [D-04]
    - B4: No row in 69-RESEARCH.md cites unreleased rmcp work as evidence for a Severity rating. Scan for the word "unreleased", "unpublished", "main branch"; if found, verify the citation is a footnote/context only, not the severity-scoring evidence. [D-02]

    **C. Evidence citation standard (D-03, "Specific Ideas" rule):**
    - C1: Every `pmcp evidence` cell across all tables in 69-RESEARCH.md contains a file:line citation. Heuristic: grep for `\.rs:[0-9]+` in the document; expected count ≥ sum of minimum row counts (≥ 25 across all surfaces).
    - C2: Every `rmcp evidence` cell across all tables in 69-RESEARCH.md contains either a GitHub blob URL with `#L<n>` fragment, a docs.rs URL with an anchor, OR the exact phrase "N/A — no rmcp analog".
    - C3: No table row in 69-RESEARCH.md makes a claim without an evidence citation. Spot-check: if any row has both evidence cells filled with generic statements (e.g., just a module path without a line number), flag and fix.

    **D. Severity rubric (D-14, D-16):**
    - D1: Every `Severity` cell in 69-RESEARCH.md is exactly `High`, `Medium`, or `Low`.
    - D2: No `Severity: High` row has a `Gap:` cell that is vague (≥ 10 characters AND contains a specific action phrase like "add", "replace", "expose", "support", "derive"). [D-14 "clean articulable fix"]
    - D3: Every row with `Gap: Parity` or `Gap: Strength to preserve` has `Severity: Low`.

    **E. Proposal completeness (D-17, D-18):**
    - E1: 69-PROPOSALS.md header's `**Proposal count:**` integer matches the number of `## Proposal N:` sections in the file.
    - E2: Every proposal has all six template subsections: `### Goal`, `### Scope`, `### Success Criteria`, `### Suggested Requirement IDs`, `### Estimated Plan Count`, `### Rationale / Evidence`.
    - E3: Every `### Estimated Plan Count` is an integer in {3, 4, 5}.
    - E4: Every `### Success Criteria` section has 3–5 bulleted items.
    - E5: Every proposal's `Derived from:` line cites at least one specific RESEARCH.md surface + subtopic. [D-18]

    **F. File separation (D-19):**
    - F1: 69-RESEARCH.md and 69-PROPOSALS.md are two distinct files in the phase directory. [D-19]

    **G. Anti-pattern guard (CONTEXT.md "Specific Ideas" + REQUIREMENTS.md Out of Scope):**
    - G1: No proposal in 69-PROPOSALS.md contains the phrase "adopt rmcp's" or "copy rmcp's" or proposes mimicking rmcp's trait-based architecture. If found, rewrite the Goal to express the fix in pmcp-native terms.
    - G2: No row in 69-RESEARCH.md frames rmcp negatively. If found, rewrite to factual neutrality per CONTEXT.md "Specific Ideas".

    **H. Cardinality check (D-15):**
    - H1: Proposal count in 69-PROPOSALS.md is 2–5 OR the document explicitly documents why it is outside that range.

    After running all checks, write a `## Phase 69 Quality Gate Report` appendix to the bottom of 69-RESEARCH.md (NOT to PROPOSALS.md — the research doc is the reference) summarizing PASS/FAIL for each check A1 through H1 in a short table. If all PASS, the table is the final content of the document and Plan 03 can proceed to Task 2.
  </action>
  <verify>
    <automated>grep -q "^## Phase 69 Quality Gate Report$" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md && ! grep -qE "\| FAIL \|" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - A `## Phase 69 Quality Gate Report` section exists at the bottom of 69-RESEARCH.md.
    - The report table covers checks A1 through H1 (inclusive).
    - No check reports FAIL (all inline fixes applied, or the FAIL row is removed after the fix is verified).
    - All MUST surfaces (D-05..D-10) have populated tables with the minimum row counts from Plan 01.
    - Every proposal in 69-PROPOSALS.md is template-complete per D-18.
    - Baseline pinning (rmcp version, rmcp tag, pmcp SHA) is concrete and present in 69-RESEARCH.md.
  </acceptance_criteria>
  <done>Both deliverables pass the full CONTEXT.md checklist; the quality-gate report is appended to 69-RESEARCH.md for auditability.</done>
</task>

<task type="auto">
  <name>Task 2: Land PARITY-* requirement IDs in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — in full, to locate the exact insertion point and existing formatting conventions.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md` — extract every `### Suggested Requirement IDs` block verbatim. Collect (ID, description, suggested-phase-number) tuples.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` D-18 — confirms proposals are expected to seed new REQ-IDs.
  </read_first>
  <action>
    0. **Read the proposal count from 69-PROPOSALS.md.** Grep for `^\*\*Proposal count:\*\* ([0-9]+)` in the PROPOSALS.md header (Plan 02's Task 1 step 4 writes this exact header). Capture the integer as `proposal_count` and store it in a scratch variable / local file for the verify block to re-read. If `proposal_count == 0` (valid per D-15 when zero High-severity gaps were found), skip steps 2–4 entirely — there are no PARITY-<SURFACE>-NN IDs to land, the REQUIREMENTS.md subsection and traceability rows are not added, and the baseline remains unchanged. In that branch, still perform step 5 (update the `*Last updated:*` footer with a note: `2026-04-16 — Phase 69 rmcp parity research completed with zero High-severity gaps; no PARITY-* IDs added`). If `proposal_count >= 1`, proceed with steps 1–5 below as written.

    1. **Locate the insertion point.** In REQUIREMENTS.md, find the line immediately after the last item of the "Code Mode Support" subsection (CMSUP-06) and immediately before the `## Previous Requirements` H2. Insert a new H3 subsection there.

    2. **Write the new subsection** using the exact shape from `<interfaces>`:

       ```markdown
       ### rmcp Parity (Phase 69 research — seeds follow-on phases)

       Seeded by Phase 69 research (`.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md`). Each ID corresponds to one Success Criterion in a proposal. Status remains pending until the follow-on phase ships.

       - [ ] **PARITY-<SURFACE>-NN**: [description verbatim]
       - [ ] **PARITY-<SURFACE>-NN**: [description verbatim]
       [...]
       ```

       Include every PARITY-* ID from every proposal in 69-PROPOSALS.md. Preserve the order: proposals in the order they appear in PROPOSALS.md, IDs within each proposal in the order they appear.

    3. **Update the Traceability table** at the bottom of REQUIREMENTS.md. Append one row per PARITY-* ID:

       ```markdown
       | PARITY-<SURFACE>-NN | Phase [suggested-number from proposal, or TBD] | Pending |
       ```

       If the proposal's suggested phase number is `TBD — slotted by /gsd-add-phase`, use literal `TBD` in the Phase column.

    4. **Update the coverage counters** at the end of REQUIREMENTS.md (the `**Coverage:**` block):
       - `v2.1 requirements:` increment the count by the number of PARITY-* IDs added (e.g., `20 total → 20 + K = (20+K) total`). Add a parenthetical: `(14 original + 6 inserted via Phase 67.1 + K seeded by Phase 69)`.
       - `Mapped to phases:` increment by K — all PARITY-* IDs map to future phases whose numbers are TBD but are recorded as "Phase [suggested]" in the traceability table.
       - `Unmapped:` stays 0 (PARITY-* IDs are mapped, even if the phase number is TBD).

    5. **Update the footer date** at the bottom of REQUIREMENTS.md:
       - Change `*Last updated:*` to `*Last updated: 2026-04-16 — added PARITY-* IDs seeded by Phase 69 rmcp parity research*`
  </action>
  <verify>
    <automated>python3 -c "
import re, sys, subprocess
prop_path = '.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md'
req_path = '.planning/REQUIREMENTS.md'
prop_content = open(prop_path).read()
req_content = open(req_path).read()
m = re.search(r'^\*\*Proposal count:\*\* (\d+)', prop_content, re.MULTILINE)
if not m:
    print('FAIL: Proposal count not found in PROPOSALS.md header')
    sys.exit(1)
proposal_count = int(m.group(1))
parity_ids = re.findall(r'PARITY-(?:MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}', req_content)
landed_ids = len(parity_ids)
if proposal_count == 0:
    if landed_ids != 0:
        print(f'FAIL: proposal_count == 0 but {landed_ids} PARITY-<SURFACE>-NN IDs appear in REQUIREMENTS.md (baseline should be unchanged)')
        sys.exit(1)
    if '### rmcp Parity' in req_content:
        print('FAIL: proposal_count == 0 but REQUIREMENTS.md has the rmcp Parity subsection')
        sys.exit(1)
    print('PASS: zero proposals, zero landed IDs, baseline unchanged')
    sys.exit(0)
if '### rmcp Parity' not in req_content:
    print('FAIL: proposal_count >= 1 but REQUIREMENTS.md missing ### rmcp Parity subsection')
    sys.exit(1)
minimum_expected = proposal_count * 2
if landed_ids < minimum_expected:
    print(f'FAIL: only {landed_ids} PARITY IDs landed, expected >= {minimum_expected} (2 per proposal minimum, proposal_count={proposal_count})')
    sys.exit(1)
if '2026-04-16 — added PARITY' not in req_content:
    print('FAIL: Last-updated footer missing PARITY annotation')
    sys.exit(1)
print(f'PASS: {landed_ids} PARITY IDs landed for {proposal_count} proposals')
"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains a new `### rmcp Parity (Phase 69 research — seeds follow-on phases)` subsection.
    - Every PARITY-* ID from every 69-PROPOSALS.md proposal appears in the new subsection.
    - Every PARITY-* ID also appears in the Traceability table at the bottom of REQUIREMENTS.md with a phase column entry.
    - The `**Coverage:**` counters are updated to reflect the new IDs.
    - The `*Last updated:*` footer is dated 2026-04-16 with the PARITY annotation.
    - No PARITY-* ID is duplicated in the file.
  </acceptance_criteria>
  <done>Every PARITY-* ID from 69-PROPOSALS.md is landed in REQUIREMENTS.md with full traceability and updated coverage counters.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and PROJECT.md to reflect Phase 69 completion</name>
  <files>
    .planning/STATE.md
    .planning/PROJECT.md
  </files>
  <read_first>
    - `.planning/STATE.md` — in full, to locate the exact insertion points for the new v2.1 decisions bullet and the Session Continuity block.
    - `.planning/PROJECT.md` — check whether a "Key Decisions" table exists. If not, this task skips PROJECT.md editing per the `<interfaces>` spec.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md` — scan the `## Executive Summary` to extract the integer counts (N rows total, M High-severity).
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md` — scan the header to extract the K proposal count.
  </read_first>
  <action>
    **STATE.md edits:**

    1. **Decisions bullet:** In `## Accumulated Context` → `### Decisions` → "v2.1 decisions:" bullet list, append a new final bullet:
       ```markdown
       - [Phase 69]: rmcp parity research complete — 69-RESEARCH.md (gap matrix, N rows total, M High-severity) + 69-PROPOSALS.md (K proposals). PARITY-* requirement IDs landed in REQUIREMENTS.md; follow-on phases not yet scheduled.
       ```
       Replace N, M, K with the integers read from the deliverables.

    2. **Session Continuity block:** Replace the existing `Last session:` / `Stopped at:` / `Resume:` values with:
       - `Last session: 2026-04-16T<current-ISO-timestamp>` (use the current timestamp).
       - `Stopped at: Phase 69 complete — follow-on proposals ready for ROADMAP slotting`
       - `Resume: Run /gsd:add-phase to slot a 69-PROPOSALS.md entry into the roadmap, or /gsd-plan-phase for a scheduled v2.1 phase.`

    3. **Frontmatter counters (read-then-mutate-then-assert pattern):** In the YAML frontmatter at the top of STATE.md:
       - **Before editing, capture the pre-edit baseline.** Grep the current values of `completed_phases:` and `completed_plans:` from STATE.md and store them as `pre_phases` and `pre_plans` (either in a scratch file like `/tmp/gsd-69-state-baseline.json` that the verify block can re-read, or pass them inline to the verify script via a small JSON blob appended as a hidden HTML comment — pick whichever the executor finds simpler; the scratch-file approach is recommended).
       - **Apply the edits.** Set `completed_phases` to `pre_phases + 1` (Phase 69 completion) and `completed_plans` to `pre_plans + 3` (Plans 69-01, 69-02, 69-03). Do NOT hardcode absolute values — the deltas are what matter; absolute values may drift if other phases completed concurrently.
       - Recompute `percent` using `(completed_phases / total_phases) * 100` rounded to integer.
       - Update `last_updated:` to the current ISO timestamp.
       - Update `stopped_at:` to match the Session Continuity value.
       - Leave `status:` unchanged unless the milestone transitions (not expected — v2.0 is already "Milestone complete").

    **PROJECT.md edits (conditional):**

    4. Check whether PROJECT.md contains a section heading like `## Key Decisions` or `### Key Decisions` with a markdown table. If it does:
       - Append one row to that table:
         ```markdown
         | 2026-04-16 | Phase 69 rmcp ergonomics parity research | K proposals seeded; follow-on phases to slot into late v2.1 / v2.2 | .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md |
         ```
       - Replace K with the actual proposal count.
       - Match the column count and ordering of the existing table header — if the existing table has different columns (e.g., "Decision | Impact | Status"), adapt the row shape to match.

       If PROJECT.md does NOT have a Key Decisions table, write nothing to PROJECT.md (the edit is conditional). Log the skip in the task output.

    **Verification pass:** After edits, re-read STATE.md and confirm:
    - The frontmatter `completed_phases` increment is reflected.
    - The new v2.1 decisions bullet is present.
    - The Session Continuity values are updated.
  </action>
  <verify>
    <automated>grep -q "\[Phase 69\]: rmcp parity research complete" .planning/STATE.md && grep -q "^Stopped at: Phase 69 complete" .planning/STATE.md && grep -q "^Resume: Run /gsd:add-phase" .planning/STATE.md && python3 -c "
import re, json, os, sys
baseline_path = '/tmp/gsd-69-state-baseline.json'
if not os.path.exists(baseline_path):
    print(f'FAIL: pre-edit baseline file {baseline_path} not found — Task 3 action step 3 must capture pre_phases/pre_plans before mutating STATE.md')
    sys.exit(1)
with open(baseline_path) as f:
    baseline = json.load(f)
pre_phases = int(baseline['pre_phases'])
pre_plans = int(baseline['pre_plans'])
content = open('.planning/STATE.md').read()
m_phases = re.search(r'^completed_phases: (\d+)', content, re.MULTILINE)
m_plans = re.search(r'^completed_plans: (\d+)', content, re.MULTILINE)
if not m_phases or not m_plans:
    print('FAIL: completed_phases or completed_plans not found in STATE.md frontmatter')
    sys.exit(1)
post_phases = int(m_phases.group(1))
post_plans = int(m_plans.group(1))
expected_phases = pre_phases + 1
expected_plans = pre_plans + 3
if post_phases != expected_phases:
    print(f'FAIL: completed_phases delta wrong — pre={pre_phases} post={post_phases} expected={expected_phases} (pre+1)')
    sys.exit(1)
if post_plans != expected_plans:
    print(f'FAIL: completed_plans delta wrong — pre={pre_plans} post={post_plans} expected={expected_plans} (pre+3)')
    sys.exit(1)
print(f'PASS: completed_phases {pre_phases} -> {post_phases} (+1), completed_plans {pre_plans} -> {post_plans} (+3)')
"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter `completed_phases` is incremented by at least 1.
    - STATE.md frontmatter `completed_plans` is incremented by at least 3.
    - STATE.md frontmatter `last_updated` is a fresh ISO timestamp.
    - STATE.md `### Decisions` "v2.1 decisions" block contains a new bullet starting with `- [Phase 69]: rmcp parity research complete`.
    - STATE.md Session Continuity block's `Stopped at:` and `Resume:` are updated per the action spec.
    - PROJECT.md is either updated (if Key Decisions table exists) OR explicitly unchanged (documented in the task output).
    - No other sections of STATE.md or PROJECT.md are disturbed.
  </acceptance_criteria>
  <done>Phase 69 completion is reflected in STATE.md (and PROJECT.md if applicable); session continuity points downstream consumers at the follow-on proposals.</done>
</task>

</tasks>

<threat_model>
Documentation-only phase. No code, config, or API surface changes. Threats considered: (1) inaccurate evidence citations leading downstream phases astray — mitigated by the Task 1 quality-gate sweep that verifies every citation passes the file:line / URL-with-anchor standard; (2) biased gap severity scoring leading to poor prioritization — mitigated by Task 1 check D2 which requires every High row's Gap cell to articulate a specific action verb, and by the D-15 cardinality re-check. Additional integration risk: stale STATE.md counters drift from reality — mitigated by Task 3 verify block recomputing from the baseline.
</threat_model>

<verification>
Grep-verifiable post-conditions:
- `grep -q "^## Phase 69 Quality Gate Report$" 69-RESEARCH.md`
- `! grep -qE "\| FAIL \|" 69-RESEARCH.md` (no FAIL row remaining in the report table)
- `grep -q "^### rmcp Parity" .planning/REQUIREMENTS.md`
- `grep -cE "PARITY-(MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}" .planning/REQUIREMENTS.md` ≥ 4
- `grep -q "\[Phase 69\]: rmcp parity research complete" .planning/STATE.md`
- `grep -q "^Stopped at: Phase 69 complete" .planning/STATE.md`
</verification>

<success_criteria>
- Both deliverables pass the full CONTEXT.md MUST checklist (D-05..D-19) via the Task 1 report.
- Every PARITY-* ID from 69-PROPOSALS.md is landed in REQUIREMENTS.md with traceability.
- STATE.md reflects Phase 69 completion in frontmatter counters, decisions bullet list, and session continuity.
- PROJECT.md is updated if a Key Decisions table exists; otherwise explicitly skipped.
- Phase 69 deliverables are discoverable from STATE.md → RESEARCH.md / PROPOSALS.md → REQUIREMENTS.md PARITY-* IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-03-SUMMARY.md` summarizing: quality-gate results (all PASS, or list any inline fixes applied), count of PARITY-* IDs landed in REQUIREMENTS.md, STATE.md counter deltas, and a one-line handoff ("Phase 69 complete; 69-PROPOSALS.md ready for /gsd:add-phase consumption").
</output>
