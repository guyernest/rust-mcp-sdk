---
phase: 69
plan: 02
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md
autonomous: true
estimated_tasks: 2
requirements: []  # Phase 69 is a seed phase — PARITY-<SURFACE>-NN IDs land via Plan 03 into REQUIREMENTS.md
user_setup: []

must_haves:
  truths:
    - "69-PROPOSALS.md exists in the phase directory as a file separate from 69-RESEARCH.md (per D-19)"
    - "Every High-severity row in 69-RESEARCH.md has a corresponding proposal in 69-PROPOSALS.md"
    - "Every proposal follows the standard template: Goal, Scope (In/Out), Success Criteria (3–5 items), Suggested REQ-IDs, Estimated Plan Count, Target Slot (late v2.1 vs v2.2)"
    - "Number of proposals is between 2 and 5 (per D-15); if outside this range, the document explicitly documents why and the severity assignments in RESEARCH.md are re-checked"
    - "Each proposal's Goal statement is at least 2 sentences — one stating what the phase delivers, one stating why it matters"
    - "Each proposal's Scope section explicitly lists In-scope items AND Out-of-scope items"
    - "Each proposal cites the specific RESEARCH.md row(s) it derives from by surface + subtopic"
  artifacts:
    - path: ".planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md"
      provides: "Follow-on phase proposals derived from High-severity gaps"
      contains: "## Proposal"
      min_lines: 80
  key_links:
    - from: "Each proposal in 69-PROPOSALS.md"
      to: "Row(s) in 69-RESEARCH.md"
      via: "surface + subtopic citation"
      pattern: "Derived from.*RESEARCH.md"

---

<objective>
Produce `69-PROPOSALS.md`, one follow-on phase proposal per High-severity gap identified in `69-RESEARCH.md` (Plan 01 output). Each proposal is ready to drop into ROADMAP.md via `/gsd-add-phase` with minimal editing, per CONTEXT.md D-18.

Purpose: Translate research findings into concrete, plannable phase proposals. Plan 01 produces evidence; Plan 02 produces a backlog. Together they constitute Phase 69's deliverables.

Output: A single markdown file at `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md` containing 2–5 proposals (per D-15), each conforming to the standard template locked in the `<interfaces>` section of this plan.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md

# Primary input — Plan 01 output
@.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md

# Prior-phase sizing references (proposals should target similar scope per D-17)
@.planning/phases/66-macros-documentation-rewrite/66-CONTEXT.md
@.planning/phases/67-docs-rs-pipeline-and-feature-flags/67-CONTEXT.md

<interfaces>
Each proposal in 69-PROPOSALS.md follows this EXACT template (lock this structure; executor just fills in the values):

```markdown
## Proposal N: [Short phase title]

**Derived from:** `69-RESEARCH.md` §[Surface Name] row(s): [subtopic name(s)]
**Severity source:** High
**Suggested phase number:** [Phase N.M or "TBD — slotted by /gsd-add-phase"]
**Target slot:** [late v2.1 | v2.2]

### Goal

[2–4 sentences. Sentence 1: what the phase delivers (e.g., "Ship attribute-level description overrides on `#[mcp_tool]` matching rmcp's pattern"). Sentence 2: why it matters for users (e.g., "Closes a first-hour-user ergonomics gap where pmcp requires rustdoc-derived descriptions while rmcp supports inline attribute-level prose"). Optional sentence 3–4: any essential scope boundary.]

### Scope

**In scope:**
- [bullet: concrete deliverable 1]
- [bullet: concrete deliverable 2]
- [bullet: ...]

**Out of scope:**
- [bullet: specific item explicitly NOT in this phase]
- [bullet: ...]

### Success Criteria (3–5 items)

- [ ] [Measurable criterion 1, e.g., "Every `#[mcp_tool]` usage site in `examples/` compiles unchanged — backwards compatible"]
- [ ] [Measurable criterion 2]
- [ ] [Measurable criterion 3]
- [ ] [optional criterion 4]
- [ ] [optional criterion 5]

### Suggested Requirement IDs

- **[REQ-ID-01]:** [one-line requirement description]
- **[REQ-ID-02]:** [one-line requirement description]
- [...]

### Estimated Plan Count

[N] plans — [brief rationale, e.g., "1 macro-edit plan + 1 doc+migration plan + 1 CI+integration plan"]. Targets multi-day phase sizing similar to Phase 66 (macros doc rewrite) and Phase 67 (docs.rs pipeline) per D-17.

### Rationale / Evidence

[2–4 sentences citing the specific RESEARCH.md row evidence that justifies High severity. Reference the file:line citations from RESEARCH.md verbatim.]
```

Document header shape (write verbatim at the top of 69-PROPOSALS.md):

```markdown
# Phase 69: Follow-on Phase Proposals

**Generated:** 2026-04-16
**Derived from:** `69-RESEARCH.md` (same phase directory)
**Proposal count:** [N] (per D-15 expected range 2–5)
**Severity bar:** Only High-severity gaps become proposals; Medium/Low gaps are noted in RESEARCH.md for possible future work (D-16).

## Summary

[Short table listing all proposals:]

| # | Title | Target slot | Plans | Suggested REQ-IDs |
|---|-------|-------------|-------|-------------------|
| 1 | [title] | [v2.1 / v2.2] | [N] | [IDs] |
| 2 | [title] | [v2.1 / v2.2] | [N] | [IDs] |
| ... |

---
```

After the header, the N proposals appear in severity-rationale order (proposal addressing the highest-pressure gap first). Each proposal is separated from the next by a `---` horizontal rule.

**REQ-ID naming convention:** Use the prefix `PARITY-` followed by a surface code and a two-digit counter:
- Tool macros proposals: `PARITY-MACRO-NN`
- Builder proposals: `PARITY-BUILDER-NN`
- Typed wrapper proposals: `PARITY-TYPED-NN`
- Handler/state proposals: `PARITY-HANDLER-NN`
- Client ergonomics proposals: `PARITY-CLIENT-NN`
- Error-type proposals: `PARITY-ERR-NN`

Counters restart at 01 per surface. Plan 03 cross-checks that these IDs land in REQUIREMENTS.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate High-severity gaps and draft proposals</name>
  <files>.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md</files>
  <read_first>
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md` — the ENTIRE document. Extract every row where `Severity = High` by scanning each surface's matrix table. For each High row, capture: surface name, subtopic name, Gap cell text, and both evidence citations.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` D-15 through D-19 — proposal count expectations, severity bar, template shape, and file separation.
    - `.planning/phases/66-macros-documentation-rewrite/66-CONTEXT.md` — referenced by D-17 as the sizing benchmark (multi-day phase, 3–5 plans).
    - `.planning/phases/67-docs-rs-pipeline-and-feature-flags/67-CONTEXT.md` — referenced by D-17 as the sizing benchmark.
    - `.planning/REQUIREMENTS.md` — check the existing v2.1 requirement ID namespace to ensure new `PARITY-*` IDs don't collide (grep for `PARITY-` in the file; if nonexistent, this is the first use of the prefix).
    - `.planning/ROADMAP.md` §Milestone headers — identify whether v2.1 is still active or has closed, to inform the `Target slot: late v2.1 vs v2.2` decision. If v2.1 is already closed, all proposals slot to v2.2.
  </read_first>
  <action>
    1. **Enumerate High-severity rows from 69-RESEARCH.md.** Produce an internal list (not written to the file) of `(surface, subtopic, gap_text)` tuples for every High row. Count them.

    2. **Cardinality check per D-15.** Expected range is 2–5 High rows. If the count is outside this range:
       - **0 High rows:** Write 69-PROPOSALS.md with the header, the summary table showing zero proposals, and a single `## No Follow-on Proposals` section explaining: "Source-reading research (per D-03) found zero High-severity gaps. Medium/Low findings are noted in `69-RESEARCH.md` for possible future work per D-16. This outcome triggers the D-15 re-read check — Plan 03 verifies severity assignments in RESEARCH.md are calibrated correctly." Proceed to Task 2.
       - **1 High row:** Acceptable, below the expected range. Write the one proposal and note in the summary: "Single proposal is below D-15's expected 2–5 range; severity calibration re-checked and confirmed."
       - **2–5 High rows:** Normal path. Write one proposal per High row.
       - **6+ High rows:** Above the expected range. Write the proposals but note in the summary: "Proposal count ([N]) exceeds D-15's expected range of 5; this may indicate severity inflation. Plan 03 re-checks RESEARCH.md severity assignments."

       Do NOT drop High rows to fit the range. D-15 is a calibration check, not a ceiling.

    3. **Cluster-or-split decision per High row.** For each High row, decide whether it is its own proposal OR should be bundled with another High row into a shared proposal. Bundling is allowed when two High rows share the same surface AND would be fixed by the same code change (e.g., two rows in Tool Macros both fixed by adding attribute-level overrides). When bundling, the proposal's `Derived from` line lists both rows. Default is one proposal per row unless the bundling is obvious.

    4. **Write the document header** with the exact shape from `<interfaces>`, filling in:
       - `**Proposal count:**` → final integer after any bundling.
       - The summary table with one row per proposal.

    5. **Write each proposal** using the template from `<interfaces>`. For each proposal:
       - **Title:** short, action-oriented (e.g., "Attribute-level tool description overrides", "Client builder fluent API", "Top-level error type thiserror migration").
       - **Derived from:** cite the RESEARCH.md section and subtopic names verbatim (e.g., "`69-RESEARCH.md` §Tool Macros rows: attribute richness, state injection").
       - **Suggested phase number:** use `Phase N.M` placeholder (e.g., `Phase 70`, `Phase 68.1`) — `/gsd-add-phase` assigns the final number; this is a suggestion only. If unsure, write `TBD — slotted by /gsd-add-phase`. Prefer a concrete placeholder like `Phase 70` when the proposal clearly slots immediately after Phase 69 in the current milestone. Use `TBD — slotted by /gsd-add-phase` only when the target milestone or sequencing is ambiguous (e.g., proposals targeting v2.2).
       - **Target slot:** `late v2.1` if the fix is a polish-level extension of existing v2.1 scope (macros, docs, examples) AND v2.1 is still open per the ROADMAP read. `v2.2` if the fix is a new surface area or if v2.1 is closed.
       - **Goal:** 2–4 sentences per the `<interfaces>` spec. Sentence 1 is the deliverable; sentence 2 is the user value; optional sentences 3–4 are scope boundaries. Do NOT open with "This proposal..." or "The goal of this phase..." — write direct declarative sentences.
       - **Scope In/Out:** minimum 3 In-scope bullets, minimum 2 Out-of-scope bullets. Out-of-scope bullets MUST include the items D-11 (Tower / service integration) and D-12 (transport construction) UNLESS they are directly relevant to this proposal — in which case explicitly handle them in-scope.
       - **Success Criteria:** 3–5 bullets, each measurable (contains a verb like "compiles", "passes", "documents", "returns", "enforces"). No criterion may be "works correctly" or "is ergonomic" — those are not measurable.
       - **Suggested Requirement IDs:** 2–4 IDs using the naming convention from `<interfaces>`. Each ID has a one-line description. The IDs form a coherent requirements cluster for the proposed phase.
       - **Estimated Plan Count:** integer 3–5 (per D-17). Include a one-sentence rationale naming the plans at a high level.
       - **Rationale / Evidence:** 2–4 sentences citing the RESEARCH.md row(s) verbatim — include the exact Gap cell text and the pmcp evidence file:line.

    6. **Proposal ordering:** Sort proposals by severity-pressure — the proposal addressing the most user-facing gap comes first. When severity is equivalent, sort by surface order (Tool Macros before Builder before Typed Wrappers before Handler before Client before Errors).

    7. **Anti-pattern guard:** Do NOT propose rmcp-architecture-mimicry (per CONTEXT.md "Specific Ideas" final bullet and REQUIREMENTS.md Out of Scope "Copying rmcp's trait-based architecture docs"). Each proposal must frame the fix in pmcp-native terms. If a RESEARCH.md High row's Gap cell reads like "adopt rmcp's X", rewrite the proposal's Goal to specify how pmcp would achieve equivalent ergonomics without copying the architecture.
  </action>
  <verify>
    <automated>test -f .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md && grep -qE "^\*\*Proposal count:\*\* [0-9]+" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md && grep -qE "^## (Proposal|No Follow-on Proposals)" .planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md</automated>
  </verify>
  <acceptance_criteria>
    - 69-PROPOSALS.md exists.
    - The header's `**Proposal count:**` field contains an integer matching the actual number of `## Proposal N:` sections in the file.
    - Every `## Proposal N:` section contains all six template subsections: Goal, Scope (with both In/Out), Success Criteria, Suggested Requirement IDs, Estimated Plan Count, Rationale / Evidence.
    - Every proposal's `Derived from` line cites a specific RESEARCH.md surface + subtopic.
    - Every proposal's `Estimated Plan Count` is an integer between 3 and 5 (per D-17).
    - Every proposal's `Success Criteria` has between 3 and 5 bulleted items.
    - No proposal uses the phrase "adopt rmcp's" or "copy rmcp's" — frames fixes in pmcp-native terms.
    - If 0 High rows exist in RESEARCH.md, the file has a `## No Follow-on Proposals` section explaining the outcome.
  </acceptance_criteria>
  <done>Every High-severity gap from 69-RESEARCH.md is translated into a plannable follow-on phase proposal with complete template fields.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-validate proposals against RESEARCH.md and CONTEXT.md</name>
  <files>.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md</files>
  <read_first>
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md` — re-read in full after Task 1's draft.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-RESEARCH.md` — re-read the High rows to confirm every High row has a matching proposal, and no proposal cites a row that isn't actually High.
    - `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-CONTEXT.md` D-18 template requirements — verify each proposal has all six required subsections.
  </read_first>
  <action>
    Execute the following validation sweep, making edits inline as issues are found:

    1. **High-row ↔ Proposal bijection check.**
       - Build the set of High rows in RESEARCH.md: `S_research = { (surface, subtopic) | Severity=High }`.
       - Build the set of cited rows in PROPOSALS.md: `S_proposals = union of all (surface, subtopic) pairs on every `Derived from:` line`.
       - Verify `S_research ⊆ S_proposals` — every High row is addressed.
       - Verify `S_proposals ⊆ S_research ∪ {empty}` — no proposal cites a non-High row (a proposal cannot "derive from" a Medium or Low row; those are future work per D-16).
       - If any imbalance exists, fix PROPOSALS.md: add missing proposals, remove spurious citations.

    2. **Template completeness sweep.** For each `## Proposal N:` section, verify presence of all six required subsections: `### Goal`, `### Scope` (with both `**In scope:**` and `**Out of scope:**` bolded subheadings), `### Success Criteria`, `### Suggested Requirement IDs`, `### Estimated Plan Count`, `### Rationale / Evidence`. Add any missing subsections.

    3. **Measurability check on Success Criteria.** For each proposal's Success Criteria bullets, verify each bullet contains at least one of: a compile-test verb ("compiles", "passes", "builds"), a behavioral verb ("returns", "emits", "accepts"), a documentation verb ("documents", "describes"), or a coverage quantifier ("≥80%", "zero warnings", "all N examples"). Rewrite any bullet that reads "works", "is ergonomic", "feels good", "looks clean" — those are not measurable.

    4. **REQ-ID uniqueness check.** Collect every `PARITY-*-NN` ID across all proposals. Verify uniqueness. If two proposals share an ID, renumber. Verify the prefix-per-surface convention from `<interfaces>` is followed (MACRO / BUILDER / TYPED / HANDLER / CLIENT / ERR).

    5. **Plan-count sanity.** Each proposal must have `Estimated Plan Count: N` where N ∈ {3, 4, 5}. Below 3 = too small (should be bundled or promoted to backlog note); above 5 = too big (should be split into two proposals). Adjust.

    6. **Update the summary table at the top of the document** so its row count matches the final proposal count, its `#`, `Title`, `Target slot`, `Plans`, and `Suggested REQ-IDs` columns reflect the final state.

    7. **Final consistency assertion:** Add a line at the end of the document: `**Validated:** All N proposals cross-checked against 69-RESEARCH.md High rows on [date]. Plan 03 performs final quality gate.`
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
path = '.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-PROPOSALS.md'
content = open(path).read()
proposals = re.findall(r'^## Proposal \d+:', content, re.MULTILINE)
no_proposals_section = '## No Follow-on Proposals' in content
n_proposals = len(proposals)
if n_proposals == 0 and not no_proposals_section:
    print('FAIL: zero proposals and no No-Follow-on-Proposals section')
    sys.exit(1)
header_count_match = re.search(r'^\*\*Proposal count:\*\* (\d+)', content, re.MULTILINE)
if not header_count_match:
    print('FAIL: no Proposal count in header')
    sys.exit(1)
if int(header_count_match.group(1)) != n_proposals:
    print(f'FAIL: header count {header_count_match.group(1)} != actual {n_proposals}')
    sys.exit(1)
for i, prop in enumerate(proposals, 1):
    prop_body = content.split(prop)[1].split('## Proposal')[0] if i < n_proposals else content.split(prop)[1]
    for required in ['### Goal', '### Scope', '### Success Criteria', '### Suggested Requirement IDs', '### Estimated Plan Count', '### Rationale']:
        if required not in prop_body:
            print(f'FAIL: Proposal {i} missing {required}')
            sys.exit(1)
print(f'PASS: {n_proposals} proposals, all template-complete')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every High row in RESEARCH.md is cited by at least one proposal's `Derived from:` line.
    - No proposal cites a non-High row.
    - Every `## Proposal N:` section contains all six template subsections (Goal, Scope, Success Criteria, Suggested REQ IDs, Estimated Plan Count, Rationale).
    - Every Success Criteria bullet is measurable (contains a verb from the allowed list in action step 3).
    - All `PARITY-*-NN` IDs across all proposals are unique and follow the naming convention.
    - Every `Estimated Plan Count` is an integer in {3, 4, 5}.
    - The summary table at the top of the document matches the final proposal count exactly.
    - A `**Validated:**` footer line exists.
  </acceptance_criteria>
  <done>69-PROPOSALS.md is internally consistent, cross-validated against RESEARCH.md, and every proposal is template-complete and ready for Plan 03's final quality gate.</done>
</task>

</tasks>

<threat_model>
Documentation-only phase. No code, config, or API surface changes. Threats considered: (1) inaccurate evidence citations leading downstream phases astray — mitigated by requiring file-path+line-number citations (inherited from Plan 01's RESEARCH.md) and bijection cross-check in Task 2; (2) biased gap severity scoring leading to poor prioritization — mitigated by the explicit severity rubric in CONTEXT.md D-14/D-16 and the D-15 cardinality re-check when proposal count falls outside the 2–5 range.
</threat_model>

<verification>
Grep-verifiable post-conditions:
- `grep -c "^## Proposal [0-9]" 69-PROPOSALS.md` matches the integer in the `**Proposal count:**` header line.
- `grep -c "^### Goal$" 69-PROPOSALS.md` equals the proposal count.
- `grep -c "^### Scope$" 69-PROPOSALS.md` equals the proposal count.
- `grep -c "^### Success Criteria" 69-PROPOSALS.md` equals the proposal count.
- `grep -c "^### Estimated Plan Count" 69-PROPOSALS.md` equals the proposal count.
- `grep -cE "PARITY-(MACRO|BUILDER|TYPED|HANDLER|CLIENT|ERR)-[0-9]{2}" 69-PROPOSALS.md` ≥ 2 × proposal count (average 2+ IDs per proposal).
- `grep -q "^\*\*Validated:\*\*" 69-PROPOSALS.md` — validation footer present.
</verification>

<success_criteria>
- `69-PROPOSALS.md` exists and is separate from `69-RESEARCH.md` (per D-19).
- Proposal count is between 2 and 5 (per D-15), OR the document explicitly documents why the count is outside that range.
- Every proposal has all six template subsections.
- Every proposal derives from at least one High-severity row in 69-RESEARCH.md.
- Every High-severity row in 69-RESEARCH.md is addressed by at least one proposal.
- Every proposal has 3–5 success criteria, 2–4 suggested requirement IDs, and 3–5 estimated plans.
- No proposal frames a fix as "adopt rmcp's X" — all fixes are pmcp-native.
</success_criteria>

<output>
After completion, create `.planning/phases/69-rmcp-parity-research-gap-analysis-across-ergonomics-transpor/69-02-SUMMARY.md` summarizing: proposal count, list of proposal titles, collected `PARITY-*` REQ-IDs ready for Plan 03 to land in REQUIREMENTS.md, and total estimated plan count across all proposals.
</output>
