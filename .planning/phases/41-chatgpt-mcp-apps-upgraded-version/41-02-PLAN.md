---
phase: 41-chatgpt-mcp-apps-upgraded-version
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mcp-preview/assets/widget-runtime.mjs
  - crates/mcp-preview/assets/index.html
autonomous: true
requirements: [P41-04]

must_haves:
  truths:
    - "Widget sends ui/initialize (not ui/ready) for handshake"
    - "Host sends ui/notifications/tool-input and ui/notifications/tool-result to widget"
    - "Widget sends ui/message (not ui/sendMessage) and ui/open-link (not ui/openLink)"
    - "Host sends ui/resource-teardown (not ui/teardown) to widget"
    - "AppBridge host accepts both old and new method names from widgets for backward compat"
  artifacts:
    - path: "crates/mcp-preview/assets/widget-runtime.mjs"
      provides: "Updated App class with correct protocol method names"
      contains: "ui/notifications/tool-input"
    - path: "crates/mcp-preview/assets/index.html"
      provides: "Updated AppBridge with correct notification method names"
      contains: "ui/notifications/tool-input"
  key_links:
    - from: "crates/mcp-preview/assets/widget-runtime.mjs"
      to: "crates/mcp-preview/assets/index.html"
      via: "postMessage JSON-RPC bridge"
      pattern: "ui/initialize"
---

<objective>
Update the bridge protocol method names in both widget-runtime.mjs (App class, widget side) and index.html (AppBridge class, host side) to match the official ChatGPT MCP Apps protocol.

Purpose: The current bridge uses non-standard method names (ui/toolInput, ui/sendMessage, etc.) that differ from the official spec (ui/notifications/tool-input, ui/message, etc.).
Output: Bridge protocol aligned with ChatGPT's actual implementation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-chatgpt-mcp-apps-upgraded-version/41-RESEARCH.md

@crates/mcp-preview/assets/widget-runtime.mjs
@crates/mcp-preview/assets/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update widget-runtime.mjs App class method names</name>
  <files>crates/mcp-preview/assets/widget-runtime.mjs</files>
  <action>
Update the App class in widget-runtime.mjs to use the correct ChatGPT MCP Apps protocol method names.

**Outgoing methods (Widget -> Host):**
1. `ui/initialize` -- already correct, keep as-is
2. `ui/sendMessage` -> `ui/message` (the `sendMessage()` method body)
3. `ui/openLink` -> `ui/open-link` (the `openLink()` method body)
4. `ui/log` -- keep as-is (SDK extension, not part of ChatGPT protocol)

**Incoming notification handler (_handleNotification cases):**
1. `case "ui/toolInput":` -> `case "ui/notifications/tool-input":`
2. `case "ui/toolResult":` -> `case "ui/notifications/tool-result":`
3. `case "ui/toolCancelled":` -> `case "ui/notifications/tool-cancelled":`
4. `case "ui/hostContextChanged":` -> keep as-is (SDK extension) OR add `case "ui/notifications/context-changed":` as alias
5. `case "ui/teardown":` -> `case "ui/resource-teardown":`

**Backward compatibility:** Also keep OLD case labels as fallbacks. Use fall-through:
```javascript
case "ui/notifications/tool-input":
case "ui/toolInput":  // backward compat
    this._onToolInput(params);
    break;
```

This ensures widgets work with both old-style hosts and new ChatGPT-style hosts.
  </action>
  <verify>
    <automated>cargo build -p mcp-preview</automated>
  </verify>
  <done>widget-runtime.mjs uses official method names as primary, with old names as fallback; App class sends ui/message and ui/open-link</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html AppBridge host-side method names</name>
  <files>crates/mcp-preview/assets/index.html</files>
  <action>
Update the AppBridge class in index.html to use correct ChatGPT MCP Apps protocol method names.

**Outgoing notifications (Host -> Widget):**
1. `sendToolInput()` method: change `this._transport.notify("ui/toolInput", ...)` -> `this._transport.notify("ui/notifications/tool-input", ...)`
2. `sendToolResult()` method: change `this._transport.notify("ui/toolResult", ...)` -> `this._transport.notify("ui/notifications/tool-result", ...)`
3. `sendHostContextChanged()` method: change `this._transport.notify("ui/hostContextChanged", ...)` -> `this._transport.notify("ui/notifications/context-changed", ...)`
4. Teardown: change `this._transport.notify("ui/teardown")` -> `this._transport.notify("ui/resource-teardown")`

**Incoming request handler (_handleRequest cases):**
1. `case "ui/initialize":` -- already correct, keep
2. `case "ui/sendMessage":` -> add `case "ui/message":` as primary, keep old as fallback:
   ```javascript
   case "ui/message":
   case "ui/sendMessage":  // backward compat
   ```
3. `case "ui/openLink":` -> add `case "ui/open-link":` as primary, keep old as fallback:
   ```javascript
   case "ui/open-link":
   case "ui/openLink":  // backward compat
   ```

**Also update any other references** in index.html that mention the old method names in comments or string literals.

**After `ui/initialize` response**, send `ui/notifications/initialized` notification to the widget:
```javascript
case "ui/initialize":
    // ... existing capability negotiation ...
    // After responding, send initialized notification
    this._transport.notify("ui/notifications/initialized", {});
    break;
```
  </action>
  <verify>
    <automated>cargo build -p mcp-preview</automated>
  </verify>
  <done>AppBridge sends notifications using official method names; accepts both old and new names from widgets; sends ui/notifications/initialized after handshake</done>
</task>

</tasks>

<verification>
cargo build -p mcp-preview
cargo clippy -p mcp-preview -- -D warnings
</verification>

<success_criteria>
- widget-runtime.mjs App class uses `ui/notifications/tool-input`, `ui/notifications/tool-result`, `ui/message`, `ui/open-link`, `ui/resource-teardown`
- index.html AppBridge uses matching names for outgoing notifications
- Both sides accept old method names as fallback for backward compatibility
- AppBridge sends `ui/notifications/initialized` after handshake
- mcp-preview builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/41-chatgpt-mcp-apps-upgraded-version/41-02-SUMMARY.md`
</output>
