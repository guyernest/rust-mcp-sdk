---
phase: 90.2
slug: openapi-built-in-server-advanced-example-contoso-m365-oauth
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-30
---

# Phase 90.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Task IDs below are provisional (requirement-level) until the planner finalizes plans/waves.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust) + `mdbook` for docs |
| **Config file** | none — Cargo workspace (`crates/pmcp-openapi-server`) |
| **Quick run command** | `cargo test -p pmcp-openapi-server --test parity_replay -- --test-threads=1` |
| **Full suite command** | `cargo test -p pmcp-openapi-server -- --test-threads=1 && make book && (cd pmcp-course && mdbook build)` |
| **Estimated runtime** | ~45–90 seconds |

---

## Sampling Rate

- **After every task commit:** Run the quick command (`parity_replay` single-threaded).
- **After every plan wave:** Run the full suite command.
- **Before `/gsd:verify-work`:** Full suite green (parity + Code Mode Rust test + both books render).
- **Max feedback latency:** 90 seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 90.2-0X-01 | curated spec | 1 | P902-SPEC | — | N/A | fixture-validity | `cargo test -p pmcp-openapi-server --test parity_replay contoso -- --test-threads=1` (spec parses via OpenApiSchema::parse) | ❌ W0 | ⬜ pending |
| 90.2-0X-02 | config | 1 | P902-CONFIG | T-90.2-secrets | only `${...}` placeholders + dev-guarded token_secret reach disk | fixture-validity | `cargo test … contoso_fixture` (from_toml_strict_validated accepts; grep `type = "oauth_passthrough"`, both tool names, `customer_id`) | ❌ W0 | ⬜ pending |
| 90.2-0X-03 | fixture data | 1 | P902-FIXTURE | — | inert static workbook data; reference date pinned (no wall-clock) | unit/fixture | `cargo test … contoso_fixture` (≥1 match + ≥1 non-match for >100-in-3mo over pinned date) | ❌ W0 | ⬜ pending |
| 90.2-0X-04 | parity + passthrough | 2 | P902-PARITY | T-90.2-passthrough | forwarded `Authorization: Bearer <user-tok>` reaches Graph mock; fails if absent/wrong | integration (wiremock + ServerTester api_key) | `cargo test -p pmcp-openapi-server --test parity_replay contoso_parity -- --test-threads=1` | ❌ W0 | ⬜ pending |
| 90.2-0X-05 | Code Mode headline | 2 | P902-CODEMODE | — | deterministic result over pinned fixture; no `now` | integration (JsCodeExecutor over token-threaded HttpCodeExecutor) | `cargo test -p pmcp-openapi-server --test … contoso_code_mode -- --test-threads=1` (asserts exact customer-id set) | ❌ W0 | ⬜ pending |
| 90.2-0X-06 | pointable example + min.rs | 2 | P902-EXAMPLE | T-90.2-secrets | no real credential in shipped example | example-run + smoke | `cargo run --example contoso_m365_min -p pmcp-openapi-server` exits 0; `cargo test … pointable_contoso_example_parses` | ❌ W0 | ⬜ pending |
| 90.2-0X-07 | book chapter | 1 | P902-DOCS-BOOK | — | governance note (admin ceiling + per-user token + no standing creds) | doc-render + grep | `make book` exits 0; grep chapter for `oauth_passthrough`, both tools, headline query, SUMMARY.md nav | ❌ W0 | ⬜ pending |
| 90.2-0X-08 | course chapter | 1 | P902-DOCS-COURSE | — | same content, course voice | doc-render + grep | `(cd pmcp-course && mdbook build)` exits 0; grep + course SUMMARY.md nav | ❌ W0 | ⬜ pending |
| 90.2-0X-09 | live scaffold | 2 | P902-PARITY (live) | — | real-tenant gated; not run in CI | live (`#[ignore]` + env-gated) | `PMCP_GRAPH_LIVE_TEST=1 … cargo test contoso_live -- --ignored` (best-effort, NOT a gate) | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml` — config fixture (P902-CONFIG)
- [ ] `crates/pmcp-openapi-server/tests/fixtures/contoso-m365.yaml` (or equivalent) — curated Graph spec (P902-SPEC)
- [ ] `crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml` — scenario replay (P902-PARITY)
- [ ] New `#[test]` functions in `parity_replay.rs` (or a sibling `contoso_parity.rs`) — fixture-validity + parity + passthrough asserts
- [ ] A Rust Code Mode test (extend `oauth_passthrough_e2e.rs` seam or a new `contoso_code_mode.rs`) — P902-CODEMODE deterministic-set assertion
- [ ] No new framework install — `cargo test`, `wiremock`, `mcp-tester`, `mdbook` already present from 90.1

*Existing 90.1 infrastructure (wiremock harness, ServerTester, mdbook) covers all phase requirements; this phase adds fixtures + test functions only.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real Microsoft Graph delegated read against a live tenant | P902-PARITY (live) | Requires a real Azure AD tenant, app registration, admin consent, real workbook — cannot run in CI | Set `PMCP_GRAPH_LIVE_TEST=1` + real tenant/app/user env vars, run the `#[ignore]` live test; optional/best-effort, NOT the completion gate |

*All other phase behaviors have automated verification.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (delivered by Plan 01/02/03 tasks)
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-30
