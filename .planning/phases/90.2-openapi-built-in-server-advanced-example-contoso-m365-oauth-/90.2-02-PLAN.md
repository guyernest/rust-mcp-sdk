---
phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/pmcp-openapi-server/tests/contoso_m365_parity.rs
  - crates/pmcp-openapi-server/examples/contoso-m365.toml
  - crates/pmcp-openapi-server/examples/contoso_m365_min.rs
autonomous: true
requirements: [P902-PARITY, P902-EXAMPLE, P902-SPEC, P902-CONFIG, P902-FIXTURE]
must_haves:
  truths:
    - "Offline parity test calls get_customer/get_customer_orders for a known customer_id and asserts returned rows match the fixture"
    - "The same test asserts the inbound user bearer was FORWARDED to the Graph backend and FAILS if absent/wrong"
    - "Fixture-validity test parses the config + spec and asserts oauth_passthrough + both script tools"
    - "cargo run --example contoso_m365_min exits 0 without serving"
    - "A smoke test parses examples/contoso-m365.toml via from_toml_strict_validated"
  artifacts:
    - path: "crates/pmcp-openapi-server/tests/contoso_m365_parity.rs"
      provides: "Offline wiremock replay + forwarded-bearer passthrough assertion + fixture/spec validity + pointable smoke"
      contains: "contoso-user-tok"
    - path: "crates/pmcp-openapi-server/examples/contoso-m365.toml"
      provides: "Shipped pointable config (same surface as the tests/ fixture)"
      contains: 'type = "oauth_passthrough"'
    - path: "crates/pmcp-openapi-server/examples/contoso_m365_min.rs"
      provides: "Build-and-exit example (dispatch + build_server, no serve)"
      contains: "build_server"
  key_links:
    - from: "ServerTester::new 4th arg"
      to: "wiremock Graph mock authorization matcher"
      via: "inbound bearer injected into extra_headers, captured, forwarded"
      pattern: "Some\\(\"contoso-user-tok\"\\)"
    - from: "recorded backend requests"
      to: "Authorization: Bearer contoso-user-tok"
      via: "per-request header assertion (fails if absent)"
      pattern: "Bearer contoso-user-tok"
---

<objective>
Prove the two Contoso tools work offline AND that delegated `oauth_passthrough` forwards the inbound user bearer to the Graph backend, then ship the user-pointable example. The parity test mirrors 90.1's `parity_replay.rs` but passes `Some("contoso-user-tok")` as the `ServerTester::new` 4th arg so the wiremock Graph mock can REQUIRE and the test can ASSERT the forwarded `Authorization: Bearer` header — the passthrough proof.

Purpose: P902-PARITY (tool correctness + forwarded-bearer) + P902-EXAMPLE (shipped pointable config + build-only example) + the fixture/spec-validity gates for P902-SPEC/CONFIG/FIXTURE.
Output: `tests/contoso_m365_parity.rs`, `examples/contoso-m365.toml`, `examples/contoso_m365_min.rs`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-SPEC.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-RESEARCH.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-PATTERNS.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-01-SUMMARY.md

<interfaces>
<!-- From crates/mcp-tester/src/tester.rs:90-101 — the inbound-bearer hook (RQ-1). -->
<!-- ServerTester::new(url, timeout, insecure, api_key: Option<&str>, force_transport: Option<&str>, http_middleware_chain) -->
<!-- When api_key=Some("tok") AND http_middleware_chain=None AND force_transport=Some("http"): -->
<!--   extra_headers gets ("Authorization", "Bearer tok") + ("X-API-Key", "tok"). -->

<!-- From crates/pmcp-openapi-server/src/lib.rs — the real binary seam reused offline: -->
<!-- pub use ... build_server, dispatch, run_serving, Args (london_tube_min.rs + parity_replay.rs already import build_server + dispatch). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contoso_m365_parity.rs — fixture/spec validity + offline wiremock parity + forwarded-bearer passthrough assertion + pointable smoke</name>
  <files>crates/pmcp-openapi-server/tests/contoso_m365_parity.rs</files>
  <read_first>
    - crates/pmcp-openapi-server/tests/parity_replay.rs (THE analog — copy these pieces: fixtures_dir/examples_dir helpers; assert_london_tube_code_mode_surface lines 56-73; pointable_example_config_parses_and_validates lines 80-95; london_tube_fixture lines 100-180 incl. the auth matches! at 144-151, is_script_tool at 159-162, OpenApiSchema::parse + operation_for at 165-179; mount_london_tube 192-211; temp_config_pointing_at 216-225; london_tube_parity_through_real_binary_path 235-348 incl. recorded-request assertion 316-337; parity_live_tfl 363-438)
    - crates/mcp-tester/src/tester.rs:90-101 (ServerTester::new 4th-arg inbound bearer hook — pass Some("contoso-user-tok"))
    - crates/pmcp-openapi-server/tests/oauth_passthrough_e2e.rs:18-20 (positional verify-filter test-fn naming lesson)
    - 90.2-01-SUMMARY.md (the customer_id->address mapping, exact workbook rows, shipped spec path form, resource URIs + prompt name)
  </read_first>
  <action>
    Create tests/contoso_m365_parity.rs mirroring parity_replay.rs. Prefix EVERY test fn `contoso_m365_*` (Pitfall 5 — the positional verify filter resolves on the file stem).
    Copy + re-skin a `assert_contoso_code_mode_surface(cfg, label)` helper asserting the three resource URIs + the `start_code_mode` prompt name (the exact URIs/name recorded in 90.2-01-SUMMARY) — reused by BOTH the fixture test and the pointable smoke to prevent drift.
    `contoso_m365_fixture` test: load tests/fixtures/contoso-m365.toml via `ServerConfig::from_toml_strict_validated`; assert both tool names present; assert the auth is `AuthConfig::OAuthPassthrough { .. }` via `matches!` (ADAPTED from london-tube's ApiKey match at 144-151); assert BOTH tools `is_script_tool()`; parse tests/fixtures/contoso-m365-api.yaml via `OpenApiSchema::parse` and confirm `operation_for` resolves the range-read op (asserts P902-SPEC parses — whichever path form Plan 01 shipped); run `assert_contoso_code_mode_surface`.
    `contoso_m365_pointable_example_parses` test: copy pointable_example_config_parses_and_validates pointed at examples/contoso-m365.toml; assert parses + `assert_contoso_code_mode_surface`.
    `mount_contoso(server)` helper: mount the wiremock Graph range-read mock(s) — `method("GET")`, `path("/drives/CONTOSO_DRIVE/items/CUSTOMERS_ITEM/workbook/worksheets/Customers/range(address='<the C007 address from SUMMARY>')")`, AND `.and(header("authorization", "Bearer contoso-user-tok"))` (PASSTHROUGH PROOF — the mock REQUIRES the forwarded bearer), responding with `set_body_json` of the Graph workbookRange shape `{ "address": ..., "values": [[ <the C007 row> ]] }`; mount a second matcher for the get_customer_orders block address. Use the addresses + row values recorded in 90.2-01-SUMMARY.
    `contoso_m365_parity_through_real_binary_path` test (mirror london_tube_parity_through_real_binary_path): stand up the wiremock backend via mount_contoso, write a temp config pointing [backend] base_url at the mock (temp_config_pointing_at), `run_serving` the real binary, build `ServerTester::new(&url, Duration::from_secs(30), false, Some("contoso-user-tok"), Some("http"), None)` (the 4th arg is the KEY DIFFERENCE vs london-tube's None), replay contoso-m365-scenarios.yaml gating per-step success. THEN assert the forwarded header on EVERY recorded backend request: `assert_eq!(req.headers.get("authorization").map(to_str), Some("Bearer contoso-user-tok"))` (adapted from london-tube's app_key=dummy URL check at 325-337) — this FAILS if the bearer is absent/wrong, proving passthrough.
    `contoso_m365_live` scaffold (mirror parity_live_tfl): `#[ignore]` + `PMCP_OPENAPI_LIVE_TEST=1`-gated, best-effort, NOT the gate.
    NOTE: no `std::env::set_var` for a secret is needed (the bearer comes from the inbound token, not env). Run single-threaded.
  </action>
  <verify>
    <automated>cargo test -p pmcp-openapi-server --test contoso_m365_parity -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-openapi-server --test contoso_m365_parity -- --test-threads=1` passes
    - `grep -c 'Some("contoso-user-tok")' crates/pmcp-openapi-server/tests/contoso_m365_parity.rs` returns >= 1 (the ServerTester 4th arg)
    - `grep -c 'Bearer contoso-user-tok' ...contoso_m365_parity.rs` returns >= 2 (mock requires it + recorded-request assertion)
    - `grep -Ec 'fn contoso_m365_' ...contoso_m365_parity.rs` returns >= 4 (fixture, pointable, parity, live)
    - Two-sided passthrough proof: temporarily removing the `Some("contoso-user-tok")` (4th arg -> None) makes `contoso_m365_parity_through_real_binary_path` FAIL (the mock requires the bearer); restore -> green. Record this in the SUMMARY.
    - `grep -c 'OAuthPassthrough' ...contoso_m365_parity.rs` returns >= 1 (the auth-variant assertion)
  </acceptance_criteria>
  <done>contoso_m365_parity.rs passes single-threaded offline; asserts config+spec validity, both tools return fixture rows, and the inbound bearer is forwarded to the Graph mock (fails if absent); live scaffold is #[ignore]+env-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Ship examples/contoso-m365.toml (pointable) + examples/contoso_m365_min.rs (build-and-exit)</name>
  <files>crates/pmcp-openapi-server/examples/contoso-m365.toml, crates/pmcp-openapi-server/examples/contoso_m365_min.rs</files>
  <read_first>
    - crates/pmcp-openapi-server/examples/london-tube.toml (analog — pointable-config header comment block + body)
    - crates/pmcp-openapi-server/examples/london_tube_min.rs (analog — build-and-exit: from_toml_strict_validated -> dispatch -> build_server -> print summary -> return WITHOUT serving)
    - crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml (Plan 01 — the pointable example MUST carry the SAME surface: same auth, tools, resource URIs, prompt; assert_contoso_code_mode_surface checks both)
    - crates/pmcp-openapi-server/Cargo.toml:14 (exclude = ["tests/"] — examples/ ships, tests/ does NOT, so the pointable config MUST live under examples/)
  </read_first>
  <action>
    Create examples/contoso-m365.toml by copying the tests/fixtures/contoso-m365.toml body (Plan 01) and prepending the london-tube.toml pointable header comment block (re-skinned to Contoso, explaining the "point pmcp-openapi-server --config at me" role). Keep the auth, both script tools, the three resource URIs, the start_code_mode prompt, and the dev-guarded code_mode IDENTICAL to the fixture so `assert_contoso_code_mode_surface` passes for both (drift guard).
    Create examples/contoso_m365_min.rs by copying london_tube_min.rs and re-skinning the embedded `CONFIG` const: `use pmcp_openapi_server::{build_server, dispatch};` + `use pmcp_server_toolkit::ServerConfig;`; `#[tokio::main] async fn main()` that does `ServerConfig::from_toml_strict_validated(CONFIG)?` -> `dispatch(&cfg).await?` (no network) -> `build_server(&cfg, connector, http_exec, None)?` (None = curated-only, no spec) -> prints the `name + N tool(s)/resource(s)/prompt(s)` summary -> `Ok(())` returning WITHOUT serving. The embedded `CONFIG` is a TRIMMED contoso config (oauth_passthrough + the get_customer script tool + >=1 resource + the prompt + dev-guarded inline token_secret) so `cargo run` needs no env. NO real secret.
    Examples are auto-discovered (london_tube_min.rs has no `[[example]]` block) — no Cargo.toml edit needed.
  </action>
  <verify>
    <automated>cargo run --example contoso_m365_min -p pmcp-openapi-server</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run --example contoso_m365_min -p pmcp-openapi-server` exits 0 and RETURNS (does not hang/serve)
    - `grep -c 'type = "oauth_passthrough"' crates/pmcp-openapi-server/examples/contoso-m365.toml` returns 1
    - `grep -c 'build_server' crates/pmcp-openapi-server/examples/contoso_m365_min.rs` returns >= 1 AND `grep -c '\.serve\|StreamableHttpServer' ...contoso_m365_min.rs` returns 0 (no serving)
    - `contoso_m365_pointable_example_parses` (Plan 02 Task 1) passes — examples/contoso-m365.toml parses + matches the code-mode surface
    - No real secret: `grep -E '\$\{TENANT|\$\{CLIENT_SECRET|eyJ' ...examples/contoso-m365.toml ...examples/contoso_m365_min.rs` returns nothing beyond `${VAR}` placeholders
  </acceptance_criteria>
  <done>examples/contoso-m365.toml ships the same surface as the fixture (drift-guarded); contoso_m365_min.rs builds-and-exits 0 without serving; both carry no real secret.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> server | inbound `Authorization: Bearer` is untrusted client input; captured per-request, never stored |
| server -> Graph backend | the forwarded user token is the ONLY credential; the server holds no standing credential |
| repo commit -> published crate | examples/ ships in the tarball — highest-exposure artifact for a leaked secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.2-passthrough-02 | Spoofing / Elevation of Privilege | oauth_passthrough relay | mitigate | The parity test ASSERTS the forwarded `Authorization: Bearer contoso-user-tok` on every recorded backend request and FAILS if absent/wrong (two-sided proof recorded) — guarantees the server forwards the user's token and never substitutes a server-held credential. |
| T-90.2-secrets-02 | Information Disclosure | examples/contoso-m365.toml + contoso_m365_min.rs (SHIPPED) | mitigate | Only `${VAR}` placeholders + the dev-guarded inline `token_secret`; acceptance grep rejects real tenant/client secrets and JWT-shaped literals. |
| T-90.2-content-02 | Tampering | wiremock canned responses | accept | Test-only inert JSON authored by us; no live Graph contacted in CI. |
</threat_model>

<verification>
- `cargo test -p pmcp-openapi-server --test contoso_m365_parity -- --test-threads=1` green.
- `cargo run --example contoso_m365_min -p pmcp-openapi-server` exits 0.
- Two-sided passthrough proof recorded (None 4th arg -> fail; Some -> pass).
</verification>

<success_criteria>
- Offline parity test proves tool correctness AND forwarded-bearer passthrough (fails without the bearer).
- Fixture + spec + pointable-example validity gates pass.
- Pointable example ships under examples/ with the same surface as the fixture; build-only example exits 0 without serving.
- No real secret in any shipped artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-02-SUMMARY.md`. RECORD: the two-sided passthrough proof result (4th-arg None -> fail, Some -> pass), the resolved spec path form confirmed by OpenApiSchema::parse, and the wiremock addresses/rows used.
</output>
