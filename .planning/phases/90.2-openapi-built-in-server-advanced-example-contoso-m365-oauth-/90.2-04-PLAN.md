---
phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pmcp-book/src/openapi-graph-m365.md
  - pmcp-book/src/SUMMARY.md
  - pmcp-course/src/openapi-graph-m365.md
  - pmcp-course/src/SUMMARY.md
autonomous: true
requirements: [P902-DOCS-BOOK, P902-DOCS-COURSE]
must_haves:
  truths:
    - "A new dedicated book chapter exists, is wired into pmcp-book SUMMARY.md, and make book exits 0"
    - "A new dedicated course chapter exists, is wired into pmcp-course SUMMARY.md, and mdbook build exits 0"
    - "Both chapters explain admin-consent governance + per-user passthrough + no standing credentials, show the oauth_passthrough config block, both tool names, and the headline Code Mode query"
  artifacts:
    - path: "pmcp-book/src/openapi-graph-m365.md"
      provides: "Book advanced-example chapter (governance + Excel narrative + two tools + headline query)"
      contains: "oauth_passthrough"
    - path: "pmcp-course/src/openapi-graph-m365.md"
      provides: "Course chapter, same substance in course voice"
      contains: "oauth_passthrough"
  key_links:
    - from: "pmcp-book/src/SUMMARY.md"
      to: "openapi-graph-m365.md"
      via: "nav line sibling of the existing OpenAPI chapter"
      pattern: "openapi-graph-m365.md"
    - from: "pmcp-course/src/SUMMARY.md"
      to: "./openapi-graph-m365.md"
      via: "nav line sibling of the existing OpenAPI chapter"
      pattern: "openapi-graph-m365.md"
---

<objective>
Write the two dedicated advanced-example chapters (book + course) telling the Contoso M365 story: keep your existing Excel files, connect them to AI via MCP; a business analyst curates the API slice; `oauth_passthrough` means an admin consents once to a bounded scope (the ceiling) while the signed-in user's forwarded token governs per-file access and the server holds no standing credentials. Walk through the two `customer_id`-keyed tools and the headline Code Mode query. Match 90.1 chapter depth (D-05: concise config walkthrough + 1-2 paragraph governance note — NOT a Mermaid diagram or a threat subsection).

Purpose: P902-DOCS-BOOK + P902-DOCS-COURSE.
Output: `pmcp-book/src/openapi-graph-m365.md` (+ SUMMARY nav), `pmcp-course/src/openapi-graph-m365.md` (+ SUMMARY nav).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-SPEC.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-CONTEXT.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pmcp-book/src/openapi-graph-m365.md + wire into pmcp-book SUMMARY.md</name>
  <files>pmcp-book/src/openapi-graph-m365.md, pmcp-book/src/SUMMARY.md</files>
  <read_first>
    - pmcp-book/src/openapi-built-in-server.md (THE analog — 295 lines; reuse section skeleton: "The Problem (Why Config, Not Code)" line 17, "Outgoing Authentication" line 144, "Customize Through Config — Two Kinds of Tools" line 103, "Resources & Prompts (Code Mode Context)" line 175, "The OpenAPI Spec Is Optional" line 254, "What You Built" line 282)
    - pmcp-book/src/SUMMARY.md:38 (existing OpenAPI nav line — add a sibling line immediately after it)
    - crates/pmcp-openapi-server/examples/contoso-m365.toml (Plan 02 — the actual config block to show, if available; else the locked oauth_passthrough block from PATTERNS Pattern 1)
    - .planning/phases/90.2-.../90.2-PATTERNS.md (§ pmcp-book/src/openapi-graph-m365.md — heading reuse map + required greps)
  </read_first>
  <action>
    Create pmcp-book/src/openapi-graph-m365.md re-skinning the analog's section flow to Contoso/Graph/Excel (D-06 framing: org "Contoso", "Sales" SharePoint site, `Customers.xlsx`, customers Northwind Traders / Fabrikam / Tailspin Toys).
    - "The Problem" section -> the "keep your existing Excel files, connect them to AI" headline + the business-analyst-curates-the-API-slice point.
    - "Outgoing Authentication" section -> THE HEADLINE governance note (1-2 paragraphs, D-05): show the `[backend.auth] type = "oauth_passthrough"` config block; explain admin consents ONCE to a bounded scope (the ceiling of what the server may request); the signed-in user's forwarded token governs per-file access; the server holds NO standing credentials and can only ever act as the calling user. At most ONE sentence may mention `oauth2_client_credentials` as a contrast (boundary — do not build it).
    - "Two Kinds of Tools" section -> the two script tools `get_customer` and `get_customer_orders` (each `customer_id`-keyed, templating the Excel range address), with everything richer left to Code Mode.
    - "Resources & Prompts" + "OpenAPI Spec Is Optional" sections -> re-skin briefly.
    - "What You Built" -> the headline Code Mode query "customers who bought more than 100 in the last 3 months" (use the exact phrasing; if 90.2-03-SUMMARY exists, reference the deterministic result framing accurately).
    Then add the SUMMARY.md nav line immediately after pmcp-book/src/SUMMARY.md:38, e.g. `- [Chapter 12.12: OpenAPI over Microsoft Graph (Contoso M365)](openapi-graph-m365.md)`.
    Use doctest-safe / mdbook-safe fenced blocks (no executable rust fences that would fail `make book`'s build; toml/text/json fences are fine).
  </action>
  <verify>
    <automated>make book</automated>
  </verify>
  <acceptance_criteria>
    - `make book` exits 0
    - `grep -c 'oauth_passthrough' pmcp-book/src/openapi-graph-m365.md` returns >= 1
    - `grep -c 'get_customer' pmcp-book/src/openapi-graph-m365.md` returns >= 1 AND `grep -c 'get_customer_orders' ...` returns >= 1
    - `grep -Ec 'last 3 months|bought more than 100|>100' pmcp-book/src/openapi-graph-m365.md` returns >= 1 (the headline query)
    - `grep -Eic 'admin consent|standing credential|bounded scope|on behalf|as the calling user' pmcp-book/src/openapi-graph-m365.md` returns >= 1 (governance explanation)
    - `grep -c 'openapi-graph-m365.md' pmcp-book/src/SUMMARY.md` returns 1 (nav wired)
    - At most ONE mention of client-credentials: `grep -ci 'client_credentials\|client-credentials' pmcp-book/src/openapi-graph-m365.md` returns <= 1
  </acceptance_criteria>
  <done>Book chapter exists with governance note + oauth_passthrough block + both tool names + headline query; wired into SUMMARY.md; make book exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Write pmcp-course/src/openapi-graph-m365.md + wire into pmcp-course SUMMARY.md</name>
  <files>pmcp-course/src/openapi-graph-m365.md, pmcp-course/src/SUMMARY.md</files>
  <read_first>
    - pmcp-course/src/openapi-built-in-server.md (THE analog — 319 lines; reuse skeleton: "What You'll Learn" line 20, "Step 4: Authenticate to the Backend" line 154, "Step 3: Two Kinds of Tools" line 112, "Resources & Prompts (Code Mode Context)" line 191, "Exercise:" line 288, "Key Takeaways" line 307)
    - pmcp-course/src/SUMMARY.md:78 (existing OpenAPI nav line — add a sibling line after it)
    - pmcp-book/src/openapi-graph-m365.md (Task 1 — same substance; the course chapter is the tighter-voice sibling)
    - .planning/phases/90.2-.../90.2-PATTERNS.md (§ pmcp-course/src/openapi-graph-m365.md — heading reuse map)
  </read_first>
  <action>
    Create pmcp-course/src/openapi-graph-m365.md with the SAME substance as the book chapter (Task 1) in the tighter course voice, reusing the course skeleton headings. Re-skin "Step 4: Authenticate to the Backend" to the passthrough governance note (D-05 — admin-consent ceiling + per-user forwarded token + no standing credentials), "Step 3: Two Kinds of Tools" to `get_customer`/`get_customer_orders`, and include the headline Code Mode query phrasing and an "Exercise:" + "Key Takeaways" closing. At most ONE sentence on client-credentials.
    Add the SUMMARY.md nav line after pmcp-course/src/SUMMARY.md:78, e.g. `- [OpenAPI over Microsoft Graph (Contoso M365)](./openapi-graph-m365.md)`.
    mdbook-safe fences only.
  </action>
  <verify>
    <automated>cd pmcp-course && mdbook build</automated>
  </verify>
  <acceptance_criteria>
    - `cd pmcp-course && mdbook build` exits 0
    - `grep -c 'oauth_passthrough' pmcp-course/src/openapi-graph-m365.md` returns >= 1
    - `grep -c 'get_customer' pmcp-course/src/openapi-graph-m365.md` returns >= 1 AND `grep -c 'get_customer_orders' ...` returns >= 1
    - `grep -Ec 'last 3 months|bought more than 100|>100' pmcp-course/src/openapi-graph-m365.md` returns >= 1
    - `grep -Eic 'admin consent|standing credential|bounded scope|on behalf|as the calling user' pmcp-course/src/openapi-graph-m365.md` returns >= 1
    - `grep -c 'openapi-graph-m365.md' pmcp-course/src/SUMMARY.md` returns 1
  </acceptance_criteria>
  <done>Course chapter exists with the same substance in course voice; wired into the course SUMMARY.md; mdbook build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> reader | the governance narrative shapes how operators reason about the trust model; inaccuracy would mislead |
| committed doc -> public | published mdbook content is public; no secret may appear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.2-docs-01 | Information Disclosure | chapter config blocks | mitigate | Config snippets show only `${VAR}` placeholders + the dev-guarded inline token_secret with a LOUD replace-for-production note; no real tenant/client/user credential. |
| T-90.2-docs-02 | Tampering (misinformation) | governance narrative | mitigate | The governance claims (admin-consent ceiling, per-user forwarded token, no standing credential) are traced to the behavior proven by Plan 02/03 passthrough tests — not aspirational. |
</threat_model>

<verification>
- `make book` exits 0; `cd pmcp-course && mdbook build` exits 0.
- All required-content greps pass in both chapters; both wired into their SUMMARY.md.
</verification>

<success_criteria>
- Two dedicated chapters explain the governance + Excel narrative, show the oauth_passthrough block, both tool names, and the headline query; both build cleanly and are nav-wired.
- No real secret in either chapter; at most one client-credentials mention each.
</success_criteria>

<output>
After completion, create `.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-04-SUMMARY.md` noting the chapter numbers/nav placement and confirming both books build.
</output>
