---
phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml
  - crates/pmcp-openapi-server/tests/fixtures/contoso-m365-api.yaml
  - crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml
autonomous: true
requirements: [P902-SPEC, P902-CONFIG, P902-FIXTURE]
must_haves:
  truths:
    - "contoso-m365.toml parses via from_toml_strict_validated"
    - "Config uses oauth_passthrough backend auth (not api_key)"
    - "Two script tools get_customer and get_customer_orders each take a customer_id parameter"
    - "Curated Graph spec parses via OpenApiSchema::parse and contains only the range-read op (no write, no file-listing)"
    - "Workbook layout is date-pinned to a fixed reference date with >=1 headline match and >=1 non-match"
  artifacts:
    - path: "crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml"
      provides: "oauth_passthrough config + two script tools + resources + prompt + code_mode"
      contains: 'type = "oauth_passthrough"'
    - path: "crates/pmcp-openapi-server/tests/fixtures/contoso-m365-api.yaml"
      provides: "Curated Graph Excel range-read OpenAPI spec"
      contains: "getWorksheetRange"
    - path: "crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml"
      provides: "Tool-correctness scenario for the parity replay"
      contains: "get_customer"
  key_links:
    - from: "contoso-m365.toml [[prompts]] include_resources"
      to: "contoso-m365.toml [[resources]] uri"
      via: "exact URI string match"
      pattern: "docs://contoso-m365"
    - from: "get_customer script"
      to: "Graph range-read path"
      via: "customer_id -> row -> range(address=...) built in JS"
      pattern: "range\\(address="
---

<objective>
Author the three Contoso M365 fixture files that are the foundation for every downstream test, example, and doc: the `oauth_passthrough` config with two `customer_id`-keyed script tools, the curated Graph Excel range-read OpenAPI spec, and the tool-correctness replay scenario. The workbook layout (rows, ids, dates, amounts) is fixed here and pinned to a reference date so the headline "bought >100 in last 3 months" query is deterministic forever.

Purpose: Mirror the 90.1 london-tube fixture set, re-skinned to Contoso/Graph/passthrough. No `src/` change — this phase only CONFIGURES machinery that already ships from Phase 90.
Output: `tests/fixtures/contoso-m365.toml`, `tests/fixtures/contoso-m365-api.yaml`, `tests/fixtures/contoso-m365-scenarios.yaml`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-SPEC.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-CONTEXT.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-RESEARCH.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author contoso-m365.toml config (oauth_passthrough + two script tools + resources + prompt + code_mode)</name>
  <files>crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml</files>
  <read_first>
    - crates/pmcp-openapi-server/tests/fixtures/london-tube.toml (THE analog — copy structure verbatim: [server]/[backend]/[backend.auth]/[backend.http]/[[tools]] with [[tools.parameters]] + [tools.annotations]/[[resources]] x3/[[prompts]] x1/[code_mode])
    - crates/pmcp-server-toolkit/src/http/auth.rs:122-130 (AuthConfig::OAuthPassthrough — target_header defaults "Authorization", required defaults true)
    - crates/pmcp-server-toolkit/src/config.rs:578-784 (deny_unknown_fields exact field names: ParamDecl, ToolDecl, ResourceDecl, PromptDecl, AnnotationsDecl)
    - .planning/phases/90.2-.../90.2-PATTERNS.md (Pattern Assignments § contoso-m365.toml — the re-skin diffs)
  </read_first>
  <action>
    Re-skin london-tube.toml into tests/fixtures/contoso-m365.toml per D-01/D-02/D-03/D-06.
    Replace the api_key backend with: `[backend] base_url = "https://graph.microsoft.com/v1.0"` and `[backend.auth] type = "oauth_passthrough"` + `target_header = "Authorization"` + `required = true`.
    Define BOTH tools as SCRIPT tools (the connector does literal str::replace, no math — per D-04 / RESEARCH Pattern 2):
      Tool 1 `get_customer` with one required `[[tools.parameters]]` `name = "customer_id"`, `type = "string"`; script parses `args.customer_id` (e.g. 'C007' -> index 7), computes the Customers-sheet row = index + header-offset, builds `addr` = `A{row}:D{row}`, binds the result of `await api.get` against path `/drives/CONTOSO_DRIVE/items/CUSTOMERS_ITEM/workbook/worksheets/Customers/range(address='${addr}')?$select=values` to a `const` before `return`, returns `.values`.
      Tool 2 `get_customer_orders` same shape, one required `customer_id` param; script reads the contiguous Orders block for that id (D-03 — Orders sheet grouped/sorted by customer_id so each customer's orders form an address-derivable contiguous range), worksheet `Orders`, path `/drives/CONTOSO_DRIVE/items/ORDERS_ITEM/workbook/worksheets/Orders/range(address='${ordersAddr}')?$select=values`.
      Both tools carry `[tools.annotations]` with `read_only_hint = true`, `idempotent_hint = true`, `open_world_hint = true`, `cost_hint = "low"`.
    NO `Date` use anywhere in either script (the JS engine has no Date builtin — RESEARCH Pitfall 1/A4). Bind every `api.get` to a `const` before `return` (engine-accuracy rule from Phase 90 Plan 05 — RESEARCH Pattern 2).
    Add three `[[resources]]` with Contoso URIs `docs://contoso-m365/schema`, `docs://contoso-m365/examples`, `code-mode://learnings` (Customers/Orders sheet schema, sample request/response payloads, Code Mode learnings). Add one `[[prompts]]` `start_code_mode` whose `include_resources` lists those three URIs EXACTLY (Pitfall 3 — a typo'd URI fails assembly).
    Add `[code_mode]` with `enabled = true`, dev-guarded inline `token_secret = "contoso-m365-parity-dev-secret-32bytes"`, `allow_inline_token_secret_for_dev = true`.
    Use Contoso framing values (D-06): customer names Northwind Traders / Fabrikam / Tailspin Toys etc.; SharePoint "Sales" site, `Customers.xlsx` in resource docs.
    NO real secret anywhere — any tenant/client id is a `${VAR}` placeholder; the inline token_secret is the only literal and is dev-guarded.
  </action>
  <verify>
    <automated>echo "deferred: config validity is asserted by Plan 02's contoso_m365_fixture test (from_toml_strict_validated). This task only authors the fixture — the grep acceptance_criteria below verify its structure."</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type = "oauth_passthrough"' crates/pmcp-openapi-server/tests/fixtures/contoso-m365.toml` returns 1
    - `grep -c 'name = "get_customer"' ...contoso-m365.toml` returns 1 AND `grep -c 'name = "get_customer_orders"' ...` returns 1
    - `grep -c 'name = "customer_id"' ...contoso-m365.toml` returns 2 (one per tool)
    - `grep -c 'range(address=' ...contoso-m365.toml` returns >= 2 (both script tools template a range address)
    - `grep -c 'allow_inline_token_secret_for_dev = true' ...contoso-m365.toml` returns 1
    - `grep -c 'docs://contoso-m365' ...contoso-m365.toml` returns >= 2 (resource defs + prompt include_resources)
    - NO `new Date` / `Date.now` / `${TENANT` literal secret: `grep -E 'Date\.|new Date' ...contoso-m365.toml` returns nothing
    - The Task-2 + Plan-02 fixture-validity test will assert `from_toml_strict_validated` accepts it and both tools `is_script_tool()`
  </acceptance_criteria>
  <done>contoso-m365.toml exists with oauth_passthrough auth, both script tools (each customer_id-keyed, building a range address in JS), three resources, one start_code_mode prompt with exact-URI include_resources, and dev-guarded code_mode; no real secret; no Date use.</done>
</task>

<task type="auto">
  <name>Task 2: Author contoso-m365-api.yaml curated Graph spec + contoso-m365-scenarios.yaml replay scenario (with OData-path-parse fallback)</name>
  <files>crates/pmcp-openapi-server/tests/fixtures/contoso-m365-api.yaml, crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml</files>
  <read_first>
    - crates/pmcp-openapi-server/tests/fixtures/london-tube-api.yaml (analog — 55-line minimal OpenAPI 3.0 with in:path + in:query params)
    - crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml (analog — list_tools/list_resources/list_prompts + validate_code + per-tool tool_call steps with type:contains assertions)
    - crates/pmcp-openapi-server/tests/parity_replay.rs:165-179 (how london-tube parses its spec via OpenApiSchema::parse + operation_for — Task 1 of Plan 02 will run the same assertion against this spec)
    - .planning/phases/90.2-.../90.2-RESEARCH.md (Code Examples § curated Graph spec + Assumption A1 fallback; Open Question 1)
  </read_first>
  <action>
    Author tests/fixtures/contoso-m365-api.yaml as a minimal OpenAPI 3.0 doc (mirror london-tube-api.yaml): `servers: [{url: https://graph.microsoft.com/v1.0}]`; ONE GET path declaring the Graph Excel range-read op with `operationId: getWorksheetRange`, parameters `drive-id`/`item-id`/`worksheet-id`/`address` as `in: path required: true type: string` and `$select` as `in: query required: false`; a single `"200"` response describing a workbookRange object. NO write op, NO SharePoint file-listing op (P902-SPEC boundary).
    PRIMARY path string: `/drives/{drive-id}/items/{item-id}/workbook/worksheets/{worksheet-id}/range(address='{address}')`.
    OData-segment RISK (A1 / Open Question 1): the parenthesized `range(address='{address}')` segment is non-standard. After writing, run `OpenApiSchema::parse` against it (Plan 02 Task 1 does this formally, but smoke-check here). If `OpenApiSchema::parse` REJECTS the segment, FALL BACK to the simplified path `/drives/{drive-id}/items/{item-id}/workbook/worksheets/{worksheet-id}/range` and document `address` as a query param — the spec is CONTEXT-only (tools dispatch from `[[tools]]` script, not the spec — assemble.rs:266-291), so the simplified path still satisfies P902-SPEC. Record which path form was used in the plan SUMMARY.
    Author tests/fixtures/contoso-m365-scenarios.yaml (mirror london-tube-scenarios.yaml): `list_tools` step asserting both tool names present; `list_resources` + `list_prompts` discovery steps; per-tool `tool_call` steps — `get_customer` with `arguments: {customer_id: "C007"}` asserting `type: contains` value "Northwind Traders" (or whichever name C007 maps to in the Task-1 layout), and `get_customer_orders` with `arguments: {customer_id: "C007"}` asserting a known order value. Do NOT add a computed-result assertion to a code-mode step here (RQ-2 — the deterministic headline result lives in the Plan 03 Rust test; the YAML validate_code/tool_call steps assert success/contains only).
    Keep the scenario asserted values CONSISTENT with the Task-1 workbook layout (the same customer_id->row mapping the script tools compute), since Plan 02's wiremock mock will return rows for those exact addresses.
  </action>
  <verify>
    <automated>test -f crates/pmcp-openapi-server/tests/fixtures/contoso-m365-api.yaml && test -f crates/pmcp-openapi-server/tests/fixtures/contoso-m365-scenarios.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getWorksheetRange' ...contoso-m365-api.yaml` returns 1
    - `grep -Ec 'in: path' ...contoso-m365-api.yaml` returns >= 3 (drive-id/item-id/worksheet-id at minimum)
    - NO write/list op: `grep -Ei 'post:|put:|patch:|delete:|listChildren|/children' ...contoso-m365-api.yaml` returns nothing
    - `grep -c 'get_customer' ...contoso-m365-scenarios.yaml` returns >= 1 AND `grep -c 'get_customer_orders' ...` returns >= 1
    - `grep -c 'customer_id: "C007"' ...contoso-m365-scenarios.yaml` returns >= 2 (one per tool_call)
    - The SUMMARY records whether the PRIMARY OData path or the FALLBACK plain `/range` path was used (Plan 02 Task 1 will assert OpenApiSchema::parse accepts whichever form shipped)
  </acceptance_criteria>
  <done>Curated Graph spec exists (range-read op only, no write/list), with the verified-parsing path form recorded; replay scenario exists with both tool_call steps using customer_id args and contains-assertions consistent with the Task-1 workbook layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo commit -> published crate | tests/ is excluded from the tarball; these fixtures stay test-only, but committed-to-git literals are still public |
| config -> backend (Graph) | the only credential path is the per-user forwarded token; the config holds NO standing credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.2-secrets-01 | Information Disclosure | contoso-m365.toml (committed fixture) | mitigate | Only `${VAR}` placeholders for any tenant/client id; the single literal is the dev-guarded inline `token_secret` flagged `allow_inline_token_secret_for_dev = true` (mirrors london-tube). Acceptance greps assert no real credential. |
| T-90.2-content-01 | Tampering | workbook fixture rows + scenario assertions | accept | Inert static data authored by us; never executed; deterministic pinned reference date. No external input shapes it. |
| T-90.2-passthrough-01 | Elevation of Privilege | [backend.auth] type | mitigate | `oauth_passthrough` means the server holds no standing credential — it can only act AS the calling user; `target_header` is operator-fixed in committed config (client controls only the VALUE). Proven at runtime in Plan 02/03. |
</threat_model>

<verification>
- `from_toml_strict_validated` (run by Plan 02 Task 1) accepts contoso-m365.toml; both tools `is_script_tool()`; auth is `OAuthPassthrough`.
- `OpenApiSchema::parse` (Plan 02 Task 1) accepts the shipped spec path form (primary OData or documented fallback).
- All acceptance greps above pass.
</verification>

<success_criteria>
- Three fixture files exist; config uses oauth_passthrough + two customer_id-keyed script tools + 3 resources + 1 prompt + dev-guarded code_mode; curated spec contains only the range-read op; scenario exercises both tools with customer_id args.
- No real secret committed; no `Date` use in any script.
- Workbook layout pinned to a reference date with >=1 headline match and >=1 non-match (the exact set asserted by Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-01-SUMMARY.md`. RECORD: (a) the customer_id->row/address mapping convention, (b) the exact workbook rows (ids, names, segments, regions, order ids/dates/amounts) including which customers match/don't-match the headline query, (c) which spec path form shipped (primary OData vs fallback), (d) the resource URIs + prompt name (Plan 02/03 + the pointable example must reuse these EXACTLY).
</output>
