---
phase: 90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/pmcp-openapi-server/tests/contoso_m365_code_mode.rs
autonomous: true
requirements: [P902-CODEMODE, P902-FIXTURE]
must_haves:
  truths:
    - "An offline Rust Code Mode test computes 'customers who bought >100 in last 3 months' and asserts the EXACT deterministic customer-id set from the fixture"
    - "The headline script uses a PINNED reference-date literal, never wall-clock now"
    - "The Code Mode executor forwards the inbound user bearer to the Graph mock for both Customers and Orders reads"
    - "Changing fixture amounts/dates would change the asserted set"
  artifacts:
    - path: "crates/pmcp-openapi-server/tests/contoso_m365_code_mode.rs"
      provides: "JsCodeExecutor-driven deterministic headline-query proof over token-threaded HttpCodeExecutor"
      contains: "JsCodeExecutor"
  key_links:
    - from: "passthrough_exec + extra_with_token"
      to: "wiremock Customers + Orders range mocks (authorization-matched)"
      via: "request_executor_from_extra -> JsCodeExecutor.execute(HEADLINE)"
      pattern: "request_executor_from_extra"
    - from: "HEADLINE script REF constant"
      to: "asserted customer-id set"
      via: "join + trailing-3-month filter + sum>100, all from the pinned literal"
      pattern: "const REF ="
---

<objective>
Prove the demo's hero query end-to-end offline: an `execute_code`-style Code Mode workflow that loads Customers + Orders via the Graph range-read tools, joins on `customer_id`, filters orders to the trailing 3 months of a PINNED reference date, sums `amount` per customer, and returns the deterministic set with total > 100. This MUST be a Rust test (not a YAML step) because `execute_code` requires an HMAC approval token a flat scenario cannot thread (RQ-2) — so the test drives `JsCodeExecutor` directly over the same token-threaded `HttpCodeExecutor` the toolkit handlers use.

Purpose: P902-CODEMODE (deterministic computed result) + P902-FIXTURE (>=1 match, >=1 non-match proven by the asserted set).
Output: `tests/contoso_m365_code_mode.rs`.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-SPEC.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-RESEARCH.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-PATTERNS.md
@.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-01-SUMMARY.md

<interfaces>
<!-- From crates/pmcp-openapi-server/tests/oauth_passthrough_e2e.rs:24-72 — copy these helpers verbatim: -->
<!-- use pmcp_server_toolkit::code_mode::{request_executor_from_extra, CodeExecutor, ExecutionConfig, HttpCodeExecutor, JsCodeExecutor}; -->
<!-- use pmcp_server_toolkit::http::auth::{create_passthrough_auth_provider, AuthConfig}; -->
<!-- fn passthrough_exec(base_url: String, required: bool) -> HttpCodeExecutor  (lines 36-46) -->
<!-- fn extra_with_token(token: Option<&str>) -> pmcp::RequestHandlerExtra      (lines 50-61) -->
<!-- fn code_mode_executor(base, extra) -> JsCodeExecutor<HttpCodeExecutor>     (lines 66-72; = request_executor_from_extra + JsCodeExecutor::new) -->
<!-- executor.execute(SCRIPT, None).await -> serde_json::Value (the computed result) -->

<!-- JS engine reality (Phase 90 Plan 05 + RESEARCH Pitfall 1/A4): NO Date builtin. -->
<!-- Available: Math.*, parseInt/parseFloat, Object.keys, array methods, string methods, template literals. -->
<!-- api.get path must be a string/template literal; bind ApiCall to a const before return; .slice() bounds need numeric literals. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contoso_m365_code_mode.rs — deterministic headline query over token-threaded JsCodeExecutor</name>
  <files>crates/pmcp-openapi-server/tests/contoso_m365_code_mode.rs</files>
  <read_first>
    - crates/pmcp-openapi-server/tests/oauth_passthrough_e2e.rs (THE analog — whole file; copy passthrough_exec/extra_with_token/code_mode_executor helpers + the required_true_token_present test body shape at 82-105; #![cfg(feature = "openapi-code-mode")] at line 22; positional-filter naming lesson at 18-20)
    - 90.2-01-SUMMARY.md (the EXACT workbook rows — customer ids/names, order ids/dates/amounts, which customers match vs don't-match the headline query, and the Customers/Orders range addresses the script must read)
    - .planning/phases/90.2-.../90.2-RESEARCH.md (Pattern 5 headline script + Pitfall 1 date handling + the JS-subset engine-accuracy rules)
    - .planning/phases/90.2-.../90.2-PATTERNS.md (§ contoso_m365_code_mode.rs — the re-skin diffs + the HEADLINE script skeleton)
  </read_first>
  <action>
    Create tests/contoso_m365_code_mode.rs gated `#![cfg(feature = "openapi-code-mode")]` (line-1-region, mirror oauth_passthrough_e2e.rs:22). Prefix EVERY test fn `contoso_m365_code_mode_*` (positional verify-filter lesson).
    Copy the three helpers VERBATIM from oauth_passthrough_e2e.rs: `passthrough_exec(base_url, required)`, `extra_with_token(token)`, `code_mode_executor(base, extra)`.
    Author the `HEADLINE` script const computing the hero query, engine-accurate (NO Date builtin):
      - First line `const REF = "<the pinned reference date from 90.2-01-SUMMARY>";` (a string literal — NEVER wall-clock `now`).
      - `const customers = await api.get("/drives/CONTOSO_DRIVE/items/CUSTOMERS_ITEM/workbook/worksheets/Customers/range(address='<the all-customers block address from SUMMARY>')?$select=values");` then read `customers.values`.
      - `const orders = await api.get("/drives/CONTOSO_DRIVE/items/ORDERS_ITEM/workbook/worksheets/Orders/range(address='<the all-orders block address from SUMMARY>')?$select=values");` then `orders.values`.
      - Join Orders->Customers on `customer_id`; filter `order_date` into the trailing-3-month window `[REF-3months, REF]` using ISO `YYYY-MM-DD` lexicographic string comparison (compute the lower-bound string from REF with integer month/year math on the parsed parts — NO Date); sum `amount` per customer; collect the customer ids whose total `> 100`; sort for a stable order; bind the array to a `const` and `return` it.
    Test body `contoso_m365_code_mode_headline_query_returns_deterministic_set` (mirror oauth_passthrough_e2e_required_true_token_present...): start a `MockServer`; mount a GET mock for the Customers range address AND one for the Orders range address, EACH `.and(header("authorization", "Bearer contoso-user-tok"))` (passthrough forwarded into the Code Mode reads too), responding with the workbookRange `{ "values": [[...]] }` bodies built from the 90.2-01-SUMMARY rows; build `passthrough_exec(server.uri(), true)` + `extra_with_token(Some("Bearer contoso-user-tok"))` + `code_mode_executor`; `executor.execute(HEADLINE, None).await`; assert the result EQUALS the exact deterministic customer-id set from the fixture, e.g. `assert_eq!(result, json!(["C007","C012"]))` (use the actual set from SUMMARY — must include >=1 match and exclude >=1 non-match).
    Run single-threaded.
  </action>
  <verify>
    <automated>cargo test -p pmcp-openapi-server --features openapi-code-mode --test contoso_m365_code_mode -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-openapi-server --features openapi-code-mode --test contoso_m365_code_mode -- --test-threads=1` passes
    - `grep -c 'const REF =' crates/pmcp-openapi-server/tests/contoso_m365_code_mode.rs` returns 1 (pinned reference-date literal)
    - NO wall-clock: `grep -E 'Date\.|new Date|Instant::now|SystemTime' ...contoso_m365_code_mode.rs` returns nothing in the HEADLINE script region
    - `grep -c 'Bearer contoso-user-tok' ...contoso_m365_code_mode.rs` returns >= 2 (Customers mock + Orders mock both require the forwarded bearer)
    - `grep -c 'assert_eq!' ...contoso_m365_code_mode.rs` returns >= 1 asserting the exact customer-id set (the set value matches 90.2-01-SUMMARY's match/non-match rows)
    - `grep -c 'request_executor_from_extra\|JsCodeExecutor' ...contoso_m365_code_mode.rs` returns >= 1 (drives the executor directly, not a YAML step)
    - Determinism sensitivity: the asserted set is derived only from the fixture rows + the pinned REF (not `now`) — re-running the test on any later date yields the identical set.
  </acceptance_criteria>
  <done>The Rust Code Mode test passes offline single-threaded; computes the headline >100-in-3-months set from the pinned reference date over token-threaded reads, and asserts the exact deterministic customer-id set (>=1 match, >=1 non-match).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code Mode executor -> Graph backend | the per-request forwarded user bearer is the only credential reaching the Customers/Orders reads |
| script logic -> time | the "last 3 months" window is computed from a committed literal, not the host clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.2-passthrough-03 | Elevation of Privilege | Code Mode reads to Graph | mitigate | Both wiremock mocks REQUIRE `authorization: Bearer contoso-user-tok`; the test fails if the Code Mode executor does not forward the inbound user token — proves Code Mode runs AS the calling user, no standing credential. |
| T-90.2-content-03 | Tampering | inert workbook fixture rows | accept | Static test data authored by us; deterministic; no live Graph in CI. |
| T-90.2-rot-01 | Repudiation/Reliability | wall-clock dependence | mitigate | Pinned reference-date literal (`const REF`); acceptance grep rejects any `Date`/`now`/`SystemTime` in the script — the asserted set cannot rot over time. |
</threat_model>

<verification>
- `cargo test -p pmcp-openapi-server --features openapi-code-mode --test contoso_m365_code_mode -- --test-threads=1` green.
- The asserted customer-id set matches the match/non-match rows recorded in 90.2-01-SUMMARY.
</verification>

<success_criteria>
- The headline query computes the exact deterministic customer-id set offline, from a pinned reference date, with the inbound bearer forwarded to both reads.
- No wall-clock/`Date` dependence anywhere; the set is stable across calendar time.
</success_criteria>

<output>
After completion, create `.planning/phases/90.2-openapi-built-in-server-advanced-example-contoso-m365-oauth-/90.2-03-SUMMARY.md`. RECORD: the pinned REF value, the asserted customer-id set, and the trailing-3-month string-compare approach used (so the docs chapter in Plan 04 can show the same query accurately).
</output>
