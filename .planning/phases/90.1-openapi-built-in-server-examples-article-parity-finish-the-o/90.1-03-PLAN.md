---
phase: 90.1-openapi-built-in-server-examples-article-parity-finish-the-o
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml
autonomous: true
requirements: [P901-PARITY]
must_haves:
  truths:
    - "The scenario file has a list_resources discovery step"
    - "The scenario file has a list_prompts discovery step"
    - "Each new step is individually binding through the per-step gate in parity_replay.rs"
    - "The offline replay (london_tube_parity_through_real_binary_path) passes single-threaded"
  artifacts:
    - path: "crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml"
      provides: "Resource + prompt discovery parity steps"
      contains: "list_resources"
  key_links:
    - from: "london-tube-scenarios.yaml list_resources/list_prompts steps"
      to: "the served surface from the enriched london-tube.toml (Plan 01)"
      via: "mcp-tester ScenarioExecutor replay → per-step success gate"
      pattern: "list_(resources|prompts)"
---

<objective>
Extend `london-tube-scenarios.yaml` with `list_resources` + `list_prompts` discovery steps
so the offline replay proves the served surface exposes the resources and the prompt the
enriched fixture (Plan 01) adds. Mirrors the SQL `generated.yaml:22-36` shape exactly.

Purpose: Closes scope item 4 / P901-PARITY — the replay parity that the served (already-wired)
resource/prompt surface lists. Depends on Plan 01 (the fixture must ship resources/prompts to list).
Output: two new scenario steps; the existing `london_tube_parity_through_real_binary_path` replay
test makes each individually binding via its per-step gate (no parity_replay.rs change needed).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-RESEARCH.md
@.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-PATTERNS.md

<interfaces>
<!-- mcp-tester scenario step shape (verified SQL gold-standard generated.yaml:22-36) -->
operation.type: list_resources  → assertions: success + exists path:resources
operation.type: list_prompts    → assertions: success + exists path:prompts

<!-- The replay test gate (parity_replay.rs:220-238) iterates step_results and fails
     if any step.success is false. New steps become binding automatically; no Rust change. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add list_resources + list_prompts discovery steps</name>
  <read_first>
    - crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml (the file being modified — full; locate the `List available tools` step ~lines 29-35 and the `# Capability Discovery` comment block ~lines 25-27)
    - crates/pmcp-sql-server/tests/fixtures/generated.yaml lines 22-36 (the exact list_resources/list_prompts step shape to mirror)
    - crates/pmcp-openapi-server/tests/parity_replay.rs lines 220-238 (the per-step gate that makes new steps binding — confirm no Rust change is needed)
  </read_first>
  <action>
    Insert two steps into `london-tube-scenarios.yaml` immediately AFTER the existing
    `List available tools` step and BEFORE the `validate_code` step, within the existing
    `# Capability Discovery` block. Copy the structure verbatim from generated.yaml:22-36:

    Step `List available resources`: `operation.type: list_resources`; assertions
    `type: success` and `type: exists` with `path: resources`.

    Step `List available prompts`: `operation.type: list_prompts`; assertions
    `type: success` and `type: exists` with `path: prompts`.

    Match the existing file's YAML indentation exactly. Do NOT add a `get_prompt` step
    (SQL stops at `list_prompts`; A1 unverified for mcp-tester 0.7.0 — `list_resources` +
    `list_prompts` + `exists` is the proven binding minimum). Resources are static inline
    content — these steps need NO backend round-trip and NO new wiremock mounts (offline
    discipline). Do NOT touch `parity_replay.rs` — the per-step gate auto-binds the new steps.
  </action>
  <verify>
    <automated>cargo test -p pmcp-openapi-server --test parity_replay london_tube_parity_through_real_binary_path -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'list_resources' crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml` succeeds
    - `grep -q 'list_prompts' crates/pmcp-openapi-server/tests/fixtures/london-tube-scenarios.yaml` succeeds
    - The two steps sit between `List available tools` and the `validate_code` step
    - `cargo test -p pmcp-openapi-server --test parity_replay london_tube_parity_through_real_binary_path -- --test-threads=1` exits 0 (every per-step gate passes, including the two new discovery steps against the enriched fixture)
    - No new wiremock mount / no network step added; test remains offline + single-threaded
    - `parity_replay.rs` unchanged (only the YAML scenario file is modified)
  </acceptance_criteria>
  <done>Scenario file has list_resources + list_prompts steps; the offline single-threaded replay passes each step against the enriched fixture's served surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay harness → wiremock backend | Test must stay offline; no real TfL network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90.1-04 | Denial of Service (CI flakiness) | new scenario steps | mitigate | `list_resources`/`list_prompts` assert static content with no backend round-trip; test runs single-threaded against an ephemeral wiremock port — no network, no shared-env flakiness. |
| T-90.1-01 | Information Disclosure | scenario file | accept | The scenario file contains no secrets; `TFL_APP_KEY=dummy` is set by the harness, not the YAML. |
</threat_model>

<verification>
- `cargo test -p pmcp-openapi-server --test parity_replay london_tube_parity_through_real_binary_path -- --test-threads=1` exits 0
- Two new discovery steps present and binding; replay stays offline/single-threaded
</verification>

<success_criteria>
- `london-tube-scenarios.yaml` gains list_resources + list_prompts steps mirroring SQL generated.yaml
- The offline replay passes each new step against the enriched fixture's served surface
- No parity_replay.rs change, no new wiremock mount
</success_criteria>

<output>
After completion, create `.planning/phases/90.1-openapi-built-in-server-examples-article-parity-finish-the-o/90.1-03-SUMMARY.md`
</output>
