---
phase: 84
plan: 03
type: execute
wave: 1
depends_on: [00, 01]
files_modified:
  - crates/pmcp-server-toolkit/src/code_mode.rs
  - crates/pmcp-server-toolkit/src/config.rs
  - crates/pmcp-server-toolkit/src/tools.rs
  - crates/pmcp-server-toolkit/src/lib.rs
  - crates/pmcp-server-toolkit/src/builder_ext.rs
  - crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs
autonomous: true
requirements:
  - CONN-04
must_haves:
  truths:
    - "`pmcp_server_toolkit::code_mode::build_code_mode_prompt(connector, config).await` returns identical text to `assemble_code_mode_prompt(connector, config).await` for every input (both names valid public surface)"
    - "`DatabaseSection.url: Option<String>` field is parseable from TOML and supports `env:VAR_NAME` indirection at parse time (resolution happens at consumer level, parallel to `[code_mode].token_secret` per P83 R6/R9)"
    - "Existing `synthesize_from_config(config)` API is preserved unchanged — all 11+ P83 callers compile without modification (additive-only change per REF-01 superset invariant)"
    - "NEW `synthesize_from_config_with_connector(config, connector: Arc<dyn SqlConnector>)` exists alongside the existing API and threads the connector into `SynthesizedToolHandler` for runtime tool execution"
    - "When `ToolDecl.ui_resource_uri.is_some()`, the synthesized `ToolInfo` carries `widget_meta` so pmcp core's `with_widget_enrichment` populates `structured_content` (D-06)"
    - "When a widget tool handler returns `Vec<Value>` rows, the resulting `CallToolResult.structured_content` is `Some(Value::Array(rows))` — verified by `synthesizer_structured_content.rs` integration test"
    - "REF-01 superset invariant holds: all 3 reference configs (`open-images`, `imdb`, `msr-vtt`) continue to parse cleanly (`tests/reference_configs.rs` still green)"
    - "All 11+ existing callers of `synthesize_from_config(cfg)` continue compiling unchanged (5 inline tests in tools.rs + 1 doctest + 1 REF-01 test + 3 prop tests + 1 lib.rs fn-type assertion + 1 lib.rs re-export + 1 builder_ext.rs production call)"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/code_mode.rs"
      provides: "`build_code_mode_prompt` alias next to existing `assemble_code_mode_prompt`"
      contains: "pub async fn build_code_mode_prompt"
    - path: "crates/pmcp-server-toolkit/src/config.rs"
      provides: "`DatabaseSection.url: Option<String>` additive field"
      contains: "pub url: Option<String>"
    - path: "crates/pmcp-server-toolkit/src/tools.rs"
      provides: "NEW `synthesize_from_config_with_connector` variant + widget_meta flip on `info` construction; existing `synthesize_from_config(config)` unchanged"
      contains: "synthesize_from_config_with_connector"
    - path: "crates/pmcp-server-toolkit/src/lib.rs"
      provides: "Re-export of the new connector-threaded variant"
      contains: "synthesize_from_config_with_connector"
    - path: "crates/pmcp-server-toolkit/src/builder_ext.rs"
      provides: "Production caller switches to `synthesize_from_config_with_connector` when a connector is available (Shape A wiring)"
      contains: "synthesize_from_config_with_connector"
    - path: "crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs"
      provides: "D-06 integration anchor — widget tool emits structured_content"
  key_links:
    - from: "build_code_mode_prompt"
      to: "assemble_code_mode_prompt"
      via: "thin function alias"
      pattern: "assemble_code_mode_prompt"
    - from: "synthesize_from_config_with_connector"
      to: "SynthesizedToolHandler.connector field"
      via: "Arc<dyn SqlConnector> thread-through (additive — existing `synthesize_from_config` unchanged)"
      pattern: "self.connector"
    - from: "SynthesizedToolHandler.handle"
      to: "connector.execute"
      via: "thread connector reference through handler body (only present when handler built via the new variant)"
      pattern: "connector.execute"
    - from: "ToolDecl.ui_resource_uri"
      to: "ToolInfo::with_widget_meta"
      via: "conditional flip in shared synthesizer helper used by both `synthesize_from_config` and `synthesize_from_config_with_connector`"
      pattern: "with_widget_meta"
---

<objective>
Three coordinated surgical edits across the toolkit core that satisfy CONN-04 (literal `build_code_mode_prompt` name), D-06 (widget tools emit `structured_content`), and D-08's URL constructor support:

1. **`build_code_mode_prompt` alias** — Add a thin async function alias next to the existing `assemble_code_mode_prompt` in `code_mode.rs` (per RESEARCH §"Open Questions" Q2 / Landmine #15 / D-12: recommendation is alias-next-to, not deprecated rename).
2. **`DatabaseSection.url` field** — Add a single additive `url: Option<String>` field to the existing `DatabaseSection` struct in `config.rs` (REF-01 additive-only; supports `env:VAR_NAME` indirection per P83 R6/R9 pattern). This is the URL Postgres/MySQL connectors consume in Wave 2.
3. **Synthesizer wiring (ADDITIVE)** — Introduce a NEW `synthesize_from_config_with_connector(cfg, connector: Arc<dyn SqlConnector>)` function alongside the existing `synthesize_from_config(cfg)`. The existing API is preserved unchanged — all 11+ P83 callers compile without modification. The new variant builds `SynthesizedToolHandler`s that hold the connector reference and call `connector.execute()` in their `handle()` body. The widget_meta flip (when `ToolDecl.ui_resource_uri.is_some()`) is factored into a shared helper used by both variants so D-06 fires regardless of which entry point is used.
4. **Integration test** — New `tests/synthesizer_structured_content.rs` verifies the D-06 contract end-to-end against an in-memory `SqliteConnector` (which Plan 04 ships).

Purpose: These changes are tightly coupled (config field + new synthesizer variant + widget_meta flip) and must land together to keep the toolkit core compiling and reference configs parsing. The ADDITIVE approach to the synthesizer signature (variant-not-overload) ensures the 11+ existing call sites identified in the BLOCKER-#2 audit (5 inline tests in `src/tools.rs`, 1 public doctest at `src/tools.rs:71`, 1 fn-type assertion at `src/lib.rs:130`, 1 re-export at `src/lib.rs:91`, 1 production call at `src/builder_ext.rs:151`, 1 REF-01 test at `tests/reference_configs.rs:70`, and 3 property tests at `tests/tool_synthesis_props.rs:62/71/85`) remain untouched. Plan 04's `SqliteConnector` and Plans 05/06/07's per-backend connectors all consume the new variant.

Output: 6 modified/new files. All existing tests still pass with zero modifications; new integration test passes once Plan 04 ships `SqliteConnector` (the test is `#[cfg(all(feature = "code-mode", feature = "sqlite"))]`-gated).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-CONTEXT.md
@.planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-RESEARCH.md
@.planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-PATTERNS.md
@crates/pmcp-server-toolkit/src/code_mode.rs
@crates/pmcp-server-toolkit/src/config.rs
@crates/pmcp-server-toolkit/src/tools.rs
@crates/pmcp-server-toolkit/src/lib.rs
@crates/pmcp-server-toolkit/src/builder_ext.rs
@crates/pmcp-server-toolkit/tests/code_mode_wiring.rs
@crates/pmcp-server-toolkit/tests/reference_configs.rs
@crates/pmcp-server-toolkit/tests/tool_synthesis_props.rs
@src/server/core.rs
@src/types/tools.rs

<interfaces>
<!-- KEY pmcp core gate (src/server/core.rs:593-601 — read this before touching tools.rs):
let call_result = if let Some(info) = tool_info.filter(|i| i.widget_meta().is_some()) {
    let summary = summarize_structured_output(&value);
    CallToolResult::new(vec![Content::text(summary)]).with_widget_enrichment(info, value)
} else {
    let text = serde_json::to_string_pretty(&value)?;
    CallToolResult::new(vec![Content::text(text)])
};
This is why we MUST flip widget_meta on the ToolInfo for D-06 to fire.

ToolInfo::with_widget_meta constructor (src/types/tools.rs:320-340):
pub fn with_widget_meta(mut self, meta: WidgetMeta) -> Self { /* ... */ }

WidgetMeta::new().domain(&uri) — the chaining pattern.

EXISTING synthesize_from_config signature (tools.rs:74, UNCHANGED):
pub fn synthesize_from_config(config: &ServerConfig) -> Result<Vec<SynthesizedTool>>

NEW (additive) variant added alongside:
pub fn synthesize_from_config_with_connector(
    config: &ServerConfig,
    connector: Arc<dyn pmcp_server_toolkit::sql::SqlConnector>,
) -> Result<Vec<SynthesizedTool>>

Existing 11 caller sites (DO NOT MODIFY):
  - crates/pmcp-server-toolkit/src/tools.rs:71 (public doctest)
  - crates/pmcp-server-toolkit/src/tools.rs:234, 247, 286, 313, 336, 354 (6 inline test cases)
  - crates/pmcp-server-toolkit/src/lib.rs:91 (pub use re-export)
  - crates/pmcp-server-toolkit/src/lib.rs:130 (fn-type assertion)
  - crates/pmcp-server-toolkit/src/builder_ext.rs:151 (production call)
  - crates/pmcp-server-toolkit/tests/reference_configs.rs:70 (REF-01 test)
  - crates/pmcp-server-toolkit/tests/tool_synthesis_props.rs:62, 71, 85 (3 property tests)

Current SynthesizedToolHandler (tools.rs:185-204):
struct SynthesizedToolHandler { decl: ToolDecl, info: ToolInfo }
After: variants — one no-connector (existing path) and one with `connector: Arc<dyn SqlConnector>` field for the new entry point. Cleanest impl: `connector: Option<Arc<dyn SqlConnector>>` so a single struct handles both paths; `handle()` returns `Err(pmcp::Error::Internal("no connector wired — call via synthesize_from_config_with_connector"))` if `connector.is_none()` and the tool actually has SQL to execute.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `build_code_mode_prompt` alias + `DatabaseSection.url` field</name>
  <files>
    crates/pmcp-server-toolkit/src/code_mode.rs,
    crates/pmcp-server-toolkit/src/config.rs
  </files>
  <read_first>
    crates/pmcp-server-toolkit/src/code_mode.rs (lines 1-50 imports + lines 354-395 `assemble_code_mode_prompt`),
    crates/pmcp-server-toolkit/src/config.rs (lines 286-320 `DatabaseSection`),
    .planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-PATTERNS.md (rows for `src/code_mode.rs` and `src/config.rs`),
    .planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-RESEARCH.md §5 (config extension) + §"Open Questions" Q2 + Landmine #15
  </read_first>
  <action>
    **In `crates/pmcp-server-toolkit/src/code_mode.rs`** — add after the existing `assemble_code_mode_prompt` function (around line 390), a thin alias:
    
    Function signature exactly: `pub async fn build_code_mode_prompt(connector: &(dyn SqlConnector + '_), config: &ServerConfig) -> pmcp::Result<String>` — body: `assemble_code_mode_prompt(connector, config).await`. Add `/// Alias for [`assemble_code_mode_prompt`] satisfying CONN-04's literal naming. Identical behavior; both names are valid public surface.` doc comment. Include a `# Example` doctest using `no_run` (per PATTERNS row, identical to existing one with the new name).
    
    NO `#[deprecated]` attribute. Both names stay valid — per RESEARCH Q2 recommendation + the P83 dual-naming precedent (`register_code_mode_tools` vs `code_mode_tools_from_executor`).
    
    **In `crates/pmcp-server-toolkit/src/config.rs`** — extend the existing `DatabaseSection` struct (lines ~286-314) by adding a single new field BEFORE the closing `}` of the struct definition:
    
    ```
    /// Connection URL for Postgres / MySQL backends. Supports `env:VAR_NAME`
    /// indirection at the consumer-resolution layer (the toolkit parses the
    /// string as-is and leaves resolution to the per-backend connector or
    /// the secret-resolution machinery from P83 R6/R9). Optional/unused for
    /// Athena (uses `region` + `workgroup` + `output_location`) and SQLite
    /// (uses `database` for the file path or `:memory:` literal).
    #[serde(default)]
    pub url: Option<String>,
    ```
    
    Position: ABOVE the existing `pool` field so source-control diffs land cleanly.
    
    The struct already carries `#[serde(deny_unknown_fields)]` — the additive field does not break strict parsing. The 3 reference configs (`open-images`, `imdb`, `msr-vtt`) don't emit `url` and continue to parse.
    
    Add a unit test `tests/reference_configs.rs` does NOT need modification — but add an inline `#[cfg(test)]` test in `config.rs` named `database_url_optional_field_parses` that parses a minimal config with `[database] url = "env:DATABASE_URL"` and asserts `cfg.database.unwrap().url == Some("env:DATABASE_URL".into())`.
    
    No other field renames, no rearrangement, no breaking changes. PMAT cog impact: 0.
  </action>
  <verify>
    <automated>cargo build -p pmcp-server-toolkit --features sqlite --features code-mode && cargo test -p pmcp-server-toolkit --features sqlite --test reference_configs && cargo test -p pmcp-server-toolkit --features sqlite --lib config::tests::database_url_optional_field_parses && cargo test --doc -p pmcp-server-toolkit --features sqlite build_code_mode_prompt</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q "pub async fn build_code_mode_prompt" crates/pmcp-server-toolkit/src/code_mode.rs` exits 0
    - Source: `grep -q "pub url: Option<String>" crates/pmcp-server-toolkit/src/config.rs` exits 0
    - Source: `! grep -q "#\[deprecated\]" crates/pmcp-server-toolkit/src/code_mode.rs` (no deprecation marker on the alias)
    - Test: `cargo test -p pmcp-server-toolkit --features sqlite --test reference_configs` exits 0 (REF-01 invariant intact)
    - Test: `cargo test --doc -p pmcp-server-toolkit --features sqlite build_code_mode_prompt` exits 0
    - Behavior: parsing `[server] name="x" version="0.0.1"\n[database] url = "env:DATABASE_URL"\n` produces `cfg.database.unwrap().url == Some("env:DATABASE_URL".into())`
    - Behavior: `build_code_mode_prompt(&conn, &cfg).await` returns the same `String` as `assemble_code_mode_prompt(&conn, &cfg).await` for any connector + config
  </acceptance_criteria>
  <done>Both `assemble_code_mode_prompt` and `build_code_mode_prompt` exist as parallel public functions; `DatabaseSection.url: Option<String>` parseable; reference configs still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `synthesize_from_config_with_connector` variant (ADDITIVE — preserve existing API)</name>
  <files>
    crates/pmcp-server-toolkit/src/tools.rs,
    crates/pmcp-server-toolkit/src/lib.rs,
    crates/pmcp-server-toolkit/src/builder_ext.rs
  </files>
  <behavior>
    - **Existing `synthesize_from_config(config: &ServerConfig) -> Result<Vec<SynthesizedTool>>` is UNCHANGED.** All 11 existing callers compile without modification.
    - **NEW `synthesize_from_config_with_connector(config: &ServerConfig, connector: Arc<dyn SqlConnector>) -> Result<Vec<SynthesizedTool>>`** is added alongside.
    - The two functions share an internal helper (e.g., `fn synthesize_inner(config, connector: Option<Arc<dyn SqlConnector>>) -> Result<Vec<SynthesizedTool>>`) so logic is not duplicated.
    - The widget_meta flip happens in `synthesize_inner` (or a small extracted helper called by it): when `ToolDecl.ui_resource_uri.is_some()`, `info = info.with_widget_meta(WidgetMeta::new().domain(uri))`. This fires regardless of which entry point was used — D-06 contract holds for both paths.
    - `SynthesizedToolHandler` gains a `connector: Option<Arc<dyn SqlConnector>>` field (defaulted to `None` for handlers built via the existing `synthesize_from_config`).
    - `SynthesizedToolHandler::handle(args, _extra)` body:
      1. Reads `decl.sql` (returns `pmcp::Error::Internal` if missing)
      2. Builds `&[(String, Value)]` from `decl.parameters` filtered against `args`
      3. If `self.connector.is_some()`, calls `self.connector.as_ref().unwrap().execute(sql, &named_params).await`, maps `ConnectorError` → `pmcp::Error::Internal` (with sanitized message — no raw driver text), returns rows as `Value::Array(rows)` (pmcp core's `with_widget_enrichment` puts this in `structured_content` when widget_meta is set)
      4. If `self.connector.is_none()`, returns `Err(pmcp::Error::Internal("tool requires connector wiring — build via synthesize_from_config_with_connector"))` — preserves P83 behavior where the no-connector path was test-only.
    - The 11+ existing callers continue to compile and pass without any modification.
    - `pub use crate::tools::synthesize_from_config_with_connector;` added to `src/lib.rs` (additive re-export — existing `pub use crate::tools::synthesize_from_config;` at line 91 UNCHANGED).
    - The existing fn-type assertion at `src/lib.rs:130` (`let _: fn(&ServerConfig) -> Result<Vec<crate::tools::SynthesizedTool>> = synthesize_from_config;`) continues to compile (signature unchanged).
    - `pub type SynthesizedTool = (String, ToolInfo, Arc<dyn ToolHandler>)` type alias (per P83 Plan 05 disposition) is preserved.
    - PMAT: each touched function stays ≤ cog 25 (split helpers if needed — Pattern G; `synthesize_inner` + `extract_named_params` + `apply_widget_meta`).
  </behavior>
  <read_first>
    crates/pmcp-server-toolkit/src/tools.rs (lines 74-92 + lines 185-220 — current synthesizer + handler; lines 234, 247, 286, 313, 336, 354 — the 6 inline test caller sites that MUST NOT be modified),
    crates/pmcp-server-toolkit/src/lib.rs (line 91 `pub use crate::tools::synthesize_from_config;` re-export and line 130 fn-type assertion — both MUST NOT be modified, only ADD a new re-export),
    crates/pmcp-server-toolkit/src/builder_ext.rs (line 151 — production call site; this Task 2 switches it to the new variant since builder_ext.rs is the natural Shape A wiring point per D-06),
    crates/pmcp-server-toolkit/tests/reference_configs.rs (line 70 — REF-01 test caller, MUST NOT be modified),
    crates/pmcp-server-toolkit/tests/tool_synthesis_props.rs (lines 62, 71, 85 — property test caller sites, MUST NOT be modified),
    crates/pmcp-server-toolkit/src/sql/mod.rs (`SqlConnector` trait — now 3-method post Plan 01),
    src/server/core.rs (lines 593-601 — the widget_meta gate),
    src/types/tools.rs (lines 320-340 `ToolInfo::with_widget_meta` + lines 582-600 `with_widget_enrichment`),
    .planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-PATTERNS.md (rows for `src/tools.rs` + `tests/synthesizer_structured_content.rs`),
    .planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-RESEARCH.md §4 (the `structured_content` wiring section)
  </read_first>
  <action>
    Additive surgery in `crates/pmcp-server-toolkit/src/tools.rs`:
    
    1. **Add `use std::sync::Arc;` and `use crate::sql::SqlConnector;` and `use pmcp::types::WidgetMeta;`** near top of file (if not already present).
    
    2. **LEAVE existing `pub fn synthesize_from_config(config: &ServerConfig) -> pmcp::Result<Vec<SynthesizedTool>>` UNCHANGED.** Refactor its body to delegate to a new shared helper: `fn synthesize_inner(config: &ServerConfig, connector: Option<Arc<dyn SqlConnector>>) -> pmcp::Result<Vec<SynthesizedTool>>`. The existing function becomes a one-liner: `synthesize_inner(config, None)`. This keeps the public signature identical so all 11 existing callers compile unchanged.
    
    3. **Add NEW `pub fn synthesize_from_config_with_connector(config: &ServerConfig, connector: Arc<dyn SqlConnector>) -> pmcp::Result<Vec<SynthesizedTool>>`** — one-line body: `synthesize_inner(config, Some(connector))`.
    
    4. **In `synthesize_inner`'s per-tool iteration**, after building `info` via the existing `ToolInfo::new(...)` / `::with_annotations(...)` flow, conditionally flip widget_meta (factored helper if cog ≤ 25 requires):
       ```
       let info = if let Some(uri) = decl.ui_resource_uri.as_deref() {
           info.with_widget_meta(WidgetMeta::new().domain(uri))
       } else {
           info
       };
       ```
       Position: AFTER the annotation merge but BEFORE constructing the `SynthesizedToolHandler`. Per PATTERNS row + RESEARCH §4 Option A. This fires for BOTH entry points because it lives in the shared helper.
    
    5. **Update `SynthesizedToolHandler` struct** (line ~185): add `connector: Option<Arc<dyn SqlConnector>>` field. When `synthesize_inner` is called with `None`, the handler stores `None`; when called via `synthesize_from_config_with_connector`, it stores `Some(connector.clone())` per tool.
    
    6. **Extend `SynthesizedToolHandler::handle` body** (lines ~192-203):
       ```
       async fn handle(&self, args: Value, _extra: RequestHandlerExtra) -> pmcp::Result<Value> {
           let sql = self.decl.sql.as_deref().ok_or_else(|| pmcp::Error::Internal(
               format!("tool '{}' has no `sql` declared", self.info.name)))?;
           let named_params: Vec<(String, Value)> = self.decl.parameters.iter()
               .filter_map(|p| args.get(&p.name).map(|v| (p.name.clone(), v.clone())))
               .collect();
           let connector = self.connector.as_ref().ok_or_else(|| pmcp::Error::Internal(
               format!("tool '{}' requires connector wiring — build via synthesize_from_config_with_connector", self.info.name)))?;
           let rows = connector.execute(sql, &named_params).await
               .map_err(|e| pmcp::Error::Internal(format!("connector error: {e}")))?;
           Ok(Value::Array(rows))
       }
       ```
       Notes: (a) the error mapping uses `format!("{e}")` which uses `ConnectorError::Display` — Plan 01 Task 2 ensures Display does not leak credentials. (b) `info.name` vs `info.name()` — use whichever the current code uses (read-first; struct-field access).
    
    7. **Helper extraction (PMAT discipline)**: If the new `handle` body + `synthesize_inner` loop crosses cog 25, extract `fn apply_widget_meta(info: ToolInfo, decl: &ToolDecl) -> ToolInfo` and/or `fn extract_named_params(decl: &ToolDecl, args: &Value) -> Vec<(String, Value)>` per Pattern G. Each helper ≤ cog 25, no `#[allow]`.
    
    8. **DO NOT MODIFY the 11 existing callers.** All of these continue calling `synthesize_from_config(cfg)` and their `Vec<SynthesizedTool>` outputs are byte-identical to before this plan (no connector wired → handler returns `Err` only when actually invoked at runtime, but the existing tests don't invoke handlers — they only assert the tool list shape).
       - tools.rs:71 (doctest) — UNCHANGED
       - tools.rs:234, 247, 286, 313, 336, 354 — UNCHANGED
       - lib.rs:91 (re-export) — UNCHANGED; ADD a NEW `pub use crate::tools::synthesize_from_config_with_connector;` next to it
       - lib.rs:130 (fn-type assertion) — UNCHANGED; signature of `synthesize_from_config` is preserved
       - reference_configs.rs:70 — UNCHANGED
       - tool_synthesis_props.rs:62, 71, 85 — UNCHANGED
    
    9. **In `crates/pmcp-server-toolkit/src/builder_ext.rs` (line 151 — production call site):** UPDATE this caller to use `synthesize_from_config_with_connector(config, Arc::clone(&connector))` when the builder has a connector available. The builder ergonomics (how the connector reaches `builder_ext.rs`) come from D-08's URL flow — the relevant change in Task 2 is: import `Arc` + `SqlConnector`, accept an `Arc<dyn SqlConnector>` field on the builder (or thread it as a function parameter to `tools_from_config`), and call the new variant. If the builder API today does not yet carry a connector, add a `with_connector(self, Arc<dyn SqlConnector>) -> Self` builder method; if no connector is set, fall back to calling the existing `synthesize_from_config(config)` (preserving today's behavior so `tools_from_config`-only consumers don't break).
    
    10. **Add to `crates/pmcp-server-toolkit/src/lib.rs`** — ONLY add a new line after the existing `pub use crate::tools::synthesize_from_config;` (line 91): `pub use crate::tools::synthesize_from_config_with_connector;`. Do NOT add a second fn-type assertion at line 130 in this plan (acceptable to defer to a future plan if the assertion is desired; the existing assertion still compiles).
  </action>
  <verify>
    <automated>cargo build -p pmcp-server-toolkit --features sqlite --features code-mode && cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --lib && cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --test reference_configs && cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --test tool_synthesis_props && pmat analyze complexity --format json --max-cognitive 25 -- crates/pmcp-server-toolkit/src/tools.rs | tee /tmp/pmat.json && ! jq -e '.violations[]? | select(.path | contains("crates/pmcp-server-toolkit/src/tools.rs"))' /tmp/pmat.json</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q "pub fn synthesize_from_config_with_connector" crates/pmcp-server-toolkit/src/tools.rs` exits 0
    - Source: existing `pub fn synthesize_from_config(config: &ServerConfig) -> ` line UNCHANGED — `grep -q "pub fn synthesize_from_config(config: &ServerConfig) ->" crates/pmcp-server-toolkit/src/tools.rs` exits 0
    - Source: `grep -q "connector: Option<Arc<dyn SqlConnector>>" crates/pmcp-server-toolkit/src/tools.rs` exits 0
    - Source: `grep -q "with_widget_meta" crates/pmcp-server-toolkit/src/tools.rs` exits 0
    - Source: `grep -q "synthesize_inner" crates/pmcp-server-toolkit/src/tools.rs` exits 0 (shared helper)
    - Source: `grep -q "pub use crate::tools::synthesize_from_config_with_connector" crates/pmcp-server-toolkit/src/lib.rs` exits 0
    - Source: existing line 91 unchanged — `grep -q "pub use crate::tools::synthesize_from_config;" crates/pmcp-server-toolkit/src/lib.rs` exits 0
    - Source: existing line 130 fn-type assertion compiles — `grep -q "fn(&ServerConfig) -> Result<Vec<crate::tools::SynthesizedTool>> = synthesize_from_config" crates/pmcp-server-toolkit/src/lib.rs` exits 0
    - Source: `grep -q "synthesize_from_config_with_connector" crates/pmcp-server-toolkit/src/builder_ext.rs` exits 0 (production caller switched)
    - Build: `cargo build -p pmcp-server-toolkit --features sqlite --features code-mode` exits 0
    - Test: `cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --lib` exits 0 (all 6 inline tests at lines 234/247/286/313/336/354 still pass unchanged)
    - Test: `cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --test reference_configs` exits 0 (REF-01 caller at line 70 unchanged + still green)
    - Test: `cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --test tool_synthesis_props` exits 0 (3 prop tests at lines 62/71/85 unchanged + still green)
    - Doctest: `cargo test --doc -p pmcp-server-toolkit --features sqlite synthesize_from_config` exits 0 (line-71 doctest unchanged + still green)
    - PMAT: `pmat analyze complexity --max-cognitive 25 -- crates/pmcp-server-toolkit/src/tools.rs` reports zero violations on the modified file (no `#[allow(clippy::cognitive_complexity)]` added)
    - Diff sanity: `git diff crates/pmcp-server-toolkit/src/tools.rs` shows NO modification to lines 234, 247, 286, 313, 336, 354 nor to line 71 (the public doctest); only additions for the new variant and the shared helper
    - Diff sanity: `git diff crates/pmcp-server-toolkit/src/lib.rs` shows ONLY a new `pub use ... _with_connector;` line added, NO modification to lines 91 or 130
  </acceptance_criteria>
  <done>New `synthesize_from_config_with_connector` exists alongside the unchanged `synthesize_from_config`; both share `synthesize_inner` (which flips widget_meta when `ui_resource_uri` is set); all 11 existing callers compile and pass tests without modification; `builder_ext.rs` production caller upgraded to the new variant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create `tests/synthesizer_structured_content.rs` — D-06 integration anchor</name>
  <files>crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs</files>
  <behavior>
    - Integration test asserts: when a `[[tools]]` config entry has `ui_resource_uri = "ui://employees"`, the synthesized `ToolInfo` (built via the NEW `synthesize_from_config_with_connector`) has `widget_meta().is_some() == true`
    - Integration test asserts: when `SynthesizedToolHandler::handle` returns `Value::Array(rows)` and pmcp core wraps it via `with_widget_enrichment`, the resulting `CallToolResult.structured_content == Some(Value::Array(rows))`
    - Integration test asserts: when `ui_resource_uri` is absent, the synthesized `ToolInfo` has `widget_meta().is_none()` and pmcp core falls back to `Content::text` pretty-JSON (RESEARCH §4 contract — model still reads rows from text content)
    - Test is gated `#[cfg(all(feature = "code-mode", feature = "sqlite"))]` — only runs when both features active
    - Test uses `SqliteConnector::open_in_memory()` (lands in Plan 04) as the concrete connector; Plan 04 must merge before this test passes (acceptable — the test compiles in this plan but RED-asserts until Plan 04)
    - Test #3 seeds the in-memory SQLite table by calling `connector.execute()` (Plan 04's runtime API) directly — NO helper like `open_in_memory_with_setup` is required from Plan 04
  </behavior>
  <read_first>
    crates/pmcp-server-toolkit/tests/code_mode_wiring.rs (lines 1-60 — test header + tokio::test pattern),
    crates/pmcp-server-toolkit/src/tools.rs (updated synthesizer from Task 2),
    src/server/core.rs (lines 593-601 — the conversion gate),
    src/types/tools.rs (lines 582-600 — `with_widget_enrichment` body),
    .planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-PATTERNS.md (row for `tests/synthesizer_structured_content.rs`),
    .planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-RESEARCH.md §4 "Two resolution paths for D-06"
  </read_first>
  <action>
    Create new file `crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs`:
    
    Header:
    ```
    //! D-06 integration anchor — synthesizer emits structuredContent for widget tools.
    //!
    //! Per RESEARCH §4 Option A: when `ToolDecl.ui_resource_uri.is_some()`, the
    //! synthesizer flips `widget_meta` on the synthesized `ToolInfo` so pmcp
    //! core's `CallToolResult::with_widget_enrichment` populates `structured_content`
    //! with the `Vec<Value>` rows returned by the connector. This test asserts the
    //! contract end-to-end against an in-memory SQLite connector.
    
    #![cfg(all(feature = "code-mode", feature = "sqlite"))]
    ```
    
    Test bodies:
    
    1. **`#[tokio::test] async fn widget_tool_gets_widget_meta()`** — Parse a minimal config containing one `[[tools]]` entry with `sql = "SELECT 1 AS x"` + `ui_resource_uri = "ui://test"`. Build `let conn: Arc<dyn SqlConnector> = Arc::new(SqliteConnector::open_in_memory()?);`. Call `synthesize_from_config_with_connector(&cfg, conn)`. Assert `tools[0].1.widget_meta().is_some()` (the `.1` is the `ToolInfo` per the tuple alias).
    
    2. **`#[tokio::test] async fn non_widget_tool_has_no_widget_meta()`** — Same but with `ui_resource_uri` absent. Assert `tools[0].1.widget_meta().is_none()`.
    
    3. **`#[tokio::test] async fn widget_handler_produces_value_array()`** — Build a config with a tool whose sql returns 2 rows. Setup steps:
       ```
       let conn: Arc<dyn SqlConnector> = Arc::new(SqliteConnector::open_in_memory()?);
       conn.execute("CREATE TABLE t (x INTEGER)", &[]).await?;
       conn.execute(
           "INSERT INTO t VALUES (?), (?)",
           &[("p1".into(), serde_json::json!(1)), ("p2".into(), serde_json::json!(2))],
       ).await?;
       ```
       Then synthesize tools via `synthesize_from_config_with_connector(&cfg, Arc::clone(&conn))`. Call the handler's `handle(json!({}), extra)` (extra constructed via `RequestHandlerExtra::default()` or the crate's test-fixture pattern). Assert the returned `Value` is `Value::Array(rows)` where `rows.len() == 2` and `rows[0]["x"] == Value::from(1)`.
       
       Note: This test uses ONLY the public Plan-04 API surface: `SqliteConnector::open_in_memory()` + `connector.execute(sql, &params)`. NO setup-helper (`open_in_memory_with_setup`) is required from Plan 04. The table creation + seeding happens via explicit `.execute()` calls on the connector itself — this is the same path real consumers will use.
    
    4. **`#[tokio::test] async fn end_to_end_structured_content_via_pmcp_core()`** — If feasible without a full ServerCore boot: construct a `ToolInfo` with widget_meta and call `CallToolResult::with_widget_enrichment(info, Value::Array(rows))`. Assert `result.structured_content == Some(Value::Array(rows))`. (Bypasses the full server loop but directly exercises the widget-enrichment gate.) This is the "binding test" that proves D-06 fires.
    
    Use the same single-thread tokio test pattern from `tests/code_mode_wiring.rs` lines 1-21 (which uses `#[tokio::test(flavor = "current_thread")]` or simply `#[tokio::test]` — read the existing file).
    
    Note: Tests #1, #2, and #3 depend on `SqliteConnector::open_in_memory()` from Plan 04. Until Plan 04 lands, these tests fail at compile or at runtime (acceptable — the contract anchor is the file existence + the assertions, which Plan 04 satisfies). Test #4 stands alone if `with_widget_enrichment` is exercisable without sqlite.
  </action>
  <verify>
    <automated>cargo build -p pmcp-server-toolkit --features sqlite --features code-mode --tests</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs`
    - Source: `grep -q "#!\[cfg(all(feature = \"code-mode\", feature = \"sqlite\"))\]" crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs` exits 0
    - Source: `grep -q "widget_meta()" crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs` exits 0
    - Source: `grep -q "structured_content" crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs` exits 0
    - Source: `grep -q "synthesize_from_config_with_connector" crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs` exits 0 (uses the new variant)
    - Source: `grep -q "open_in_memory()" crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs` exits 0 (uses public Plan-04 API only)
    - Source: `! grep -q "open_in_memory_with_setup" crates/pmcp-server-toolkit/tests/synthesizer_structured_content.rs` (no nonexistent helper)
    - Source: at least 4 `#[tokio::test]` functions
    - Build: `cargo build -p pmcp-server-toolkit --features sqlite --features code-mode --tests` exits 0 (compiles; runtime pass depends on Plan 04)
    - After Plan 04: `cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --test synthesizer_structured_content` exits 0
  </acceptance_criteria>
  <done>D-06 integration anchor file present, compiles, contains 4 tokio tests covering widget-meta flip + structured_content emission contract. Test #3 seeds in-memory SQLite via the public `connector.execute()` API — no Plan-04 helper required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool args → synthesized handler → SQL execution | Tool arguments validated by JSON schema (P83 TKIT-07); the handler's `extract_named_params` filters args against declared parameters — only declared names reach `connector.execute()` |
| Synthesized handler → ConnectorError → pmcp::Error::Internal → MCP client | Error mapping uses `format!("{e}")` which uses sanitized Display (Plan 01 Task 2 guardrail) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03-01 | Tampering | Tool args injected with extra keys not in declared parameters | mitigate | Synthesizer filters: only `decl.parameters.iter().filter_map(|p| args.get(&p.name)...)` reaches `execute()`. Extra args silently dropped. JSON schema validation (P83 TKIT-07) catches the rest. |
| T-84-03-02 | Information Disclosure | `ConnectorError` raw text reaches MCP client via `pmcp::Error::Internal` | mitigate | Plan 01 Task 2 unit test asserts `ConnectorError::Connection` Display does not echo passwords. Per-backend impls (Wave 2) sanitize before wrapping. |
| T-84-03-03 | Tampering | `widget_meta` flipped on non-widget tools by mistake | accept | Conditional is explicit: `if decl.ui_resource_uri.is_some()`. Test #2 asserts non-widget tools have no widget_meta. |
| T-84-03-04 | Information Disclosure | `[database].url` field contains raw credentials in config files committed to git | mitigate | Field rustdoc steers users to `env:VAR_NAME` indirection (P83 R6/R9 pattern). Inline raw URL parsing is allowed (per D-08 — URL string is the constructor input) but the helper docs recommend env indirection. Inline-secret rejection lives in per-backend connector setup (Plans 05/06) — those plans handle the `allow_inline_url_for_dev` discipline. |
| T-84-03-05 | Denial of Service | Handler built via existing `synthesize_from_config` (no connector) receives a tool call at runtime | mitigate | `SynthesizedToolHandler::handle` returns `Err(pmcp::Error::Internal("tool requires connector wiring..."))` when `self.connector.is_none()`. This preserves P83 behavior (existing tests never invoke handlers; production consumers MUST use `synthesize_from_config_with_connector`). |
</threat_model>

<verification>
- `cargo build -p pmcp-server-toolkit --features sqlite --features code-mode` succeeds
- `cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --test reference_configs` exits 0 (REF-01 intact, caller at line 70 unmodified)
- `cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --test tool_synthesis_props` exits 0 (3 prop tests unmodified)
- `cargo test -p pmcp-server-toolkit --features sqlite --features code-mode --lib config` exits 0
- `cargo test --doc -p pmcp-server-toolkit --features sqlite build_code_mode_prompt` exits 0
- `cargo test --doc -p pmcp-server-toolkit --features sqlite synthesize_from_config` exits 0 (existing doctest at tools.rs:71 unmodified)
- `cargo build --tests -p pmcp-server-toolkit --features sqlite --features code-mode` succeeds (synthesizer_structured_content.rs compiles)
- After Plan 04 lands: `cargo test -p pmcp-server-toolkit --features sqlite --features code-mode` ALL tests pass including the new integration anchor
- PMAT clean on `src/tools.rs` — no cog 25 violations
- All 11 existing `synthesize_from_config` callers compile + pass without modification
</verification>

<success_criteria>
- `build_code_mode_prompt` exists as a thin alias next to `assemble_code_mode_prompt`
- `DatabaseSection.url: Option<String>` is parseable
- Existing `synthesize_from_config(cfg)` is preserved UNCHANGED — all 11 P83 callers still compile + pass
- NEW `synthesize_from_config_with_connector(cfg, connector)` exists as an additive entry point
- Both entry points share a `synthesize_inner` helper that flips widget_meta when `ui_resource_uri` is set
- `SynthesizedToolHandler::handle` calls `connector.execute()` when connector is wired; returns explicit error otherwise
- `builder_ext.rs` production caller switched to the new variant
- `lib.rs` re-exports both functions (additive — line 91 unchanged, new export added below)
- All 3 reference configs continue to parse (REF-01 invariant)
- Integration test `synthesizer_structured_content.rs` compiles and contains 4 test functions
- Test #3 uses only Plan-04's public API (`open_in_memory()` + `execute()`) — no nonexistent `open_in_memory_with_setup` helper required
- PMAT clean (no cognitive complexity violations introduced)
</success_criteria>

<output>
After completion, create `.planning/phases/84-sql-connectors-postgres-mysql-athena-sqlite/84-03-SUMMARY.md`
</output>
