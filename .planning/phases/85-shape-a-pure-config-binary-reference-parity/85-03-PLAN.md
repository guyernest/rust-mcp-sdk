---
phase: 85-shape-a-pure-config-binary-reference-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/pmcp-sql-server/Cargo.toml
  - crates/pmcp-sql-server/src/lib.rs
  - crates/pmcp-sql-server/src/main.rs
  - crates/pmcp-sql-server/tests/fixtures/chinook.ddl
  - crates/pmcp-sql-server/tests/fixtures/reference-config.toml
  - crates/pmcp-sql-server/tests/fixtures/generated.yaml
  - crates/pmcp-sql-server/tests/schema_fixture.rs
autonomous: true
requirements: [REF-02]
must_haves:
  truths:
    - "crates/pmcp-sql-server/ exists in the workspace and builds (empty lib stub)"
    - "The Chinook DDL fixture is committed and self-contained (no pmcp-run dependency)"
    - "The reference config.toml and generated.yaml scenarios are vendored into the SDK repo"
    - "The DDL fixture, fed to SqliteConnector + the curated tool SQL, reproduces the reference schema shape"
  artifacts:
    - path: "crates/pmcp-sql-server/Cargo.toml"
      provides: "Feature-gated 4-connector crate manifest (lib + bin split)"
      contains: "default = [\"sqlite\""
    - path: "crates/pmcp-sql-server/tests/fixtures/chinook.ddl"
      provides: "Self-contained Chinook DDL (--schema input + parity DB seed)"
      contains: "CREATE TABLE"
    - path: "crates/pmcp-sql-server/tests/fixtures/generated.yaml"
      provides: "Vendored 31-scenario parity contract"
    - path: "crates/pmcp-sql-server/tests/fixtures/reference-config.toml"
      provides: "Vendored Chinook reference config (the Shape A target)"
  key_links:
    - from: "Cargo.toml [workspace.members]"
      to: "crates/pmcp-sql-server"
      via: "members array insert"
      pattern: "pmcp-sql-server"
    - from: "tests/fixtures/chinook.ddl"
      to: "SqliteConnector (DB built from DDL in test setup)"
      via: "execute_batch on a fresh in-memory/temp DB"
      pattern: "CREATE TABLE"
---

<objective>
Close RESEARCH Open Question #1: the Chinook DB + `generated.yaml` live in the SEPARATE `pmcp-run` repo. Vendor the Chinook DDL fixture + the scenario YAML + the reference config into the SDK repo so the parity test (Plan 06) is self-contained and CI-runnable with no cross-repo dependency. Also scaffold the empty `crates/pmcp-sql-server/` crate (manifest + lib/main stub) so Waves 2-3 have a place to build.

Purpose: The parity harness can't run in CI against a fixture that lives in another repo. Committing the DDL (not the binary `.db`) keeps the published crate lean and lets the test build a fresh DB deterministically. D-06 chose generating the DDL from `sqlite3 chinook.db .schema`.
Output: New crate skeleton in the workspace + three committed fixtures + a fixture-shape sanity test.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-CONTEXT.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-RESEARCH.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-PATTERNS.md

<interfaces>
Source fixtures to vendor (read + copy verbatim):
- /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/reference/config.toml → tests/fixtures/reference-config.toml
- /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/reference/scenarios/generated.yaml (8298 bytes, 31 scenarios) → tests/fixtures/generated.yaml
- DDL: generate via `sqlite3 /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/reference/chinook.db .schema` (11 CREATE TABLE statements) → tests/fixtures/chinook.ddl

Crate manifest shape (PATTERNS §Cargo.toml, verified versions — re-confirm connector versions at execution time):
```toml
[package]
name = "pmcp-sql-server"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/paiml/rust-mcp-sdk"

[lib]
name = "pmcp_sql_server"
path = "src/lib.rs"

[[bin]]
name = "pmcp-sql-server"
path = "src/main.rs"

[dependencies]
pmcp = { version = "...", path = "../..", features = ["streamable-http"] }
pmcp-server-toolkit = { version = "...", path = "../pmcp-server-toolkit", features = ["code-mode", "sqlite"] }
pmcp-toolkit-postgres = { path = "../pmcp-toolkit-postgres", optional = true }
pmcp-toolkit-mysql    = { path = "../pmcp-toolkit-mysql", optional = true }
pmcp-toolkit-athena   = { path = "../pmcp-toolkit-athena", optional = true }
clap = { version = "4", features = ["derive", "env"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[features]
default = ["sqlite", "postgres", "mysql", "athena"]   # D-07 all default-on
sqlite   = ["pmcp-server-toolkit/sqlite"]
postgres = ["dep:pmcp-toolkit-postgres"]
mysql    = ["dep:pmcp-toolkit-mysql"]
athena   = ["dep:pmcp-toolkit-athena"]

[dev-dependencies]
mcp-tester = { path = "../mcp-tester" }
tempfile = "3"
proptest = "1"
```
Root Cargo.toml [workspace.members] (~line 541) — insert "crates/pmcp-sql-server".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the pmcp-sql-server crate (manifest + lib/main stub) and register in the workspace</name>
  <files>Cargo.toml, crates/pmcp-sql-server/Cargo.toml, crates/pmcp-sql-server/src/lib.rs, crates/pmcp-sql-server/src/main.rs</files>
  <read_first>
    - crates/mcp-tester/Cargo.toml (the lib+bin-split workspace-binary analog; clap, pmcp path-dep with streamable-http, tracing-subscriber pin)
    - Cargo.toml (root — [workspace.members] array, the one-token insert point; check exact connector version strings of pmcp-toolkit-* and pmcp / pmcp-server-toolkit)
    - crates/pmcp-toolkit-postgres/Cargo.toml, crates/pmcp-toolkit-mysql/Cargo.toml, crates/pmcp-toolkit-athena/Cargo.toml (confirm version + that optional dep-by-path resolves; these are unpublished Phase 84 crates)
    - crates/pmcp-server-toolkit/examples/e01_toolkit_minimal.rs (the lib::run assembly target the stub leaves a TODO-free placeholder for; Wave 2 fills it)
  </read_first>
  <action>
    Create `crates/pmcp-sql-server/Cargo.toml` per the manifest in interfaces — VERIFY the pmcp / pmcp-server-toolkit / connector-crate version strings from their own Cargo.toml at execution time (do not hardcode 2.8.1 blindly; the workspace may have bumped). Use the lib+bin split so the testable `run` lives in lib.rs and main.rs is a thin shim. Create `src/lib.rs` with a minimal compiling stub: a `pub async fn run(...)` placeholder returning `Ok(())` (or a documented `unimplemented!`-free stub that Wave 2 replaces) and a crate-level doc comment describing the binary's purpose; create `src/main.rs` as a 3-line `#[tokio::main]` shim calling into lib. NO `unimplemented!()` / `todo!()` / SATD comments (Toyota Way zero-SATD) — the stub must compile and pass clippy cleanly. Insert `"crates/pmcp-sql-server"` into the root `Cargo.toml` `[workspace.members]` array (single-token edit, alongside the other `crates/pmcp-toolkit-*` entries). Verify the crate builds with `--no-default-features --features sqlite` AND with default features.
  </action>
  <verify>
    <automated>cargo build -p pmcp-sql-server --no-default-features --features sqlite && cargo build -p pmcp-sql-server</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p pmcp-sql-server` succeeds (default features, all 4 connectors)
    - `cargo build -p pmcp-sql-server --no-default-features --features sqlite` succeeds (lean single-backend)
    - Source assertion: `grep -n "pmcp-sql-server" Cargo.toml` matches the workspace members array
    - `cargo clippy -p pmcp-sql-server --all-features -- -D warnings` clean; no todo!/unimplemented!/SATD in the stub
  </acceptance_criteria>
  <done>The crate exists in the workspace, builds under default + sqlite-only features, lib/main split in place, clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vendor the Chinook DDL + scenarios + reference config and add a fixture-shape sanity test</name>
  <files>crates/pmcp-sql-server/tests/fixtures/chinook.ddl, crates/pmcp-sql-server/tests/fixtures/reference-config.toml, crates/pmcp-sql-server/tests/fixtures/generated.yaml, crates/pmcp-sql-server/tests/schema_fixture.rs</files>
  <read_first>
    - /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/reference/scenarios/generated.yaml (the 31-scenario parity contract — copy verbatim, do not edit)
    - /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/reference/config.toml (the Shape A reference config — copy verbatim)
    - crates/pmcp-server-toolkit/src/sql/sqlite.rs (lines 72-88 open/open_in_memory; line 226-240 schema_text reads sqlite_master — the DDL fixture, loaded into a fresh DB, must produce schema_text() output covering the same tables)
    - crates/pmcp-server-toolkit/tests/backend_core_smoke.rs (the in-memory SQLite test style for building a DB + asserting against it)
  </read_first>
  <behavior>
    - Test: chinook.ddl is non-empty and contains the curated-tool tables (Artist, Album, Track, Genre) referenced by the reference config's [[tools]] SQL
    - Test: loading chinook.ddl into an in-memory SQLite DB (execute_batch) succeeds and creates >= 11 tables
    - Test: after loading, a SELECT against one curated table (e.g. Artist) returns rows or an empty set without error (DB is queryable)
    - Test: the vendored generated.yaml parses via mcp_tester::TestScenario (valid scenario file — confirms the parity contract is consumable)
  </behavior>
  <action>
    Generate the DDL with `sqlite3 /Users/guy/Development/mcp/sdk/pmcp-run/built-in/sql-api/reference/chinook.db .schema` and write it to `tests/fixtures/chinook.ddl` (D-06: this is the `--schema` input AND the parity DB seed; the binary `.db` is NOT committed to keep the published crate lean — exclude via `[package] exclude` or `.gitignore` if needed). Copy `reference/scenarios/generated.yaml` verbatim to `tests/fixtures/generated.yaml` and `reference/config.toml` verbatim to `tests/fixtures/reference-config.toml`. NOTE the reference config's `file_path = "/var/task/assets/chinook.db"` is a Lambda path — Plan 06's harness overrides it to point at a test-built temp DB; do not edit the fixture. Create `tests/schema_fixture.rs` implementing the four behaviors: include_str! the DDL, build an in-memory SQLite DB and `execute_batch` the DDL (rusqlite is available under the sqlite feature via the toolkit), assert table count and queryability; parse the vendored generated.yaml via `mcp_tester::TestScenario` (dev-dep) to confirm it is a valid scenario file. This is the REF-02 fixture foundation; the actual parity replay is Plan 06.
  </action>
  <verify>
    <automated>cargo test -p pmcp-sql-server --no-default-features --features sqlite --test schema_fixture -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-sql-server --test schema_fixture` passes
    - Source assertion: `grep -c "CREATE TABLE" crates/pmcp-sql-server/tests/fixtures/chinook.ddl` is >= 11
    - The vendored generated.yaml parses via mcp_tester::TestScenario (no parse error)
    - reference-config.toml is byte-identical to the pmcp-run source (verbatim vendor)
    - The published crate does not bundle the binary chinook.db (verify via `cargo package --list -p pmcp-sql-server` or a [package] exclude entry)
  </acceptance_criteria>
  <done>chinook.ddl, generated.yaml, reference-config.toml vendored; DDL builds a queryable DB; scenarios parse; fixtures self-contained for CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed DDL fixture → test SQLite DB | the DDL seeds a test database; it is repo-controlled (not external input), low risk |
| vendored config.toml → schema resource | the reference config's table list becomes a public MCP resource downstream (D-05 redaction applies in Plan 04/06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03-01 | Information disclosure | committed chinook.db binary | accept | Binary DB NOT committed (DDL only) — avoids shipping a ~1MB blob in the published crate; the DDL is public demo data (Chinook), no sensitive content |
| T-85-03-02 | Tampering | vendored generated.yaml drift | accept | Vendored verbatim as the parity contract; if pmcp-run updates scenarios, a manual re-vendor is required (documented in SUMMARY) — staleness is a known, accepted tradeoff (D-10 rejected golden-snapshot for the same reason but the scenario file IS the contract) |
</threat_model>

<verification>
- `cargo test -p pmcp-sql-server --no-default-features --features sqlite -- --test-threads=1` green
- `cargo build -p pmcp-sql-server` (default features) green
</verification>

<success_criteria>
- New crate registered + builds under default and sqlite-only features
- Chinook DDL + scenarios + reference config vendored and self-contained (REF-02 fixture foundation)
- DDL builds a queryable SQLite DB; generated.yaml parses as a valid scenario
</success_criteria>

<output>
After completion, create `.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-03-SUMMARY.md`
</output>
