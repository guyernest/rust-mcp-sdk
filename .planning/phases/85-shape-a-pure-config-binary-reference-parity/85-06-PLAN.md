---
phase: 85-shape-a-pure-config-binary-reference-parity
plan: 06
type: execute
wave: 4
depends_on: [85-05]
files_modified:
  - crates/pmcp-sql-server/tests/parity_chinook.rs
  - crates/pmcp-sql-server/examples/sql_server_min.rs
  - crates/pmcp-sql-server/src/lib.rs
  - CLAUDE.md
  - crates/pmcp-server-toolkit/fuzz/corpus/pmcp_server_toolkit_config_parser/seed-chinook-superset.toml
autonomous: true
requirements: [REF-02, SHAP-A-01]
must_haves:
  truths:
    - "Spawning the Shape A repro against the Chinook config + DDL fixture and replaying generated.yaml passes all 31 scenarios"
    - "The replay validates curated tools + validate_code (8×) + execute_code + start_code_mode prompt + resources in one harness (SC-3 + SC-4)"
    - "A Shape C ≤15-line example builds and runs"
    - "Public API items carry doctests"
    - "pmcp-sql-server is in the CLAUDE.md publish order after the per-backend connector crates"
    - "The config-parser fuzz corpus includes a Chinook-superset seed (ALWAYS fuzz)"
  artifacts:
    - path: "crates/pmcp-sql-server/tests/parity_chinook.rs"
      provides: "REF-02/SC-3/SC-4 spawn + replay generated.yaml"
      contains: "result.success"
    - path: "crates/pmcp-sql-server/examples/sql_server_min.rs"
      provides: "Shape C ≤15-line runnable example"
    - path: "CLAUDE.md"
      provides: "pmcp-sql-server publish-order slot"
      contains: "pmcp-sql-server"
  key_links:
    - from: "tests/parity_chinook.rs"
      to: "spawned StreamableHttpServer + mcp_tester ScenarioExecutor"
      via: "ServerTester::new(http url, force_transport=http) → execute(TestScenario::from_file(generated.yaml))"
      pattern: "ScenarioExecutor"
    - from: "parity result"
      to: "all 31 scenarios pass"
      via: "result.success assertion"
      pattern: "assert!.*result.success"
---

<objective>
Land the binding REF-02 parity assertion (D-10/D-11): spawn the Shape A reproduction against the vendored Chinook config + DDL fixture, replay `generated.yaml` via the mcp-tester library, and assert all 31 scenarios pass. The scenarios encode the production server's behavior — passing them = result parity (SC-4), and because they exercise `validate_code` (8×), `execute_code`, the `start_code_mode` prompt, curated tools, and resources, this single replay also proves code-mode policy parity (SC-3).

Plus the ALWAYS-matrix closeout: a Shape C ≤15-line runnable example, doctests on public items, the CLAUDE.md publish-order slot, and a config-parser fuzz corpus seed for the new Chinook-superset fields.

Purpose: This is the phase's headline proof — the toolkit lift reproduces a production pmcp-run server with zero user Rust. It is also the ALWAYS-coverage gate (CLAUDE.md): fuzz + property (prior plans) + unit (prior plans) + doctest + integration + example all present.
Output: the parity integration test, the example, doctests, the publish-order edit, and the fuzz seed.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-CONTEXT.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-RESEARCH.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-PATTERNS.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-05-SUMMARY.md

<interfaces>
mcp-tester library replay (VERIFIED — result accessor is `.success`, NOT `all_passed()`):
```rust
// crates/mcp-tester/src/lib.rs (run_scenario_with_transport pattern, lines 205-260)
use mcp_tester::{ServerTester, ScenarioExecutor, TestScenario};
let mut tester = ServerTester::new(
    &format!("http://{addr}"), Duration::from_secs(30),
    false /*insecure*/, None /*api_key*/, Some("http") /*force_transport*/, None /*middleware*/)?;
tester.initialize().await?;
let scenario = TestScenario::from_file("tests/fixtures/generated.yaml")?;
let mut exec = ScenarioExecutor::new(&mut tester, true /*detailed*/);
let result = exec.execute(scenario).await?;
assert!(result.success, "31 parity scenarios must pass: {result:?}");  // ScenarioResult.success: bool (scenario.rs:202)
```

Server spawn (from Plan 05 — reuse lib::build_server / the serve helper):
```rust
// 127.0.0.1:0 bind → start() returns the REAL bound addr (test-threads=1 safe)
let (addr, handle) = serve(server, "127.0.0.1:0".parse()?).await?;
// ... replay ... handle.abort();
```

Env setup (Pitfall 3 / Runtime State): the harness MUST set CODE_MODE_SECRET to >=16 bytes BEFORE building the server (the Chinook config uses token_secret = "${CODE_MODE_SECRET}", resolved by Plan 01's ${VAR} expansion).

The Chinook config's file_path is a Lambda path (/var/task/assets/chinook.db) — override it to point at a temp DB built from chinook.ddl, OR build an in-memory SqliteConnector seeded with the DDL and inject it (the dispatch reads file_path; for the test, build the connector directly from the DDL-seeded DB rather than dispatching on the Lambda path).

generated.yaml shape (VERIFIED histogram): list_tools×1, list_resources×1, list_prompts×1, tool_call×22, get_prompt×1 (start_code_mode), read_resource×3; assertions success×24, contains×8, exists×7, failure×5. The failure assertions on DELETE/DDL validate_code calls ARE the policy-enforcement (SC-3) proof.

Shape C example target: SHAP-A-01 wants a runnable example; VALIDATION names examples/sql_server_min.rs. The ≤15-line body mirrors e01_toolkit_minimal.rs but uses a connector + the binary's run/build_server. (Note: SHAP-C-01's strict ≤15-line library example is owned by Phase 86; this is the ALWAYS-matrix runnable example for the binary crate.)

CLAUDE.md publish order: pmcp-sql-server depends on pmcp-server-toolkit + the 4 connectors → slot it AFTER the per-backend connector crates (after pmcp-toolkit-athena, item 8) and before/around mcp-tester. Add a line to the numbered publish-order list + a note that it has no inter-deps with mcp-tester.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parity_chinook.rs — spawn the Shape A repro + replay generated.yaml, assert all 31 scenarios pass (REF-02/SC-3/SC-4)</name>
  <files>crates/pmcp-sql-server/tests/parity_chinook.rs</files>
  <read_first>
    - crates/mcp-tester/src/lib.rs (lines 205-260 run_scenario_with_transport — the exact load → ServerTester::new → ScenarioExecutor::new → execute → result.success flow; result.success is the accessor, scenario.rs:202)
    - crates/mcp-tester/src/scenario.rs (lines 200-215 ScenarioResult — confirm .success: bool; Operation/Assertion enums map to generated.yaml's list_tools/list_resources/list_prompts/tool_call/get_prompt/read_resource + success/contains/exists/failure)
    - tests/streamable_http_integration.rs (lines 32-90 — 127.0.0.1:0 spawn + start() → (addr, handle) + StreamableHttpTransport client; the test-threads=1-safe ephemeral-port pattern)
    - crates/pmcp-sql-server/src/lib.rs (the serve/build_server entry points from Plan 05 to reuse)
    - crates/pmcp-sql-server/tests/fixtures/{generated.yaml,reference-config.toml,chinook.ddl} (Plan 03 vendored fixtures — the parity contract + config + DB seed)
    - crates/pmcp-server-toolkit/src/sql/sqlite.rs (open_in_memory + execute_batch path to seed the DB from chinook.ddl)
  </read_first>
  <behavior>
    - Test: set CODE_MODE_SECRET (>=16 bytes); build an in-memory SqliteConnector seeded with chinook.ddl; assemble the server from reference-config.toml + the DDL as --schema; spawn on 127.0.0.1:0; poll readiness; replay generated.yaml; assert result.success is true
    - Test: the replay covers all 31 scenarios (list_tools/resources/prompts, search_tracks/list_artists/get_album_tracks tool_calls, validate_code×8, execute_code, start_code_mode prompt, read_resource×3) — a non-success result fails the test with the failing step detail
    - Test: the 5 `failure` assertions (DELETE/DDL validate_code) pass — i.e. the policy correctly REJECTS those (SC-3 enforcement)
  </behavior>
  <action>
    Create `tests/parity_chinook.rs`. Setup: `std::env::set_var("CODE_MODE_SECRET", "<at-least-16-byte-test-secret>")` (V6 min length). Build the Chinook server: parse the vendored `reference-config.toml`, build a connector from a DB seeded with `chinook.ddl` (use `SqliteConnector::open_in_memory()` then `execute_batch` the DDL, OR write the DDL to a tempfile-built `.db` via tempfile and `SqliteConnector::open` — prefer in-memory for speed; the Chinook config's Lambda `file_path` is overridden by injecting this connector directly into `build_server` rather than dispatching on the path). Feed `chinook.ddl` content as the `--schema` text to `build_server`. Spawn via the Plan 05 `serve` helper on `127.0.0.1:0`, read the real bound addr. Poll readiness (prefer a poll loop calling `tester.initialize()` with backoff over a fixed sleep — D-disc). Replay: `ServerTester::new(http url, Duration::from_secs(30), false, None, Some("http"), None)` → `TestScenario::from_file("tests/fixtures/generated.yaml")` → `ScenarioExecutor::new(&mut tester, true).execute(scenario).await?` → `assert!(result.success, "...{result:?}")`. `handle.abort()` at the end. The test runs with `--test-threads=1` (ephemeral port + single process). This is the REF-02/SC-3/SC-4 binding assertion. Record in the SUMMARY the verifier note that REF-02's literal "open-images" wording is intentionally satisfied by the SQLite Chinook reference that owns the scenarios (D-01 approved scope reading, not a gap).
  </action>
  <verify>
    <automated>cargo test -p pmcp-sql-server --no-default-features --features sqlite --test parity_chinook -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-sql-server --test parity_chinook -- --test-threads=1` passes; result.success is true (all 31 scenarios)
    - The DELETE/DDL validate_code scenarios (failure assertions) pass — policy enforcement confirmed (SC-3)
    - Source assertion: `grep -n "result.success" crates/pmcp-sql-server/tests/parity_chinook.rs` matches; `grep -n "generated.yaml" ...` matches
    - Test uses the spawned HTTP server (force_transport "http") and aborts the handle (no leaked task)
  </acceptance_criteria>
  <done>The Shape A repro replays generated.yaml with all 31 scenarios passing — curated tools + code-mode pair + prompt + resources, including DELETE/DDL policy rejection (REF-02 / SC-3 / SC-4).</done>
</task>

<task type="auto">
  <name>Task 2: Shape C example + doctests + CLAUDE.md publish-order + fuzz corpus seed (ALWAYS closeout)</name>
  <files>crates/pmcp-sql-server/examples/sql_server_min.rs, crates/pmcp-sql-server/src/lib.rs, CLAUDE.md, crates/pmcp-server-toolkit/fuzz/corpus/pmcp_server_toolkit_config_parser/seed-chinook-superset.toml</files>
  <read_first>
    - crates/pmcp-server-toolkit/examples/e01_toolkit_minimal.rs (the ≤15-line Shape C body shape — single crate-root import, try_* builder chain, no panic)
    - crates/pmcp-sql-server/src/lib.rs (the public run/build_server surface that needs doctests; the example calls into this)
    - CLAUDE.md (§"Release & Publish Workflow" — the numbered publish-order list 1-11; pmcp-sql-server slots after item 8 pmcp-toolkit-athena, with a no-inter-deps-with-mcp-tester note like the connector-crate note)
    - crates/pmcp-server-toolkit/fuzz/corpus/pmcp_server_toolkit_config_parser/ (existing named seed-*.toml convention from Phase 84 — add ONE seed exercising the new file_path/is_reference/[shared_policy_store] fields; named seed only, libfuzzer hash entries left untracked)
    - crates/pmcp-server-toolkit/tests/reference_configs.rs (lines 124-157 — the seed-parse smoke test convention; the new seed must parse, not panic)
  </read_first>
  <action>
    (a) Create `examples/sql_server_min.rs` — a runnable example demonstrating library use of the binary crate: build a server from an inline minimal config + an in-memory SQLite connector + a short DDL schema string, in ≤15 lines of `main` body (mirror e01_toolkit_minimal.rs; set the token secret env first; use the try_* / build_server path; print a confirmation). Register it as an `[[example]]` in the crate Cargo.toml if needed and ensure it runs under `--features sqlite`. (b) Add doctests to the public API items in `src/lib.rs` (run, build_server if pub, and any pub dispatch/cli re-exports) — `rust,no_run` doctests showing usage; every public item must have one (TEST-03 ALWAYS convention). (c) Edit CLAUDE.md §"Release & Publish Workflow": insert `pmcp-sql-server` into the numbered publish-order list AFTER the three per-backend connector crates (after item 8 `pmcp-toolkit-athena`), with a one-line note that it depends on pmcp-server-toolkit + the four connectors and has no inter-dep with mcp-tester (renumber the trailing items, or insert as 8.x if renumbering is risky — match the existing list style). (d) Add `fuzz/corpus/pmcp_server_toolkit_config_parser/seed-chinook-superset.toml` — a small valid config exercising `[database] file_path`, `[server] is_reference`, and `[shared_policy_store]` (the Plan 01 superset fields) so the existing fuzz target covers them (ALWAYS fuzz; extend, do not duplicate — Phase 84 convention). Track only the named seed file.
  </action>
  <verify>
    <automated>cargo run -p pmcp-sql-server --example sql_server_min --no-default-features --features sqlite && cargo test -p pmcp-sql-server --doc --no-default-features --features sqlite</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p pmcp-sql-server --example sql_server_min --features sqlite` exits 0 and prints a confirmation
    - `cargo test -p pmcp-sql-server --doc` passes; every public item in lib.rs has a doctest (`cargo doc` shows no missing-docs warning if missing_docs is denied)
    - Source assertion: `grep -n "pmcp-sql-server" CLAUDE.md` matches in the publish-order section
    - The fuzz seed parses: `cargo test -p pmcp-server-toolkit --test reference_configs` (seed-parse smoke) still green with the new seed; optionally `cargo fuzz run pmcp_server_toolkit_config_parser -- -max_total_time=30` exits clean
    - `make quality-gate` passes (or, if blocked by the documented pre-existing pmcp-widget-utils pedantic lints noted in Phase 84, scope the verification sweep to the touched crates and record the deferral in the SUMMARY)
  </acceptance_criteria>
  <done>Shape C example runs; doctests on public items pass; CLAUDE.md publish-order updated; fuzz corpus seeded for the new superset fields — ALWAYS matrix complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replayed scenarios → spawned HTTP server | the parity harness drives the full MCP surface incl. code-mode; the failure assertions verify policy rejects writes/DDL |
| malformed config (fuzz) → toolkit parser | the new superset fields must never panic the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02-02 (re-verified) | Elevation of privilege | code-mode policy via replay | mitigate | The generated.yaml `failure` assertions on DELETE/DDL validate_code calls confirm the static policy rejects writes/DDL end-to-end through HTTP (SC-3 black-box proof on top of Plan 02's white-box unit test) |
| T-85-06-01 | Denial of service | new config fields under fuzz | mitigate | seed-chinook-superset.toml extends the existing pmcp_server_toolkit_config_parser fuzz target so file_path/is_reference/[shared_policy_store] parsing is covered by the no-panic invariant (ALWAYS fuzz) |
| T-85-06-02 | Spoofing | parity harness HTTP transport | accept | Test-only loopback (127.0.0.1:0) with force_transport "http"; the SDK transport sends correct Content-Type/Accept headers (Pitfall 5) — no production exposure |
</threat_model>

<verification>
- `cargo test -p pmcp-sql-server --no-default-features --features sqlite -- --test-threads=1` green (incl. parity_chinook)
- `cargo run -p pmcp-sql-server --example sql_server_min --features sqlite` exits 0
- `cargo test -p pmcp-sql-server --doc` green
- `make quality-gate` green (or scoped per documented Phase 84 deferral)
</verification>

<success_criteria>
- Shape A repro replays generated.yaml — all 31 scenarios pass (REF-02 / SC-3 / SC-4)
- ALWAYS matrix complete: fuzz (seed) + property (Plans 01/02/04) + unit (prior) + doctest + integration (parity) + runnable example
- pmcp-sql-server in CLAUDE.md publish order after the per-backend connectors
</success_criteria>

<output>
After completion, create `.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-06-SUMMARY.md`
</output>
