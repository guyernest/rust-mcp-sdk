---
phase: 85-shape-a-pure-config-binary-reference-parity
plan: 02
type: execute
wave: 2
depends_on: [85-01]
files_modified:
  - crates/pmcp-server-toolkit/src/code_mode.rs
  - crates/pmcp-server-toolkit/tests/code_mode_tools.rs
autonomous: true
requirements: [SHAP-A-01]
must_haves:
  truths:
    - "register_code_mode_tools actually registers validate_code and execute_code (today it registers ZERO tools)"
    - "A SqlCodeExecutor adapter bridges the toolkit's single-method SqlConnector::execute to SELECT/DML/DDL"
    - "Code-mode policy (allow_writes/allow_deletes/allow_ddl) is enforced statically from [code_mode] config"
    - "A config-driven server CANNOT bypass write/DDL guards — DELETE/DDL on a read-only config is rejected"
    - "The --schema DDL text can seed the code-mode prompt WITHOUT calling connector.schema_text() (file replaces live introspection)"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/code_mode.rs"
      provides: "Real register_code_mode_tools body + SqlCodeExecutor adapter + file-based prompt seam"
      contains: "SqlCodeExecutor"
    - path: "crates/pmcp-server-toolkit/tests/code_mode_tools.rs"
      provides: "Registration + static-policy-enforcement integration test"
  key_links:
    - from: "register_code_mode_tools"
      to: "pmcp::ServerBuilder validate_code/execute_code tools"
      via: "CodeModeToolBuilder + tool_arc registration"
      pattern: "execute_code"
    - from: "SqlCodeExecutor::execute"
      to: "Arc<dyn SqlConnector>::execute"
      via: "re-validate then dispatch by query type"
      pattern: "connector.*execute"
    - from: "assemble_code_mode_prompt_with_schema"
      to: "--schema file text"
      via: "pure string assembly (no connector.schema_text call)"
      pattern: "schema_text: &str"
---

<objective>
Close RESEARCH Gap #2: `register_code_mode_tools` (Phase 83) is shape-preserving — it builds + validates a `ValidationPipeline` (so R9/secret errors fire) but registers ZERO tools (Plan 08 was deferred). SC-3/SC-4 require `validate_code`/`execute_code` to EXIST and enforce policy. This is the core security-parity surface.

The toolkit's `SqlConnector` exposes a SINGLE `execute(sql, params)` method; the production `SqlCodeModeHandler` mirror is over a different 2-method `DatabaseConnector` trait (`execute_query`/`execute_statement`). The adapter must be RE-DERIVED for the single-method trait — re-validate the SQL (defense-in-depth) then call `connector.execute(...)` regardless of query type, since the toolkit trait has one entry point.

This plan ALSO adds the `--schema` file → code-mode prompt seam (`assemble_code_mode_prompt_with_schema`). The existing `assemble_code_mode_prompt(connector, config)` reads `connector.schema_text()` live — for non-SQLite backends that hits the network (breaking SC-1) and ignores the admin-redacted `--schema` file (D-04/D-05). All three changes live in `code_mode.rs`, so they are co-located here (after Plan 01's `${VAR}` expansion to the same file).

Purpose: Without registered code-mode tools, the 8 `validate_code` scenarios + `execute_code` scenario in the parity harness (Plan 06) cannot pass. Static policy enforcement IS the SC-3 claim (D-13). The file-based prompt seam is the D-05 redaction guarantee + SC-1 prerequisite.
Output: A real `register_code_mode_tools` body + `SqlCodeExecutor` adapter + the file-based prompt helper + a registration/policy-enforcement test.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-CONTEXT.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-RESEARCH.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-PATTERNS.md
@.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-01-SUMMARY.md

<interfaces>
From crates/pmcp-server-toolkit/src/code_mode.rs (existing helpers to REUSE — do not reimplement):
```rust
pub fn validation_pipeline_from_config(config: &ServerConfig) -> Result<ValidationPipeline>;  // line 117 — R9 gate + policy
fn build_cm_config(section: &CodeModeSection) -> CodeModeConfig;                                // line 206 — config → CodeModeConfig
fn resolve_token_secret(section: &CodeModeSection) -> Result<SecretValue>;                      // line 284 — secret resolution (Plan 01 added ${VAR})
pub async fn assemble_code_mode_prompt(connector: &(dyn SqlConnector + '_), config: &ServerConfig) -> Result<String>;  // line 365 — connector-based (reads schema_text); the file-based variant mirrors this
fn format_curated_tables(config: &ServerConfig) -> String;                                      // reused by the prompt assemblers
pub use pmcp_code_mode::{CodeExecutor, CodeModeConfig, HmacTokenGenerator, NoopPolicyEvaluator, PolicyEvaluator, ValidationPipeline};
// current shape-preserving stub (lines 176-192):
pub fn register_code_mode_tools(builder: pmcp::ServerBuilder, config: &ServerConfig) -> Result<pmcp::ServerBuilder>;
```

From crates/pmcp-server-toolkit/src/sql/mod.rs (the SINGLE-method trait the adapter bridges + Dialect):
```rust
pub trait SqlConnector: Send + Sync + 'static {
    fn dialect(&self) -> Dialect;
    async fn execute(&self, sql: &str, params: &[(String, serde_json::Value)])
        -> Result<Vec<serde_json::Value>, ConnectorError>;   // ONE method — re-validate then call this
    async fn schema_text(&self) -> Result<String, ConnectorError>;
}
// Dialect has name() + placeholder_guidance() (used by the prompt assemblers)
```

From crates/pmcp-code-mode/src/handler.rs (the tool builders + executor trait):
```rust
pub struct CodeModeToolBuilder { /* ... */ }
impl CodeModeToolBuilder {
    pub fn new(code_format: &str) -> Self;       // line 306 — "sql"
    pub fn build_validate_tool(&self) -> ToolInfo;  // line 318 (input schema incl. dry_run)
    pub fn build_execute_tool(&self) -> ToolInfo;   // line 354
}
// CodeExecutor::execute(code, variables) -> Result<Value, ExecutionError>
```

From crates/pmcp-server-toolkit/src/builder_ext.rs (the call site the binary uses — must light up after this plan):
```rust
fn try_code_mode_from_config(self, config: &ServerConfig) -> Result<Self>;  // line 211 — delegates to register_code_mode_tools
```

CROSS-REPO ANALOG (read, then re-derive for single-method trait):
pmcp-run/built-in/sql-api/crates/mcp-sql-server-core/src/code_mode.rs:75-204 — SqlCodeModeHandler (CodeExecutor impl, 2-method connector) + SqlCodeModeServer (#[derive(CodeMode)]). The execute() re-validates with SqlValidator then dispatches by QueryType. Toolkit version collapses the 2-method dispatch into a single connector.execute() call.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the SqlCodeExecutor adapter over the single-method SqlConnector</name>
  <files>crates/pmcp-server-toolkit/src/code_mode.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/code_mode.rs (full file — the home of the new struct; reuse build_cm_config:206, resolve_token_secret:284, validation_pipeline_from_config:117; note the pmcp_code_mode re-exports at lines 58-63; Plan 01 already added ${VAR} to resolve_token_secret — do NOT touch that branch)
    - crates/pmcp-server-toolkit/src/sql/mod.rs (lines 101-145 — the single-method SqlConnector::execute the adapter calls)
    - pmcp-run/built-in/sql-api/crates/mcp-sql-server-core/src/code_mode.rs (lines 75-176 — the production SqlCodeModeHandler::execute to mirror; re-validate then dispatch — collapse its execute_query/execute_statement split into one connector.execute call)
    - crates/pmcp-code-mode/src/handler.rs (the CodeExecutor trait + async_trait re-export)
  </read_first>
  <behavior>
    - Test: SqlCodeExecutor::execute("SELECT * FROM Artist", None) under a read-only config returns Ok with a JSON result (rows)
    - Test: SqlCodeExecutor::execute("DELETE FROM Artist", None) under a config with allow_deletes=false returns Err (policy rejection via re-validation), connector.execute is NOT reached
    - Test: SqlCodeExecutor::execute("DROP TABLE Artist", None) under allow_ddl=false returns Err
    - Test: a malformed SQL string returns Err, never panics
  </behavior>
  <action>
    Add a `pub struct SqlCodeExecutor { connector: Arc<dyn SqlConnector>, config: ServerConfig (or the CodeModeSection + server_name slice needed for re-validation) }` to code_mode.rs, mirroring the production `SqlCodeModeHandler` struct shape (code_mode.rs:75-79 cross-repo) but holding the toolkit's `Arc<dyn SqlConnector>`. Impl `pmcp_code_mode::CodeExecutor` via `#[pmcp_code_mode::async_trait]` (use the re-export per D-15 single-import convention). The `execute(code, _vars)` body: (1) re-validate the SQL against the `[code_mode]` policy using the same validator the pipeline uses (defense-in-depth — guards token replay with modified SQL, per the production analog and threat T-85-02-01); a policy violation returns `Err(ExecutionError::...)` BEFORE touching the connector; (2) on pass, call `self.connector.execute(code, &[]).await`, mapping `ConnectorError` to `ExecutionError::BackendError` with a SANITIZED message (do not echo raw backend internals — V7); (3) return `serde_json::json!({ "rows": <values> })` (the single-method trait returns `Vec<Value>` rows; there is no separate columns/rows_affected split — document this divergence from production in a doc comment, NOT a SATD comment). Keep `execute` cognitive complexity <= 25 (extract a `revalidate(&self, code) -> Result<...>` helper). Add unit tests inline (or in Task 2's file) for the four behaviors using `MockSqlConnector` (pub(crate), reachable under `--features sqlite` per Phase 84 decision) or `SqliteConnector::open_in_memory()`.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features "code-mode sqlite" code_mode -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "struct SqlCodeExecutor" crates/pmcp-server-toolkit/src/code_mode.rs` matches; `grep -n "impl.*CodeExecutor.*for SqlCodeExecutor" ...` matches
    - Behavior: read-only SELECT succeeds; DELETE/DDL rejected by re-validation before connector.execute (assert via a connector that records calls, or assert Err type)
    - Malformed SQL → Err, no panic
    - PMAT: SqlCodeExecutor::execute + helpers each cognitive complexity <= 25
  </acceptance_criteria>
  <done>SqlCodeExecutor implements CodeExecutor over the single-method SqlConnector, re-validating policy before execution; read-only SELECT works, DELETE/DDL rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make register_code_mode_tools register validate_code + execute_code with static policy</name>
  <files>crates/pmcp-server-toolkit/src/code_mode.rs, crates/pmcp-server-toolkit/tests/code_mode_tools.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/code_mode.rs (register_code_mode_tools current stub lines 176-192 — replace the deferred-Plan-08 comment with a real body)
    - crates/pmcp-server-toolkit/src/builder_ext.rs (lines 180-220 — code_mode_from_config / try_code_mode_from_config delegate here; the SIGNATURE of register_code_mode_tools must stay `(ServerBuilder, &ServerConfig) -> Result<ServerBuilder>` so all callers compile unchanged)
    - crates/pmcp-code-mode/src/handler.rs (CodeModeToolBuilder::{new,build_validate_tool,build_execute_tool}; the CodeModeHandler/CodeModeToolBuilder wiring used by the derive macro)
    - crates/pmcp-code-mode-derive/src/lib.rs (what #[derive(CodeMode)] generates — `.tool("validate_code"...).tool("execute_code"...)`; decide reuse-derive-macro vs hand-write per RESEARCH A2)
    - crates/pmcp-server-toolkit/tests/code_mode_wiring.rs (lines 44-184 — existing pipeline/policy tests; the new test extends this surface)
  </read_first>
  <behavior>
    - Test: after register_code_mode_tools on a config WITH [code_mode], the built Server exposes get_tool("validate_code").is_some() AND get_tool("execute_code").is_some()
    - Test: a config WITHOUT [code_mode] registers neither (no-op preserved)
    - Test: validate_code on "DELETE FROM Artist" under allow_deletes=false reports a policy failure (does NOT issue an approval token)
    - Test: validate_code on a read-only SELECT under the read-only policy succeeds
    - Test: R9 inline-secret rejection still fires (no regression) for an inline literal token_secret
  </behavior>
  <action>
    Replace the shape-preserving body of `register_code_mode_tools` (code_mode.rs:176-192) with real registration. Keep the SIGNATURE `(builder: pmcp::ServerBuilder, config: &ServerConfig) -> Result<pmcp::ServerBuilder>` (builder_ext callers must compile untouched). When `config.code_mode.is_none()` keep the no-op return. Otherwise: reuse `validation_pipeline_from_config` (R9 gate), `build_cm_config`, and `resolve_token_secret` to assemble the code-mode machinery, then register `validate_code` + `execute_code` tools. Decide between the two RESEARCH A2 paths and document the choice in the SUMMARY: (A) wire a toolkit `SqlCodeModeServer` struct with `#[derive(pmcp_code_mode_derive::CodeMode)]` (mirrors production, fastest if it composes with the config-driven signature) holding `code_mode_config`, `token_secret`, `policy_evaluator: Arc<dyn PolicyEvaluator>` (use `NoopPolicyEvaluator` when no AVP/Cedar configured — static enforcement per D-13), `code_executor: Arc<SqlCodeExecutor>`; OR (B) hand-build the two `ToolInfo`s via `CodeModeToolBuilder::new("sql")` + handlers driving `ValidationPipeline` + the executor, registered via `tool_arc`. NOTE the connector dependency: the executor needs `Arc<dyn SqlConnector>`. Since `register_code_mode_tools` takes only `&ServerConfig`, EITHER (i) add an additive `register_code_mode_tools_with_executor(builder, config, Arc<SqlCodeExecutor>)` variant that the binary calls (and keep the connector-less signature as a validate-pipeline-only no-tool path), OR (ii) follow the Phase 83-06 R1 split precedent (validation_pipeline_from_config + code_mode_tools_from_executor) and expose a `code_mode_tools_from_executor(builder, config, executor) -> Result<builder>`. Prefer the split that lets builder_ext's `try_code_mode_from_config` register the validate/execute tools when a connector is available — match the Phase 83-06 R1 decision recorded in STATE.md. Enforce static policy: allow_writes/allow_deletes/allow_ddl from `[code_mode]` flow through the pipeline so DELETE/DDL are rejected (SC-3). Create `tests/code_mode_tools.rs` with the five behaviors; build a real Server via `Server::builder().try_tools_from_config(...).<register>...build()` and assert via `Server::get_tool(name)` (the BLDR-03 accessor). This is the SHAP-A-01 / SC-3 anchor.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features "code-mode sqlite" --test code_mode_tools -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-server-toolkit --test code_mode_tools` passes; get_tool("validate_code") AND get_tool("execute_code") both Some after registration
    - Config without [code_mode] registers neither tool (no-op preserved)
    - validate_code on DELETE/DDL under read-only policy reports failure / issues no token
    - `cargo test -p pmcp-server-toolkit --test code_mode_wiring` still green (R9 + pipeline tests not regressed)
    - The exact registration approach (derive-macro vs hand-write; signature variant chosen) is recorded in the SUMMARY
  </acceptance_criteria>
  <done>register_code_mode_tools (or the executor-aware variant) registers validate_code + execute_code; static policy rejects DELETE/DDL; no-op when [code_mode] absent; R9 preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add assemble_code_mode_prompt_with_schema (file-based prompt seam, no live introspection)</name>
  <files>crates/pmcp-server-toolkit/src/code_mode.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/code_mode.rs (lines 365-420 — assemble_code_mode_prompt + build_code_mode_prompt alias; the ADD mirrors this but takes schema_text: &str instead of calling connector.schema_text(); reuse format_curated_tables + dialect.name()/placeholder_guidance())
    - crates/pmcp-server-toolkit/src/sql/mod.rs (Dialect enum + its name()/placeholder_guidance() methods — the helper takes Dialect directly so non-SQLite backends never trigger a live schema_text() round-trip, preserving SC-1)
    - 85-RESEARCH.md §Pattern 3 (option A recommended: additive pure-string overload; the --schema file content REPLACES live schema_text)
    - 85-CONTEXT.md D-04/D-05 (the file is the default + only path; what's in the file IS what the client sees — redaction guarantee)
  </read_first>
  <behavior>
    - Test: assemble_code_mode_prompt_with_schema("CREATE TABLE Artist(...)", Dialect::Sqlite, &cfg) returns a string containing "# Code Mode", the dialect name, the schema text verbatim, and curated table descriptions from cfg
    - Test: the helper makes NO async call (it is a sync fn — proves it cannot trigger live schema_text)
    - Test: an empty schema_text produces a valid (non-panicking) prompt with the header still present
  </behavior>
  <action>
    Add `pub fn assemble_code_mode_prompt_with_schema(schema_text: &str, dialect: Dialect, config: &ServerConfig) -> String` to code_mode.rs, mirroring the body of `assemble_code_mode_prompt` (lines 365-389) EXCEPT: it is a sync fn (no async, no connector), takes the dialect + schema text as params, and uses the passed `schema_text` instead of `connector.schema_text().await`. Reuse `format_curated_tables(config)` and `dialect.name()` / `dialect.placeholder_guidance()`. This is the D-05 seam — the `--schema` file content becomes the prompt body, and because it never calls a connector it works for lazy non-SQLite backends (SC-1). Add a doc comment explaining it is the file-based counterpart to the connector-based `assemble_code_mode_prompt` (no SATD). Add inline unit tests for the three behaviors. Keep cog <= 25 (it should be a near-copy, trivially under). The original `assemble_code_mode_prompt` / `build_code_mode_prompt` stay unchanged (additive).
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features "code-mode sqlite" assemble_code_mode_prompt_with_schema -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-server-toolkit assemble_code_mode_prompt_with_schema` passes
    - Source assertion: `grep -n "pub fn assemble_code_mode_prompt_with_schema" crates/pmcp-server-toolkit/src/code_mode.rs` matches; the signature is sync (no `async`)
    - The output contains the verbatim schema_text + dialect header + curated tables
    - The original `assemble_code_mode_prompt` / `build_code_mode_prompt` are unchanged (additive)
  </acceptance_criteria>
  <done>A sync file-based prompt assembler exists, reusing the curated-table + dialect formatting, never calling a connector — the --schema redaction seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-generated SQL (validate_code/execute_code) → SQL backend | untrusted code crosses into the connector; policy must gate it |
| approval token (validate_code → execute_code) | HMAC token binds code hash; replay with modified SQL must be caught |
| --schema file → public code-mode prompt | admin-redacted file is the source of truth; no live introspection bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02-01 | Tampering | SqlCodeExecutor::execute | mitigate | Defense-in-depth re-validation of the SQL against `[code_mode]` policy BEFORE connector.execute — guards token replay with a different statement (mirrors production SqlCodeModeHandler) |
| T-85-02-02 | Elevation of privilege | static `[code_mode]` policy | mitigate | allow_writes/allow_deletes/allow_ddl enforced statically from config (D-13); DELETE/DDL on read-only config rejected by validation pipeline — a config-driven server CANNOT bypass guards (SC-3 claim) |
| T-85-02-03 | Spoofing | HMAC approval token | mitigate | Token bound to code hash via `HmacTokenGenerator` (never hand-rolled); secret resolved via `resolve_token_secret` (>=16 bytes, T-85-01-01) |
| T-85-02-04 | Information disclosure | ConnectorError → ExecutionError mapping | mitigate | Backend error messages sanitized before surfacing (V7, T-84-04-02 precedent) — no raw internals echoed to the client |
| T-85-02-05 | Information disclosure | --schema → prompt body | mitigate | The file-based assembler (no connector.schema_text) means only the admin-redacted file content reaches the prompt; live DDL never leaks (D-04/D-05) |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features "code-mode sqlite" -- --test-threads=1` green
- register_code_mode_tools signature unchanged for existing callers; any new variant is additive
</verification>

<success_criteria>
- validate_code + execute_code are registered and reachable via get_tool (SHAP-A-01)
- Static [code_mode] policy enforced — DELETE/DDL rejected on read-only config (SC-3)
- SqlCodeExecutor bridges the single-method SqlConnector with defense-in-depth re-validation
- The --schema file seeds the code-mode prompt without live introspection (D-05, SC-1 prerequisite)
</success_criteria>

<output>
After completion, create `.planning/phases/85-shape-a-pure-config-binary-reference-parity/85-02-SUMMARY.md`
</output>
