---
phase: 122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - crates/pmcp-package/deny.toml
  - cargo-pmcp/tests/package_capture_contract.rs
autonomous: true
requirements: [PKGX-01]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`make quality-gate` executes `cargo-pmcp/tests/package_capture_contract.rs`, `cargo-pmcp/tests/package_inspect.rs` and `cargo-pmcp/tests/pmcp_package_pin.rs` and fails if any of the three passes ZERO tests — measured by `scripts/named-test-binary-count.awk`, not by a substring match on cargo output."
    - "A crate that is not on `crates/pmcp-package/deny.toml`'s `[bans].allow` list fails `make no-crypto-check`, demonstrated by a recorded negative-control run that adds a signing crate and observes RED."
    - "`make no-crypto-check` fails closed on unreadable evidence: a missing `crates/pmcp-package/deny.toml` and an emptied `allow` list each abort the gate rather than reporting a vacuous pass."
  prohibitions:
    - statement: "No signing or crypto-protocol crate may enter `pmcp-package`'s RESOLVED dependency graph, and `crates/pmcp-package/src/digest/verify.rs` must stay an integrity check — it must never become a signature check."
      status: flagged
      flagged: true
    - statement: "A gate in this phase must never report success on output it could not read: a missing config, an empty allow list, a truncated cargo run, or a zero-passed named binary must all fail."
      status: flagged
      flagged: true
  artifacts:
    - crates/pmcp-package/deny.toml
    - "Makefile target `test-cargo-pmcp-integration`"
    - "Makefile target `no-crypto-check`"
    - "Makefile variable `PURITY_NO_CRYPTO_CRATES`"
  key_links:
    - "`test-all` -> `test-cargo-pmcp-integration` -> `scripts/named-test-binary-count.awk` (the gate-reach chain that makes SC1's word `blocking` true)"
    - "`quality-gate` -> `no-crypto-check` -> `cargo deny --manifest-path crates/pmcp-package/Cargo.toml check --config deny.toml bans` (the only mechanism that reaches a workspace-EXCLUDED crate)"
---

<objective>
Close the two Wave-0 infrastructure gaps this phase's success criteria silently depend on, BEFORE any attestation code is written.

Gap 1 (measured in `122-RESEARCH.md` Pitfall 1): `cargo-pmcp/tests/*` integration binaries are executed by NO gate in this repo. `make test-cargo-pmcp` runs `cargo test -p cargo-pmcp --lib`; `make test-integration` and CI's test job resolve to the root `pmcp` package; `org-gate-checks.yml`'s `workspace-test` runs `--lib --bins` and is not in `gate.needs`. SC1 requires the new attestation contract test to be *blocking*. Copying the model file literally would ship a criterion that is green while measuring nothing.

Gap 2 (D-12/D-13): the no-crypto boundary (SC4) must be machine-checked over the RESOLVED dependency graph, which an `include_str!`-style manifest tripwire structurally cannot see.

Purpose: make "blocking" and "machine-checked" measurable facts rather than claims, so every later plan in this phase inherits a gate that actually runs.
Output: two new Makefile targets chained into the existing gate, one crate-local cargo-deny allowlist, and one corrected module-doc claim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-CONTEXT.md
@.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-RESEARCH.md
@.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate reach for cargo-pmcp/tests/ — one target, three named binaries, proven by the extractor</name>
  <files>Makefile</files>
  <read_first>
    - `Makefile` lines 396-524 — `test-openapi-server-guard-selftest` (the six-fixture extractor self-test) and `test-openapi-server` (the target to mirror, including the full four-verdict `case` block and every failure message)
    - `Makefile` lines 228-300 — `test-unit`, `test-tester`, `test-cargo-pmcp`, and the comment block at 239-261 explaining WHY the root-scoped targets miss non-root crates
    - `Makefile` line 805 — the `test-all` prerequisite list this new target must join
    - `scripts/named-test-binary-count.awk` — the `-1` / `-2` / `0` verdicts, the ANSI stripping, and why the PASSED count (not `running N`) is the reading
    - `.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-RESEARCH.md` Pitfall 1 — the four-gate table and the measured `-1` readings
  </read_first>
  <action>
Add a new phony Makefile target named `test-cargo-pmcp-integration` that closes the measured hole for `cargo-pmcp/tests/`, and add it to `test-all`'s prerequisite list (`Makefile:805`) immediately after `test-cargo-pmcp`.

Concrete target shape, mirroring `test-openapi-server` (`Makefile:491-523`) rather than inventing a second idiom:

- Declare `test-cargo-pmcp-integration: test-openapi-server-guard-selftest` so the extractor's own six-fixture self-test runs first — the gate and the proof of the gate must not drift, and that self-test target already exists and is already a prerequisite of `test-openapi-server`.
- Capture the run into a shell variable: `out=$$(RUST_LOG=$(RUST_LOG) RUST_BACKTRACE=$(RUST_BACKTRACE) $(CARGO) test -p cargo-pmcp --test package_capture_contract --test package_inspect --test pmcp_package_pin -- --test-threads=1 2>&1)`, capture `status=$$?`, echo `$$out`, and `exit $$status` when nonzero.
- Scope to those three `--test` selectors deliberately, NOT a bare `cargo test -p cargo-pmcp`: this repo's cargo-pmcp deploy/doctor integration suites are known to race without serialization, and the phase's own goal is a narrow, fast, reliable gate. Record that reason in a comment above the target.
- Use `-- --test-threads=1` for the same reason `test-openapi-server` does, and say so in the comment.
- Assert a nonzero summed passed count via `ran=$$(echo "$$out" | awk '/^test result:/ { total += $$4 } END { print total+0 }')` and fail when it is `0` with a message naming that the gate is not reaching this crate.
- Then run the named-binary loop with `REQUIRED_TEST_BINARIES="package_capture_contract package_inspect pmcp_package_pin"`, invoking `awk -v want="tests/$$b.rs" -f scripts/named-test-binary-count.awk` per name and dispatching on the extractor's reading with the SAME four-verdict `case` block `test-openapi-server` uses. Copy all four failure messages verbatim in structure (the `-1` renamed-file cause, the `-2` truncated-output cause, the `0` "RAN but passed ZERO tests" cause including the explanation that the summed total stays nonzero so the count guard cannot catch it, and the non-numeric "extractor produced no usable reading" cause). Each verdict exists for a measured failure class; do not collapse them.
- Add the APPEND-ONLY discipline note that `Makefile:481-484` already states for the sibling list: `package_attestation_contract` is added to `REQUIRED_TEST_BINARIES` by plan 122-04, the plan that CREATES that binary. A name added before its binary exists turns this gate red for every commit in between; removing a name to quiet a red gate deletes the proof instead of fixing it.

Do NOT modify `test-cargo-pmcp` (`Makefile:283-295`) — it covers the lib target and the scaffold-pin tripwires, and this target covers `tests/`. Two targets, two reaches, both in `test-all`.
  </action>
  <acceptance_criteria>
    - `make test-cargo-pmcp-integration` exits 0 and prints a `✓ <name> passed <n> tests` line for each of `package_capture_contract`, `package_inspect`, `pmcp_package_pin`, each with `<n>` greater than zero.
    - `grep -n 'test-cargo-pmcp-integration' Makefile` shows the name on the `test-all:` prerequisite line as well as on the target's own `.PHONY` and rule lines.
    - Negative control, run and recorded in the SUMMARY: temporarily rename `cargo-pmcp/tests/package_inspect.rs` to `package_inspect.rs.bak`, run the target, observe it exit nonzero with the `never RAN` verdict message, then restore the file and observe green again.
    - The target's `case` block contains four distinct arms (`-1`, `-2`, `0`, non-numeric) — verified by reading, and each arm's message names a different cause.
    - `make quality-gate` still reaches this target through `test-all` (confirmed by the target's output appearing in a `make test-all` run).
  </acceptance_criteria>
  <verify>
    <automated>make test-cargo-pmcp-integration</automated>
  </verify>
  <done>The three existing cargo-pmcp integration binaries are executed by `make quality-gate` and each is asserted to have passed a nonzero number of tests; a renamed binary turns the gate red.</done>
</task>

<task type="auto">
  <name>Task 2: No-crypto boundary — a generated cargo-deny allowlist for the workspace-excluded pmcp-package</name>
  <files>crates/pmcp-package/deny.toml, Makefile</files>
  <read_first>
    - `crates/pmcp-workbook-runtime/deny.toml` — the crate-local four-stanza bans-only shape to copy, including the header comment that explains the exact invocation and why the workspace-global config is untouched
    - `Makefile` lines 1080-1101 — Layer 2's WR-02 fail-closed guard, the `{ name = ` parity grep, and the exact `cargo deny --manifest-path <m> check --config deny.toml bans` argument order (cargo-deny 0.18.3 accepts `--config` only AFTER `check`)
    - `Makefile` lines 909-948 — the purity-gate layer descriptions and `PURITY_CRATES`, the sibling list this one parallels
    - `Makefile` lines 1103-1145 — `pmcp-package-gate` (the `--manifest-path` precedent for this workspace-EXCLUDED crate) and the `quality-gate` chain this target must join
    - `crates/pmcp-package/Cargo.toml` — the `[workspace]` table at the top (why root cargo commands cannot reach this crate) and the ten direct dependencies
    - `.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-RESEARCH.md` Pattern 4 and Pitfall 3 — the four measured cargo-deny probes and the instruction to GENERATE the list
  </read_first>
  <action>
Create `crates/pmcp-package/deny.toml` and add a `no-crypto-check` Makefile target that enforces it, realizing D-12 (reuse purity-check Layer 2's mechanism) and D-13 (allowlist, not denylist).

`crates/pmcp-package/deny.toml` — copy `crates/pmcp-workbook-runtime/deny.toml`'s four-stanza skeleton (`[advisories] version = 2, ignore = []`; `[licenses] version = 2, allow = [], confidence-threshold = 0.0`; `[sources] unknown-registry = "allow", unknown-git = "allow"`; `[bans] multiple-versions = "allow", wildcards = "allow"`), then INVERT the ban stanza: replace `deny = [...]` with `allow = [...]`.

GENERATE the allow list from the actual resolved graph; do NOT transcribe a list from any planning document — `122-CONTEXT.md` records 90 packages and `122-RESEARCH.md` measured 89 (with dev-deps) / 59 (without), so any hand-copied list is wrong on the day it is written. Generate with:

  cargo metadata --manifest-path crates/pmcp-package/Cargo.toml --format-version 1

and render one `{ name = "<crate>" }` entry per unique package name, sorted. Use the `{ name = ` spelling (not `{ crate = `) so the entries match the Makefile parity-grep idiom already in the file at `Makefile:1091`; both spellings are accepted by cargo-deny 0.18.3. Then run the gate and let cargo-deny's own `error[not-allowed]` output — which prints the full dependency path per offender — drive any remaining additions.

Set `[graph] exclude-dev = false` (dev-dependencies INCLUDED) and write the reasoning into the config as a comment, because this is the discretion item `122-CONTEXT.md` flags: a dev-dep IS an arrival path for a signing crate, the whole point of an allowlist is catching the crate nobody thought of, and the churn cost when `proptest`/`tempfile` bump is a one-line edit visible in review. State the alternative and why it was rejected, so a later reader does not flip it silently.

Write the hashing-versus-signing distinction into the config as REASONING, not as an implicit fact about which names appear (D-13's explicit requirement). Name the admitted hash-function crates — `sha2`, `digest`, `crypto-common`, `block-buffer`, `generic-array`, `typenum`, `cpufeatures` — state that the canonical manifest digest requires sha256, state that `digest` and `crypto-common` are trait crates for hash functions rather than signature crates, and state that a name-keyed denylist would have false-positived on day one against crates literally named `crypto-common` and `digest` — which is the concrete measured reason this is an allowlist.

Also record in the header what this gate CANNOT see (the repo convention that a mechanism states its blind spots): it checks crate NAMES over the resolved graph, so a signing capability added inside an already-admitted crate under a new cargo feature is invisible to it.

Makefile — add a `PURITY_NO_CRYPTO_CRATES := pmcp-package` variable as a SIBLING of `PURITY_CRATES` (not a member of it: the Makefile enforces byte-identical `[bans]` lists across `PURITY_CRATES` members, the workbook crates ban Excel readers, and this is a different boundary), plus a phony `no-crypto-check` target that:

- runs under `set -euo pipefail`;
- fails closed when `crates/$$crate/deny.toml` is absent, with the WR-02 reasoning restated: cargo-deny 0.18.3 WARNs rather than fails on a missing `--config` path and would report a vacuous pass;
- fails closed when the config has no non-empty `allow` list — cargo-deny reports success for an EMPTY allow list exactly as vacuously as for a missing file, and only a non-empty `allow` makes the check deny-by-default;
- notes in a comment that the parity loop `PURITY_CRATES` runs is DEGENERATE for a one-crate list and is therefore deliberately omitted here rather than shipped as a loop comparing a list to itself;
- invokes `cargo deny --manifest-path crates/$$crate/Cargo.toml check --config deny.toml bans` in exactly that argument order.

Chain `no-crypto-check` into `quality-gate` (`Makefile:1116-1141`) alongside `purity-check` and `pmcp-package-gate`.
  </action>
  <acceptance_criteria>
    - `make no-crypto-check` exits 0 against the current tree and its output includes a cargo-deny `bans ok` summary for `pmcp-package`.
    - `crates/pmcp-package/deny.toml` contains a `[bans]` stanza whose `allow` array is non-empty, and a `[graph]` stanza setting `exclude-dev` explicitly with an adjacent comment giving the reason for the value chosen.
    - The file's header comment names each admitted hash-function crate and states why hashing is admitted while signing is not, and states what the gate cannot see.
    - Fail-closed control 1: `mv crates/pmcp-package/deny.toml /tmp/ && make no-crypto-check` exits nonzero with a message naming the missing config; restore and re-run green.
    - Fail-closed control 2: temporarily empty the `allow` array, run `make no-crypto-check`, observe nonzero exit with a message naming the empty allow list; restore and re-run green.
    - Negative control (the SC4 proof, recorded in the SUMMARY with its command and output): temporarily add a signing crate — `ed25519-dalek = "2"` — to `crates/pmcp-package/Cargo.toml` `[dependencies]`, run `make no-crypto-check`, and observe a nonzero exit whose output contains `not-allowed` and prints the dependency path. Then revert the manifest and confirm green.
    - `grep -n 'no-crypto-check' Makefile` shows the name inside the `quality-gate` recipe.
  </acceptance_criteria>
  <verify>
    <automated>make no-crypto-check</automated>
  </verify>
  <done>`pmcp-package`'s dependency graph is allowlisted; an unlisted crate — including one arriving transitively or through a dev-dependency — fails `make quality-gate`, and the gate fails closed on a missing or emptied config.</done>
  <precondition>`cargo deny --version` reports 0.18.3 (the CI pin at `.github/workflows/ci.yml:269`); an older CLI rejects `--config` after `check` and a newer one may change `[graph]` key validation.</precondition>
</task>

<task type="auto">
  <name>Task 3: Correct the model contract test's factually stale gate claim</name>
  <files>cargo-pmcp/tests/package_capture_contract.rs</files>
  <read_first>
    - `cargo-pmcp/tests/package_capture_contract.rs` lines 1-30 — the module docs whose gate claim was measured false, and the `schema()` loader the new sibling test will mirror
    - `.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-PATTERNS.md` section `cargo-pmcp/tests/package_attestation_contract.rs` — the "Correction to make when copying the docs" note
  </read_first>
  <action>
The module docs of `cargo-pmcp/tests/package_capture_contract.rs` assert that this file runs in the normal cargo test workspace gate. `122-RESEARCH.md` measured that claim false: before Task 1 of this plan, `scripts/named-test-binary-count.awk` reported `-1` (never RAN) for this binary under every candidate gate.

Rewrite that sentence so it names the SPECIFIC mechanism that now makes it true rather than gesturing at "the normal gate": say that the file is executed by the `test-cargo-pmcp-integration` Makefile target, that the target is chained into `test-all` and therefore into `make quality-gate`, and that the target asserts a nonzero passed count for this binary by name via `scripts/named-test-binary-count.awk`.

Add one sentence recording the history, because this file is the model `122-04` copies and the next reader must not re-inherit the false claim: state that this binary was reachable by no gate until Phase 122 added that target, and that a bare `cargo test -p cargo-pmcp --lib` still does not select it.

Change nothing else in the file — no test bodies, no assertions, no `SDL_PATH`.
  </action>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test package_capture_contract` still reports 3 passed, 0 failed.
    - The module docs name `test-cargo-pmcp-integration` and `scripts/named-test-binary-count.awk` by their exact spellings.
    - `git diff --stat cargo-pmcp/tests/package_capture_contract.rs` shows changes only within the leading `//!` block; the diff touches no line at or below the `use apollo_compiler::` import.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cargo-pmcp --test package_capture_contract</automated>
  </verify>
  <done>The model file's gate claim is true and names the mechanism that makes it true, so the sibling attestation contract test can copy it without inheriting a falsehood.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io -> `pmcp-package`'s resolved dependency graph | Third-party code enters the format crate's trust boundary transitively and through dev-dependencies; nothing in the compilation unit can observe this |
| CI/dev shell -> gate verdict | The gate reads cargo's textual output; unreadable or partial output must not be interpreted as success |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-03 | Elevation of Privilege | `pmcp-package` dependency graph | high | mitigate | `[bans].allow` allowlist evaluated over the RESOLVED graph with `exclude-dev = false`; an `include_str!` manifest tripwire structurally cannot see transitive arrivals (D-12/D-13) |
| T-122-04 | Repudiation | a gate that claims to be blocking but runs nothing | high | mitigate | `REQUIRED_TEST_BINARIES` + `scripts/named-test-binary-count.awk` per-binary passed-count assertion with four distinct fail-closed verdicts; proven by a rename negative control |
| T-122-10 | Tampering | `crates/pmcp-package/deny.toml` deleted, renamed, or emptied | high | mitigate | Two fail-closed guards: `test -f` on the config (WR-02) and a non-empty-`allow` grep, each exercised by its own control run |
| T-122-11 | Information Disclosure | cargo-deny offender output in CI logs | low | accept | The output names crate names and dependency paths only — public crates.io metadata, no secret material |
| T-122-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs ZERO external packages (`122-RESEARCH.md` Package Legitimacy Audit); the D-13 allowlist makes any future arrival a gate failure, and the negative control proves it |
</threat_model>

<verification>
- `make test-cargo-pmcp-integration` green, with a nonzero named count for all three binaries.
- `make no-crypto-check` green; both fail-closed controls and the `ed25519-dalek` negative control run and recorded.
- `make quality-gate` green end to end (mandatory before any commit per CLAUDE.md).
</verification>

<success_criteria>
- SC1's word "blocking" is now measurable: a named cargo-pmcp integration binary that stops running turns `make quality-gate` red.
- SC4's no-crypto boundary is machine-checked over the resolved graph, not stated.
- The model contract test no longer carries a measured-false claim for `122-04` to copy.
</success_criteria>

## Artifacts this phase produces

Symbols created or first introduced by THIS plan (excluded from drift verification — they do not exist on `main`):

- `Makefile` target `test-cargo-pmcp-integration` (phony)
- `Makefile` variable `PURITY_NO_CRYPTO_CRATES`
- `Makefile` target `no-crypto-check` (phony)
- File `crates/pmcp-package/deny.toml`
- The `REQUIRED_TEST_BINARIES` value `"package_capture_contract package_inspect pmcp_package_pin"` (extended by 122-04 with `package_attestation_contract`)

Symbols created elsewhere in Phase 122 and referenced here only by name: `cargo-pmcp/tests/package_attestation_contract.rs` (created by 122-04).

<output>
Create `.planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-01-SUMMARY.md` when done. Record the three negative-control runs (renamed binary, missing config, emptied allow list) and the `ed25519-dalek` control with their exact commands and observed exit codes.
</output>
