---
phase: 123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba
plan: 03
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - cargo-pmcp/src/commands/package/render.rs
  - cargo-pmcp/src/commands/package/load.rs
  - cargo-pmcp/src/commands/package/save.rs
  - cargo-pmcp/src/commands/package/mod.rs
  - cargo-pmcp/tests/package_save_load.rs
autonomous: true
requirements: [PKGX-02]

estimate:
  tokens: 37500
  raw_tokens: 75000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`load` prints the slots the target environment must fill, enumerated by `required_slots()` — each slot rendered with its ENVIRONMENT VARIABLE name and, separately, the dotted CONFIG PATH the resolved value is written to. The two are never the same string and are never conflated (SC1)."
    - "`load` renders package-internal pin facts for every component reference: declared range plus resolved version and digest, or pinned-directly, or a range whose `resolved_from` is absent. An absent `resolved_from` renders as CANNOT REPORT and never as NO SKEW (D-14, and `reference.rs:93-97`'s explicit consumer obligation)."
    - "`load`'s output states plainly that comparing these pins against what a target environment already runs is `import`'s job, platform-side (D-14) — nothing rendered is derived from anything but the package itself."
    - "On an attestation whose claimed subject does not name this package, `load` writes the layout, renders issuer / claimed subject / actual unattested digest side by side, and exits 1 — the render happening BEFORE the failure and OUTSIDE the quiet gate, mirroring `inspect` (D-15 / 122 D-06)."
    - "Integrity failure and subject mismatch stay visibly different verdicts: corrupt bytes fail closed inside `unpack_*` and write nothing; a well-formed package with a false claim writes, reports and exits 1. These two behaviours are not harmonized (122 D-03 / D-15)."
    - "`load` and `pull` share ONE renderer, so the two verbs cannot drift into two output formats, and there is no `--format json` anywhere in the package verb group (D-16)."
    - "`load`'s rendered output is DETERMINISTIC: `required_slots` is ordered by slot key with a stable sort that preserves equal-keyed duplicates, and component pin facts are rendered in a specified, stable order — so two runs over one package produce byte-identical stdout."
    - "`save` refuses a non-server package kind by name, saying that kind is not yet supported, while `load` handles every kind `detect_kind` recognizes (D-13)."
    - "`save` treats a `.pmcp/deploy.toml` that does not parse as a `DeployDescriptor` as a HARD error naming the unmodelled table — deliberately diverging from `load_deploy_descriptor`'s other callers, who treat it as a graceful legacy-deploy fallback (Pitfall 6)."
  prohibitions:
    - statement: "No `--format json` or any second output format may be added to `save`, `load` or `pull` — `inspect.rs:28`'s principle is that a reader never has to learn two output formats (D-16)."
      status: flagged
      flagged: true
    - statement: "An absent `PinnedRef.resolved_from` must never be rendered, summarized or counted as evidence of no skew — reading an absent fact as a positive claim is precisely the failure that field exists to prevent."
      status: flagged
      flagged: true
    - statement: "The subject-mismatch verdict must not be collapsed into, or harmonized with, an integrity failure in any later cleanup."
      status: flagged
      flagged: true
    - statement: "The subject-mismatch exit must not be placed inside the `should_output()` gate — a mismatch that went silent under `--quiet` would be a hole in exactly the automated context that needs the check most."
      status: flagged
      flagged: true
    - statement: "`load` must not compare a package's pins against any environment state — that comparison is `import`'s job, platform-side, and needs deployed-state knowledge nothing offline has (D-14)."
      status: flagged
      flagged: true
  artifacts:
    - cargo-pmcp/src/commands/package/render.rs
    - "`--kind` flag on `cargo pmcp package save`"
  key_links:
    - "`required_slots` (`slot/required.rs:100`) -> `render.rs` — the enumerator SC1's 'prints the slots' means; `detect_deviation` is NOT the enumerator (PKG-04's correction)"
    - "`ComponentRef` / `PinnedRef.resolved_from` (`reference.rs:56-64,150-157`) -> `render.rs`'s three-state pin report — the field Phase 122 D-10 added and deferred to this phase"
    - "`SubjectVerdict::matches()` (`unpack.rs:181-183`) -> `load`'s exit-1 gate — the CLI never re-derives a digest comparison of its own"
    - "`render.rs` -> `load.rs` and (plan 05) `pull.rs` — one renderer, so D-16 holds across both verbs by construction"
---

<objective>
Expand out from plan 01's proven slice: give `load` the report it exists to produce, and give `save`
the two refusals that keep it honest about its scope.

Plan 01's tracer proved the path — `save` writes a tar, `load` reads it back into a layout `inspect`
opens. What it deliberately did not do is REPORT. SC1's "prints the slots the target environment must
fill" and D-14's pin-fact rendering are the artifact the platform needs to see to close their half of
PKGX-02, and 122 added `PinnedRef.resolved_from` specifically for this phase to consume.

Purpose: make `load` useful to the human operator who has to fill env vars in a target environment,
without inventing a single fact that is not derivable offline from the package alone.
Output: `render.rs` (shared by `load` now and `pull` in plan 05, so D-16 holds by construction), the
D-15 subject-mismatch verdict, and `save`'s kind guard plus its deliberate hard-error divergence.

**Scope note carried from D-14, so it is not rediscovered mid-task:** `ServerPackage` has NO
`ComponentRef` field at all (`name`, `version`, `digest`, `deploy`, `policies`, `tools`,
`config_slots`). Ranges and pins live only on team and workflow packages, so pin-fact reporting fires
only on a `load` of THOSE kinds — which D-13's kind-agnostic read path already covers. Do not go
looking for a component list on a server package; there is none.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-CONTEXT.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-RESEARCH.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-PATTERNS.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-01-SUMMARY.md
@CLAUDE.md
</context>

<!-- planner-discipline-allow: --format -->
<!-- planner-discipline-allow: format.*json -->
<!-- planner-discipline-allow: detect_deviation -->

> **Why the allowances above.** Task 1's acceptance criteria negative-grep the source for `--format`
> and for a JSON-format flag, and bound `detect_deviation` to at most one occurrence. All three
> literals must appear in the task action, because the action's job is to tell the executor exactly
> which flag must not exist (D-16) and exactly which function is deliberately NOT the enumerator
> (PKG-04's correction). The single permitted `detect_deviation` occurrence is the rustdoc that records
> why it is unused — that is the intended state, not an accident, and the count bound is what keeps a
> future reader from re-introducing it as a call.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `render.rs` — the one human-text renderer `load` and `pull` both use</name>
  <files>cargo-pmcp/src/commands/package/render.rs, cargo-pmcp/src/commands/package/mod.rs</files>
  <read_first>
    - `cargo-pmcp/src/commands/package/inspect.rs` — the rendering half in full: `render_kind` (`:160-203`), `refuse_a_subject_that_does_not_name_this_package` (`:217`), `field` (`:236`), `header` (`:241`), `render_agent` (`:246`), `render_team` (`:256`), `render_server` (`:270`), `render_attestation` (`:298`), `render_workflow` (`:326`), `truncate` (`:335`); and the module header's one-output-format principle at `:28`
    - `crates/pmcp-package/src/slot/required.rs:25-110` — `RequiredSlot`'s three fields, `SlotClass`, `required_slots`'s ordering and duplicate-preserving contract, and the rustdoc at `:70-74` insisting that a slot's `name` is the ENVIRONMENT VARIABLE while `config_key` is the CONFIG PATH, and that they are never the same string
    - `crates/pmcp-package/src/reference.rs:50-160` — `PinnedRef`'s five fields, the `resolved_from` rustdoc INCLUDING the consumer obligation at `:93-97` naming this very phase, and the `ComponentRef` enum's two variants
    - `crates/pmcp-package/src/oci/unpack.rs:150-340` — `SubjectVerdict` and its `matches()`, `UnpackedAttestation`'s four fields, `UnpackedServer`'s five fields, `UnpackedTeam`'s two fields, and the rustdoc stating the crate never writes to disk using annotation values
    - `crates/pmcp-package/src/slot/types.rs:185-240` — `ConfigSlot` and `SlotType`
    - `cargo-pmcp/src/commands/package/mod.rs` — as plan 01 left it, for the new `pub mod render;` declaration
  </read_first>
  <behavior>
    - Given a slot set, the renderer prints one line per required slot showing the environment-variable name and, as a separate labelled element, the config path — and a secret slot shows no value, because an identity-bearing slot structurally carries none.
    - Given a `ComponentRef::Range`, output says the range was declared and that it is unresolved.
    - Given a `ComponentRef::Pinned` whose `resolved_from` is `Some(range)`, output shows both the declared range and the resolved version plus digest.
    - Given a `ComponentRef::Pinned` whose `resolved_from` is `None`, output says the component was pinned directly and that the declared range CANNOT BE REPORTED — never that there is no skew.
    - Given no attestation, the carriage rendering says the package is unattested.
    - Given an attestation whose subject matches, the rendering shows issuer and payload type and states the subject matches.
    - Given an attestation whose subject does not match, the rendering shows issuer, claimed subject and actual unattested digest side by side.
    - Rendering the same inputs twice produces byte-identical output.
  </behavior>
  <action>
Create `cargo-pmcp/src/commands/package/render.rs` holding every human-text rendering `load` and (plan
05) `pull` produce, so the two verbs cannot drift into two output formats. Declare it in `mod.rs`.

Do NOT move `inspect.rs`'s renderers into it. `inspect` is shipped, its output is a surface people
already read, and this phase has no mandate to churn it. Instead, match `inspect`'s VISUAL vocabulary —
the same label/value shape its `field` and `header` helpers produce — so a reader moving between
`inspect` and `load` sees one house style. Say that in the module header, and cite `inspect.rs:28`'s
principle as the reason there is no second format here either. There is no `--format json`; D-16 is
explicit, and a machine-readable rendering is a clean follow-on if a consumer ever asks.

**Slot rendering (SC1's "prints the slots").** Take `&[ConfigSlot]`, call `required_slots`, and render
each `RequiredSlot`. Two things must not be garbled: the slot's `name` is the environment variable the
target environment sets, and `config_key` is the dotted config path the resolved value is written to —
label them distinctly and never print the config path where the variable name belongs, because a
derived variable built from a config path is one no environment can portably set. Render `class`
(identity-bearing versus behavior-relevant) so an operator can see which entries are credentials.
`config_key` is `Option`; a `None` renders as "fills no config key", not as an empty string.

`required_slots` is the enumerator here — NOT `detect_deviation`. PKG-04's correction is that
`detect_deviation` compares one already-known tested/proposed pair and short-circuits on
identity-bearing slots, so it could never name the credential. The two answer different questions and
this rendering asks the enumeration question. Record that in the function's rustdoc so nobody
"improves" it by switching. (`detect_deviation`'s endpoint-drift rendering was left to discretion;
this plan declines it, on the ground that `load` has exactly one environment in hand — the package's —
and a drift report needs two.)

**Pin-fact rendering (D-14).** Walk a component's `ComponentRef`s and render THREE states, never two:
a `Range` is declared-but-unresolved; a `Pinned` with `resolved_from: Some(range)` shows declared range
alongside resolved version and digest; a `Pinned` with `resolved_from: None` was pinned directly and the
declared range CANNOT BE REPORTED. That third state is not a formatting nicety —
`reference.rs:93-97` names this phase and states the obligation verbatim: treat `None` as "cannot
report" and NEVER as "no skew", because reading an absent fact as a positive claim is precisely the
failure the field exists to prevent. Quote that obligation in the renderer's rustdoc.

End the pin section with a line stating that comparing these pins against what the target environment
actually runs is `import`'s job, platform-side. That is not decoration: it is the boundary D-14 draws,
and an operator who does not see it stated will assume the CLI checked something it cannot check.

**Carriage-state rendering (three states).** Unattested; attested-and-matching; attested-mismatched
with issuer, claimed subject and actual unattested digest side by side. Read the verdict from
`SubjectVerdict::matches()` — never re-derive a digest comparison in the CLI, because a CLI-side copy
would drift from `inspect`'s. Render `claimed` and `issuer` as DATA: they are attacker-controlled
strings from layer annotations, so bound their rendered length the way `inspect`'s `truncate` does and
never use either as a path.

**Determinism.** Every list this module renders must have a specified order. `required_slots` already
guarantees ordering by slot key with a stable sort that preserves equal-keyed duplicates — render in
the order it returns and say so. For component pin facts, sort by component name then type, and state
the tiebreak. Add a unit test asserting that rendering identical inputs twice yields identical strings.

Structure the module so every renderer returns a `String` (or writes into a `&mut String`) rather than
printing directly, with printing left to the caller. That is what makes the determinism test possible
and what lets `load` and `pull` gate printing on `should_output()` without duplicating format logic.
Keep the module free of `clap` and `GlobalFlags` so it stays testable under `cargo test --lib`.
  </action>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --lib` exits 0 with a nonzero passed count and its output names the `render` module's tests.
    - Eight named unit tests exist, one per `behavior` row.
    - The `resolved_from: None` test asserts the rendered string contains a phrase meaning "cannot report" and asserts it does NOT contain any phrase asserting agreement or absence of skew.
    - A slot-rendering test asserts the environment-variable name and the config path appear under DIFFERENT labels, and that a secret slot's rendering contains no value.
    - The determinism test renders the same inputs twice and asserts string equality.
    - `grep -rn 'format.*json\|--format' cargo-pmcp/src/commands/package/render.rs cargo-pmcp/src/commands/package/load.rs cargo-pmcp/src/commands/package/save.rs` returns no matches (D-16).
    - `grep -c 'detect_deviation' cargo-pmcp/src/commands/package/render.rs` returns at most 1, and that occurrence is inside a rustdoc explaining why it is NOT used.
    - `cargo build -p cargo-pmcp --lib` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS= cargo test -p cargo-pmcp --lib 2>&amp;1 | tee /tmp/123-03-t1.log &amp;&amp; test "$(awk '/^test result:/ { t += $4 } END { print t+0 }' /tmp/123-03-t1.log)" -gt 0 &amp;&amp; grep -q 'render' /tmp/123-03-t1.log</automated>
  </verify>
  <done>One renderer produces every human-readable line `load` and `pull` emit; the three pin states are distinct; an absent `resolved_from` reads as "cannot report"; and rendering is deterministic, proven by a test rather than by inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `load` wires the report, and the subject-mismatch verdict exits 1 outside the quiet gate</name>
  <files>cargo-pmcp/src/commands/package/load.rs, cargo-pmcp/tests/package_save_load.rs</files>
  <read_first>
    - `cargo-pmcp/src/commands/package/load.rs` — as plan 01 left it, so the report is wired into real code
    - `cargo-pmcp/src/commands/package/render.rs` — as Task 1 left it
    - `cargo-pmcp/src/commands/package/inspect.rs:150-235` — the `detect_kind` dispatch shape, and specifically the comment at `:183-188` explaining why the subject check sits OUTSIDE the `if output` block and AFTER the rendering; plus `refuse_a_subject_that_does_not_name_this_package` at `:217`
    - `cargo-pmcp/src/commands/package/inspect.rs:36-59` — the module header's integrity-versus-claim rule and its "these two behaviours must NOT be harmonized in a later cleanup" sentence
    - `crates/pmcp-package/src/oci/unpack.rs:620-840` — `unpack_server` and `unpack_team`, and note (State of the Art) that `unpack_team` now returns `UnpackedTeam`, not a bare `TeamPackage`
    - `cargo-pmcp/tests/package_save_load.rs` — as plans 01 left it, for where the new cases go
    - `crates/pmcp-package/tests/golden_fixtures/` — the existing corpus, for a team/workflow package fixture to exercise the pin-fact path
  </read_first>
  <behavior>
    - `load` of a server package prints the kind, name, version, the required slots, and the carriage state; the pin-fact section is absent because a server package carries no component references.
    - `load` of a team package prints pin facts for its components, including the three `resolved_from` states.
    - `load` of a package carrying no attestation prints the unattested carriage state and exits 0.
    - `load` of a package whose attestation subject matches prints the matching state and exits 0.
    - `load` of a package whose attestation subject does NOT match writes the layout, prints issuer / claimed / actual, and exits 1.
    - The same mismatch under `--quiet` still exits 1 and still writes the layout, though it prints nothing.
    - `load` of a tar whose blob bytes are corrupt fails closed inside the unpack step and writes no layout — a visibly different verdict from the mismatch case.
  </behavior>
  <action>
Replace `load`'s tracer-level confirmation output with the full report, and add the D-15 verdict.

**Dispatch.** Reuse `inspect.rs:160-203`'s `detect_kind` match shape rather than inventing a second
one — D-13 makes `load` kind-agnostic essentially for free, and a second dispatch would drift from
`inspect`'s. Each arm calls the matching `unpack_*`, hands the result to `render.rs`, and then runs the
subject check.

**Print gating, exactly as `inspect` does it.** `global_flags.should_output()` gates ONLY the
decorative rendering. Unpacking, the subject check and the exit code run regardless. `inspect.rs`
already states why in a comment worth carrying forward in substance: a mismatch that went silent when
output was suppressed would be a gate hole in exactly the automated context that needs the check most.
Place the subject check OUTSIDE the output gate and AFTER the rendering, so a human at a terminal sees
the full diagnostic before the command fails.

**The two verdicts stay different (D-15 / 122 D-03).** An integrity failure surfaces verbatim from
inside `unpack_*` — the V6 rule, never bypassed and never re-wrapped into something friendlier — and
writes nothing. A subject mismatch writes the layout, renders issuer, claimed subject and actual
unattested digest, and exits 1. Corrupt bytes mean the package is broken; a mismatched subject means the
bytes are fine and the claim is wrong. Write that distinction into `load.rs`'s module header in its own
words, together with the instruction that the two must not be harmonized in a later cleanup — the same
sentence `inspect.rs:44-56` carries, because this is the second place in the codebase where somebody
will be tempted to unify them.

The non-zero exit exists so this is gateable in CI without parsing stdout. Make that the stated reason
in the rustdoc, so nobody replaces it with a warning.

**Reading order.** Verification happens before any write (plan 01's `read_verified`), the layout is
written, and only then is the package unpacked and reported. Do not reorder: the D-15 render needs the
unattested manifest digest that `unpack_*` re-derives from the written layout.

**Tests.** Add the `behavior` cases to `tests/package_save_load.rs`, driving the real binary. For the
mismatch case, construct a package carrying an attestation whose subject names a different digest — the
same shape `inspect`'s own mismatch coverage uses — and assert BOTH the exit code and that the layout
directory exists afterwards with its blobs present. For the `--quiet` variant, assert exit 1, an empty
stdout, and that the layout was still written. For the corrupt-blob case, assert a non-zero exit AND
that no layout directory was created, so the two verdicts are proven different rather than described as
different.
  </action>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test package_save_load -- --test-threads=1` exits 0 with a nonzero passed count.
    - Seven named tests exist, one per `behavior` row.
    - The mismatch test asserts exit code 1 AND that the destination layout directory exists and contains its blobs afterwards.
    - The `--quiet` mismatch test asserts exit code 1, an EMPTY stdout, and a written layout — proving the verdict is outside the output gate.
    - The corrupt-blob test asserts a non-zero exit AND that the destination directory does not exist — proving the integrity verdict is structurally different from the mismatch verdict.
    - `load`'s subject check is called from outside any `should_output()` conditional — verified by reading, and demonstrated by the `--quiet` test.
    - Negative control, run and recorded in the SUMMARY: move the subject check inside the output gate, observe the `--quiet` mismatch test go red, restore it, observe green.
    - `cargo test -p pmcp-openapi-server --test roundtrip_e2e -- --test-threads=1` still exits 0 with a nonzero passed count.
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS= cargo test -p cargo-pmcp --test package_save_load -- --test-threads=1 2>&amp;1 | tee /tmp/123-03-t2.log &amp;&amp; test "$(awk '/^test result:/ { t += $4 } END { print t+0 }' /tmp/123-03-t2.log)" -gt 0</automated>
  </verify>
  <done>`load` prints the slots, the pin facts and the carriage state for every kind `detect_kind` recognizes; a mismatched subject writes, reports and exits 1 even under `--quiet`; and a corrupt blob still fails closed with nothing written.</done>
</task>

<task type="auto">
  <name>Task 3: `save`'s two refusals — non-server kinds, and a deploy descriptor that will not parse</name>
  <files>cargo-pmcp/src/commands/package/save.rs, cargo-pmcp/tests/package_save_load.rs</files>
  <read_first>
    - `cargo-pmcp/src/commands/package/save.rs` — as plan 01 left it, including the `SaveArgs` flag set and the `DeployConfig::load` / `load_deploy_descriptor` call site
    - `cargo-pmcp/src/deployment/stack_routing.rs:85-100` — `load_deploy_descriptor`'s rustdoc, which explicitly calls a parse failure a graceful legacy-deploy fallback and NEVER a hard error. That is the sentence this task diverges from, and a reader who follows the function to its docs will read the opposite instruction
    - `cargo-pmcp/src/deployment/config.rs:905-925` — `DeployConfig::load` and how it sets `project_root`
    - `cargo-pmcp/src/commands/package/kind.rs:20-100` — `PackageKind`'s variants and its `label()`, for naming the refused kind in the message
    - `.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-CONTEXT.md` D-13 — the deliberate asymmetry: reading costs nothing, writing needs per-kind input semantics nobody has designed
  </read_first>
  <action>
Give `save` the two refusals that keep its scope honest, and write down why each one diverges from a
neighbouring convention.

**Refusal 1 — non-server kinds (D-13).** Add a `--kind <KIND>` flag to `SaveArgs`, a `clap` value enum
over the same vocabulary `PackageKind` names, defaulting to the server kind. Any value other than
server exits non-zero with a message naming the requested kind and stating that `save` does not yet
support it. Do not silently accept and mis-pack; do not accept and warn.

The asymmetry with `load` is deliberate and must be stated in `save.rs`'s module docs: `load` is
kind-agnostic essentially for free because it reuses `detect_kind` and handles whatever it is handed,
while `save` needs per-kind INPUT semantics — an on-disk source format for agent, team and workflow
packages — that nobody has designed. Reading costs nothing; writing does not. Name that as a deferred
design question rather than a limitation to be quietly fixed.

**Refusal 2 — an unparseable deploy descriptor (Pitfall 6).** `save`'s `load_deploy_descriptor` failure
is a HARD error, naming the unmodelled table and telling the user to fix `.pmcp/deploy.toml`. Every
other caller in this repo treats that same failure as a graceful legacy-deploy fallback, and the
function's own rustdoc says so — so the divergence has to be documented AT `save`'s call site, not left
for a reader to infer. Write there: this verb produces an artifact whose whole purpose is to be trusted
in another environment, so a deploy target that could not be read is a refusal, never a default. That
is D-10's rule — every field must trace to a file the user maintains — applied to its failure path.

Make the error message actionable: name the file it read, and surface the underlying parse error in the
`anyhow` cause chain so `-v` shows the offending table.

Also confirm and, if plan 01 left it implicit, make explicit that no production path constructs a
`DeployDescriptor` from literals. The test-only helper in `crates/pmcp-openapi-server/tests/` exists to
let a TEST pack without a project on disk; copying it into production would bake a deploy target,
region and memory the user never chose into an artifact whose entire purpose is to be trusted
elsewhere. State that in the call site's comment.

**Tests.** Add to `tests/package_save_load.rs`: a `save --kind` invocation for each non-server kind
asserting a non-zero exit and that the message names that kind; a `save` run against a project whose
`.pmcp/deploy.toml` contains a table the descriptor does not model, asserting a non-zero exit, a message
naming the file, and that no output tar was created; and a `save` run with NO `.pmcp/deploy.toml` at
all, asserting a non-zero exit with a message that distinguishes "missing" from "unparseable". Assert on
the message text, not only on the exit code — the whole point of both refusals is that the user is told
what to fix.
  </action>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test package_save_load -- --test-threads=1` exits 0 with a nonzero passed count.
    - `cargo pmcp package save --kind team ...` exits non-zero and its stderr names the team kind and says it is not supported — asserted by a test, not by a manual run.
    - A `save` against an unparseable deploy descriptor exits non-zero, names `.pmcp/deploy.toml` in the message, and leaves no `--output` file on disk — all three asserted.
    - A `save` against a project with no deploy descriptor at all exits non-zero with a message distinguishable from the unparseable case (the two tests assert different substrings).
    - `cargo-pmcp/src/commands/package/save.rs` contains a comment at the `load_deploy_descriptor` call site explicitly stating that this verb diverges from the function's documented graceful-fallback contract, and why.
    - `grep -c 'minimal_deploy_descriptor' cargo-pmcp/src/` returns 0.
    - `RUSTFLAGS="" make quality-gate` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS= cargo test -p cargo-pmcp --test package_save_load -- --test-threads=1 2>&amp;1 | tee /tmp/123-03-t3.log &amp;&amp; test "$(awk '/^test result:/ { t += $4 } END { print t+0 }' /tmp/123-03-t3.log)" -gt 0</automated>
  </verify>
  <done>`save` refuses a kind it cannot pack by name, refuses an unreadable deploy descriptor instead of defaulting one, and both divergences are written down where the next reader will meet them.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created by THIS PLAN (new to the codebase — not drift):

- `cargo-pmcp/src/commands/package/render.rs` (new module)
- the slot renderer, the three-state pin-fact renderer, and the three-state carriage renderer declared in `render.rs`
- `--kind` CLI flag on `cargo pmcp package save`, plus its `clap` value enum
- the `render` module declaration added to `cargo-pmcp/src/commands/package/mod.rs`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attestation layer annotations (`claimed`, `issuer`, `payload_type`) -> the terminal | Attacker-controlled strings from a package that may be hostile; rendered as bounded DATA, never used as a path or interpreted. |
| `RestoredFile.file_name` -> anything | Attacker-controlled; `load` writes a content-addressed layout and never a named file. |
| package pin facts -> the operator's conclusions | The renderer must not let an ABSENT fact read as a positive claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-21 | Spoofing | a mis-attached attestation (valid bytes, false subject) | high | mitigate | `SubjectVerdict::matches()` drives a render-then-exit-1 verdict placed outside the output gate, so the refusal holds under `--quiet`; proven by a `--quiet` test and a recorded negative control. |
| T-123-22 | Repudiation | an absent `resolved_from` read as evidence of no version skew | high | mitigate | The renderer has THREE pin states, and the `None` state renders as "cannot report" with a test asserting the output contains no agreement phrasing — discharging `reference.rs:93-97`'s stated consumer obligation. |
| T-123-23 | Information Disclosure | rendering a secret slot's value | high | mitigate | Identity-bearing slots structurally carry no value in `SlotType`; the renderer prints the variable NAME and the config PATH only, asserted by a test that the secret slot's rendering contains no value. |
| T-123-24 | Tampering | attacker-controlled annotation strings used as a filesystem path or terminal escape | medium | mitigate | `claimed`/`issuer` are rendered as bounded, truncated data (the `inspect.rs` `truncate` shape) and never joined onto a path. |
| T-123-25 | Tampering | the subject-mismatch verdict silently collapsing into the integrity verdict in a later cleanup | medium | mitigate | The distinction is written into `load.rs`'s module header with an explicit do-not-harmonize instruction, and is proven behaviourally by two tests with OPPOSITE destination-exists assertions. |
| T-123-26 | Denial of Service | an enormous rendered report from a hostile package (thousands of slots or components) | low | accept | Output volume is bounded by package content the operator asked to inspect; per-field truncation limits any single value. Accepted because refusing to render a large-but-valid package would break the verb's purpose. |
</threat_model>

<verification>
- `RUSTFLAGS= cargo test -p cargo-pmcp --lib` — the renderer's unit tests, nonzero passed count.
- `RUSTFLAGS= cargo test -p cargo-pmcp --test package_save_load -- --test-threads=1` — nonzero passed count.
- `cargo test -p pmcp-openapi-server --test roundtrip_e2e -- --test-threads=1` — Phase 121 regression net still green.
- Two recorded negative controls: the subject check moved inside the output gate; the `resolved_from` `None` arm made to render as agreement.
- `RUSTFLAGS="" make quality-gate` exits 0.
</verification>

<success_criteria>
- `load` prints required slots with the environment variable and config path distinctly labelled.
- Three pin states render distinctly and an absent `resolved_from` reads as "cannot report".
- The rendered report states that the environment comparison is `import`'s job, platform-side.
- A subject mismatch writes, reports and exits 1 — including under `--quiet`; a corrupt blob writes nothing.
- `save` refuses a non-server kind and an unreadable deploy descriptor, each by name.
- No second output format exists anywhere in the package verb group.
</success_criteria>

<output>
Create `.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-03-SUMMARY.md` when done.
</output>
