---
phase: 123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba
plan: 05
type: execute
wave: 3
depends_on: ["123-01", "123-02", "123-03"]
files_modified:
  - cargo-pmcp/src/commands/package/pull.rs
  - cargo-pmcp/src/commands/package/mod.rs
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs
  - cargo-pmcp/tests/package_portability_contract.rs
autonomous: true
requirements: [PKGX-02]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`cargo pmcp package pull <ref> --output ./pkg/` lands its WHOLE pipeline — resolve environment, build the GraphQL request, download, re-verify every blob digest and the payload digest locally, write the layout, report — with only the HTTP call behind a seam an offline test feeds a local tar (D-04)."
    - "`pull` resolves its environment through the EXISTING `pmcp_run` seam: `get_api_base_url()`'s `PMCP_API_URL` -> `PMCP_RUN_API_URL` -> `configured_api_base_url()` -> default precedence, `configure`'s resolver, and the TTL'd endpoint-keyed config cache. There is NO second API path (SC3)."
    - "No new base-URL environment variable, no second token cache, and no second `reqwest::Client` construction outside `graphql.rs` — checkable by grep over `cargo-pmcp/src/` (SC3)."
    - "Transport is never trusted: every blob's sha256 and the payload digest are re-derived LOCALLY from the downloaded bytes, in memory, before a single byte is written (§5.1 / D-06)."
    - "A `pull` whose artifact fails verification leaves the destination byte-for-byte as it was found — a tampered artifact never exists on disk in a form `inspect` would open (D-06)."
    - "ANY failure of the pull path is wrapped with a context line naming `getPackageArtifact` as the required pmcp.run capability and its parked status, while the underlying error stays in the `anyhow` cause chain so `-v` still shows the socket error (D-05, SC4)."
    - "The download is BOUNDED by a named byte cap enforced while streaming — the cap refuses on overflow rather than collecting the body and then measuring it, because collecting an over-cap body performs exactly the allocation the cap exists to prevent."
    - "The presigned `downloadUrl` is fetched WITHOUT the pmcp.run `Authorization` header attached, and is never logged or persisted — it is a bearer credential to a different origin."
    - "`pull` and `load` share plan 03's ONE renderer, so the two verbs cannot drift into two output formats (D-16)."
    - "The six pipeline stages are six named functions, so no single `execute` trips the cognitive-complexity gate (C-2, cog <= 25, CI-enforced at PMAT 3.15.0)."
  prohibitions:
    - statement: "No new base-URL environment variable, no second token or config cache, and no second `reqwest::Client` may be introduced anywhere in `cargo-pmcp/src/` — SC3 forbids a second API path and it is checkable by grep."
      status: flagged
      flagged: true
    - statement: "The pmcp.run `Authorization` header must NEVER be attached to the presigned `downloadUrl`, and that URL must never be printed, logged or written to disk."
      status: flagged
      flagged: true
    - statement: "`pull` must not write anything before verification completes — no partial layout, no temp file inside the destination, no `index.json` written ahead of its blobs."
      status: flagged
      flagged: true
    - statement: "No signature check may be added to `pull`. `digest::verify` is and remains an INTEGRITY check; signature verification would require a crypto dependency the milestone's scoping decision forbids and `make no-crypto-check` machine-enforces."
      status: flagged
      flagged: true
    - statement: "The response body must not be collected before it is measured — a Content-Length header is an early-refusal optimisation and never the authority on how many bytes will arrive."
      status: flagged
      flagged: true
    - statement: "`pull` must not implement, imply or leave a hook for an upload direction — `export`/`push` are retired (D-01)."
      status: flagged
      flagged: true
  artifacts:
    - cargo-pmcp/src/commands/package/pull.rs
    - "`PackageCommand::Pull` variant in `cargo-pmcp/src/commands/package/mod.rs`"
    - "the transport-seam trait/function `pull` calls, and its offline test double"
    - "the `getPackageArtifact` transport function in `cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs`"
  key_links:
    - "`pull` -> `auth::get_credentials()` -> `get_api_base_url()` (`auth.rs:113-124`) -> `configured_api_base_url()` (`auth.rs:149`) -> `configure`'s `resolve_active_target_name` (`resolver.rs:137`) — SC3's reuse-and-prove chain, already built"
    - "`pull` -> `get_package_artifact_request_body` (plan 02) -> TRANSPORT SEAM -> `decode_get_package_artifact_response` (plan 02) — the pure codec on both sides of the one impure step"
    - "TRANSPORT SEAM -> `artifact::read_verified` (plan 01) -> `artifact::write_layout` -> `detect_kind` -> `unpack_*` -> `render.rs` (plan 03) — the verification-then-write-then-report tail `load` already proves"
    - "`graphql.rs`'s `CLIENT: OnceLock<reqwest::Client>` (`:481-482`) and `GRAPHQL_AUTH_HEADER` (`graphql_contract.rs:94`) — the shared client and the raw-token header shape, reused not rebuilt"
---

<objective>
Land `pull` — the remote leg of PKGX-02 — as a COMPLETE pipeline with exactly one seam, so that when
the platform ships `getPackageArtifact` the unparking is deleting a gate rather than writing the
security-relevant half.

D-04 is explicit about why this is not a stub: the verification half is the security-relevant part
(§5.1's *transport is never trusted*) and it is fully testable with no backend. A contract-plus-error
stub would mean that when the backend ships, unparking is WRITING the verification path — the exact
anti-pattern Phase 122 established the discipline against.

This plan joins the two Wave-1 halves. Plan 01 built the reader and the layout writer; plan 02 built
the operation string and its pure codec; plan 03 built the renderer. `pull` is: resolve environment
(reusing the seam SC3 names, which already exists) -> build request (pure) -> ONE impure transport step
-> verify in memory -> write -> report. Everything except that one step already runs offline.

Purpose: prove SC3 by reuse rather than by construction, and prove SC4's named failure by wrapping every
failure of the pull path with the missing capability's name while keeping the cause chain intact.
Output: `pull.rs` decomposed into six named stages, the transport seam plus its offline double, the
`graphql.rs` transport function, and the offline tests that drive the whole pipeline from a local tar.

`export` is retired (D-01): there is no upload direction here, and no hook left for one. The shipped
`import` verb is untouched (D-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-CONTEXT.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-RESEARCH.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-PATTERNS.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-01-SUMMARY.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-02-SUMMARY.md
@.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The six-stage pipeline and the one transport seam</name>
  <files>cargo-pmcp/src/commands/package/pull.rs, cargo-pmcp/src/commands/package/mod.rs, cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs</files>
  <read_first>
    - `cargo-pmcp/src/commands/package/capture.rs` — the WHOLE file: the remote-verb shape `pull` copies. Its imports (`:6-15`), its `execute` entry with `get_credentials().await.context("Not authenticated. Run: cargo pmcp login")` (`:74-79`), its named bounded-operation constants with rustdocs explaining the numbers (`:20-32`), and its decomposition into `execute` / `submit` / poll / report / failure-handler — the model for C-2 compliance
    - `cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs:45-60` — `DEFAULT_API_URL` and `CONFIG_CACHE_DURATION_SECS`
    - `cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs:110-230` — `get_api_base_url`'s four-step precedence, `configured_api_base_url`'s route through `configure`'s resolver, and `load_cached_config`'s endpoint-keyed TTL check. This is SC3's subject and it is ALREADY BUILT: reuse it, do not re-derive it
    - `cargo-pmcp/src/deployment/targets/pmcp_run/auth.rs:510-530` — `get_credentials`
    - `cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs:160-180` — the connect/read timeout discipline, and `:435-500` — `execute_graphql`, its stale-endpoint refresh-and-retry-once behaviour, `execute_graphql_at`, the process-wide `OnceLock` client, and the raw-token `GRAPHQL_AUTH_HEADER` usage with its comment
    - `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs` — as plan 02 left it: `GET_PACKAGE_ARTIFACT_QUERY`, `GetPackageArtifactOutcome` and its download-URL-is-a-credential rustdoc, the request builder and the response decoder
    - `cargo-pmcp/src/commands/package/artifact.rs` — as plan 01 left it: `read_verified`, `write_layout`, and the byte caps
    - `cargo-pmcp/src/commands/package/load.rs` and `render.rs` — as plans 01/03 left them, for the verify-write-report tail `pull` reuses rather than reimplements
    - `cargo-pmcp/src/commands/package/mod.rs:60-100` — the `name@version` reference splitter that splits on the LAST `@`
  </read_first>
  <behavior>
    - Given a fake transport returning a conformant tar, `pull` writes a layout the same shape `load` writes and prints the same report.
    - Given a fake transport returning a tar with one corrupted blob, `pull` returns an error and the destination does not exist afterwards.
    - Given a fake transport returning a payload whose digest does not match the `payloadDigest` the response declared, `pull` returns an error and writes nothing.
    - Given a fake transport returning a body larger than the download cap, `pull` refuses naming the cap and does not allocate the whole body.
    - Given a fake transport that errors, `pull` returns an error whose top-level message names the missing pmcp.run capability and whose cause chain still contains the transport error.
    - Given a destination that already exists, `pull` refuses unless `--force`, before any network call is attempted.
    - Given an empty reference, `pull` refuses before any network call is attempted.
  </behavior>
  <action>
Create `cargo-pmcp/src/commands/package/pull.rs` and wire it as a third new `PackageCommand` variant.

**Decompose into six named functions — this is a hard constraint, not a style note.** CI runs
`pmat quality-gate --fail-on-violation --checks complexity` at cognitive complexity 25, and a single
`execute` doing resolve, request, transport, verify, write and report will trip it. One function per
stage, exactly as `capture.rs` already models with its `execute`/`submit`/poll/report split. Name them
for their stage so a stack trace reads as the pipeline.

**Stage 1 — resolve the environment, by REUSE. SC3 is a reuse-and-prove criterion, not a build
criterion.** Call `auth::get_credentials()` with the same not-authenticated context message
`capture.rs` uses. Do NOT read any environment variable for a base URL, do NOT add a constant, do NOT
construct a cache path: `get_api_base_url()` already implements the precedence SC3 names and
`configured_api_base_url()` already routes through `configure`'s resolver. Write into `pull.rs`'s
module docs that this stage adds nothing and why — that is the criterion being satisfied by
construction, and a future contributor who "adds a `PULL_API_URL` for convenience" breaks it.

**Stage 2 — build the request, purely.** Call plan 02's `get_package_artifact_request_body` with the
reference. Split a `name@version` reference with the existing splitter in `mod.rs` if the reference
needs decomposition; do not write a second parser. Refuse an empty reference here, before any network
work.

**Stage 3 — the transport seam (D-04, rated `costly`).** Define ONE narrow seam and put both impure
steps behind it: POST the GraphQL operation, then GET the returned `downloadUrl`. Return the artifact
bytes plus the declared `payloadDigest`. Shape it as a trait with an async method (or an async function
pointer / boxed closure) so an offline test can substitute a double that returns bytes read from a
local file — that substitution is the entire testability argument for this plan, and it is why the seam
is the LAST thing to be wired live.

Keep the seam's surface minimal: it takes a reference and credentials, it returns bytes and a declared
digest, and it knows nothing about layouts, verification or rendering. A wide seam constrains the live
wiring later; D-04 already flags the shape as `costly` for that reason.

Implement the live side in `graphql.rs`, beside the operations that already live there:
- Reuse `execute_graphql` (not a hand-rolled POST) so the stale-endpoint refresh-and-retry-once
  behaviour and the process-wide `OnceLock` client both apply. Do NOT construct a second
  `reqwest::Client`.
- Decode with plan 02's `decode_get_package_artifact_response`.
- Fetch `downloadUrl` with a PLAIN GET carrying NO `Authorization` header. That URL is presigned and is
  a bearer credential in its own right; attaching the pmcp.run token would leak a credential to a
  different origin. Write that reason at the call site, not just in the type's rustdoc, because the
  call site is where somebody will "helpfully" add the header. Never log or persist the URL — including
  in error messages, which means the download error must be constructed without interpolating it.
- Bound the download with a named byte-cap constant carrying a rustdoc that justifies its value, in the
  shape `capture.rs:20-32` uses. Enforce it WHILE STREAMING — consume `bytes_stream()` with a running
  total and refuse the moment the total exceeds the cap. Do not call a whole-body collect and then
  check the length: Phase 113 recorded that collecting an over-cap body performs exactly the unbounded
  allocation the cap exists to prevent, and a `Content-Length` header is attacker-influenced, an
  early-refusal optimisation and never the authority.
- Apply the same connect/read timeout discipline `graphql.rs:167-169` already uses.

**Stage 4 — verify in memory (D-06).** Feed the bytes to plan 01's `read_verified`, which runs the
framing gates, the byte caps and the per-blob content-address check without touching the filesystem.
Then compare the artifact's own payload digest against the `payloadDigest` the response declared, and
refuse a mismatch by name. Note in the code that this second comparison depends on the platform's answer
to the open question written into `contracts/pmcp-run/portability-v1.graphql` — whether `payloadDigest`
is the OCI manifest digest or a digest over the tar bytes — and state which reading the code currently
implements so the assumption is visible rather than buried.

**Stage 5 — write.** `artifact::write_layout` into `--output`, refusing a pre-existing destination
unless `--force`, checked BEFORE the network call so a doomed pull does not spend a round trip.

**Stage 6 — report.** Call plan 03's renderer. `pull` and `load` must print the same thing for the same
package; that is what makes D-16 hold across both verbs by construction rather than by discipline.
Apply the same quiet-mode rule `load` uses: rendering is gated on `should_output()`, the subject-mismatch
verdict and its exit code are not.

**Args.** `PullArgs` with the reference positional, `--output <DIR>` and `--force`. No `--format`.

**`mod.rs`.** Add `pub mod pull;`, the `Pull(pull::PullArgs)` variant with a doc comment in the settled
vocabulary (fetches a published artifact from pmcp.run), and an async dispatch arm following the remote
verbs' shape. Extend the module header's direction list. Do not touch `Import` (D-03).
  </action>
  <acceptance_criteria>
    - `cargo build -p cargo-pmcp` exits 0 and `cargo pmcp package --help` lists `pull` (the asserted pin is plan 06).
    - `pull.rs` declares six functions whose names map one-to-one onto the six stages, and `execute` calls them in order — verified by reading; each is separately callable.
    - `pmat analyze complexity --format json --max-cognitive 25 | jq '.violations[] | select(.path | contains("commands/package"))'` produces no output for the files this plan touches.
    - `grep -rnE 'PMCP_[A-Z_]*URL' cargo-pmcp/src/ | grep -v 'deployment/targets/pmcp_run/auth.rs'` returns no matches (SC3 — no new base-URL environment variable).
    - `grep -rn 'reqwest::Client::new\|reqwest::Client::builder' cargo-pmcp/src/deployment/targets/pmcp_run/` shows no NEW construction site relative to the pre-plan tree, demonstrated by a `git diff` excerpt in the SUMMARY (SC3 — no second client).
    - `grep -rn 'cache_path' cargo-pmcp/src/commands/package/pull.rs` returns no matches (SC3 — no second token or config cache).
    - The `downloadUrl` GET carries no `Authorization` header — verified by reading the call site, and by a comment there stating why.
    - The download cap is enforced over a byte stream with a running total, not by measuring a collected body — verified by reading; the over-cap `behavior` test proves the refusal.
    - `RUSTFLAGS="" make quality-gate` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">D-04 rates the transport seam `costly`: its shape constrains how the live leg is later wired, and a wide seam would force the eventual live wiring to reshape the verification tail. Keep it to bytes-plus-declared-digest in, nothing else out.</reversibility>
  <verify>
    <automated>cargo build -p cargo-pmcp &amp;&amp; ! grep -rnE 'PMCP_[A-Z_]*URL' cargo-pmcp/src/ | grep -v 'deployment/targets/pmcp_run/auth.rs' | grep -q .</automated>
  </verify>
  <done>`pull` exists as a six-stage pipeline with exactly one impure step behind a substitutable seam; it reuses the base-URL, resolver, credential and client machinery that already exists; and the presigned URL is fetched unauthenticated, unlogged and under a streaming byte cap.</done>
</task>

<task type="auto">
  <name>Task 2: D-05 — every pull failure names the missing capability, with the cause chain intact</name>
  <files>cargo-pmcp/src/commands/package/pull.rs</files>
  <read_first>
    - `cargo-pmcp/src/commands/package/pull.rs` — as Task 1 left it, so the wrap is applied to real call sites
    - `cargo-pmcp/src/commands/package/capture.rs:70-90` — the `.context(...)` idiom and how the message tells the user what to DO
    - `cargo-pmcp/src/commands/package/inspect.rs:160-200` — `.context("unpack server package")` per stage, the per-stage context habit this repo already has
    - `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs:125-148` — the parked-capability rustdoc language: do not hunt for a production call site, do not delete this as dead code. The same register applies to `pull`'s parked-status message
    - `.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-CONTEXT.md` D-05 — including the ACCEPTED COST that must be stated rather than hidden
  </read_first>
  <action>
Make SC4's second half literally true: *invoking `pull` with no reachable backend fails with a message
naming the missing platform capability rather than a raw transport error.*

Wrap the pull path — the whole path, not only the network step — in one outermost context frame whose
message names `getPackageArtifact` as the required pmcp.run capability and states that it is not yet
available on the platform. Use `anyhow`'s context, so the underlying error is PRESERVED as a cause and
`-v` still shows the socket error, the 401, or the field-undefined GraphQL error. The chain is the
mitigation; do not replace the error, do not swallow it, do not stringify it into the top-level message.

Keep the per-stage contexts underneath it. A reader should be able to see from the chain WHICH stage
failed — resolving the environment, building the request, downloading, verifying, writing or reporting
— under a top-level line that says what capability the whole verb needs. One frame per stage, one frame
for the verb.

**Write the accepted cost into the code, not only into planning.** While parked, a genuine network
outage is attributed to the parked capability: an unreachable host, an expired token and an
unimplemented field all surface under the same top line. That is the deliberate trade D-05 makes to
satisfy SC4 literally, the intact cause chain is the mitigation, and the "not yet available" wording is
the ONLY thing that changes at unparking. State all three sentences in `pull.rs`'s module docs so the
next reader inherits the reasoning rather than rediscovering the smell.

Express the parked status with rustdoc and message text — never with a self-admitted-technical-debt
marker (C-3), matching how `VERIFY_ATTESTATION_QUERY` parks its own operation.

Do not apply this wrap to `save` or `load`. They are fully local and have nothing parked; giving them a
"missing platform capability" frame would be actively misleading.
  </action>
  <acceptance_criteria>
    - A test drives `pull` with a transport double that errors, and asserts the top-level error message contains `getPackageArtifact` AND a phrase indicating the capability is not yet available.
    - The same test asserts the ORIGINAL error is still reachable in the `anyhow` cause chain — asserted by walking `.chain()` and finding the double's message, not by string-matching the whole formatted output.
    - A second test asserts the chain identifies the failing STAGE (for example, the download stage's context frame is present when the transport fails, and the verification stage's frame is present when a corrupted tar is returned).
    - `pull.rs`'s module docs state the accepted cost, the mitigation and what changes at unparking, in three explicit sentences.
    - `grep -rn 'getPackageArtifact' cargo-pmcp/src/commands/package/save.rs cargo-pmcp/src/commands/package/load.rs` returns no matches — the wrap is scoped to `pull` only.
    - `grep -rn 'TODO\|FIXME' cargo-pmcp/src/commands/package/pull.rs` returns no matches (C-3).
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS= cargo test -p cargo-pmcp --test package_portability_contract -- --test-threads=1 2>&amp;1 | tee /tmp/123-05-t2.log &amp;&amp; test "$(awk '/^test result:/ { t += $4 } END { print t+0 }' /tmp/123-05-t2.log)" -gt 4</automated>
  </verify>
  <done>Every failure of the pull path surfaces under one line naming the capability the platform has not shipped, the real cause is still one `-v` away, and the accepted cost of that attribution is written down rather than hidden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drive the whole pipeline offline, and prove the destination is untouched on refusal</name>
  <files>cargo-pmcp/tests/package_portability_contract.rs</files>
  <read_first>
    - `cargo-pmcp/tests/package_portability_contract.rs` — as plan 02 left it: the SDL fixture, the four offline tests and the parked live leg the new cases join
    - `cargo-pmcp/src/commands/package/pull.rs` — as Tasks 1 and 2 left it, especially the seam's exact signature
    - `cargo-pmcp/tests/package_artifact_framing.rs` — as plan 04 left it, and `crates/pmcp-package/tests/golden_fixtures/artifact_tar_v1/` — the independently authored conformant and hostile tars this task feeds through the seam
    - `crates/pmcp-openapi-server/tests/parity_replay.rs:307-346` — the live-leg double gate, so the live case added here matches the one plan 02 already placed
    - `.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-RESEARCH.md` § *Common Pitfalls* Pitfall 8 — a selector that matches zero tests exits 0; assert a nonzero count
  </read_first>
  <behavior>
    - The seam double returning the conformant golden tar drives `pull` to a written layout, and the resulting directory is structurally identical to one `load` produces from the same bytes.
    - The seam double returning a hostile golden tar (one per violation) drives `pull` to an error, and the destination does not exist afterwards.
    - The seam double returning a payload whose bytes do not match the declared `payloadDigest` drives `pull` to an error naming the digest mismatch, with nothing written.
    - The seam double returning a body over the cap drives `pull` to a refusal naming the cap.
    - The seam double erroring drives `pull` to the D-05 wrapped error with the cause intact.
    - `pull` with a pre-existing destination and no `--force` refuses before the double is ever invoked, proven by the double recording that it was not called.
  </behavior>
  <action>
Add the offline pipeline tests to `cargo-pmcp/tests/package_portability_contract.rs`, beside plan 02's
contract tests, so the whole `pull` path is exercised with no backend in existence.

**Feed the seam the golden fixtures, not tars the test just built.** Plan 04 authored a conformant tar
and one hostile sibling per framing rule, as checked-in bytes never regenerated from the writer under
test. Using them here means the `pull` path is proven against bytes NEITHER the SDK's writer nor this
test produced — which is as close to a platform-produced artifact as this repo can get, and strictly
stronger than a self-generated tar. Say that in the test module's docs.

**Prove the destination is untouched on refusal, for every refusal.** For each hostile case, assert
both a non-zero result AND that the destination path does not exist afterwards. This is SC5's
verify-before-write half and it is the read-side mirror of `pack_server`'s rejected-pack invariant. A
test asserting only the error would pass against an implementation that wrote a partial layout and then
failed.

**Prove the pre-network refusals really are pre-network.** Give the seam double a call counter and
assert it is zero for the pre-existing-destination case and for the empty-reference case. An assertion
that `pull` "returned an error" cannot distinguish a refusal from a failed round trip.

**Prove `pull` and `load` agree.** Take the conformant fixture, run it through `load` from a temp file
and through `pull` via the double, and assert the two destination directories have identical entry sets
and identical blob bytes. That is what makes D-16 and the shared-renderer claim measurable rather than
architectural.

**Assert a nonzero count in every command this plan's verify block runs.** A nextest-style selector that
matches nothing exits 0 and this project has been bitten by that seven times in one phase; the Makefile
states the rule and the plans must follow it.

**Extend the parked live leg, do not duplicate it.** Plan 02 placed the `#[ignore]`d, env-gated live
test with its printed skip reasons. Extend that same test so it drives the FULL pipeline against the
real backend when enabled — request, download, verify, write, report — rather than adding a second
live test. Its body must already name what the backend must ship, so unparking remains deleting the two
gate lines.

Do NOT edit the Makefile here; plan 07 appends every new binary name at once.
  </action>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test package_portability_contract -- --test-threads=1` exits 0 with a passed count of at least 12 and exactly 1 ignored.
    - One named test exists per `behavior` row, plus one refusal test per hostile golden fixture.
    - Every refusal test asserts BOTH the error AND that the destination path does not exist afterwards.
    - The pre-existing-destination test and the empty-reference test each assert the seam double's call counter is 0.
    - The `pull`-versus-`load` agreement test compares entry sets and blob bytes of two independently produced destination directories and asserts equality.
    - Negative control, run and recorded in the SUMMARY: move `write_layout` ahead of the payload-digest comparison in `pull`, observe the digest-mismatch test's destination-does-not-exist assertion go red, restore it, observe green. This proves the verify-before-write ordering is checked, not assumed.
    - Negative control 2, run and recorded: remove the D-05 context frame, observe the wrapped-error test go red on the missing capability name while the cause-chain assertion still passes — proving the two assertions are independent.
    - `RUSTFLAGS="" make quality-gate` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>RUSTFLAGS= cargo test -p cargo-pmcp --test package_portability_contract -- --test-threads=1 2>&amp;1 | tee /tmp/123-05-t3.log &amp;&amp; test "$(awk '/^test result:/ { t += $4 } END { print t+0 }' /tmp/123-05-t3.log)" -ge 12</automated>
  </verify>
  <done>The entire `pull` pipeline runs offline against independently authored artifact bytes; every refusal leaves the destination untouched, proven per case; the pre-network refusals are proven to be pre-network by a call counter; and `pull` and `load` are shown to produce identical layouts.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files created by THIS PLAN (new to the codebase — not drift):

- `cargo-pmcp/src/commands/package/pull.rs` (new module)
- `PullArgs` (struct) and its `--output` / `--force` flags, plus the positional reference argument
- the six stage functions declared in `pull.rs` (resolve, build request, transport, verify, write, report)
- the transport-seam trait/function and its offline test double
- the download byte-cap constant declared for the presigned-URL fetch
- `PackageCommand::Pull` (new enum variant)
- the `getPackageArtifact` transport function added to `cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs`
- the offline pipeline tests added to `cargo-pmcp/tests/package_portability_contract.rs`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pmcp.run GraphQL endpoint -> `pull` | Untrusted remote; the response's fields are validated and its declared digest is never taken as authority over the bytes. |
| presigned `downloadUrl` -> the artifact bytes | Fully untrusted transport. §5.1: transport is never trusted; every digest is re-derived locally. |
| `downloadUrl` itself -> logs, errors, disk | The URL is a bearer credential to a different origin; crossing this boundary is a disclosure event. |
| verified bytes -> the destination directory | One-way: nothing reaches the destination that has not passed every gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-41 | Tampering | malicious blob substitution in transit | critical | mitigate | Every blob's sha256 is re-derived locally from the downloaded bytes and compared to its own content-address before any write; `unpack_*` then verifies again. Proven with hostile golden fixtures fed through the seam. |
| T-123-42 | Tampering | a payload whose bytes do not match the digest the platform declared | high | mitigate | The declared `payloadDigest` is compared against a locally re-derived digest; a mismatch refuses with nothing written, proven by a test whose destination-does-not-exist assertion has a recorded negative control. |
| T-123-43 | Information Disclosure | the presigned `downloadUrl` leaked into logs, an error message, or a persisted file | high | mitigate | Fetched with no `Authorization` header and never interpolated into any message or written anywhere; the reason is stated at the call site as well as on the type. |
| T-123-44 | Information Disclosure | the pmcp.run access token sent to the presigned URL's origin | high | mitigate | The `downloadUrl` GET is a plain unauthenticated request; the header is attached only to the GraphQL POST via the existing shared path. Recorded as SDL question A3 pending platform confirmation. |
| T-123-45 | Denial of Service | an oversized or endless download body | high | mitigate | A named byte cap enforced WHILE STREAMING with a running total, refusing on overflow rather than collecting then measuring; connect and read timeouts inherited from the existing transport discipline. |
| T-123-46 | Tampering | a partially written destination after a failed verification | high | mitigate | D-06 ordering: verification is complete in memory before `write_layout` is called; asserted per hostile case and pinned by a recorded ordering negative control. |
| T-123-47 | Spoofing | a hostile endpoint impersonating pmcp.run | medium | mitigate | HTTPS via the existing shared client; trust does not rest on the endpoint because the artifact's digests are re-derived locally. Residual: a hostile endpoint can serve a valid package that is simply the wrong one — attestation subject checking (plan 03 / D-15) is the control, and signature verification is deliberately out of scope. |
| T-123-48 | Elevation of Privilege | a second, weaker API path bypassing the resolver and credential machinery | medium | mitigate | SC3's grep assertions: no new base-URL environment variable, no second cache, no second client. Enforced as acceptance criteria over `cargo-pmcp/src/`. |
| T-123-49 | Repudiation | a network outage misattributed to the parked capability | low | accept | D-05's stated, accepted cost. The intact `anyhow` cause chain is the mitigation and the trade is written into `pull.rs`'s module docs. |
</threat_model>

<verification>
- `RUSTFLAGS= cargo test -p cargo-pmcp --test package_portability_contract -- --test-threads=1` — at least 12 passed, 1 ignored.
- `cargo build -p cargo-pmcp` and `cargo build -p cargo-pmcp --lib` both exit 0.
- SC3 greps over `cargo-pmcp/src/`: no new base-URL environment variable, no second cache path in `pull.rs`, no new `reqwest::Client` construction site.
- `pmat analyze complexity --max-cognitive 25` reports no violation in the files this plan touches (C-2).
- Two recorded negative controls: the verify-before-write ordering, and the D-05 context frame.
- `RUSTFLAGS="" make quality-gate` exits 0.
</verification>

<success_criteria>
- `pull` exists as six named stages with exactly one substitutable transport seam.
- The whole pipeline runs offline against independently authored artifact bytes.
- Every verification failure leaves the destination byte-for-byte unchanged, proven per case.
- Every failure names `getPackageArtifact` at the top with the real cause one `-v` away.
- No second API path exists: no new base-URL variable, no second cache, no second client.
- The presigned URL is fetched unauthenticated, never logged, and bounded by a streaming cap.
</success_criteria>

<output>
Create `.planning/phases/123-export-import-verbs-contract-first-parked-on-the-pmcp-run-ba/123-05-SUMMARY.md` when done.
</output>
