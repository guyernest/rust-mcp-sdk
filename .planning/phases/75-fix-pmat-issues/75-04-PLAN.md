---
phase: 75-fix-pmat-issues
plan: 04
type: execute
wave: 4
depends_on: ["75-03"]
files_modified:
  - crates/mcp-tester/src/diagnostics.rs
  - crates/mcp-tester/src/main.rs
  - crates/mcp-preview/src/handlers/websocket.rs
  - crates/mcp-preview/src/handlers/api.rs
  - crates/pmcp-server/pmcp-server-lambda/src/main.rs
  # examples/** and fuzz/** entries below are runtime-determined under examples/ and fuzz/,
  # scoped by Wave 0 spike result (75-W0-SPIKE-RESULTS.md::chosen_path). See Task 4-B Step 1
  # for the re-measurement procedure that enumerates the specific files.
  - examples/**
  - fuzz/**
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 5 scattered-hotspot functions in crates/mcp-tester/, crates/mcp-preview/, crates/pmcp-server/ have cog ≤25 OR ≤50 with `// Why:` `#[allow]`"
    - "examples/ violations (21) are addressed via the path chosen in 75-W0-SPIKE-RESULTS.md (path-filter OR bulk `#[allow]` with shared `// Why:` for demo code)"
    - "fuzz/ violations (3-4) are addressed via P5 with `// Why:` for fuzz-harness inline-variant rationale; test_auth_flow (cog 122) refactored mandatorily per D-03"
    - "SATD triage per D-04 completed: each of the 33 SATDs is either deleted (trivial/obsolete), replaced with `// See #NNN —` (real follow-up filed as GitHub issue), or fixed in-place (cheap-to-fix)"
    - "`pmat quality-gate --fail-on-violation --checks complexity` exits 0 — the badge can flip"
    - "Wave 4 commit message records `pmat-complexity: 0 (was MM)`"
  artifacts:
    - path: ".planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md"
      provides: "Per-SATD disposition record (delete / issue-NNN / fix-in-place) for D-04 audit trail"
      contains: "## SATD"
  key_links:
    - from: "75-W0-SPIKE-RESULTS.md::chosen_path"
      to: "examples/ violation handling strategy in this plan"
      via: "Wave 0 spike output drives Wave 4 task selection"
      pattern: "chosen_path:"
---

<objective>
Close out the remaining cognitive-complexity violations: 5 scattered hotspots in `crates/mcp-tester/`, `crates/mcp-preview/`, `crates/pmcp-server/` + the gating-relevant 21 examples/ violations + 3-4 fuzz/ violations. Apply per-D-04 SATD triage to the 33 outstanding TODO/FIXME comments. After this plan lands, `pmat quality-gate --fail-on-violation --checks complexity` exits 0 — the badge can flip (Wave 5 then enforces the gate going forward).

**Purpose:** Drop PMAT complexity violation count to 0 (or to whatever residual count the badge tolerates per `--fail-on-violation` semantics). Resolve D-09's examples/ handling per the empirical Wave 0 spike result. Triage SATD per D-04.

**Output:** 5 source files modified for scattered hotspots + bulk examples/ + fuzz/ updates per Wave 0 path + new `75-04-SATD-TRIAGE.md` audit document. After this plan merges, the gate is mechanically passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-fix-pmat-issues/75-CONTEXT.md
@.planning/phases/75-fix-pmat-issues/75-RESEARCH.md
@.planning/phases/75-fix-pmat-issues/75-PATTERNS.md
@.planning/phases/75-fix-pmat-issues/75-VALIDATION.md
@.planning/phases/75-fix-pmat-issues/75-00-PLAN.md
@.planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md
@.planning/phases/75-fix-pmat-issues/75-03-SUMMARY.md
@CLAUDE.md
@crates/mcp-tester/src/diagnostics.rs

<refactor_techniques>
P1 dominant for the 5 scattered hotspots. P5 for examples/ and fuzz/ if Wave 0 spike says path-filter doesn't work.
</refactor_techniques>

<within_wave_parallelism>
Tasks 4-A (scattered hotspots), 4-B (examples + fuzz per spike result), and 4-C (SATD triage) touch DISJOINT file sets and run in parallel. Task 4-D (final gate verification) is sequential after all three complete.
</within_wave_parallelism>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client → mcp-preview | Untrusted WS frames; refactor of handle_socket must preserve message dispatch |
| Lambda event → pmcp-server-lambda | Untrusted lambda payloads; handler refactor must preserve event routing |
| GitHub issue tracker → repo | SATD triage may file new issues — no security implication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-01 | Tampering | mcp-preview WS handler refactor could mis-route message types | mitigate | New `crates/mcp-preview/tests/handlers_integration.rs` per PATTERNS.md ("MANDATORY ... add ... before refactor"); per-message-type round-trip tests cover |
| T-75-04-02 | Information Disclosure | mcp-tester diagnostics refactor could leak / suppress diagnostic results | mitigate | Existing `crates/mcp-tester/tests/property_tests.rs` + integration tests cover |
| T-75-04-03 | Tampering | Lambda handler refactor could mis-route lambda events | mitigate | Existing in-file tests; if absent, add lambda_event_handler_smoke per PATTERNS.md |
| T-75-04-04 | None | examples/ bulk `#[allow]` is a documentation-clarity tradeoff, not a security issue | accept | Examples are demo code by definition; `// Why: illustrative demo` is the documented project-wide rationale |
| T-75-04-05 | Tampering | fuzz/test_auth_flow refactor (cog 122) could weaken fuzz coverage of auth flow variants | mitigate | Refactor extracts per-variant helpers but keeps the fuzzed input → variant mapping 1:1; `cargo fuzz run auth_flows` for ≥30s smoke per VALIDATION.md spirit |

**Refactor scope note:** Wave 4 closes out remaining violations and triages SATD. Test infrastructure must be added for mcp-preview before refactor (no existing test dir).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 4-A: Refactor scattered crate hotspots (5 functions, 5 files)</name>
  <files>crates/mcp-tester/src/diagnostics.rs, crates/mcp-tester/src/main.rs, crates/mcp-preview/src/handlers/websocket.rs, crates/mcp-preview/src/handlers/api.rs, crates/pmcp-server/pmcp-server-lambda/src/main.rs</files>
  <read_first>
    - crates/mcp-tester/src/diagnostics.rs (run_diagnostics_internal @ 28 cog 55)
    - crates/mcp-tester/src/main.rs (main @ 244 cog 40)
    - crates/mcp-preview/src/handlers/websocket.rs (handle_socket @ 50 cog 37)
    - crates/mcp-preview/src/handlers/api.rs (list_resources @ 179 cog 31)
    - crates/pmcp-server/pmcp-server-lambda/src/main.rs (handler @ 89 cog 26)
    - crates/mcp-tester/tests/property_tests.rs (existing coverage for tester)
    - src/server/streamable_http_server.rs lines 457-534 (analog dispatch shape for handle_socket per PATTERNS.md "For mcp-preview/src/handlers/websocket.rs::handle_socket specifically, the target shape is the same dispatch-table refactor")
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Wave 4 — full hotspot table; "no general analog spans all 5 files — they're heterogeneous")
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Wave 3 expansion table for these 5 fns)
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Refactor 5 scattered hotspot functions, one atomic commit each.

    **Pre-refactor for mcp-preview (no existing test dir):**
    Add `crates/mcp-preview/tests/handlers_integration.rs` with at least one round-trip test per handler (one for `handle_socket`, one for `list_resources`). Use a minimal test client. Commit FIRST: `test(75-04): add mcp-preview handlers integration tests pre-refactor`.

    **Pre-refactor for pmcp-server-lambda (verify in-file tests):**
    If `crates/pmcp-server/pmcp-server-lambda/src/main.rs` lacks an in-file `#[cfg(test)] mod tests`, add a `lambda_event_handler_smoke` test that constructs a representative lambda event and asserts the handler returns Ok. Commit: `test(75-04): add lambda handler smoke test pre-refactor`.

    **Refactor 1: `crates/mcp-tester/src/diagnostics.rs::run_diagnostics_internal` @ 28, cog 55 → ≤25.** Apply **P1** — extract per-diagnostic helpers (e.g. `run_protocol_handshake_diagnostic`, `run_capability_negotiation_diagnostic`, `run_tool_listing_diagnostic`, etc. — read function body to enumerate the diagnostic stages). Commit: `refactor(75-04): run_diagnostics_internal (cog 55→MM)`.

    **Refactor 2: `crates/mcp-tester/src/main.rs::main` @ 244, cog 40 → ≤25.** Apply **P1** — extract per-subcommand entry points. Each `Command::Test`, `Command::List`, etc. arm becomes a `run_test_subcommand`, `run_list_subcommand`, etc. helper. Commit: `refactor(75-04): mcp-tester main (cog 40→MM)`.

    **Refactor 3: `crates/mcp-preview/src/handlers/websocket.rs::handle_socket` @ 50, cog 37 → ≤25.** Apply **P4** dispatch — analog is `streamable_http_server.rs:457-534` per PATTERNS.md. Extract per-message-type handlers (`handle_init_message`, `handle_tool_call_message`, etc.). Commit: `refactor(75-04): handle_socket (cog 37→MM)`.

    **Refactor 4: `crates/mcp-preview/src/handlers/api.rs::list_resources` @ 179, cog 31 → ≤25.** Apply **P1** — extract per-resource-class helpers. Commit: `refactor(75-04): list_resources (cog 31→MM)`.

    **Refactor 5: `crates/pmcp-server/pmcp-server-lambda/src/main.rs::handler` @ 89, cog 26 → ≤25.** Borderline P1 — extract per-event-class helpers. If residual is exactly 25 or 26, P1 with one extract should land. Commit: `refactor(75-04): lambda handler (cog 26→MM)`.

    After each commit:
    - `cargo test --workspace --all-features -- --test-threads=1`
    - `cargo clippy --all-targets --all-features -- -D warnings`

    Any function with residual >25 after P1 extraction: P5 with `// Why:` per W0 template, ≤50 ceiling.
  </action>
  <verify>
    <automated>cargo test --workspace --all-features -- --test-threads=1 && cargo clippy --all-targets --all-features -- -D warnings && pmat analyze complexity --format json --max-cognitive 25 2>/dev/null | jq -e '[.violations[] | select(.path | test("crates/mcp-tester/src/(diagnostics|main)\\.rs|crates/mcp-preview/src/handlers/(websocket|api)\\.rs|crates/pmcp-server/pmcp-server-lambda/src/main\\.rs"))] | length' | xargs -I{} test {} -le 0</automated>
  </verify>
  <acceptance_criteria>
    - All 5 named functions have cog ≤25 OR ≤50 with `// Why:` `#[allow]`
    - No function in any of the 5 files exceeds cog 50
    - `crates/mcp-preview/tests/handlers_integration.rs` exists with ≥2 tests (per-handler)
    - `cargo test --workspace --all-features -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count strictly decreases by at least 5
    - All new `#[allow]` sites have `// Why:` per template
  </acceptance_criteria>
  <done>5 scattered crate hotspots resolved; new test scaffolding added where missing.</done>
</task>

<task type="auto">
  <name>Task 4-B: Address examples/ + fuzz/ violations per Wave 0 spike result</name>
  <files>examples/** and fuzz/** — specific files determined at runtime from a fresh `pmat analyze complexity --format json` after Wave 3 lands; see &lt;action&gt; Step 1 for re-measurement procedure (runtime-determined under examples/ and fuzz/, scoped by Wave 0 spike result), .pmatignore, .pmat/project.toml</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md (the EMPIRICAL ANSWER drives the choice between bulk `#[allow]` vs path filter; chosen_path: a/b/c)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (Pitfall 5 examples/ list; Pitfall 6 fuzz/ list)
    - .planning/phases/75-fix-pmat-issues/75-PATTERNS.md (Cross-Cutting Concerns — "Examples/ Violations (21 — Pitfall 5)" + "Fuzz/ Violations (3-4 — same Pitfall)")
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-09 mechanism)
    - .planning/phases/75-fix-pmat-issues/75-00-PLAN.md (`<project_wide_why_template>`)
  </read_first>
  <action>
    The path is determined by Wave 0's spike result. Read `75-W0-SPIKE-RESULTS.md` first; the `chosen_path:` value selects which sub-action below applies.

    **If `include_works: true` AND `chosen_path: (a)` — path filter works:**

    1. Add `--include 'src/**,crates/*/src/**,cargo-pmcp/src/**,pmcp-macros/src/**'` (or whatever syntax the spike confirmed) to the verification command in `75-VALIDATION.md` and to Wave 5's planned CI gate step.
    2. examples/ and fuzz/ violations remain in place but are excluded from the gate. No file edits needed in this task except updating VALIDATION.md to record the selected flag.
    3. Skip to fuzz/test_auth_flow refactor (still mandatory per D-03 — see below).

    **If `include_works: false` AND `chosen_path: (b)` — bulk `#[allow]` on examples:**

    1. For each of the 21 examples/ functions identified in RESEARCH.md Pitfall 6, add the project-wide demo `// Why:` justified `#[allow]` per PATTERNS.md Cross-Cutting Concerns section:
    ```rust
    // Why: illustrative demo code for documentation, intentionally inline for reader clarity
    #[allow(clippy::cognitive_complexity)]
    fn main() { /* example body */ }
    ```

    Functions to annotate (per RESEARCH.md Pitfall 6):
    - `examples/wasm-mcp-server/src/lib.rs::main` (cog 83)
    - `examples/27-course-server-minimal/src/main.rs::load_course_content` (cog 66)
    - `examples/c03_client_resources/...` (cog/line per spike re-measure)
    - `examples/s17_advanced_typed_tools/...`
    - `examples/26-server-tester/...`
    - `examples/t08_simd_parsing_performance/...`
    - `examples/t06_streamable_http_client/...`
    - `examples/t02_websocket_server_enhanced/...`
    - `examples/c06_multiple_clients_parallel/...`
    - `examples/s11_error_handling/...`
    - `examples/fermyon-spin/handle_request`
    - (any other examples/ violation per fresh `pmat analyze complexity --format json | jq '.violations[] | select(.path | startswith("examples/"))'`)

    BUT: per D-03, any examples/ function with cog >50 (e.g. `wasm-mcp-server/src/lib.rs::main` at 83, `27-course-server-minimal/src/main.rs::load_course_content` at 66) — P5 alone is FORBIDDEN. Either refactor the example down, OR (preferred per minimum-effort principle for demo code) bulk-`#[allow]` THEN add a small refactor that extracts a few helpers to bring residual ≤50.

    Commit: `chore(75-04): bulk #[allow] on examples/ for demo-code clarity per Wave 0 spike (chosen_path: b)`.

    **If `chosen_path: (c)` — other (e.g. .pmatignore works for complexity check):**

    1. Implement whatever the spike doc prescribes — likely creating/modifying `.pmatignore` or `.pmat/project.toml [analysis] exclude_patterns`.
    2. Verify the resulting `pmat quality-gate --fail-on-violation --checks complexity` exits 0.
    3. Commit: `chore(75-04): configure PMAT exclusions per Wave 0 spike (chosen_path: c)`.

    **fuzz/ violations (apply REGARDLESS of path):**

    1. **`fuzz/auth_flows::test_auth_flow` (cog 122)** — D-03 mandates refactor; cog 122 is 2.4× over cap 50. Apply **P1** to extract per-flow-variant helpers (e.g. `fuzz_oauth_pkce_flow`, `fuzz_oauth_authorization_code_flow`). The fuzzed input → variant mapping must remain 1:1 (PER threat_model). After refactor, run `cargo fuzz build` to verify the harness still compiles. Commit: `refactor(75-04): fuzz/auth_flows::test_auth_flow (cog 122→MM)`.

    2. **Remaining fuzz/ violations** (`test_pkce_flow` cog 45, `simulate_transport_operations` cog 46, `test_websocket_framing` cog 30): apply **P5** with shared `// Why:` per PATTERNS.md:
    ```rust
    // Why: fuzz harness — variant enumeration must remain inline so each branch
    // maps directly to a fuzzed input class.
    #[allow(clippy::cognitive_complexity)]
    fn test_pkce_flow(...) { ... }
    ```
    These are all ≤50 so P5 is permitted per D-03. Commit: `refactor(75-04): #[allow] for fuzz/ harness functions per Wave 0 plan`.

    After all sub-actions in this task:
    - `cargo test --workspace --all-features -- --test-threads=1` (refactor regressions)
    - `cargo build --workspace --examples` (examples still compile after `#[allow]` additions)
    - `cd fuzz && cargo build` (fuzz targets still compile)
    - `cargo clippy --all-targets --all-features -- -D warnings`
    - `pmat quality-gate --fail-on-violation --checks complexity` — should exit 0 OR have only the fuzz/ residual that's covered by P5
  </action>
  <verify>
    <automated>cargo test --workspace --all-features -- --test-threads=1 && cargo build --workspace --examples && cargo clippy --all-targets --all-features -- -D warnings && pmat quality-gate --fail-on-violation --checks complexity</automated>
  </verify>
  <acceptance_criteria>
    - `pmat quality-gate --fail-on-violation --checks complexity` exits 0 — the gate signal that drives the badge
    - For chosen_path b: all 21 examples/ functions have a `// Why:` annotated `#[allow]` (or have been refactored ≤25)
    - For chosen_path a: VALIDATION.md records the `--include` flag; no examples/ files modified
    - For chosen_path c: `.pmatignore` or `.pmat/project.toml` modification verified to exclude examples/ + fuzz/ from `--checks complexity`
    - fuzz/auth_flows::test_auth_flow refactored to ≤50 (cog 122 was over D-03 ceiling)
    - All other fuzz/ violations have `// Why:` `#[allow]`
    - `cargo build --workspace --examples` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - PMAT total complexity violation count = 0 (or = the count of P5'd fuzz functions if the gate counts them despite annotation)
  </acceptance_criteria>
  <done>Examples + fuzz violations addressed per the empirical spike result; gate exits 0.</done>
</task>

<task type="auto">
  <name>Task 4-C: SATD triage per D-04 (33 comments, three-way disposition)</name>
  <files>.planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md, (multiple source files per triage outcome)</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-CONTEXT.md (D-04 three-way policy: trivial → delete; real follow-up → file issue + `// See #NNN —` ref; cheap-to-fix → fix in-place)
    - CLAUDE.md ("Zero SATD comments allowed" + project SATD policy)
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (SATD context — 33 total per baseline)
  </read_first>
  <action>
    Triage all 33 SATD (TODO/FIXME/HACK/XXX) comments per D-04's three-way policy.

    **Step 1: Inventory.** Generate the current SATD list:
    ```bash
    pmat analyze satd --format json > /tmp/satd-inventory.json
    # OR if PMAT JSON output not available:
    grep -rn -E '(TODO|FIXME|HACK|XXX)' src/ crates/*/src/ cargo-pmcp/src/ pmcp-macros/src/ | grep -v 'target/' > /tmp/satd-inventory.txt
    ```

    **Step 2: Triage each into a per-comment row in `75-04-SATD-TRIAGE.md`:**
    ```markdown
    # Phase 75 — SATD Triage Audit

    Per D-04: each SATD is (a) trivial/obsolete → DELETE, (b) real follow-up → file issue + replace with `// See #NNN`, (c) cheap fix (<30min) → FIX-IN-PLACE.

    | # | File:line | SATD text | Disposition | Action |
    |---|-----------|-----------|-------------|--------|
    | 1 | src/server/foo.rs:42 | `TODO: handle malformed input` | (b) issue | Filed paiml/rust-mcp-sdk#NNN; replaced with `// See #NNN — defer to error-handling phase` |
    | 2 | src/client/bar.rs:99 | `FIXME: deprecated API` | (a) delete | Code already removed in commit XYZ; comment was stale |
    | 3 | crates/foo/lib.rs:7 | `TODO: validate length` | (c) fix | Added `if input.len() > MAX { return Err(...) }`; SATD comment removed |
    | ... | (33 rows total) |
    ```

    **Step 3: Bias the triage per CONTEXT.md `<specifics>`:**
    - SATDs in `pmcp-code-mode/`, `pentest/`, `deployment/` (newer code) → bias toward (b) issue (active follow-up).
    - SATDs in `streamable_http_server.rs` (mature src/) → bias toward (a) delete or (c) fix-in-place.

    **Step 4: Execute dispositions.** For each row in the triage table:
    - (a) Delete the comment (and any attached dead code). Atomic commit: `chore(75-04): delete obsolete SATD <file>:<line>`.
    - (b) File a GitHub issue using `gh issue create -R paiml/rust-mcp-sdk --title "<title>" --body "<context>"`. Replace the SATD line with `// See #NNN — <one-line reason>`. Atomic commit: `chore(75-04): file issue #NNN for SATD <file>:<line>`.
    - (c) Apply the cheap fix in-place. Remove the SATD comment. Atomic commit: `fix(75-04): address SATD <file>:<line>`.

    Issue grouping policy (planner's discretion per CONTEXT.md `<specifics>`): If multiple SATDs cluster around the same area (e.g. 3 SATDs in `task_prompt_handler.rs` about resolution failure handling), file ONE umbrella issue and reference it from all three SATD-replacement comments. Otherwise file per-SATD.

    **Step 5: Verify count drop.**
    ```bash
    pmat quality-gate --fail-on-violation --checks satd 2>&1 | tail -3
    # Should show count strictly less than baseline (was 33). Per D-01, SATD is best-effort —
    # not gating; even getting it down to 5-10 is a meaningful improvement.
    ```

    Commit the audit doc separately: `docs(75-04): SATD triage audit per D-04`.

    Constraint: Do NOT touch SATDs that fall outside the workspace (e.g. in `target/`, `.cargo/`, third-party vendored code). Do NOT bulk-convert all 33 to issues OR all 33 to deletions — the per-comment triage IS the deliverable per D-04.

    **Scope constraint (D-04 boundary):** The 33 SATD baseline count from CONTEXT.md may include `examples/`, `tests/`, `benches/`, and `fuzz/` paths. This triage scopes ONLY to `src/`, `crates/*/src/`, `cargo-pmcp/src/`, `pmcp-macros/src/` per the Step 1 inventory grep above. If the post-triage `pmat analyze satd` count is higher than expected (i.e. the gate count exceeds the in-scope row count from `75-04-SATD-TRIAGE.md`), re-run `pmat analyze satd --format json` to check whether out-of-scope SATDs (under `examples/`, `tests/`, `benches/`, `fuzz/`) are pulling the gate count up; if yes, document them in `75-04-SUMMARY.md` as "deferred — out of D-04 scope" rather than triaging.
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md && grep -cE '^\| [0-9]+ \|' .planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md | xargs -I{} test {} -ge 30 && cargo test --workspace --all-features -- --test-threads=1 && cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `75-04-SATD-TRIAGE.md` exists with ≥30 rows (covering 33 SATDs minus any duplicates the inventory may dedupe)
    - Each row has a disposition letter (a/b/c) and an action description
    - For (b) dispositions, the action mentions a filed `paiml/rust-mcp-sdk#NNN` issue number
    - For (c) dispositions, the source file shows the in-place fix (verifiable by `git diff`)
    - `pmat quality-gate --fail-on-violation --checks satd 2>&1 | grep "violations"` shows count strictly less than 33
    - `cargo test --workspace --all-features -- --test-threads=1` exits 0
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0
  </acceptance_criteria>
  <done>SATD count drops from 33; per-comment triage record exists; D-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 4-D: Final pre-Wave-5 gate verification (the badge can flip)</name>
  <files>.planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md</files>
  <read_first>
    - .planning/phases/75-fix-pmat-issues/75-W0-SPIKE-RESULTS.md (the chosen path)
    - .planning/phases/75-fix-pmat-issues/75-04-SATD-TRIAGE.md (triage outcomes)
    - .planning/phases/75-fix-pmat-issues/75-VALIDATION.md (Phase gate criteria)
  </read_first>
  <action>
    Run the FULL gate locally and record the outcome. This is the proof point that Wave 5 (CI enforcement) can land without immediately turning the gate red.

    1. Run `pmat quality-gate --fail-on-violation --checks complexity --format json > /tmp/final-complexity.json; echo "exit=$?"`. Record the exit code AND the full JSON's violation count.

    2. Run `pmat quality-gate --fail-on-violation --format json > /tmp/final-all-checks.json; echo "exit=$?"`. Record exit code and per-check breakdown (complexity, satd, duplicate, entropy, sections counts).

    3. Run `make quality-gate` to confirm the project-standard gate is also green.

    4. Run `cargo test --workspace --all-features -- --test-threads=1` for the final regression check.

    5. Write `.planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md`:
    ```markdown
    # Phase 75 — Final Count Pre-Wave-5

    Date: <today>
    Branch: <current branch>
    PMAT version: <pmat --version>

    ## --checks complexity (the gate signal per D-01)
    Exit code: 0 (or 1 with explanation)
    Violations: NN (was 94 baseline)
    Path filter applied: <command line used, per Wave 0 chosen_path>

    ## --fail-on-violation (all checks)
    Exit code: 0 (or 1 with breakdown)
    - complexity: NN
    - satd: NN (was 33)
    - duplicate: NN (was 1554; expected unchanged per RESEARCH.md Pitfall 3)
    - entropy: NN (was 4)
    - sections: NN (was 2)

    ## make quality-gate
    Exit code: 0

    ## cargo test --workspace --all-features
    Exit code: 0

    ## Verdict
    READY FOR WAVE 5: yes/no
    Rationale: <one paragraph>
    ```

    6. Commit: `docs(75-04): final pre-Wave-5 gate verification — pmat-complexity: NN (was 94)`.

    If `READY FOR WAVE 5: no` (i.e. complexity violations remain), STOP and reopen the upstream tasks that failed to clear them. Do NOT proceed to Wave 5 with a still-red complexity gate.
  </action>
  <verify>
    <automated>test -f .planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md && grep -E 'READY FOR WAVE 5: yes' .planning/phases/75-fix-pmat-issues/75-04-FINAL-COUNT.md && pmat quality-gate --fail-on-violation --checks complexity</automated>
  </verify>
  <acceptance_criteria>
    - `75-04-FINAL-COUNT.md` exists with verdict "READY FOR WAVE 5: yes"
    - `pmat quality-gate --fail-on-violation --checks complexity` exits 0
    - `make quality-gate` exits 0
    - `cargo test --workspace --all-features -- --test-threads=1` exits 0
    - Final-count doc records the per-check breakdown for audit
  </acceptance_criteria>
  <done>Phase 75 gate is mechanically passing; Wave 5 can land enforcement without immediately blocking PRs.</done>
</task>

</tasks>

<verification>
After all 4 Wave 4 tasks complete:

1. **The badge gate signal**: `pmat quality-gate --fail-on-violation --checks complexity` exits 0.
2. **Per-directory zero-violations** for the 5 scattered crates files: `pmat analyze complexity --format json --max-cognitive 25 | jq '[.violations[] | select(.path | test("crates/(mcp-tester|mcp-preview|pmcp-server)/"))] | length'` = 0.
3. **Examples/fuzz handled per spike**: Either via path filter (no source changes) or via bulk `#[allow]` (source changes recorded in 75-04-SATD-TRIAGE.md sister doc).
4. **SATD count drops**: ≤ 5 (target — exact count depends on triage outcomes; D-04 doesn't mandate zero).
5. **D-02 conformance**: Every `#[allow(clippy::cognitive_complexity)]` added in this wave has `// Why:` directly above.
6. **D-03 conformance**: No function in any modified file exceeds cog 50.
7. **Workspace test green**: `cargo test --workspace --all-features -- --test-threads=1` exits 0.
8. **`make quality-gate` exits 0**.
9. **Final-count doc exists** with `READY FOR WAVE 5: yes`.

Wave-merge commit message format: `refactor(75-04): wave 4 complete — pmat-complexity: 0 (was MM)`.
</verification>

<success_criteria>
- 5 scattered hotspots refactored (Task 4-A)
- examples/ + fuzz/ violations addressed per Wave 0 chosen_path (Task 4-B)
- 33 SATDs triaged per D-04 with per-comment audit record (Task 4-C)
- Final-count doc verifies gate signal exits 0 (Task 4-D)
- Wave 5 can land CI enforcement without immediately blocking PRs
</success_criteria>

<output>
After completion, create `.planning/phases/75-fix-pmat-issues/75-04-SUMMARY.md` per the GSD summary template, including:
- Per-function before/after cog scores for the 5 scattered hotspots
- Path chosen for examples/fuzz (a/b/c) and rationale
- SATD triage outcome breakdown (count of a/b/c)
- Final gate exit code recorded
- Wave-rollup PMAT count delta (target: 0 violations)
</output>
