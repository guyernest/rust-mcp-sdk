---
phase: 45-extend-mcp-apps-support-to-claude-desktop
task: UAT verification and example fixes
total_tasks: 6
status: complete
last_updated: 2026-03-09T23:45:00Z
---

<current_state>
Phase 45 UAT complete. All 6 items resolved — examples migrated, host layer
support added to ServerBuilder, and mcp-preview ChatGPT enrichment re-added.
</current_state>

<completed_work>

- Migrated all 3 MCP Apps examples (chess, dataviz, map) from `ChatGptAdapter` to `McpAppsAdapter`
- All tools now use `TypedSyncTool::new().with_description().with_ui("ui://app/...")` pattern
- Resource handlers use standard postMessage bridge instead of ChatGPT skybridge
- All 3 examples compile and pass clippy with zero warnings
- 735 workspace lib tests pass
- Added `.with_host_layer(HostType::ChatGpt)` to all 3 example server builders
- Added `with_host_layer()` method to `ServerBuilder` (was only on `ServerCoreBuilder`)
- Applied host layer enrichment in `ServerBuilder::build()` (tool_infos cache)
- Re-added `enrich_meta_for_chatgpt` to mcp-preview for list_tools, call_tool, and list_resources
- Verified chess `new_game` returns full board state (8x8 array matches widget JS)
- Verified map `search_cities` has correct input schema via schemars derive
- Protocol tab in standard mode is a pre-existing issue, not a Phase 45 regression
</completed_work>

<decisions_made>

- `ServerBuilder` now supports `with_host_layer()` matching `ServerCoreBuilder` API
- mcp-preview enrichment uses `entry().or_insert` so server-provided keys are never overwritten
- Items 2, 3, 5 from original checkpoint were non-issues upon investigation
</decisions_made>
