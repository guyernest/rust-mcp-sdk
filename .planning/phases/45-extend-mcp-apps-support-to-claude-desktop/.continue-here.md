---
phase: 45-extend-mcp-apps-support-to-claude-desktop
task: MCP Apps examples rework
total_tasks: 4
status: in_progress
last_updated: 2026-03-10T01:54:21Z
---

<current_state>
Phase 45 plans (45-01, 45-02, 45-03) are complete but the MCP Apps examples
in /examples/ need rework to properly demonstrate the outputSchema +
structuredContent pattern. Server-side and widget-side changes are done and
compile clean (zero clippy warnings). Need testing and one widget JS fix.
</current_state>

<completed_work>

- Migrated all 3 examples from TypedSyncTool to TypedToolWithOutput (adds outputSchema)
  - chess: GameState, MoveResult, ValidMovesResult output types
  - map: SearchCitiesResult, CityDetailsResult, NearbyCitiesResult output types
  - dataviz: QueryResult, TableListResult, ColumnInfo, TableDescription output types
- Made map search_cities input `query` field required (was all-optional)
- Added ui/notifications/tool-result postMessage listener to all 3 widgets
  - board.html: handles GameState, MoveResult, ValidMovesResult
  - map.html: handles city list (with NearbyCity normalization), city details
  - dashboard.html: handles query results, table list, table description
- /simplify review completed — fixed:
  - Removed unused SqlError struct (dataviz)
  - Removed redundant recommended_zoom field (map)
  - Removed redundant map_state echo-back from SearchCitiesResult and input (map)
  - Hoisted query_lower before iterator (map)
  - Removed no-op { passive: true } from all 3 widget listeners
  - Removed unused map_state field from SearchCitiesInput
- All 3 examples compile with zero clippy warnings
</completed_work>

<remaining_work>

- Fix map widget JS: remove `map_state` from searchCities() callTool args (line ~329 in map.html)
- Test chess example: verify chess_new_game returns correct board (pieces at right positions)
- Test all 3 examples with mcp-preview to verify structuredContent flows to widgets
- Test with Claude Desktop if available
- Memory file mcp-apps-examples-issues.md needs updating when complete
</remaining_work>

<decisions_made>

- Kept boolean-flag pattern (success/found + optional fields) instead of Rust enum — flat JSON shape needed for widget compatibility
- Handlers use Box::pin(async move { sync code }) because TypedToolWithOutput requires async signature — noted as SDK-level improvement for future (sync variant)
- ResourceHandler boilerplate duplication across examples accepted — extracting WidgetResourceHandler is a separate SDK phase
- Shared JS listener extraction into bridge is a separate SDK phase
</decisions_made>

<blockers>
None.
</blockers>

<context>
The reference implementation is the open-images server at pmcp-run/built-in/sql-api/servers/open-images.
Key pattern: tools define outputSchema → SDK populates structuredContent in tool result →
host pushes structuredContent to widget via ui/notifications/tool-result postMessage →
widget renders data. Text content in tool reply = summary, not data repetition.
These examples are educational — developers will copy and modify them.
</context>

<next_action>
1. Fix map.html widget JS to remove map_state from searchCities callTool args
2. Run chess example and call chess_new_game to verify board output
3. Test with mcp-preview to verify end-to-end structuredContent flow
</next_action>
