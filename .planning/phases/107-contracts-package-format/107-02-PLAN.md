---
phase: 107-contracts-package-format
plan: 02
type: execute
wave: 2
depends_on: ["107-01"]
files_modified:
  - crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json
  - crates/pmcp-package/tests/golden_fixtures/team_small_review_v1.json
  - crates/pmcp-package/tests/digest_stability.rs
  - Cargo.toml
  - CLAUDE.md
  - .github/workflows/release.yml
autonomous: true
requirements: [PKG-02]
must_haves:
  truths:
    - "Golden fixtures cover all four package kinds: server, workflow, agent, team"
    - "digest_stability.rs asserts agent and team fixture digests are stable across ≥100 recomputations"
    - "pmcp-package is listed in CLAUDE.md publish order as a leaf before cargo-pmcp"
    - "release.yml has a publish step for pmcp-package using --manifest-path (not -p, since the crate is workspace-excluded)"
    - "cargo metadata at repo root succeeds (isolation verified; exclude entry added only if required)"
  artifacts:
    - path: "crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json"
      provides: "AgentPackage golden fixture (wire-freeze coverage)"
    - path: "crates/pmcp-package/tests/golden_fixtures/team_small_review_v1.json"
      provides: "TeamPackage golden fixture (wire-freeze coverage)"
    - path: ".github/workflows/release.yml"
      provides: "crates.io publish step for pmcp-package"
      contains: "pmcp-package"
  key_links:
    - from: "crates/pmcp-package/tests/digest_stability.rs"
      to: "crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json"
      via: "stability test deserializes the fixture and asserts digest stability"
      pattern: "agent_.*_v1|team_.*_v1"
    - from: ".github/workflows/release.yml"
      to: "crates/pmcp-package/Cargo.toml"
      via: "cargo publish --manifest-path crates/pmcp-package/Cargo.toml"
      pattern: "manifest-path crates/pmcp-package"
---

<objective>
Complete the PKG-02 wire-freeze story: extend golden-fixture coverage to all four serialized package
kinds so a shape change in ANY kind fails CI, and wire `pmcp-package` into the repo's publish
machinery (publish-order docs + release workflow) so it publishes to crates.io as a leaf ahead of
cargo-pmcp. The wire-freeze POLICY text already ships in Plan 01's README/CHANGELOG; this plan adds
the mechanical enforcement gap and the publish path.

Purpose: The wire-freeze claim ("0.1.x digest-stable") is only real if every serialized shape is
fixture-guarded, and the crate can only ship if the release workflow knows to publish it (it is
workspace-excluded, so `-p pmcp-package` does NOT reach it).
Output: agent + team golden fixtures + stability tests; CLAUDE.md publish-order entry; release.yml
publish step via `--manifest-path`; verified workspace isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-contracts-package-format/107-RESEARCH.md
@.planning/phases/107-contracts-package-format/107-PATTERNS.md
@.planning/phases/107-contracts-package-format/107-01-SUMMARY.md

<notes>
- `pmcp-package` has its own `[workspace]` table and is NOT a root workspace member. Therefore
  `cargo publish -p pmcp-package` from the root will NOT find it — the release step MUST use
  `cargo publish --manifest-path crates/pmcp-package/Cargo.toml`.
- Publish order: `pmcp-package` is a LEAF (no internal deps). It only needs to publish BEFORE
  cargo-pmcp (which pins it in Phase 110). Placing it early is correct.
- A4 resolution: root Cargo.toml `members` is an explicit list and the crate has its own
  `[workspace]` table, so it is already isolated. Only add a root `exclude` entry if `cargo
  metadata` at the repo root errors on it.
- Scope note (W1 / PKG-02 shipping): this plan wires the publish MACHINERY (CLAUDE.md publish order
  + release.yml step). The literal `cargo publish --manifest-path crates/pmcp-package/Cargo.toml`
  runs at milestone release time via the tag-triggered `release.yml` (repo precedent — publishes are
  never done from a plan), so the actual crates.io publish is a REQUIRED follow-up release action
  before PKG-02 is considered "Shipped."
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agent + team golden fixtures and stability tests</name>
  <files>crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json, crates/pmcp-package/tests/golden_fixtures/team_small_review_v1.json, crates/pmcp-package/tests/digest_stability.rs</files>
  <read_first>
    - crates/pmcp-package/tests/digest_stability.rs (existing server fixture test + the inline AgentPackage example showing the exact field set)
    - crates/pmcp-package/tests/golden_fixtures/server_team_fs_v1.json (canonical single-line JSON shape to match)
    - crates/pmcp-package/src/package/agent.rs (AgentPackage struct — authoritative field names/types)
    - crates/pmcp-package/src/package/team.rs (TeamPackage struct — authoritative field names/types)
  </read_first>
  <action>
    Author two new golden fixtures matching the compact canonical-JSON style of
    `server_team_fs_v1.json`. `agent_pto_researcher_v1.json` serializes a valid `AgentPackage`
    (fields per src/package/agent.rs, cross-checked against the inline example already in
    digest_stability.rs: name, version, instructions, llm, max_tokens, max_iterations, connectors,
    tool_selection, input_schema, output_schema, importance, finalizer_role, budget_defaults).
    `team_small_review_v1.json` serializes a valid `TeamPackage` (fields per src/package/team.rs).
    Produce each fixture by serializing a constructed struct (or by hand-matching the struct's serde
    output) so `serde_json::from_slice::<AgentPackage>` / `::<TeamPackage>` round-trips without error.
    Then extend `digest_stability.rs` with two tests mirroring
    `server_fixture_digest_is_stable_across_100_computations` exactly — swap the type and fixture
    name: one reads `agent_pto_researcher_v1.json` into `AgentPackage`, one reads
    `team_small_review_v1.json` into `TeamPackage`, each computing `manifest_digest` ≥100× and
    asserting equality. This closes the coverage gap so a serialized-shape change in agent OR team
    packages fails a fixture (the 0.2.0 trigger).
  </action>
  <verify>
    <automated>cd crates/pmcp-package && cargo test --test digest_stability</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pmcp-package/tests/golden_fixtures/agent_pto_researcher_v1.json` exists and parses as an `AgentPackage`
    - `crates/pmcp-package/tests/golden_fixtures/team_small_review_v1.json` exists and parses as a `TeamPackage`
    - `digest_stability.rs` contains a test that deserializes `AgentPackage` from the agent fixture and one that deserializes `TeamPackage` from the team fixture
    - `cargo test --test digest_stability` exits 0 with all four kinds (server, workflow, agent, team) covered
  </acceptance_criteria>
  <done>All four package kinds have checked-in golden fixtures whose digests are asserted stable, so any serialized-shape change fails CI.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pmcp-package into publish order and release workflow</name>
  <files>Cargo.toml, CLAUDE.md, .github/workflows/release.yml</files>
  <read_first>
    - CLAUDE.md (the "### Workspace Crates (publish order)" numbered list, items 1–12)
    - .github/workflows/release.yml (the publish-crates job and an existing leaf publish block, e.g. the pmcp-widget-utils step, to mirror the skip-if-already-published pattern)
    - Cargo.toml (root workspace [workspace] members line ~578 and exclude line ~580)
  </read_first>
  <action>
    (1) Verify isolation (A4): run `cargo metadata --format-version 1 --no-deps` at the repo root.
    If it succeeds and does not list/err on pmcp-package, leave the root `Cargo.toml` workspace
    `members`/`exclude` UNCHANGED. Only if it errors referencing `crates/pmcp-package`, add
    `"crates/pmcp-package"` to the `[workspace] exclude` array (~line 580). Do NOT add it to
    `members`.
    (2) CLAUDE.md: insert a new entry in the "Workspace Crates (publish order)" list for
    `pmcp-package` as a LEAF with no internal deps, noting it is a standalone workspace-excluded
    crate published via `cargo publish --manifest-path crates/pmcp-package/Cargo.toml`, and that it
    must publish BEFORE `cargo-pmcp` (which pins `pmcp-package = "0.1"` in Phase 110). Place it as an
    early leaf entry (e.g. "1b." right after `pmcp-widget-utils`).
    (3) release.yml: add a publish step for pmcp-package in the `publish-crates` job, placed before
    the cargo-pmcp publish step. Mirror an existing leaf step's structure (name, run, and the
    "already published, continuing..." skip-on-failure guard) but use
    `cargo publish --manifest-path crates/pmcp-package/Cargo.toml` (NOT `-p pmcp-package`, which
    cannot resolve a workspace-excluded crate). Add a short crates.io index wait after it if the
    surrounding steps use one.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo metadata --format-version 1 --no-deps >/dev/null && grep -q "pmcp-package" CLAUDE.md && grep -q "manifest-path crates/pmcp-package/Cargo.toml" .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo metadata --format-version 1 --no-deps` at repo root exits 0
    - `CLAUDE.md` "Workspace Crates (publish order)" list contains a `pmcp-package` entry marked as a leaf published before cargo-pmcp
    - `.github/workflows/release.yml` contains a publish step with `cargo publish --manifest-path crates/pmcp-package/Cargo.toml`
    - `.github/workflows/release.yml` does NOT contain `cargo publish -p pmcp-package`
    - Root `Cargo.toml` `members` array does NOT contain `crates/pmcp-package`
  </acceptance_criteria>
  <done>pmcp-package is documented in the publish order and wired into the release workflow via manifest-path so a tag push publishes it as a leaf, and its workspace isolation is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serialized package → SHA-256 digest | Fixture-guarded wire-freeze boundary across all four kinds |
| repo tag push → crates.io publish | Release workflow publishes the new crate; wrong invocation (`-p`) silently skips it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-04 | Tampering | agent/team serialized shape | mitigate | New golden fixtures + ≥100× stability tests fail CI on any shape change (the 0.2.0 trigger); closes the server/workflow-only coverage gap |
| T-107-05 | Denial of Service (release integrity) | release.yml publish step | mitigate | Use `--manifest-path` (workspace-excluded crate cannot be reached by `-p`); mirror the existing skip-if-already-published guard so re-runs are idempotent |
| T-107-SC | Tampering | dependency set (supply chain) | accept | No new packages introduced; deps already pinned + audited in Plan 01; `cargo audit` in `make quality-gate` |
</threat_model>

<verification>
- `cd crates/pmcp-package && cargo test --test digest_stability` passes with all four fixtures.
- `cargo metadata --format-version 1 --no-deps` at repo root exits 0.
- release.yml contains the manifest-path publish step; CLAUDE.md lists the crate.
- `make quality-gate` (repo-wide) green before phase gate.
</verification>

<success_criteria>
All four package kinds are fixture-guarded for wire-freeze, `pmcp-package` is documented in the
publish order and wired into release.yml via `--manifest-path` so a tag push publishes it as a leaf
ahead of cargo-pmcp, and workspace isolation is confirmed.
</success_criteria>

<output>
Create `.planning/phases/107-contracts-package-format/107-02-SUMMARY.md` when done.
</output>
