---
phase: 107-contracts-package-format
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/team-servers-v1.yaml
  - contracts/team-servers/fixtures/team-fs/**
  - contracts/team-servers/fixtures/mem-mcp/**
  - contracts/team-servers/fixtures/approval-mcp/**
  - contracts/team-servers/fixtures/team-mcp/**
  - tests/team_contracts_conformance.rs
autonomous: true
requirements: [PKG-03]
must_haves:
  truths:
    - "contracts/team-servers-v1.yaml defines one equation per team-server surface (team-fs, mem-mcp, approval-mcp, team-mcp) and enumerates all 25 static tool names"
    - "The contract is marked as a namespaced provisional PMCP extension"
    - "Shared conformance fixtures exist as request/response JSON pairs, one directory per server"
    - "tests/team_contracts_conformance.rs asserts every fixture's tool name appears in the contract and every fixture parses"
    - "No binding.yaml is authored for these equations (deferred to Phase 109)"
  artifacts:
    - path: "contracts/team-servers-v1.yaml"
      provides: "Provable-contracts YAML for the four team-server tool surfaces"
      contains: "team_mcp__"
    - path: "tests/team_contracts_conformance.rs"
      provides: "Structural conformance gate over contract + fixtures"
    - path: "contracts/team-servers/fixtures/team-fs"
      provides: "fs__* request/response conformance fixtures"
  key_links:
    - from: "tests/team_contracts_conformance.rs"
      to: "contracts/team-servers-v1.yaml"
      via: "test reads the YAML text and asserts equation keys + tool-name literals"
      pattern: "team-servers-v1.yaml"
    - from: "tests/team_contracts_conformance.rs"
      to: "contracts/team-servers/fixtures"
      via: "test parses each fixture JSON and cross-references the tool name against the contract"
      pattern: "fixtures"
---

<objective>
Capture the four team-server tool contracts as a versioned provable-contracts YAML plus shared
conformance fixtures (PKG-03), following the in-repo `contracts/*.yaml` house convention. This is a
contract-authoring job: it documents the tool surfaces and dispatch semantics as namespaced
provisional PMCP extensions, and ships the shared request/response fixtures that both the Phase 109
reference servers and the platform servers will verify against (TEAM-06). Bindings to concrete
functions are DEFERRED to Phase 109 (the reference servers do not exist in-repo yet).

Purpose: Contract-first house rule — the contract must precede the Phase 108/109 implementations.
The fixtures are the single shared artifact that makes "same fixtures the platform servers can run"
(TEAM-06) real.
Output: `contracts/team-servers-v1.yaml` (4 equations), `contracts/team-servers/fixtures/**`
(per-server request/response JSON), and a `tests/team_contracts_conformance.rs` structural gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-contracts-package-format/107-RESEARCH.md
@.planning/phases/107-contracts-package-format/107-PATTERNS.md

House contract format template (read as the structure to mirror):
`contracts/mcp-protocol-sdk-v1.yaml` (metadata block + equations map)

<notes>
- Exact tool surfaces (VERIFIED from pmcp-run source):
  - team-fs (11): fs__list fs__read fs__write fs__append_file fs__head fs__stat
    fs__create_directory fs__get_download_url fs__sync_to_review fs__sync_from_review fs__complete_task
  - mem-mcp (6): mem__add mem__get mem__search mem__list_recent mem__delete mem__complete_task
  - approval-mcp: resolve_approval  get_approval  (static) + team_approval__ask_<member> (dynamic, per human roster member)
  - team-mcp: team_mcp__<member> (dynamic; tools/list computed per-request from config)
- team-mcp CORRECT surface (NOT the obsolete raw-JSON-RPC bypass): member lookup by DDB id NOT name;
  `ToolOutput::Result` owns the full CallToolResult incl. top-level `_meta[related_task]` (pmcp 2.12.0);
  strict `x-pmcp-team-depth` parse; self-call guard compares ids not names; ancestor-cycle guard;
  advertised-schema == enforced-schema.
- Granularity (Open Question 3): ONE equation per server surface (4 equations), per-tool detail
  inside `formula`/`invariants` — matches how the template groups by capability.
- Do NOT author a `binding.yaml` for these equations (Pitfall 4 / A3) — the target functions live in
  crates/pmcp-team-servers (Phase 109). `pmat comply check` would fail on unresolved bindings.
- Fixture location (A2): `contracts/team-servers/fixtures/<server>/` request/response JSON pairs —
  a cross-repo coordination point; this is the conservative default per RESEARCH recommendation.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author contracts/team-servers-v1.yaml</name>
  <files>contracts/team-servers-v1.yaml</files>
  <read_first>
    - contracts/mcp-protocol-sdk-v1.yaml (metadata block lines 1-14 + equation structure: formula/domain/codomain/invariants/preconditions/postconditions/lean_theorem)
    - .planning/phases/107-contracts-package-format/107-PATTERNS.md (the exact tool-surface enumeration + team-mcp dispatch invariants to encode)
  </read_first>
  <action>
    Author `contracts/team-servers-v1.yaml` in the house format. Copy the `metadata` block shape from
    `mcp-protocol-sdk-v1.yaml` (version: 1.0.0, created date, author: PAIML Engineering, description).
    The description MUST mark this as a NAMESPACED PROVISIONAL PMCP EXTENSION (fs__/mem__/team_mcp__/
    team_approval__ namespaces; provisional per WG posture). Author exactly four equations, one per
    server surface: `fs_tool_surface` (enumerate all 11 fs__* tools in the formula; invariants:
    fs__list carries annotations.read_only_hint == true, fs__complete_task follows SEP-1686
    task-completion), `mem_tool_surface` (enumerate all 6 mem__* tools), `approval_tool_surface`
    (static resolve_approval + get_approval, plus dynamic team_approval__ask_<member> family — one per
    human roster member), and `team_dispatch_surface` (dynamic team_mcp__<member>; tools/list computed
    per-request from config). For team_dispatch_surface, capture the CORRECT dispatch invariants
    (NOT the obsolete bypass): member lookup by DDB id not name; ToolOutput::Result owns the full
    CallToolResult including top-level _meta[related_task] (pmcp 2.12.0); strict x-pmcp-team-depth
    parse; self-call guard compares ids not names; ancestor-cycle guard; advertised-schema ==
    enforced-schema. Each equation carries formula/domain/codomain/invariants/preconditions/
    postconditions like the template; set `lean_theorem` to `status: planned` style placeholders or
    omit (no proof obligations wired this phase). Do NOT author a sibling binding.yaml.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && python3 -c "import yaml,sys; d=yaml.safe_load(open('contracts/team-servers-v1.yaml')); eqs=d['equations']; assert set(['fs_tool_surface','mem_tool_surface','approval_tool_surface','team_dispatch_surface'])<=set(eqs), eqs.keys(); t=open('contracts/team-servers-v1.yaml').read(); [t.index(x) for x in ['fs__list','fs__complete_task','mem__add','mem__complete_task','resolve_approval','team_approval__ask_','team_mcp__']]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/team-servers-v1.yaml` parses as valid YAML with an `equations` map containing keys `fs_tool_surface`, `mem_tool_surface`, `approval_tool_surface`, `team_dispatch_surface`
    - The file contains all 11 fs__* tool names, all 6 mem__* tool names, `resolve_approval`, `get_approval`, `team_approval__ask_`, and `team_mcp__`
    - The `metadata.description` contains the words `provisional` and `extension`
    - The team_dispatch_surface invariants mention `ToolOutput::Result` and top-level `_meta` (not the raw-JSON-RPC bypass)
    - No `contracts/team-servers-binding.yaml` or `contracts/binding.yaml` change is introduced
  </acceptance_criteria>
  <done>The four team-server tool surfaces and team-mcp dispatch semantics are captured as a well-formed provable-contracts YAML marked namespaced/provisional, with no premature bindings.</done>
</task>

<task type="auto">
  <name>Task 2: Author shared conformance fixtures + structural conformance test</name>
  <files>contracts/team-servers/fixtures/team-fs/**, contracts/team-servers/fixtures/mem-mcp/**, contracts/team-servers/fixtures/approval-mcp/**, contracts/team-servers/fixtures/team-mcp/**, tests/team_contracts_conformance.rs</files>
  <read_first>
    - contracts/team-servers-v1.yaml (the contract authored in Task 1 — tool names the fixtures must reference)
    - crates/pmcp-package/tests/golden_fixtures/server_team_fs_v1.json (compact JSON shape reference)
    - .planning/phases/107-contracts-package-format/107-PATTERNS.md ("contracts/team-servers/fixtures/**" role + request/response pairing)
  </read_first>
  <action>
    Create shared conformance fixtures under `contracts/team-servers/fixtures/<server>/`, one
    directory per server (team-fs, mem-mcp, approval-mcp, team-mcp). Each fixture is a JSON object
    with a `request` (a `tools/call` params object: `{ "name": "<tool>", "arguments": {...} }`) and an
    `expected_response` (a representative `CallToolResult`-shaped object). Provide at least one fixture
    per static tool family: a representative fs__* call (e.g. fs__list showing read_only_hint context
    and fs__complete_task showing SEP-1686 top-level `_meta` related_task), a representative mem__*
    call (mem__add and mem__search), a resolve_approval/get_approval pair, and a team_mcp__<member>
    dispatch fixture whose expected_response places `related_task` in top-level `_meta` (the correct
    ToolOutput::Result surface). Then author `tests/team_contracts_conformance.rs` as a root-workspace
    integration test (part of the pmcp crate) that: (a) reads
    `concat!(env!("CARGO_MANIFEST_DIR"), "/contracts/team-servers-v1.yaml")` as text and asserts the
    four equation keys and all 25 static tool-name literals are present; (b) walks
    `contracts/team-servers/fixtures/`, parses every `*.json` with `serde_json` (asserting each has
    `request.name` and `expected_response`), and asserts each fixture's `request.name` tool string (or
    its `team_mcp__` / `team_approval__` prefix) appears in the contract YAML text. Use only
    already-available deps (`serde_json`, std) — do NOT add serde_yaml.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo test --test team_contracts_conformance</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/team-servers/fixtures/` contains one subdirectory per server (team-fs, mem-mcp, approval-mcp, team-mcp), each with at least one `*.json` fixture
    - Every fixture JSON parses and contains a `request.name` and an `expected_response`
    - At least one fixture's `expected_response` places `related_task` under a top-level `_meta`
    - `tests/team_contracts_conformance.rs` exists and `cargo test --test team_contracts_conformance` exits 0
    - The conformance test fails if a fixture references a tool name absent from `contracts/team-servers-v1.yaml` (cross-reference assertion present)
  </acceptance_criteria>
  <done>Shared per-server conformance fixtures exist and a dependency-free structural test proves every fixture's tool surface is captured in the contract — the TEAM-06 shared-fixture foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract YAML → reference & platform servers | Advertised tool surface both consumers verify against (TEAM-06) |
| conformance fixture (test data) → CI assertions | Fixtures assert the surface; drift between fixtures and contract is caught by the test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-06 | Tampering | contract drift (advertised ≠ enforced tool schema) | mitigate | `tests/team_contracts_conformance.rs` cross-references every fixture tool name against the contract YAML; a fixture referencing an uncaptured tool fails CI |
| T-107-07 | Spoofing / Elevation | team-mcp member dispatch | mitigate (documented in contract) | team_dispatch_surface invariants encode self-call guard (ids not names), ancestor-cycle guard, and strict x-pmcp-team-depth parse — the correct dispatch contract for Phase 109 to implement against |
| T-107-08 | Repudiation | premature bindings to nonexistent functions | accept | No binding.yaml authored (A3); bindings deferred to Phase 109/TEAM-06 so `pmat comply` is not gated on unresolved functions |
</threat_model>

<verification>
- `cargo test --test team_contracts_conformance` passes.
- `contracts/team-servers-v1.yaml` parses as YAML with the four expected equation keys.
- Every fixture references a tool captured in the contract.
- `make quality-gate` (repo-wide) green before phase gate.
</verification>

<success_criteria>
The four team-server tool surfaces and team-mcp dispatch semantics are captured as a namespaced
provisional provable-contracts YAML, shared per-server request/response conformance fixtures exist,
and a dependency-free conformance test proves every fixture's tool surface is present in the
contract — with bindings correctly deferred to Phase 109.
</success_criteria>

<output>
Create `.planning/phases/107-contracts-package-format/107-03-SUMMARY.md` when done.
</output>
