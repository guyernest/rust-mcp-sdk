---
phase: 86-shapes-b-c-d-scaffold-library-example-deploy
plan: 04
type: execute
wave: 4
depends_on: [86-03]
files_modified:
  - cargo-pmcp/tests/scaffold_sql_server.rs
  - cargo-pmcp/Cargo.toml
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "A tempdir scaffold via `new --kind sql-server` builds and `cargo run`s against an embedded SQLite"
    - "The spawned server answers tools/list and at least one tools/call"
    - "The unpublished pmcp-server-toolkit 0.1.0 is resolvable in the tempdir via a [patch.crates-io]/path override (Pitfall §1)"
  artifacts:
    - path: "cargo-pmcp/tests/scaffold_sql_server.rs"
      provides: "TEST-05 end-to-end: scaffold → patch deps → cargo run → poll → tools/list + tools/call"
      contains: "test_tools_list"
  key_links:
    - from: "scaffold_sql_server.rs tempdir Cargo.toml"
      to: "in-repo path crates (pmcp, pmcp-server-toolkit)"
      via: "[patch.crates-io] override (Pitfall §1 / Assumption A1)"
      pattern: "patch.crates-io"
    - from: "scaffold_sql_server.rs"
      to: "spawned cargo run subprocess HTTP port"
      via: "20-attempt readiness poll (parity_chinook.rs idiom)"
      pattern: "test_initialize"
---

<objective>
Build TEST-05: an end-to-end integration test that scaffolds a project with
`cargo pmcp new --kind sql-server` into a tempdir, makes the unpublished
`pmcp-server-toolkit 0.1.0` resolvable via a `[patch.crates-io]`/path override
(RESEARCH Pitfall §1, Assumption A1 — the crate is not yet on crates.io), shells out to
a REAL `cargo run` subprocess in that tempdir (SC-1's "verified end-to-end" promise —
NOT an in-process compile), polls for HTTP readiness using the verbatim
`parity_chinook.rs` 20-attempt backoff loop, then asserts `tools/list` shows the curated
tool and one `tools/call` succeeds. `--test-threads=1` (ephemeral port + process env,
Pitfall §5).

Purpose: prove the Shape B scaffold actually runs and serves out of the box.
Output: `cargo-pmcp/tests/scaffold_sql_server.rs` (CI-runnable, zero creds).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-03-SUMMARY.md

<interfaces>
<!-- spawn-poll-tester pattern + the tempdir dep-resolution fix. -->
From crates/pmcp-sql-server/tests/parity_chinook.rs (verified, lines 113-211 — copy the poll verbatim):
```rust
let mut tester = ServerTester::new(&url, Duration::from_secs(30), false, None, Some("http"), None)?;
for attempt in 0..20u32 {
    if matches!(tester.test_initialize().await.status, mcp_tester::report::TestStatus::Passed) { break; }
    tokio::time::sleep(Duration::from_millis(50 * u64::from(attempt + 1))).await;
}
```
mcp-tester: test_tools_list() (tester.rs:1343) ; test_tool(name, args) (tester.rs:1501).
Pitfall §1: tempdir Cargo.toml needs `[patch.crates-io] pmcp = { path = "<repo>" }` + `pmcp-server-toolkit = { path = "<repo>/crates/pmcp-server-toolkit" }` (+ any transitive: pmcp-code-mode, pmcp-widget-utils) so `cargo run` resolves the unpublished 0.1.0. Repo root = env!("CARGO_MANIFEST_DIR") joined up from cargo-pmcp/.
Existing cargo-pmcp dev-deps to mirror (Cargo.toml): mcp-tester (path), tempfile, tokio.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tempdir scaffold + [patch.crates-io] override + subprocess cargo run + poll + tools/list + tools/call</name>
  <read_first>
    - crates/pmcp-sql-server/tests/parity_chinook.rs (the canonical spawn → readiness-poll → ServerTester drive pattern, lines 113-211; the tempfile::tempdir + ephemeral-port idiom lines 113-163)
    - crates/mcp-tester/src/tester.rs (ServerTester::new signature; test_initialize; test_tools_list :1343; test_tool :1501)
    - cargo-pmcp/Cargo.toml (the file being modified — read `[dev-dependencies]` to confirm/add mcp-tester (path), tempfile, tokio with rt-multi-thread+macros; and the `[[test]]` registration convention if any)
    - cargo-pmcp/src/templates/sql_server.rs (the emitter under test — confirm the emitted Cargo.toml dep names/versions the patch override must shadow; Plan 03 output)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "cargo-pmcp/tests/scaffold_sql_server.rs (NEW)" lines 114-149 — the KEY DIFFERENCE note: subprocess cargo run + patch override + --test-threads=1)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Pitfall §1 dep resolution; Code Examples §4 spawn+poll; Pitfall §5 --test-threads=1)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-03-SUMMARY.md (confirm the emitted file names, default bind addr, and whether main.rs prints its bound addr to stdout)
  </read_first>
  <action>
    Create cargo-pmcp/tests/scaffold_sql_server.rs as a `#[tokio::test]` (multi-thread): (1) `tempfile::tempdir()`; (2) invoke the scaffold emitter — either call `cargo_pmcp::commands::new::execute(name, Some(tmp_path), None, Some("sql-server".into()), &flags)` in-process OR shell `cargo run -p cargo-pmcp -- new --kind sql-server <name>` with `current_dir(tmp)`; (3) APPEND a `[patch.crates-io]` section to the emitted `<tmp>/<name>/Cargo.toml` pointing `pmcp` + `pmcp-server-toolkit` (and any transitive path deps the build needs: `pmcp-code-mode`, `pmcp-widget-utils`) at the in-repo crates resolved from `env!("CARGO_MANIFEST_DIR")` (Pitfall §1) — read the current Cargo.toml, append the patch block, write it back; (4) spawn `std::process::Command::new("cargo").args(["run"]).current_dir(<crate dir>)` capturing stdout, and parse the bound addr the scaffold prints (per Plan 03 SUMMARY) OR use the scaffold's documented default port; (5) build the `url` and run the parity_chinook.rs 20-attempt readiness poll VERBATIM; (6) `assert!(initialized)`; (7) assert `tester.test_tools_list().await` includes the curated `list_books` tool; (8) assert `tester.test_tool("list_books", serde_json::json!({"limit": 5})).await` succeeds; (9) kill the subprocess + let tempdir auto-clean. Ensure cargo-pmcp `[dev-dependencies]` has mcp-tester (path), tempfile, tokio (rt-multi-thread, macros), serde_json — add any missing. Document in a top-of-file comment that this test requires `--test-threads=1` (Pitfall §5) and that it builds the scaffold crate (slow; first run compiles the toolkit).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test scaffold_sql_server -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cargo test -p cargo-pmcp --test scaffold_sql_server -- --test-threads=1` exits 0.
    - Behavior assertion: the test spawns a REAL `cargo run` subprocess in the tempdir (NOT an in-process `run_serving`) — `grep -n "Command::new" cargo-pmcp/tests/scaffold_sql_server.rs` present; the test does NOT call `run_serving`.
    - Source assertion: the test writes a `[patch.crates-io]` block into the tempdir Cargo.toml pointing at the in-repo path crates (Pitfall §1) — `grep -n "patch.crates-io" cargo-pmcp/tests/scaffold_sql_server.rs`.
    - Behavior assertion: the test asserts BOTH `test_tools_list` (shows `list_books`) AND `test_tool("list_books", ...)` succeeds (TEST-05 requires tools/list + one tools/call).
    - The readiness poll is the parity_chinook.rs 20-attempt linear-backoff loop (no fixed sleep).
  </acceptance_criteria>
  <done>TEST-05 scaffolds, patches deps, runs the real binary, polls readiness, and proves tools/list + tools/call end-to-end in CI with zero creds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → spawned scaffold HTTP server | the test acts as an MCP client crossing into the server's tools surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04-01 | Tampering | tempdir Cargo.toml patch override pointing at arbitrary paths | accept | The patch paths are derived from `env!("CARGO_MANIFEST_DIR")` (this repo), not external input; the tempdir is auto-cleaned. Test-only code. |
| T-86-04-02 | Denial of Service | leaked subprocess if an assertion panics before kill | mitigate | Spawn the child with a guard/`Drop` that kills it (or kill before every early-return assert); bind `127.0.0.1:0` ephemeral so a leaked port does not collide; `--test-threads=1` serializes. |
| T-86-04-03 | Information Disclosure | scaffold's inline DEV token_secret surfaced in test logs | accept | The secret is the public DEV-ONLY literal already in the emitted template (D-06); no real credential is involved. |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --test scaffold_sql_server -- --test-threads=1` passes.
- Subprocess `cargo run` (not in-process); `[patch.crates-io]` override present; tools/list + tools/call asserted.
</verification>

<success_criteria>
- TEST-05 proves `cargo pmcp new --kind sql-server` → `cargo run` → tools/list + tools/call end-to-end in a tempdir (SC-1, TEST-05).
- Zero creds, CI-runnable; the unpublished toolkit resolves via the patch override.
</success_criteria>

<output>
After completion, create `.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-04-SUMMARY.md`
</output>
