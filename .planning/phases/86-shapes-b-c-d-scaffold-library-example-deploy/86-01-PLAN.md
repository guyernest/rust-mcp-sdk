---
phase: 86-shapes-b-c-d-scaffold-library-example-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-server-toolkit/src/sql/sqlite.rs
  - crates/pmcp-server-toolkit/src/sql/mod.rs
  - crates/pmcp-server-toolkit/Cargo.toml
autonomous: true
requirements: [SHAP-B-01, SHAP-C-01]
must_haves:
  truths:
    - "A multi-statement schema.sql (DDL + INSERTs) bootstraps a SQLite DB in ONE call"
    - "The new batch helper has a runnable doctest and a unit test (ALWAYS coverage)"
    - "The deploy single-crate resolution question (RESEARCH Open Q#2) is answered before Shape D is planned-into-code"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/sql/sqlite.rs"
      provides: "SqliteConnector::execute_batch multi-statement bootstrap helper"
      contains: "execute_batch"
    - path: "crates/pmcp-server-toolkit/Cargo.toml"
      provides: "tokio feature set adequate for a serving #[tokio::main] example (macros + rt-multi-thread available to examples via dev-deps)"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/sql/sqlite.rs::execute_batch"
      to: "rusqlite::Connection::execute_batch"
      via: "spawn_blocking closure mirroring SqliteConnector::execute"
      pattern: "execute_batch"
---

<objective>
Add the single load-bearing piece of genuinely-new toolkit logic this phase needs:
a `SqliteConnector::execute_batch` helper that runs a multi-statement `schema.sql`
(DDL + INSERTs) in ONE call. RESEARCH Pitfall §3 confirms `SqlConnector::execute` is
single-statement (`prepare` handles ONE statement); without a batch helper the
≤15-line Shape C wiring (Plan 02) cannot bootstrap the demo DB inside the line budget.
Also confirm the tokio dev-dep feature set lets a serving `#[tokio::main]` example
compile, and resolve RESEARCH Open Question #2 (does the deploy `find_lambda_package_dir`
resolve a single-crate layout) so Plan 05 can be implemented without rediscovery.

Purpose: unblock the ≤15-line budget (D-07) and the on-first-run DB bootstrap (D-04);
de-risk Shape D before it is coded.
Output: a tested `execute_batch` helper (doctest + unit test), the example's tokio
feature availability confirmed, and a recorded answer to the deploy-resolution spike.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md

<interfaces>
<!-- The exact shape execute_batch must mirror. Source: sqlite.rs verified this session. -->
From crates/pmcp-server-toolkit/src/sql/sqlite.rs:
```rust
pub struct SqliteConnector { conn: Arc<Mutex<Connection>> }
impl SqliteConnector {
    pub fn open(path: &Path) -> Result<Self, ConnectorError>;          // line 72
    pub fn open_in_memory() -> Result<Self, ConnectorError>;           // line 88
}
// SqlConnector::execute (line ~202) — the spawn_blocking + Arc<Mutex<Connection>> shape to mirror:
async fn execute(&self, sql: &str, params: &[(String, Value)]) -> Result<Vec<Value>, ConnectorError> {
    let conn = Arc::clone(&self.conn);
    let sql = sql.to_string();
    tokio::task::spawn_blocking(move || -> Result<_, ConnectorError> {
        let guard = conn.lock().map_err(|_| ConnectorError::Driver("mutex poisoned".into()))?;
        // ... single-statement prepare/bind/collect ...
    }).await.map_err(|e| ConnectorError::Driver(format!("join error: {e}")))?
}
```
ConnectorError variants available: Driver, Query, Connection, ParameterBind (#[non_exhaustive]).
Toolkit tokio dep (Cargo.toml line 44): `tokio = { features = ["sync", "rt"] }` — NOT macros/rt-multi-thread.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SqliteConnector::execute_batch multi-statement helper + doctest + unit test</name>
  <read_first>
    - crates/pmcp-server-toolkit/src/sql/sqlite.rs (the file being modified; read open/open_in_memory lines 63-95 and execute lines ~202-228 — the spawn_blocking + Arc<Mutex<Connection>> closure shape to mirror; read the doctest convention at lines ~30-41)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "crates/pmcp-server-toolkit/src/sql/sqlite.rs (MOD)" lines 283-303 — the analog + the "mirror execute, call execute_batch" excerpt; CONFIRMED ABSENT note)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Pitfall §3 — why single-statement execute can't run schema.sql)
  </read_first>
  <behavior>
    - Test 1 (doctest): open_in_memory() → execute_batch("CREATE TABLE t(id INTEGER); INSERT INTO t VALUES (1); INSERT INTO t VALUES (2);") → execute("SELECT COUNT(*) AS c FROM t", &[]) returns 2 rows-worth (count = 2). Proves multiple statements ran in one call.
    - Test 2 (unit): a multi-statement batch with 2 CREATE TABLE + 3 INSERT statements succeeds; a follow-up SELECT against each table returns the seeded rows.
    - Test 3 (unit): a batch containing a syntactically-invalid statement returns Err(ConnectorError::Query(_)) (not a panic) — robustness invariant per CLAUDE.md ZERO-tolerance.
  </behavior>
  <action>
    Add `pub async fn execute_batch(&self, sql: &str) -> Result<(), ConnectorError>` to the `impl SqliteConnector` block in crates/pmcp-server-toolkit/src/sql/sqlite.rs (an inherent method on the concrete connector — NOT on the SqlConnector trait, to avoid touching the locked 3-method trait surface from CONN-01). Mirror the existing `execute` body EXACTLY for concurrency: `Arc::clone(&self.conn)`, `sql.to_string()`, `tokio::task::spawn_blocking(move || { let guard = conn.lock().map_err(|_| ConnectorError::Driver("mutex poisoned".into()))?; guard.execute_batch(&sql).map_err(|e| ConnectorError::Query(e.to_string()))?; Ok(()) }).await.map_err(|e| ConnectorError::Driver(format!("join error: {e}")))?`. Call `rusqlite::Connection::execute_batch` (runs multiple `;`-separated statements in one call). Gate the whole method with `#[cfg(feature = "sqlite")]` consistent with the surrounding file. Write the rustdoc with a `# Errors` section (mirror open's doc at lines 64-71) AND a `rust` doctest matching Test 1 above using the `cargo run --features sqlite` doctest convention already in the file header. Add the two `#[tokio::test]` unit tests (Test 2, Test 3) in the existing `#[cfg(test)] mod tests` block at the bottom of sqlite.rs (grep for `mod tests`). Do NOT add a SATD/TODO comment. Keep the function cog complexity ≤25 (it is a single linear closure — trivially under).
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features sqlite,code-mode execute_batch -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "pub async fn execute_batch" crates/pmcp-server-toolkit/src/sql/sqlite.rs` returns exactly one match inside the `impl SqliteConnector` block.
    - Source assertion: the method body calls `execute_batch` on the rusqlite guard (NOT `prepare`/`execute`) and wraps it in `spawn_blocking`.
    - Behavior: `cargo test -p pmcp-server-toolkit --features sqlite,code-mode execute_batch -- --test-threads=1` exits 0 (unit tests pass).
    - Behavior: `cargo test --doc -p pmcp-server-toolkit --features sqlite,code-mode -- sqlite` exits 0 (the new doctest compiles + runs).
    - The SqlConnector trait definition in sql/mod.rs is UNCHANGED (still 3 methods) — `git diff crates/pmcp-server-toolkit/src/sql/mod.rs` shows no trait-method addition (only a possible re-export line if needed).
  </acceptance_criteria>
  <done>execute_batch exists as a tested inherent method on SqliteConnector; doctest + 2 unit tests pass; the locked SqlConnector 3-method trait is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm tokio feature availability for a serving example + record deploy single-crate resolution spike answer</name>
  <read_first>
    - crates/pmcp-server-toolkit/Cargo.toml (the file being modified; read the `[dependencies] tokio` line 44 — features ["sync","rt"]; the `[features]` block lines 58-66; the `[[example]]` blocks lines 70-76; any `[dev-dependencies]`)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "crates/pmcp-server-toolkit/Cargo.toml (MOD)" lines 307-327 — the tokio-feature-set caveat)
    - cargo-pmcp/src/deployment/builder.rs (read `find_lambda_package_dir` at line 312 and `bundle_assets_if_configured` at line 462 — to answer whether a single-crate layout resolves; this is the spike, read-only)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Open Question §2 — the exact unknown to resolve)
  </read_first>
  <action>
    (a) Ensure a serving `#[tokio::main]` example (Plan 02) will compile: add `tokio = { version = "1", features = ["macros", "rt-multi-thread"] }` to `[dev-dependencies]` in crates/pmcp-server-toolkit/Cargo.toml if not already present (examples are built with dev-deps; the main `[dependencies]` tokio stays `["sync","rt"]` — do NOT widen the published lib's tokio surface). Register the Plan 02 example slot now so the manifest is ready: append an `[[example]]` block `name = "sql_server_http"`, `required-features = ["sqlite", "code-mode"]` mirroring the existing `sqlite_minimal` block (the example FILE is created in Plan 02; registering the slot here keeps the manifest change isolated to this plan's file ownership). (b) SPIKE (read-only, record findings in the SUMMARY — do NOT modify deploy code here): trace `find_lambda_package_dir` (builder.rs:312) and `bundle_assets_if_configured` (builder.rs:462) and record in the SUMMARY whether a single-crate config-driven layout (one Cargo.toml at the project root that IS the lambda package) resolves correctly, and where the detection seam should plug into `execute_async` (deploy/mod.rs:549). This answer is the input Plan 05 consumes.
  </action>
  <verify>
    <automated>cargo build -p pmcp-server-toolkit --features sqlite,code-mode 2>&1 | tail -5; grep -c 'name = "sql_server_http"' crates/pmcp-server-toolkit/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -A2 'name = "sql_server_http"' crates/pmcp-server-toolkit/Cargo.toml` shows `required-features = ["sqlite", "code-mode"]`.
    - Source assertion: `[dev-dependencies]` in the toolkit Cargo.toml provides tokio `macros` + `rt-multi-thread` (grep for `rt-multi-thread`), OR the SUMMARY documents that the existing dev-dep set already covers it.
    - The published `[dependencies]` tokio line is UNCHANGED (still `["sync", "rt"]`) — `git diff` shows no widening of the lib's tokio features.
    - Behavior: `cargo build -p pmcp-server-toolkit --features sqlite,code-mode` succeeds (manifest still valid; the not-yet-created example slot does not break a plain build).
    - SUMMARY records: (1) yes/no does `find_lambda_package_dir` resolve a single-crate layout, (2) the exact line/arm in `execute_async` where Plan 05's detection seam goes.
  </acceptance_criteria>
  <done>Example slot registered with correct features; serving example's tokio deps available via dev-deps without widening the lib; deploy single-crate resolution + seam placement recorded for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.sql file → SqliteConnector::execute_batch | DDL/INSERT text read from disk and executed against the demo DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01-01 | Tampering | execute_batch SQL text | accept | execute_batch runs trusted developer-authored schema.sql (the scaffold's own bundled DDL), NOT untrusted client input — no parameterization needed for a batch bootstrap; client query params still flow through the parameterized `execute` path (T-84-04-01), unchanged here. |
| T-86-01-02 | Information Disclosure | ConnectorError from a bad batch | mitigate | Errors map to `ConnectorError::Query(e.to_string())` whose Display is the bounded rusqlite message; the helper does not echo the full schema.sql text or any file path into the error (consistent with T-84-04-02 path-free errors). |
| T-86-01-03 | Denial of Service | spawn_blocking pool exhaustion on a huge batch | accept | Single bootstrap call at startup, demo-scale schema; not a per-request path. Low risk. |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --features sqlite,code-mode -- --test-threads=1` passes (incl. execute_batch unit tests).
- `cargo test --doc -p pmcp-server-toolkit --features sqlite,code-mode` passes (execute_batch doctest).
- `cargo build -p pmcp-server-toolkit --features sqlite,code-mode` succeeds.
- SqlConnector trait (sql/mod.rs) unchanged — 3 methods.
</verification>

<success_criteria>
- execute_batch helper exists, is tested (doctest + 2 unit tests), keeps the ≤15-line Shape C bootstrap to one line.
- Toolkit manifest carries the sql_server_http example slot + adequate dev-dep tokio features.
- Deploy single-crate resolution + detection-seam placement answered in the SUMMARY (Plan 05 input).
</success_criteria>

<output>
After completion, create `.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-01-SUMMARY.md`
</output>
