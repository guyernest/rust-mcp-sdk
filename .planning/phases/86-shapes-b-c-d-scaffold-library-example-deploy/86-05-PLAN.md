---
phase: 86-shapes-b-c-d-scaffold-library-example-deploy
plan: 05
type: execute
wave: 4
depends_on: [86-01, 86-03]
files_modified:
  - cargo-pmcp/src/commands/deploy/mod.rs
  - cargo-pmcp/src/templates/sql_server.rs
  - cargo-pmcp/tests/deploy_config_driven.rs
autonomous: true
requirements: [SHAP-D-01]
must_haves:
  truths:
    - "`cargo pmcp deploy` detects a config-driven project (config.toml + schema.sql + toolkit dep) and routes to the per-project Lambda build"
    - "The scaffold emits a deploy.toml carrying [assets] include = config.toml + schema.sql so the existing bundler picks them up"
    - "The PmcpRun target is reused via detection with ZERO TargetEntry enum changes (D-10)"
  artifacts:
    - path: "cargo-pmcp/src/commands/deploy/mod.rs"
      provides: "config-driven-project detection seam in execute_async (additive branch)"
      contains: "config_driven"
    - path: "cargo-pmcp/src/templates/sql_server.rs"
      provides: "emitted deploy.toml with [assets] include = config.toml + schema.sql"
      contains: "assets"
    - path: "cargo-pmcp/tests/deploy_config_driven.rs"
      provides: "detection unit test + TargetEntry-unchanged regression guard"
      contains: "TargetEntry"
  key_links:
    - from: "deploy::execute_async detection seam"
      to: "get_target_id then bundle_assets_if_configured"
      via: "detection (config.toml + schema.sql + toolkit dep), NOT a new enum variant"
      pattern: "bundle_assets_if_configured|get_target_id"
    - from: "scaffold deploy.toml [assets]"
      to: "Lambda /var/task/assets/<db> at runtime via pmcp::assets"
      via: "bundle_assets_if_configured zips config.toml + schema.sql"
      pattern: "assets"
---

<objective>
Build Shape D: make `cargo pmcp deploy` handle a config-driven server with ZERO new deploy
subsystem code and ZERO `TargetEntry` enum changes (D-09, D-10, SHAP-D-01). The bundling
machinery already exists (`bundle_assets_if_configured` zips bootstrap + `[assets]` globs;
`get_target_id` selects `pmcp-run` by detection). This plan: (1) adds a config-driven-project
detection seam to `execute_async` (per the Plan 01 spike's recorded seam placement); (2) makes
the Plan 03 scaffold emit a `deploy.toml` carrying `[assets] include = ["config.toml", "schema.sql"]`
so the existing bundler picks them up, with the deployed `file_path` pointing at
`/var/task/assets/<db>` (Pitfall §6); (3) adds a detection unit test + a `TargetEntry`-unchanged
regression guard (D-10). The real deploy round-trip is exercised by the env-gated Plan 06 (TEST-06).

Purpose: config-only server deploys to pmcp.run reusing the existing pipeline, no breaking changes.
Output: detection seam + scaffold deploy.toml asset wiring + detection/regression tests.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-01-SUMMARY.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-03-SUMMARY.md

<interfaces>
<!-- The existing deploy seam (UNCHANGED machinery to plug into). Sources verified this session. -->
From cargo-pmcp/src/commands/deploy/mod.rs (verified):
```rust
async fn execute_async(&self, global_flags: &GlobalFlags) -> Result<()> {   // line 549
    let project_root = find_workspace_root()?;
    let target_id = self.get_target_id(&project_root)?;     // line 553 — flag > deploy.toml > "aws-lambda"
    let target = registry.get(&target_id)?;
    // None arm (~880-947): target.build(&config).await? -> target.deploy(...).await? -> run_post_deploy_tests(...)
}
fn get_target_id(&self, project_root: &PathBuf) -> Result<String> { ... }   // line 1017 — D-10: reuse, do NOT modify
```
From cargo-pmcp/src/deployment/builder.rs (verified — UNCHANGED machinery):
```rust
fn find_lambda_package_dir(&self, server_name: &str) -> Result<PathBuf>;     // line 312
fn bundle_assets_if_configured(&self, binary_path: &Path) -> Result<Option<PathBuf>>;  // line 462 — reads [assets] include + special-cases config.toml
```
From cargo-pmcp/src/commands/configure/config.rs:
```rust
enum TargetEntry { PmcpRun(..), AwsLambda(..), GoogleCloudRun(..), CloudflareWorkers(..) }  // D-10: MUST be unchanged
```
AssetsConfig (deployment/config.rs:374-427): include: Vec<String>, base_dir, has_assets(), resolve_files(). Lambda extracts assets/<rel> to $LAMBDA_TASK_ROOT/assets/ (builder.rs:521); config.toml special-cased (builder.rs:516-518). pmcp::assets base = /var/task on Lambda (loader.rs:95-99).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config-driven-project detection seam to execute_async + emit deploy.toml [assets] in the scaffold</name>
  <read_first>
    - cargo-pmcp/src/commands/deploy/mod.rs (the file being modified; read `execute_async` lines 549-560 + the None build/deploy/verify arm ~880-947; `get_target_id` lines 1017-1030 — REUSE, do NOT modify the enum)
    - cargo-pmcp/src/deployment/builder.rs (read `find_lambda_package_dir` :312 and `bundle_assets_if_configured` :462 — the UNCHANGED machinery; confirm single-crate resolution per Plan 01 spike)
    - cargo-pmcp/src/deployment/config.rs (read `AssetsConfig` lines 374-427 — include/base_dir/has_assets/resolve_files)
    - cargo-pmcp/src/templates/sql_server.rs (the file being modified — Plan 03's emitter; add a `generate_deploy_toml` fn to it + wire into its `generate` orchestrator)
    - crates/pmcp-sql-server/tests/fixtures/reference-config.toml (read the `file_path = "/var/task/assets/chinook.db"` line ~37 — the Lambda asset-path convention to emit, Pitfall §6)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "cargo-pmcp/src/commands/deploy/mod.rs (MOD)" lines 250-279 — detection-based, zero enum change; Pattern 3 lines 224-227)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (D-09/D-10; Pitfall §6 asset path; Open Question §2 detection-seam placement)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-01-SUMMARY.md (the recorded answer: does find_lambda_package_dir resolve a single-crate layout + exact seam placement)
  </read_first>
  <action>
    (a) Detection seam in cargo-pmcp/src/commands/deploy/mod.rs: at the location the Plan 01 spike recorded (in/near `execute_async` before `target.build`), add a small `fn is_config_driven_project(project_root: &Path) -> bool` returning true when `config.toml` AND `schema.sql` exist at the project root AND `Cargo.toml` references `pmcp-server-toolkit` (the D-09 heuristic). Use it to emit an informational note and ensure the existing `[assets]`-driven bundling path runs for the single-crate layout. Do NOT add a new `TargetEntry` variant, a new `PmcpRunEntry` field, or a new DeploymentTarget impl (Anti-Pattern / D-10). Reuse `get_target_id` (PmcpRun by detection) and the existing `target.build -> target.deploy -> run_post_deploy_tests` flow UNCHANGED. If the Plan 01 spike found `find_lambda_package_dir` does NOT resolve the single-crate layout, add only the minimal additive fix it recommended (still no enum change). Keep the new fn cog complexity ≤25. (b) Scaffold deploy.toml: add a `generate_deploy_toml(dir, name)` fn to cargo-pmcp/src/templates/sql_server.rs (called from its `generate` orchestrator) emitting a `deploy.toml` with the target wiring + an `[assets]` block `include = ["config.toml", "schema.sql"]` so `bundle_assets_if_configured` bundles them. Deploy posture: `token_secret` sourced from a secrets ref (`env:CODE_MODE_SECRET`) — NOT the inline dev default (D-06 deploy path); add a comment in the emitted deploy.toml that on Lambda the SQLite `file_path` resolves under `/var/task/assets/` (Pitfall §6, matching reference-config.toml).
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp 2>&1 | tail -5; cd /tmp && rm -rf depscaf && cargo run -p cargo-pmcp --manifest-path /Users/guy/Development/mcp/sdk/rust-mcp-sdk/cargo-pmcp/Cargo.toml -- new --kind sql-server depscaf && grep -q assets depscaf/deploy.toml && grep -q config.toml depscaf/deploy.toml && grep -q schema.sql depscaf/deploy.toml && echo DEPLOY_TOML_OK</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the verify command prints `DEPLOY_TOML_OK` (emitted deploy.toml has `[assets] include` with both config.toml and schema.sql).
    - Source assertion: `cargo-pmcp/src/commands/deploy/mod.rs` adds an `is_config_driven_project`-style detection fn and does NOT add a `TargetEntry` variant or `PmcpRunEntry` field (D-10).
    - Source assertion: emitted deploy.toml sources `token_secret` from `env:CODE_MODE_SECRET` (NOT the inline dev literal) and comments the `/var/task/assets/` Lambda path (Pitfall §6).
    - Behavior: `cargo build -p cargo-pmcp` succeeds.
  </acceptance_criteria>
  <done>Deploy detects config-driven projects and bundles config.toml + schema.sql via the existing assets pipeline; the scaffold emits the deploy.toml that drives it; no enum change.</done>
</task>

<task type="auto">
  <name>Task 2: Detection unit test + TargetEntry-unchanged regression guard (D-10)</name>
  <read_first>
    - cargo-pmcp/src/commands/configure/config.rs (read the `TargetEntry` enum + its variants — the regression guard asserts these are exactly PmcpRun/AwsLambda/GoogleCloudRun/CloudflareWorkers, unchanged)
    - cargo-pmcp/src/commands/deploy/mod.rs (the `is_config_driven_project` fn added in Task 1 — under test)
    - cargo-pmcp/tests/widgets_orchestrator.rs (an existing cargo-pmcp integration-test file for structure/conventions)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-VALIDATION.md (the SHAP-D-01 row: "unit (detection) + integration (bundle) + regression assert")
  </read_first>
  <action>
    Create cargo-pmcp/tests/deploy_config_driven.rs with: (1) a `#[test] detects_config_driven_project` that builds a tempdir with `config.toml` + `schema.sql` + a `Cargo.toml` mentioning `pmcp-server-toolkit` and asserts the detection predicate returns true; plus a negative case (missing schema.sql → false). If `is_config_driven_project` is private, either make it `pub(crate)` and reach it via a small test-only re-export, or assert the behavior through a thin public wrapper — do NOT loosen visibility beyond `pub(crate)`. (2) a `#[test] target_entry_enum_unchanged` regression guard (D-10) that exhaustively `match`es a `TargetEntry` value over exactly the four variants `PmcpRun | AwsLambda | GoogleCloudRun | CloudflareWorkers` so that ADDING or RENAMING a variant breaks compilation — a compile-time guard that the enum was not modified. Add a comment citing D-10. (3) (optional, if cheap) assert the emitted scaffold deploy.toml's `AssetsConfig.include` parses to `["config.toml", "schema.sql"]` via `DeployConfig::load`.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test deploy_config_driven -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cargo test -p cargo-pmcp --test deploy_config_driven -- --test-threads=1` exits 0.
    - Behavior assertion: `detects_config_driven_project` passes for a config.toml+schema.sql+toolkit-dep tempdir and is false when schema.sql is absent.
    - Source assertion: `target_entry_enum_unchanged` exhaustively matches exactly the four `TargetEntry` variants (a compile-time D-10 guard) — `grep -n "PmcpRun\|AwsLambda\|GoogleCloudRun\|CloudflareWorkers" cargo-pmcp/tests/deploy_config_driven.rs`.
    - `git diff cargo-pmcp/src/commands/configure/config.rs` shows NO change to the TargetEntry enum (D-10).
  </acceptance_criteria>
  <done>Detection is unit-tested both ways and the TargetEntry enum is regression-guarded at compile time (D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local project files → deploy bundle → Lambda runtime | config.toml + schema.sql cross from dev machine into the deployed function's /var/task |
| deploy secrets resolution | token_secret/credentials resolved from env/Secrets Manager at deploy time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-05-01 | Information Disclosure | the inline DEV token_secret accidentally shipped to the deployed Lambda | mitigate | The emitted deploy.toml sources `token_secret` from a secrets ref (`env:CODE_MODE_SECRET`), NOT the inline dev default (D-06); deploy secrets resolve transiently (deploy/mod.rs:846) and are never written into deploy.toml. |
| T-86-05-02 | Information Disclosure | credential leak in deploy logs/errors | mitigate | Reuse the existing deploy pipeline whose connector errors are redacted at source (sanitize_url/strip_aws_credentials); add no new logging of secrets in the detection seam. |
| T-86-05-03 | Tampering | SQLite asset path mismatch causing a fallback/insecure DB open at runtime | mitigate | Emitted deploy posture documents `/var/task/assets/<db>` (Pitfall §6, matching reference-config.toml); the post-deploy `check` (Plan 06) catches a DB-open failure. |
| T-86-05-04 | Tampering | a hidden breaking change to the target system | mitigate | D-10 regression guard: `target_entry_enum_unchanged` fails compilation if any variant is added/renamed; no new variant/field is introduced. |
</threat_model>

<verification>
- `cargo build -p cargo-pmcp` succeeds; scaffold emits a deploy.toml with `[assets]` include of config.toml + schema.sql.
- `cargo test -p cargo-pmcp --test deploy_config_driven -- --test-threads=1` passes (detection + D-10 guard).
- TargetEntry enum unchanged; deploy posture uses a secrets ref.
</verification>

<success_criteria>
- `cargo pmcp deploy` packages a config-only server via the existing Lambda build + asset bundle, detection-based, with no breaking changes (SHAP-D-01).
- Scaffold emits the deploy.toml that drives bundling; D-10 enforced by a compile-time guard.
- Real deploy round-trip deferred to the env-gated Plan 06 (TEST-06).
</success_criteria>

<output>
After completion, create `.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-05-SUMMARY.md`
</output>
