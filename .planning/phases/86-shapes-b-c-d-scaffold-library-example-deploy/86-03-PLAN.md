---
phase: 86-shapes-b-c-d-scaffold-library-example-deploy
plan: 03
type: execute
wave: 3
depends_on: [86-02]
files_modified:
  - cargo-pmcp/src/main.rs
  - cargo-pmcp/src/commands/new.rs
  - cargo-pmcp/src/templates/mod.rs
  - cargo-pmcp/src/templates/sql_server.rs
  - cargo-pmcp/README.md
autonomous: true
requirements: [SHAP-B-01]
must_haves:
  truths:
    - "`cargo pmcp new --kind sql-server <name>` emits a SINGLE runnable crate (Cargo.toml + src/main.rs + config.toml + schema.sql)"
    - "The emitted src/main.rs is byte-identical (modulo paths) to the Plan 02 Shape C wiring"
    - "The emitted config.toml ships [code_mode] enabled=true with an inline DEV token_secret + allow_inline_token_secret_for_dev=true"
    - "The existing 526-line Rust sqlite-explorer template is untouched; the None-kind workspace path is unchanged"
    - "The cargo-pmcp README documents the new command surfaces this phase touches (new --kind sql-server, config-only deploy)"
  artifacts:
    - path: "cargo-pmcp/src/templates/sql_server.rs"
      provides: "Single-crate emitter (one generate_<file> fn per output, raw fs::write)"
      contains: "fn generate"
      min_lines: 40
    - path: "cargo-pmcp/src/commands/new.rs"
      provides: "--kind sql-server branch in execute"
      contains: "sql-server"
    - path: "cargo-pmcp/src/main.rs"
      provides: "kind arg on the New {} CLI variant threaded through dispatch"
      contains: "kind"
    - path: "cargo-pmcp/README.md"
      provides: "scoped docs for new --kind sql-server + config-only deploy (folded todo)"
      contains: "sql-server"
  key_links:
    - from: "cargo-pmcp/src/main.rs New {} variant"
      to: "commands::new::execute(..., kind, ...)"
      via: "dispatch at main.rs:519"
      pattern: "Commands::New"
    - from: "new::execute kind==Some(sql-server)"
      to: "templates::sql_server::generate"
      via: "single-crate branch (bypasses create_workspace_structure)"
      pattern: "sql_server::generate"
---

<objective>
Build Shape B: teach `cargo pmcp new` a `--kind sql-server` path (D-01, taken literally —
both SC-1 and TEST-05 name this exact command) that emits a SINGLE runnable crate
(`Cargo.toml` + `src/main.rs` + `config.toml` + `schema.sql`), distinct from `new`'s
current multi-crate workspace scaffolding. SQLite-only (D-02). The emitted `src/main.rs`
is a byte-identical copy of the Plan 02 Shape C wiring (SC-1: "scaffold main.rs IS the
Shape C wiring") — which is why Plan 02 ships first. The emitted `config.toml` ships
`[code_mode] enabled = true` + inline DEV `token_secret` + `allow_inline_token_secret_for_dev`
(D-06); `schema.sql` is the bundled DDL+INSERT bootstrap (D-04). The existing 526-line Rust
`sqlite-explorer` template stays UNTOUCHED as the escape-hatch (D-03), and the `None`-kind
workspace path is unchanged. Also folds the scoped cargo-pmcp README todo: document ONLY the
new command surfaces this phase touches (`new --kind sql-server`, config-only `deploy`) — the
broad README rewrite stays Phase 89.

Purpose: zero-Rust starter project that `cargo run`s out of the box.
Output: the `--kind sql-server` command surface + the single-crate template emitter + scoped docs.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-02-SUMMARY.md
@crates/pmcp-server-toolkit/examples/sql_server_http.rs

<interfaces>
<!-- The dispatch seam + emitter analog. Sources verified this session. -->
From cargo-pmcp/src/commands/new.rs (verified):
```rust
pub fn execute(name: String, path: Option<String>, tier: Option<String>, global_flags: &crate::commands::GlobalFlags) -> Result<()>;  // line 29
// existing guard (new.rs:62-64): if workspace_dir.exists() { anyhow::bail!("Directory '{}' already exists", ...) }
// existing None path: create_workspace_structure(...) -> templates::workspace::generate(...) -> templates::server_common::generate(...)
```
From cargo-pmcp/src/main.rs (verified):
```rust
New { name: String, #[arg(long)] path: Option<String> },     // line 92 — ADD #[arg(long)] kind: Option<String>
// dispatch line 519: Commands::New { name, path } => commands::new::execute(name, path, None, global_flags),
```
From cargo-pmcp/src/templates/workspace.rs (the emitter pattern to mirror):
```rust
pub fn generate(dir: &Path, name: &str) -> Result<()> { generate_cargo_toml(dir, name)?; ...; Ok(()) }
fn generate_cargo_toml(dir: &Path, _name: &str) -> Result<()> {
    let content = r#"..."#;  // or format!(r#"# {}"#, name) — NOTE: format! requires {{ }} to escape literal braces
    fs::write(dir.join("Cargo.toml"), content).context("Failed to create Cargo.toml")?;
    Ok(())
}
```
Scaffold Cargo.toml deps (86-RESEARCH Standard Stack — Assumption A6 published-version coupling):
pmcp = { version = "2.8.1", features = ["streamable-http"] }; pmcp-server-toolkit = { version = "0.1.0", features = ["code-mode","sqlite"] }; clap; tokio (macros, rt-multi-thread); tracing; tracing-subscriber (env-filter).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --kind arg on the New {} CLI variant + thread it through new::execute dispatch</name>
  <read_first>
    - cargo-pmcp/src/main.rs (the file being modified; read the `New {}` variant at lines 92-99 and the dispatch arm at line 519; the `Dev {}` variant at ~161-173 for the `#[arg(long)]` optional-field style to mirror)
    - cargo-pmcp/src/commands/new.rs (the file being modified; read `execute` lines 29-92 — the seam to branch; the directory-exists guard lines 62-64; `print_default_next_steps` ~160-191)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (sections "cargo-pmcp/src/main.rs (MOD)" lines 203-227 and "cargo-pmcp/src/commands/new.rs (MOD)" lines 177-199)
  </read_first>
  <action>
    In cargo-pmcp/src/main.rs add `#[arg(long)] kind: Option<String>` to the `New {}` variant (mirror the `path` arg style). Update the dispatch arm at main.rs:519 from `Commands::New { name, path } => commands::new::execute(name, path, None, global_flags)` to pass `kind` through — preferred: add `kind` as a new distinct param to `execute` and keep the existing `tier` slot at `None` for the workspace path: `commands::new::execute(name, path, None, kind, global_flags)`. In cargo-pmcp/src/commands/new.rs add the `kind: Option<String>` param to `execute`'s signature and, BEFORE `create_workspace_structure`, branch: `if kind.as_deref() == Some("sql-server") { return execute_sql_server(&workspace_dir, &name, global_flags); }` (reuse the existing directory-exists guard at lines 62-64 verbatim, so it runs before the branch). `execute_sql_server` is a new private fn that creates `<dir>/src`, calls `templates::sql_server::generate(&workspace_dir, &name)` (Task 2), and prints `cd <name> && cargo run` next-steps via a new `print_sql_server_next_steps` (gate prints behind `global_flags.should_output()`). Reject any unknown `--kind` value with an actionable `anyhow::bail!("unknown --kind '{}'; supported: sql-server", k)`. Do NOT alter the `None`-kind workspace path.
  </action>
  <verify>
    <automated>cargo build -p cargo-pmcp 2>&1 | tail -5; cargo run -p cargo-pmcp -- new --help 2>&1 | grep -i kind</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `cargo run -p cargo-pmcp -- new --help` lists a `--kind` option.
    - Source assertion: `grep -n "kind" cargo-pmcp/src/main.rs` shows the arg on the New variant AND threaded in the dispatch arm.
    - Source assertion: new.rs `execute` branches on `kind == Some("sql-server")` to a single-crate path and leaves `create_workspace_structure` (the None path) unchanged.
    - Behavior: `cargo run -p cargo-pmcp -- new --kind bogus foo` exits non-zero with the "unknown --kind" actionable error.
    - Behavior: `cargo build -p cargo-pmcp` succeeds.
  </acceptance_criteria>
  <done>--kind sql-server is a recognized command surface routing to the single-crate emitter; the workspace path and the Rust escape-hatch are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Single-crate template emitter (sql_server.rs) emitting the 4 files + scoped README docs</name>
  <read_first>
    - cargo-pmcp/src/templates/workspace.rs (the canonical emitter analog — `generate` orchestrator + one `generate_<file>` fn per output, each a raw `fs::write(dir.join("X"), <literal>).context(...)`; note format! `{{`/`}}` escaping for literal braces)
    - cargo-pmcp/src/templates/mod.rs (the module index — add `pub mod sql_server;`)
    - crates/pmcp-server-toolkit/examples/sql_server_http.rs (the EXACT Shape C wiring the emitted src/main.rs must reproduce byte-identically modulo file paths — created in Plan 02)
    - crates/pmcp-server-toolkit/examples/fixtures/config.toml (the config.toml shape to emit — Plan 02 fixture)
    - crates/pmcp-server-toolkit/examples/fixtures/schema.sql (the schema.sql shape to emit — Plan 02 fixture)
    - cargo-pmcp/README.md (the file being modified — read its existing command-reference structure to slot the new sections consistently)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "cargo-pmcp/src/templates/sql_server.rs (NEW)" lines 27-75; Shared Pattern: generated config.toml lines 367-401; Shared Pattern: the ≤15-line wiring lines 333-365)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Standard Stack Installation block lines 111-128 — the scaffold Cargo.toml deps + the BOTH code-mode AND sqlite features note; Assumption A6 published-version coupling)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-CONTEXT.md (Folded Todos — the scoped README todo: document ONLY the new command surfaces; broad rewrite is Phase 89)
  </read_first>
  <action>
    Create cargo-pmcp/src/templates/sql_server.rs mirroring workspace.rs: a `pub fn generate(dir: &Path, name: &str) -> Result<()>` orchestrator calling four private `generate_<file>` fns, each a single `fs::write(...).context(...)`. (1) `generate_cargo_toml` emits the single-crate `[package] name=<name>` + the `[dependencies]` block from 86-RESEARCH Standard Stack: `pmcp = { version = "2.8.1", features = ["streamable-http"] }`, `pmcp-server-toolkit = { version = "0.1.0", features = ["code-mode", "sqlite"] }` (BOTH features — D-06 needs code-mode, D-02 needs sqlite), clap (derive, env), tokio (macros, rt-multi-thread), tracing, tracing-subscriber (env-filter). Use `format!` to inject `name` (escape any literal `{}` as `{{`/`}}`). (2) `generate_main_rs` emits `src/main.rs` = the Plan 02 Shape C body verbatim, resolving config/schema/db paths relative to the crate root (e.g. `"config.toml"`, `"schema.sql"`, `"demo.db"`) rather than the example's CARGO_MANIFEST_DIR fixtures path — this is the only allowed deviation from byte-identity (D-07 "modulo paths"). (3) `generate_config_toml` emits the 86-PATTERNS generated-config: `[server]` name=<name>+version, `[database] type="sqlite" file_path="demo.db"`, `[[database.tables]]`, `[code_mode] enabled=true allow_writes=false require_limit=true max_limit=1000` + the loud `# DEV ONLY — replace with a secrets ref (token_secret = "env:CODE_MODE_SECRET") for production.` comment + `token_secret="dev-only-insecure-secret-min-16-bytes"` + `allow_inline_token_secret_for_dev=true` (Pitfall §4), and a `[[tools]]` `list_books` (SELECT ... LIMIT :limit) + `[[tools.parameters]]` limit. Emit ONLY deny_unknown_fields-known keys. (4) `generate_schema_sql` emits the books DDL + a few semicolon-free INSERT rows. Add `pub mod sql_server;` to cargo-pmcp/src/templates/mod.rs (alphabetically placed). Gate informational prints behind PMCP_QUIET / should_output (PATTERNS Shared Pattern: quiet-output guard). FINALLY (folded scoped README todo): add a short section to cargo-pmcp/README.md documenting ONLY the two new command surfaces this phase touches — `cargo pmcp new --kind sql-server <name>` (what 4 files it emits + the `cargo run` quick-start) and config-only `cargo pmcp deploy` (the detection-based config-only-server deploy, one paragraph). Do NOT rewrite the broader README (that is Phase 89).
  </action>
  <verify>
    <automated>cd /tmp && rm -rf scaf_test && cargo run -p cargo-pmcp --manifest-path /Users/guy/Development/mcp/sdk/rust-mcp-sdk/cargo-pmcp/Cargo.toml -- new --kind sql-server scaf_test && test -f scaf_test/Cargo.toml && test -f scaf_test/src/main.rs && test -f scaf_test/config.toml && test -f scaf_test/schema.sql && grep -q allow_inline_token_secret_for_dev scaf_test/config.toml && grep -q try_code_mode_from_config_with_connector scaf_test/src/main.rs && grep -q 'sql-server' /Users/guy/Development/mcp/sdk/rust-mcp-sdk/cargo-pmcp/README.md && echo EMIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the verify command prints `EMIT_OK` — all four files emitted (`Cargo.toml`, `src/main.rs`, `config.toml`, `schema.sql`).
    - Source assertion: emitted `config.toml` contains `allow_inline_token_secret_for_dev = true`, a ≥16-byte `token_secret`, and the loud DEV-ONLY comment (D-06).
    - Source assertion: emitted `src/main.rs` contains `try_code_mode_from_config_with_connector` AND `execute_batch` AND `StreamableHttpServer::with_config` (byte-identical to the Plan 02 wiring modulo paths).
    - Source assertion: emitted `Cargo.toml` pins `pmcp-server-toolkit` with `features = ["code-mode", "sqlite"]` (BOTH).
    - Source assertion: `cargo-pmcp/README.md` gains a section mentioning `new --kind sql-server` AND config-only `deploy` (folded scoped todo); the broad README is NOT rewritten.
    - Source assertion: `cargo-pmcp/src/templates/sqlite_explorer.rs` is UNCHANGED (`git diff --stat` empty — D-03 escape-hatch untouched).
  </acceptance_criteria>
  <done>The single-crate emitter produces a 4-file runnable starter whose main.rs is the Plan 02 Shape C wiring and whose config.toml ships the inline-dev code-mode posture; the new command surfaces are documented (scoped); the Rust escape-hatch is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `--kind`/`name` input → filesystem emission | user-supplied crate name written into paths + emitted Cargo.toml |
| emitted config.toml token_secret | the committed DEV secret in a generated starter project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03-01 | Information Disclosure | inline `token_secret` committed in the scaffold's config.toml | mitigate | The emitted secret is a LOUD `# DEV ONLY — replace with a secrets ref for production` literal (D-06) gated by `allow_inline_token_secret_for_dev = true`; the comment names the `env:CODE_MODE_SECRET` production path. Shape D deploy (Plan 05) sources from a secrets ref, never the inline default. |
| T-86-03-02 | Tampering | unsanitized `name` used in `dir.join(name)` (path traversal) | mitigate | Reuse `new`'s existing directory-exists guard (new.rs:62-64); `name` is a crate dir name; reject `--kind` values not in the allowlist (`sql-server` only). No further shell interpolation. |
| T-86-03-03 | Tampering | emitted config.toml weakening code-mode policy via a typo | mitigate | Emitted config uses ONLY deny_unknown_fields-known keys; any future typo becomes a hard parse error at the consumer (config.rs:101), surfaced on first `cargo run`. |
| T-86-03-04 | Elevation of Privilege | scaffold ships write/DDL-enabled code-mode by default | mitigate | Emitted `[code_mode] allow_writes = false` (default-deny deletes/ddl) — read-only-leaning demo defaults (Security Domain). |
</threat_model>

<verification>
- `cargo build -p cargo-pmcp` succeeds; `new --help` shows `--kind`.
- `cargo run -p cargo-pmcp -- new --kind sql-server <name>` emits 4 files; config.toml carries the inline-dev code-mode posture; main.rs is the Plan 02 wiring.
- README documents the two new command surfaces (scoped); `sqlite_explorer.rs` untouched (D-03); None-kind workspace path untouched.
</verification>

<success_criteria>
- `cargo pmcp new --kind sql-server` scaffolds a single runnable crate (SHAP-B-01).
- Emitted main.rs == Shape C wiring (modulo paths); config.toml ships D-06 posture; schema.sql bootstraps the demo DB.
- The Rust escape-hatch template is preserved (D-03); the new command surfaces are documented (folded scoped todo).
</success_criteria>

<output>
After completion, create `.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-03-SUMMARY.md`
</output>
