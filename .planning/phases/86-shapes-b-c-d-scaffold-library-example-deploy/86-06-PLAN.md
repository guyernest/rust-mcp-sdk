---
phase: 86-shapes-b-c-d-scaffold-library-example-deploy
plan: 06
type: execute
wave: 5
depends_on: [86-05]
files_modified:
  - cargo-pmcp/tests/deploy_config_only.rs
autonomous: true
requirements: [TEST-06]
must_haves:
  truths:
    - "A real pmcp.run config-only deploy runs the Phase 79 post-deploy lifecycle (check + conformance + apps) cleanly"
    - "The test is gated by PMCP_RUN_DEPLOY_TEST and SKIPS (prints reason + returns, never fails) when the gate is absent"
    - "Normal CI (no creds) stays green; the gated test is the SC-4 deliverable (D-11)"
  artifacts:
    - path: "cargo-pmcp/tests/deploy_config_only.rs"
      provides: "env-gated real pmcp.run deploy integration test asserting run_post_deploy_tests Ok"
      contains: "PMCP_RUN_DEPLOY_TEST"
  key_links:
    - from: "deploy_config_only.rs gate"
      to: "early-return skip when PMCP_RUN_DEPLOY_TEST absent"
      via: "npm_skip_gate idiom (widgets_orchestrator.rs:39)"
      pattern: "PMCP_RUN_DEPLOY_TEST"
    - from: "deploy_config_only.rs"
      to: "run_post_deploy_tests(check + conformance + apps)"
      via: "Phase 79 lifecycle assertion after a real deploy"
      pattern: "run_post_deploy_tests|post_deploy"
---

<objective>
Build TEST-06: an env-gated integration test that, ONLY when `PMCP_RUN_DEPLOY_TEST` is set
(and pmcp.run/AWS creds are present), scaffolds + deploys a config-only server to a REAL
pmcp.run target via the Plan 05 deploy path and asserts the Phase 79 post-deploy lifecycle
(`check` + `conformance` + `apps`) runs cleanly (D-11, SC-4). When the gate is absent the
test prints a reason and `return`s — it NEVER fails the suite (the `npm_skip_gate` idiom),
so normal CI stays green without creds. SC-4 explicitly permits a real target; this gated
test IS the deliverable (D-11 tension recorded: there is no always-on mock — do not add one).

Purpose: authentic proof that a config-only server deploys and passes the hosted lifecycle.
Output: `cargo-pmcp/tests/deploy_config_only.rs` (env-gated, skipped in normal CI).
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-05-SUMMARY.md

<interfaces>
<!-- gate idiom + the lifecycle to assert. Sources verified this session. -->
From cargo-pmcp/tests/widgets_orchestrator.rs (npm_skip_gate idiom, lines 36-45):
```rust
fn deploy_gate() -> Option<&'static str> {
    if std::env::var("PMCP_RUN_DEPLOY_TEST").is_ok() { None }
    else { Some("PMCP_RUN_DEPLOY_TEST not set — skipping real pmcp.run deploy integration test") }
}
// at test top: if let Some(reason) = deploy_gate() { eprintln!("{reason}"); return; }  // SKIP, do NOT fail
```
From cargo-pmcp/src/deployment/post_deploy_tests.rs (verified, line 791):
```rust
pub async fn run_post_deploy_tests(
    url: &str, target_id: &str /* "pmcp-run" */, widgets_present: bool,
    config: &PostDeployTestsConfig,  // .checks default = ["connectivity","conformance","apps"]
    quiet: bool,
) -> std::result::Result<(), OrchestrationFailure>;
```
Real deploy path = the Plan 05 `cargo pmcp deploy` flow (reused execute_async, no new deploy code).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Env-gated real pmcp.run config-only deploy + Phase 79 lifecycle assertion</name>
  <read_first>
    - cargo-pmcp/tests/widgets_orchestrator.rs (the `npm_skip_gate` early-return idiom, lines 36-45 — copy the shape, swap the env var to PMCP_RUN_DEPLOY_TEST)
    - cargo-pmcp/src/deployment/post_deploy_tests.rs (read `run_post_deploy_tests` at line 791 — the signature + the default checks ["connectivity","conformance","apps"] to assert ran clean)
    - cargo-pmcp/src/commands/deploy/mod.rs (the Plan 05 deploy path the test drives — reuse it, do NOT add new deploy code)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "cargo-pmcp/tests/deploy_config_only.rs (NEW)" lines 153-173 — gate idiom + what to assert; the "DO NOT fail the suite" contract)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Code Examples §5 gate idiom; §6 run_post_deploy_tests signature; D-11 tension; Assumption A5 creds-AND-gate requirement; Runtime State Inventory teardown note)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-05-SUMMARY.md (confirm the deploy detection seam + scaffold deploy.toml are in place)
  </read_first>
  <action>
    Create cargo-pmcp/tests/deploy_config_only.rs with a `deploy_gate()` fn (the widgets_orchestrator.rs:36-45 idiom, env var `PMCP_RUN_DEPLOY_TEST`) and a `#[tokio::test] config_only_deploy_runs_phase79_lifecycle` that FIRST does `if let Some(reason) = deploy_gate() { eprintln!("{reason}"); return; }` (skip, never fail). When gated ON: (1) scaffold a config-only project via `new --kind sql-server` into a tempdir (or a disposable test-server directory); (2) drive the real `cargo pmcp deploy` flow against the gated pmcp.run target (reuse the Plan 05 path — no new deploy code; target a DISPOSABLE/test server name per the Runtime State Inventory teardown note so repeated runs are safe); (3) capture the deployed `url` and assert the Phase 79 lifecycle ran clean — either by asserting the deploy command exited 0 (which itself invokes `run_post_deploy_tests`), or by calling `run_post_deploy_tests(url, "pmcp-run", widgets_present, &PostDeployTestsConfig::default(), quiet)` and asserting `Ok(())` (check + conformance + apps). Document at the top of the file (comment) that the test requires BOTH `PMCP_RUN_DEPLOY_TEST=1` AND live pmcp.run/AWS creds + `cargo lambda` (Assumption A5) — if the gate is set without creds the deploy fails fast with an install/cred hint (acceptable; that is operator error, not a CI path). `--test-threads=1`. Do NOT add an always-on mock (D-11 — the gated test is the SC-4 deliverable).
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test deploy_config_only -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Behavior (CI default — gate ABSENT): `cargo test -p cargo-pmcp --test deploy_config_only -- --test-threads=1` exits 0 with the test SKIPPED (prints the "PMCP_RUN_DEPLOY_TEST not set" reason and returns) — it does NOT fail and does NOT attempt a deploy.
    - Source assertion: the gate fn keys on `PMCP_RUN_DEPLOY_TEST` and early-returns (`grep -n "PMCP_RUN_DEPLOY_TEST" cargo-pmcp/tests/deploy_config_only.rs`); the skip path uses `eprintln! + return`, NOT `panic!`/`assert!`.
    - Source assertion: the gated-on path asserts the Phase 79 lifecycle ran clean — references `run_post_deploy_tests` / the deploy command's exit-0 (check + conformance + apps).
    - The test adds NO new deploy subsystem code (reuses the Plan 05 path) and adds NO always-on mock target (D-11).
    - Manual gate (operator, documented in SUMMARY): with `PMCP_RUN_DEPLOY_TEST=1` + creds, the test deploys and the lifecycle passes (86-VALIDATION.md Manual-Only Verifications row).
  </acceptance_criteria>
  <done>TEST-06 exists as an env-gated authentic deploy test that skips cleanly in CI and, when enabled, proves the config-only deploy + Phase 79 lifecycle (SC-4 / D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real pmcp.run platform | the gated test deploys a live function + bundled demo DB to a hosted target |
| gate env var → deploy activation | the presence of PMCP_RUN_DEPLOY_TEST + creds activates a real cloud action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-06-01 | Elevation of Privilege | an accidental real deploy from CI | mitigate | Default-deny gate: the test deploys ONLY when `PMCP_RUN_DEPLOY_TEST` is set AND creds are present; CI (no gate) skips with a printed reason and never reaches the deploy call (npm_skip_gate contract). |
| T-86-06-02 | Information Disclosure | live creds/secret leaked in test output | mitigate | The test does not print creds; deploy secrets resolve transiently via the existing pipeline (redacted-at-source errors); `token_secret` comes from a secrets ref on the deploy path (Plan 05 / D-06), not an inline literal. |
| T-86-06-03 | Denial of Service / cost | repeated runs leaving orphaned deployed servers | mitigate | Target a disposable/test server name (Runtime State Inventory teardown note); D-11 records that an intentional gated run may leave a deployed server (acceptable when run deliberately). |
</threat_model>

<verification>
- CI default: `cargo test -p cargo-pmcp --test deploy_config_only -- --test-threads=1` passes by SKIPPING (gate absent), never failing, never deploying.
- Gate keys on `PMCP_RUN_DEPLOY_TEST`; gated-on path asserts the Phase 79 lifecycle (check + conformance + apps) ran clean.
- No new deploy code; no always-on mock (D-11).
</verification>

<success_criteria>
- TEST-06 is an authentic env-gated deploy test (SC-4 deliverable per D-11), green-by-skip in normal CI.
- When enabled with creds, it deploys a config-only server to pmcp.run and confirms the Phase 79 post-deploy lifecycle runs cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-06-SUMMARY.md`
</output>
