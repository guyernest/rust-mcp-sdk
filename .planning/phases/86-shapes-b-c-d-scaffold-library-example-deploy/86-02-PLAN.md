---
phase: 86-shapes-b-c-d-scaffold-library-example-deploy
plan: 02
type: execute
wave: 2
depends_on: [86-01]
files_modified:
  - crates/pmcp-server-toolkit/examples/sql_server_http.rs
  - crates/pmcp-server-toolkit/examples/fixtures/config.toml
  - crates/pmcp-server-toolkit/examples/fixtures/schema.sql
  - crates/pmcp-server-toolkit/tests/sql_server_http_example.rs
autonomous: true
requirements: [SHAP-C-01]
must_haves:
  truths:
    - "A developer runs `cargo run --example sql_server_http` and gets a serving HTTP MCP server"
    - "The example main.rs body is ≤15 lines"
    - "An MCP client can initialize, tools/list, and tools/call against the example"
    - "code_mode (validate_code/execute_code) is wired with an inline DEV token_secret"
  artifacts:
    - path: "crates/pmcp-server-toolkit/examples/sql_server_http.rs"
      provides: "The canonical ≤15-line Shape C wiring (toolkit + SQLite connector + serve HTTP)"
      contains: "try_code_mode_from_config_with_connector"
      min_lines: 20
    - path: "crates/pmcp-server-toolkit/tests/sql_server_http_example.rs"
      provides: "Spawn-poll integration test + the ≤15-line body-count assertion"
      contains: "test_tools_list"
  key_links:
    - from: "examples/sql_server_http.rs"
      to: "StreamableHttpServer::with_config(...).start()"
      via: "inlined serve() body (NOT pmcp_sql_server::serve — Pitfall §2)"
      pattern: "StreamableHttpServer::with_config"
    - from: "examples/sql_server_http.rs::execute_batch"
      to: "examples/fixtures/schema.sql"
      via: "SqliteConnector::execute_batch bootstrap (Plan 01 helper)"
      pattern: "execute_batch"
---

<objective>
Build Shape C: a runnable `examples/` MCP server in ≤15 lines of `main.rs` that proves
library use of `pmcp-server-toolkit` + the in-toolkit SQLite connector (D-07, D-08,
SHAP-C-01). This is THE canonical wiring — Plan 03's scaffold emits a byte-identical
copy of this same shape (SC-1: "scaffold main.rs IS the Shape C wiring"), which is why
it is built FIRST. Serves streamable HTTP (D-05), bootstraps a file-backed demo DB from
schema.sql via the Plan 01 execute_batch helper (D-04), and ships code_mode enabled with
an inline DEV token_secret (D-06). A spawn-poll integration test proves
initialize → tools/list → tools/call, plus a body-line-count assertion enforces ≤15.

Purpose: prove the line budget and the inline-secret config shape against verified APIs
before the scaffold emits them.
Output: a registered, runnable, CI-green Shape C example + its integration test.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md
@.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-01-SUMMARY.md

<interfaces>
<!-- Verified APIs the ≤15-line body composes. Sources cited in 86-PATTERNS.md Shared Pattern. -->
From crates/pmcp-server-toolkit (builder_ext.rs verified this session):
```rust
trait ServerBuilderExt {
    fn try_tools_from_config_with_connector(self, cfg: &ServerConfig, conn: Arc<dyn SqlConnector>) -> Result<Self>;     // builder_ext.rs:299
    fn try_code_mode_from_config_with_connector(self, cfg: &ServerConfig, conn: Arc<dyn SqlConnector>) -> Result<Self>; // builder_ext.rs:344 — no-op when [code_mode] absent
}
pub fn ServerConfig::from_toml_strict_validated(toml: &str) -> Result<ServerConfig>;  // config.rs:197 — deny_unknown_fields + validate
impl SqliteConnector { pub fn open(path: &Path) -> Result<Self, ConnectorError>; pub async fn execute_batch(&self, sql: &str) -> Result<(), ConnectorError>; }  // sqlite.rs (execute_batch added in Plan 01)
// StaticResourceHandler::from(&cfg) — the simplified resource surface (NOT build_server's merged/synthesized handler)
```
From pmcp (verified):
```rust
pmcp::Server::builder().name(&str).version(&str)...build() -> Result<Server>;
pmcp::server::streamable_http_server::{StreamableHttpServer, StreamableHttpServerConfig};
StreamableHttpServer::with_config(addr: SocketAddr, Arc<Mutex<Server>>, StreamableHttpServerConfig) -> StreamableHttpServer;
StreamableHttpServer::start(self) -> Result<(SocketAddr, JoinHandle<()>)>;   // returns REAL bound addr
```
mcp-tester (dev-dep, tester.rs): ServerTester::new(url, timeout, insecure, api_key, force_transport, mw) ; test_initialize() ; test_tools_list() (1343) ; test_tool(name, args) (1501).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the ≤15-line serving Shape C example + its fixture config.toml/schema.sql</name>
  <read_first>
    - crates/pmcp-server-toolkit/examples/sqlite_minimal.rs (file-layout/attrs/feature-gate convention to mirror — note it does NOT serve and uses :memory:; the new example MUST serve + be file-backed)
    - crates/pmcp-sql-server/src/lib.rs (read `serve` at lines 156-164 — the StreamableHttpServer body to INLINE; do NOT import pmcp_sql_server::serve per Pitfall §2)
    - crates/pmcp-server-toolkit/src/builder_ext.rs (read try_tools_from_config_with_connector :299 and try_code_mode_from_config_with_connector :344 — the exact chain + that code_mode no-ops when [code_mode] absent)
    - crates/pmcp-server-toolkit/src/config.rs (read from_toml_strict_validated :197 and the deny_unknown_fields field set :39-63 — emit ONLY known keys)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (Shared Pattern: the ≤15-line wiring lines 333-365; Shared Pattern: generated config.toml lines 367-401)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-RESEARCH.md (Code Examples §1 the verified 14-line body; Pitfall §2 serve-location; Pitfall §3 bootstrap; Pitfall §4 inline-secret)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-01-SUMMARY.md (confirm execute_batch signature shipped)
  </read_first>
  <behavior>
    - The example's main() body (statements between `{` of main and the final `Ok(())`) is ≤15 lines.
    - Running it: reads config.toml + schema.sql relative to the example's working dir, opens a file-backed SQLite DB, bootstraps it via `conn.execute_batch(&schema_sql)`, builds the server via the toolkit chain, serves streamable HTTP on a bound address, and awaits the handle.
    - The fixture config.toml parses through `from_toml_strict_validated` (deny_unknown_fields) and has `[code_mode] enabled = true` with `allow_inline_token_secret_for_dev = true` + a ≥16-byte inline `token_secret` so code_mode wiring succeeds (Pitfall §4).
  </behavior>
  <action>
    Create crates/pmcp-server-toolkit/examples/sql_server_http.rs as the verified ≤15-line body from 86-PATTERNS.md Shared Pattern (lines 337-363): `let cfg = ServerConfig::from_toml_strict_validated(&std::fs::read_to_string(<config path>)?)?;` → `let conn: Arc<dyn SqlConnector> = Arc::new(SqliteConnector::open(<db path>.as_ref())?);` → `conn.execute_batch(&std::fs::read_to_string(<schema path>)?).await?;` (ONE line — the Plan 01 helper) → `Server::builder().name(&cfg.server.name).version(&cfg.server.version).try_tools_from_config_with_connector(&cfg, conn.clone())?.try_code_mode_from_config_with_connector(&cfg, conn)?.resources_arc(Arc::new(StaticResourceHandler::from(&cfg))).build()?` → inline the serve() body: `StreamableHttpServer::with_config(<addr>.parse()?, Arc::new(Mutex::new(server)), StreamableHttpServerConfig::default())` then `.start().await?` then `handle.await?`. Use `#[tokio::main]` (multi-thread — NOT current_thread; serving needs it). Add a `//! Run with: cargo run --example sql_server_http --features sqlite,code-mode -p pmcp-server-toolkit` header doc mirroring sqlite_minimal.rs. Resolve config/schema/db paths relative to the example fixtures dir (use `env!("CARGO_MANIFEST_DIR")` joined to `examples/fixtures/` so `cargo run --example` works from any cwd; the bind addr default is discretion — use `127.0.0.1:0` to avoid port collisions OR a documented fixed port, and print the bound addr to stdout so the Task 2 test can parse it). Create examples/fixtures/config.toml from the 86-PATTERNS.md generated-config shape: `[server]` name+version, `[database] type="sqlite" file_path=<a writable demo db path>`, `[[database.tables]]`, `[code_mode] enabled=true allow_writes=false require_limit=true max_limit=1000 token_secret="dev-only-insecure-secret-min-16-bytes" allow_inline_token_secret_for_dev=true` with the loud "DEV ONLY — replace via a secrets ref for production" comment (D-06), and at least one `[[tools]]` (`list_books` SELECT ... LIMIT :limit) with a `[[tools.parameters]]` limit (integer, default 20). Create examples/fixtures/schema.sql with a small books table DDL + a few semicolon-free INSERT rows (keep seed data free of embedded `;` per Assumption A3). Emit ONLY deny_unknown_fields-known keys. Do NOT re-implement build_server (anti-pattern — blows the budget).
  </action>
  <verify>
    <automated>cargo run -p pmcp-server-toolkit --example sql_server_http --features sqlite,code-mode &  SRV=$!; sleep 3; kill $SRV 2>/dev/null; echo "started+stopped ok"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cargo run -p pmcp-server-toolkit --example sql_server_http --features sqlite,code-mode` compiles and begins serving (process binds a port; does not exit with InlineSecretRejected or a config parse error).
    - Source assertion: the example imports `StreamableHttpServer` directly and does NOT contain `pmcp_sql_server::serve` (Pitfall §2) — `grep -L 'pmcp_sql_server' crates/pmcp-server-toolkit/examples/sql_server_http.rs`.
    - Source assertion: the example calls `try_code_mode_from_config_with_connector` AND `execute_batch` (grep both present).
    - Source assertion: fixture config.toml contains `allow_inline_token_secret_for_dev = true` and a `token_secret` literal ≥16 bytes (Pitfall §4) and the loud DEV-ONLY comment (D-06).
    - The example body is ≤15 lines (verified by the Task 2 count assertion).
  </acceptance_criteria>
  <done>The serving ≤15-line Shape C example compiles, runs, bootstraps the demo DB, and wires code_mode with the inline dev secret; fixtures parse strict.</done>
</task>

<task type="auto">
  <name>Task 2: Spawn-poll integration test (initialize → tools/list → tools/call) + ≤15-line body-count assertion</name>
  <read_first>
    - crates/pmcp-sql-server/tests/parity_chinook.rs (the spawn → readiness-poll → ServerTester drive pattern, lines 113-211 — copy the 20-attempt linear-backoff poll verbatim)
    - crates/mcp-tester/src/tester.rs (ServerTester::new signature; test_initialize; test_tools_list :1343; test_tool :1501)
    - .planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-PATTERNS.md (section "cargo-pmcp/tests/scaffold_sql_server.rs" lines 114-149 — the readiness-poll excerpt; note this test spawns the EXAMPLE binary, not a scaffolded crate, so it can spawn the example subprocess directly)
    - crates/pmcp-server-toolkit/examples/sql_server_http.rs (the file under test, created in Task 1)
  </read_first>
  <action>
    Create crates/pmcp-server-toolkit/tests/sql_server_http_example.rs as a `#[tokio::test]` that: (a) spawns the example as a subprocess via `std::process::Command::new(env!("CARGO_BIN_EXE_..."))` — OR, simpler since the example is in this crate, build+run it with `cargo run --example sql_server_http --features sqlite,code-mode` from the test working dir, parsing the bound addr from its stdout (Task 1 prints it); (b) runs the parity_chinook.rs readiness-poll loop verbatim (20 attempts, `tokio::time::sleep(Duration::from_millis(50 * (attempt+1)))`, break on `TestStatus::Passed`); (c) asserts `tester.test_tools_list().await` shows the `list_books` tool; (d) asserts `tester.test_tool("list_books", json!({"limit": 5})).await` succeeds (returns rows / structuredContent). Add a SECOND plain `#[test]` `example_body_is_at_most_15_lines` that reads `include_str!("../examples/sql_server_http.rs")`, locates the `async fn main` body, counts non-empty/non-comment statement lines between the opening `{` and the final `Ok(())`, and `assert!(count <= 15, ...)` (SHAP-C-01 ≤15-line requirement; 86-VALIDATION.md line 47). Mark the test module `#![cfg(all(feature = "sqlite", feature = "code-mode"))]` or gate appropriately. `--test-threads=1` (ephemeral port + process env, Pitfall §5).
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --features sqlite,code-mode --test sql_server_http_example -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cargo test -p pmcp-server-toolkit --features sqlite,code-mode --test sql_server_http_example -- --test-threads=1` exits 0.
    - Behavior assertion: the test asserts `test_tools_list` includes `list_books` AND `test_tool("list_books", {...})` succeeds (tools/list + tools/call both proven).
    - Behavior assertion: `example_body_is_at_most_15_lines` passes (the body is ≤15 lines) — this is the SHAP-C-01 line-budget gate.
    - The readiness poll matches parity_chinook.rs (20 attempts, linear backoff) — no fixed sleep-and-hope.
  </acceptance_criteria>
  <done>Integration test proves the example serves initialize/tools-list/tools-call; the ≤15-line body assertion is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → example HTTP server | untrusted JSON-RPC tools/call params cross into SQL execution |
| config.toml token_secret → code_mode HMAC | inline DEV secret material lives in the fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-02-01 | Tampering | tools/call `:limit` param → SQL | mitigate | Curated `[[tools]]` use named `:value` params bound via `raw_bind_parameter` (sql/sqlite.rs:160) — never string-concatenated; no `:table` identifier substitution (out of scope). |
| T-86-02-02 | Information Disclosure | inline token_secret in the example fixture | mitigate | The fixture secret is a LOUD DEV-ONLY literal (D-06) requiring `allow_inline_token_secret_for_dev = true`; the comment instructs production to use `env:CODE_MODE_SECRET`. It is an example fixture, not a deploy artifact. |
| T-86-02-03 | Spoofing | DNS rebinding against the local example server | mitigate | Uses `StreamableHttpServer` defaults (`AllowedOrigins::localhost()`, lib.rs:127) — never hand-rolls the HTTP layer (Don't Hand-Roll). |
| T-86-02-04 | Elevation of Privilege | write/DDL on the read-only demo | mitigate | `[code_mode] allow_writes = false` (+ default-deny deletes/ddl) rejects INSERT/DELETE/DROP at validation. |
| T-86-02-05 | Spoofing | forged code-mode approval token | mitigate | HMAC-signed approval tokens (16-byte-min secret via HmacTokenGenerator); execute_code rejects invalid tokens — toolkit machinery, not hand-rolled. |
</threat_model>

<verification>
- `cargo run -p pmcp-server-toolkit --example sql_server_http --features sqlite,code-mode` serves HTTP.
- `cargo test -p pmcp-server-toolkit --features sqlite,code-mode --test sql_server_http_example -- --test-threads=1` passes (tools/list + tools/call + ≤15-line assertion).
- Example body ≤15 lines; no `pmcp_sql_server::serve` import; inline-secret dev flag present.
</verification>

<success_criteria>
- Shape C runnable example serves a complete MCP server in ≤15 lines (SHAP-C-01).
- Integration test proves initialize/tools-list/tools-call against it (zero creds, CI-runnable per D-08).
- The wiring shape is locked here for Plan 03 to emit byte-identically.
</success_criteria>

<output>
After completion, create `.planning/phases/86-shapes-b-c-d-scaffold-library-example-deploy/86-02-SUMMARY.md`
</output>
