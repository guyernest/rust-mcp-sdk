---
phase: 100-excel-workbook-built-in-servers-v2
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - crates/pmcp-workbook-compiler/src/ingest/cell_map.rs
  - crates/pmcp-workbook-compiler/src/ingest/mod.rs
  - crates/pmcp-workbook-compiler/src/manifest/synth.rs
  - crates/pmcp-workbook-compiler/tests/harvest_roundtrip_prop.rs
  - crates/pmcp-workbook-compiler/tests/template_harvest_e2e.rs
  - crates/pmcp-workbook-compiler/fuzz/fuzz_targets/workbook_table_ingest.rs
  - crates/pmcp-workbook-compiler/fuzz/Cargo.toml
autonomous: true
requirements: [WBV2-02]
must_haves:
  truths:
    - "Ingest harvests each Excel Table's name and column header names (not just its area) into the WorkbookMap"
    - "Per-row harvest projects type (value-cell parseability), unit (value-cell number format), enum (data-validation list), and tier (tier column) into the synthesized manifest"
    - "A malformed table XML produces a clean CompileError, never a panic crossing the umya-isolation boundary — the umya read/get_tables extraction seam is wrapped in std::panic::catch_unwind that maps a caught panic to CompileError::Ingest"
    - "The per-row type/unit/enum/tier projection is total and stable over arbitrary well-formed rows (property-proven)"
    - "Ingesting the REAL generated template.xlsx end-to-end harvests row-level key/dtype/unit/enum/tier/description and the table+caption→tool linkage (integration-proven, not just thin projection helpers)"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/ingest/cell_map.rs"
      provides: "TableRecord{name,area,columns} on SheetRecord"
      contains: "struct TableRecord"
    - path: "crates/pmcp-workbook-compiler/src/ingest/mod.rs"
      provides: "table_records() harvesting get_name()/get_columns() inside a catch_unwind seam"
      contains: "get_columns"
    - path: "crates/pmcp-workbook-compiler/tests/harvest_roundtrip_prop.rs"
      provides: "proptest: harvest projection is total + stable over arbitrary well-formed rows"
      contains: "proptest!"
    - path: "crates/pmcp-workbook-compiler/tests/template_harvest_e2e.rs"
      provides: "integration: real template.xlsx harvest → row-level fields + table/caption→tool linkage"
    - path: "crates/pmcp-workbook-compiler/fuzz/fuzz_targets/workbook_table_ingest.rs"
      provides: "malformed-table-XML clean-error fuzz target asserting the catch_unwind seam"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/ingest/mod.rs"
      to: "umya_spreadsheet Table::get_name/get_columns"
      via: "table_records harvest"
      pattern: "get_name|get_columns"
    - from: "crates/pmcp-workbook-compiler/src/ingest/mod.rs"
      to: "std::panic::catch_unwind"
      via: "umya table-extraction containment seam → CompileError::Ingest"
      pattern: "catch_unwind"
    - from: "crates/pmcp-workbook-compiler/src/manifest/synth.rs"
      to: "number_format → unit projection"
      via: "per-row unit harvest"
      pattern: "number_format"
---

<objective>
Make ingest HARVEST Excel Tables (WBV2-02): today `table_ranges` reads only each table's
*area* and `SheetRecord.tables: Vec<RangeRef>` carries no name or column metadata (Pitfall 1).
Add a `TableRecord { name, area, columns }`, a `table_records()` ingest fn calling umya's
verified `Table::get_name()` / `Table::get_columns()`, and per-row harvest of type/unit/enum/tier from
each row's `value` cell. Contain umya's malformed-table-XML `.unwrap()` (Pitfall 2 / §2 caveat 1)
by wrapping the umya read/table-extraction seam in `std::panic::catch_unwind`, mapping a caught
panic to a clean `CompileError::Ingest`, proven by a new fuzz target — and prove the harvest
projection is total + stable with a property test plus a real-template end-to-end integration test.

Purpose: The harvested Table name/columns/rows are the raw material the manifest model lift
(Plan 03) and multi-tool emission (Plan 04) consume. Without harvested names there is no tool
name and no per-row schema.

Output: Extended ingest model + harvest fn (panic-contained) + per-row synth projection + a
property test + a real-template integration test + a fuzz target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/workbook-table-authoring-contract.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md

<interfaces>
<!-- Extracted from the working tree (plain grep). Executor uses these directly. -->
From crates/pmcp-workbook-compiler/src/ingest/cell_map.rs:
  - struct DataValidationRecord (cell_map.rs:99) — owned-record precedent (Debug,Clone,PartialEq,Eq,Serialize,schemars::JsonSchema; owned RangeRef/String only)
  - struct SheetRecord (cell_map.rs:144); field `pub tables: Vec<RangeRef>` (cell_map.rs:164); default ctor sets `tables: Vec::new()` (cell_map.rs:233)
  - CellRecord.number_format = style.number_format().map(|nf| nf.format_code()) (cell_map.rs:126-129) — the unit source field

From crates/pmcp-workbook-compiler/src/ingest/mod.rs:
  - fn table_ranges(ws, sheet_name) -> Vec<RangeRef> (mod.rs:311) — reads t.area() ONLY; the extension point
  - fn data_validations(...) (mod.rs:332) — emits one DataValidationRecord per (DV × range), dv_type "list" + raw formula1 = the enum/tier dropdown source
  - ingest() maps ingest failure → CompileError::Ingest (mod.rs:293-294) — the umya-isolation boundary; the catch_unwind seam wraps the umya read/get_tables extraction inside this entry-point so a panic becomes CompileError::Ingest

From crates/pmcp-workbook-compiler/src/manifest/synth.rs:
  - apply_dv_fork / freeze_or_reason (synth.rs:206-224) — DV→allowed_values enum harvest precedent
  - dtype derives from value-cell parseability: f64::parse → Number else Text (synth.rs:441-445)

umya 3.0.0 (VERIFIED, compiler-only): Worksheet::get_tables() -> &[Table]; Table::get_name();
  Table::get_area() -> (Coordinate, Coordinate); Table::get_columns() -> &[TableColumn];
  TableColumn::get_name(). §2 caveat 1: umya `.unwrap()`s on malformed table XML — hence the
  catch_unwind containment seam in Task 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TableRecord + harvest table name/columns via table_records() inside a catch_unwind containment seam</name>
  <files>crates/pmcp-workbook-compiler/src/ingest/cell_map.rs, crates/pmcp-workbook-compiler/src/ingest/mod.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/ingest/cell_map.rs (DataValidationRecord owned-record shape at :99-108; SheetRecord host at :144-174; `pub tables: Vec<RangeRef>` at :164; module-doc "no umya type crosses" quarantine invariant)
    - crates/pmcp-workbook-compiler/src/ingest/mod.rs (table_ranges at :311-323 reads t.area() only — the extension point; data_validations at :332-356 = FLAT-MAP + umya-isolation discipline, unwrap_or(&[]) not .unwrap(); ingest() failure → CompileError::Ingest at :293-294 — the seam the catch_unwind wraps)
    - docs/design/workbook-table-authoring-contract.md §2 (umya get_tables/get_name/get_columns confirmed; caveat 1: umya .unwrap()s on malformed table XML; §"umya-isolation boundary so a bad workbook is a clean compile error, not a panic" line 40), §3.1-§3.2 (region types, standard columns)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md §4 (table_records harvest), §5 (TableRecord struct shape)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md Pitfall 1 (metadata silently dropped), Pitfall 2 (umya panic on malformed table XML), and §"umya Table read" code example
  </read_first>
  <behavior>
    - Test: ingesting `tests/fixtures/template.xlsx` yields a SheetRecord whose harvested tables include records named `Inputs`, `Calculate_Tax`, `Estimate_Refund` each with columns starting `["name","value","description"]` (Inputs also `"tier"`).
    - Test: a TableRecord carries only owned String/RangeRef (no umya type) — asserted by the type compiling inside the reader-free serde-derive set.
    - Test: a workbook with zero Tables yields an empty harvested-tables vec (no panic).
    - Test: feeding a workbook whose `xl/tables/tableN.xml` is corrupted/truncated returns `Err(CompileError::Ingest)` — NOT a panic/abort (the catch_unwind seam is exercised by a unit test, complementing the fuzz target in Task 3).
  </behavior>
  <action>
    Add `pub struct TableRecord { pub name: String, pub area: RangeRef, pub columns: Vec<String> }` to `cell_map.rs` with the owned-record derive set from `DataValidationRecord` (`#[derive(Debug, Clone, PartialEq, Eq, Serialize, schemars::JsonSchema)]`, doc comment per field, owned types only — uphold the "no umya type crosses" invariant). Extend `SheetRecord` (cell_map.rs:144) to carry the harvested records: add a sibling field `pub table_records: Vec<TableRecord>` (additive — keep the existing `tables: Vec<RangeRef>` to avoid breaking other consumers, or replace it if grep shows `tables` is only consumed by `table_ranges`; default ctor at :233 gains `table_records: Vec::new()`). In `ingest/mod.rs` add `fn table_records(ws: &umya_spreadsheet::Worksheet, sheet_name: &str) -> Vec<TableRecord>` modeled on `table_ranges` (:311) but calling `t.get_name()` → name and `t.get_columns().iter().map(|c| c.get_name().to_string()).collect()` → columns, with `t.get_area()` → RangeRef. NEVER `.unwrap()` a umya accessor — use the `unwrap_or` discipline from `data_validations`.
    PANIC CONTAINMENT SEAM (resolves review finding #2 — the EXACT seam): umya `.unwrap()`s internally on malformed table XML (§2 caveat 1), so a defensive accessor discipline is necessary but NOT sufficient — a panic can still originate inside umya's own `get_tables()`/`Table` accessors. Wrap the umya read + table-extraction work (the call(s) into `ws.get_tables()` and the per-table `get_name`/`get_columns`/`get_area` extraction — i.e. the whole umya-touching extraction span, NOT the downstream owned-record processing) in `std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| ...))` and map an `Err(_)` (caught panic) to `CompileError::Ingest` with a message like "malformed table XML in sheet {sheet}". Put this catch_unwind at the `ingest::ingest` entry-point span that drives `table_records` (the same boundary that already maps ingest failure → CompileError::Ingest at :293-294) so the entire umya read is contained; optionally install a `std::panic::set_hook`/`take_hook` quiet-hook around it to suppress the default panic print (document the choice). The contract: a malformed table workbook is a clean `CompileError`, never a process abort. (REMOVED the previously-contradictory "do NOT catch panics here" sentence — catch_unwind at THIS seam is exactly how the clean-error contract is met; Task 3's fuzz target asserts THIS boundary.) Keep each fn cognitive-complexity <=25 (PMAT) — factor the catch_unwind wrapper as its own helper (e.g. `fn extract_tables_contained(ws, sheet) -> Result<Vec<TableRecord>, CompileError>`). Wire the call into the same per-worksheet ingest loop that calls `table_ranges`.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --lib ingest -- table_record</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/pmcp-workbook-compiler/src/ingest/cell_map.rs` contains `pub struct TableRecord` with fields `name`, `area`, `columns`.
    - Source: `crates/pmcp-workbook-compiler/src/ingest/mod.rs` contains `fn table_records` calling `get_columns` and `get_name`, and a `std::panic::catch_unwind` (grep `catch_unwind` returns ≥1) wrapping the umya table-extraction span and mapping a caught panic → `CompileError::Ingest`.
    - Source: `grep -v '^#' ingest/mod.rs | grep -c '\.unwrap()'` shows no new bare `.unwrap()` on a umya accessor in `table_records`.
    - Behavior: `cargo test -p pmcp-workbook-compiler --lib ingest` exits 0; harvest of `template.xlsx` reports the three table names with their columns; the corrupted-tableN.xml unit test returns `Err(CompileError::Ingest)` (no panic).
  </acceptance_criteria>
  <done>Table name + column headers are harvested into owned TableRecords on SheetRecord, reader-free, with the umya table-parse panic contained at a catch_unwind seam that yields a clean CompileError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-row harvest of type / unit / enum / tier from the value cell</name>
  <files>crates/pmcp-workbook-compiler/src/manifest/synth.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/manifest/synth.rs (apply_dv_fork / freeze_or_reason DV→enum precedent at :206-224; dtype-from-parseability at :441-445; dv_dynamic_finding reason-coded WARNING pattern)
    - crates/pmcp-workbook-compiler/src/ingest/cell_map.rs (CellRecord.number_format at :126-129 — the unit source)
    - docs/design/workbook-table-authoring-contract.md §3.3 (Harvest rules table: field key←name col, type←value cell type, unit←value cell number format [currency→USD, %→rate, date→date], enum←data-validation list, description←description col, tier←tier col, example/oracle←value cell)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md §6 (synth.rs per-row harvest — DV→enum precedent, type witness, NEW number-format→unit projector reading EXISTING number_format field)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (CellRole fields: dtype, unit, allowed_values at :120-132, tier:Option<InputTier> at :120 — the harvest targets)
  </read_first>
  <behavior>
    - Test: an Inputs row whose value cell parses as f64 with a currency number format harvests dtype=Number, unit="USD".
    - Test: an Inputs row whose value cell has a percent number format harvests unit="rate".
    - Test: an Inputs row whose value cell carries a list data-validation harvests allowed_values=Some([...]) (enum), reusing freeze_or_reason.
    - Test: an Inputs row whose `tier` column cell = "strict" harvests tier=strict; "variable" or blank → variable.
  </behavior>
  <action>
    Drive synth from the harvested `TableRecord`s instead of (eventually instead of, fully in Plan 04) the named-range path. For each input-Table row: read the `name` column cell → field key; the `value` column cell → dtype via the existing parseability rule (:441-445); the value cell's `number_format` (cell_map.rs:126-129) → unit via a NEW `fn number_format_to_unit(fmt: &str) -> Option<String>` (currency code/`$`→`"USD"`, `%`→`"rate"`, date format→`"date"`, else `None`); the covering data-validation list → `allowed_values` via the EXISTING `freeze_or_reason` (:206-224, emit the same reason-coded WARNING on ineligible DV — never block); the `tier` column cell → `InputTier` (`strict` vs default `variable`). For each output-Table row harvest key/dtype/unit/description the same way (no tier on outputs). Keep the existing `dv_dynamic_finding` WARNING path for ineligible enums. Factor `number_format_to_unit` as its own pure fn (testable, cog<=25). Do NOT delete the named-range synth path in this plan — Plan 04 retires `promote_named_outputs`/`name_named_inputs`; here you ADD the table-row harvest projection so both coexist during transition. The tier dropdown dogfoods the enum mechanism (§3.3) — the same `freeze_or_reason` reads both the field enum AND the tier list.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --lib synth -- harvest</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/pmcp-workbook-compiler/src/manifest/synth.rs` contains `fn number_format_to_unit` mapping currency→"USD", percent→"rate", date→"date".
    - Behavior: `cargo test -p pmcp-workbook-compiler --lib synth` exits 0; the four harvest assertions (type/unit/enum/tier) pass against generated row fixtures.
    - Behavior: an ineligible (range-backed) data-validation produces a reason-coded WARNING finding, not an error (reuse `freeze_or_reason` / `dv_dynamic_finding`).
    - Quality: each new/changed fn is cog<=25 (local `pmat analyze complexity` flags none in synth.rs > 25).
  </acceptance_criteria>
  <done>Each Table row projects type/unit/enum/tier into a CellRole exactly per §3.3, reusing the DV enum machinery.</done>
</task>

<task type="auto">
  <name>Task 3: Fuzz target — malformed table XML produces a clean CompileError, never a panic (asserts the catch_unwind seam)</name>
  <files>crates/pmcp-workbook-compiler/fuzz/fuzz_targets/workbook_table_ingest.rs, crates/pmcp-workbook-compiler/fuzz/Cargo.toml</files>
  <read_first>
    - crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_provenance_reader.rs (the existing xlsx-bytes fuzz target shape to clone — harness, libfuzzer_sys::fuzz_target!, byte input → ingest entry)
    - crates/pmcp-workbook-compiler/fuzz/Cargo.toml (the [[bin]] registration pattern for fuzz targets)
    - crates/pmcp-workbook-compiler/src/ingest/mod.rs (Task 1's catch_unwind containment seam + ingest() entry mapping failure → CompileError::Ingest at :293-294 — the boundary the fuzz proves)
    - docs/design/workbook-table-authoring-contract.md §2 caveat 1 (umya .unwrap()s on malformed table XML — must be a clean compile error, not a panic)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md Pitfall 2 + Security Domain "Malformed .xlsx → DoS" row
  </read_first>
  <action>
    Add a libfuzzer target `workbook_table_ingest` cloning the harness of `fuzz_provenance_reader.rs`: feed arbitrary bytes (and, where the harness can, a minimal valid xlsx zip with a corrupted/truncated `xl/tables/tableN.xml`) into `pmcp_workbook_compiler::ingest::ingest`. The invariant the fuzz ASSERTS is the Task-1 catch_unwind boundary: the call ALWAYS returns a `Result` (Ok or `Err(CompileError)`) and NEVER panics/aborts — a umya panic on malformed table XML MUST have been caught at the `table_records` catch_unwind seam and mapped to `CompileError::Ingest`. (If the seam were missing/broken, libfuzzer would catch the abort — that is the point: the fuzz target is the proof of the containment seam, not merely a hope that one exists.) Register the `[[bin]]` in `fuzz/Cargo.toml`. Provide a seed corpus entry that is a malformed-table xlsx (or document the inline byte construction) so the corrupted-tableN.xml path is reached, not just the early zip-reject path. Confirm a short fuzz run finds no panic. This is the SC1 fuzz Wave-0 gap from VALIDATION.md.
  </action>
  <verify>
    <automated>cargo +nightly fuzz run workbook_table_ingest -- -runs=20000 -max_total_time=60</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `crates/pmcp-workbook-compiler/fuzz/fuzz_targets/workbook_table_ingest.rs` registered as a `[[bin]]` in `fuzz/Cargo.toml`.
    - Behavior: a 60s / 20k-run fuzz run exits 0 with no panic/crash artifact (malformed table XML → clean `CompileError` via the catch_unwind seam, never a panic).
    - Source: the fuzz target calls `pmcp_workbook_compiler::ingest::ingest` and asserts the result is a `Result` (no `.unwrap()` that could mask a panic); a seed corpus entry exercises a corrupted `xl/tables/tableN.xml`.
  </acceptance_criteria>
  <done>Malformed table XML is proven (by fuzzing) to surface as a clean CompileError via the catch_unwind containment seam inside the umya-isolation boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Property test — harvest projection is total and stable over arbitrary well-formed rows (CLAUDE.md PROPERTY requirement)</name>
  <files>crates/pmcp-workbook-compiler/tests/harvest_roundtrip_prop.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/manifest/synth.rs (Task 2's per-row harvest: number_format_to_unit, dtype-from-parseability :441-445, freeze_or_reason :206-224, tier projection — the projection under test)
    - crates/pmcp-workbook-compiler/src/ingest/cell_map.rs (TableRecord from Task 1; CellRecord.number_format :126-129)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (CellRole target fields: dtype, unit, allowed_values, tier :120-132 — the projection codomain)
    - docs/design/workbook-table-authoring-contract.md §3.3 (the deterministic harvest rules the property encodes)
    - any existing `proptest!` block in the workbook crates (e.g. dag.rs / synth.rs test mods) for the project proptest style + strategy helpers
  </read_first>
  <behavior>
    - Property (totality): for an arbitrary well-formed input row (random name string, random value-cell content, random number-format string drawn from {currency, percent, date, general}, optional list-DV, random tier-column value), the harvest projection yields a CellRole with a defined dtype and a defined tier (never panics, never returns None for a required field) — the projection is total.
    - Property (stability/determinism): harvesting the same row twice yields byte-identical (PartialEq) CellRole projections — the projection is a pure function of the row, with no order/clock/hash-iteration dependence.
    - Property (unit closure): number_format_to_unit only ever returns one of {Some("USD"), Some("rate"), Some("date"), None} — the unit codomain is closed (no arbitrary format string leaks through as a unit).
    - Property (tier closure): the tier projection only ever yields strict or variable; any non-"strict" / blank tier cell maps to variable (never an undefined tier).
  </behavior>
  <action>
    Add `crates/pmcp-workbook-compiler/tests/harvest_roundtrip_prop.rs` as a `proptest`-based integration test (this is the CLAUDE.md ALWAYS PROPERTY requirement for WBV2-02, complementing Task 2's example-based unit tests, Task 3's fuzz target, and Task 5's real-template end-to-end integration test). Define a `prop_compose!`/strategy that generates an arbitrary well-formed harvest row: a non-empty `name` string, a `value`-cell content (numeric or text), a `number_format` drawn from a small representative set (a currency code, a `%` format, a date format, `General`/empty), an optional list data-validation, and a `tier` cell value (`strict`/`variable`/blank/garbage). Drive the row through the Task-2 harvest projection (call `number_format_to_unit` + the dtype/tier/enum projection — expose them via the crate's test-visible API or a thin harness fn). Assert the four invariants in the `<behavior>` block: totality (defined dtype + defined tier, no panic), stability (harvest twice → equal CellRole), unit closure (unit ∈ {USD,rate,date,None}), tier closure (tier ∈ {strict,variable}). Use the project's existing proptest config (default ≥256 cases). Keep the strategy + body cog<=25.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --test harvest_roundtrip_prop</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `crates/pmcp-workbook-compiler/tests/harvest_roundtrip_prop.rs` containing a `proptest!` block exercising the harvest projection.
    - Behavior: `cargo test -p pmcp-workbook-compiler --test harvest_roundtrip_prop` exits 0; the property runs ≥256 cases and finds no totality/stability/closure violation.
    - Source: the property asserts unit ∈ {USD, rate, date, None} and tier ∈ {strict, variable} (closed codomains) and harvest-twice equality (determinism).
    - Quality: `pmat analyze complexity` flags no fn in the test > cog 25.
  </acceptance_criteria>
  <done>The per-row type/unit/enum/tier harvest is property-proven total + stable, satisfying the CLAUDE.md ALWAYS PROPERTY requirement for WBV2-02.</done>
</task>

<task type="auto">
  <name>Task 5: End-to-end integration — ingest the REAL generated template.xlsx and verify row-level harvested fields + table/caption→tool linkage</name>
  <files>crates/pmcp-workbook-compiler/tests/template_harvest_e2e.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/tests/fixtures/template.xlsx (Plan 01's shipped template — the REAL artifact this test ingests end-to-end, not a thin projection helper)
    - crates/pmcp-workbook-compiler/src/ingest/mod.rs (Task 1's ingest::ingest + table_records — the real harvest entry-point)
    - crates/pmcp-workbook-compiler/src/manifest/synth.rs (Task 2's per-row harvest into CellRole — the projection the e2e test confirms end-to-end)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (CellRole fields key/dtype/unit/allowed_values/tier; description; the harvest targets the e2e asserts)
    - docs/design/workbook-table-authoring-contract.md §3.3 (per-row harvest rules), §4-§4.2 (output Table → tool; caption → tool description; the calculate_tax/estimate_refund linkage), §7 (the template's authored values: income/filing/withheld/rate; the two output tables + captions)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-VALIDATION.md (Per-Task Verification Map — add a row for this e2e test; finding #7)
  </read_first>
  <behavior>
    - Test: `ingest::ingest(tests/fixtures/template.xlsx)` → synth harvests the Inputs rows with CORRECT row-level fields: `income` dtype=Number unit="USD"; `filing` allowed_values=Some(["single","married"]) (enum); `rate` unit="rate" tier=strict; `withheld` dtype=Number unit="USD"; each input row carries its `description` column text.
    - Test: the two output Tables harvest as `Calculate_Tax` (caption/description "Compute federal tax from income & filing") and `Estimate_Refund` (caption/description "Estimate refund given withholding") — the table name + caption→tool-description linkage is established from the REAL file (not a hand-built TableRecord).
    - Test: each harvested output row (tax_owed/effective_rate/refund) carries its dtype + value oracle from the authored cached `<v>`.
  </behavior>
  <action>
    Add `crates/pmcp-workbook-compiler/tests/template_harvest_e2e.rs` (resolves review finding #7 — at least one property OR integration test must ingest the REAL generated template.xlsx, not just thin projection helpers). Ingest the committed `crates/pmcp-workbook-compiler/tests/fixtures/template.xlsx` through the real `ingest::ingest` → synth path (no hand-built TableRecords, no mocked rows). Assert the row-level harvested fields per the `<behavior>` block: for each Inputs row the harvested key/dtype/unit/enum/tier/description match the §7 template authoring; for each output Table the name + caption→tool-description linkage and per-row dtype + oracle are correct. This is the deep integration test that catches real workbook/table coordinate bugs the thin-harness property test (Task 4) cannot. Keep the test fn cog<=25 — factor a small assert-row helper. (Together: Task 1/2 = unit, Task 3 = fuzz, Task 4 = property over arbitrary rows, Task 5 = integration over the real file — the full ALWAYS set for WBV2-02 with at least one REAL-artifact ingest.)
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --test template_harvest_e2e</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `crates/pmcp-workbook-compiler/tests/template_harvest_e2e.rs` ingesting the REAL `tests/fixtures/template.xlsx` via `ingest::ingest` (grep `ingest::ingest` present; no hand-built TableRecord literals).
    - Behavior: `cargo test -p pmcp-workbook-compiler --test template_harvest_e2e` exits 0; it asserts income=USD/Number, filing enum=[single,married], rate=rate/strict, each input description, and the Calculate_Tax/Estimate_Refund name+caption→tool-description linkage plus per-row oracle from the real file.
    - Quality: `pmat analyze complexity` flags no fn in the test > cog 25.
  </acceptance_criteria>
  <done>The harvest is proven end-to-end against the REAL shipped template.xlsx — row-level fields and the table/caption→tool linkage are confirmed from the actual artifact, not a thin helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted .xlsx → umya reader (compiler) | Malformed table XML can panic umya; the catch_unwind containment seam at the ingest table-extraction span must map it to CompileError, never a panic |
| umya types → manifest model | Only owned String/RangeRef may cross; no umya type leaks into the reader-free model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03 | Denial of Service | malformed table XML (umya .unwrap panic) | mitigate | `table_records` wraps the umya read/get_tables extraction in `std::panic::catch_unwind` mapping a caught panic → `CompileError::Ingest` (mod.rs:293-294 boundary); NEW fuzz target `workbook_table_ingest` proves clean-error not panic over 60s/20k runs + a unit test feeds a corrupted tableN.xml |
| T-100-04 | Denial of Service | zip-bomb / billion-laughs / oversized table | mitigate | Existing `MAX_CELL_COUNT` DoS guard (ingest/mod.rs) + quick-xml entity-expansion-disabled provenance path remain in force (untouched) |
| T-100-05 | Elevation (boundary breach) | umya type leaking past ingest | mitigate | `TableRecord` holds only owned String/RangeRef (reader-free serde set); `make purity-check` cargo-tree ban stays green (asserted in Plan 06) |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs — umya 3.0.0 is an existing compiler-only dep; proptest is an existing dev-dep |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-compiler --lib ingest` + `synth` green (incl. the corrupted-tableN.xml catch_unwind unit test).
- `cargo test -p pmcp-workbook-compiler --test harvest_roundtrip_prop` green (property).
- `cargo test -p pmcp-workbook-compiler --test template_harvest_e2e` green (real-template integration).
- `cargo +nightly fuzz run workbook_table_ingest` finds no panic (asserts the catch_unwind seam).
- `make purity-check` (run at wave merge) stays green — no umya leak from TableRecord.
</verification>

<success_criteria>
WBV2-02 satisfied: ingest harvests Table name/columns; per-row type/unit/enum/tier projected into the manifest; the projection is property-proven total + stable AND integration-proven end-to-end against the real template.xlsx; malformed table XML contained as a clean CompileError via a catch_unwind seam (fuzz-proven). Maps to ROADMAP Success Criterion 1.
</success_criteria>

<output>
Create `.planning/phases/100-excel-workbook-built-in-servers-v2/100-02-SUMMARY.md` when done
</output>
</content>
</invoke>
