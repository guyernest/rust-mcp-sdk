---
phase: 100-excel-workbook-built-in-servers-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-compiler/src/fixture_author.rs
  - cargo-pmcp/src/templates/workbook_bundle/template.xlsx
  - cargo-pmcp/src/templates/workbook_bundle/template.gen.json
  - cargo-pmcp/src/templates/workbook_bundle/template.provenance-override.json
  - crates/pmcp-workbook-compiler/tests/fixtures/template.xlsx
  - crates/pmcp-workbook-compiler/tests/template_provenance.rs
autonomous: true
requirements: [WBV2-01]
must_haves:
  truths:
    - "A shipped template.xlsx exists carrying an Inputs Excel Table (name|value|description|tier) with a tier dropdown and a sample enum dropdown, calc + reference regions, and >=1 named output Excel Table with a caption description"
    - "The shipped template.xlsx classifies as ProvenanceClass::ExcelTrusted (authored by rust_xlsxwriter, not umya)"
    - "The template is regeneratable deterministically via the env-gated PMCP_REGEN_FIXTURES path with a committed .gen.json sidecar"
  artifacts:
    - path: "cargo-pmcp/src/templates/workbook_bundle/template.xlsx"
      provides: "The shipped BA starting point + training artifact + honest reference fixture"
    - path: "crates/pmcp-workbook-compiler/src/fixture_author.rs"
      provides: "Table-emitting author surface (add_table/set_columns/add_data_validation)"
      contains: "add_table"
    - path: "crates/pmcp-workbook-compiler/tests/template_provenance.rs"
      provides: "ExcelTrusted classification assertion on the shipped template"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/fixture_author.rs"
      to: "rust_xlsxwriter::Worksheet::add_table"
      via: "Table author surface extension"
      pattern: "add_table|add_data_validation"
    - from: "crates/pmcp-workbook-compiler/tests/template_provenance.rs"
      to: "provenance::gate::classify"
      via: "ExcelTrusted assertion"
      pattern: "ExcelTrusted"
---

<objective>
Author and ship a provenance-valid `template.xlsx` (WBV2-01) using `rust_xlsxwriter` — the
ONLY writer that yields `ProvenanceClass::ExcelTrusted`. This one artifact does three jobs
(§7 of the design contract): the BA starting point, the training/documentation artifact, and
the honest reference fixture that replaces the misleading hand-authored `tax-calc`/`leap1900`
fixtures whose `in_*`/`out_*` defined names masked the original bug.

Purpose: This template is the anchor for every downstream plan — the harvest (Plan 02), the
multi-tool model (Plan 03/04), and the `workbook explain` snapshot (Plan 05) all ingest it.
It must exist and be provenance-valid before harvest work can target a real Table.

Output: A committed `template.xlsx` (under the CLI templates dir AND the compiler test
fixtures dir — one source, two consumers), the extended `fixture_author` Table-author surface,
and a test asserting `ExcelTrusted`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/workbook-table-authoring-contract.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend fixture_author with a Table-emitting surface (add_table / set_columns / add_data_validation)</name>
  <files>crates/pmcp-workbook-compiler/src/fixture_author.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/fixture_author.rs (CURRENT state — author_xlsx at :171-188 writes cells + define_name; WorkbookSpec/DefinedNameSpec at :137-160; the provenance-valid recipe at :12-39; Formula::new().set_result() cached-oracle pattern at :32-37,220-225; regenerate_fixtures env-gated path at :39-49,~416)
    - docs/design/workbook-table-authoring-contract.md §3.1-§3.3 (region types + standard columns), §7 (template contents + annotated reference diagram)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md §13 (fixture_author.rs analog: WHY rust_xlsxwriter, cached-<v>=oracle, RETIRE in_*/out_* defined-name injection, reproducible-fixture discipline)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §2 (verified rust_xlsxwriter 0.95 add_table/Table::set_name/set_columns + worksheet.rs:8968,9388 add_data_validation) and Pitfall 4 (the module is #![cfg(test)])
  </read_first>
  <behavior>
    - Test: a WorkbookSpec carrying a TableSpec { name, header_row, columns, sheet } authored via author_xlsx produces an .xlsx whose worksheet, re-read by umya get_tables(), reports the same table name and the same column header names.
    - Test: a TableSpec with a tier-column data-validation { variable, strict } and a sample enum data-validation produces an .xlsx whose data_validations() ingest reports a dv_type "list" over the corresponding value-cell ranges.
    - Test: an output TableSpec authored with a caption cell directly above the table writes that caption string at (header_row-1, table_start_col).
  </behavior>
  <action>
    Add a `TableSpec` struct to `fixture_author.rs` (fields: `name: &str` = ListObject/tool name, `sheet: &str`, `top_left: (u32,u16)`, `columns: &[&str]` = header names e.g. `["name","value","description","tier"]`, `caption: Option<&str>` = the cell directly above the table = the tool description, `rows: &[&[AuthoredCell]]` = body rows reusing the existing `AuthoredCell` enum so formula cells keep the `Formula::new(f).set_result(cached)` cached-`<v>` oracle from :32-37). Add `data_validations: &[DataValidationSpec]` where `DataValidationSpec { range, kind: DvKind }` and `DvKind` covers `List(&[&str])` for the `{variable, strict}` tier dropdown AND a sample enum dropdown — dogfooding the enum-from-dropdown mechanism per §3.3. Extend `author_xlsx` (:171-188): after writing cells, for each `TableSpec` call `worksheet.add_table(first_row, first_col, last_row, last_col, &table)` with `rust_xlsxwriter::Table::new().set_name(spec.name).set_columns(...)` (set each column header via `TableColumn::new().set_header(name)`); write the caption cell above the table; for each `DataValidationSpec` build a `rust_xlsxwriter::DataValidation::new().allow_list_strings(&values)` and call `worksheet.add_data_validation(...)` over the range. Keep `WorkbookSpec.tables: Vec<TableSpec>` as an additive field. Do NOT remove the existing cell/define_name machinery yet — the `in_*`/`out_*` `DefinedNameSpec` injection (:384-392 and the 11 sites in the cleanup map) is RETIRED in Plan 02/04, not here; this plan only ADDS the Table surface and authors the template using it. Preserve the umya-isolation / no-`.unwrap()` discipline. Keep cognitive complexity <=25 per fn (PMAT) — one helper fn per concern (write_table, write_data_validations, write_caption). Resolve Assumption A2 by keeping the author `#[cfg(test)]` and committing a GENERATED template (option b from Pitfall 4) — do NOT promote rust_xlsxwriter into a non-test build (avoid the linker-pull-into-production landmine).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --lib fixture_author -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Source: `crates/pmcp-workbook-compiler/src/fixture_author.rs` contains `add_table` and `add_data_validation` calls and a `pub(crate) struct TableSpec`.
    - Source: `fixture_author.rs` still carries `#![cfg(test)]` (no rust_xlsxwriter pulled into a non-test build).
    - Behavior: `cargo test -p pmcp-workbook-compiler --lib fixture_author` exits 0 with the three round-trip Table/DV/caption assertions green.
    - Quality: no new `.unwrap()` on a umya accessor; `cargo clippy -p pmcp-workbook-compiler --all-targets` produces no new warnings for this file.
  </acceptance_criteria>
  <done>The Table-author surface (name/columns/caption/data-validation) exists, is test-covered by umya round-trip, and stays test-only.</done>
</task>

<task type="auto">
  <name>Task 2: Author + commit the shipped template.xlsx (Inputs Table + named output Table) and assert ExcelTrusted</name>
  <files>cargo-pmcp/src/templates/workbook_bundle/template.xlsx, cargo-pmcp/src/templates/workbook_bundle/template.gen.json, cargo-pmcp/src/templates/workbook_bundle/template.provenance-override.json, crates/pmcp-workbook-compiler/tests/fixtures/template.xlsx, crates/pmcp-workbook-compiler/tests/template_provenance.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/fixture_author.rs (the regenerate_fixtures #[ignore] env-gated generator at :39-49,~416 and the .gen.json sidecar discipline)
    - cargo-pmcp/src/templates/workbook_bundle/ (CURRENT contents: tax-calc.xlsx, tax-calc@1.1.0/, *.provenance-override.json — the misleading set this template replaces)
    - crates/pmcp-workbook-compiler/tests/fixtures/ (CURRENT: tax-calc.xlsx / loan-calc.xlsx / leap1900-probe.xlsx + .gen.json + .provenance-override.json sidecars — the committed-fixture pattern to mirror)
    - docs/design/workbook-table-authoring-contract.md §7 (annotated reference diagram: 0_meta optional {server,version}; Inputs Table income/filing/withheld/rate with tier; calc + VLOOKUP reference; "Calculate Tax" + "Estimate Refund" output Tables with captions)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §"Open Questions Resolved" OQ-1 (0_meta minimal {server,version}) and Open-Q3 (template lives under cargo-pmcp/src/templates/workbook_bundle/ AND copied to tests/fixtures/)
    - crates/pmcp-workbook-compiler/src/provenance/gate.rs (classify → ProvenanceClass; the ExcelTrusted variant the test asserts) — DO NOT MODIFY, read only
  </read_first>
  <action>
    Build a `WorkbookSpec` for the tax-suite template from §7's annotated diagram: an optional `0_meta` cell region carrying `server: tax-suite` + `version: 1` (per OQ-1 minimal key set — fully optional); an Inputs `TableSpec` named `Inputs` columns `name|value|description|tier` with rows `income` (100000, currency format → USD unit witness), `filing` (a sample enum dropdown `{single, married}` → demonstrates enum harvest), `withheld` (15000, currency), `rate` (0.22, percent format, tier=`strict` → not caller-exposed), with the tier column carrying a `{variable, strict}` data-validation; a calc sheet + a small VLOOKUP/rate reference region (the DAG interior, not exposed); two named output `TableSpec`s — `Calculate_Tax` (caption "Compute federal tax from income & filing", rows `tax_owed`=18241 / `effective_rate`=0.182 as `Formula::set_result` cached oracles) and `Estimate_Refund` (caption "Estimate refund given withholding", row `refund`=-3241). Wire the formula DAG so `Calculate_Tax` cells are reachable only from `income`+`filing` and `Estimate_Refund` adds `withheld` (so Plan 03's DAG-derived inputs prove the §4.2 example). Generate `template.xlsx` via the env-gated `PMCP_REGEN_FIXTURES=1 cargo test -p pmcp-workbook-compiler regenerate_fixtures -- --ignored` path (add a `regenerate_template` arm) and COMMIT the produced `.xlsx` + a `template.gen.json` sidecar (the reproducible-fixture discipline). Copy the committed `template.xlsx` to BOTH `cargo-pmcp/src/templates/workbook_bundle/template.xlsx` and `crates/pmcp-workbook-compiler/tests/fixtures/template.xlsx` (one source, two consumers — Open-Q3). Add `crates/pmcp-workbook-compiler/tests/template_provenance.rs` asserting `classify(template.xlsx) == ProvenanceClass::ExcelTrusted` (reuse the assertion style from the existing fixture provenance check). Do NOT delete the old `tax-calc`/`leap1900` fixtures in this plan — they are removed in Plan 04 once the harvest+emit path no longer needs them as the legacy oracle.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --test template_provenance</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `cargo-pmcp/src/templates/workbook_bundle/template.xlsx` AND `crates/pmcp-workbook-compiler/tests/fixtures/template.xlsx` (byte-identical, one source).
    - File exists: `cargo-pmcp/src/templates/workbook_bundle/template.gen.json` (regeneration sidecar).
    - Behavior: `cargo test -p pmcp-workbook-compiler --test template_provenance` exits 0; the test asserts `ProvenanceClass::ExcelTrusted`.
    - Behavior: re-running `PMCP_REGEN_FIXTURES=1 cargo test -p pmcp-workbook-compiler regenerate_template -- --ignored` reproduces a template whose `classify()` is `ExcelTrusted` (deterministic regen).
    - Source: the template carries Excel Tables named `Inputs`, `Calculate_Tax`, `Estimate_Refund` (verified via a umya `get_tables()` read in the test) with the tier and a sample enum data-validation present.
  </acceptance_criteria>
  <done>A provenance-valid, regeneratable template.xlsx is committed in both consumer locations and proven ExcelTrusted by an automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author (rust_xlsxwriter) → committed .xlsx | The template becomes the honest reference oracle; a umya-authored book here would be UmyaFabricated and silently mislead |
| committed .xlsx → provenance gate | The gate must classify the shipped template ExcelTrusted, not fabricated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Spoofing | template provenance identity | mitigate | Author with rust_xlsxwriter only (ExcelTrusted by construction, RESEARCH §2); template_provenance.rs asserts `classify()==ExcelTrusted` — a umya-authored regression fails the test |
| T-100-02 | Tampering | committed fixture drift from its generator | mitigate | env-gated `regenerate_template` + committed `.gen.json` sidecar make the template reproducible; drift is detectable by re-running the deterministic generator |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — rust_xlsxwriter 0.95 + umya 3.0.0 are existing workspace deps (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-compiler --lib fixture_author` green (Table-author surface).
- `cargo test -p pmcp-workbook-compiler --test template_provenance` green (ExcelTrusted).
- `template.xlsx` present in both the CLI templates dir and the compiler test fixtures dir.
</verification>

<success_criteria>
WBV2-01 satisfied: a provenance-valid `template.xlsx` (Inputs Table with tier + sample enum dropdown, calc/reference regions, >=1 named output Table with a caption) is shipped, classifies ExcelTrusted, is regeneratable, and doubles as the honest reference fixture. Maps to ROADMAP Success Criterion 4.
</success_criteria>

<output>
Create `.planning/phases/100-excel-workbook-built-in-servers-v2/100-01-SUMMARY.md` when done
</output>
