---
phase: 100-excel-workbook-built-in-servers-v2
plan: 08
type: execute
gap_closure: true
wave: 1
depends_on: [100-07]
autonomous: true
requirements: [WBV2-04, WBV2-05, WBV2-06]
files_modified:
  - cargo-pmcp/src/commands/workbook/explain_surface.rs
  - cargo-pmcp/src/commands/workbook/explain.rs
  - cargo-pmcp/src/lib.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
  - crates/pmcp-workbook-compiler/src/artifact/cell_map.rs
  - crates/pmcp-server-toolkit/src/workbook/handler.rs
  - crates/pmcp-server-toolkit/src/workbook/schema.rs

must_haves:
  truths:
    - "`cargo pmcp workbook explain template.xlsx` previews the SAME tool names + per-tool input keys/types the served binary registers (stripped keys, DAG-derived inputs, range + cross-sheet cases)"
    - "A served input row whose `name` is value-shaped (e.g. `60000`) fails compile with a cell-located Error"
    - "An output Table whose name sanitizes to a reserved meta-tool name (explain/get_manifest/diff_version/render_workbook) fails compile with a cell-located Error"
    - "Two output keys that strip to the same served key fail compile"
    - "`get_manifest` advertises the stripped served key (== the served tool schema's keys), not the raw in_/out_ prefixed name"
    - "Perturbing one output's cached value blocks the per-tool reconcile (the per-tool oracle grades the table path)"
  artifacts:
    - path: "cargo-pmcp/src/commands/workbook/explain_surface.rs"
      provides: "explain projecting through the production build_tools/json_key_for_role projection (bespoke walker deleted)"
      contains: "project_tool_surface_from_workbook"
    - path: "crates/pmcp-workbook-compiler/src/lib.rs"
      provides: "public non-persisting tool-surface projection + validate_output_keys + value-shaped input reject + reserved/collision output-key gates + per-tool oracle wiring"
      contains: "pub fn project_tool_surface"
    - path: "crates/pmcp-server-toolkit/src/workbook/handler.rs"
      provides: "get_manifest input/output projections surfacing the stripped json_key"
      contains: "json_key_for_role"
  key_links:
    - from: "cargo-pmcp/src/commands/workbook/explain_surface.rs"
      to: "pmcp_workbook_compiler::project_tool_surface_from_workbook"
      via: "explain drives the production projection (build_tools)"
      pattern: "project_tool_surface_from_workbook"
    - from: "crates/pmcp-workbook-compiler/src/lib.rs"
      to: "validate_output_keys"
      via: "stage-1 Error gate (refuse_uncallable_outputs)"
      pattern: "validate_output_keys"
    - from: "crates/pmcp-server-toolkit/src/workbook/handler.rs"
      to: "pmcp_workbook_runtime::json_key_for_role"
      via: "curated_manifest input/output projection"
      pattern: "json_key_for_role"
---

<objective>
Close the six verified pmcp.run dev-team review findings that gate Phase 100 publish:
the three HIGH blockers (H1 explain divergence, H2 value-shaped input name, H3
reserved-tool-name collision) and the three substantive MEDIUM findings (M4 output-key
collision, M5 get_manifest raw name, M6 vacuous per-tool reconcile).

Purpose: the preview (`explain`), the discovery surface (`get_manifest`), and the
compile-time gates must agree, byte-for-byte, with what the served multi-tool binary
actually registers — so a BA's pre-deploy preview cannot lie, an uncallable/colliding
tool cannot ship, and the per-tool reconcile actually grades the production table path.

Output: an explain command re-pointed at the production projection (bespoke walker
deleted); a public non-persisting tool-surface projection in the compiler; value-shaped
input + reserved-name + output-key compile gates; a stripped-key get_manifest; a wired
per-tool oracle; and the central explain↔served parity test over `template.xlsx`.

The 4 LOW findings are explicitly DEFERRED (see `<deferred>` — do NOT implement them).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-REVIEW-pmcp-run.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-07-SUMMARY.md

<interfaces>
<!-- Production projection functions the executor MUST reuse — already in the codebase.
     explain MUST project through these; do NOT re-derive from scratch. -->

From crates/pmcp-workbook-runtime/src/manifest_model.rs:
- `pub fn json_key_for_role(role: &CellRole) -> String` — name(strip in_/out_) → meaning → cell key. The served-key source of truth (F3 strip lives here).
- `pub fn sanitize_tool_name(raw: &str) -> Result<String, String>` — the single shared MCP-charset sanitizer.
- `pub fn upstream_input_leaves(dag, cell_key, input_cells) -> ...` — the per-tool DAG derivation (handles ranges + cross-sheet refs natively).

From crates/pmcp-workbook-compiler/src/artifact/cell_map.rs:
- `pub fn build_tools(manifest: &Manifest, dag: &Dag, output_tables: &[OutputTable]) -> Result<(Vec<Tool>, Vec<LintFinding>), String>` — the per-Table fan-out with DAG-derived `input_keys` + oracle. The SERVED surface primitive.
- `pub struct OutputTable { name, description, output_cells }` and `pub use ...{CellEntry, CellMap, Tool}`.
- `pub fn reconcile_tools(computed, tools) -> Result<ToolReconcileReport, String>`; `ToolReconcileReport::any_mismatch`.
- `fn oracle_value(role) -> Option<CellValue>` — currently returns None for outputs (the M6 hole; outputs are never tiered).
- `pub(crate) fn entry(role)`, `pub(crate) fn shared_inputs(manifest)` — the shared-pool projection.

From crates/pmcp-workbook-compiler/src/lib.rs (the production compile pipeline, already wired in 100-07):
- `fn promote_harvested_tables(manifest, map)` — re-roles Table output formula cells → Role::Output, names input rows.
- `fn output_tables_from_harvest(map, manifest) -> Vec<OutputTable>` — per-Table membership.
- `fn validate_input_keys(manifest, report)` + `fn refuse_uncallable_inputs(manifest) -> Result<(), CompileError>` — the stage-1 input-key gate to MIRROR for outputs/value-shaped names.
- `fn comparison_from_outputs(map, manifest) -> reconcile::ComparisonMap` — builds the authored cached-`<v>` value-by-key map (the M6 oracle source to reuse).
- `fn build_ir_and_dag(map, manifest) -> Result<(ir, Dag), _>`; `fn refuse_colliding_output_tables(output_tables)` (stage-1 collision gate to extend for reserved names).

From crates/pmcp-server-toolkit/src/workbook/handler.rs:
- `ExplainHandler::NAME = "explain"`, `GetManifestHandler::NAME = "get_manifest"`, `DiffVersionHandler::NAME = "diff_version"`, `RenderWorkbookHandler::NAME = "render_workbook"` — derive the reserved set from these constants; do NOT hardcode a drifting copy.
- `fn curated_manifest(bundle, stamp)` / `fn input_projection(role)` — currently emit `role.name` (raw prefixed); M5 fixes these to emit the stripped `json_key`.

From crates/pmcp-server-toolkit/src/workbook/schema.rs:
- `pub fn input_schema_for_tool(manifest, cell_map, tool)`, `pub fn output_schema_for_tool(manifest, tool)` — the SERVED schema (uses `entry.json_key`); the parity test asserts get_manifest/explain == these keys.
</interfaces>

<source_of_truth>
The verified findings (file:line + prescribed fix + acceptance per finding) live in:
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-REVIEW-pmcp-run.md
</source_of_truth>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-point `explain` at the production projection (H1) + the parity test</name>
  <read_first>
    - cargo-pmcp/src/commands/workbook/explain_surface.rs (the divergent re-implementation to REPLACE: harvest_input_pool / tool_for_table / reachable_addrs / extract_a1_refs / is_input_table)
    - cargo-pmcp/src/commands/workbook/explain.rs (the thin CLI arm — its `execute` drives `explain_workbook`/`format_tool_surface`)
    - cargo-pmcp/src/lib.rs (the `#[path]` lib seam `pub mod workbook_explain` the example + integration test reach)
    - crates/pmcp-workbook-compiler/src/lib.rs (compile_workbook_inner steps 2→3b-tables→7a: ingest → stage1 synth → promote_named_outputs → name_named_inputs → promote_harvested_tables → build_ir_and_dag → output_tables_from_harvest — the EXACT pre-emit pipeline explain must reuse)
    - crates/pmcp-workbook-compiler/src/artifact/cell_map.rs (build_tools / OutputTable / json_key_for_role usage / upstream_input_leaves)
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (input_schema_for_tool / output_schema_for_tool — the served surface the parity test compares against)
  </read_first>
  <action>
    Add a PUBLIC, non-persisting projection to the compiler crate (crates/pmcp-workbook-compiler/src/lib.rs): a `pub fn project_tool_surface_from_workbook(workbook_path: &Path) -> Result<(Manifest, Dag, Vec<OutputTable>), CompileError>` (or an equivalently-named public function returning the production `Vec<Tool>` directly). It MUST run the SAME pre-emit pipeline `compile_workbook_inner` uses — `ingest::ingest` → `stage1::run_stage1` synth → `promote_named_outputs` → `name_named_inputs` → `promote_harvested_tables` → `build_ir_and_dag` → `output_tables_from_harvest` → `build_tools` — and STOP before ratify/reconcile/emit (a read-only projection, writes NO bundle). Reuse the existing private steps; do NOT fork a second pipeline. Per-tool inputs come from `build_tools`' DAG-derived `input_keys` (`upstream_input_leaves`), per-tool input/output keys from `json_key_for_role` (F3 strip applied), tool names from `sanitize_tool_name`.

    Then rewrite cargo-pmcp/src/commands/workbook/explain_surface.rs to DRIVE that production projection instead of re-deriving the surface. DELETE the bespoke derivation entirely: `harvest_input_pool`, `tool_for_table`, `inputs_fed_by_any_table`, `reachable_addrs`, `extract_a1_refs`, `is_input_table`, `PoolEntry`, and their A1/column helpers that exist only to feed them. Keep the `ToolSurface`/`InputParam`/`OutputField` render DTOs + `format_tool_surface`/`render_text` (the BA-facing text/json renderers) but POPULATE them by mapping the production `Tool` list (names, per-tool `input_keys` → typed `InputParam` via the served `input_schema_for_tool`/`json_key_for_role`, outputs via `output_schema_for_tool`/the manifest role dtype) — so explain output cannot drift from the served surface by construction. Update `explain_workbook` to call the new compiler projection. Update `explain.rs` only if its call signature changes; keep its `--format`/advisory-header behavior.

    Central parity test (load-bearing, per the H1 acceptance): add a test that compiles `template.xlsx` through the production path AND runs `explain_workbook` on the SAME workbook, asserting explain's tool names + per-tool input keys + types are byte-identical to the served tool surface (the `input_schema_for_tool`/`output_schema_for_tool` keys), including the stripped keys and a DAG-derived input reached via a range and/or a cross-sheet ref. If `template.xlsx` lacks a range/cross-sheet output formula, extend the fixture-author spec (`fixture_author.rs::template_spec`) to include one and regenerate the fixture via the existing env-gated `regenerate_template`, keeping both `template.xlsx` copies + `template.gen.json` byte-identical (mirror the 100-07 regeneration). Place the test where `compile_workbook_with_fixture_override` is reachable (in-`src` `#[cfg(test)]`, per the 100-07 CR-01 reachability rule) if it must drive the override-compile; otherwise a cargo-pmcp integration test that shells the public projection is acceptable. This parity test ALSO closes the WR-01 cross-sheet bug for free.

    Add an explain-projection property/fuzz arm: over arbitrary harvested input/output Table shapes, assert explain's projected per-tool input-key SET equals `build_tools`' `input_keys` set for the same manifest+dag (the projections cannot diverge).
  </action>
  <acceptance_criteria>
    - explain_surface.rs no longer contains `reachable_addrs`, `extract_a1_refs`, `is_input_table`, `harvest_input_pool`, or `tool_for_table` (grep returns zero matches: `grep -nE 'fn (reachable_addrs|extract_a1_refs|is_input_table|harvest_input_pool|tool_for_table)' cargo-pmcp/src/commands/workbook/explain_surface.rs` == 0).
    - explain_surface.rs references `project_tool_surface_from_workbook` (or the new public compiler projection) — `grep -c project_tool_surface_from_workbook cargo-pmcp/src/commands/workbook/explain_surface.rs` ≥ 1.
    - The parity test asserts explain's tool names == the served `cell_map.tools[*].name` (sanitized) for `template.xlsx`, AND for EACH tool explain's input keys == `input_schema_for_tool(...).properties.inputs.properties` keys and output keys == `output_schema_for_tool(...).properties.outputs.properties` keys (stripped, no `in_`/`out_`).
    - The parity test includes at least one input reached through a range (`SUM(B2:B9)`) and/or a cross-sheet ref (`Sheet2!B5`) and asserts that input IS surfaced on the consuming tool (the old walker dropped these).
    - A property/fuzz test asserts explain's per-tool input-key set == `build_tools` `input_keys` set for the same (manifest, dag).
    - `cargo fmt --all -- --check` clean; `cargo clippy -p pmcp-workbook-compiler -p cargo-pmcp --all-targets` 0 new warnings; `cargo test -p pmcp-workbook-compiler && cargo test -p cargo-pmcp --test workbook_explain` green.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler && cargo test -p cargo-pmcp --test workbook_explain && grep -qE 'fn (reachable_addrs|extract_a1_refs|is_input_table)' cargo-pmcp/src/commands/workbook/explain_surface.rs && echo BESPOKE_STILL_PRESENT && exit 1 || echo OK</automated>
  </verify>
  <done>explain projects through the production build_tools projection; the bespoke walker is deleted; the explain↔served parity test over template.xlsx (incl. a range/cross-sheet input) passes; the projection-equivalence property test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile-time gates — value-shaped input name (H2), reserved-name (H3), output-key collision (M4)</name>
  <read_first>
    - crates/pmcp-workbook-compiler/src/lib.rs (validate_input_keys ~975, refuse_uncallable_inputs ~1074, refuse_colliding_output_tables ~910, the stage-1 Error gate at compile_workbook_inner steps 3c/7b, and the gated-update lane prepare_candidate_inner ~1205/1210/1244 which MUST run the same gates)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (json_key_for_role ~175, strip_governance_prefix ~189, sanitize_tool_name ~218)
    - crates/pmcp-workbook-compiler/src/artifact/cell_map.rs (tool_name_collision_findings ~350, OutputTable, the existing LintFinding shape + split_cell_key)
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (the four reserved NAME constants: ExplainHandler::NAME / GetManifestHandler::NAME / DiffVersionHandler::NAME / RenderWorkbookHandler::NAME)
  </read_first>
  <action>
    H2 — value-shaped input name: in `validate_input_keys`, after computing the served `key = json_key_for_role(role)` and the empty-check, REJECT a value-shaped served key with a new cell-precise `Severity::Error` LintFinding (a new `value_shaped_input_key_finding(cell, key)` mirroring `unnamed_input_finding`/`empty_input_key_finding`). "Value-shaped" = the key parses as a number (integer or decimal, optional leading sign) — i.e. a `name` of `60000` (or `1.5`, `-3`) is rejected; a valid identifier (`income`, `q1_2024`) passes. Keep the rule narrow (numeric-only), not "any key starting with a digit", to avoid false-positives on legitimate identifiers. This finding feeds the SAME `refuse_uncallable_inputs` Error gate already wired into both compile lanes — no new gate call site needed.

    H3 — reserved-tool-name collision: derive the reserved meta-tool set from the handler NAME constants (re-export them as a `pub const` slice/array from the toolkit, e.g. `pmcp_server_toolkit::workbook::RESERVED_TOOL_NAMES`, sourced from `[ExplainHandler::NAME, GetManifestHandler::NAME, DiffVersionHandler::NAME, RenderWorkbookHandler::NAME]`, so it cannot drift). Add a blocking compile-time check (a `refuse_reserved_output_table_names(output_tables)` that the compiler calls ALONGSIDE `refuse_colliding_output_tables` at step 7b in BOTH lanes): for each OutputTable, `sanitize_tool_name(&table.name)` and if the result is in the reserved set, push a cell-precise `Severity::Error` LintFinding located at the Table's first output cell, surfaced as `CompileError::Lint` via the shared `stage1::render_aggregate`. The compiler must depend on (or re-export) the reserved-name source — if a compiler→toolkit dependency is undesirable, define the reserved NAME constants in `pmcp-workbook-runtime` (the shared leaf) and have BOTH the toolkit handlers and the compiler gate read them from there (single source). Choose the wiring that keeps `make purity-check` green (runtime is the existing shared leaf).

    M4 — output-key collision: add `fn validate_output_keys(manifest, report)` mirroring `validate_input_keys` over each tool's OUTPUT keys (the `Role::Output` cells' served `json_key_for_role`): flag (a) duplicate served keys (two outputs stripping to the same key), (b) empty/whitespace served key, (c) value-shaped served key — each a cell-precise `Severity::Error`. Add a `fn refuse_uncallable_outputs(manifest) -> Result<(), CompileError>` mirroring `refuse_uncallable_inputs`, and wire it into the stage-1 Error gate in BOTH compile lanes (next to `refuse_uncallable_inputs` at step 3c). Reuse `split_cell_key` + `stage1::render_aggregate`.

    All three gates run in BOTH `compile_workbook_inner` (seed lane) AND `prepare_candidate_inner` (gated-update lane) so the seed/update paths cannot drift. Keep each new fn under cog 25 (extract a thin finding-builder per defect like the existing input-key findings). Add unit tests: a numeric input name fails compile with a cell-located error; a valid identifier compiles; an output Table named to sanitize to `explain` fails compile with a reserved-name error; two outputs stripping to one key fail compile; the corpus (tax-calc/loan-calc/leap1900 + template.xlsx) still compiles. Add a property arm: any served key that is a finite numeric literal is rejected by validate_input_keys AND validate_output_keys (round-trip the reject predicate).
  </action>
  <acceptance_criteria>
    - An input row whose `name` is `60000` fails compile with a cell-located Error whose message names the offending cell; a `name` of `income` still compiles (unit tests assert both).
    - An output Table whose name sanitizes to `explain` (e.g. authored `Explain` / `ex-plain`) fails compile with a reserved-name Error located at the Table's first output cell.
    - The reserved set is DERIVED from the handler NAME constants (or a shared-runtime const both read) — `grep` shows the compiler gate referencing `RESERVED_TOOL_NAMES` / the NAME constants, NOT a hardcoded string-literal list of four names.
    - Two output rows whose served keys strip to the same key fail compile (`validate_output_keys` unit test); `validate_output_keys` is wired into `refuse_uncallable_outputs` and called in BOTH lanes (`grep -c refuse_uncallable_outputs crates/pmcp-workbook-compiler/src/lib.rs` ≥ 3: defn + 2 call sites).
    - `make purity-check` green (no new compiler→non-leaf dependency that breaks the gate).
    - `cargo test -p pmcp-workbook-compiler -p pmcp-server-toolkit` green; corpus + template.xlsx still compile; PMAT cog ≤25 over touched src files.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler -p pmcp-server-toolkit --features "workbook workbook-embedded" && [ "$(grep -c refuse_uncallable_outputs crates/pmcp-workbook-compiler/src/lib.rs)" -ge 3 ] && make purity-check</automated>
  </verify>
  <done>value-shaped input names, reserved-name output Tables, and colliding output keys all fail compile with cell-located errors in BOTH compile lanes; reserved set derived from the handler NAME constants; purity gate green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: get_manifest stripped key (M5) + wire the per-tool oracle (M6)</name>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (curated_manifest ~400, input_projection ~389, the output projection ~409 — currently emit raw `role.name`)
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (input_schema_for_tool / output_schema_for_tool — the served keys get_manifest must match)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (json_key_for_role ~175 — the stripped-key source)
    - crates/pmcp-workbook-compiler/src/artifact/cell_map.rs (build_one_tool ~135 oracle build, oracle_value ~424 — the None-returning hole; comparison_from_outputs_for_tool ~246 the consumer; reconcile_tools)
    - crates/pmcp-workbook-compiler/src/lib.rs (comparison_from_outputs ~615 — the authored cached-`<v>` value-by-key map to source the per-tool oracle from; reconcile_output_tables ~934 the per-tool reconcile call; build_ir_and_dag/seed_from_inputs/eval producing the run)
  </read_first>
  <action>
    M5 — get_manifest stripped key: in `curated_manifest` (handler.rs), replace the raw `role.name` emitted for the input and output projections with the served key `json_key_for_role(role)` (the SAME stripped key `input_schema_for_tool`/`output_schema_for_tool` advertise), so discovery == call surface. Keep the raw prefixed `role.name` only as an internal/governance field if a consumer needs it (emit it under a distinct key like `governance_name`, not as the advertised `name`). Apply to both `input_projection` and the inline output `json!` block. Add a toolkit test asserting `get_manifest`'s advertised input keys == the served tool schema's input keys and its output keys == the served output-schema keys for a multi-tool bundle (e.g. the template fixture) — a stripped `income`/`tax_owed`, never `in_income`/`out_tax_owed`.

    M6 — wire the per-tool oracle (PREFERRED per the finding): populate each tool's `oracle` from the cached workbook cell values so per-tool reconcile actually grades the table path. In `build_tools`/`build_one_tool` (cell_map.rs), the oracle currently comes from `oracle_value(role)` which returns `None` for outputs (outputs are never tiered). Change the oracle source so it carries each output cell's authored cached `<v>` value: pass the authored value-by-key map (the SAME map `comparison_from_outputs` builds from the cached cells in lib.rs) INTO `build_tools` (a new `output_oracles: &BTreeMap<String, CellValue>` param keyed by cell key, or reuse the existing `comparison_from_outputs` ComparisonMap), and in `build_one_tool` look up each output cell's cached value to fill `oracle[json_key]`. Then in `reconcile_output_tables` (lib.rs), grade each derived tool's COMPUTED outputs (from `run.computed`) against that cached oracle via `reconcile_tools`/`comparison_from_outputs_for_tool` — which now has non-empty per-tool oracles, so a per-tool mismatch genuinely blocks. Keep the change ADDITIVE: do NOT remove the shared step-7 `comparison_from_outputs` reconcile (it remains the binding named-output gate); the per-tool reconcile becomes the per-tool safety net the multi-tool design promised. Update `oracle_value`'s now-stale comment to reflect that output oracles are supplied by the orchestrator's cached-value map (no longer "supplied by Plan 04" / a dead net).

    Add a test that perturbs ONE output's cached value in the compile fixture (or constructs a manifest+dag+output_tables with a deliberately-wrong cached oracle for one tool) and asserts the per-tool reconcile (`reconcile_output_tables`/`reconcile_tools.any_mismatch`) BLOCKS the emit (`CompileError::Reconcile`). Confirm the named-range corpus (which routes the empty-set single-tool fallback) is unaffected. If — and only if — full wiring proves genuinely out of scope (it should NOT, the cached map already exists in `comparison_from_outputs`), the documented fallback is to remove the per-tool `reconcile_output_tables` call and correct every comment/commit that claims it gates the emit; prefer wiring.

    Keep cog ≤25 (the oracle-fill loop stays a thin helper). Run fmt/clippy/test before commit.
  </action>
  <acceptance_criteria>
    - `get_manifest`'s advertised input keys == `input_schema_for_tool` keys and output keys == `output_schema_for_tool` keys (toolkit test): stripped (`income`/`tax_owed`), never `in_`/`out_` prefixed.
    - `curated_manifest` emits `json_key_for_role(role)` as the advertised `name` for inputs AND outputs — `grep -c json_key_for_role crates/pmcp-server-toolkit/src/workbook/handler.rs` ≥ 2.
    - After wiring, each tool's `oracle` is NON-empty for `template.xlsx` (a test asserts `build_tools(...)[i].oracle` is non-empty for an output-bearing tool); the per-tool reconcile is no longer vacuous.
    - A test perturbs one output's cached value and asserts the per-tool reconcile BLOCKS the emit (`CompileError::Reconcile` / `any_mismatch() == true`); an unperturbed compile passes.
    - The named-range corpus + the shared step-7 reconcile are unchanged (regression-green); `oracle_value`'s comment no longer claims the oracle is "supplied by Plan 04"/a dead net.
    - `cargo test -p pmcp-workbook-compiler -p pmcp-server-toolkit` green; `make purity-check` green; PMAT cog ≤25.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler -p pmcp-server-toolkit --features "workbook workbook-embedded" && [ "$(grep -c json_key_for_role crates/pmcp-server-toolkit/src/workbook/handler.rs)" -ge 2 ] && make purity-check</automated>
  </verify>
  <done>get_manifest advertises the stripped served key (== served schema); each tool's oracle is populated from the cached cell values so the per-tool reconcile blocks on a perturbed output; corpus + shared reconcile unchanged.</done>
</task>

</tasks>

<deferred>
The 4 LOW findings from 100-REVIEW-pmcp-run.md are EXPLICITLY OUT OF SCOPE for this plan
(tracked follow-up, NOT 100-08):
- **L-i:** overrides keys are raw (`in_income`) while inputs are stripped (`income`) in the same schema (schema.rs:401 vs :320).
- **L-ii:** the legacy `in_*`/`out_*` compile path still runs alongside the table path (lib.rs:348-350,651-668) — Rule-4 deferral; do NOT remove `promote_named_outputs`/`name_named_inputs`.
- **L-iii:** stale rustdoc says "five tools / calculate" (mod.rs:5-6,164-165).
- **L-iv:** the `tax-calc@1.1.0` byte-stability golden is still hand-built/authoritative.
Do NOT implement these. Honor the Rule-4 deferral (keep the named-range path; keep `make purity-check` green).
</deferred>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BA-authored .xlsx → compiler | Untrusted workbook content (Table names, input/output `name` cells) crosses into the compile gates and the served surface |
| compiler projection → explain preview | The preview must equal the served surface; divergence is a silent-broken-deploy (the H1 class) |
| get_manifest discovery → tool call | An agent reads get_manifest then calls a tool; key mismatch = uncallable (M5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-08-H1 | Spoofing/Info-disclosure | `explain` preview vs served surface | mitigate | explain projects through the production `build_tools`/`json_key_for_role`; bespoke walker deleted; the explain↔served parity test over `template.xlsx` is the binding proof (Task 1) |
| T-100-08-H2 | Tampering | `validate_input_keys` value-shaped name | mitigate | reject a numeric/value-shaped served input key with a cell-precise `Severity::Error` in BOTH compile lanes (Task 2) |
| T-100-08-H3 | Spoofing | output Table name vs reserved meta-tool set | mitigate | blocking compile-time check rejecting any output-Table name sanitizing to {explain,get_manifest,diff_version,render_workbook}, reserved set derived from the handler NAME constants (Task 2) |
| T-100-08-M4 | Tampering | `validate_output_keys` collision | mitigate | mirror `validate_input_keys` over output keys (dup/empty/value-shaped); wire into the stage-1 Error gate in both lanes (Task 2) |
| T-100-08-M5 | Info-disclosure | `get_manifest` raw vs served key | mitigate | surface the stripped `json_key` in get_manifest input/output projections so discovery == call surface (Task 3) |
| T-100-08-M6 | Repudiation | vacuous per-tool reconcile | mitigate | populate each tool's oracle from the cached cell values so per-tool reconcile actually grades the table path; a perturbed cached value blocks the emit (Task 3) |
| T-100-08-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan (no new dependencies; reuse existing runtime/compiler/toolkit functions) |
</threat_model>

<verification>
- explain↔served parity test over `template.xlsx` passes (tool names + per-tool input/output keys + types byte-identical; incl. a range/cross-sheet input).
- A numeric input name (`60000`) fails compile with a cell-located error; a valid identifier compiles.
- An output Table sanitizing to a reserved meta-tool name fails compile.
- Two outputs stripping to one key fail compile.
- `get_manifest` advertised keys == served tool schema keys (stripped).
- Perturbing one output's cached value blocks the per-tool reconcile.
- `make quality-gate` + `make purity-check` green; zero clippy warnings; PMAT cog ≤25 over touched src/ files; each task ran `cargo fmt` + `cargo clippy --all-targets` + `cargo test` before its commit (no pre-commit hook installed).
- ALWAYS arms present: unit + a property/fuzz arm for the new behaviors (Task 1 projection-equivalence; Task 2 numeric-key reject predicate).
</verification>

<success_criteria>
All six findings (H1, H2, H3, M4, M5, M6) closed with their REVIEW-prescribed acceptance
tests passing; the explain↔served parity test is present and green; the 4 LOW findings
remain deferred (untouched); the Rule-4 named-range path is intact; `make quality-gate`
and `make purity-check` are green.
</success_criteria>

<output>
REQUIRED ORDER (executor): Write SUMMARY.md → commit → only then narration.

Create `.planning/phases/100-excel-workbook-built-in-servers-v2/100-08-SUMMARY.md` when done.

Note the documented placement deviation precedent (100-07): tests that drive
`compile_workbook_with_fixture_override` live in-`src` `#[cfg(test)]` (the CR-01
reachability rule), NOT external `tests/` integration crates. The parity test (Task 1)
may follow that placement; record the choice in the SUMMARY.
</output>
