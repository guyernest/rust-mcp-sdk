---
phase: 100-excel-workbook-built-in-servers-v2
plan: 07
type: execute
gap_closure: true
wave: 7
depends_on: [100-03, 100-04]
files_modified:
  - crates/pmcp-workbook-compiler/src/artifact/mod.rs
  - crates/pmcp-workbook-compiler/src/artifact/cell_map.rs
  - crates/pmcp-workbook-compiler/src/gate/accept.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
  - crates/pmcp-workbook-compiler/src/reemit_golden.rs
  - crates/pmcp-server-toolkit/src/workbook/schema.rs
  - crates/pmcp-workbook-compiler/tests/template_compile_e2e.rs
autonomous: true
requirements: [WBV2-04]

user_setup: []

must_haves:
  truths:
    - "A real (full ingest→synth→DAG→emit) compile of the Table-authored template.xlsx emits TWO MCP tools — calculate_tax and estimate_refund — not one workflow-named tool"
    - "calculate_tax and estimate_refund each carry a NON-EMPTY, DAG-derived input_keys list, and the two lists are DISJOINT on withheld (estimate_refund has it, calculate_tax does not)"
    - "The served per-tool input schema (input_schema_for_tool) for a production-compiled tool advertises every key the runtime accepts — never an empty inputs.properties while validate_input accepts the pool (CR-02 closed)"
    - "tool_name_collision_findings runs at compile time: two output Tables sanitizing to the same MCP name fail compile with a cell-precise Error before any bundle is written"
    - "reconcile_tools grades each tool against its own oracle on the production path; any per-tool oracle mismatch blocks the emit"
    - "The reemit_golden producer/consumer proof FAILS if a fresh compile regresses to a single tool or to empty input_keys (the subset-only blindspot is closed)"
    - "The named-range fixture corpus (tax-calc.xlsx / loan-calc.xlsx / leap1900) still compiles green via the single-tool fallback (zero output Tables harvested)"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/artifact/mod.rs"
      provides: "emit_bundle wires build_tools (multi-tool) when output Tables are supplied, else build_cell_map fallback"
      contains: "build_tools"
    - path: "crates/pmcp-workbook-compiler/src/lib.rs"
      provides: "compile_workbook_inner derives OutputTable membership from harvested table_records, folds collision findings into the stage-1 Error gate, and reconciles per-tool"
      contains: "tool_name_collision_findings"
    - path: "crates/pmcp-workbook-compiler/tests/template_compile_e2e.rs"
      provides: "the central real-compile E2E proof (2 tools, disjoint populated input_keys, non-empty served schema)"
      contains: "estimate_refund"
    - path: "crates/pmcp-workbook-compiler/src/reemit_golden.rs"
      provides: "tightened proof asserting exact tool count + populated input_keys"
      contains: "input_keys"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/lib.rs (compile_workbook_inner)"
      to: "build_tools / tool_name_collision_findings / reconcile_tools"
      via: "OutputTable membership derived from map.sheets[].table_records + the already-built dag"
      pattern: "build_tools|tool_name_collision_findings|reconcile_tools"
    - from: "crates/pmcp-workbook-compiler/src/artifact/mod.rs (emit_bundle)"
      to: "build_tools"
      via: "PromoteInputs threads output_tables + dag into emit_bundle"
      pattern: "build_tools"
    - from: "crates/pmcp-server-toolkit/src/workbook/schema.rs (input_schema_for_tool)"
      to: "tool.input_keys"
      via: "per-tool projection over populated input_keys (non-empty served schema)"
      pattern: "input_keys"
---

<objective>
Close the Phase 100 BLOCKER gap (WBV2-04 / ROADMAP Success Criterion #2): wire the
per-Table multi-tool fan-out — `build_tools` + `tool_name_collision_findings` +
`reconcile_tools` — into the PRODUCTION compile pipeline, replacing the transitional
single-tool `build_cell_map` call at `artifact/mod.rs:180`. Today `emit_bundle` always
emits ONE `manifest.workflow` tool with `input_keys: Vec::new()`; the per-Table
primitives landed in Plan 03/04 have ZERO non-test callers (confirmed in
`100-VERIFICATION.md`). The served-side wiring (registration loop, handler, schema) is
already correct — the gap is exclusively the compiler emit path.

Purpose: make the headline deliverable reach production. A real compile of the
Table-authored `template.xlsx` must emit `calculate_tax` + `estimate_refund`, each with
a DAG-derived, populated, disjoint `input_keys` and a non-empty served input schema —
closing CR-01 (dead fan-out) and CR-02 (empty served schema) together. The committed
golden's subset-only proof masked the gap; this plan tightens it so a single-tool
regression can never pass again.

Output: a wired multi-tool emit path with a single-tool fallback for the named-range
corpus, the stage-1 collision gate, per-tool reconcile, a tightened reemit proof, and a
new real-compile E2E test that is the authoritative WBV2-04 proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/100-excel-workbook-built-in-servers-v2/100-VERIFICATION.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-REVIEW.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-04-SUMMARY.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-03-SUMMARY.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

From crates/pmcp-workbook-compiler/src/artifact/cell_map.rs (Plan 03/04 primitives — WIRED-FROM-test-only today):

  pub struct OutputTable { pub name: String, pub description: Option<String>, pub output_cells: Vec<String> }
  pub fn build_tools(manifest: &Manifest, dag: &Dag, output_tables: &[OutputTable])
        -> Result<(Vec<Tool>, Vec<LintFinding>), String>   // input_keys = DAG-derived, mapped to served json_keys
  pub fn tool_name_collision_findings(output_tables: &[OutputTable]) -> Vec<LintFinding>  // Severity::Error on collide/unmappable
  pub fn reconcile_tools(computed: &BTreeMap<String, CellValue>, tools: &[Tool]) -> Result<ToolReconcileReport, String>
  pub fn build_cell_map(manifest: &Manifest) -> Result<CellMap, String>  // the TRANSITIONAL single-tool path (fallback)
  // CellMap { inputs: Vec<CellEntry>, tools: Vec<Tool> }; Tool { name, description, input_keys, outputs, oracle }

From crates/pmcp-workbook-compiler/src/artifact/mod.rs:
  pub fn emit_bundle(bundle_id, version, ir: &HashMap<String,Cell>, manifest: &Manifest,
        layout, changelog, parser_equivalence, workbook_hash: String, out_root: &Path) -> Result<BundleLock, EmitError>
  // line ~180: let cell_map = build_cell_map(&ratified).map_err(EmitError::CellMap)?;  <-- THE wiring site

From crates/pmcp-workbook-compiler/src/gate/accept.rs:
  pub struct PromoteInputs<'a> { bundle_id, version, ir, manifest, layout, changelog, parser_equivalence, workbook_hash }
  pub fn promote(lane: &EmitLane, out_root: &Path, inputs: &PromoteInputs<'_>) -> Result<(BundleLock, PathBuf), AcceptError>
  // promote() forwards PromoteInputs straight into emit_bundle (accept.rs:239)

From crates/pmcp-workbook-compiler/src/lib.rs (compile_workbook_inner — the production driver):
  // line 350: let (ir, dag) = build_ir_and_dag(&map, &manifest)?;        <-- dag is in scope at emit time
  // line 360: let comparison = comparison_from_outputs(&map, &manifest); // single-tool reconcile (kept for fallback)
  // line 376-394: build layout/changelog → PromoteInputs → gate::accept::promote(Seed, ...)
  // promote_named_outputs / name_named_inputs SURVIVE here (Rule-4 deferral) — DO NOT remove them this plan
  // stage1::render_aggregate(&errors) already used at line 789 to render an Error LintReport into CompileError::Lint

From crates/pmcp-workbook-compiler/src/ingest/cell_map.rs:
  pub struct TableRecord { pub name: String, pub area: RangeRef, pub columns: Vec<String> }
  // SheetRecord.table_records: Vec<TableRecord>;  RangeRef { sheet, start, end }
  // template.xlsx harvests: Inputs (A3:D6), Calculate_Tax (area A10:C12, caption A9), Estimate_Refund (area A16:C17, caption A15)

From crates/pmcp-server-toolkit/src/workbook/schema.rs:
  pub fn input_schema_for_tool(manifest: &Manifest, cell_map: &CellMap, tool: &Tool) -> Value
  // line 341-352: projects ONLY keys in tool.input_keys; empty input_keys => empty inputs.properties (CR-02)

From crates/pmcp-workbook-compiler/tests/template_harvest_e2e.rs (reusable harvest helpers proving template.xlsx):
  // caption_above(sheet, table) -> Option<&str>  (the caption one row above area.start = tool description)
  // Calculate_Tax caption "Compute federal tax from income & filing"; Estimate_Refund caption "Estimate refund given withholding"
  // oracles: tax_owed=18241, effective_rate=0.182, refund=-3241; withheld input on B6
</interfaces>

<note_on_freshness>
template.xlsx is RAW ExcelTrusted but carries fullCalcOnLoad="1" (the SAME staleness
signal tax-calc.xlsx carries). The production compile_workbook REFUSES it for staleness;
the in-crate #[cfg(test)] compile_workbook_with_fixture_override demotes fullCalcOnLoad to
a Warning (it CANNOT weaken provenance refusal — see reemit_golden.rs). The E2E proof and
the reemit proof therefore compile through compile_workbook_with_fixture_override — this is
a REAL compile (full ingest→harvest→synth→DAG→emit), NOT a golden load.
</note_on_freshness>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread OutputTable membership + dag into emit_bundle; call build_tools with a single-tool fallback</name>
  <read_first>
    - crates/pmcp-workbook-compiler/src/artifact/mod.rs (emit_bundle line 180 — the build_cell_map call site; PromoteInputs/promote forward args here)
    - crates/pmcp-workbook-compiler/src/artifact/cell_map.rs (build_tools / build_cell_map / OutputTable signatures)
    - crates/pmcp-workbook-compiler/src/gate/accept.rs (PromoteInputs struct + promote forwarding emit_bundle, lines 168-264)
  </read_first>
  <behavior>
    - When emit_bundle is given a NON-EMPTY &[OutputTable] + &Dag, the emitted cell_map.tools is the build_tools fan-out (one Tool per output Table, populated input_keys), NOT a single workflow-named tool.
    - When given an EMPTY &[OutputTable], emit_bundle falls back to build_cell_map (single transitional tool) so the named-range corpus is unchanged.
    - A build_tools error (zero output tables passed where non-empty was expected, or an internal failure) surfaces as EmitError::CellMap (unchanged error type).
  </behavior>
  <action>
    Add two borrowed fields to `PromoteInputs` in `gate/accept.rs`: `output_tables: &'a [OutputTable]` and `dag: &'a Dag` (re-export `OutputTable`/`Dag` paths already available via the crate). Forward both from `promote` into a widened `emit_bundle` call. In `emit_bundle` (`artifact/mod.rs`), add matching `output_tables: &[OutputTable]` and `dag: &Dag` params; replace the line-180 `build_cell_map(&ratified)` call with a branch: if `output_tables` is non-empty, call `build_tools(&ratified, dag, output_tables)` and assemble a `CellMap { inputs, tools }` (derive `inputs` the same way `build_cell_map` does — iterate `Role::Input` cells via the shared `entry` helper, exposed from `cell_map.rs` as needed); on `Err` map to `EmitError::CellMap`. Discard the `build_tools` lint findings here (collision/feeds-no-tool gating happens in Task 2 BEFORE emit). If `output_tables` is empty, keep `build_cell_map(&ratified)` verbatim (the corpus fallback). Update the in-crate `emit_bundle` unit tests (`emit_sample`) to pass `&[]` + a trivial `&Dag::new()` so they exercise the fallback unchanged. Do NOT touch `promote_named_outputs`/`name_named_inputs` (Rule-4 deferral stays). Run `cargo fmt -p pmcp-workbook-compiler`, `cargo clippy -p pmcp-workbook-compiler --all-targets`, `cargo test -p pmcp-workbook-compiler` before committing.
  </action>
  <acceptance_criteria>
    - `grep -n "output_tables" crates/pmcp-workbook-compiler/src/gate/accept.rs` shows the field on `PromoteInputs` AND its forward into `emit_bundle`.
    - `grep -n "build_tools" crates/pmcp-workbook-compiler/src/artifact/mod.rs` returns a non-test call site inside `emit_bundle`.
    - A new in-crate unit test in `artifact/mod.rs` (`emit_with_output_tables_fans_out`): passing two `OutputTable`s + a `Dag` yields `cell_map.tools.len() == 2`; passing `&[]` yields `tools.len() == 1` (fallback). `cargo test -p pmcp-workbook-compiler emit_with_output_tables_fans_out` passes.
    - `cargo clippy -p pmcp-workbook-compiler --all-targets` reports zero new warnings; cognitive complexity of the new/edited emit branch stays ≤25 (extract a helper if needed).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --lib emit_with_output_tables_fans_out -- --exact</automated>
  </verify>
  <done>emit_bundle calls build_tools on a non-empty output-table set and falls back to build_cell_map on an empty one; PromoteInputs threads output_tables + dag; the corpus fallback unit test and the new fan-out unit test both pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive OutputTable membership in compile_workbook_inner; gate collisions in stage-1; reconcile per-tool</name>
  <read_first>
    - crates/pmcp-workbook-compiler/src/lib.rs (compile_workbook_inner lines 287-396 — manifest/ir/dag built; PromoteInputs assembled at 383; render_aggregate used at 789)
    - crates/pmcp-workbook-compiler/src/artifact/cell_map.rs (OutputTable, tool_name_collision_findings, reconcile_tools, build_tools)
    - crates/pmcp-workbook-compiler/src/ingest/cell_map.rs (TableRecord { name, area }, SheetRecord.table_records, RangeRef)
    - crates/pmcp-workbook-compiler/tests/template_harvest_e2e.rs (caption_above + area→cells reference logic for tables)
  </read_first>
  <behavior>
    - For a Table-authored workbook (template.xlsx) compile_workbook_inner builds a non-empty Vec<OutputTable>: one per harvested Table whose body cells are Role::Output, name = TableRecord.name, description = the caption one row above area.start, output_cells = the manifest Role::Output cell keys falling inside that Table's area.
    - For a named-range workbook (tax-calc.xlsx — zero output Tables harvested as Role::Output) the derived Vec is EMPTY, so the single-tool fallback path runs and the corpus stays green.
    - tool_name_collision_findings over the derived OutputTables, if it contains any Severity::Error, blocks the compile as CompileError::Lint (cell-precise) BEFORE any bundle write.
    - reconcile_tools grades each derived tool against its own oracle; ToolReconcileReport::any_mismatch blocks the emit (CompileError::Reconcile) — only on the multi-tool path; the named-range fallback keeps the existing comparison_from_outputs reconcile.
  </behavior>
  <action>
    In `lib.rs`, add a private helper `output_tables_from_harvest(map: &ingest::WorkbookMap, manifest: &Manifest) -> Vec<OutputTable>`: for each `SheetRecord.table_records` entry, compute the fully-qualified output cell keys inside `area` (use `RangeRef` start/end with `range_ref::cell_key`; a small A1 row/col expansion helper — keep cognitive complexity ≤25 by extracting an `cells_in_area` helper) and KEEP only those present in `manifest.cells` with `role == Role::Output`; skip Tables that contribute zero output cells (e.g. the Inputs Table). Set `name = record.name`, `description` = the caption cell one row above `area.start` (read from the same `SheetRecord.cells`, mirroring `template_harvest_e2e::caption_above`). After `build_ir_and_dag` (line 350) and after `name_named_inputs`/`refuse_uncallable_inputs`, call this helper to get `output_tables`. Run `tool_name_collision_findings(&output_tables)`; collect `Severity::Error` findings and, if any, return `Err(CompileError::Lint(stage1::render_aggregate(&errors)))` (the SAME aggregate render `refuse_uncallable_inputs` uses). When `output_tables` is non-empty, ALSO build the tools (`build_tools(&manifest, &dag, &output_tables)`) and run `reconcile_tools(&computed, &tools)` where `computed` is a `BTreeMap<String, CellValue>` built from the run's computed values (NOTE: `RunResult` exposes `run.computed: HashMap<String, CellValue>` — there is NO `computed_as_btreemap` accessor; convert explicitly, e.g. `let computed: BTreeMap<_, _> = run.computed.iter().map(|(k, v)| (k.clone(), v.clone())).collect();`, since `reconcile_tools` takes `&BTreeMap`); if `any_mismatch()`, return `Err(CompileError::Reconcile(report.render()))`. Thread `&output_tables` + `&dag` into the `PromoteInputs` constructed at line 383. Leave the existing `comparison_from_outputs` + `reconcile::reconcile` block intact (it still guards the named-range fallback and the shared output oracle). Do NOT remove `promote_named_outputs`/`name_named_inputs`. Run fmt/clippy/test before committing.
  </action>
  <acceptance_criteria>
    - `grep -n "tool_name_collision_findings\|reconcile_tools\|output_tables_from_harvest" crates/pmcp-workbook-compiler/src/lib.rs` returns non-test call sites inside `compile_workbook_inner`.
    - A new in-crate `#[cfg(test)]` unit test in `lib.rs` (`output_tables_from_harvest_groups_output_cells`): given a synthetic `WorkbookMap` with a `Calculate_Tax` table area covering two `Role::Output` cells and an `Inputs` table covering only `Role::Input` cells, the helper returns exactly ONE `OutputTable` named `Calculate_Tax` with both output cell keys and skips `Inputs`. Passes.
    - A new in-crate unit test (`colliding_output_tables_block_compile_path`) proves two OutputTables sanitizing to the same MCP name produce a `Severity::Error` finding that `render_aggregate` includes (asserts the rule `manifest/tool-name-collision`).
    - `cargo test -p pmcp-workbook-compiler` passes (named-range corpus reemit_loan/quirks still green — the empty-OutputTables fallback preserves them).
    - `cargo clippy -p pmcp-workbook-compiler --all-targets` zero new warnings; each new fn ≤ cog 25.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --lib output_tables_from_harvest_groups_output_cells colliding_output_tables_block_compile_path</automated>
  </verify>
  <done>compile_workbook_inner derives OutputTable membership from harvested table_records, folds collision Errors into the stage-1 gate, reconciles per-tool on the multi-tool path, and threads output_tables+dag into PromoteInputs — while the named-range corpus runs the empty-set fallback unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close CR-02 served-schema fallback; real-compile E2E proof; tighten reemit_golden</name>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (input_schema_for_tool lines 337-353 — the empty-input_keys projection)
    - crates/pmcp-workbook-compiler/src/reemit_golden.rs (the is_subset proof masking the gap — checks at lines 90-146, 201-213)
    - crates/pmcp-workbook-compiler/tests/template_harvest_e2e.rs (compile-via-override pattern is in src/reemit_golden.rs::compile_fixture; reuse the same compile_workbook_with_fixture_override entry)
    - crates/pmcp-workbook-compiler/src/lib.rs (compile_workbook_with_fixture_override — the #[cfg(test)] real-compile entry that demotes fullCalcOnLoad)
  </read_first>
  <behavior>
    - input_schema_for_tool, given a Tool whose input_keys is empty, projects the FULL cell_map.inputs pool (defense-in-depth: "no DAG derivation" => "all shared inputs") so the served schema is never stricter than validate_input. Given a populated input_keys it projects exactly those keys (unchanged).
    - A real compile of template.xlsx via compile_workbook_with_fixture_override emits a bundle whose cell_map.tools has exactly 2 tools: calculate_tax and estimate_refund (sanitized names); each tool's input_keys is non-empty; estimate_refund.input_keys contains withheld and calculate_tax.input_keys does NOT; input_schema_for_tool for each tool advertises a non-empty inputs.properties.
    - The reemit_golden proof FAILS if a fresh compile yields fewer than the golden's tool count or any tool with empty input_keys.
  </behavior>
  <action>
    (a) In `schema.rs::input_schema_for_tool`, add a `let project_all = tool.input_keys.is_empty();` and change the membership test to `if project_all || reached.contains(entry.json_key.as_str())` so an empty-input_keys tool advertises the full pool (CR-02 defense-in-depth; the production multi-tool path now always populates input_keys, so this only fires for a hand-built/fallback single-tool bundle). Add a toolkit unit test asserting a Tool with empty input_keys yields a non-empty `inputs.properties`. (b) Create `crates/pmcp-workbook-compiler/tests/template_compile_e2e.rs` — wait: it must reach `compile_workbook_with_fixture_override`, which is `#[cfg(test)]`-only and INVISIBLE to an external `tests/` crate (the CR-01 reachability rule that put `reemit_golden` in `src/`). Instead add the E2E proof as a new `#[cfg(test)]` module `template_compile_e2e` declared in `lib.rs` beside `reemit_golden` (mirror its `mod` declaration + doc comment). It compiles the committed `tests/fixtures/template.xlsx` via `compile_workbook_with_fixture_override(&xlsx, &out_root, "tax-calc-template", version, approver)`, loads the emitted bundle via `pmcp_server_toolkit::workbook::load_bundle`, and asserts: `cell_map.tools.len() == 2`; the sanitized tool names are exactly {`calculate_tax`, `estimate_refund`} (apply `sanitize_tool_name` to each `tool.name`); each `tool.input_keys` is non-empty; `estimate_refund.input_keys` contains `withheld` while `calculate_tax.input_keys` does not; and `input_schema_for_tool(manifest, cell_map, tool)` for each tool has a non-empty `inputs.properties` object. (Adjust the `files_modified` E2E path note: the real file is the in-`src` module + its `mod` line in lib.rs; the listed `tests/template_compile_e2e.rs` is superseded by this in-crate placement — record the deviation in the SUMMARY.) (c) In `reemit_golden.rs`, ADD two assertions that defeat the subset blindspot: load the golden `cell_map.json` and assert the GOLDEN carries `tools.len() == 2` AND every golden tool has a non-empty `input_keys` (so the proof's baseline can never silently degrade), and in `structural_eq_check4_loads_via_toolkit` assert the FRESH-compiled fixture bundle (the named-range tax-calc) tool(s) — whatever the fallback emits — plus a new check that compiling template.xlsx through the override yields ≥2 tools each with non-empty input_keys (the positive multi-tool assertion the old subset checks lacked). Keep the existing subset checks for the named-range source. Run fmt/clippy/test for BOTH crates before committing: `cargo test -p pmcp-workbook-compiler` and `cargo test -p pmcp-server-toolkit --features workbook,workbook-embedded`.
  </action>
  <acceptance_criteria>
    - `grep -n "project_all" crates/pmcp-server-toolkit/src/workbook/schema.rs` shows the empty-input_keys fallback; a toolkit unit test `empty_input_keys_projects_full_pool` passes.
    - The in-crate `template_compile_e2e` module asserts a fresh override-compile of template.xlsx emits exactly 2 tools named `calculate_tax` + `estimate_refund`, each with non-empty input_keys, withheld disjoint, non-empty served schema. `cargo test -p pmcp-workbook-compiler template_compile_e2e` passes and would FAIL if emit_bundle reverted to build_cell_map (single tool / empty input_keys).
    - `reemit_golden.rs` asserts the golden has exactly 2 tools with populated input_keys AND a fresh template override-compile yields ≥2 populated-input_keys tools; `cargo test -p pmcp-workbook-compiler reemit_golden` (or the `structural_eq_*` tests) pass and would FAIL on a single-tool/empty-input_keys regression.
    - `cargo test -p pmcp-server-toolkit --features workbook,workbook-embedded` passes (served schema non-empty for the production tool shape).
    - `cargo clippy --all-targets` on both crates: zero new warnings.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler template_compile_e2e structural_eq && cargo test -p pmcp-server-toolkit --features "workbook workbook-embedded" empty_input_keys_projects_full_pool</automated>
  </verify>
  <done>The served schema advertises the full pool when input_keys is empty (CR-02 closed); a real override-compile of template.xlsx proves 2 tools with disjoint populated input_keys + non-empty served schema; and reemit_golden now asserts exact tool count + populated input_keys so a single-tool regression fails the proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harvested TableRecord → emitted Tool surface | A workbook author's Table names/areas drive the served MCP tool names + per-tool input schemas; a hostile/ambiguous Table set must fail compile, not boot a silently-broken server |
| compiler emit → served loader/registration | The emitted cell_map.tools is the contract the served registration loop + per-tool schema read; a single-tool/empty-input_keys bundle inverts schema/runtime parity (CR-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-17 | Spoofing | two output Tables sanitizing to one MCP name | mitigate | Task 2 folds `tool_name_collision_findings` Errors into the stage-1 gate (cell-precise CompileError::Lint) BEFORE any bundle write — closes the WR-02 silent last-writer-wins |
| T-100-04-CR02 | Information disclosure | input_schema_for_tool empty schema vs runtime acceptance | mitigate | Task 3 (a) full-pool fallback for empty input_keys; Task 2 guarantees production tools carry populated input_keys, so the served schema is never stricter than validate_input |
| T-100-18 | Tampering | reemit proof passing a single-tool regression via is_subset | mitigate | Task 3 (c) asserts exact golden tool count + populated input_keys + a positive multi-tool template compile, so the proof can never again pass a single-tool/empty-keys regression |
| T-100-19 | Denial of service | malformed/oversized harvested Table area expansion | accept | `cells_in_area` operates over already-ingested owned `CellRecord`s (bounded by MAX_CELL_COUNT at ingest, Plan 02 T-100-04); no unbounded expansion from attacker-controlled A1 strings |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — all primitives (build_tools/reconcile_tools/collision lint) already landed in Plan 03/04; proptest/umya are existing deps. No `## Package Legitimacy Audit` install tasks in this plan |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-compiler` — full compiler suite green (multi-tool wiring + collision gate + per-tool reconcile + tightened reemit + the named-range corpus fallback via reemit_loan/quirks_reconcile).
- `cargo test -p pmcp-server-toolkit --features "workbook workbook-embedded"` — served per-tool schema non-empty for the production tool shape; existing multi-tool/byte-stability suites green.
- `make purity-check` — umya/calamine remain absent from every served/runtime cone (no new cross-boundary leak from the OutputTable derivation, which reads owned ingest types only).
- `cargo fmt --all -- --check` clean; `cargo clippy -p pmcp-workbook-compiler -p pmcp-server-toolkit --all-targets` zero new warnings (no pre-commit hook installed in this checkout — each task runs these explicitly before its commit).
- ALWAYS arms present: UNIT (emit fan-out, output_tables_from_harvest grouping, collision-blocks, empty-input_keys fallback); the tightened reemit producer/consumer proof; the real-compile E2E (the WBV2-04 acceptance proof). A property/fuzz arm is NOT added — the load-bearing new logic (area→cells grouping + membership) is exercised exhaustively by the deterministic E2E over the real template; the underlying build_tools/upstream_input_leaves already carry Plan 03's proptest + fuzz arms.
</verification>

<success_criteria>
- A real override-compile of `template.xlsx` emits exactly TWO tools — `calculate_tax` + `estimate_refund` — each with a non-empty, DAG-derived `input_keys`; the two key sets are disjoint on `withheld`; and the served `input_schema_for_tool` advertises a non-empty `inputs.properties` for each (WBV2-04 / Success Criterion #2 MET on the production path).
- `tool_name_collision_findings` and `reconcile_tools` have non-test callers in `compile_workbook_inner`; a collision is a cell-precise compile failure before any bundle write.
- `reemit_golden.rs` asserts exact tool count + populated input_keys; a single-tool/empty-input_keys regression FAILS the proof.
- The named-range fixture corpus (`reemit_loan`, `quirks_reconcile`, `tax-calc`) still compiles green via the empty-OutputTables single-tool fallback.
- `make purity-check` green; fmt/clippy clean; no SATD; new fns ≤ cog 25.
</success_criteria>

<output>
Create `.planning/phases/100-excel-workbook-built-in-servers-v2/100-07-SUMMARY.md` when done.
Record any deviation (notably: the E2E proof lives as an in-`src` `#[cfg(test)]` module
`template_compile_e2e` declared in lib.rs — NOT the external `tests/template_compile_e2e.rs`
named in frontmatter — because `compile_workbook_with_fixture_override` is `#[cfg(test)]`-only
and invisible to an external integration crate, the SAME CR-01 reachability reason that places
`reemit_golden` in `src/`).
</output>
