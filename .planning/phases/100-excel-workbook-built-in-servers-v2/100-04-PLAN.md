---
phase: 100-excel-workbook-built-in-servers-v2
plan: 04
type: execute
wave: 4
depends_on: ["100-03"]
files_modified:
  - crates/pmcp-server-toolkit/src/workbook/schema.rs
  - crates/pmcp-server-toolkit/src/workbook/handler.rs
  - crates/pmcp-server-toolkit/src/workbook/mod.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
  - crates/pmcp-workbook-runtime/src/artifact_model.rs
  - crates/pmcp-workbook-runtime/src/manifest_model.rs
  - crates/pmcp-workbook-compiler/src/fixture_author.rs
  - crates/pmcp-server-toolkit/tests/workbook_tool_name_prop.rs
  - crates/pmcp-server-toolkit/tests/workbook_multi_tool.rs
  - examples/workbook_table_authoring.rs
autonomous: true
requirements: [WBV2-04, WBV2-05]
must_haves:
  truths:
    - "tools/list returns one named MCP tool per output Table, each with a DAG-derived inputSchema and a non-empty outputSchema (structuredContent)"
    - "Each tool name is the output Table name sanitized to ^[a-zA-Z0-9_-]{1,64}$ (locked semantics: lowercase, illegal-char-run → single _, trim to 64, all-invalid/empty → reject); an unmappable name fails compile with a cell-precise lint"
    - "Two output Tables whose names sanitize to the SAME MCP name fail compile with a cell-precise post-sanitize collision diagnostic naming both offending Tables"
    - "Each tool's computed outputs reconcile against its own output-Table value oracle via an aggregated per-tool report (any tool mismatch → non-zero gate exit); a structurally-broken workbook (blank name, dup key, value-less row, no caption) fails compile with a cell-precise message"
    - "sanitize_tool_name output ALWAYS matches ^[a-zA-Z0-9_-]{1,64}$ or is rejected (property-proven); post-sanitize collisions are detected (property-proven); every per-tool inputSchema keeps additionalProperties:false (property-proven)"
    - "The transitional CellMap::outputs() accessor from Plan 03 is REMOVED (no dead compat code survives — zero SATD); F2 override advertising is retained verbatim; F1, F3-input, strip_governance_prefix, name_named_inputs, CalculateHandler are retired"
  artifacts:
    - path: "crates/pmcp-server-toolkit/src/workbook/handler.rs"
      provides: "WorkbookToolHandler — one handler per Tool with per-tool I/O schema + sanitize_tool_name with locked semantics"
      contains: "WorkbookToolHandler"
    - path: "crates/pmcp-server-toolkit/src/workbook/mod.rs"
      provides: "N-handler registration loop over bundle.tools"
      contains: "for tool in"
    - path: "crates/pmcp-server-toolkit/tests/workbook_tool_name_prop.rs"
      provides: "proptest: sanitize_tool_name charset + post-sanitize collision + per-tool additionalProperties:false invariants"
      contains: "proptest!"
    - path: "crates/pmcp-server-toolkit/tests/workbook_multi_tool.rs"
      provides: "integration test: tools/list returns N tools with I/O schemas"
    - path: "examples/workbook_table_authoring.rs"
      provides: "cargo run --example demonstration (author → compile → list tools)"
  key_links:
    - from: "crates/pmcp-server-toolkit/src/workbook/mod.rs"
      to: "WorkbookToolHandler::new(bundle, tool)"
      via: "registration loop over bundle.tools"
      pattern: "for tool in"
    - from: "crates/pmcp-server-toolkit/src/workbook/schema.rs"
      to: "tool.input_keys / tool.outputs"
      via: "per-tool schema projection (F2 retained)"
      pattern: "variable_tier_keys"
    - from: "crates/pmcp-workbook-compiler/src/lib.rs"
      to: "comparison_from_outputs_for_tool(map, manifest, tool)"
      via: "per-tool reconcile loop over bundle.tools aggregated into ToolReconcileReport"
      pattern: "comparison_from_outputs_for_tool"
    - from: "crates/pmcp-workbook-compiler/src/lib.rs"
      to: "row-level LintFinding (table cell address)"
      via: "reshaped F1 → row lints + post-sanitize collision lint"
      pattern: "LintFinding::new"
---

<objective>
Land the single→multi-tool fan-out (WBV2-04) and the per-tool reconcile + fail-helpful row
lints + legacy retirement (WBV2-05) as ONE coherent change set. Per Pitfall 3, the single-tool
assumption is baked into FIVE call sites that must change together so `make quality-gate` only
ever runs on a consistent tree: `schema.rs:76,275`, `handler.rs:144-201`, `mod.rs:248-271`, and
`lib.rs:567` (`comparison_from_outputs`). This plan also retires the per-cell named-range model
(F1, F3-input, `strip_governance_prefix`, `name_named_inputs`, `CalculateHandler`) — keeping F2
verbatim — REMOVES the transitional `CellMap::outputs()` shim from Plan 03 (so no dead compat
code survives), and ships the WBV2-04/05 ALWAYS-requirements (property test + integration test +
example).

ATOMIC BUT ORDERED (resolves review finding #3): the plan stays ONE wave/plan (the design mandates
the fan-out is atomic), but it is executed in THREE ORDERED COMMIT CHECKPOINTS to shrink the
debugging blast radius:
  - Checkpoint A (Task 1): served fan-out — per-tool schema + N handlers + registration loop —
    with the Plan 03 transitional `outputs()` accessor STILL PRESENT so the tree stays green.
  - Checkpoint B (Task 2 first half): per-tool reconcile (`comparison_from_outputs_for_tool` +
    aggregated report) + cell-precise row lints + post-sanitize collision lint.
  - Checkpoint C (Task 2 second half): RETIRE F1/F3-input/`strip_governance_prefix`/
    `name_named_inputs`/`CalculateHandler`, REMOVE the Plan 03 `outputs()` shim, and replace the
    misleading fixtures.
Each checkpoint is its own commit; the tree must build green at A and B before C lands.

Purpose: This is the plan that makes the table-based contract observable end to end — N named
tools an LLM can select, each reconciled against its own oracle, with a fail-helpful linter.

Output: per-tool schema/handler/registration, per-tool reconcile with an aggregated report,
reshaped row lints + collision lint, retired legacy fns + the removed Plan 03 shim, a
security-boundary property test (charset + collision + strict envelope), an integration test,
and a `cargo run --example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/workbook-table-authoring-contract.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md
@.claude/skills/spike-findings-rust-mcp-sdk/SKILL.md

<interfaces>
From crates/pmcp-server-toolkit/src/workbook/schema.rs:
  - fn output_schema_for_manifest(manifest, cell_map) -> Value (:76) — iterate cell_map.outputs → per-tool tool.outputs
  - fn input_schema_for_manifest(manifest, cell_map) -> Value (:~274) — iterate cell_map.inputs → tool.input_keys subset; additionalProperties:false (:57,91); enum-from-allowed_values
  - F2 block: variable_tier_keys override advertising (:297-310) + tests (:533-608) — KEEP VERBATIM
  - dtype_json_type (:24-30), result_envelope_schema (:114-140) — reuse per tool unchanged
From crates/pmcp-server-toolkit/src/workbook/handler.rs:
  - struct CalculateHandler { bundle, stamp } (:144); const NAME="calculate" (:151); fn compute (:162); fn metadata→ToolInfo::with_ui(...).with_output_schema(...) (:180-200)
  - render_at_boundary (:132-138), #[async_trait] impl ToolHandler (:174-178) — reuse unchanged
  - meta tools ExplainHandler(:228)/GetManifestHandler(:334)/DiffVersionHandler/RenderWorkbookHandler — KEEP unchanged
From crates/pmcp-server-toolkit/src/workbook/mod.rs:
  - with_workbook_bundle (:218); the .tool_arc chain (:249-265); zero-output tracing::warn (:235-244)
From crates/pmcp-workbook-compiler/src/lib.rs:
  - promote_named_outputs (:603, called :321), name_named_inputs (:637, called :325), refuse_uncallable_inputs (:768, called :331)
  - unnamed_input_finding (:707) / split_cell_key (:699) / comparison_from_outputs (:567, called :354,:914)
  - Comparison type (the per-output grade) — the per-tool report aggregates N of these
From crates/pmcp-workbook-runtime/src/manifest_model.rs:
  - json_key_for_role (:175) calls strip_governance_prefix (:189) — RESHAPE: drop the strip, key = name verbatim
  - Role::from_name_prefix (:58) + const_ branch + test (:371-376) — AUDIT: grep call sites; from_name_prefix/const_ confirmed STILL present in tree (manifest_model.rs:58,372)
  - tools[] / Tool / upstream_input_leaves from Plan 03
From crates/pmcp-workbook-runtime/src/artifact_model.rs:
  - the Plan 03 TRANSITIONAL `#[deprecated] fn outputs(&self)` accessor — REMOVE in Checkpoint C (Task 2) once all call sites iterate per-tool
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Checkpoint A): Per-tool schema + N handlers + registration loop + locked sanitize semantics (served fan-out, F2 retained, shim still present)</name>
  <files>crates/pmcp-server-toolkit/src/workbook/schema.rs, crates/pmcp-server-toolkit/src/workbook/handler.rs, crates/pmcp-server-toolkit/src/workbook/mod.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (FULL: input/output_schema_for_manifest; the F2 variable_tier_keys block :297-310 + tests :533-608; additionalProperties:false strict envelope :57,91; dtype_json_type :24-30; result_envelope_schema :114-140)
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (CalculateHandler :144-201 — the one→N template; with_output_schema TypedToolWithOutput :180-200; render_at_boundary :132-138; meta tools to KEEP)
    - crates/pmcp-server-toolkit/src/workbook/mod.rs (with_workbook_bundle :218; .tool_arc chain :249-271; zero-output warn :235-244)
    - crates/pmcp-workbook-runtime/src/artifact_model.rs (Tool type from Plan 03 — name/description/input_keys/outputs; the transitional outputs() accessor still present this checkpoint)
    - docs/design/workbook-table-authoring-contract.md §4 (multi-tool), §5 (tool naming: server prefix → no uniqueness engineering; name = output Table name sanitized to ^[a-zA-Z0-9_-]{1,64}$ — "sanitize casing/charset; reject empty"; description = caption)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md §9 (per-tool schema, F2 KEEP), §10 (one handler per Table, with_output_schema), §11 (N-handler loop, zero-tools warn)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §"Tool registration loop" + Pitfall 3 (all five call sites in ONE wave) + Security Domain V5 (strict envelope must survive)
    - .claude/skills/spike-findings-rust-mcp-sdk/SKILL.md (TypedToolWithOutput dual-surface invariant: every tool emits non-empty outputSchema → structuredContent)
  </read_first>
  <behavior>
    - Test: input_schema_for_tool(manifest, tool) emits a schema containing ONLY the tool's DAG-derived input_keys, additionalProperties:false, with enum keys carried from allowed_values, AND the F2 variable_tier_keys override props (unchanged).
    - Test: output_schema_for_tool(manifest, tool) emits a non-empty outputSchema over the tool's own outputs (TypedToolWithOutput invariant).
    - Test: WorkbookToolHandler::new(bundle, tool).metadata() returns a ToolInfo whose name == sanitized tool name, description == caption, and which carries both input and output schemas.
    - Test (sanitize semantics, LOCKED): `sanitize_tool_name("Calculate Tax")` == Ok("calculate_tax"); `sanitize_tool_name("Calculate_Tax")` == Ok("calculate_tax") (lowercasing); a run of illegal chars collapses to a single `_` (`"a  b"`/`"a@@b"` → `"a_b"`); leading/trailing illegal chars are trimmed (no leading/trailing `_`); a >64-char input is truncated to 64; an empty string and an all-illegal string return `Err`.
  </behavior>
  <action>
    schema.rs: parameterize the two builders per-tool — add `input_schema_for_tool(manifest, &Tool)` iterating `tool.input_keys` (resolving each key's CellRole for dtype/unit/enum) and `output_schema_for_tool(manifest, &Tool)` iterating `tool.outputs`. Both reuse `dtype_json_type`, `result_envelope_schema`, and the `additionalProperties:false` strict envelope (V5 — MUST survive; a client trusting the schema must never send a key the runtime rejects). KEEP the F2 `variable_tier_keys` override-advertising block (:297-310) verbatim — it is independent of the input model; `crate::workbook::input::variable_tier_keys` stays the single source. handler.rs: add `pub struct WorkbookToolHandler { bundle: Arc<WorkbookBundle>, tool: Tool, stamp: ProvStamp }` cloned from `CalculateHandler`'s shape; `compute` validates against this tool's input subset, runs the bundle, and projects ONLY this tool's outputs; `metadata()` returns `ToolInfo::with_ui(sanitized_name, tool.description, input_schema_for_tool(...), WORKBOOK_TOOL_UI).with_output_schema(output_schema_for_tool(...))`. Reuse `render_at_boundary` + the `#[async_trait] impl ToolHandler` unchanged.
    SANITIZER (LOCKED semantics, resolves review finding #4 first half): add `fn sanitize_tool_name(raw: &str) -> Result<String, WorkbookToolError>` enforcing `^[a-zA-Z0-9_-]{1,64}$` with these EXACT, locked rules: (1) lowercase every ASCII letter (so `Calculate_Tax`→`calculate_tax`, matching the tests); (2) replace each maximal RUN of illegal characters (anything not `[a-z0-9_-]` after lowercasing) with a SINGLE `_` (collapse runs, do not emit one `_` per illegal char); (3) trim leading and trailing `_`/`-` so the name has no governance-noise edges; (4) truncate to 64 chars AFTER the above; (5) if the result is empty (input was empty or all-illegal) return `Err(WorkbookToolError::UnmappableToolName)`. Document these five rules in the fn's rustdoc as the locked contract. mod.rs: replace the single `CalculateHandler` `.tool_arc` (:249) with a loop `let mut builder = self; for tool in &bundle.tools { let name = sanitize_tool_name(&tool.name)?; builder = builder.tool_arc(&name, Arc::new(WorkbookToolHandler::new(bundle.clone(), tool.clone()))); }`, then chain the 4 meta tools (Explain/GetManifest/DiffVersion/RenderWorkbook) + the resource UNCHANGED. Generalize the zero-output `tracing::warn` (:235-244) to "bundle declares zero tools." DELETE `CalculateHandler` (replaced by the per-tool handler) — the single generic `calculate` is explicitly rejected by §4. Keep every fn cog<=25 (PMAT) — factor `sanitize_tool_name` and per-tool schema builders as their own fns. This task changes schema.rs + handler.rs + mod.rs together (Pitfall 3). Checkpoint A: the Plan 03 transitional `outputs()` accessor is STILL PRESENT (any not-yet-reshaped reconcile/lib.rs call site keeps using it) — the tree must build green at the end of this task; the accessor is removed in Task 2 Checkpoint C.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --lib workbook && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    <!-- Ordered per-concern checkpoints — verify each in sequence before moving on (Checkpoint A: atomic per Pitfall 3, gate at each call-site boundary; the tree MUST build green at the end). -->
    - (1) schema.rs / input: `input_schema_for_tool` exists, iterates ONLY `tool.input_keys`, and the emitted schema has `additionalProperties` set to `false` (V5 strict envelope survives).
    - (2) schema.rs / F2: the `variable_tier_keys` override block (:297-310) is present VERBATIM (grep `variable_tier_keys`) and the F2 tests (schema.rs:533-608) still pass unchanged.
    - (3) schema.rs / output: `output_schema_for_tool` exists, iterates `tool.outputs`, and emits a NON-EMPTY outputSchema (TypedToolWithOutput invariant).
    - (4) handler.rs / handler: `struct WorkbookToolHandler` exists with `{ bundle, tool, stamp }`; its `compute` projects ONLY this tool's outputs; `metadata()` carries both per-tool I/O schemas.
    - (5) handler.rs / sanitize (LOCKED): `fn sanitize_tool_name` lowercases, collapses illegal runs to a single `_`, trims edge `_`/`-`, truncates to 64, and returns `Err` on empty/all-invalid; unit tests prove `Calculate Tax`→`calculate_tax`, `Calculate_Tax`→`calculate_tax`, run-collapse, trim, and empty/all-illegal→Err.
    - (6) handler.rs / retire: `CalculateHandler` is removed (grep returns nothing); the 4 meta handlers are untouched.
    - (7) mod.rs / loop: registration uses `for tool in &bundle.tools` (a loop, not a fixed `.tool_arc(CalculateHandler::NAME, ...)`); the zero-output warn is generalized to "zero tools."
    - (8) gate (Checkpoint A green): `cargo test -p pmcp-server-toolkit --lib workbook` exits 0 AND `cargo build --workspace` exits 0 (the Plan 03 outputs() shim still present keeps the tree green); `pmat analyze complexity` flags no new fn > cog 25 in the three files.
  </acceptance_criteria>
  <done>Checkpoint A: tools/list emits one named tool per output Table with per-tool I/O schemas and locked sanitize semantics; F2 retained; the generic calculate is gone; the workspace builds green (shim still present).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Checkpoints B+C): Per-tool reconcile report + post-sanitize collision lint + reshape F1 row lints; then retire legacy fns + REMOVE the Plan 03 shim</name>
  <files>crates/pmcp-workbook-compiler/src/lib.rs, crates/pmcp-workbook-runtime/src/manifest_model.rs, crates/pmcp-workbook-runtime/src/artifact_model.rs, crates/pmcp-workbook-compiler/src/fixture_author.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/lib.rs (orchestration :309-365 — promote_named_outputs :321, name_named_inputs :325, refuse_uncallable_inputs :331, comparison_from_outputs :354 + :914; the fns at :567,:603,:637,:768; located-finding precedent unnamed_input_finding :707-723 + split_cell_key :699-704; the "define a named range in_<name>" repair text :715-721,:738-741,:757; the Comparison type + how :354/:914 currently fold it into the run report)
    - crates/pmcp-workbook-runtime/src/manifest_model.rs (json_key_for_role :175-180 + strip_governance_prefix :182-198 — RESHAPE/DELETE; Role::from_name_prefix :58 + const_ branch + test :371-376 — AUDIT)
    - crates/pmcp-workbook-runtime/src/artifact_model.rs (the Plan 03 transitional `#[deprecated] fn outputs(&self)` accessor — REMOVE this checkpoint once all call sites iterate per-tool)
    - crates/pmcp-workbook-runtime/src/finding.rs (LintFinding::new(severity, rule, sheet, cell, message, repair) :44-80; LintReport collect-all + has_errors gate :86-113)
    - docs/design/workbook-table-authoring-contract.md §8 (fail-helpful row lints: blank name, dup key, value-less row, input feeds no tool, output Table with no caption, unmappable tool name), §9 (cleanup ledger: F2 KEEP / F3-output SUBSUMED / F3-input RETIRE / F1 RESHAPE / strip+name_named_inputs RETIRE), §5 (sanitize → collision = two Tables → same MCP name must fail)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md §7 (lib.rs retire/reshape exact fates table + KEEP split_cell_key) and §1 (manifest_model RETIRE: json_key_for_role reshape, strip_governance_prefix DELETE, from_name_prefix AUDIT)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §"Open Questions (RESOLVED)" Q1 (grep from_name_prefix/const_ call sites before delete — the fate is execution-time-determined, recorded in SUMMARY), Open-Q2 (per-tool reconcile partition: each Tool.outputs[].cell defines its partition; reuse the comparator)
  </read_first>
  <behavior>
    - Test: a workbook with a blank `name` cell in an input row fails compile with a finding whose sheet!addr is that exact cell and whose message/repair point at the table row (NOT "define a named range in_<name>").
    - Test: a duplicate input key, a value-less row, an output Table with no caption, and a tool name unmappable to MCP charset each fail compile with their own cell-precise finding code.
    - Test (POST-SANITIZE COLLISION): two output Tables named `Calculate Tax`, `calculate_tax` (and `calculate-tax`) — which all sanitize to `calculate_tax` — fail compile with a `tool-name-collision` finding that names BOTH offending Tables and their cell/sheet locations; a non-colliding pair compiles.
    - Test: each tool's computed outputs reconcile against ITS OWN output-Table value oracle (per-tool partition aggregated into the report); a wrong oracle in ONE tool fails only that tool's reconcile and makes the aggregated report's gate exit non-zero.
    - Test (retirement): `strip_governance_prefix` no longer exists; `json_key_for_role` returns the table `name` verbatim (no strip); `name_named_inputs` and `promote_named_outputs`'s out_* path are gone; the Plan 03 `CellMap::outputs()` accessor no longer exists.
  </behavior>
  <action>
    CHECKPOINT B — reconcile report shape + lints (resolves review findings #6 + #4 second half):
    Per-tool reconcile (Open-Q2): refactor `comparison_from_outputs` (:567) into `fn comparison_from_outputs_for_tool(computed: &BTreeMap<String, CellValue>, manifest: &Manifest, tool: &Tool) -> Comparison` — it partitions the run's `computed` map to ONLY `tool.outputs[].cell` keys, grades them against `tool.oracle`, and returns that tool's `Comparison`. AGGREGATED REPORT SHAPE (the concrete target the executor builds against): introduce `struct ToolReconcileReport { per_tool: Vec<(String /*sanitized tool name*/, Comparison)> }` with `impl ToolReconcileReport { fn any_mismatch(&self) -> bool { self.per_tool.iter().any(|(_, c)| !c.is_match()) } fn render(&self) -> String /* one section per tool: tool name header + that tool's Comparison rows; failing tools first */ }`. Build it in a loop `for tool in &bundle.tools { report.per_tool.push((sanitize_tool_name(&tool.name)?, comparison_from_outputs_for_tool(&computed, manifest, tool))); }` at the :354 and :914 call sites, replacing the single-Comparison fold. GATE EXIT: derive the gate's non-zero exit from `report.any_mismatch()` (ANY tool mismatch → non-zero); multiple failures render as multiple sections via `render()` (failing tools listed first). Wire `ToolReconcileReport` into the existing run-report struct in place of the single `Comparison` (note in SUMMARY exactly how it folds into the existing report type).
    RESHAPE F1 (review #3 Checkpoint B): RESHAPE `refuse_uncallable_inputs` (3c) into row-level lints: re-target the SAME `LintFinding` rule codes (`manifest/input-no-semantic-key`, dup-key, empty-key) at table ROWS via `split_cell_key` (KEEP), and DROP the "define a named range `in_<name>`" repair text (:715-721,:738-741,:757) — new repair text names the table row/cell. ADD the new row lints from §8: value-less row, output Table with no caption (missing tool description), and tool name unmappable to MCP charset (call `sanitize_tool_name`'s validation), plus the "input feeds no tool" lint (from Plan 03's build_tools, surfaced here in the collect-all report).
    POST-SANITIZE COLLISION LINT (resolves review #4): add a `tool-name-collision` lint — after sanitizing every output Table name, group by sanitized name; any group with ≥2 source Tables emits ONE `LintFinding` (Severity = error) whose message names ALL colliding source Table names and whose `sheet!cell` locations point at each offending Table's name/caption cell (cell-precise per both Tables). This runs in the compiler before tool registration so a collision is a clean compile failure, not a silent last-writer-wins at `tool_arc`.
    CHECKPOINT C — retirements + remove the Plan 03 shim (review #3 Checkpoint C):
    Re-wire the orchestration (lib.rs:309-365): replace `promote_named_outputs` (3a) + `name_named_inputs` (3b) with the table-harvest-driven manifest from Plan 02/03 (output Role + name come from the harvested Tables, not `out_*`/`in_*` defined names). RETIRE in manifest_model.rs: DELETE `strip_governance_prefix` entirely; RESHAPE `json_key_for_role` to `role.name.or(meaning).or(cell)` with NO strip. AUDIT `Role::from_name_prefix`/`const_` (Q1): grep its call sites across the workspace; the `const_` (governed-constant) branch may still be consumed by the D-04 overlap/Guide check — KEEP only the still-used branch, DELETE the fn (and its test :371-376) if no non-table consumer remains; record the grep result + decision in the SUMMARY. RETIRE in fixture_author.rs: delete the `in_*`/`out_*` `DefinedNameSpec` injection (the 11 sites: :388,543,575,622,660,664,702,865,869,873,935) and the `define_name` loop usage for governance prefixes — fixtures now declare Tables (Plan 01 surface). REMOVE THE PLAN 03 TRANSITIONAL SHIM (resolves review finding #1): now that all served + reconcile call sites iterate per-tool, DELETE the `#[deprecated] fn outputs(&self)` accessor from `artifact_model.rs` — `grep -rn 'fn outputs' crates/pmcp-workbook-runtime/src/artifact_model.rs` must return nothing, and `grep -rn '\.outputs()' crates/` must show no remaining caller (the shim is fully retired so no dead compat code survives — zero SATD). Keep all retire/reshape fns cog<=25.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --lib -- row_lint reconcile json_key collision && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    <!-- Ordered per-checkpoint criteria — verify B before C; the tree must build green at the end of B and again at the end of C. -->
    - (B1) reconcile report: `comparison_from_outputs_for_tool(computed, manifest, tool) -> Comparison` exists; `ToolReconcileReport { per_tool }` with `any_mismatch()` + `render()` exists and is built in a loop over `bundle.tools` at :354 and :914; a wrong oracle in one tool fails ONLY that tool and makes `any_mismatch()` true → non-zero gate exit.
    - (B2) F1 reshape: the dup-key / empty-key / no-semantic-key `LintFinding` rule codes now target table ROWS via `split_cell_key`; `grep -v '^#' lib.rs | grep -c 'named range'` returns 0 (the in_*/out_* repair guidance is gone).
    - (B3) new row lints: compiling a workbook with a blank `name` cell exits non-zero naming the exact sheet!cell; likewise for dup key, value-less row, no-caption output Table, and unmappable tool name (five distinct finding codes); the "input feeds no tool" lint surfaces in the collect-all report.
    - (B4) collision lint: two Tables sanitizing to the same MCP name (`Calculate Tax` + `calculate_tax`) fail compile with ONE `tool-name-collision` finding naming BOTH source Tables and their cell locations; a non-colliding pair compiles. (Checkpoint B build green: `cargo build --workspace` exits 0 with the shim STILL present.)
    - (C1) retire strip: `crates/pmcp-workbook-runtime/src/manifest_model.rs` no longer contains `fn strip_governance_prefix`; `json_key_for_role` contains no `strip_governance_prefix` call (returns the name verbatim).
    - (C2) retire named-input/output: `lib.rs` no longer contains `fn name_named_inputs`; `promote_named_outputs`'s `out_*` defined-name path is removed (output detection sourced from Tables).
    - (C3) audit from_name_prefix: `Role::from_name_prefix` is either deleted (with its test :371-376) OR reduced to only a still-consumed `const_` branch, with the call-site grep result recorded in the SUMMARY.
    - (C4) retire fixtures: the 11 `in_*`/`out_*` `DefinedNameSpec` injection sites in `fixture_author.rs` are removed; fixtures declare Tables instead.
    - (C5) REMOVE the Plan 03 shim: `grep -rn 'fn outputs' crates/pmcp-workbook-runtime/src/artifact_model.rs` returns nothing AND `grep -rn '\.outputs()' crates/` shows no remaining caller (the transitional accessor is fully deleted — zero dead compat code).
    - (C6) gate (Checkpoint C green): `cargo test -p pmcp-workbook-compiler --lib` exits 0 AND `cargo build --workspace` exits 0 with the shim removed; `pmat analyze complexity` flags no new fn > cog 25.
  </acceptance_criteria>
  <done>Checkpoint B: per-tool reconcile via an aggregated ToolReconcileReport (any mismatch → non-zero) + cell-precise row lints + post-sanitize collision lint. Checkpoint C: F1/F3-input/strip/name_named_inputs retired AND the Plan 03 transitional outputs() shim deleted; F2 untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-tool integration test + cargo run --example demonstration + retire stale fixtures</name>
  <files>crates/pmcp-server-toolkit/tests/workbook_multi_tool.rs, examples/workbook_table_authoring.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/tests/workbook_integration.rs (the existing in-process tools/list integration harness to model on)
    - crates/pmcp-workbook-compiler/examples/compile_a_workbook.rs (the existing compiler example shape)
    - crates/pmcp-server-toolkit/examples/workbook_server_http.rs (the existing served-workbook example)
    - docs/design/workbook-table-authoring-contract.md §4, §7 (the two-tool tax-suite template the test/example exercise)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-VALIDATION.md (Wave-0 gaps: integration test tools/list returns one tool per output Table; examples/workbook_table_authoring.rs)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §"Runtime State Inventory" (stale tax-calc/leap1900 fixtures are not live state — safe to replace once the new path is green) and CLAUDE.md ALWAYS-requirements (example per feature)
  </read_first>
  <action>
    Add `crates/pmcp-server-toolkit/tests/workbook_multi_tool.rs`: compile the shipped `template.xlsx` (Plan 01) into a bundle, build a server via `with_workbook_bundle`, drive `tools/list` in-process (model on `workbook_integration.rs`), and assert it returns EXACTLY the two tools `calculate_tax` + `estimate_refund` (sanitized names) — each with a non-empty `inputSchema` carrying ONLY its DAG-derived keys (calculate_tax: income/filing; estimate_refund: income/filing/withheld) and a non-empty `outputSchema`; assert the two tool key sets differ (DAG derivation proof). Add `examples/workbook_table_authoring.rs` (the ALWAYS-requirement demonstration): author the template via the Plan 01 generator path (or load the committed `template.xlsx`), compile it, build the server, and print the emitted tool surface (names + per-tool I/O schema) — the "your Excel process becomes a governed AI-callable tool" story. RETIRE the now-superseded stale fixtures: delete `crates/pmcp-workbook-compiler/tests/fixtures/{tax-calc,leap1900-probe,loan-calc}.xlsx` + their `.gen.json`/`.provenance-override.json` sidecars AND `cargo-pmcp/src/templates/workbook_bundle/tax-calc.xlsx`/`tax-calc@1.1.0/` ONLY if no remaining test references them after this plan's changes (grep first; if a Phase-96 test still consumes loan-calc/leap1900 for the dialect/quirk corpus, KEEP those and retire only the in_*/out_*-keyed tax-calc) — record exactly which fixtures were removed vs kept in the SUMMARY. Update any example/test that referenced the deleted `CalculateHandler::NAME` to the new per-tool names.
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --test workbook_multi_tool && cargo run --example workbook_table_authoring</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `crates/pmcp-server-toolkit/tests/workbook_multi_tool.rs` and `examples/workbook_table_authoring.rs`.
    - Behavior: `cargo test -p pmcp-server-toolkit --test workbook_multi_tool` exits 0; `tools/list` returns exactly `calculate_tax` + `estimate_refund` with disjoint, DAG-derived input key sets and non-empty output schemas.
    - Behavior: `cargo run --example workbook_table_authoring` runs (exit 0) and prints each output-table tool name + description + input/output schema.
    - Source: no test/example references `CalculateHandler` after this plan (the generic calculate tool is gone).
    - Source: the SUMMARY records which stale fixtures were deleted vs kept (and why), with a grep showing no remaining references to deleted ones.
  </acceptance_criteria>
  <done>The multi-tool surface is proven by an integration test and demonstrated by a runnable example; misleading fixtures retired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Property test — sanitize_tool_name charset + post-sanitize collision + per-tool additionalProperties:false (CLAUDE.md PROPERTY, security boundary)</name>
  <files>crates/pmcp-server-toolkit/tests/workbook_tool_name_prop.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/src/workbook/handler.rs (Task 1's `sanitize_tool_name(raw) -> Result<String, WorkbookToolError>` with the LOCKED five-rule semantics — the function under property)
    - crates/pmcp-workbook-compiler/src/lib.rs (Task 2's `tool-name-collision` lint — the collision detection under property)
    - crates/pmcp-server-toolkit/src/workbook/schema.rs (Task 1's `input_schema_for_tool` — the additionalProperties:false envelope under property; the strict-envelope :57,91 precedent)
    - crates/pmcp-workbook-runtime/src/artifact_model.rs (Tool — construct arbitrary Tools for the schema property)
    - docs/design/workbook-table-authoring-contract.md §5 (tool naming charset ^[a-zA-Z0-9_-]{1,64}$ + collision must fail), §4 (per-tool strict schema)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md Security Domain V5 (the strict additionalProperties:false envelope MUST survive — this is the property's security target)
    - any existing `proptest!` block in the toolkit/runtime crates for the project proptest style
  </read_first>
  <behavior>
    - Property (name charset, security boundary): for an ARBITRARY input string (incl. unicode, whitespace, punctuation, empty), `sanitize_tool_name(s)` either returns `Err` OR returns an `Ok(name)` whose every byte matches `^[a-zA-Z0-9_-]{1,64}$` — it NEVER returns an Ok name containing an illegal char or exceeding 64 chars (an uncallable/charset-illegal tool can never ship).
    - Property (empty/all-invalid rejection): an empty string and a string of only-illegal characters (no `[a-z0-9]`) always return `Err` (fail-closed per the locked rule 5).
    - Property (post-sanitize collision): for arbitrary distinct raw names A and B, if `sanitize_tool_name(A) == sanitize_tool_name(B) == Ok(same)`, then the compiler's collision detector flags A and B as a collision (no two distinct source Tables silently collapse to one MCP tool). Seed with the known triple `Calculate Tax` / `calculate_tax` / `calculate-tax` → one collision naming all three.
    - Property (strict envelope, security boundary): for an arbitrary `Tool` (arbitrary input_keys/outputs), `input_schema_for_tool(manifest, &tool)` ALWAYS emits a JSON object with `additionalProperties == false` — the V5 strict envelope can never be relaxed by any tool shape.
  </behavior>
  <action>
    Add `crates/pmcp-server-toolkit/tests/workbook_tool_name_prop.rs` as a `proptest`-based integration test — the CLAUDE.md ALWAYS PROPERTY requirement for WBV2-04/05, targeting the security boundaries T-100-10 (tool-name spoofing), T-100-11 (strict-envelope relaxation), and T-100-17 (post-sanitize collision collapse). Property 1 (charset): generate an arbitrary `String` (proptest unicode strategy + explicit edge seeds: empty, all-whitespace, all-punctuation, a 200-char string), call `sanitize_tool_name`, assert `Err` OR matches `^[a-zA-Z0-9_-]{1,64}$`; assert empty / all-invalid → `Err`. Property 2 (collision, resolves review #4): generate a pair of arbitrary raw names; if both sanitize Ok to the SAME string, drive the Task-2 collision detector over a two-Table manifest built from them and assert it returns a `tool-name-collision` error identifying both; include the explicit `Calculate Tax`/`calculate_tax`/`calculate-tax` seed asserting one collision error names all the offending tables' locations. Property 3 (strict envelope): generate an arbitrary `Tool` (a `prop_compose!` over name/input_keys/outputs), call `input_schema_for_tool`, parse the returned `Value`, assert `value["additionalProperties"] == json!(false)`. Use the project's existing proptest config (≥256 cases). Keep the strategy + body cog<=25. (These three properties are the PROPERTY arm; Task 3's integration test + example are the integration/example arms; sanitize/schema/collision unit tests in Tasks 1-2 are the unit arm — together satisfying the ALWAYS fuzz/property/unit/example set for WBV2-04/05, with the harvest/DAG fuzz arms covered upstream in Plans 02/03.)
  </action>
  <verify>
    <automated>cargo test -p pmcp-server-toolkit --test workbook_tool_name_prop</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `crates/pmcp-server-toolkit/tests/workbook_tool_name_prop.rs` with a `proptest!` block.
    - Behavior: `cargo test -p pmcp-server-toolkit --test workbook_tool_name_prop` exits 0; ≥256 cases run with no charset/length/envelope/collision violation.
    - Source: Property 1 asserts every `Ok` from `sanitize_tool_name` matches `^[a-zA-Z0-9_-]{1,64}$` and empty/all-invalid → `Err`; Property 2 asserts equal-sanitization distinct raw names are flagged as a collision (incl. the Calculate_Tax/calculate-tax seed naming both tables); Property 3 asserts every per-tool input schema has `additionalProperties == false`.
    - Quality: `pmat analyze complexity` flags no fn in the test > cog 25.
  </acceptance_criteria>
  <done>The tool-name charset boundary (T-100-10), the post-sanitize collision boundary (T-100-17), and the strict-envelope boundary (T-100-11) are property-proven, satisfying the CLAUDE.md ALWAYS PROPERTY requirement for WBV2-04/05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM client → per-tool inputSchema | A client trusts the advertised schema; it must never be able to send a key the runtime then rejects, nor pin a computed/strict cell |
| output Table name → MCP tool name | An unsanitized/empty name would yield an uncallable or charset-illegal tool; two names sanitizing to the same MCP name would silently collapse two tools into one |
| BA-governed strict constant → tool input | A strict-tier field exposed as an input is a governance/privilege bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-08 | Tampering | strict (BA-governed) constant supplied as a caller input | mitigate | tier column → InputTier::strict excluded from every tool's caller inputs; `is_strict_constant` reject (manifest_model.rs:205) + `strict_constant_override` error (error.rs:143) preserved; per-tool schema must not advertise a strict cell as an input |
| T-100-09 | Tampering | caller pins a computed output via override | mitigate | `is_computed` reject gate retained (manifest_model.rs:215); per-tool inputSchema only advertises DAG-derived Role::Input leaves (Plan 03 property) |
| T-100-10 | Spoofing/Tampering | unmappable/empty tool name → uncallable or charset-illegal tool | mitigate | `sanitize_tool_name` enforces `^[a-zA-Z0-9_-]{1,64}$` (locked five-rule semantics) and rejects empty/all-invalid; an unmappable name is a cell-precise compile lint (fail-closed); Task 4 property proves the charset invariant over arbitrary input strings |
| T-100-11 | Information Disclosure | per-tool schema relaxing the strict envelope | mitigate | every per-tool inputSchema keeps `additionalProperties:false` mirroring the runtime `deny_unknown_fields` DTO (V5); F2 override block is the only sanctioned extra-key surface; Task 4 property proves additionalProperties==false over arbitrary Tools |
| T-100-16 | Tampering (process) | Plan 03 transitional outputs() shim surviving as dead compat code (SATD) | mitigate | Checkpoint C deletes the `#[deprecated] fn outputs` accessor; criterion C5 greps it (and all `.outputs()` callers) gone — zero dead compat code |
| T-100-17 | Spoofing/Tampering | two output Tables sanitizing to the same MCP name silently collapse to one tool | mitigate | post-sanitize collision lint groups by sanitized name; ≥2 source Tables → a cell-precise `tool-name-collision` compile error naming all offenders (fail-closed before tool_arc); Task 4 property proves equal-sanitization distinct raw names are always flagged |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs — all deps are existing workspace members (proptest is an existing dev-dep) |
</threat_model>

<verification>
- `cargo test -p pmcp-server-toolkit --lib workbook` + `--test workbook_multi_tool` + `--test workbook_tool_name_prop` green.
- `cargo test -p pmcp-workbook-compiler --lib` green (row lints + collision lint + per-tool reconcile + retirement).
- `cargo build --workspace` green at Checkpoint A, Checkpoint B (shim present), and Checkpoint C (shim removed).
- `cargo run --example workbook_table_authoring` exits 0.
- F2 tests (schema.rs:533-608) unchanged and green; `strip_governance_prefix`/`name_named_inputs`/`CalculateHandler`/`CellMap::outputs()` shim all gone.
</verification>

<success_criteria>
WBV2-04 + WBV2-05 satisfied: one named, described MCP tool per output Table with DAG-derived inputSchema + non-empty outputSchema (structuredContent); locked sanitize semantics + post-sanitize collision detection; per-tool reconcile against each tool's own oracle via an aggregated ToolReconcileReport (any mismatch → non-zero); fail-helpful cell-precise row lints; the tool-name/collision/strict-envelope security boundaries property-proven; F1/F3-input/strip_governance_prefix/name_named_inputs/CalculateHandler retired AND the Plan 03 transitional shim removed; F2 retained. Maps to ROADMAP Success Criteria 2, 3, 5.
</success_criteria>

<output>
Create `.planning/phases/100-excel-workbook-built-in-servers-v2/100-04-SUMMARY.md` when done
</output>
</content>
</invoke>
