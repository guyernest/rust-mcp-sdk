---
phase: 100-excel-workbook-built-in-servers-v2
plan: 05
type: execute
wave: 5
depends_on: ["100-04"]
files_modified:
  - cargo-pmcp/src/commands/workbook/explain.rs
  - cargo-pmcp/src/commands/workbook/mod.rs
  - cargo-pmcp/tests/workbook_explain.rs
  - cargo-pmcp/examples/workbook_explain.rs
  - pmcp-book/src/workbook-table-authoring.md
  - pmcp-book/src/SUMMARY.md
  - pmcp-course/src/workbook-table-authoring.md
  - pmcp-course/src/SUMMARY.md
autonomous: true
requirements: [WBV2-06, WBV2-07]
must_haves:
  truths:
    - "cargo pmcp workbook explain <file> renders the emitted tool surface (tool names, descriptions, per-tool input/output schemas) to stdout before deploy"
    - "explain supports --format text (default) and --format json; data goes to stdout, advisory to stderr"
    - "A cargo run --example workbook_explain demonstration renders the shipped template's tool surface"
    - "A pmcp-book chapter and a pmcp-course chapter teach the table-based authoring contract seeded from the spec + the shipped template, with no retired in_*/out_* terminology"
  artifacts:
    - path: "cargo-pmcp/src/commands/workbook/explain.rs"
      provides: "workbook explain read-only ingest→synth→render subcommand"
      contains: "fn execute"
    - path: "cargo-pmcp/tests/workbook_explain.rs"
      provides: "snapshot test of the explain text render"
    - path: "cargo-pmcp/examples/workbook_explain.rs"
      provides: "cargo run --example demonstration of the explain tool-surface render"
      contains: "format_tool_surface"
    - path: "pmcp-book/src/workbook-table-authoring.md"
      provides: "BA-facing book chapter"
    - path: "pmcp-course/src/workbook-table-authoring.md"
      provides: "course chapter"
  key_links:
    - from: "cargo-pmcp/src/commands/workbook/mod.rs"
      to: "explain::execute"
      via: "WorkbookCommand::Explain dispatch"
      pattern: "Explain"
    - from: "cargo-pmcp/src/commands/workbook/explain.rs"
      to: "pmcp_workbook_compiler::ingest::ingest"
      via: "read-only ingest (no bundle written)"
      pattern: "ingest::ingest"
---

<objective>
Ship the `cargo pmcp workbook explain <file>` dry-run preview (WBV2-06, §8) — "here is the tool
surface an AI will see" rendered BEFORE deploy, the single best guard against the silent-broken-
deploy class. Model it on the proven read-only `workbook lint` shape (text→stdout, advisory→
stderr, dual `--format text|json`). Demonstrate it with a `cargo run --example` (CLAUDE.md ALWAYS
requirement). Then write the BA-facing docs/training (WBV2-07): a pmcp-book chapter and a
pmcp-course chapter seeded from the design contract + the shipped template — the "your Excel
process becomes a governed, AI-callable tool" story — teaching ONLY the table model.

Purpose: WBV2-06 makes the emitted tool surface inspectable; WBV2-07 makes the contract teachable.
Both depend on the multi-tool surface existing (Plan 04).

Output: the `explain` subcommand + a snapshot test + a runnable example + two mdBook chapters wired into their SUMMARYs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/workbook-table-authoring-contract.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md

<interfaces>
From cargo-pmcp/src/commands/workbook/lint.rs:
  - LintArgs { workbook_path: PathBuf, #[arg(long, default_value="text")] format: String } (:28-37)
  - fn execute(args, gf) — should_output()/PMCP_QUIET gate; print to stdout/stderr (Phase-74 D-11) (:46-60)
  - fn lint_workbook(path) -> Result<LintReport> via pmcp_workbook_compiler::ingest::ingest (:63-68)
  - format_lint_report / render_text / severity_label (:100-134) — pure String fn, JSON add-on, snapshot-testable
From cargo-pmcp/src/commands/workbook/mod.rs:
  - enum WorkbookCommand { Compile, Lint, Emit } (:75-81) + execute() match (:88-90); `pub mod` declarations (:21-25)
From Plan 03/04: artifact_model::Tool (name/description/input_keys/outputs); build_tools; per-tool schema builders
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: workbook explain subcommand + cargo run --example demonstration (read-only ingest→synth→render tool surface)</name>
  <files>cargo-pmcp/src/commands/workbook/explain.rs, cargo-pmcp/src/commands/workbook/mod.rs, cargo-pmcp/tests/workbook_explain.rs, cargo-pmcp/examples/workbook_explain.rs</files>
  <read_first>
    - cargo-pmcp/src/commands/workbook/lint.rs (FULL — clone the read-only shape end to end: LintArgs :28-37, execute :46-60, lint_workbook ingest :63-68, format_lint_report/render_text :100-134; the data→stdout/advisory→stderr Phase-74 D-11 discipline)
    - cargo-pmcp/src/commands/workbook/mod.rs (WorkbookCommand enum :75-81 + dispatch :88-90 + `pub mod` block :21-25 — add Explain variant + `pub mod explain;`)
    - docs/design/workbook-table-authoring-contract.md §8 (dry-run preview: tool names, descriptions, per-tool input/output schemas before deploy)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-PATTERNS.md §12 (clone lint.rs: subcommand registration, dual-format renderer, read-only ingest shape, text-render style + snapshot)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §"Open Questions Resolved" OQ-3 (TEXT first: per-tool name/description/inputSchema [key: type [unit] [enum]]/outputSchema [key: type [unit]]; --format json thin add-on)
    - crates/pmcp-workbook-compiler/src/lib.rs + artifact/cell_map.rs (the ingest→synth→build_tools path explain reuses without writing a bundle)
    - cargo-pmcp/examples/ (an existing cargo-pmcp example for the example harness/style; CLAUDE.md ALWAYS: a literal `cargo run --example` per feature)
  </read_first>
  <behavior>
    - Test: explain on the shipped template.xlsx renders both tools (calculate_tax, estimate_refund) with their descriptions and per-tool input keys (calculate_tax: income/filing; estimate_refund: income/filing/withheld) and output keys to stdout.
    - Test: --format json emits parseable JSON over the same tool surface (pure format fn, no stdout capture needed).
    - Test (snapshot): the text render of the template's tool surface matches a committed snapshot fixture.
    - Example: `cargo run --example workbook_explain` ingests the shipped template, projects the tool surface, and prints the text render (exit 0).
  </behavior>
  <action>
    Add `pub mod explain;` to `cargo-pmcp/src/commands/workbook/mod.rs` and an `Explain(explain::ExplainArgs)` variant to the `WorkbookCommand` enum (:75-81) plus its dispatch arm `WorkbookCommand::Explain(args) => explain::execute(args, global_flags)` (:88-90). Create `explain.rs` cloning `lint.rs`'s structure: `ExplainArgs { workbook_path: PathBuf, #[arg(long, default_value="text")] format: String }`; `execute` ingests (no bundle written) via `pmcp_workbook_compiler::ingest::ingest`, runs synth → `build_tools` to project the tool surface, then renders. Provide a PURE `fn format_tool_surface(tools: &[ToolSurface], format: &str) -> String` so JSON is testable without stdout capture (the lint.rs idiom). Text render (OQ-3): one block per tool — `name`, `description`, an `inputs:` list (`key: type [unit] [enum-values]`) and an `outputs:` list (`key: type [unit]`); data→stdout, advisory→stderr, honoring `should_output()`/`PMCP_QUIET`. Add `cargo-pmcp/tests/workbook_explain.rs` snapshot-testing the text render against the committed template (the SC3 Wave-0 gap). Add `cargo-pmcp/examples/workbook_explain.rs` (the CLAUDE.md ALWAYS example arm): a `main()` that loads the shipped `template.xlsx`, calls the same read-only ingest→synth→build_tools→`format_tool_surface` path, and prints the text render to stdout — demonstrating "preview the tool surface before deploy" as a runnable program (NOT only the CLI invocation). Keep each fn cog<=25.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test workbook_explain && cargo run -p cargo-pmcp --example workbook_explain && cargo run -p cargo-pmcp -- workbook explain crates/pmcp-workbook-compiler/tests/fixtures/template.xlsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cargo-pmcp/src/commands/workbook/explain.rs` exists with `fn execute` and a pure `format_tool_surface`; `mod.rs` carries `pub mod explain;` and an `Explain` enum variant + dispatch arm.
    - File exists: `cargo-pmcp/examples/workbook_explain.rs`; `cargo run -p cargo-pmcp --example workbook_explain` exits 0 and prints the template's tool surface (the CLAUDE.md ALWAYS example).
    - CLI: `cargo pmcp workbook explain template.xlsx` prints each output-table tool name + description + input/output schema to stdout (exit 0).
    - Behavior: `--format json` emits valid parseable JSON of the same surface; `cargo test -p cargo-pmcp --test workbook_explain` exits 0 (snapshot matches).
    - Source: `explain.rs` calls `pmcp_workbook_compiler::ingest::ingest` and writes NO bundle (read-only).
  </acceptance_criteria>
  <done>A BA can preview the exact tool surface an LLM will see before deploying — via CLI AND a runnable example.</done>
</task>

<task type="auto">
  <name>Task 2: BA-facing docs/training — pmcp-book + pmcp-course chapters (table model only, no retired terminology)</name>
  <files>pmcp-book/src/workbook-table-authoring.md, pmcp-book/src/SUMMARY.md, pmcp-course/src/workbook-table-authoring.md, pmcp-course/src/SUMMARY.md</files>
  <read_first>
    - docs/design/workbook-table-authoring-contract.md (WHOLE — the chapters are seeded from §1 north star, §3 authoring model, §4 multi-tool, §7 annotated reference diagram, §8 fail-helpful + explain)
    - pmcp-book/src/SUMMARY.md (the mdBook chapter index — find the workbook section to wire the new chapter into)
    - pmcp-course/src/SUMMARY.md (the course chapter index)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §"Proposed Requirement Breakdown" WBV2-07 (the BA training story arm of SC4)
    - cargo-pmcp/src/templates/workbook_bundle/template.xlsx (the shipped template the chapters reference as the worked example)
  </read_first>
  <action>
    Write `pmcp-book/src/workbook-table-authoring.md`: a BA-facing chapter teaching the table-based contract — the four region types (§3.1), the standard columns `name|value|description|tier` and what `value` does (type/unit/enum/test-seed/oracle, §3.2-§3.3), output Table → MCP tool with caption description (§4), DAG-derived per-tool inputs (§4.2 worked example: calculate_tax vs estimate_refund), governance via the tier dropdown (§6), and the `cargo pmcp workbook explain` preview + fail-helpful lint workflow (§8). Use the shipped `template.xlsx` as the running worked example and reproduce the §7 annotated reference diagram. Lead with the `cargo pmcp` CLI workflow (the project's docs convention). Write `pmcp-course/src/workbook-table-authoring.md` aligned with the book chapter (course voice — exercises/checkpoints), seeded from the same spec sections. Wire BOTH new files into their respective `SUMMARY.md` indexes under the existing workbook section. Cross-link the design contract for the authoritative reference. Do NOT restate retired `in_*`/`out_*`/`named range`/`define_name` mechanics — the chapter teaches ONLY the table model (the named-range model is superseded, §9). The "reads as a coherent tool surface to a human" property (the one manual-only verification in VALIDATION.md) is exercised by the explain section pointing the reader to run it on the template.
  </action>
  <verify>
    <automated>cd pmcp-book && mdbook build && cd ../pmcp-course && mdbook build && ! grep -rniE 'in_[a-z]|out_[a-z]|named range|define_name' pmcp-book/src/workbook-table-authoring.md pmcp-course/src/workbook-table-authoring.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `pmcp-book/src/workbook-table-authoring.md` and `pmcp-course/src/workbook-table-authoring.md`, each linked in its `SUMMARY.md`.
    - Behavior: `mdbook build` succeeds for both books (exit 0) with the new chapters included (no broken-link/build error).
    - Source (negative assertion): a grep for the retired model exits NON-zero (i.e. finds NOTHING) — `grep -rniE 'in_[a-z]|out_[a-z]|named range|define_name' pmcp-book/src/workbook-table-authoring.md pmcp-course/src/workbook-table-authoring.md` returns no match; neither chapter documents the retired `in_*`/`out_*` named-range / define_name model.
    - Source: both chapters reference the shipped `template.xlsx` and the `cargo pmcp workbook explain` workflow; the book chapter reproduces the §7 annotated reference diagram and the calculate_tax/estimate_refund DAG-derived-input worked example.
  </acceptance_criteria>
  <done>The table-based authoring contract is taught in both the book and the course, seeded from the spec + template, with the retired named-range model proven absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted .xlsx → explain ingest | explain ingests an arbitrary workbook read-only; it inherits the compiler's umya-isolation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-12 | Denial of Service | malformed .xlsx fed to `workbook explain` | mitigate | explain calls the SAME `ingest::ingest` boundary as lint/compile (CompileError, not panic); the Plan 02 table-ingest fuzz target covers the malformed-table path |
| T-100-13 | Information Disclosure | explain rendering a strict-constant cell as a callable input | mitigate | explain renders the SAME per-tool schema the server emits (Plan 04), which excludes strict-tier/computed cells — preview and runtime cannot diverge |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs — mdbook is an existing doc toolchain; no new crate |
</threat_model>

<verification>
- `cargo test -p cargo-pmcp --test workbook_explain` green; `cargo run -p cargo-pmcp --example workbook_explain` exits 0; `cargo pmcp workbook explain template.xlsx` runs.
- `mdbook build` green for pmcp-book and pmcp-course; the retired-terminology negative grep finds nothing.
</verification>

<success_criteria>
WBV2-06 + WBV2-07 satisfied: `workbook explain` previews the tool surface (text + json) before deploy, demonstrated by a runnable example; book + course chapters teach the table-based contract with the retired named-range model proven absent. Maps to ROADMAP Success Criteria 3 + 4 (training arm).
</success_criteria>

<output>
Create `.planning/phases/100-excel-workbook-built-in-servers-v2/100-05-SUMMARY.md` when done
</output>
</output>
