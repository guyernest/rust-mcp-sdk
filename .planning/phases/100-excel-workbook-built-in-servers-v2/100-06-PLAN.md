---
phase: 100-excel-workbook-built-in-servers-v2
plan: 06
type: execute
wave: 6
depends_on: ["100-04", "100-05"]
files_modified:
  - .planning/phases/100-excel-workbook-built-in-servers-v2/100-06-SUMMARY.md
autonomous: true
requirements: [WBV2-08]
must_haves:
  truths:
    - "make quality-gate exits 0 (fmt --all + clippy pedantic/nursery + build + test + audit) across the workspace"
    - "PMAT reports zero new cognitive-complexity violations (cog <=25, hard cap 50) in the touched crates"
    - "make purity-check exits 0 — no umya / calamine / quick-xml in pmcp-workbook-runtime or any served-binary dependency tree; rust_xlsxwriter confined to the compiler/author"
    - "A retired-symbol sweep grep proves strip_governance_prefix, name_named_inputs, promote_named_outputs, CalculateHandler, the Plan 03 CellMap::outputs() shim, and old in_*/out_* fixture-injection sites no longer exist anywhere in the workspace"
  artifacts:
    - path: ".planning/phases/100-excel-workbook-built-in-servers-v2/100-06-SUMMARY.md"
      provides: "Recorded gate results (quality-gate + PMAT + purity + retired-symbol sweep), all green"
  key_links:
    - from: "the served binary dependency tree (pmcp-workbook-server / pmcp-server-toolkit served path)"
      to: "no umya/calamine/quick-xml"
      via: "make purity-check cargo-tree ban"
      pattern: "purity-check"
    - from: "the retired named-range model"
      to: "no surviving symbol (strip_governance_prefix/name_named_inputs/promote_named_outputs/CalculateHandler/CellMap::outputs shim/in_*/out_*)"
      via: "retired-symbol sweep grep gate"
      pattern: "strip_governance_prefix|name_named_inputs|CalculateHandler"
---

<objective>
The phase-wide quality/purity gate (WBV2-08, ROADMAP Success Criterion 5). After all code waves
land, run the full Toyota-Way gate, the purity boundary assertion, AND a retired-symbol sweep on a
consistent tree, fix any residual violations, and record the results. This plan is the explicit
final gate the spec and CLAUDE.md demand: `make quality-gate` + PMAT (cog<=25) + `make purity-check`
all green, the umya-isolation boundary proven intact (no calamine; umya/quick-xml confined to the
compiler; rust_xlsxwriter confined to compiler/author and out of every served tree), and the
per-cell named-range model proven fully RETIRED (no surviving symbol anywhere).

Purpose: Guarantee the breaking surface change ships once, coherently, defect-free, with the
purity boundary that is the load-bearing invariant of the entire workbook subsystem untouched and
with zero dead compat code from the retired model or the Plan 03 to 04 transitional shim.

Output: green gates + a retired-symbol sweep + a SUMMARY recording each result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/workbook-table-authoring-contract.md
@.planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run make quality-gate + PMAT complexity gate; fix any residual violations</name>
  <files>(any source file flagged by the gate — fix in place)</files>
  <read_first>
    - CLAUDE.md (Quality Gate Enforcement: `make quality-gate` = fmt --all + clippy pedantic/nursery + build + test + audit; CI PMAT `pmat quality-gate --fail-on-violation --checks complexity`, cog<=25 hard cap 50 with annotated allow; the 6 refactor techniques P1-P6 in 75-RESEARCH.md)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md Pitfall 5 (cognitive complexity — Phase 99 just did a cog-reduction on these same crates; match that style) + §"Architectural Responsibility Map"
    - .planning/phases/75-fix-pmat-issues/75-RESEARCH.md (P1-P6 refactor techniques) — only if a cog>25 violation surfaces
  </read_first>
  <action>
    Run `make quality-gate` (the exact CI gate per CLAUDE.md — do NOT substitute bare `cargo clippy`, which is weaker than the pedantic+nursery `make lint`). Run the PMAT complexity gate locally: `pmat analyze complexity --format json --max-cognitive 25 | jq '.violations[] | select(.path | startswith("crates/pmcp-workbook"))'` plus the cargo-pmcp and server-toolkit workbook paths. For any cog>25 function introduced by Plans 01-05, apply one of the P1-P6 techniques (extract helper, table-dispatch, early-return, etc.) to bring it under 25 — do NOT add an `#[allow(clippy::cognitive_complexity)]` unless the function is irreducibly complex (parser/AST/protocol dispatch), and only with a `// Why:` annotation per the CLAUDE.md template (hard cap 50). Fix any fmt/clippy/audit findings. Re-run until both gates are clean. Zero SATD comments; comprehensive rustdoc on new public APIs.
  </action>
  <verify>
    <automated>make quality-gate</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `make quality-gate` exits 0 (fmt --all + clippy pedantic/nursery + build + test + audit all pass).
    - Behavior: `pmat analyze complexity --max-cognitive 25` reports zero violations under `crates/pmcp-workbook*`, `crates/pmcp-server-toolkit/src/workbook`, and `cargo-pmcp/src/commands/workbook` (or each remaining one carries a `// Why:`-annotated allow under the cap-50 ceiling).
    - Source: zero SATD comments introduced; no bare `#[allow(clippy::cognitive_complexity)]` without a `// Why:` justification.
  </acceptance_criteria>
  <done>The full Toyota-Way quality gate and the PMAT complexity gate are green on the consistent post-Plan-05 tree.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the purity boundary — make purity-check green; no reader in any served tree</name>
  <files>(any Cargo.toml feature/dep flagged by the purity check — fix in place)</files>
  <read_first>
    - CLAUDE.md (WBRT-04 purity boundary; `make purity-check`; the umya-isolation invariant)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §"Security Domain" (umya reader leaking into served binary → Elevation; Makefile:506+ cargo-tree ban) + §"Architectural Responsibility Map" (umya MUST stay confined to the compiler; rust_xlsxwriter is the ONLY provenance-clean writer, compiler/author-only)
    - .planning/STATE.md (Phase 91-03 WBRT-04 three-layer purity gate: cargo-tree per-crate/per-feature negative+positive + crate-local cargo-deny bans + crate split)
    - docs/design/workbook-table-authoring-contract.md §2 (purity boundary untouched: no new reader; umya confined to compiler), §6 (provenance orthogonal/untouched)
  </read_first>
  <action>
    Run `make purity-check` (the WBRT-04 three-layer gate: cargo-tree negative assertions that `umya-spreadsheet`/`calamine`/`quick-xml` do NOT appear in `pmcp-workbook-runtime` or the served-binary trees — `pmcp-workbook-server`, the `pmcp-server-toolkit` `workbook` served feature — plus the crate-local cargo-deny bans). Confirm: (1) NO `calamine` anywhere (the anti-pattern second reader was never added); (2) `umya-spreadsheet` appears ONLY in `pmcp-workbook-compiler`; (3) `rust_xlsxwriter` appears ONLY in the compiler/author path (the Plan 01 Table author is `#[cfg(test)]`, so it must NOT pull rust_xlsxwriter into any non-test served build — verify via `cargo tree -e features`); (4) `quick-xml` stays quarantined in the compiler provenance path. If any reader leaked into a served tree (e.g. a misplaced `TableRecord` consumer or an accidental feature unification from Plan 02/04), fix the dependency/feature boundary until the gate is green. Run `cargo tree -p pmcp-workbook-runtime -i umya-spreadsheet` and `-i calamine` and confirm both report "package not found in tree."
  </action>
  <verify>
    <automated>make purity-check</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `make purity-check` exits 0.
    - Behavior: `cargo tree -p pmcp-workbook-runtime -i umya-spreadsheet` and `cargo tree -p pmcp-workbook-runtime -i calamine` both report the package is not in the tree.
    - Source: `grep -rl calamine crates/ cargo-pmcp/` returns no Cargo.toml (no second reader ever added).
    - Behavior: `cargo tree -p pmcp-workbook-server -e features` shows neither `umya-spreadsheet` nor `rust_xlsxwriter` in the served binary tree.
  </acceptance_criteria>
  <done>The umya-isolation purity boundary is proven intact post-redesign: no calamine, umya/quick-xml compiler-only, rust_xlsxwriter compiler/author-only, no reader in any served tree.</done>
</task>

<task type="auto">
  <name>Task 3: Retired-symbol sweep — prove the per-cell named-range model + the Plan 03 shim no longer exist anywhere</name>
  <files>(grep-only gate; any stray surviving symbol found is fixed in place in its source file)</files>
  <read_first>
    - docs/design/workbook-table-authoring-contract.md §9 (cleanup ledger: F1 RESHAPE / F3-input RETIRE / F2 KEEP / strip_governance_prefix + name_named_inputs RETIRE) and §10 ("No legacy compat; retire the per-cell named-range model; keep F2")
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-04-PLAN.md (the Checkpoint C retirements + the removed Plan 03 CellMap::outputs() shim — the symbols this sweep confirms gone: strip_governance_prefix, name_named_inputs, promote_named_outputs's out_* path, CalculateHandler, the outputs() accessor)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-03-PLAN.md (the transitional #[deprecated] fn outputs(&self) accessor that Plan 04 must have deleted)
    - .planning/phases/100-excel-workbook-built-in-servers-v2/100-RESEARCH.md §"Open Questions (RESOLVED)" Q1 (Role::from_name_prefix/const_ fate — if the AUDIT kept a still-consumed const_ branch, exclude it from the sweep and record why)
  </read_first>
  <action>
    Resolve review finding #10: run an explicit retired-symbol sweep over the whole non-planning workspace (`crates/`, `cargo-pmcp/`, `examples/`, excluding `.planning/` and `target/`) asserting NONE of the retired named-range-model symbols survive. The banned symbols (each must return ZERO source matches):
      (1) `strip_governance_prefix` — deleted in Plan 04 Checkpoint C.
      (2) `name_named_inputs` — deleted in Plan 04 Checkpoint C.
      (3) `promote_named_outputs` — its out_* defined-name path removed; the fn name itself must be gone (output detection sourced from Tables).
      (4) `CalculateHandler` — replaced by the per-tool `WorkbookToolHandler` in Plan 04 Checkpoint A.
      (5) the Plan 03 transitional `CellMap::outputs()` accessor — a `fn outputs` grep on artifact_model.rs returns nothing AND no `.outputs()` caller on a CellMap remains.
      (6) old in_*/out_* fixture-injection sites — no `DefinedNameSpec` constructing an in_/out_-prefixed governance name, and no `define_name(` call injecting an in_/out_ governance prefix in fixture_author.rs or any fixture/test.
    Use a single combined gate over `*.rs` sources (the `verify` automated command below), plus the `fn outputs`/`.outputs()` greps from (5) and the in_/out_ injection greps from (6). EXCEPTION: if the Plan 04 Q1 audit KEPT a still-consumed `Role::from_name_prefix`/`const_` branch (recorded in the Plan 04 SUMMARY), that surviving symbol is legitimate and is excluded from this sweep with a note in the SUMMARY citing the Plan 04 audit decision (do NOT delete a still-consumed const_ governance branch — `from_name_prefix`/`const_` are deliberately NOT in the banned list above for this reason). If ANY banned symbol still has a source match, it is a retirement defect — delete it in its source file (or, if it is a legitimately-kept const_ branch, document the exception) and re-run the sweep until clean. Record the exact grep commands + their (empty) output + any documented exception in the SUMMARY. Tie this to WBV2-08 / Success Criterion 5 (the named-range model is retired, F2 retained).
  </action>
  <verify>
    <automated>! grep -rnE 'strip_governance_prefix|name_named_inputs|promote_named_outputs|CalculateHandler' crates/ cargo-pmcp/ examples/ --include=*.rs && ! grep -rn 'fn outputs' crates/pmcp-workbook-runtime/src/artifact_model.rs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `grep -rnE 'strip_governance_prefix|name_named_inputs|promote_named_outputs|CalculateHandler' crates/ cargo-pmcp/ examples/ --include=*.rs` returns NO matches (the combined gate exits non-zero = clean) — all four retired symbols are gone workspace-wide.
    - Behavior: `grep -rn 'fn outputs' crates/pmcp-workbook-runtime/src/artifact_model.rs` returns nothing AND `grep -rn '\.outputs()' crates/` shows no remaining CellMap caller — the Plan 03 transitional shim is fully removed.
    - Behavior: no in_/out_-prefixed `DefinedNameSpec`/`define_name(` governance-injection site remains in `fixture_author.rs` or any fixture/test (grep returns nothing for the in_/out_ injection pattern).
    - Source: the SUMMARY records each sweep command + its empty output; if a still-consumed `Role::from_name_prefix`/`const_` branch was kept by the Plan 04 audit, it is documented here as the single legitimate exception with a citation to the Plan 04 SUMMARY.
  </acceptance_criteria>
  <done>The per-cell named-range model and the Plan 03 transitional shim are proven fully retired workspace-wide; only an explicitly-documented, still-consumed const_ branch (if any) survives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler (umya/quick-xml/rust_xlsxwriter) → served binary | The reader/writer must NEVER enter the served tree — the load-bearing invariant of the whole subsystem |
| retired named-range model → shipped code | A surviving retired symbol (or the Plan 03 shim) would be dead compat code contradicting the "no legacy compat" locked decision (§10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-14 | Elevation (boundary breach) | umya/calamine/quick-xml/rust_xlsxwriter leaking into the served binary | mitigate | `make purity-check` three-layer cargo-tree + cargo-deny gate (WBRT-04) is run and must be green; explicit `cargo tree -i` negative assertions for umya + calamine on the runtime crate |
| T-100-15 | Tampering | a cog>25 / SATD / clippy regression shipping silently | mitigate | `make quality-gate` (CI-exact) + PMAT complexity gate run and required green before phase close |
| T-100-18 | Tampering (process) | a retired named-range symbol or the Plan 03 shim surviving as dead compat code | mitigate | the Task 3 retired-symbol sweep greps strip_governance_prefix/name_named_inputs/promote_named_outputs/CalculateHandler/CellMap::outputs()/in_*-out_* injection and requires ZERO matches; a surviving symbol is a retirement defect fixed before phase close |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — `cargo audit` (inside quality-gate) covers existing-dep advisories |
</threat_model>

<verification>
- `make quality-gate` exits 0.
- `make purity-check` exits 0.
- `cargo tree -i umya-spreadsheet`/`-i calamine` on the runtime crate both report "not in tree."
- The Task 3 retired-symbol sweep returns ZERO matches for every banned symbol (named-range model + Plan 03 shim fully retired).
</verification>

<success_criteria>
WBV2-08 satisfied: make quality-gate + PMAT (cog<=25) + make purity-check all green; the per-cell named-range model is retired AND proven gone by an explicit retired-symbol sweep (strip_governance_prefix/name_named_inputs/promote_named_outputs/CalculateHandler/CellMap::outputs() shim/in_*-out_* sites all absent); F2 retained; the purity boundary is intact. Maps to ROADMAP Success Criterion 5.
</success_criteria>

<output>
Create `.planning/phases/100-excel-workbook-built-in-servers-v2/100-06-SUMMARY.md` when done — record each gate's exit status, the purity cargo-tree negative-assertion output, and the retired-symbol sweep commands + their empty output.
</output>
</content>
</invoke>
