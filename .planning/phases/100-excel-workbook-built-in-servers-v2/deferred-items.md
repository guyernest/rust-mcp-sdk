# Deferred Items — Phase 100 (out-of-scope discoveries)

| Item | File:Line | Discovered | Disposition |
|------|-----------|-----------|-------------|
| Pre-existing clippy `map_or` simplification warning in a `#[cfg(test)]` property test (NOT introduced by Plan 03 — predates HEAD `626569ba`; in a file Plan 03 only touches via a lib.rs re-export). Out of scope per SCOPE BOUNDARY. | `crates/pmcp-workbook-runtime/src/manifest_model.rs:869` | Plan 03 Task 1 | Leave for a future cleanup; not gated by `make lint` (root pmcp crate only) and is a test-only `map_or`→`is_some_and` style lint. |
| Pre-existing `pmcp-toolkit-mysql` build error E0277 (`sqlx::query(&String)` no longer satisfies `SqlSafeStr` after a sqlx API bump — wants `AssertSqlSafe()`/`QueryBuilder`). VERIFIED present at HEAD via `git stash` build (unrelated to the workbook model lift). Out of scope per SCOPE BOUNDARY. | `crates/pmcp-toolkit-mysql/src/lib.rs:267` | Plan 03 Task 5 (workspace build) | Belongs to the SQL-connector milestone, not the workbook phase. `cargo build --workspace` cannot be globally green until this is fixed independently; Plan 03 verifies the workbook crate cone (runtime + compiler + server-toolkit + workbook-server + cargo-pmcp) builds + test-compiles green instead. |
| Pre-existing `unused import` warning `pmcp_code_mode::CodeExecutor` in a `#[cfg(test)]` block. | `crates/pmcp-server-toolkit/src/code_mode.rs:557` | Plan 03 Task 5 | Not introduced by Plan 03; out of scope. |
| Pre-existing cargo-pmcp test failures (3): `templates_workbook_server::tests::embedded_bundle_matches_committed_golden`, `embedded_xlsx_matches_committed_source`, `test_support_cache::proptests::normalize_round_trip_idempotent`. VERIFIED present at HEAD via `git stash` (identical 437-passed/3-failed count with AND without Plan 03 — ZERO new failures introduced). The cargo-pmcp `src/templates/workbook_bundle/` is a manually-maintained EMBEDDED MIRROR that had already drifted from the committed golden at HEAD (the embedded `tax-calc.xlsx` differs at byte 19, and the BUNDLE.lock evidence hash already diverged). The auth-cache proptest is wholly unrelated. | `cargo-pmcp/src/templates/workbook_server.rs:410,440`; `cargo-pmcp/src/commands/auth_cmd/cache.rs:419` | Plan 03 Task 5 | Out of scope (pre-existing two-copy mirror drift + unrelated proptest). NOTE for Plan 04: when Plan 04 regenerates the golden into the per-Table multi-tool shape, also refresh the cargo-pmcp embedded mirror (`workbook_bundle/tax-calc@1.1.0/` + `tax-calc.xlsx`) so these embedded-mirror tests go green — the xlsx drift must be resolved there too. |
