---
phase: 83-toolkit-core-lift-pmcp-server-toolkit
plan: 09
type: execute
wave: 6
depends_on:
  - 02
  - 03
  - 04
  - 05
  - 06
  - 07
  - 08
files_modified:
  - crates/pmcp-server-toolkit/fuzz/Cargo.toml
  - crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs
  - contracts/binding.yaml
  - .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md
  - .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md
  - crates/pmcp-server-toolkit/Cargo.toml
autonomous: true
requirements:
  - TKIT-01
  - TKIT-08
  - TEST-02
  - TEST-03

tags:
  - toolkit
  - fuzz
  - contract
  - shim-diff
  - migration-guide
  - publish-gate

must_haves:
  truths:
    - "`crates/pmcp-server-toolkit/fuzz/Cargo.toml` declares a `pmcp-server-toolkit-fuzz` workspace-isolated crate (Phase 77 pattern verbatim — PATTERNS §17)"
    - "`crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs` ships a libFuzzer target stressing `ServerConfig::from_toml(arbitrary_bytes)` (CLAUDE.md ALWAYS-fuzz)"
    - "`cargo +nightly fuzz run pmcp_server_toolkit_config_parser -- -max_total_time=60` exits 0 with no panics OR is documented as a CI/nightly target (matches Phase 77 disposition: source lands even if stable can't run; CI nightly runs it)"
    - "`cargo +nightly check -p pmcp-server-toolkit-fuzz` succeeds locally (Phase 77 stable-check disposition)"
    - "`contracts/binding.yaml` is extended with ≥6 rows covering toolkit public symbols: synthesize_from_config, executor_from_config, StaticAuthProvider, SecretsProvider trait, StaticResourceHandler, StaticPromptHandler, SqlConnector trait, translate_placeholders, assemble_code_mode_prompt, ServerBuilderExt (one row per TKIT-02..10 anchor)"
    - "`pmat comply check` exits 0 against the toolkit crate (or documents the absence per CLAUDE.md if pmat is not available locally)"
    - "`.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md` exists as the operator-handoff artifact (D-04) containing: (a) target lib.rs content for `pmcp-run/built-in/shared/mcp-server-common/src/lib.rs`, (b) Cargo.toml diff for `pmcp-run/built-in/shared/mcp-server-common/Cargo.toml`, (c) apply instructions for the operator"
    - "`.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md` exists as the developer-facing migration guide describing the one-page recipe for moving from in-tree path-deps to the public toolkit (REF-03 anchor; Phase 89 owns the book chapter)"
    - "`Cargo.toml` `exclude = [...]` validated by `cargo package --list -p pmcp-server-toolkit` — output must NOT include `.planning/`, `.pmat/`, `fixtures/`, `tests/`, `fuzz/` (Pitfall 6)"
    - "Final `make quality-gate` is green across the whole workspace (NOT just `-p pmcp-server-toolkit`)"
    - "Workspace-wide cross-feature builds pass: `cargo build --workspace`, `cargo test --workspace --no-fail-fast`"
    - "`cargo publish --dry-run -p pmcp-server-toolkit` succeeds (publish-gate verification per CLAUDE.md §Release & Publish Workflow)"
  artifacts:
    - path: crates/pmcp-server-toolkit/fuzz/Cargo.toml
      provides: "Workspace-isolated fuzz crate manifest"
      contains: "cargo-fuzz = true"
    - path: crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs
      provides: "libFuzzer target for ServerConfig::from_toml DoS protection"
      contains: "ServerConfig"
    - path: contracts/binding.yaml
      provides: "Toolkit public-API contract entries"
      contains: "pmcp_server_toolkit"
    - path: .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md
      provides: "Operator handoff: re-export shim for pmcp-run/built-in/shared/mcp-server-common (D-04)"
      contains: "pub use pmcp_server_toolkit"
    - path: .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md
      provides: "Developer migration guide (REF-03 anchor; Phase 89 expands to book chapter)"
      contains: "pmcp-server-toolkit"
  key_links:
    - from: "fuzz target"
      to: "ServerConfig::from_toml"
      via: "libFuzzer + toml::from_str"
      pattern: "ServerConfig::from_toml|toml::from_str.*ServerConfig"
    - from: "contracts/binding.yaml extension rows"
      to: "pmcp_server_toolkit public symbols"
      via: "module_path field"
      pattern: "pmcp_server_toolkit"
    - from: "shim-pmcp-run-shared.md"
      to: "pmcp-run/built-in/shared/mcp-server-common/src/lib.rs"
      via: "pub use pmcp_server_toolkit::*"
      pattern: "pub use pmcp_server_toolkit"
---

<objective>
Wave-5 capstone: ship the ALWAYS-fuzz target, the `contracts/binding.yaml` extension with one row per TKIT-02..10 public symbol, the operator-handoff shim diff (D-04) for the cross-repo migration, the developer migration guide, and the publish-gate verification (`cargo publish --dry-run` + `cargo package --list`).

Purpose: TEST-02 fuzz (ALWAYS-fuzz CLAUDE.md requirement); CMSUP-05 / Contract-First Development requirement; D-04 operator handoff; REF-03 migration recipe anchor; TKIT-01 publishability verification.

Output: The toolkit crate is publishable, fuzz-protected against malformed TOML, contractually documented, AND the cross-repo operator handoff bundle is ready for the pmcp-run team to apply.
</objective>

<execution_context>
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/Development/mcp/sdk/rust-mcp-sdk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md
@.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-08-SUMMARY.md
@CLAUDE.md
@contracts/binding.yaml

<interfaces>
<!-- Phase 77 fuzz target shape — PATTERNS §17 says mirror this exactly with a different `T` for toml::from_str. -->

From `cargo-pmcp/fuzz/Cargo.toml:1-50`:
```toml
[package]
name = "cargo-pmcp-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
cargo-pmcp = { path = ".." }
toml = "1.0"

# Prevent this from interfering with workspace
[workspace]

[[bin]]
name = "pmcp_config_toml_parser"
path = "fuzz_targets/pmcp_config_toml_parser.rs"
doc = false
test = false
bench = false
```

From `cargo-pmcp/fuzz/fuzz_targets/pmcp_config_toml_parser.rs:1-26`:
```rust
#![no_main]
use libfuzzer_sys::fuzz_target;
fuzz_target!(|data: &[u8]| {
    let Ok(s) = std::str::from_utf8(data) else { return; };
    let _: Result<cargo_pmcp::test_support::configure_config::TargetConfigV1, _> = toml::from_str(s);
});
```

From `contracts/binding.yaml:1-90` (existing-row shape — PATTERNS §18):
```yaml
- contract: mcp-protocol-sdk-v1.yaml
  equation: jsonrpc_framing
  function: JSONRPCRequest::validate
  module_path: pmcp::types::jsonrpc
  signature: 'pub fn validate(&self) -> Result<(), crate::Error>'
  status: implemented
  notes: Validates jsonrpc field equals "2.0"; returns Error::Validation on mismatch
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ship the fuzz target (CLAUDE.md ALWAYS-fuzz)</name>
  <read_first>
    - `cargo-pmcp/fuzz/Cargo.toml` (full — primary analog per PATTERNS §17)
    - `cargo-pmcp/fuzz/fuzz_targets/pmcp_config_toml_parser.rs` (full — primary analog)
    - `crates/pmcp-server-toolkit/src/config.rs` (Plan 04) — `ServerConfig::from_toml` target signature
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §17 (verbatim mirror)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md` §"Validation Architecture" Wave 0 entry for fuzz target
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md` §"Decisions" D-17 (fuzz target is a P83 deliverable)
  </read_first>
  <action>
Create `crates/pmcp-server-toolkit/fuzz/Cargo.toml`:

```toml
[package]
name = "pmcp-server-toolkit-fuzz"
version = "0.0.0"
publish = false
edition = "2021"
license = "MIT OR Apache-2.0"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
pmcp-server-toolkit = { path = ".." }
toml = "1.0"

# Prevent this from interfering with workspace
[workspace]

[[bin]]
name = "pmcp_server_toolkit_config_parser"
path = "fuzz_targets/pmcp_server_toolkit_config_parser.rs"
doc = false
test = false
bench = false
```

Create `crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs`:

```rust
//! Phase 83 fuzz target — stress `pmcp_server_toolkit::config::ServerConfig::from_toml`
//! against arbitrary byte sequences. The parser must not panic on adversarial
//! input — `toml::from_str` either succeeds (valid TOML matching schema) or
//! returns Err.
//!
//! Threat model: T-83-04-01 (adversarial TOML → parser DoS via panic).
//!
//! Run nightly: `cargo +nightly fuzz run pmcp_server_toolkit_config_parser`
//! Smoke (60s): `cargo +nightly fuzz run pmcp_server_toolkit_config_parser -- -max_total_time=60`
//!
//! On stable, `cargo +nightly check --bin pmcp_server_toolkit_config_parser` from
//! `crates/pmcp-server-toolkit/fuzz/` verifies the target compiles. This matches
//! Phase 77's disposition: source lands, CI nightly exercises the fuzzer.
//!
//! Cannot reuse `cargo-pmcp`'s Phase 77 fuzz target — that one targets
//! `cargo_pmcp::test_support::configure_config::TargetConfigV1`, a different
//! schema (per RESEARCH §"Wave 0 Gaps").

#![no_main]

use libfuzzer_sys::fuzz_target;
use pmcp_server_toolkit::config::ServerConfig;

fuzz_target!(|data: &[u8]| {
    let Ok(s) = std::str::from_utf8(data) else {
        return;
    };
    // Discard the result — we only care that the parser doesn't panic.
    let _: Result<ServerConfig, _> = ServerConfig::from_toml(s);
});
```

Verify the target compiles by running:
```
cd crates/pmcp-server-toolkit/fuzz && cargo +nightly check --bin pmcp_server_toolkit_config_parser
```

If `cargo +nightly` is unavailable (e.g. only stable toolchain installed), the compile-check shape from Phase 77 still requires the source files to exist; cmd will fail with "nightly toolchain not installed" — that's the documented Phase 77 disposition (per CLAUDE.md). The target source LANDS and CI nightly exercises it.

Run the 60-second smoke if nightly is available:
```
cd crates/pmcp-server-toolkit/fuzz && cargo +nightly fuzz run pmcp_server_toolkit_config_parser -- -max_total_time=60
```

Expected: zero crashes, zero leaks. If a crash is found, READ the crash input (`fuzz/artifacts/.../crash-*`) and fix the underlying parse bug in `config.rs` — DO NOT just add the input to the corpus.

After the fuzz workspace is in place, verify `cargo build --workspace` from the workspace root still succeeds (the `[workspace]` line in `fuzz/Cargo.toml` excludes the fuzz crate from the main workspace per the Phase 77 pattern).
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && test -f crates/pmcp-server-toolkit/fuzz/Cargo.toml && test -f crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs && grep -q "cargo-fuzz = true" crates/pmcp-server-toolkit/fuzz/Cargo.toml && grep -q "^\\[workspace\\]" crates/pmcp-server-toolkit/fuzz/Cargo.toml && grep -q "pmcp_server_toolkit::config::ServerConfig" crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs && grep -q "ServerConfig::from_toml" crates/pmcp-server-toolkit/fuzz/fuzz_targets/pmcp_server_toolkit_config_parser.rs && (cd crates/pmcp-server-toolkit/fuzz && (cargo +nightly check --bin pmcp_server_toolkit_config_parser 2>&1 || cargo check --bin pmcp_server_toolkit_config_parser 2>&1) | tee /tmp/p83-09-t1.log; ! grep -E "^error\\[" /tmp/p83-09-t1.log) && cargo build --workspace --quiet && echo "ok"</automated>
  </verify>
  <done>
    - `fuzz/Cargo.toml` exists with `cargo-fuzz = true` + isolated `[workspace]` line.
    - `fuzz_targets/pmcp_server_toolkit_config_parser.rs` exists, calls `ServerConfig::from_toml`.
    - The target compiles under nightly (or stable, both ideally — Phase 77 disposition accepts source landing).
    - `cargo build --workspace` (main workspace) still succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend contracts/binding.yaml with toolkit public-API rows</name>
  <read_first>
    - `contracts/binding.yaml` (full — confirm existing row shape; check the trailing newline + comment convention)
    - `crates/pmcp-server-toolkit/src/lib.rs` (final, post-Plan 08) — every `pub use` re-export is a contract candidate
    - `crates/pmcp-server-toolkit/src/{auth, secrets, config, resources, prompts, tools, code_mode, sql/mod, builder_ext}.rs` — confirm exact signatures for public fns / structs / traits
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §18 (append-row pattern)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md` §"Open Questions" #1 (extend, not new file)
    - `CLAUDE.md` §"Contract-First Development" (use `pmat comply check` to validate)
  </read_first>
  <action>
**Sub-step 2a — generate rustdoc for signature extraction:**

Before pasting ANY row into `contracts/binding.yaml`, generate the canonical rustdoc so that signatures in the YAML match implementation verbatim (checker dimension: verification_derivation):

```sh
cargo doc -p pmcp-server-toolkit --no-deps
# Open the generated rustdoc; on macOS:
#   open target/doc/pmcp_server_toolkit/index.html
# Or, if available, use cargo-public-api for a flat symbol list:
#   cargo public-api -p pmcp-server-toolkit
```

**Sub-step 2b — copy canonical signatures verbatim (consistent alias treatment):**

For each public symbol you plan to add to `contracts/binding.yaml`, copy its canonical signature directly from the rustdoc page (or `cargo public-api` output) into the corresponding `signature:` field. Verbatim — do NOT paraphrase, re-order args, or rename types.

Especially: the toolkit's `crate::error::Result<T>` alias must be handled **consistently** across all rows. Pick ONE of:
- (a) Keep the alias form: `Result<T>` (where `T` is the success type; reader infers the alias).
- (b) Expand to the concrete form: `Result<T, ToolkitError>`.

Whichever form you pick, apply it to **every** toolkit row. Inconsistency between rows (e.g. one row uses `Result<Box<dyn CodeExecutor>, ToolkitError>` while another uses `Result<Box<dyn CodeExecutor>>`) causes `pmat comply check` to flag spurious mismatches against the actual rustdoc-emitted signatures.

The signatures in the row examples below are illustrative — pull the actual current shape from rustdoc before committing.

**Sub-step 2c — append the rows:**

Append a new section to `contracts/binding.yaml` (at the END of the file, before any trailing newline) per PATTERNS §18:

```yaml

# === Toolkit Core (Phase 83) ===

- contract: mcp-protocol-sdk-v1.yaml
  equation: config_strict_parse
  function: ServerConfig::from_toml
  module_path: pmcp_server_toolkit::config
  signature: 'pub fn from_toml(toml_str: &str) -> crate::error::Result<Self>'
  status: implemented
  notes: TKIT-01 — strict-parse entry point; #[serde(deny_unknown_fields)] on every section (D-13)

- contract: mcp-protocol-sdk-v1.yaml
  equation: auth_provider_contract
  function: StaticAuthProvider::new
  module_path: pmcp_server_toolkit::auth
  signature: 'pub fn new(expected_token: impl Into<String>) -> Self'
  status: implemented
  notes: TKIT-02 — concrete impl of pmcp::AuthProvider trait (dev/test bearer-token)

- contract: mcp-protocol-sdk-v1.yaml
  equation: secrets_provider_contract
  function: SecretsProvider::get
  module_path: pmcp_server_toolkit::secrets
  signature: 'async fn get(&self, name: &str) -> Result<TokenSecret, ToolkitError>'
  status: implemented
  notes: >-
    TKIT-03 — toolkit's secrets surface. Return type is TokenSecret (never raw String/Vec<u8>);
    secrecy+zeroize guarantee per CMSUP-02. EnvSecrets is the default impl.

- contract: mcp-protocol-sdk-v1.yaml
  equation: static_resource_handler_contract
  function: StaticResourceHandler::new
  module_path: pmcp_server_toolkit::resources
  signature: 'pub fn new(resources: IndexMap<String, ResourceEntry>) -> Self'
  status: implemented
  notes: TKIT-04 — impl of pmcp::ResourceHandler; MIME-typed wire via Content::resource_with_text

- contract: mcp-protocol-sdk-v1.yaml
  equation: static_prompt_handler_contract
  function: StaticPromptHandler::handle
  module_path: pmcp_server_toolkit::prompts
  signature: 'async fn handle(&self, args: HashMap<String, String>, _extra: RequestHandlerExtra) -> Result<GetPromptResult>'
  status: implemented
  notes: TKIT-05 — impl of pmcp::PromptHandler; arg-validation returns pmcp::Error::validation

- contract: mcp-protocol-sdk-v1.yaml
  equation: hmac_token_reexport
  function: pmcp_server_toolkit::code_mode::HmacTokenGenerator
  module_path: pmcp_server_toolkit::code_mode
  signature: 'pub use pmcp_code_mode::HmacTokenGenerator'
  status: implemented
  notes: TKIT-06 — re-export only (D-16). No duplicate HMAC code in the toolkit.

- contract: mcp-protocol-sdk-v1.yaml
  equation: tool_synthesis
  function: synthesize_from_config
  module_path: pmcp_server_toolkit::tools
  signature: 'pub fn synthesize_from_config(config: &ServerConfig) -> Result<Vec<(String, ToolInfo, Arc<dyn ToolHandler>)>, ToolkitError>'
  status: implemented
  notes: >-
    TKIT-07 — turns [[tools]] entries into pmcp ToolInfo + handler pairs. Handler.metadata()
    returns Some(ToolInfo) per the Phase 82 tool_arc registration invariant (RESEARCH §Risks #2).

- contract: mcp-protocol-sdk-v1.yaml
  equation: backend_core_smoke
  function: backend_core_construction_surface_smoke
  module_path: pmcp_server_toolkit (integration test)
  signature: '#[tokio::test] async fn backend_core_construction_surface_smoke()'
  status: implemented
  notes: TKIT-08 — D-03 anchor; replays mcp-sql/graphql/openapi-core construction without cross-repo CI

- contract: mcp-protocol-sdk-v1.yaml
  equation: code_mode_wiring
  function: executor_from_config
  module_path: pmcp_server_toolkit::code_mode
  signature: 'pub fn executor_from_config(config: &ServerConfig) -> Result<Box<dyn CodeExecutor>, ToolkitError>'
  status: implemented
  notes: TKIT-09 — wires [code_mode] block + [code_mode.limits] into pmcp_code_mode::CodeExecutor

- contract: mcp-protocol-sdk-v1.yaml
  equation: code_mode_prompt_assembly
  function: assemble_code_mode_prompt
  module_path: pmcp_server_toolkit::code_mode
  signature: 'pub async fn assemble_code_mode_prompt(connector: &dyn SqlConnector, config: &ServerConfig) -> Result<String, ToolkitError>'
  status: implemented
  notes: TKIT-10 — combines connector.schema_text() with [[database.tables]] descriptions

- contract: mcp-protocol-sdk-v1.yaml
  equation: sql_connector_trait
  function: SqlConnector
  module_path: pmcp_server_toolkit::sql
  signature: 'pub trait SqlConnector: Send + Sync + 'static { fn dialect(&self) -> Dialect; async fn execute(...); async fn schema_text(...); }'
  status: stub
  notes: TKIT-10 trait stub; per-backend impls land in Phase 84 (per-spike-005)

- contract: mcp-protocol-sdk-v1.yaml
  equation: server_builder_ext
  function: ServerBuilderExt::tools_from_config
  module_path: pmcp_server_toolkit::builder_ext
  signature: 'fn tools_from_config(self, config: &ServerConfig) -> Self'
  status: implemented
  notes: D-10 — wires synthesize_from_config into pmcp::ServerBuilder via Phase 82 tool_arc
```

Adjust signatures to match actual implementations from Plans 02–08 (e.g. if `StaticResourceHandler::new` accepts a different argument shape, update the row).

Run `pmat comply check` from the workspace root if pmat is available. If it errors, fix the contract entries to satisfy the validator. If pmat is not locally available, document this in the Plan 09 SUMMARY (CI may run pmat separately; CLAUDE.md §"PMAT Quality-Gate Proxy Mode" suggests local pmat for dev).
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && grep -q "Toolkit Core (Phase 83)" contracts/binding.yaml && grep -q "pmcp_server_toolkit::config" contracts/binding.yaml && grep -q "pmcp_server_toolkit::secrets" contracts/binding.yaml && grep -q "pmcp_server_toolkit::tools" contracts/binding.yaml && grep -q "pmcp_server_toolkit::code_mode" contracts/binding.yaml && grep -q "pmcp_server_toolkit::sql" contracts/binding.yaml && grep -q "pmcp_server_toolkit::builder_ext" contracts/binding.yaml && [ "$(grep -c "module_path: pmcp_server_toolkit" contracts/binding.yaml)" -ge 8 ] && (pmat comply check 2>&1 || pmat --version 2>&1 | grep -q "command not found" && echo "pmat-absent-acceptable" ) && echo "ok"</automated>
  </verify>
  <done>
    - `contracts/binding.yaml` extended with ≥10 rows covering toolkit public API (one per TKIT-02..10 plus extras for re-exports and synthesizer).
    - `pmat comply check` exits 0 (or is documented absent — CI handles validation).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write shim diff artifact + migration guide</name>
  <read_first>
    - External: `~/Development/mcp/sdk/pmcp-run/built-in/shared/mcp-server-common/Cargo.toml` (full — operator must apply Cargo.toml diff here)
    - External: `~/Development/mcp/sdk/pmcp-run/built-in/shared/mcp-server-common/src/lib.rs` (full — operator replaces with re-export shim)
    - `crates/pmcp-server-toolkit/src/lib.rs` (final — what the shim re-exports through)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-CONTEXT.md` §"Decisions" D-01, D-02, D-04
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md` §"Phase Requirements" TKIT-08 row
  </read_first>
  <action>
**Step A — operator handoff (D-04): `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md`**

```markdown
# Phase 83 D-04 Operator Handoff — pmcp-run shim diff

**Target repository:** `pmcp-run` (sibling repo; external to rust-mcp-sdk)
**Target path:** `pmcp-run/built-in/shared/mcp-server-common/`
**Operator:** the owner of the `pmcp-run` repo
**Apply after:** `pmcp-server-toolkit 0.1.0` ships to crates.io
**Status:** P83 ships this artifact; cross-repo PR is tracked separately

## Why this exists

Per CONTEXT.md D-01 / D-02, the toolkit publish in Phase 83 happens FIRST.
The `pmcp-run/built-in/shared/mcp-server-common/` crate then becomes a pure
re-export shim that `pub use pmcp_server_toolkit::*` to keep the three
backend cores (`mcp-sql-server-core`, `mcp-graphql-server-core`,
`mcp-openapi-server-core`) building with zero source diff in those cores'
files.

This artifact is a copy-paste-ready apply-bundle. Drop the contents below
into the corresponding file paths in the `pmcp-run` repo and the shim is in
place.

## File 1: `pmcp-run/built-in/shared/mcp-server-common/src/lib.rs`

Replace the entire file with:

```rust
// Phase 83 shim: mcp-server-common now re-exports pmcp-server-toolkit.
//
// The toolkit lives at crates.io as `pmcp-server-toolkit` and is the
// source of truth for AuthProvider/SecretsProvider/StaticResourceHandler/
// StaticPromptHandler/HMAC token machinery/ToolInfo synthesizer/code-mode
// wiring. This shim exists so the three backend cores in this repo
// (mcp-sql-server-core, mcp-graphql-server-core, mcp-openapi-server-core)
// can swap their path-dep on this crate for a versioned crates.io dep on
// pmcp-server-toolkit without modifying any source.
//
// Per Phase 83 CONTEXT.md D-02: this shim is deletable once each backend
// core updates its Cargo.toml dep line. Until then, this file makes the
// transition incremental and safe.

pub use pmcp_server_toolkit::*;

// Feature-gated re-exports for AVP and DDB shapes that mcp-server-common
// historically had — if either is still referenced by any backend core,
// the shim forwards through to pmcp-server-toolkit's gated APIs.

#[cfg(feature = "avp")]
pub use pmcp_server_toolkit::code_mode::{AvpClient, AvpConfig, AvpPolicyEvaluator};

// Note: DDB (ddb / dynamo-config features) were dropped from the toolkit
// per Phase 83 D-14 — those concerns are pmcp-run-specific. If the
// backend cores need DDB types, they should depend on whatever crate
// owns them in the pmcp-run ecosystem (likely `pmcp-run-storage` or
// equivalent), not on this shim.
```

## File 2: `pmcp-run/built-in/shared/mcp-server-common/Cargo.toml`

Diff to apply (the operator merges this; exact line numbers depend on the
current file):

```diff
[package]
name = "mcp-server-common"
- version = "0.X.Y"     # whatever the current version is
+ version = "0.Z.Z"     # bump to signal the shim transition
 edition = "2021"

[dependencies]
- # All previous direct deps that the toolkit now provides transitively
- pmcp = { ... }
- async-trait = { ... }
- serde = { ... }
- # ... etc.
+ # Single dep — the toolkit pulls everything else transitively
+ pmcp-server-toolkit = { version = "0.1.0", features = ["code-mode"] }
+
+ # Keep ddb / dynamo-config / openapi-code-mode / js-runtime / mcp-code-mode
+ # deps if any backend core still references them; the toolkit DROPPED these
+ # features per D-14 and they live elsewhere now.

[features]
- # Slim the feature list — most are now sourced from pmcp-server-toolkit.
- default = []
- code-mode = []
- avp = []
- aws = []
- # ddb, dynamo-config, openapi-code-mode, js-runtime, mcp-code-mode — keep
- # only if cores still gate on them; otherwise delete.
+ default = []
+ code-mode = ["pmcp-server-toolkit/code-mode"]
+ avp = ["pmcp-server-toolkit/avp"]
+ aws = ["pmcp-server-toolkit/aws"]
```

## Apply instructions

```sh
# In the pmcp-run repo:
cd ~/Development/mcp/sdk/pmcp-run
git checkout -b chore/p83-toolkit-shim

# Replace src/lib.rs (paste File 1 content over the existing file)
$EDITOR built-in/shared/mcp-server-common/src/lib.rs

# Apply the Cargo.toml diff (manual merge — exact lines vary)
$EDITOR built-in/shared/mcp-server-common/Cargo.toml

# Verify each backend core still builds
cargo build -p mcp-sql-server-core
cargo build -p mcp-graphql-server-core
cargo build -p mcp-openapi-server-core
cargo test --workspace

# Submit PR
git add built-in/shared/mcp-server-common
git commit -m "chore(shared): swap mcp-server-common to pmcp-server-toolkit re-export shim (Phase 83 D-02)"
git push -u origin chore/p83-toolkit-shim
```

## Rollback

If a backend core breaks at build time, the shim either re-exports something
under a different name (file a bug against `pmcp-server-toolkit`) or one of
the dropped features (`ddb`, `dynamo-config`, `openapi-code-mode`,
`js-runtime`, `mcp-code-mode`) was actually still being used. In the latter
case, restore the specific dep line in `mcp-server-common/Cargo.toml` and
update its `lib.rs` to also re-export from the relevant non-toolkit crate.

## Reference: P83 SC-5

Per ROADMAP §Phase 83 SC-5, the in-toolkit smoke test
(`crates/pmcp-server-toolkit/tests/backend_core_smoke.rs`, Plan 08 Task 2)
already PROVES the toolkit's public API surface covers what the backend
cores construct today. This artifact is the cross-repo apply step; the
smoke test is the local confidence that the apply will succeed.
```

**Step B — developer migration guide: `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md`**

```markdown
# Migrating to `pmcp-server-toolkit`

**Audience:** Developers who currently build MCP servers against
`pmcp-run`'s internal `mcp-server-common` (or who hand-rolled the
AuthProvider/SecretsProvider/static-handler/HMAC-token plumbing themselves).

**Source of truth:** This document is the Phase 83 anchor for REF-03.
Phase 89 expands this into a full book chapter in `pmcp-book/src/`; this
file is the developer-facing one-pager.

## What changed in Phase 83

The proto-SDK that lived at `pmcp-run/built-in/shared/mcp-server-common/`
is now a public crates.io crate: `pmcp-server-toolkit`.

It ships:
- `AuthProvider` impls (`StaticAuthProvider` + AWS-gated)
- `SecretsProvider` trait + `EnvSecrets` (+ AWS-gated)
- `StaticResourceHandler`, `StaticPromptHandler`
- HMAC token machinery (re-exported from `pmcp-code-mode`)
- `[[tools]]` → `ToolInfo` synthesizer with zero per-tool Rust handlers
- `[code_mode]` config → `CodeExecutor` wiring
- `SqlConnector` trait stub (impls in Phase 84 per-backend crates)

## One-page migration

### Before

Your `Cargo.toml`:
```toml
[dependencies]
mcp-server-common = { path = "../shared/mcp-server-common" }
pmcp = "..."
async-trait = "..."
# ... and all the deps mcp-server-common needed transitively
```

Your `main.rs`:
```rust
use mcp_server_common::{StaticAuthProvider, SecretsProvider, ...};
```

### After

Your `Cargo.toml`:
```toml
[dependencies]
pmcp = "2.8.1"
pmcp-server-toolkit = { version = "0.1.0", features = ["code-mode"] }
# That's it — the toolkit pulls the rest transitively
```

Your `main.rs`:
```rust
use pmcp_server_toolkit::{
    auth::StaticAuthProvider,
    config::ServerConfig,
    resources::StaticResourceHandler,
    secrets::EnvSecrets,
    ServerBuilderExt,
};

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let cfg = ServerConfig::from_toml(include_str!("config.toml"))?;
    let server = pmcp::Server::builder()
        .name(&cfg.server.name)
        .version(&cfg.server.version)
        .tools_from_config(&cfg)
        .code_mode_from_config(&cfg)
        .resources_arc(std::sync::Arc::new(StaticResourceHandler::from(&cfg)))
        .auth_provider_arc(std::sync::Arc::new(StaticAuthProvider::new("dev-token")))
        .build()?;
    server.run_stdio().await?;
    Ok(())
}
```

## Symbol mapping

| Old (mcp-server-common) | New (pmcp-server-toolkit) |
|-------------------------|---------------------------|
| `mcp_server_common::StaticAuthProvider` | `pmcp_server_toolkit::auth::StaticAuthProvider` |
| `mcp_server_common::SecretsProvider` | `pmcp_server_toolkit::secrets::SecretsProvider` |
| `mcp_server_common::EnvSecrets` | `pmcp_server_toolkit::secrets::EnvSecrets` |
| `mcp_server_common::StaticResourceHandler` | `pmcp_server_toolkit::resources::StaticResourceHandler` |
| `mcp_server_common::StaticPromptHandler` | `pmcp_server_toolkit::prompts::StaticPromptHandler` |
| (custom HMAC code) | `pmcp_server_toolkit::code_mode::HmacTokenGenerator` (re-exported from `pmcp-code-mode`) |
| (custom ToolInfo construction) | `pmcp_server_toolkit::tools::synthesize_from_config` |
| (custom code-mode glue) | `pmcp_server_toolkit::code_mode::executor_from_config` + `.code_mode_from_config(&cfg)` |

## Dropped from `mcp-server-common`

Per CONTEXT.md D-14 these features were intentionally NOT lifted; they
were either pmcp-run-specific or live in a future phase:

- `ddb`, `dynamo-config` — pmcp-run team-mcp / durable-agent concerns;
  re-introduce only if a generalised pattern emerges.
- `openapi-code-mode`, `js-runtime`, `mcp-code-mode` — Phase 3 OpenAPI
  territory; gated by spike 007 (`openapi-auth-policy-pluggability`).

If your server uses any of these, keep the corresponding deps explicit
in your `Cargo.toml` (don't expect them through the toolkit).

## REF-01 superset guarantee

Your existing `config.toml` from a pmcp-run sql-api server (open-images /
imdb / msr-vtt) parses unchanged through `ServerConfig::from_toml`. If
yours doesn't, it's a P83 bug — file an issue.

## Where to go next

- Phase 84 ships the per-backend `SqlConnector` impls (`pmcp-toolkit-postgres`,
  `pmcp-toolkit-mysql`, `pmcp-toolkit-athena`, plus `sqlite` feature).
- Phase 85 ships `pmcp-sql-server` Shape A — a pure-config-driven binary.
- Phase 86 ships `cargo pmcp new --kind sql-server` scaffold + Shape C
  ≤15-line `main.rs` example.
- Phase 87 ships `pmcp-config-helper` — a Type 2 MCP authoring skills
  server bundled with config-authoring guidance.
- Phase 89 expands this one-pager into a full book chapter under
  `pmcp-book/src/`.

## Reporting issues

`pmcp-server-toolkit` issues belong in the `paiml/rust-mcp-sdk` issue
tracker. Cross-reference Phase 83 by including `phase-83` in the issue
labels.
```

Both files are static documentation; verify with `markdownlint` if
available, otherwise just confirm they exist and contain the expected
sections.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && test -f .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md && test -f .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md && grep -q "pub use pmcp_server_toolkit::\\*" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md && grep -q "## Apply instructions" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md && grep -q "## Symbol mapping" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md && grep -q "REF-01 superset" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md && [ "$(wc -l < .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md)" -ge 50 ] && [ "$(wc -l < .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md)" -ge 50 ] && echo "ok"
    </automated>
  </verify>
  <done>
    - `shim-pmcp-run-shared.md` exists with all 5 sections (Why, File 1 lib.rs, File 2 Cargo.toml diff, Apply instructions, Rollback, Reference).
    - `pmcp-server-toolkit-migration.md` exists with all sections (What changed, One-page migration, Symbol mapping, Dropped, REF-01 superset, Where to go next, Reporting issues).
    - Both files >50 lines.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Publish-gate verification (cargo package --list + cargo publish --dry-run) + final quality gate</name>
  <read_first>
    - `crates/pmcp-server-toolkit/Cargo.toml` (Plan 01 — `exclude = [...]` setting + all metadata)
    - `CLAUDE.md` §"Release & Publish Workflow" — pre-flight checklist
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-RESEARCH.md` §"Pitfall 6" (10MB publish limit)
    - `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-PATTERNS.md` §1 (Cargo.toml `exclude` whitelist)
  </read_first>
  <action>
Verify the toolkit is ready to publish to crates.io (TKIT-01 publishability anchor):

**Step A — `cargo package --list` audit.**

Run:
```
cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk
cargo package --list -p pmcp-server-toolkit > /tmp/p83-pkg-list.txt 2>&1
```

Inspect `/tmp/p83-pkg-list.txt`. Expected files (Pitfall 6 + PATTERNS §1):
- `Cargo.toml`
- `Cargo.toml.orig`
- `src/lib.rs` + every `src/*.rs` module file
- `src/sql/mod.rs`
- `README.md`
- `LICENSE` / `LICENSE-MIT` / `LICENSE-APACHE` (if present in repo root)
- `examples/e01_toolkit_minimal.rs`

**NOT** expected (must NOT appear):
- `.planning/` anything
- `.pmat/` anything
- `tests/` anything
- `tests/fixtures/` anything
- `fuzz/` anything

Use grep to confirm absence:
```
! grep -E "^(\.planning|\.pmat|tests|fuzz)/" /tmp/p83-pkg-list.txt
```

If forbidden paths leak through, update `exclude = [...]` in `crates/pmcp-server-toolkit/Cargo.toml`. CLAUDE.md §"Release & Publish Workflow" warns this is the recurring trap.

**Step B — `cargo publish --dry-run`.**

Run:
```
cargo publish --dry-run -p pmcp-server-toolkit --allow-dirty 2>&1 | tee /tmp/p83-publish-dry-run.log
```

Expected: succeeds. The `--allow-dirty` flag is OK during planning (uncommitted changes from prior plans); the real publish in the milestone release will require a clean tree.

Common failures + fixes:
- "missing license field" — add `license = "MIT OR Apache-2.0"` to Cargo.toml `[package]`.
- "missing description" — add a one-line `description = "..."` to `[package]`.
- "missing repository URL" — add `repository = "https://github.com/paiml/rust-mcp-sdk"` to `[package]`.
- "path dependency cannot be published" — the `path = "../.."` workspace-version trick (D-05) requires the `version = "..."` field on each path-dep so cargo emits the version on publish; verify the Plan 01 Cargo.toml has BOTH `version` and `path` on every dep that points inside the workspace.
- "tarball too large" — Step A's `exclude` audit should have caught this; revisit.

**Step C — workspace-wide quality gate.**

```
make quality-gate 2>&1 | tee /tmp/p83-final-quality-gate.log
```

This is the END-of-phase confidence check. Expected: ALL of fmt, clippy pedantic+nursery, build (workspace), test (workspace), audit pass.

**Step D — workspace-wide cross-feature build matrix.**

```
cargo build --workspace
cargo build --workspace --all-features
cargo test --workspace --no-fail-fast 2>&1 | tee /tmp/p83-workspace-test.log | tail -50
```

Expected: every test in the workspace passes. The toolkit's tests run alongside `pmcp`'s, `pmcp-code-mode`'s, etc. No regression in other crates.

**Step E — Phase-83 VALIDATION.md sign-off.**

Edit `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-VALIDATION.md`:
- Set `nyquist_compliant: true` in the frontmatter.
- Mark every row in the per-task verification matrix as `✅` if the verifying test from Plans 01-09 passed.
- Update the `Approval:` line to `Approval: pending operator review` (final operator sign-off happens at `/gsd-verify-work`).
  </action>
  <verify>
    <automated>cargo package --list -p pmcp-server-toolkit > /tmp/p83-pkg-list.txt 2>&1 && ! grep -qE "^(\.planning|\.pmat|tests|fuzz)/" /tmp/p83-pkg-list.txt && cargo publish --dry-run -p pmcp-server-toolkit --allow-dirty 2>&1 | tee /tmp/p83-publish.log | grep -qE "(Packaged|Verifying|Compiling)" && make quality-gate 2>&1 | tail -50 | grep -E "(Quality Gate.*PASSED|all checks passed)" && cargo build --workspace --quiet && grep -q "nyquist_compliant: true" .planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-VALIDATION.md && echo "ok"</automated>
  </verify>
  <done>
    - `cargo package --list -p pmcp-server-toolkit` outputs ONLY allowed files (no `.planning`, `.pmat`, `tests`, `fuzz` leakage).
    - `cargo publish --dry-run -p pmcp-server-toolkit --allow-dirty` succeeds.
    - `make quality-gate` passes workspace-wide.
    - `cargo build --workspace` and `cargo test --workspace` succeed.
    - `83-VALIDATION.md` frontmatter has `nyquist_compliant: true`; matrix rows updated to reflect test pass/fail status.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted-toml→fuzz-target | Fuzz target feeds arbitrary bytes to the parser; any panic is a defect (DoS vector against deployed servers consuming untrusted config snippets, though config is typically operator-trusted) |
| crates.io-publish→public | Once published, every byte of the toolkit's tarball is auditable by every downstream consumer; secrets/PII/fixtures leaking through the `exclude` filter is information disclosure |
| contracts.yaml→audit | The contract entries claim what the toolkit guarantees; out-of-sync entries become provable false claims (Repudiation if audited) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-09-01 | Denial of Service | ServerConfig::from_toml panic on adversarial TOML | mitigate | libFuzzer target ships in Task 1; CI nightly runs it. Matches Phase 77 disposition (source lands, nightly exercises) |
| T-83-09-02 | Information Disclosure | Crates.io tarball ships `.planning/` / fixtures / .pmat | mitigate | Task 4 Step A audits `cargo package --list`; fails if forbidden paths appear. `exclude = [...]` whitelist set in Plan 01 (Pitfall 6) |
| T-83-09-03 | Repudiation | contracts/binding.yaml entries diverge from actual public API | mitigate | `pmat comply check` exits 0 (Task 2 verify). Every TKIT-XX requirement maps to a row; one-row-per-requirement discipline keeps drift visible at code-review time |
| T-83-09-04 | Tampering | Operator applies the shim diff and breaks pmcp-run backend cores | accept | Plan 08's in-toolkit smoke test (TKIT-08 anchor) proves the import surface; the shim diff is the cross-repo apply. Rollback section in shim-pmcp-run-shared.md documents recovery |
| T-83-09-05 | Spoofing | Migration guide misleads developers about feature parity | mitigate | "Dropped from mcp-server-common" section explicitly enumerates the D-14-dropped features so developers don't expect them through the toolkit |
| T-83-09-06 | Elevation of Privilege | `cargo publish --dry-run` succeeds but a transient dep change later breaks real publish | accept | Pre-flight checklist in CLAUDE.md § Release covers this; release time uses fresh `cargo update` + `--no-verify` is forbidden. P83 dry-run is the confidence check, not the only gate |
</threat_model>

<verification>
1. `crates/pmcp-server-toolkit/fuzz/Cargo.toml` and the fuzz target file exist.
2. `cargo +nightly check --bin pmcp_server_toolkit_config_parser` (from the fuzz dir) succeeds; or stable check succeeds; or documented absence (matches Phase 77 disposition).
3. `cargo build --workspace` succeeds (fuzz crate's isolated `[workspace]` keeps it out of the main workspace).
4. `contracts/binding.yaml` extended with ≥10 toolkit rows; `pmat comply check` exits 0 (or documented absent).
5. `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/shim-pmcp-run-shared.md` exists with all 5 sections, ≥50 lines.
6. `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/pmcp-server-toolkit-migration.md` exists with all sections, ≥50 lines.
7. `cargo package --list -p pmcp-server-toolkit` shows NO `.planning/`, `.pmat/`, `tests/`, `fuzz/` entries.
8. `cargo publish --dry-run -p pmcp-server-toolkit --allow-dirty` succeeds.
9. `make quality-gate` passes workspace-wide.
10. `cargo test --workspace --no-fail-fast` passes (no regression in other crates).
11. `83-VALIDATION.md` has `nyquist_compliant: true` in frontmatter.
</verification>

<success_criteria>
- TEST-02 ALWAYS-fuzz: fuzz target ships and is wired into the toolkit's `fuzz/` workspace.
- TKIT-01 publishability: `cargo publish --dry-run` succeeds; tarball excludes `.planning/`, `.pmat/`, `tests/`, `fuzz/`.
- TKIT-08 (cross-repo handoff): operator-facing shim diff + developer-facing migration guide both shipped.
- Contract-First Development (CLAUDE.md): `contracts/binding.yaml` extended with one row per TKIT-XX public symbol.
- Workspace-wide green: `make quality-gate` + `cargo test --workspace` pass.
- 83-VALIDATION.md sign-off: `nyquist_compliant: true` set.
</success_criteria>

<output>
After completion, create `.planning/phases/83-toolkit-core-lift-pmcp-server-toolkit/83-09-SUMMARY.md` per the standard template. Include:
- `cargo package --list` output (or count of files in the published tarball).
- `cargo publish --dry-run` exit code + tarball size in KB.
- Number of contract rows added.
- Confirmation that all 12 P83 requirements (TKIT-01..10 + TEST-02 + TEST-03) are now covered by at least one passing test or artifact.
- Cross-repo handoff: link to the shim diff artifact + the migration guide; recommendation to operator on when to submit the pmcp-run PR (only after `pmcp-server-toolkit 0.1.0` actually ships to crates.io).
</output>
