---
phase: 114-tasks-extension-migration
plan: 17
type: execute
wave: 10
depends_on: [114-14, 114-06]
files_modified:
  - examples/s50_v2_tasks_server.rs
  - examples/s51_v2_tasks_agent.rs
  - src/client/mod.rs
autonomous: true
requirements: [TASK-01, TASK-02, TASK-04]
must_haves:
  truths:
    - "A developer can run two commands and watch a v2 task go create -> input_required -> update -> terminal"
    - "The client half is an autonomous poll loop, not an interactive chat client"
    - "The examples FAIL (non-zero exit) when the surface does not behave as documented"
    - "The client can supply task inputs, so wait_for_task's input_required arm is no longer a dead end"
  artifacts:
    - path: "examples/s50_v2_tasks_server.rs"
      provides: "The v2 tasks server half (D-05), in-crate InMemoryTaskStore"
      min_lines: 200
    - path: "examples/s51_v2_tasks_agent.rs"
      provides: "The agent-shaped autonomous poll loop that feeds input via tasks/update"
      min_lines: 200
    - path: "src/client/mod.rs"
      provides: "Client tasks_update() and an input-supplying poll helper"
      contains: "tasks_update"
  key_links:
    - from: "examples/s51_v2_tasks_agent.rs"
      to: "src/client/mod.rs"
      via: "the example drives the PRODUCTION client methods, never a re-implemented loop"
      pattern: "tasks_update"
---

<objective>
Ship the paired runnable example that makes this phase's central claim checkable rather than asserted, and
give the client the `tasks_update` method the agent half needs (D-05, D-06).

Purpose: the owner steer for this phase is that one SDK serves both servers and agent clients, and that
aligning to v2 tasks is a small symmetric change on each side. That claim is only checkable if both halves
exist and run. CLAUDE.md's ALWAYS list also requires a runnable `cargo run --example` for every new
feature. The client half is **agent-shaped on purpose** — an autonomous poll loop, not an interactive chat
client — because that is the shape 113 D-07 made first-class and the one that matters for pmcp's
enterprise/Lambda focus.

Output: `examples/s50_v2_tasks_server.rs`, `examples/s51_v2_tasks_agent.rs`, and the client
`tasks_update()` method plus an input-supplying poll path.

**Numbering, measured (PATTERNS Fact 2):** `s49` is already taken TWICE (`s49_sampling_host.rs` and
`s49_v2_subscriptions_client.rs`; 52 `sNN_` examples total), so RESEARCH's proposed `s49_v2_tasks_server.rs`
would be a third collision. The next free pair is **`s50`/`s51`**.

**Pitfall 3, decided (DQ8):** the server half uses the **in-crate `InMemoryTaskStore`**, which has no
`allow_anonymous` check. `pmcp-tasks`' `GenericTaskStore` rejects both `ANONYMOUS_PRINCIPAL` (`""`) and
`"local"` while `allow_anonymous` defaults to `false`, so pairing a no-auth example server with a
pmcp-tasks backend would fail the first time anyone ran it. The choice is deliberate and the example says
so in a comment, pointing at 114-07's `anonymous_owner_is_refused_by_default_on_this_backend` test for the
production-backend story.

**Boundary with Phase 117 (D-06):** this client half is agent-*shaped*. Wiring it into the actual
`pmcp-agent` crate remains CLNT-03 / Phase 117, which inherits a de-risked surface rather than a blank one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-tasks-extension-migration/114-PATTERNS.md
@.planning/phases/114-tasks-extension-migration/114-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client tasks_update() and an input-supplying poll path</name>
  <read_first>
    - `src/client/mod.rs` lines 1223-1235 — `tasks_get`, the four-line method shape
      (`ensure_initialized` → `assert_capability` → typed request → `parse_task_payload`)
    - `src/client/mod.rs` lines 798-819 — `send_untyped_request` and `server_discover`'s use of it. Under
      PATTERNS Fact 1 `tasks_update` CANNOT build a `ClientRequest` variant, so it must go through
      `send_untyped_request` with the method string.
    - `src/client/mod.rs` lines 1323-1386 — `wait_for_task`: `web_time::Instant` (wasm-safe), the
      `poll_decision()` single-source classifier matched exhaustively with NO `_` arm, the `MIN_POLL_MS`
      floor, the remaining-budget clamp, and critically the `TaskPollDecision::InputRequired` arm which
      currently **errors out** with "wait_for_task cannot provide input — handle the elicitation, then
      resume polling". The agent example is exactly the consumer that should instead call `tasks/update`.
    - `src/types/mrtr.rs` — `InputResponses` and `InputResponse`, the types the caller supplies
  </read_first>
  <files>src/client/mod.rs</files>
  <action>
Add `tasks_update(&mut self, task_id: &str, responses: InputResponses) -> Result<()>` following
`tasks_get`'s shape but sending through `send_untyped_request` with the `tasks/update` method string (no
`ClientRequest` variant exists, by design — see 114-13). It must call the era-aware
`assert_capability("tasks", ..)` first, so an un-negotiated call fails fast with no bytes on the wire, and
it returns `()` because the ack is empty.

Then give the poll loop a way to supply input WITHOUT changing `wait_for_task`'s existing signature or
behaviour. Add a sibling — e.g. `wait_for_task_with_inputs(..)` taking a callback that receives the task's
`inputRequests` and returns `InputResponses` — that reuses `wait_for_task`'s machinery: the same
`poll_decision()` classifier matched exhaustively with no `_` arm, the same `MIN_POLL_MS` floor, the same
remaining-budget clamp, the same `web_time::Instant`. On `InputRequired` it invokes the callback and calls
`tasks_update`, then resumes polling; on every other decision it behaves identically to `wait_for_task`.

Leave `wait_for_task` itself byte-compatible. Its `InputRequired` arm's error is correct for a caller that
did not supply a responder, and changing it would alter behaviour for existing callers. Update its rustdoc
to point at the new sibling as the answer to the message it prints.

Add a bound on the callback loop: a maximum number of input rounds, so a server that keeps re-requesting
input cannot spin a client forever. Reuse the existing client-side round-limit convention rather than
minting a new constant if one already covers this (`DEFAULT_MRTR_ROUND_LIMIT` is the analogous client-side
bound); if a new constant is genuinely needed, document its relationship to the existing one and add a
`const _: () = assert!(..)` pinning that relationship.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'test(/tasks_update/) or test(/wait_for_task/)' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/client/mod.rs` contains `pub async fn tasks_update` sending via `send_untyped_request`
    - `tasks_update` calls `assert_capability` before sending: a unit test asserts an un-negotiated call performs ZERO transport sends
    - A sibling poll method exists that handles `InputRequired` by calling `tasks_update` and resuming
    - `wait_for_task`'s signature and behaviour are unchanged: `git diff src/client/mod.rs` shows no edit inside its body except rustdoc
    - The new poll path's decision match has NO `_` arm: `grep -A20 '<new fn name>' src/client/mod.rs | grep -c '_ =>'` is 0
    - The input-round loop is bounded, and the bound's relationship to the existing client round limit is documented or pinned by a `const _: () = assert!`
    - `cargo semver-checks check-release` — no update required (additive methods on an existing type)
    - `make wasm-build` green (the client compiles for wasm; `web_time::Instant` reused, not `std::time`)
  </acceptance_criteria>
  <done>The client can feed a task and poll it to a terminal state without the caller writing a loop.</done>
</task>

<task type="auto">
  <name>Task 2: examples/s50_v2_tasks_server.rs (server half)</name>
  <read_first>
    - `examples/s47_v2_stateless_mrtr.rs` lines 1-130 — the server-half shape to copy: a module rustdoc
      opening with BOTH `cargo run` commands, a `# What this demonstrates` bullet list, a
      deployment-contract section for any env var involved, named consts (`TOOL_NAME`, a
      `DEFAULT_ADDR = "127.0.0.1:8147"`-style binding), one `ToolHandler` whose `handle` is organized by
      labelled sections (`// ---- Round 2: resume from the VERIFIED continuation. ----`), and an
      `argv[1]`-or-default address binding
    - `src/server/task_store.rs` — the in-crate `InMemoryTaskStore` constructor and the
      `input_requests` / `input_responses` internal record fields (114-04)
    - `src/server/task_dispatch.rs` — the v2 create trigger (114-12), so the example tool is shaped to fire it
  </read_first>
  <files>examples/s50_v2_tasks_server.rs</files>
  <action>
Write the server half: a v2-opt-in streamable-HTTP server with an in-crate `InMemoryTaskStore` and one
task-capable tool whose handler creates a task, records one `inputRequests` entry, and completes once the
input arrives.

Follow `s47`'s structure exactly: module rustdoc opening with BOTH `cargo run` commands, a
`# What this demonstrates` list (extension negotiation on `server/discover`; the server-directed create
trigger; `tasks/get` inlining; `tasks/update`; the retirement of `tasks/list` and `tasks/result` on v2),
named consts, a labelled-sections handler, and an `argv[1]`-or-default listen address on a port not used by
another example.

Two things the example must state in comments rather than leave implicit:
  - **Why the in-crate store:** `pmcp-tasks`' `GenericTaskStore` refuses the anonymous owner while
    `allow_anonymous` is `false` (its default), so a no-auth example paired with that backend would fail on
    first run. Point at 114-07's `anonymous_owner_is_refused_by_default_on_this_backend` test for the
    production-backend story.
  - **The shared-bucket caveat itself:** this server has no auth provider, so every v2 caller shares one
    owner bucket. State it plainly — D-07 requires the caveat be stated, not implied — and state what a real
    deployment does instead (configure OAuth so the owner is the `sub`).

Add a rustdoc section listing what a reader should SEE on stdout for a successful run, so a divergence is
noticeable without reading the code.
  </action>
  <verify>
    <automated>cargo build --features full --example s50_v2_tasks_server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --features full --example s50_v2_tasks_server` exits 0 with no warnings
    - The module rustdoc contains BOTH `cargo run --example s50_v2_tasks_server` and `cargo run --example s51_v2_tasks_agent` commands
    - The rustdoc contains a `# What this demonstrates` list naming extension negotiation, the create trigger, `tasks/get` inlining, `tasks/update` and the v2 retirements
    - The file contains a comment naming `allow_anonymous` and why the in-crate store was chosen
    - The file contains a comment stating the shared-bucket caveat and what a real deployment does instead
    - The listen address is `argv[1]`-or-default and the default port collides with no other example: `grep -rho '127\.0\.0\.1:[0-9]*' examples/ | sort | uniq -d` does not list this example's port
    - `make lint` exits 0 (examples are linted)
  </acceptance_criteria>
  <done>A single command starts a v2 tasks server whose behaviour and caveats are documented at the top of the file.</done>
</task>

<task type="auto">
  <name>Task 3: examples/s51_v2_tasks_agent.rs (agent-shaped client half)</name>
  <read_first>
    - `examples/s48_v2_mrtr_client.rs` (whole file, 236 lines) — the agent-half shape: three `demo_*` async
      fns called from `main`, each printing a numbered banner and **returning `Err` when the demonstration
      did not behave as documented** (the example is an executable assertion, not a printout); the
      `v2_client(url, handler)` constructor using `StreamableHttpTransportConfigBuilder` +
      `ClientBuilder::with_protocol_version(PROTOCOL_VERSION_2026_07_28)`; and its closing rustdoc line
      that the handler "answers PROGRAMMATICALLY rather than reading stdin, so the example stays scriptable
      — and it is the shape an autonomous agent uses"
    - `src/client/mod.rs` — the `tasks_update` and input-supplying poll methods added in Task 1, and
      `server_discover`'s rustdoc requirement that a v2 client call it EXPLICITLY (it stores the
      capabilities so `assert_capability` can then enforce)
    - `src/client/mod.rs` — the `with_tasks_extension()`-style builder knob added by 114-06
  </read_first>
  <files>examples/s51_v2_tasks_agent.rs</files>
  <action>
Write the client half as an **autonomous poll loop** — never an interactive one. No stdin reads anywhere:
every answer is produced programmatically, which is exactly what makes it the agent shape D-05 asks for and
what keeps the example scriptable in CI.

Structure it as `demo_*` async fns called from `main`, each printing a numbered banner and returning `Err`
when the surface misbehaves, so the process exits non-zero on a regression:

  1. `demo_negotiation` — build a v2 client with the tasks-extension declaration, call `server_discover`
     EXPLICITLY, and assert `extensions["io.modelcontextprotocol/tasks"]` is present. Returns `Err` if absent.
  2. `demo_autonomous_task_round_trip` — call the task-capable tool, receive `resultType:"task"` with a flat
     `taskId`, then drive the production poll helper from Task 1: poll `tasks/get`, on `input_required`
     produce the responses programmatically and call `tasks_update`, resume polling, and assert the task
     reaches a terminal status with an inlined `result`. This is the phase's headline claim, executed.
  3. `demo_undeclared_client_is_refused` — a client built WITHOUT the declaration; assert the tasks call
     fails fast locally (no round trip) or is refused by the server with the missing-capability code, and
     that no `taskId` is ever returned to it.
  4. `demo_retired_methods_are_gone_on_v2` — assert `tasks/list` and `tasks/result` answer `-32601`, so the
     example documents the retirement rather than leaving a reader to wonder.

Drive the PRODUCTION client methods throughout. Do not re-implement a poll loop, a backoff, or a decode in
the example — a re-implemented loop in an example is how the example and the SDK drift apart, and the
110-06 precedent (drive the production seam, not a re-implemented engine) is the house rule.

Close with the rustdoc line making the agent-shape claim explicit, in `s48`'s voice.
  </action>
  <verify>
    <automated>cargo build --features full --example s51_v2_tasks_agent 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --features full --example s51_v2_tasks_agent` exits 0 with no warnings
    - The file contains no stdin read: `grep -cE 'stdin|read_line' examples/s51_v2_tasks_agent.rs` is 0
    - Four `demo_` functions exist and each returns a `Result`; `main` propagates with `?` so a failure exits non-zero
    - The example calls `server_discover` explicitly before any tasks call
    - The example calls the production `tasks_update` and the production poll helper: `grep -c 'tasks_update' examples/s51_v2_tasks_agent.rs` ≥ 1, and no local poll loop over `tasks_get` with a sleep exists
    - END-TO-END PROOF recorded in the SUMMARY: `s50` started, `s51` run against it, both exit 0, with the captured stdout of `s51` pasted verbatim showing the create → input_required → update → terminal progression
    - Failure proof recorded in the SUMMARY: with the server's `tasks/update` route disabled, `s51` exits NON-ZERO (the example is an executable assertion, not a printout); reverted
  </acceptance_criteria>
  <done>Two commands demonstrate the whole v2 task lifecycle autonomously, and the demonstration fails loudly when the surface does not behave as documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example server → any local caller | The example has no auth provider, so it is a shared-bucket demo and must say so |
| task input channel → agent trust model | The spec is explicit that a task is NOT a higher-trust channel; a client-side obligation the agent half must not violate |
| example code → user expectations | An example that prints success regardless of behaviour teaches the wrong contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-88 | Information Disclosure | shared owner bucket in the example server | accept | No auth provider by design for a runnable demo; the caveat is stated in a comment (D-07) together with what a real deployment does instead; bound to localhost by default |
| T-114-89 | Spoofing | treating a task payload as higher-trust than an elicitation | mitigate | The spec's rule ("Hosts MUST apply the same trust model to these payloads as they would to standard elicitation/sampling requests. A task is not a higher-trust channel") is stated in the agent example's rustdoc, and the example's responder produces values programmatically without executing anything from the server |
| T-114-90 | Repudiation | an example that passes regardless of behaviour | mitigate | Every `demo_*` returns `Err` on a divergence and `main` propagates; a recorded control shows the example exiting non-zero with the server route disabled |
| T-114-91 | Denial of Service | unbounded client-side input round loop | mitigate | The poll helper's input-round count is bounded (Task 1), with the bound related to the existing client round limit |
| T-114-92 | Tampering | example drifting from the SDK via a re-implemented loop | mitigate | The example drives production client methods only; acceptance criteria assert no local poll loop exists (the 110-06 precedent) |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `Cargo.toml`/`Cargo.lock` byte-unchanged (examples need no new dev-dependency) |
</threat_model>

<verification>
- `cargo build --features full --example s50_v2_tasks_server` and `--example s51_v2_tasks_agent` both exit 0
- End-to-end run recorded: `s50` up, `s51` exits 0, stdout pasted in the SUMMARY
- `cargo nextest run --features full -E 'test(/tasks_update/) or test(/wait_for_task/)'` exits 0
- `make lint` exits 0; `make wasm-build` green; `make test-examples` exits 0 if that target covers new examples
- `cargo semver-checks check-release` — no update required
</verification>

<success_criteria>
- The paired example runs end to end and demonstrates create → input_required → update → terminal autonomously
- The client half uses production methods only and exits non-zero on a divergence
- Both Pitfall-3 and shared-bucket caveats are stated in the example source, not left implicit
</success_criteria>

<output>
Create `.planning/phases/114-tasks-extension-migration/114-17-SUMMARY.md` when done
</output>
