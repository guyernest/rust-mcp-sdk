---
phase: 114-tasks-extension-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/task_store.rs
  - src/server/tasks.rs
autonomous: true
requirements: [TASK-02, TASK-06]
must_haves:
  truths:
    - "An existing out-of-tree TaskStore or TaskRouter implementation still compiles unchanged"
    - "Dispatch can ask a backend whether it accepts task inputs and get an honest answer"
    - "The in-crate InMemoryTaskStore can accept inputs, so the core path works with no external crate"
  artifacts:
    - path: "src/server/task_store.rs"
      provides: "Defaulted input-delivery method + supports_inputs() probe + InMemoryTaskStore impl + inputRequests/inputResponses on the internal record"
      contains: "supports_inputs"
    - path: "src/server/tasks.rs"
      provides: "Defaulted TaskRouter::handle_tasks_update returning a not-supported error"
      contains: "handle_tasks_update"
  key_links:
    - from: "src/server/task_store.rs"
      to: "src/server/task_dispatch.rs"
      via: "supports_inputs() consulted before the store path is served, mirroring supports_results()"
      pattern: "supports_inputs"
---

<objective>
Open the two additive trait seams `tasks/update` needs (D-12) and implement input delivery on the
in-crate `InMemoryTaskStore` (D-13 site 3), so the core path and the phase's paired example work
standalone with no `pmcp-tasks` dependency.

Purpose: `tasks/update` is the one genuinely-new v2 feature, and it must reach the backends
without a second registration knob and without breaking any existing implementor. The established
in-file precedents are exact: `set_result`/`get_result` + `supports_results()` on `TaskStore`, and
`create_workflow_task` defaulted-to-an-error on `TaskRouter`.

Output: one defaulted method + one probe on `TaskStore`, one defaulted method on `TaskRouter`, the
`InMemoryTaskStore` override, and the internal-record fields the kinds are read from.

**Why the record carries the kinds (D-17, security-bearing):** `tasks/update`'s `inputResponses`
entries must be decoded KIND-DIRECTED, and the kinds must come from what the *server itself*
recorded — never from anything the client sends. The persisted task record is the tasks analogue
of Phase 113's AEAD-sealed continuation. 113-27 closed **D-113-O**, where untagged "first shape
that fits" decoding mis-typed an elicitation answer as Sampling (`ElicitResult` and
`CreateMessageResult` structurally overlap) and a handler re-elicited 16 times before dying on a
misleading error. Storing `inputRequests` on the record is what makes the correct decoder
reachable in 114-14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-tasks-extension-migration/114-PATTERNS.md
@.planning/phases/114-tasks-extension-migration/114-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TaskStore input-delivery seam + supports_inputs() probe</name>
  <read_first>
    - `src/server/task_store.rs` lines 281-330 — `set_result`'s FULL shape: the "this is an
      **additive** trait method with a default implementation, so existing out-of-tree
      implementations keep compiling" rustdoc, the explicit-never-silent `TaskStoreError::Internal`
      default, the owner-scoping MUST, the `# Errors` section and the runnable doctest. Copy all
      five elements.
    - `src/server/task_store.rs` lines 381-390 — `supports_results()`, the probe shape:
      "Defaults to `false`. The dispatch layer consults this before serving the store-result path,
      so a store that cannot persist results falls through to the [`TaskRouter`] instead of
      silently dropping or serving empty results."
    - `src/server/task_store.rs` lines 397-409 — the internal `TaskRecord` and its rustdoc on why
      `result` lives on the INTERNAL record and never on the wire `Task` (so it is purged together
      with the task by `cleanup_expired`, with no separate unexpiring map)
    - `src/types/mrtr.rs` lines 514-532 — `InputRequests` / `InputResponses`
      (`BTreeMap<String, _>`, chosen "so the wire key order is deterministic for tests and digests,
      and duplicate keys are impossible") and `InputRequest::kind()`
  </read_first>
  <files>src/server/task_store.rs</files>
  <action>
Four changes in `src/server/task_store.rs`:

1. Add `async fn deliver_task_inputs(&self, task_id: &str, owner_id: &str, responses: crate::types::mrtr::InputResponses) -> Result<TaskInputDelivery, TaskStoreError>`
   as an **additive defaulted** trait method whose default returns
   `TaskStoreError::Internal { message: "store does not support task input delivery" }`. Copy
   `set_result`'s rustdoc structure verbatim in shape: the additive/out-of-tree sentence, the
   explicit-never-silent statement, the owner-scoping MUST (implementations MUST scope by
   `owner_id`, mirroring `get`/`cancel`, so one owner cannot feed another owner's task), a
   `# Errors` list, and a runnable doctest.

2. Add `pub struct TaskInputDelivery` (or an enum if that reads better — the executor decides, but
   it must express all three outcomes distinctly): the set of keys ACCEPTED, the set IGNORED
   (not currently outstanding: never issued, already answered, or superseded), and whether the
   outstanding set is now COMPLETE. The spec requires all three to be distinguishable: a server
   SHOULD ignore a non-outstanding key rather than error, and MAY accept a partial set — in which
   case the task **remains `input_required`**. Returning only `Ok(())` would make the
   partial-vs-complete distinction unrepresentable and force 114-14 to re-read the record.

3. Add `fn supports_inputs(&self) -> bool { false }` with `supports_results`'s doc shape,
   including the sentence about dispatch consulting it so a store that cannot accept inputs falls
   through rather than pretending.

4. Extend the internal `TaskRecord` with `input_requests: Option<crate::types::mrtr::InputRequests>`
   and `input_responses: Option<crate::types::mrtr::InputResponses>`, both defaulting to absent.
   Carry over the existing rustdoc argument verbatim in shape: these live on the INTERNAL record,
   never on the wire `Task`, so they are purged with the task by `cleanup_expired`. State
   explicitly that `input_requests` is the SERVER-recorded kinds source that 114-14's kind-directed
   decode reads, and that it MUST NOT be settable from client input.

`TaskRecord` is private, so (4) is not a public-API change. Items (1)-(3) are additive trait
surface. Do NOT change any existing method signature, do NOT make any new method required, and do
NOT touch `set_result`/`get_result`/`supports_results`.
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'test(/task_store/)' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/task_store.rs` contains `fn supports_inputs(&self) -> bool` with a `false` default body
    - `src/server/task_store.rs` contains `async fn deliver_task_inputs` with a defaulted body returning `TaskStoreError::Internal`
    - The `deliver_task_inputs` rustdoc contains the words `additive` and `owner_id`
    - `deliver_task_inputs` has a doctest that compiles: `cargo test --features full --doc task_store` exits 0
    - `TaskRecord` contains `input_requests` and `input_responses` fields
    - `cargo semver-checks check-release` reports no update required
    - A scratch out-of-tree-shaped `impl TaskStore` with only the pre-114 required methods still compiles (assert via a `#[cfg(test)]` minimal impl in the same file that does NOT implement the two new items)
  </acceptance_criteria>
  <done>Both new items are defaulted, the record can hold server-recorded kinds, and a pre-114 implementor compiles untouched.</done>
</task>

<task type="auto">
  <name>Task 2: InMemoryTaskStore input delivery (D-13 site 3)</name>
  <read_first>
    - `src/server/task_store.rs` lines 637-680 — `InMemoryTaskStore::get_result` +
      `supports_results() { true }`: the override shape, and critically `Self::validate_access`,
      which is what turns an owner mismatch into `NotFound` rather than a distinguishable error
    - `src/types/tasks.rs` line ~56 — `TaskStatus::can_transition_to` (the validated 5-state
      machine, already pinned by 46 transition tests in `crates/pmcp-tasks/tests/state_machine.rs`);
      the transition MUST be validated through this, never re-derived
    - `src/types/mrtr.rs` lines 816-832 — the five DoS bounds (`MAX_INPUT_RESPONSES` 64,
      `MAX_INPUT_RESPONSE_BYTES` 65_536, `MAX_INPUT_RESPONSES_TOTAL_BYTES` 262_144,
      `MAX_INPUT_RESPONSE_DEPTH` 32, `MAX_REQUEST_STATE_LEN` 8192) and
      `check_input_response_bounds` — do NOT mint new constants here
  </read_first>
  <files>src/server/task_store.rs</files>
  <action>
Implement `deliver_task_inputs` and `supports_inputs() -> true` for `InMemoryTaskStore`, under one
lock/entry guard so the whole operation is atomic with respect to concurrent callers (the in-crate
store uses `dashmap`; hold the entry, do not read-then-write across an await):

- Look up the record; `NotFound` if absent. Then `Self::validate_access(record, task_id, owner_id)`
  so a wrong owner surfaces as `NotFound` — never a distinguishable owner error. This is
  PROJECT.md's standing no-info-leak decision and must be routed through the existing helper, not
  re-expressed.
- Refuse unless the current status is `InputRequired`. A `Completed`, `Failed` or `Cancelled` task
  cannot be fed. Use `TaskStatus::can_transition_to` for the transition validity check rather than
  a hand-written match.
- Partition the delivered keys against `record.input_requests`: keys present and not already
  answered are ACCEPTED and merged into `record.input_responses`; every other key is IGNORED (not
  an error). Report both sets plus whether the outstanding set is now complete via
  `TaskInputDelivery`.
- Transition `InputRequired → Working` and bump `last_updated_at` **only when the delivered set
  COMPLETES the outstanding set**. A partial delivery persists the responses and the task STAYS
  `input_required` — this is the spec qualification of D-16 that research surfaced ("A server MAY
  accept a partial set; the task then remains `input_required` until the rest arrive"). The atomic
  unit is *(persist responses [+ transition iff now complete])*.
- Do not enforce byte/count/depth bounds here — those are enforced at ingress in 114-14, before
  any decode, using the five existing MRTR constants. Note that in the rustdoc so the division of
  responsibility is recorded at both ends.

Add unit tests in the file's existing test module, one per behaviour and named for it:
`deliver_inputs_completes_the_outstanding_set_and_transitions`,
`deliver_inputs_partial_set_persists_and_stays_input_required`,
`deliver_inputs_ignores_keys_that_are_not_outstanding`,
`deliver_inputs_ignores_a_key_already_answered`,
`deliver_inputs_on_a_completed_task_is_refused`,
`deliver_inputs_for_another_owner_is_not_found` (assert the error is `NotFound`, and assert the
rendered message does NOT contain the owner id or the string `owner`).
  </action>
  <verify>
    <automated>cargo nextest run --features full -E 'test(deliver_inputs)' 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Six named tests pass: `cargo nextest run --features full -E 'test(deliver_inputs)'` exits 0 with 6 tests
    - `InMemoryTaskStore` implements `supports_inputs()` returning `true`
    - `deliver_inputs_partial_set_persists_and_stays_input_required` asserts the status is still `input_required` AND that the persisted responses contain the delivered key
    - `deliver_inputs_for_another_owner_is_not_found` asserts the error renders WITHOUT the substring `owner` and without the other owner's id
    - The implementation calls `Self::validate_access` and `can_transition_to` (grep both in the impl body)
    - No new numeric constant is introduced: `git diff src/server/task_store.rs | grep -E '^\+.*const [A-Z_]+: (usize|u64|u32) = [0-9]'` matches nothing
  </acceptance_criteria>
  <done>The core path can accept task inputs, transitions only on a complete set, and leaks nothing about another owner's task.</done>
</task>

<task type="auto">
  <name>Task 3: TaskRouter::handle_tasks_update defaulted seam</name>
  <read_first>
    - `src/server/tasks.rs` lines 88-115 — `create_workflow_task`, the exact defaulted-to-an-error
      shape D-12 names, and lines 20-22 + 45-60 where the module doc explains that every param and
      return is `serde_json::Value` **by design** (the seam D-11 keeps the reshape above) and the
      five sibling methods' `(params, owner_id)` argument order
  </read_first>
  <files>src/server/tasks.rs</files>
  <action>
Add `async fn handle_tasks_update(&self, _params: Value, _owner_id: &str) -> Result<Value>` to the
`TaskRouter` trait, defaulted to `Err(crate::error::Error::internal("tasks/update not supported by this router"))`
— the `create_workflow_task` shape line for line.

Match the five siblings' argument order and `Value`-in/`Value`-out discipline exactly: this method
sits BELOW the `serde_json::Value` seam, so it receives already-validated params as a `Value` and
returns a `Value`. Do not introduce a typed parameter here; that would move the reshape below the
seam and break D-11.

Rustdoc must state: (a) it is additive and defaulted so every existing router keeps compiling;
(b) the default is an explicit error, never a silent success, so dispatch can answer honestly when
a router cannot accept inputs; (c) the caller (`TaskDispatch`) has ALREADY resolved the owner
through the v2 identity table and bounds-checked and kind-decoded the responses — this method must
not re-derive the owner from `params` (that would be the IDOR the owner-prefixed key design
prevents).
  </action>
  <verify>
    <automated>cargo build --features full 2>&1 | tail -5 && cargo nextest run --features full -E 'test(/tasks/)' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/tasks.rs` contains `async fn handle_tasks_update` with a defaulted error body
    - The defaulted body returns an error mentioning `tasks/update`
    - The rustdoc contains the words `additive` and states the owner must not be re-derived from `params`
    - `cargo build --features full` exits 0 with no new warnings
    - `cargo semver-checks check-release` reports no update required
    - `crates/pmcp-tasks` still compiles unchanged: `cargo check -p pmcp-tasks` exits 0 with a zero-byte diff to `crates/pmcp-tasks/`
  </acceptance_criteria>
  <done>A router can opt into tasks/update; one that does not still compiles and reports honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-supplied `inputResponses` → persisted task record | Untrusted values are persisted; only the KEYS the server already recorded may be accepted |
| caller A's owner id → caller B's task record | The store is the enforcement point for owner isolation |
| out-of-tree implementor → trait contract | A defaulted method must fail explicitly, never succeed silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-10 | Information Disclosure | `InMemoryTaskStore::deliver_task_inputs` owner check | mitigate | Routed through the existing `Self::validate_access`, so a wrong owner yields `NotFound`; test asserts the rendered message contains neither `owner` nor the other owner's id |
| T-114-11 | Tampering | client choosing which input kind its answer is decoded as | mitigate | `input_requests` on the internal record is written only by the server and is documented MUST-NOT-be-client-settable; it is the kinds source 114-14 decodes against (D-113-O class) |
| T-114-12 | Tampering | replay of an already-answered `inputRequests` key | mitigate | Already-answered keys are IGNORED, not re-accepted (`deliver_inputs_ignores_a_key_already_answered`); the spec requires keys be unique over a task's lifetime and never reused after a response is delivered |
| T-114-13 | Tampering | lost update on concurrent delivery | mitigate | The in-crate store performs the read/partition/write under one entry guard with no intervening await; the CAS equivalent for the production backends is 114-07's `put_if_version` path |
| T-114-14 | Elevation of Privilege | feeding a terminal task | mitigate | Refused unless the current status is `InputRequired`, validated via `TaskStatus::can_transition_to` (46 existing transition tests) |
| T-114-15 | Denial of Service | oversized `inputResponses` | transfer | Bounds are enforced at ingress in 114-14 using the five existing MRTR constants; this plan mints no new limits and records the division of responsibility in both rustdocs |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `Cargo.toml`/`Cargo.lock` byte-unchanged |
</threat_model>

<verification>
- `cargo nextest run --features full -E 'test(/task_store/) or test(deliver_inputs)'` exits 0
- `cargo test --features full --doc task_store` exits 0 (the new doctest runs)
- `cargo semver-checks check-release` — no update required (all additions defaulted / private)
- `cargo check -p pmcp-tasks` exits 0 with `git diff --stat -- crates/pmcp-tasks/` empty
- `make lint` exits 0; `make wasm-build` green with no new warning classes
</verification>

<success_criteria>
- Two defaulted trait methods and one probe, all additive, semver green
- In-crate store delivers inputs atomically, transitions only on a complete set, and never leaks owner information
- A pre-114 out-of-tree implementor compiles with zero changes
</success_criteria>

<output>
Create `.planning/phases/114-tasks-extension-migration/114-04-SUMMARY.md` when done
</output>
