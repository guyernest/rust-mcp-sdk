---
phase: 95-shape-a-binary-pmcp-workbook-server
plan: 02
type: execute
wave: 2
depends_on: ["95-01"]
files_modified:
  - crates/pmcp-workbook-server/Cargo.toml
  - crates/pmcp-workbook-server/tests/assemble.rs
  - crates/pmcp-workbook-server/tests/http_smoke.rs
  - crates/pmcp-workbook-server/tests/parity_workbook.rs
  - crates/pmcp-workbook-server/tests/bundle_id_props.rs
  - Makefile
  - CLAUDE.md
autonomous: true
requirements: [WBCL-06]

must_haves:
  truths:
    - "An integration test asserts a server built from the golden bundle exposes all five workbook tools + the workbook:// resource"
    - "An ephemeral-port HTTP smoke test drives an MCP initialize over StreamableHttpTransport and asserts the response, then aborts the handle"
    - "A parity test invokes the real run_serving binary path against the golden bundle and drives tools via mcp-tester (dev-dependency only, not a published dep)"
    - "Property/fuzz-style coverage exercises the --bundle-id assertion (matching id succeeds, mismatched id fails closed)"
    - "make purity-check confirms the pmcp-workbook-server served tree is reader-free (no umya/quick-xml/zip-via-reader/swc/pmcp-code-mode)"
    - "CLAUDE.md Release & Publish Workflow lists pmcp-workbook-server at slot 9a"
  artifacts:
    - path: "crates/pmcp-workbook-server/tests/assemble.rs"
      provides: "server-surface assertions (five tools + workbook:// resource) from the golden bundle"
    - path: "crates/pmcp-workbook-server/tests/http_smoke.rs"
      provides: "ephemeral-port HTTP initialize smoke test driving the live server"
    - path: "crates/pmcp-workbook-server/tests/parity_workbook.rs"
      provides: "mcp-tester ephemeral-port parity replay through the real run_serving path"
    - path: "crates/pmcp-workbook-server/tests/bundle_id_props.rs"
      provides: "property/fuzz coverage of the --bundle-id fail-closed assertion"
    - path: "Makefile"
      provides: "purity-check assertion that the pmcp-workbook-server tree stays reader-free"
      contains: "pmcp-workbook-server"
    - path: "CLAUDE.md"
      provides: "slot 9a publish-order entry"
      contains: "pmcp-workbook-server"
  key_links:
    - from: "crates/pmcp-workbook-server/tests/parity_workbook.rs"
      to: "pmcp_workbook_server::run_serving"
      via: "real binary path against the golden bundle, ephemeral port, abort()"
      pattern: "run_serving"
    - from: "Makefile purity-check"
      to: "pmcp-workbook-server served cone"
      via: "cargo tree negative assertion (reader/JS absent), fail-closed"
      pattern: "pmcp-workbook-server"
    - from: "crates/pmcp-workbook-server/tests/http_smoke.rs"
      to: "pmcp_workbook_server::serve"
      via: "bind 127.0.0.1:0, capture real addr, MCP initialize, abort"
      pattern: "serve"
---

<objective>
Add the full test trio mirroring `pmcp-sql-server`'s tests (server-surface assemble,
ephemeral-port HTTP smoke, mcp-tester parity through the real binary path), the
ALWAYS property/fuzz coverage of the `--bundle-id` fail-closed assertion, wire the
new served cone into the `make purity-check` reader-free gate (success criterion 3),
and add the crate at slot 9a of CLAUDE.md's publish order.

Purpose: Prove WBCL-06's three success criteria observably (live server / boot-gate
→ RunError → non-zero exit / purity gate confirms no reader in tree) and satisfy the
CLAUDE.md ALWAYS testing matrix (fuzz + property + unit + example).
Output: A passing test trio + property coverage, a green purity gate covering the
binary, and the publish-order wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/95-shape-a-binary-pmcp-workbook-server/95-CONTEXT.md
@.planning/phases/95-shape-a-binary-pmcp-workbook-server/95-PATTERNS.md
@.planning/phases/95-shape-a-binary-pmcp-workbook-server/95-01-SUMMARY.md

<interfaces>
<!-- The binary surface (from Plan 95-01) the tests drive: -->
```rust
// crates/pmcp-workbook-server/src/lib.rs
pub struct Args { pub bundle_dir: PathBuf, pub bundle_id: Option<String>, pub http: String }
pub async fn serve(server: Server, addr: SocketAddr) -> Result<(SocketAddr, JoinHandle<()>), RunError>;
pub async fn run_serving(args: &Args) -> Result<(SocketAddr, JoinHandle<()>), RunError>;
pub fn build_server(args: &Args) -> Result<Server, RunError>; // re-exported from assemble
pub enum RunError { Io, Bundle, BundleIdMismatch{expected,actual}, Addr, Serve, Serving }
```

<!-- mcp-tester harness (dev-dependency ONLY, not published) — parity_chinook.rs:61 -->
```rust
use mcp_tester::{ScenarioExecutor, ServerTester, TestScenario};
// ServerTester::new(&url, 30s, false, None, Some("http"), None)
```

<!-- StreamableHttpTransportConfig (http_lazy_startup.rs:105-113) transfers verbatim -->

Golden fixture (read-only, committed): `crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/`
  bundle_id = "tax-calc", version = "1.1.0". Resolve via CARGO_MANIFEST_DIR:
  `Path::new(env!("CARGO_MANIFEST_DIR")).join("../pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test trio — assemble + http_smoke + parity; dev-deps</name>
  <files>crates/pmcp-workbook-server/Cargo.toml, crates/pmcp-workbook-server/tests/assemble.rs, crates/pmcp-workbook-server/tests/http_smoke.rs, crates/pmcp-workbook-server/tests/parity_workbook.rs</files>
  <read_first>
    - crates/pmcp-sql-server/tests/assemble.rs (server-surface assertion shape + chinook_db_path fixture-path helper)
    - crates/pmcp-sql-server/tests/http_lazy_startup.rs (ephemeral-port bind, real-addr capture, MCP initialize over StreamableHttpTransport, handle.abort())
    - crates/pmcp-sql-server/tests/parity_chinook.rs (the five-step mcp-tester replay through the real run_serving path)
    - crates/pmcp-sql-server/Cargo.toml (the [dev-dependencies] block to trim: mcp-tester + tempfile + serde_json + url)
    - crates/pmcp-workbook-server/src/lib.rs (the serve/run_serving/build_server surface from Plan 95-01)
  </read_first>
  <action>
    Add `[dev-dependencies]` to `crates/pmcp-workbook-server/Cargo.toml` mirroring sql-server's
    parity-harness shape, trimmed: `mcp-tester` (path "../mcp-tester", parity harness ONLY — NOT a
    published dep), `tempfile = "3"`, `serde_json = "1"`, `url = "2.5"` (StreamableHttpTransportConfig.url
    is a url::Url), and `proptest = "1"` (for Task 2). Drop rusqlite/serde_yaml (no SQL/yaml needed).
    Create `tests/assemble.rs`: a `golden_bundle_dir()` helper resolving
    `../pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0` from CARGO_MANIFEST_DIR; build the server via
    `build_server` and assert `server.get_tool("calculate"/"explain"/"get_manifest"/"diff_version"/"render_workbook").is_some()`
    and the `workbook://` resource is registered.
    Create `tests/http_smoke.rs`: copy the http_lazy_startup shape — build via `build_server`, `serve`
    to `127.0.0.1:0`, capture the REAL bound addr, drive an MCP `initialize` over the SDK
    `StreamableHttpTransport` (config struct transfers verbatim), assert the response id echoes,
    `handle.abort()`.
    Create `tests/parity_workbook.rs`: mirror parity_chinook's five steps — point at the committed golden
    bundle dir (no temp copy; read-only), construct `Args { bundle_dir, bundle_id: None, http: "127.0.0.1:0".into() }`,
    invoke the REAL `pmcp_workbook_server::run_serving(&args).await`, poll `ServerTester::test_initialize()`
    with backoff, construct `ServerTester::new(&url, 30s, false, None, Some("http"), None)`, drive
    `calculate`/`explain`/`get_manifest` (direct tool calls or a committed scenario) and assert success;
    `handle.abort()`. Use ONLY the synthetic golden — zero customer material.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-server --test assemble --test http_smoke --test parity_workbook -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    All three integration tests pass single-threaded; the live server answers initialize and the five tools over HTTP.
  </done>
  <acceptance_criteria>
    - tests/assemble.rs asserts all five workbook tools + workbook:// resource on a server built from the golden bundle
    - tests/http_smoke.rs binds 127.0.0.1:0, drives MCP initialize, asserts the response, aborts the handle
    - tests/parity_workbook.rs invokes pmcp_workbook_server::run_serving (the real binary path) and drives tools via mcp-tester
    - grep confirms mcp-tester appears only under [dev-dependencies] in Cargo.toml (not [dependencies])
    - cargo test -p pmcp-workbook-server --test assemble --test http_smoke --test parity_workbook -- --test-threads=1 exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Property/fuzz coverage of the --bundle-id fail-closed assertion</name>
  <files>crates/pmcp-workbook-server/tests/bundle_id_props.rs</files>
  <behavior>
    - Property: for any arbitrary bundle_id string != "tax-calc", build_server(Args{ bundle_id: Some(that), .. }) over the golden returns Err(RunError::BundleIdMismatch) and NEVER a server (fail-closed invariant)
    - Property: bundle_id == "tax-calc" (the golden's loaded id) succeeds
    - Property: bundle_id == None always succeeds (assertion is a guard, not a resolution input)
    - Edge cases covered by proptest generation: empty string, whitespace, unicode, very long strings — none of these panic; all non-matching ids fail closed
  </behavior>
  <read_first>
    - crates/pmcp-workbook-server/src/assemble.rs (the build_server + BundleIdMismatch logic under test, from Plan 95-01)
    - crates/pmcp-sql-server/tests/dispatch.rs (the proptest style + proptest-regressions convention to mirror)
  </read_first>
  <action>
    Create `tests/bundle_id_props.rs` using `proptest`. Resolve the golden bundle dir from CARGO_MANIFEST_DIR.
    Write a proptest that generates arbitrary `String` bundle ids; for ids != "tax-calc" assert
    `build_server` returns `Err` matching `RunError::BundleIdMismatch` (fail-closed, no server constructed);
    add explicit cases for `Some("tax-calc")` → Ok and `None` → Ok. This is the ALWAYS fuzz/property
    coverage required by CLAUDE.md for the one piece of genuinely-new logic (the D-01 assertion). The
    boot-integrity fail-closed behavior itself is already covered by the toolkit/runtime fuzz suite in
    Phase 91/92 (the binary adds no served logic), so this property test targets the binary-owned seam.
    Commit any generated `bundle_id_props.proptest-regressions` file (mirrors sql-server's
    dispatch.proptest-regressions convention).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-server --test bundle_id_props 2>&1 | tail -12</automated>
  </verify>
  <done>
    The property test passes; arbitrary non-matching bundle ids fail closed, matching id and None succeed, no panics.
  </done>
  <acceptance_criteria>
    - tests/bundle_id_props.rs uses proptest to generate arbitrary bundle_id strings
    - the property asserts every non-"tax-calc" id yields RunError::BundleIdMismatch with no server constructed
    - explicit cases assert Some("tax-calc") and None both succeed
    - cargo test -p pmcp-workbook-server --test bundle_id_props exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Purity-gate assertion for the served cone + CLAUDE.md slot-9a wiring</name>
  <files>Makefile, CLAUDE.md</files>
  <read_first>
    - Makefile (lines ~460-614 — the existing purity-check target; the Phase 92 `pmcp-server-toolkit --features workbook` reader-absence block is the closest analog for a new binary-tree assertion)
    - CLAUDE.md (## Release & Publish Workflow — the numbered crate publish-order list; slot 9 = pmcp-sql-server, slot 9a is the new attach point after items 5-8)
  </read_first>
  <action>
    Add a NEW fail-closed assertion to the `purity-check` target in Makefile (after the Phase 92 block,
    before the final PASSED echo) under a `purity-check: Phase 95 — pmcp-workbook-server served cone
    reader-absence` heading. Mirror the Phase 92 block exactly: `set -euo pipefail`,
    `BAN='umya|calamine|quick-xml|swc_|pmcp-code-mode'`, run `cargo tree -p pmcp-workbook-server 2>&1`
    capturing status with `|| status=$?` (NEVER `2>/dev/null` — fail closed on any non-zero cargo
    status, per the WR-01 note in the file), grep the tree for the BAN set and FAIL if any reader/JS dep
    appears. This proves the served cone (binary → toolkit[workbook] → pmcp-workbook-runtime → pmcp)
    stays reader-free (success criterion 3). Do NOT add an `umya` positive assertion (the binary is NOT
    a writer crate). The CI purity job already runs `make purity-check` verbatim, so no workflow edit is
    needed — this assertion is picked up automatically.
    Edit CLAUDE.md's ## Release & Publish Workflow numbered list to insert `pmcp-workbook-server` at
    slot 9a — AFTER `pmcp-server-toolkit` (item 5) and its transitive `pmcp-workbook-runtime` dep, sibling
    to `pmcp-sql-server` (item 9). Note its deps (pmcp-server-toolkit[workbook,http] + pmcp) and that it
    must publish after the toolkit + runtime; it has no inter-dependency with the SQL connector crates.
  </action>
  <verify>
    <automated>make purity-check 2>&1 | tail -8; grep -n "pmcp-workbook-server" Makefile CLAUDE.md</automated>
  </verify>
  <done>
    make purity-check passes and explicitly covers pmcp-workbook-server; CLAUDE.md lists it at slot 9a.
  </done>
  <acceptance_criteria>
    - Makefile purity-check target contains a `pmcp-workbook-server` reader-absence assertion that fails closed on non-zero cargo status (no 2>/dev/null swallow)
    - make purity-check exits 0 and its output mentions pmcp-workbook-server reader-free
    - CLAUDE.md ## Release & Publish Workflow lists pmcp-workbook-server at slot 9a after pmcp-server-toolkit/runtime
    - grep "pmcp-workbook-server" matches in both Makefile and CLAUDE.md
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle dir → served tools | tests assert the fail-closed gate + the --bundle-id guard behave as specified (negative coverage) |
| served cone → reader libs | the purity gate is the boundary control proving no Excel reader / JS stack can enter the published binary's dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-06 | Tampering | reader/JS dep leaking into the served binary tree | mitigate | `make purity-check` adds a fail-closed `cargo tree -p pmcp-workbook-server` negative assertion (umya/calamine/quick-xml/swc_/pmcp-code-mode absent); CI runs the same target, blocking merge on a breach (success criterion 3). |
| T-95-07 | Spoofing | regression in the --bundle-id fail-closed guard | mitigate | the proptest in Task 2 asserts every non-matching bundle id fails closed (no server constructed), guarding against a future refactor that weakens the D-01 assertion. |
| T-95-08 | Denial of Service | a crashed/never-ready listener in CI hanging the parity test | mitigate | parity/http tests bind ephemeral `127.0.0.1:0`, poll readiness with bounded backoff, and `handle.abort()` deterministically; run `--test-threads=1` to avoid ephemeral-port/env races. |
| T-95-SC | Tampering | npm/pip/cargo installs | n/a | No new package-manager installs — `mcp-tester`/`tempfile`/`serde_json`/`url`/`proptest` are existing audited workspace/registry deps already used by sql-server. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-server -- --test-threads=1` passes (all integration + property tests).
- `make purity-check` passes and names `pmcp-workbook-server` as reader-free.
- CLAUDE.md publish order lists the crate at slot 9a.
- `make quality-gate` passes (fmt/clippy/build/test) before commit.
</verification>

<success_criteria>
- Success criterion 1 observably proven: the parity/http tests stand up a live server from the golden bundle alone and exercise the five tools.
- Success criterion 2 observably proven: the property test confirms the fail-closed boot/guard path → RunError (non-zero), and the lib crash-surfacing tests (Plan 95-01) cover Serving.
- Success criterion 3 observably proven: `make purity-check` confirms no reader in the pmcp-workbook-server tree, and CLAUDE.md slot-9a wiring records the published-binary dependency posture.
</success_criteria>

<output>
Create `.planning/phases/95-shape-a-binary-pmcp-workbook-server/95-02-SUMMARY.md` when done.
</output>
