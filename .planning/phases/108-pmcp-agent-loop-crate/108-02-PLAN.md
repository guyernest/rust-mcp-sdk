---
phase: 108-pmcp-agent-loop-crate
plan: 02
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - Cargo.toml
  - CLAUDE.md
  - crates/pmcp-agent/Cargo.toml
  - crates/pmcp-agent/src/lib.rs
  - crates/pmcp-agent/src/seams/mod.rs
  - crates/pmcp-agent/src/seams/completion.rs
  - crates/pmcp-agent/src/seams/tool.rs
  - crates/pmcp-agent/src/seams/store.rs
  - crates/pmcp-agent/src/iteration/mod.rs
  - crates/pmcp-agent/src/sources/mod.rs
  - crates/pmcp-agent/src/invoker/mod.rs
  - crates/pmcp-agent/src/config/mod.rs
  - crates/pmcp-agent/src/adapter/mod.rs
  - crates/pmcp-agent/src/trace.rs
  - crates/pmcp-agent/tests/object_safety.rs
autonomous: true
requirements: [AGNT-01]
must_haves:
  truths:
    - "A developer can hold Arc<dyn CompletionSource>, Arc<dyn ToolInvoker>, Arc<dyn ConversationStore> (all object-safe)"
    - "CompletionSource::create_message returns CreateMessageResultWithTools (reuses SDK sampling types verbatim)"
    - "ToolInvoker exposes a batch method invoke_batch(Vec<ToolCall>) -> Vec<ToolCallResult>"
    - "ConversationStore load/save RunState which is Serialize + Deserialize with no floats"
    - "The crate compiles with default features and its module tree is declared for downstream plans"
  artifacts:
    - path: "crates/pmcp-agent/Cargo.toml"
      provides: "New 0.x workspace member mirroring pmcp-tasks; openai-compat/anthropic features"
      contains: "pmcp-agent"
    - path: "crates/pmcp-agent/src/seams/completion.rs"
      provides: "CompletionSource trait + CompletionError + RetryClass accessor"
      contains: "CreateMessageResultWithTools"
    - path: "crates/pmcp-agent/src/seams/store.rs"
      provides: "ConversationStore trait + RunState + InMemoryStore"
      contains: "InMemoryStore"
    - path: "crates/pmcp-agent/tests/object_safety.rs"
      provides: "Compile proof all three seams are object-safe"
      contains: "dyn CompletionSource"
  key_links:
    - from: "Cargo.toml [workspace].members"
      to: "crates/pmcp-agent"
      via: "workspace member registration"
      pattern: "crates/pmcp-agent"
    - from: "crates/pmcp-agent/src/seams/completion.rs"
      to: "pmcp::types::sampling::CreateMessageResultWithTools"
      via: "trait return type"
      pattern: "CreateMessageResultWithTools"
---

<objective>
Scaffold the new `crates/pmcp-agent` 0.x experimental workspace member (mirroring the `pmcp-tasks` isolation precedent) and define the three object-safe async effect-seam traits that the entire crate is built between — `CompletionSource`, `ToolInvoker`, `ConversationStore` (AGNT-01). This plan establishes the CONTRACTS that Wave 3 plans (iteration core, sources, invoker/config) implement against, so it also declares the full module tree with compiling stubs.

Purpose: interface-first — downstream plans receive the seam signatures, the shared `RetryClass` enum, and `RunState` directly, with no codebase scavenger hunt.
Output: a compiling `pmcp-agent` crate with three seams, `InMemoryStore`, shared error/retry types, an object-safety proof, and stub module files owned by later plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-pmcp-agent-loop-crate/108-CONTEXT.md
@.planning/phases/108-pmcp-agent-loop-crate/108-RESEARCH.md
@.planning/phases/108-pmcp-agent-loop-crate/108-PATTERNS.md

<interfaces>
<!-- Verified analogs (108-PATTERNS.md). Copy structure; do not reimplement. -->
- Isolation precedent: `crates/pmcp-tasks/Cargo.toml` (0.x member, preserve_order serde, thiserror, feature-gated optional deps block) and `crates/pmcp-tasks/src/lib.rs` (`//!` crate doc + `pub mod` + `pub use` layout).
- Seam analog (completion): `src/client/host/sampling.rs:36` `HostSamplingHandler` returns `CreateMessageResult` — the WRONG type; `CompletionSource` MUST return `CreateMessageResultWithTools` (`src/types/sampling.rs:339`) and take `CreateMessageParams` (`:200`) verbatim.
- Seam analog (store): `crates/pmcp-tasks/src/store/backend.rs` `StorageBackend`/`InMemoryBackend` (DashMap or parking_lot::Mutex<HashMap>), `StorageError` enum (`:82`); `VersionedRecord` (`:49`) for optimistic-concurrency discipline if resume needs it.
- Async trait convention: `#[async_trait] pub trait X: Send + Sync` — all object-safe (`Arc<dyn X>`), documented Send+Sync for concurrent handler access (`crates/pmcp-tasks/src/store/mod.rs:208`).
- Classification-as-data precedent for RetryClass: `src/types/tasks.rs:349` `poll_decision` / `TaskPollDecision` (#[non_exhaustive], exhaustive match, no `_` arm).
- Root workspace members line: `Cargo.toml:582` (`members = [...]`); add `"crates/pmcp-agent"` (regular member — only root `pmcp` is clippy-gated today, so a regular member is fine).
- `pmcp` is now 2.17.0 (from plan 108-01); `pmcp-agent` pins `pmcp = { version = "2.17.0", path = "../.." }` and `pmcp-package = { version = "0.1", path = "../pmcp-package" }`.
- NOTE: `loop` is a Rust keyword — name the iteration module `iteration`, NOT `loop`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crate manifest, module tree, and lib.rs; register member and publish order</name>
  <files>crates/pmcp-agent/Cargo.toml, Cargo.toml, CLAUDE.md, crates/pmcp-agent/src/lib.rs, crates/pmcp-agent/src/iteration/mod.rs, crates/pmcp-agent/src/sources/mod.rs, crates/pmcp-agent/src/invoker/mod.rs, crates/pmcp-agent/src/config/mod.rs, crates/pmcp-agent/src/adapter/mod.rs, crates/pmcp-agent/src/trace.rs</files>
  <read_first>
    - crates/pmcp-tasks/Cargo.toml (copy structure; swap the dynamodb/redis optional-dep block for reqwest-gated openai-compat/anthropic)
    - crates/pmcp-tasks/src/lib.rs (copy //! + pub mod + pub use layout)
    - Cargo.toml (root — [workspace].members at line 582; version now 2.17.0)
    - CLAUDE.md (Release & Publish Workflow "Workspace Crates (publish order)" list, ~line 220-240)
  </read_first>
  <action>
    Create crates/pmcp-agent/Cargo.toml: name `pmcp-agent`, version `0.1.0`, edition 2021, license MIT, rust-version 1.91.0. Dependencies: `pmcp = { version = "2.17.0", path = "../..", default-features = false }`, `pmcp-package = { version = "0.1", path = "../pmcp-package" }`, `serde = { version = "1.0", features = ["derive"] }`, `serde_json = { version = "1.0", features = ["preserve_order"] }` (preserve_order = replay determinism, Pitfall 3), `async-trait = "0.1"`, `thiserror = "2.0"`, `futures = "0.3"`, `tracing = "0.1"`. Optional: `reqwest = { version = "0.13", default-features = false, features = ["json","rustls"], optional = true }`. `[features] openai-compat = ["dep:reqwest"]` and `anthropic = ["dep:reqwest"]` (keeps wasm32 clean per D-13/Pitfall 4). `[dev-dependencies]`: `pmcp = { version = "2.17.0", path = "../..", features = ["full"] }`, `tokio = { version = "1", features = ["full"] }`, `proptest = "1.7"`, `pretty_assertions = "1.4"`, `futures = "0.3"`, `async-trait = "0.1"`.
    Add `"crates/pmcp-agent"` to the root Cargo.toml `[workspace].members` list. Add a `pmcp-agent` entry to the CLAUDE.md publish-order list positioned AFTER `pmcp` (core SDK) and before `pmcp-team-servers`/`cargo-pmcp`, noting it pins `pmcp = "2.17"` and is 0.x/experimental.
    Create crates/pmcp-agent/src/lib.rs with a `//!` crate doc and `pub mod seams; pub mod iteration; pub mod sources; pub mod invoker; pub mod config; pub mod adapter; pub mod trace;`. Create EMPTY stub module files (each just a `//! <role> — implemented in plan 108-NN.` doc line) for: src/iteration/mod.rs, src/sources/mod.rs, src/invoker/mod.rs, src/config/mod.rs, src/adapter/mod.rs, src/trace.rs. These stubs let the crate compile now; Wave 3/4 plans own and fill them without touching lib.rs. Re-export the seam types from lib.rs (`pub use seams::{CompletionSource, ToolInvoker, ConversationStore};`) after Task 2/3 add them.
  </action>
  <verify>
    <automated>cargo build -p pmcp-agent</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pmcp-agent/Cargo.toml` exists; `grep -q 'openai-compat' crates/pmcp-agent/Cargo.toml` and `grep -q 'anthropic' crates/pmcp-agent/Cargo.toml`.
    - `grep -q 'crates/pmcp-agent' Cargo.toml` (member registered).
    - `grep -q 'pmcp-agent' CLAUDE.md` in the publish-order list, after the `pmcp` core entry.
    - `cargo build -p pmcp-agent` succeeds with default features (no reqwest pulled).
    - `cargo build -p pmcp-agent --target wasm32-unknown-unknown` succeeds OR fails only for reasons unrelated to reqwest (default features must not pull reqwest) — if the wasm target is not installed, at minimum `cargo tree -p pmcp-agent | grep -c reqwest` returns 0 for default features.
  </acceptance_criteria>
  <done>The pmcp-agent crate builds with default features, is a registered workspace member, appears in the publish order, and declares its full module tree with compiling stubs.</done>
</task>

<task type="auto">
  <name>Task 2: CompletionSource + ToolInvoker seams, shared RetryClass, error taxonomy</name>
  <files>crates/pmcp-agent/src/seams/mod.rs, crates/pmcp-agent/src/seams/completion.rs, crates/pmcp-agent/src/seams/tool.rs, crates/pmcp-agent/src/lib.rs</files>
  <read_first>
    - src/client/host/sampling.rs (HostSamplingHandler async-trait shape)
    - src/types/sampling.rs (CreateMessageParams :200, CreateMessageResultWithTools :339, SamplingMessageContent variants)
    - src/types/tasks.rs (poll_decision :349, TaskPollDecision — the non_exhaustive/exhaustive-match precedent for RetryClass)
    - crates/pmcp-tasks/src/error.rs (TaskError thiserror shape)
    - crates/pmcp-agent/src/lib.rs (module tree from Task 1)
  </read_first>
  <action>
    Create crates/pmcp-agent/src/seams/mod.rs declaring `mod completion; mod tool;` (store added in Task 3) and re-exporting their public items, plus a shared `RetryClass` enum defined here (shared accessor approach, D-05 discretion): `#[non_exhaustive] pub enum RetryClass { Fatal, Transient { attempt_hint: u32 }, Capacity { attempt_hint: u32 } }` mirroring the `TaskPollDecision` classification-as-data shape — NO backoff/sleep policy, it is pure data. Derive Debug, Clone, PartialEq, Serialize, Deserialize.
    Create seams/completion.rs: `#[async_trait] pub trait CompletionSource: Send + Sync` with `async fn create_message(&self, params: pmcp::types::sampling::CreateMessageParams) -> Result<pmcp::types::sampling::CreateMessageResultWithTools, CompletionError>`. Define `CompletionError` via `#[derive(thiserror::Error)]` with a `pub fn retry_class(&self) -> RetryClass` accessor (classifies its variants: e.g. transport/5xx → Transient, 429/529 → Capacity, decode/auth → Fatal). Never place secret material in Error variants that could be logged.
    Create seams/tool.rs: define `ToolCall` and `ToolCallResult` data types (Serialize+Deserialize, no floats) carrying tool name + arguments (`serde_json::Value`) and result/error respectively. `#[async_trait] pub trait ToolInvoker: Send + Sync` with `async fn invoke_batch(&self, calls: Vec<ToolCall>) -> Vec<ToolCallResult>` as THE seam method (D-07); provide a default single-call helper only as a convenience wrapper over invoke_batch. Define `ToolError` via thiserror with a `retry_class()` accessor.
    Update lib.rs re-exports to surface `CompletionSource`, `ToolInvoker`, `RetryClass`, `ToolCall`, `ToolCallResult`.
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent seams::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'CreateMessageResultWithTools' crates/pmcp-agent/src/seams/completion.rs` (correct result type — Pitfall 1 avoided).
    - `grep -q 'invoke_batch' crates/pmcp-agent/src/seams/tool.rs`.
    - `grep -q 'enum RetryClass' crates/pmcp-agent/src/seams/mod.rs` and it is `#[non_exhaustive]` with Fatal/Transient/Capacity variants.
    - `RetryClass`, `ToolCall`, `ToolCallResult` derive Serialize + Deserialize and contain no `f32`/`f64` fields.
    - `cargo build -p pmcp-agent` succeeds.
  </acceptance_criteria>
  <done>CompletionSource returns CreateMessageResultWithTools; ToolInvoker exposes invoke_batch; a shared RetryClass data enum and per-seam error accessors exist with no retry/backoff policy.</done>
</task>

<task type="auto">
  <name>Task 3: ConversationStore + RunState + InMemoryStore; object-safety proof</name>
  <files>crates/pmcp-agent/src/seams/store.rs, crates/pmcp-agent/src/seams/mod.rs, crates/pmcp-agent/tests/object_safety.rs</files>
  <read_first>
    - crates/pmcp-tasks/src/store/backend.rs (StorageBackend + InMemoryBackend + StorageError :82 + VersionedRecord :49)
    - crates/pmcp-tasks/src/store/mod.rs (TaskStore Send+Sync doc :208)
    - crates/pmcp-agent/src/seams/mod.rs (from Task 2 — add store module + re-exports)
    - .planning/phases/108-pmcp-agent-loop-crate/108-RESEARCH.md (Open Question 3: RunState granularity — persist message history + iteration counter + pending tool-call set; derive the rest)
  </read_first>
  <action>
    Create crates/pmcp-agent/src/seams/store.rs. Define `RunState` (D-06): the minimum needed for mid-iteration resume — message history (Vec of the crate's message type), an iteration counter (u32/usize, NOT a timestamp — Pitfall 3), and the pending tool-call set (Vec<ToolCall>). Derive Debug, Clone, Serialize, Deserialize; NO floats anywhere; counters not wall-clock. `#[async_trait] pub trait ConversationStore: Send + Sync` with `async fn load(&self, run_id: &str) -> Result<Option<RunState>, StoreError>` and `async fn save(&self, run_id: &str, state: &RunState) -> Result<(), StoreError>`. Define `StoreError` via thiserror mirroring `StorageError`. Implement `InMemoryStore` backed by `parking_lot::Mutex<HashMap<String, RunState>>` (or DashMap) — trivial laptop default, mirroring `InMemoryBackend`. Add `mod store; pub use store::{ConversationStore, RunState, InMemoryStore, StoreError};` to seams/mod.rs and surface them from lib.rs.
    Create crates/pmcp-agent/tests/object_safety.rs: a compile-level test constructing `Arc<dyn CompletionSource>`, `Arc<dyn ToolInvoker>`, and `Arc<dyn ConversationStore>` (using `InMemoryStore` for the store, and trivial no-op stub impls for the other two inside the test) — proving all three seams are object-safe (AGNT-01).
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent --test object_safety -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'struct RunState' crates/pmcp-agent/src/seams/store.rs`; `RunState` has an iteration counter field and no `f32`/`f64`/`SystemTime`/`Instant` fields.
    - `grep -q 'struct InMemoryStore' crates/pmcp-agent/src/seams/store.rs`.
    - `tests/object_safety.rs` contains `dyn CompletionSource`, `dyn ToolInvoker`, `dyn ConversationStore` and compiles+passes.
    - A `serde_json::to_string` then `from_str` round-trip of a `RunState` is asserted equal in a unit test (replay-determinism sanity).
  </acceptance_criteria>
  <done>ConversationStore holds resumable RunState (history + counter + pending calls, no floats/time); InMemoryStore is the laptop default; all three seams are proven object-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seam trait ↔ implementations | The three seams are the only points the loop performs effects; untrusted LLM/tool data enters through completion/tool results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-02-01 | Tampering / DoS | RunState / ToolCallResult deserialization | mitigate | Typed `serde` deserialization; no floats (canonicalization + replay safety); the digestion fuzz path is exercised in plan 108-03 |
| T-108-02-02 | Information Disclosure | CompletionError / ToolError variants | mitigate | Error variants carry no secret material (no API keys/tokens); accessors return only `RetryClass` data |
| T-108-02-SC | Tampering | crates (supply chain) | accept | No new package installs (RESEARCH Package Legitimacy Audit: subset of existing verified workspace deps; reqwest already pinned 0.13, feature-gated) |
</threat_model>

<verification>
- `cargo test -p pmcp-agent -- --test-threads=1` passes (seams + object_safety).
- `cargo build -p pmcp-agent` (default features) does not pull reqwest.
- `make quality-gate` green before commit (PMAT cog ≤25 on all new fns).
</verification>

<success_criteria>
- The crate compiles with default features; wasm32 default build does not reference reqwest.
- All three seams are object-safe and `CompletionSource` reuses SDK sampling types verbatim (AGNT-01).
- Shared `RetryClass` is classification-as-data with no backoff policy; `RunState` is replay-safe (no floats/time).
- Module tree declared with stubs so Wave 3 plans can proceed in parallel without lib.rs contention.
</success_criteria>

<output>
Create `.planning/phases/108-pmcp-agent-loop-crate/108-02-SUMMARY.md` when done.
</output>
