---
phase: 108-pmcp-agent-loop-crate
plan: 04
type: execute
wave: 3
depends_on: ["108-01", "108-02"]
files_modified:
  - crates/pmcp-agent/src/sources/mod.rs
  - crates/pmcp-agent/src/sources/sampling.rs
  - crates/pmcp-agent/src/sources/openai_compat.rs
  - crates/pmcp-agent/src/sources/anthropic.rs
  - crates/pmcp-agent/tests/sampling_source.rs
autonomous: true
requirements: [AGNT-04, AGNT-05, AGNT-06]
must_haves:
  truths:
    - "SamplingSource implements CompletionSource over the server-side peer with zero extra dependencies, returning CreateMessageResultWithTools with tool_use blocks intact"
    - "OpenAiCompatSource (feature openai-compat) implements CompletionSource against any OpenAI-compatible endpoint"
    - "AnthropicSource (feature anthropic) implements CompletionSource against the Anthropic Messages API"
    - "The default (no-feature) build compiles the SamplingSource and never pulls reqwest"
    - "API keys are read from env/resolver and never logged"
  artifacts:
    - path: "crates/pmcp-agent/src/sources/sampling.rs"
      provides: "SamplingSource over peer.sample_with_tools (AGNT-04)"
      contains: "sample_with_tools"
    - path: "crates/pmcp-agent/src/sources/openai_compat.rs"
      provides: "OpenAiCompatSource (feature-gated, reqwest)"
      contains: "CompletionSource"
    - path: "crates/pmcp-agent/src/sources/anthropic.rs"
      provides: "AnthropicSource (feature-gated, reqwest)"
      contains: "CompletionSource"
    - path: "crates/pmcp-agent/tests/sampling_source.rs"
      provides: "Real Server::run + Client proof SamplingSource returns tool_use blocks"
      contains: "SamplingSource"
  key_links:
    - from: "crates/pmcp-agent/src/sources/sampling.rs"
      to: "pmcp PeerHandle::sample_with_tools"
      via: "SamplingSource::create_message calls peer.sample_with_tools"
      pattern: "sample_with_tools"
    - from: "crates/pmcp-agent/src/sources/openai_compat.rs"
      to: "OpenAI-compatible /chat/completions endpoint"
      via: "reqwest POST behind openai-compat feature"
      pattern: "reqwest"
---

<objective>
Ship the three `CompletionSource` implementations (sampling-first, three sources maximum — the trait is the extension point):

1. **`SamplingSource` (AGNT-04, zero-dep):** implements `CompletionSource` over the agent's server-side peer using the `PeerHandle::sample_with_tools` path added in plan 108-01, so a hosted agent drives tools through spec sampling. Rides the D-106-A fix (completes on the stock server loop).
2. **`OpenAiCompatSource` (AGNT-05, feature `openai-compat`):** against any OpenAI-compatible `/chat/completions` endpoint (Ollama/vLLM/OpenRouter/xAI/DeepSeek) via reqwest.
3. **`AnthropicSource` (AGNT-06, feature `anthropic`):** against the Anthropic Messages API via reqwest.

Purpose: the "same loop, different source" proof. `SamplingSource` is the hosted path; the HTTP sources are the standalone path. Both return `CreateMessageResultWithTools`.
Output: `sources/{sampling,openai_compat,anthropic}.rs` and a real-loop SamplingSource integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-pmcp-agent-loop-crate/108-CONTEXT.md
@.planning/phases/108-pmcp-agent-loop-crate/108-RESEARCH.md
@.planning/phases/108-pmcp-agent-loop-crate/108-PATTERNS.md
@crates/pmcp-agent/src/seams/mod.rs

<interfaces>
<!-- From plans 108-01/108-02 + verified facts. -->
- Seam to implement (plan 108-02): `#[async_trait] CompletionSource::create_message(CreateMessageParams) -> Result<CreateMessageResultWithTools, CompletionError>`, with `CompletionError::retry_class()`.
- Peer path (plan 108-01): `PeerHandle::sample_with_tools(CreateMessageParams) -> Result<CreateMessageResultWithTools>` on `src/server/peer_impl.rs` DispatchPeerHandle. Reachable in a tool handler via `extra.peer()`.
- Analog for SamplingSource wrapper: `src/server/peer_impl.rs:53` `DispatchPeerHandle` (dispatch + decode). SamplingSource holds a peer handle and calls `sample_with_tools`.
- Verified result/param types: `CreateMessageParams` (`src/types/sampling.rs:200`, has `tools`/`tool_choice`), `CreateMessageResultWithTools` (`:339`, `Vec<SamplingMessageContent>` with ToolUse/ToolResult), `ToolChoice`/`ToolChoiceMode`.
- HTTP source shape (Claude's discretion; reference `pmcp-run llm/service.rs` transformer shape, read-only): reqwest 0.13 `default-features=false, features=["json","rustls"]`, HTTPS only, no streaming required this phase. Transform CreateMessageParams → provider request; transform provider response → CreateMessageResultWithTools (preserve tool_use).
- Real-loop harness: `tests/common/duplex.rs` `DuplexTransport::pair()` + `call_via_server`; include via `#[path = "common/duplex.rs"] mod duplex;`. Client built with `on_sampling` returning tool_use.
- Plan 108-02 created stub `src/sources/mod.rs` for this plan to fill (owns the whole sources/ dir; does NOT touch lib.rs).
- Security: API keys ONLY from env/SlotResolver; never in `Debug`/`tracing`/error variants. reqwest with rustls, HTTPS endpoints only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SamplingSource over the server-side peer (zero-dep, AGNT-04)</name>
  <files>crates/pmcp-agent/src/sources/sampling.rs, crates/pmcp-agent/src/sources/mod.rs, crates/pmcp-agent/tests/sampling_source.rs</files>
  <read_first>
    - crates/pmcp-agent/src/seams/completion.rs (CompletionSource + CompletionError from plan 108-02)
    - src/server/peer_impl.rs (DispatchPeerHandle::sample and the new sample_with_tools from plan 108-01)
    - src/shared/peer.rs (PeerHandle trait incl. sample_with_tools)
    - tests/common/duplex.rs (real Server::run + Client harness)
    - examples/s49_sampling_host.rs, examples/s43_handler_peer_sample.rs (on_sampling + in-tool peer wiring)
  </read_first>
  <behavior>
    - `SamplingSource::create_message(params)` calls the wrapped peer's `sample_with_tools(params)` and returns the resulting `CreateMessageResultWithTools`.
    - When the client's `on_sampling` returns content containing a ToolUse block, the SamplingSource result carries that ToolUse (not dropped) — driven end-to-end through a real `Server::run` + real `Client`.
    - A peer/transport error maps to a `CompletionError` whose `retry_class()` is not `Fatal` for transient failures.
  </behavior>
  <action>
    Create sources/sampling.rs: `pub struct SamplingSource` holding a peer handle (the `Arc<dyn PeerHandle>`/peer type exposed by `RequestHandlerExtra::peer()`). Implement `#[async_trait] CompletionSource for SamplingSource` whose `create_message` calls `peer.sample_with_tools(params)` and maps `pmcp::Error` into `CompletionError`. Zero new dependencies (D-04). Add `mod sampling; pub use sampling::SamplingSource;` to sources/mod.rs.
    Create tests/sampling_source.rs using the duplex harness: a server with a tool whose handler constructs a `SamplingSource` from `extra.peer()`, calls `create_message` with a `CreateMessageParams` carrying tools + `ToolChoice::Auto`, and asserts the returned `CreateMessageResultWithTools` contains the ToolUse block; the client is built with `on_sampling` returning that tool_use. This is the AGNT-04 real-loop proof (rides D-106-A).
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent --test sampling_source -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'sample_with_tools' crates/pmcp-agent/src/sources/sampling.rs`.
    - `tests/sampling_source.rs` drives a real `Server::run` + `Client` (no raw pump) and asserts a ToolUse block survives to the `CreateMessageResultWithTools`.
    - `cargo build -p pmcp-agent` (default features) compiles SamplingSource and `cargo tree -p pmcp-agent | grep -c reqwest` returns 0 (zero-dep).
    - The test passes (does not hang — confirms it runs on the stock loop).
  </acceptance_criteria>
  <done>SamplingSource returns tool_use-bearing completions over the server-side peer on the stock server loop, with zero added dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenAiCompatSource (feature openai-compat, AGNT-05)</name>
  <files>crates/pmcp-agent/src/sources/openai_compat.rs, crates/pmcp-agent/src/sources/mod.rs</files>
  <read_first>
    - crates/pmcp-agent/src/seams/completion.rs (CompletionSource + CompletionError + retry_class)
    - src/types/sampling.rs (CreateMessageParams fields, CreateMessageResultWithTools, SamplingMessageContent ToolUse/ToolResult)
    - crates/pmcp-agent/Cargo.toml (openai-compat feature gates reqwest)
    - .planning/phases/108-pmcp-agent-loop-crate/108-RESEARCH.md (Standard Stack reqwest config; llm/service.rs transformer shape reference)
  </read_first>
  <behavior>
    - `OpenAiCompatSource::create_message(params)` POSTs a transformed `/chat/completions` request to a configured base URL with a bearer key, and maps the response into `CreateMessageResultWithTools` preserving any tool_calls as ToolUse blocks.
    - A 5xx response maps to a `CompletionError` whose `retry_class()` is `Transient`; a 429 maps to `Capacity`; a 4xx auth/decode error maps to `Fatal`.
    - The API key is never included in `Debug`, `tracing`, or error variants.
  </behavior>
  <action>
    Create sources/openai_compat.rs behind `#[cfg(feature = "openai-compat")]`: `pub struct OpenAiCompatSource` holding base URL, model, and an API key (read from the caller/SlotResolver — NOT logged). Implement `CompletionSource` via reqwest (0.13, rustls, HTTPS): transform `CreateMessageParams` (messages, system prompt, tools, tool_choice, max_tokens) into an OpenAI chat-completions request; transform the response (choices → assistant message; tool_calls → ToolUse blocks) into `CreateMessageResultWithTools`; classify HTTP status into `RetryClass` via `CompletionError::retry_class`. No streaming. Add `#[cfg(feature = "openai-compat")] mod openai_compat; #[cfg(feature = "openai-compat")] pub use openai_compat::OpenAiCompatSource;` to sources/mod.rs. Write unit tests that exercise the request-build and response-parse transforms against canned JSON (no network) and assert keys never appear in the `Debug` output.
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent --features openai-compat sources::openai_compat -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pmcp-agent/src/sources/openai_compat.rs` is gated with `#[cfg(feature = "openai-compat")]`.
    - Response-parse unit test maps an OpenAI `tool_calls` payload to ToolUse blocks in `CreateMessageResultWithTools`.
    - A unit test asserts the API key string is absent from `format!("{:?}", source)`.
    - Status-classification unit tests: 500→Transient, 429→Capacity, 401→Fatal.
    - `cargo build -p pmcp-agent` (default features) does NOT compile this module (reqwest not pulled).
  </acceptance_criteria>
  <done>OpenAiCompatSource speaks the OpenAI chat-completions shape, preserves tool calls, classifies retries as data, and never leaks the key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AnthropicSource (feature anthropic, AGNT-06)</name>
  <files>crates/pmcp-agent/src/sources/anthropic.rs, crates/pmcp-agent/src/sources/mod.rs</files>
  <read_first>
    - crates/pmcp-agent/src/sources/openai_compat.rs (mirror the structure + key-handling discipline from Task 2)
    - crates/pmcp-agent/src/seams/completion.rs (CompletionSource + retry_class)
    - src/types/sampling.rs (CreateMessageResultWithTools, SamplingMessageContent ToolUse)
  </read_first>
  <behavior>
    - `AnthropicSource::create_message(params)` POSTs a transformed Anthropic Messages API request (system top-level, messages, tools, max_tokens) with the `x-api-key`/`anthropic-version` headers, and maps the `content` blocks (incl. `tool_use`) into `CreateMessageResultWithTools`.
    - HTTP status maps to `RetryClass` (5xx→Transient, 429/529→Capacity, 4xx→Fatal).
    - The API key is never logged or included in Debug/error variants.
  </behavior>
  <action>
    Create sources/anthropic.rs behind `#[cfg(feature = "anthropic")]`: `pub struct AnthropicSource` holding base URL (default Anthropic Messages endpoint), model, and API key (from caller/SlotResolver, not logged). Implement `CompletionSource` via reqwest (rustls, HTTPS): transform `CreateMessageParams` into an Anthropic Messages request (system as top-level field, tools mapped to Anthropic tool schema, tool_choice); transform the response content blocks (`text` + `tool_use`) into `CreateMessageResultWithTools`; classify HTTP status into `RetryClass`. No streaming. Add the `#[cfg(feature = "anthropic")]` mod + re-export to sources/mod.rs. Unit tests against canned Messages-API JSON (no network) covering tool_use parsing, status classification, and key-absence-in-Debug.
  </action>
  <verify>
    <automated>cargo test -p pmcp-agent --features anthropic sources::anthropic -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pmcp-agent/src/sources/anthropic.rs` is gated with `#[cfg(feature = "anthropic")]`.
    - Response-parse unit test maps an Anthropic `tool_use` content block to a ToolUse in `CreateMessageResultWithTools`.
    - A unit test asserts the API key is absent from `format!("{:?}", source)`.
    - `cargo test -p pmcp-agent --features openai-compat,anthropic sources::` passes (both HTTP sources build together).
  </acceptance_criteria>
  <done>AnthropicSource speaks the Messages API, preserves tool_use, classifies retries as data, and never leaks the key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pmcp-agent → external LLM endpoint | OpenAiCompat/Anthropic sources send prompts + API keys over the network (HTTPS); responses are untrusted input |
| server-side peer ↔ hosting client | SamplingSource issues sampling requests answered by the host client (rides Phase 106 approval seam) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-04-01 | Information Disclosure | API key handling in HTTP sources | mitigate | Keys read from env/SlotResolver only; never in `Debug`/`tracing`/`CompletionError`; unit-tested key-absence-in-Debug (ASVS V7) |
| T-108-04-02 | Tampering | untrusted LLM response JSON | mitigate | Typed serde parse into CreateMessageResultWithTools; no `unwrap` on network JSON; parse errors → Fatal CompletionError |
| T-108-04-03 | Information Disclosure / MITM | HTTP transport | mitigate | reqwest with `rustls`, HTTPS endpoints only (ASVS V9); no plaintext HTTP default |
| T-108-04-04 | Access Control | hosted sampling invocation | accept | SamplingSource rides the Phase 106 host-side `PreflightApproval` seam; the source does not reimplement approval |
| T-108-04-SC | Tampering | reqwest (supply chain) | accept | reqwest already pinned 0.13 in workspace, feature-gated; no new package name introduced (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `cargo test -p pmcp-agent --test sampling_source -- --test-threads=1` (real-loop AGNT-04).
- `cargo test -p pmcp-agent --features openai-compat,anthropic sources:: -- --test-threads=1` (AGNT-05/06).
- `cargo build -p pmcp-agent` default features: no reqwest pulled.
- `make quality-gate` green before commit.
</verification>

<success_criteria>
- SamplingSource returns tool_use completions over the peer on the stock loop with zero deps (AGNT-04).
- OpenAiCompatSource and AnthropicSource (feature-gated) implement CompletionSource, preserve tool calls, and classify retries as data (AGNT-05/06).
- API keys are never logged; HTTPS-only; default build stays reqwest-free (wasm-clean per D-13).
</success_criteria>

<output>
Create `.planning/phases/108-pmcp-agent-loop-crate/108-04-SUMMARY.md` when done.
</output>
