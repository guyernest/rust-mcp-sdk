---
phase: 109-team-reference-servers
plan: 04
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - crates/pmcp-team-servers/src/approval/channels.rs
  - crates/pmcp-team-servers/src/approval/server.rs
  - crates/pmcp-team-servers/src/bin/approval_mcp.rs
autonomous: true
requirements: [TEAM-03]
must_haves:
  truths:
    - "approval-mcp advertises resolve_approval + get_approval (UNNAMESPACED) plus one team_approval__ask_<member> per human role"
    - "team_approval__ask_ mints a pending approval task on an in-memory TaskStore; resolve_approval transitions it to a verdict"
    - "The console channel prints the ask (question, options, approval id) — no stdin prompting; resolution is ALWAYS via the resolve_approval tool"
    - "The webhook channel (feature-gated) POSTs the ask payload + approval id to a configured URL and never resolves"
    - "Ask/resolve records carry an optional subject_task_id/subject_ref echoed by get_approval and resolve_approval"
    - "A team_approval__ask_ targeting a member absent from the configured roster errors, never panics"
  artifacts:
    - path: "crates/pmcp-team-servers/src/approval/channels.rs"
      provides: "ApprovalChannel trait + ConsoleChannel + (webhook) WebhookChannel notify-only impls"
      exports: ["ApprovalChannel", "ConsoleChannel"]
    - path: "crates/pmcp-team-servers/src/approval/server.rs"
      provides: "approval-mcp Server builder on InMemoryTaskStore; static + dynamic ask family"
      contains: "resolve_approval"
  key_links:
    - from: "src/approval/server.rs"
      to: "pmcp::InMemoryTaskStore"
      via: ".task_store() on the builder"
      pattern: "task_store"
    - from: "src/approval/server.rs (ask handler)"
      to: "src/approval/channels.rs"
      via: "notify on ask; never resolve"
      pattern: "notify|ApprovalChannel"
---

<objective>
Implement the approval-mcp reference server (TEAM-03): the approval contract served over an in-memory `TaskStore` (reuse `InMemoryTaskStore` + `.task_store()` — do NOT build a bespoke store), with two console (dev, D-10) and webhook (CI, D-11) notify-ONLY channels and a single resolution path (the `resolve_approval` tool). Advertise `resolve_approval` + `get_approval` as UNNAMESPACED legacy tools plus one `team_approval__ask_<member>` per configured human role (computed once at build, D-07). Ask/resolve records carry an optional `subject_task_id`/`subject_ref` echoed back (D-12). Fill in the HTTP-first `approval-mcp` dev binary.

Purpose: Ship the approval reference server with one deterministic, CI-safe resolution path (no TTY, no stdin) and linkable (agent output, human verdict) pairs.
Output: `src/approval/{channels,server}.rs` + `src/bin/approval_mcp.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-team-reference-servers/109-CONTEXT.md
@.planning/phases/109-team-reference-servers/109-RESEARCH.md
@.planning/phases/109-team-reference-servers/109-PATTERNS.md

<interfaces>
From crates/pmcp-agent/src/adapter/server.rs lines 244-250 (InMemoryTaskStore reuse):
- `let task_store = Arc::new(InMemoryTaskStore::new()) as Arc<dyn TaskStore>;`
- `Server::builder().name(..).version(..).tool(name, tool).task_store(task_store).build()?`
- Task lifecycle create→working→completed observable via `tasks/get` / `tasks/result`.

From crates/pmcp-agent/src/adapter/factory.rs lines 36-88 (trait-seam-with-two-impls pattern to mirror for ApprovalChannel: one trait, console impl + webhook impl, selected at wiring).

From crates/pmcp-package/src/package/team.rs:
- `HumanRole { role: String, description: String, responsibilities: Vec<String>, channel_hints: Vec<String> }`
- `TeamPackage.human_roles: Vec<HumanRole>` — the ask family is one tool per human role, computed once at build.

Contract (contracts/team-servers-v1.yaml `approval_tool_surface`, rev'd to 1.1.0 in 109-01):
- static UNNAMESPACED legacy tools: `resolve_approval`, `get_approval` (NOT namespaced — Pitfall 3)
- dynamic namespaced family: `team_approval__ask_<member>` (one per human roster member)
- unknown member → Error("unknown member"), never panic
- records carry optional `subject_task_id`/`subject_ref` echoed by get/resolve (D-12).

reqwest is feature-gated behind `webhook` (Cargo `webhook = ["dep:reqwest"]` from 109-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApprovalChannel trait + ConsoleChannel + feature-gated WebhookChannel (notify-only)</name>
  <files>crates/pmcp-team-servers/src/approval/channels.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/approval/channels.rs (stub from 109-01)
    - crates/pmcp-agent/src/adapter/factory.rs lines 36-101 (trait + multiple impls seam)
    - .planning/phases/109-team-reference-servers/109-RESEARCH.md (D-10/D-11; Security Domain SSRF + secret-leakage rows)
  </read_first>
  <action>
    Define `#[async_trait] pub trait ApprovalChannel: Send + Sync { async fn notify(&self, ask: &ApprovalAsk) -> Result<(), ChannelError>; }` where `ApprovalAsk` carries the question, options, approval id, and the optional subject reference. Implement `pub struct ConsoleChannel` whose `notify` prints the ask (question, options, approval id) via `tracing`/`println` — NO stdin read, NO TTY dependency, NO resolution (D-10). Behind `#[cfg(feature = "webhook")]` implement `pub struct WebhookChannel { url: String, secret: Option<RedactedSecret-or-String> }` whose `notify` issues a single outgoing `reqwest` POST of the ask payload + approval id, with an OPTIONAL shared-secret header; it is notify-only and NEVER a resolution path (D-11). The shared secret MUST NOT be logged (V7): never place it in a `tracing` field or the printed ask. Document that the webhook URL is operator-configured trusted input (SSRF is out of dev scope; platform keeps hardened egress). Both channels are pure outgoing notification transports — resolution always happens via the `resolve_approval` tool. Add unit tests for `ConsoleChannel::notify` (returns Ok, does not block on stdin).
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers approval::channels && cargo build -p pmcp-team-servers --features "approval-mcp webhook" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/approval/channels.rs` defines `pub trait ApprovalChannel` and `pub struct ConsoleChannel`
    - `WebhookChannel` is gated behind `#[cfg(feature = "webhook")]` and uses `reqwest` only under that cfg
    - no `stdin`/`read_line` call exists anywhere in `channels.rs`
    - the shared secret is not passed to any `tracing`/`println` argument (grep confirms no secret field in log macros)
    - `cargo build -p pmcp-team-servers --features "approval-mcp webhook"` exits 0
    - `cargo test -p pmcp-team-servers approval::channels` exits 0
  </acceptance_criteria>
  <done>Console + feature-gated webhook channels implemented as notify-only transports; no stdin, no resolution path, secret never logged.</done>
</task>

<task type="auto">
  <name>Task 2: approval-mcp Server builder (InMemoryTaskStore + static/dynamic surface + subject ref) + HTTP-first binary</name>
  <files>crates/pmcp-team-servers/src/approval/server.rs, crates/pmcp-team-servers/src/bin/approval_mcp.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/approval/channels.rs (from Task 1)
    - crates/pmcp-team-servers/src/approval/server.rs (stub from 109-01)
    - crates/pmcp-agent/src/adapter/server.rs lines 211-258 (Server::builder + InMemoryTaskStore + .task_store())
    - src/server/builder.rs (with_task_store / task_store / TaskSupport — grep for `task_store`)
    - crates/pmcp-sql-server/src/main.rs + src/lib.rs lines 156-164 (bin + serve)
  </read_first>
  <action>
    In `src/approval/server.rs` add `pub fn build_approval_mcp_server(human_roles: &[HumanRole], channel: Arc<dyn ApprovalChannel>) -> pmcp::Result<Server>`. Construct `Arc::new(InMemoryTaskStore::new()) as Arc<dyn TaskStore>` and register: `resolve_approval` and `get_approval` as UNNAMESPACED tools (NOT `team_approval__resolve` — Pitfall 3), plus exactly one `team_approval__ask_<role>` per entry in `human_roles`, computed ONCE at build (D-07 snapshot-at-entry — the channel initiator is implicit and never counted, D-05). The `ask` handler mints a pending approval task on the store, records the optional `subject_task_id`/`subject_ref` from the args, and calls `channel.notify(...)` (never resolves). `resolve_approval` transitions the task to a verdict and echoes the stored `subject_task_id`/`subject_ref`; `get_approval` returns the record incl. the subject ref (D-12). An `ask` for a member/role absent from `human_roles` cannot be dispatched (not advertised) — and any dynamic lookup path must return Error("unknown member"), never panic. Wire `.task_store(task_store)` on the builder. Add integration tests: exact `tools/list` surface (resolve_approval + get_approval + N ask tools, no extras); ask→resolve→get lifecycle; subject_ref round-trip echo; unknown-member error path.
    Overwrite `src/bin/approval_mcp.rs` with the thin `#[tokio::main]` + clap `Args` (`--package`, `--port`, `--data-dir`, `--stdio`, plus optional `--webhook-url`): load the TeamPackage, take `human_roles` from it, select `ConsoleChannel` by default or `WebhookChannel` when `--webhook-url` is set (webhook feature), build via `build_approval_mcp_server`, and serve HTTP-by-default with stdio fallback.
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers approval && cargo build -p pmcp-team-servers --features "approval-mcp http" --bin approval-mcp 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/approval/server.rs` registers `resolve_approval` and `get_approval` as unnamespaced tool names (no `team_approval__resolve`/`team_approval__get`)
    - the builder registers exactly one `team_approval__ask_` tool per `human_roles` entry and calls `.task_store(`
    - a test asserts the exact `tools/list` set = {resolve_approval, get_approval} ∪ {one ask per role}, no extras
    - a test asserts ask→resolve→get lifecycle and that a supplied `subject_task_id`/`subject_ref` is echoed by get_approval and resolve_approval
    - a test asserts an unknown-member ask path yields an error (never panic)
    - `cargo build -p pmcp-team-servers --features "approval-mcp http" --bin approval-mcp` exits 0
  </acceptance_criteria>
  <done>approval-mcp serves the approval contract on InMemoryTaskStore with the exact static+dynamic surface, one CI-safe resolution path, subject-ref linkage, and an HTTP-first binary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → approval tool args | Untrusted ask/resolve args incl. optional subject refs |
| server → configured webhook URL | Outgoing POST to operator-configured endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04-01 | Information Disclosure | webhook shared secret in logs | mitigate | Secret never placed in a `tracing`/`println` field; grep-verified (Task 1). HIGH — blocking. |
| T-109-04-02 | Server-Side Request Forgery | configured webhook URL | accept | Webhook is dev/CI-only, opt-in (`webhook` feature + `--webhook-url`), notify-only; URL documented as operator-configured trusted input; platform keeps hardened egress. |
| T-109-04-03 | Elevation of Privilege / Spoofing | unauthorized approval resolution | accept | Dev reference server, no auth surface (platform owns auth); single tool-based resolution path documented. |
| T-109-04-04 | Denial of Service | unknown-member ask / malformed args | mitigate | Unknown member returns Error, never panic; schema-valid-before-side-effect (Task 2). |
| T-109-04-SC | Tampering | dependency graph | mitigate | `reqwest` already vendored (via pmcp-agent), feature-gated behind `webhook`, off by default; no new registry package. |
</threat_model>

<verification>
- `cargo test -p pmcp-team-servers approval` green (channels + exact surface + ask/resolve/get lifecycle + subject ref + unknown-member error)
- `cargo build -p pmcp-team-servers --features "approval-mcp http" --bin approval-mcp` and `--features "approval-mcp webhook"` both succeed
</verification>

<success_criteria>
- TEAM-03 satisfied: approval contract over an in-memory TaskStore with console + webhook channels
- Exact static (unnamespaced) + dynamic ask surface; one CI-safe resolution path (D-10)
- Webhook notify-only + secret never logged (D-11/V7); subject-ref linkage (D-12)
- HTTP-first dev binary configured from a TeamPackage with stdio fallback
</success_criteria>

<output>
Create `.planning/phases/109-team-reference-servers/109-04-SUMMARY.md` when done
</output>
