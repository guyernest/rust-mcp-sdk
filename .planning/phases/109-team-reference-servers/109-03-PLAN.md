---
phase: 109-team-reference-servers
plan: 03
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - crates/pmcp-team-servers/src/mem/backend.rs
  - crates/pmcp-team-servers/src/mem/bm25.rs
  - crates/pmcp-team-servers/src/mem/server.rs
  - crates/pmcp-team-servers/src/bin/mem_mcp.rs
autonomous: true
requirements: [TEAM-04]
must_haves:
  truths:
    - "mem-mcp advertises exactly the 6 mem__* tools; unknown mem__* names error, never panic"
    - "mem__search ranks stored memories by a hand-rolled keyword/BM25 scorer with no embedder dependency"
    - "mem__add then mem__get round-trips a memory; mem__delete removes it; mem__list_recent returns newest-first"
    - "The mem-mcp dev binary binds an HTTP port by default with a --stdio fallback"
  artifacts:
    - path: "crates/pmcp-team-servers/src/mem/backend.rs"
      provides: "TeamMemoryBackend object-safe async trait (6 mem__* ops) + MemError"
      exports: ["TeamMemoryBackend", "MemError"]
    - path: "crates/pmcp-team-servers/src/mem/bm25.rs"
      provides: "Hand-rolled zero-dep BM25/keyword scorer"
      contains: "fn score"
    - path: "crates/pmcp-team-servers/src/mem/server.rs"
      provides: "mem-mcp Server builder mapping 6 mem__* tool names to backend calls"
      contains: "mem__search"
  key_links:
    - from: "src/mem/server.rs"
      to: "TeamMemoryBackend"
      via: "each mem__* tool handler calls one backend method"
      pattern: "mem__(add|get|search|list_recent|delete)"
    - from: "src/mem/backend.rs (in-memory impl)"
      to: "src/mem/bm25.rs"
      via: "mem__search ranks via the scorer"
      pattern: "score|bm25"
---

<objective>
Implement the mem-mcp reference server (TEAM-04): a `TeamMemoryBackend` object-safe async trait (6 `mem__*` operations) with an in-memory dev backend whose `mem__search` ranks by a hand-rolled, zero-dependency keyword/BM25 scorer (NO embedder dependency — TEAM-04 and milestone out-of-scope both forbid it; the `bm25` crate is explicitly rejected for bundling an embedder feature). Wire a `mem-mcp` Server builder (exactly 6 tools, unknown names error) and fill in the HTTP-first `mem-mcp` dev binary.

Purpose: Ship the memory reference server on the same trait-seam model as team-fs, with deterministic keyword ranking that is fully property-testable and dependency-light.
Output: `src/mem/{backend,bm25,server}.rs` + `src/bin/mem_mcp.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-team-reference-servers/109-CONTEXT.md
@.planning/phases/109-team-reference-servers/109-RESEARCH.md
@.planning/phases/109-team-reference-servers/109-PATTERNS.md

<interfaces>
From crates/pmcp-tasks/src/store/backend.rs (object-safe async trait + error enum + in-file exhaustive tests — same model as fs backend).

From crates/pmcp-agent/src/adapter/server.rs lines 211-258 (Server::builder + TypedTool + Arc-cloning closure handler).

From crates/pmcp-sql-server (thin bin + StreamableHttpServer serve pattern — identical shape to team-fs bin).

Contract (contracts/team-servers-v1.yaml `mem_tool_surface`): the 6 mem__* names are exactly
`mem__add mem__get mem__search mem__list_recent mem__delete mem__complete_task`.
`mem__complete_task` follows SEP-1686 (ToolOutput::Result surface).

NOTE: `src/mem/bm25.rs` has NO in-repo analog (109-PATTERNS.md "No Analog Found"). Hand-roll a ~80-line documented TF-IDF/BM25 scorer per RESEARCH "Don't Hand-Roll" guidance; prove via proptest invariants, not a codebase copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-rolled BM25/keyword scorer (zero-dep) + TeamMemoryBackend trait + in-memory backend</name>
  <files>crates/pmcp-team-servers/src/mem/bm25.rs, crates/pmcp-team-servers/src/mem/backend.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/mem/bm25.rs (stub from 109-01)
    - crates/pmcp-team-servers/src/mem/backend.rs (stub from 109-01)
    - crates/pmcp-tasks/src/store/backend.rs (trait + error enum + in-file test density to mirror)
    - .planning/phases/109-team-reference-servers/109-RESEARCH.md (Alternatives Considered: hand-roll recommended, bm25 crate rejected; TEAM-04 "no embedder")
  </read_first>
  <behavior>
    - tokenize lowercases + splits on non-alphanumeric; stopword handling is deterministic
    - score(query, doc) is 0 when no query term appears in doc; strictly positive when at least one does
    - a doc containing more of the query terms (higher term frequency, rarer terms) ranks above one containing fewer
    - scoring is deterministic: identical (corpus, query) inputs always produce identical rankings
    - adding an unrelated doc to the corpus does not change the relative order of two already-scored docs beyond documented idf effects
  </behavior>
  <action>
    In `src/mem/bm25.rs` implement a self-contained scorer: a `tokenize(&str) -> Vec<String>` (lowercase, split on non-alphanumeric), a corpus/index type holding per-document term frequencies + document lengths + a document-frequency map, and a `score(query_terms, doc_id) -> f64` implementing standard BM25 (k1≈1.2, b≈0.75, idf from doc-frequency over corpus size). No external crate — pure `std` + arithmetic. Document the formula and the k1/b constants. Add an in-file `#[cfg(test)] mod tests` with unit cases: empty query → all-zero; term-present ranks above term-absent; higher tf ranks higher.
    In `src/mem/backend.rs` define `#[async_trait] pub trait TeamMemoryBackend: Send + Sync` with the 6 ops: `add`, `get`, `search`, `list_recent`, `delete`, and a `complete_task` completion hook — each `Result<_, MemError>` with `# Errors` rustdoc. Define `pub enum MemError` via `thiserror` (`NotFound`, `InvalidArgs`). Define a `Memory` record type (`serde` derives: id, text, optional tags, created-at ordinal). Implement `pub struct InMemoryMemoryBackend` (e.g. `parking_lot::RwLock`/`Mutex` over a `Vec<Memory>` + the bm25 index) where `add` mints a uuid id and appends, `get`/`delete` by id, `list_recent` returns newest-first, and `search` builds/uses the bm25 index over stored memory text and returns ranked ids/records. In-file tests: add→get round-trip, delete removal, list_recent ordering, search returns the more-relevant memory first.
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers mem::bm25 mem::backend 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/mem/bm25.rs` contains a `fn score` and documents k1/b constants; no `use bm25`/`use tantivy` anywhere in the crate
    - `src/mem/backend.rs` contains `pub trait TeamMemoryBackend` with all 6 mem__* operations and `pub enum MemError`
    - a test asserts empty-query score is 0 and term-present ranks above term-absent
    - a test asserts mem__search returns the more-relevant memory ahead of a less-relevant one
    - `cargo test -p pmcp-team-servers mem::bm25 mem::backend` exits 0
  </acceptance_criteria>
  <done>Zero-dep BM25 scorer + TeamMemoryBackend trait + in-memory backend implemented and unit-tested; no embedder/registry search dependency.</done>
</task>

<task type="auto">
  <name>Task 2: mem-mcp Server builder (6 mem__* tools) + HTTP-first dev binary + BM25 proptest</name>
  <files>crates/pmcp-team-servers/src/mem/server.rs, crates/pmcp-team-servers/src/bin/mem_mcp.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/mem/backend.rs (TeamMemoryBackend from Task 1)
    - crates/pmcp-team-servers/src/mem/server.rs (stub from 109-01)
    - crates/pmcp-team-servers/src/fs/server.rs (sibling server-builder pattern from 109-02 if present; else pmcp-agent server builder)
    - crates/pmcp-sql-server/src/main.rs + src/lib.rs lines 156-164 (bin + serve)
  </read_first>
  <action>
    In `src/mem/server.rs` add `pub fn build_mem_mcp_server(backend: Arc<dyn TeamMemoryBackend>) -> pmcp::Result<Server>` registering exactly 6 `TypedTool`s named `mem__add … mem__complete_task`, each with an explicit input schema and a closure handler cloning the backend Arc and dispatching to the matching method. The fixed 6-tool set means unknown mem__* names are not advertised (pmcp returns "Tool 'name' not found", never panic). `mem__complete_task` returns via `ToolOutput::Result` if it carries a `related_task`. Keep the builder CLI-free.
    Overwrite `src/bin/mem_mcp.rs` with the same thin `#[tokio::main]` + clap `Args` shape as team-fs (`--package`, `--port`, `--data-dir`, `--stdio`): load the TeamPackage, construct the in-memory backend, build via `build_mem_mcp_server`, serve HTTP-by-default (`StreamableHttpServer::with_config`) with stdio fallback. Init `tracing-subscriber`.
    Add proptest invariants (in this crate's tests, e.g. `tests/mem_props.rs` or an in-`bm25.rs` proptest module — Claude's discretion): deterministic scoring (same inputs → same ranking) and monotonicity (adding a query term the doc contains never decreases its score relative to a doc that lacks it). This is the ALWAYS property requirement for mem-mcp.
  </action>
  <verify>
    <automated>cargo build -p pmcp-team-servers --features "mem-mcp http" --bin mem-mcp && cargo test -p pmcp-team-servers mem 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/mem/server.rs` contains `pub fn build_mem_mcp_server` registering all 6 mem__* tool names
    - a test builds the server and asserts `tools/list` returns exactly the 6 mem__* names
    - a proptest asserts deterministic BM25 ranking (identical inputs → identical order)
    - `cargo build -p pmcp-team-servers --features "mem-mcp http" --bin mem-mcp` exits 0
    - `src/bin/mem_mcp.rs` contains `--package`, `--stdio`, and a `StreamableHttpServer` serve path
  </acceptance_criteria>
  <done>mem-mcp advertises exactly 6 mem__* tools over the TeamMemoryBackend, ranks search via the property-tested zero-dep scorer, and runs as an HTTP-first dev binary with a stdio fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → mem__* tool args | Untrusted memory text/query strings cross into the in-memory store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-03-01 | Denial of Service | unbounded memory growth via mem__add | accept | Dev-grade in-memory backend, ephemeral per run, local trusted operator; no persistence; documented. |
| T-109-03-02 | Tampering | malformed/huge query into the scorer | mitigate | tokenize + score are total functions over strings; empty/huge inputs return 0 or bounded scores; proptest covers adversarial inputs (Task 2). |
| T-109-03-SC | Tampering | dependency graph | mitigate | No new registry packages — `bm25`/`tantivy` explicitly NOT adopted; scorer is hand-rolled zero-dep. |
</threat_model>

<verification>
- `cargo test -p pmcp-team-servers mem` green (scorer unit + property, backend round-trips, exact 6-tool surface)
- `cargo build -p pmcp-team-servers --features "mem-mcp http" --bin mem-mcp` succeeds
</verification>

<success_criteria>
- TEAM-04 satisfied: `mem__*` served over a `TeamMemoryBackend` trait with a keyword/BM25 in-memory dev backend and no embedder dependency
- Exact 6-tool surface; deterministic, property-tested ranking
- HTTP-first dev binary configured from a TeamPackage with stdio fallback
</success_criteria>

<output>
Create `.planning/phases/109-team-reference-servers/109-03-SUMMARY.md` when done
</output>
