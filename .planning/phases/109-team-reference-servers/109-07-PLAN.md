---
phase: 109-team-reference-servers
plan: 07
type: execute
wave: 3
depends_on: ["109-01", "109-02", "109-03", "109-04", "109-05"]
files_modified:
  - crates/pmcp-team-servers/src/conformance/runner.rs
  - crates/pmcp-team-servers/tests/conformance.rs
  - contracts/team-servers/fixtures/team-fs/
  - contracts/team-servers/fixtures/mem-mcp/
  - contracts/team-servers/fixtures/approval-mcp/
  - contracts/team-servers/fixtures/team-mcp/
autonomous: true
requirements: [TEAM-06]
must_haves:
  truths:
    - "The conformance runner drives any server through a real pmcp::Client over the in-memory transport: initialize → tools/list → tools/call per fixture"
    - "tools/list is asserted EXACT (advertised set + per-tool input-schema equality), not subset"
    - "Every advertised tool has ≥1 success fixture; every contract error path has an error fixture"
    - "team_mcp__member.success asserts top-level _meta.related_task is present"
    - "The runner is exportable behind the conformance feature for the platform to import as a dev-dependency"
  artifacts:
    - path: "crates/pmcp-team-servers/src/conformance/runner.rs"
      provides: "Exportable wire-level fixture runner"
      exports: ["run_fixtures", "ConformanceReport"]
    - path: "crates/pmcp-team-servers/tests/conformance.rs"
      provides: "Drives the runner against all four reference servers vs fixtures"
      contains: "run_fixtures"
    - path: "contracts/team-servers/fixtures/team-mcp/"
      provides: "Every-guard error fixtures + tools/list surface fixture"
      contains: "team_mcp__"
  key_links:
    - from: "src/conformance/runner.rs"
      to: "pmcp::Client over DuplexTransport"
      via: "initialize → tools/list → tools/call"
      pattern: "tools/list|call_tool"
    - from: "tests/conformance.rs"
      to: "contracts/team-servers/fixtures"
      via: "CARGO_MANIFEST_DIR-anchored fixture load"
      pattern: "CARGO_MANIFEST_DIR"
---

<objective>
Build the exportable wire-level conformance harness (TEAM-06, D-17/D-19): a fixture-driven runner that drives ANY server impl through a real `pmcp::Client` over the in-memory transport — `initialize` → `tools/list` (assert EXACT advertised set + per-tool input-schema equality — advertised==enforced, Pitfall 3) → `tools/call` per fixture (subset-match against `expect.response`, incl. `_meta[related_task]` and error codes). Expand the Phase-107 representative fixtures to EVERY tool + EVERY guard (D-20): ≥1 success dispatch per advertised tool, one error fixture per contract error path (unknown member, malformed/excessive depth, self-call, ancestor-cycle, invalid args), and exact `tools/list` surface fixtures for all four servers. Export the runner behind the `conformance` feature so the platform imports it as a dev-dependency and points it at its operated servers (or an HTTP endpoint).

Purpose: Prove "advertised == enforced" at the wire level for all four reference servers, and ship the runner the platform reuses.
Output: `src/conformance/runner.rs`, `tests/conformance.rs`, and expanded `contracts/team-servers/fixtures/**`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-team-reference-servers/109-CONTEXT.md
@.planning/phases/109-team-reference-servers/109-RESEARCH.md
@.planning/phases/109-team-reference-servers/109-PATTERNS.md

<interfaces>
From tests/team_contracts_conformance.rs (existing structural base — READ IN FULL):
- Fixture schema (frozen): `{ schema_version:"1", case_id, server, request{name, arguments, _meta}, expect{outcome, match, response} }`.
- outcome↔response-shape rule: `error` ⇒ `response.error.code` is a number; `success` ⇒ `response.content` is an array.
- `_meta[related_task]` presence check is the TEAM-05 fixture assertion.
- Fixtures anchored via `CARGO_MANIFEST_DIR`; STATIC_TOOL_NAMES / DYNAMIC_TOOL_PREFIXES / EXPECTED_SERVERS constants.

From tests/common/duplex.rs `call_via_server` (lines 86-100 — the wire-drive helper to promote into the runner): spawn server on one DuplexTransport half, `Client` on the other, `initialize(ClientCapabilities::default())`, then `client.list_tools()` / `client.call_tool(...)`.

From this crate: `transport::DuplexTransport::pair()` (109-01); the four server builders (`build_team_fs_server`, `build_mem_mcp_server`, `build_approval_mcp_server`, `build_team_mcp_server`) from 109-02..05.

Existing fixtures in contracts/team-servers/fixtures/: team-fs (fs__list, fs__complete_task, fs__write.invalid-args.error), mem-mcp (mem__add, mem__search), approval-mcp (get_approval, resolve_approval, team_approval__ask), team-mcp (member.success, unknown-member, self-call, malformed-depth, excessive-depth errors).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exportable wire-level conformance runner (initialize → exact tools/list → tools/call per fixture)</name>
  <files>crates/pmcp-team-servers/src/conformance/runner.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/conformance/runner.rs (stub from 109-01)
    - tests/team_contracts_conformance.rs (fixture loader lines 79-108, schema assertions 152-219, related_task check 258-269)
    - tests/common/duplex.rs lines 86-100 (call_via_server wire-drive pattern)
    - crates/pmcp-team-servers/src/transport.rs (DuplexTransport from 109-01)
  </read_first>
  <action>
    In `src/conformance/runner.rs` (gated by `#[cfg(feature = "conformance")]`, exported from lib per 109-01) implement `pub async fn run_fixtures(server: Server, fixtures_dir: &Path) -> ConformanceReport` and a `pub struct ConformanceReport { passed, failed, cases: Vec<CaseResult> }`. The runner: spawn `server.run(server_half)` over a `DuplexTransport::pair()`, connect a `Client` on the client half, `initialize`. Assert `tools/list` returns the EXACT advertised tool-name set expected for that server (equality, not subset — Pitfall 3: `resolve_approval`/`get_approval` unnamespaced, initiator never counted) AND per-tool input-schema equality against the fixtures' expected schema surface. Load fixtures via the frozen schema; for each fixture issue the `tools/call` (name + arguments + `_meta`) and subset-match the response against `expect.response`: for `success`, assert `content` is an array and any expected `_meta.related_task.taskId` is present; for `error`, assert an error outcome with a numeric code. Return the report; provide a `assert_conformant(report)` helper that panics with a readable diff (`pretty_assertions`) on any failure. Keep the runner dependency-light and free of test-only imports so the platform can import it as a dev-dependency and (per D-19) point the same drive path at an HTTP endpoint.
  </action>
  <verify>
    <automated>cargo build -p pmcp-team-servers --features conformance && cargo test -p pmcp-team-servers conformance::runner 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/conformance/runner.rs` exports `pub async fn run_fixtures` and `pub struct ConformanceReport`, gated behind `#[cfg(feature = "conformance")]`
    - the runner asserts `tools/list` EXACT equality (grep for an exact set comparison, not a subset/contains check for the tool-name set)
    - the runner checks `_meta.related_task.taskId` presence for success fixtures that expect it
    - `cargo build -p pmcp-team-servers --features conformance` exits 0
  </acceptance_criteria>
  <done>An exportable runner drives any server through a real Client, asserting exact tools/list and per-fixture tools/call outcomes including related_task and error codes.</done>
</task>

<task type="auto">
  <name>Task 2: Expand fixtures to every-tool + every-guard + drive all four servers (tests/conformance.rs)</name>
  <files>crates/pmcp-team-servers/tests/conformance.rs, contracts/team-servers/fixtures/team-fs/, contracts/team-servers/fixtures/mem-mcp/, contracts/team-servers/fixtures/approval-mcp/, contracts/team-servers/fixtures/team-mcp/</files>
  <read_first>
    - crates/pmcp-team-servers/src/conformance/runner.rs (from Task 1)
    - contracts/team-servers/fixtures/ (all four server dirs — existing representative fixtures)
    - contracts/team-servers-v1.yaml (the four equations — the exact advertised surfaces + error paths)
    - tests/team_contracts_conformance.rs (schema + coverage assertions to keep satisfied)
  </read_first>
  <action>
    Expand `contracts/team-servers/fixtures/` to D-20 coverage. For team-fs: add a success fixture for each of the 11 `fs__*` tools not yet covered, plus an exact `tools/list` surface fixture listing exactly the 11 names. For mem-mcp: a success fixture for each of the 6 `mem__*` tools + a surface fixture (exactly 6 names). For approval-mcp: success fixtures for `resolve_approval`, `get_approval`, and `team_approval__ask_<role>`, an unknown-member error fixture, and a surface fixture (resolve_approval + get_approval + one ask per configured role, unnamespaced statics). For team-mcp: keep member.success (asserting `_meta.related_task.taskId`) and the existing guard error fixtures (unknown-member, self-call, malformed-depth, excessive-depth), add an ancestor-cycle error fixture and an invalid-args error fixture, plus a per-request `tools/list` surface fixture. All fixtures use the frozen schema and are named per the existing convention (Claude's discretion on exact filenames). Fixtures stay canonical under `contracts/team-servers/fixtures/`.
    In `tests/conformance.rs` (anchor via `CARGO_MANIFEST_DIR`) build each of the four reference servers with a deterministic dev backend / FixedSource member and run `run_fixtures(server, fixtures_dir/<server>)` against its fixtures, asserting the report has zero failures. Include the every-tool + every-guard coverage assertions so a missing fixture fails the test.
  </action>
  <verify>
    <automated>cargo test -p pmcp-team-servers --test conformance --all-features 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - each server's fixture dir has ≥1 success fixture per advertised tool (11 fs, 6 mem, approval statics+ask) and an exact `tools/list` surface fixture
    - team-mcp fixtures include success (with related_task) + unknown-member + self-call + malformed-depth + excessive-depth + ancestor-cycle + invalid-args
    - `tests/conformance.rs` runs `run_fixtures` against all four servers and asserts zero failures
    - `cargo test -p pmcp-team-servers --test conformance --all-features` exits 0
    - `cargo test --test team_contracts_conformance` still exits 0 (structural base unbroken)
  </acceptance_criteria>
  <done>Fixtures cover every tool + every guard; the wire-level runner proves all four reference servers are conformant (advertised==enforced) at zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files → runner | Fixtures are trusted in-repo test data |
| exported runner → platform servers | Platform imports the runner as a dev-dependency to drive its own servers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-07-01 | Tampering | advertised≠enforced surface drift | mitigate | EXACT tools/list equality + per-tool input-schema equality + every-tool coverage fixtures make any surface drift a test failure (Task 1/2). |
| T-109-07-02 | Repudiation | a guard silently unenforced | mitigate | Every-guard error fixtures (self-call, cycle, depth, unknown-member, invalid-args) assert enforcement at the wire (Task 2). |
| T-109-07-SC | Tampering | dependency graph | mitigate | No new registry packages; `pretty_assertions` is an existing dev-dep; runner is dependency-light for platform import. |
</threat_model>

<verification>
- `cargo test -p pmcp-team-servers --test conformance --all-features` green (all four servers, zero failures)
- `cargo build -p pmcp-team-servers --features conformance` succeeds
- `cargo test --test team_contracts_conformance` still green
</verification>

<success_criteria>
- TEAM-06 satisfied: conformance tests prove each reference server's tool surface matches the PKG-03 contracts, at the wire level (D-19)
- Exportable runner (D-17) usable by the platform as a dev-dependency
- Every-tool + every-guard fixture coverage (D-20); exact tools/list surface asserted
</success_criteria>

<output>
Create `.planning/phases/109-team-reference-servers/109-07-SUMMARY.md` when done
</output>
