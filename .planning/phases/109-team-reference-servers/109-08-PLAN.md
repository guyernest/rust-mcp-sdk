---
phase: 109-team-reference-servers
plan: 08
type: execute
wave: 4
depends_on: ["109-02", "109-03", "109-04", "109-05", "109-06", "109-07"]
files_modified:
  - contracts/team-servers/binding.yaml
  - Makefile
  - crates/pmcp-team-servers/examples/doc_review_team.rs
autonomous: true
requirements: [TEAM-06]
must_haves:
  truths:
    - "contracts/team-servers/binding.yaml binds each team-servers-v1 equation to a concrete reference-server function"
    - "A `make comply` target runs pmat comply check for team-servers-v1 and is chained into quality-gate"
    - "The doc-review E2E example runs the full four-server flow on FixedSource and exits 0"
  artifacts:
    - path: "contracts/team-servers/binding.yaml"
      provides: "equation → reference-fn bindings for team-servers-v1 (D-18)"
      contains: "target_crate: pmcp-team-servers"
    - path: "crates/pmcp-team-servers/examples/doc_review_team.rs"
      provides: "doc-review E2E narrative across all four servers (D-16)"
      contains: "TeamRuntime"
  key_links:
    - from: "contracts/team-servers/binding.yaml"
      to: "pmcp_team_servers::* functions"
      via: "module_path + function per equation"
      pattern: "module_path: pmcp_team_servers"
    - from: "Makefile (comply target)"
      to: "quality-gate"
      via: "chained make invocation"
      pattern: "comply"
---

<objective>
Close the contract-first loop for team-servers (D-18): author `contracts/team-servers/binding.yaml` binding each `team-servers-v1.yaml` equation to a concrete reference-server function, and wire `pmat comply check` into the build via a `make comply` target chained into `quality-gate`. First run `pmat comply` against the EXISTING `contracts/binding.yaml` to learn the CLI + schema (Pitfall 2 — no build target runs pmat comply today; do not author bindings blind). Ship the doc-review E2E example (D-16): a narrative flow through all four servers on `FixedSource` for CI determinism.

Purpose: Operationalize the house contract-first rule for the new servers and deliver the BA-followable narrative demo (the ALWAYS runnable example).
Output: `contracts/team-servers/binding.yaml`, a `make comply` target, and `examples/doc_review_team.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-team-reference-servers/109-CONTEXT.md
@.planning/phases/109-team-reference-servers/109-RESEARCH.md
@.planning/phases/109-team-reference-servers/109-PATTERNS.md

<interfaces>
From contracts/binding.yaml (existing schema to mirror):
- top-level `version`, `target_crate`, `bindings[]` each `{ contract, equation, function, module_path, signature, status, notes }`, plus optional `critical_path[]`.

From contracts/team-servers-v1.yaml (rev'd to 1.1.0 in 109-01): equations `fs_tool_surface`, `mem_tool_surface`, `approval_tool_surface`, `team_dispatch_surface`.

From this crate's public functions (prior plans) to bind to:
- `pmcp_team_servers::fs::server::build_team_fs_server` (109-02)
- `pmcp_team_servers::mem::server::build_mem_mcp_server` (109-03)
- `pmcp_team_servers::approval::server::build_approval_mcp_server` (109-04)
- `pmcp_team_servers::team::server::build_team_mcp_server` (109-05)
- `pmcp_team_servers::compose::derive::derive_attachment` (109-01)
- `pmcp_team_servers::compose::wiring::TeamRuntime` (109-06)

From Makefile: `quality-gate:` (line 673) chains `fmt-check lint build test-all pmcp-package-gate audit unused-deps check-todos check-unwraps validate-always purity-check`; `pmat-quality` (line 775) shows the `@if command -v pmat &> /dev/null; then ... else <warn> fi` guard pattern to mirror.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe pmat comply schema, then author contracts/team-servers/binding.yaml</name>
  <files>contracts/team-servers/binding.yaml</files>
  <read_first>
    - contracts/binding.yaml (existing schema: version/target_crate/bindings[]/critical_path)
    - contracts/team-servers-v1.yaml (the four equations to bind)
    - .planning/phases/109-team-reference-servers/109-RESEARCH.md (Pitfall 2 + Open Question 1: probe pmat comply first)
  </read_first>
  <action>
    First learn the CLI + expected schema WITHOUT authoring blind: run `pmat comply --help` and `pmat comply check contracts/binding.yaml` (against the EXISTING binding) and record the accepted invocation + whether `function`/`module_path` resolve against source or a compiled crate. If `pmat` is absent or `comply` is not a subcommand, note it in the SUMMARY and proceed to author the binding matching the existing `contracts/binding.yaml` shape exactly (fallback).
    Author `contracts/team-servers/binding.yaml` with `version: 1.1.0`, `target_crate: pmcp-team-servers`, and one `bindings[]` entry per equation: `fs_tool_surface` → `pmcp_team_servers::fs::server::build_team_fs_server`; `mem_tool_surface` → `build_mem_mcp_server`; `approval_tool_surface` → `build_approval_mcp_server`; `team_dispatch_surface` → `build_team_mcp_server`. Each entry carries `{ contract: team-servers-v1.yaml, equation, function, module_path, signature, status: implemented, notes }` with the real signature from the reference server function and a note referencing the D-decision it satisfies. Optionally add a `derive_attachment` / `TeamRuntime` binding note documenting the composition-derivation rule. Keep the field names byte-for-byte compatible with the existing binding.yaml so `pmat comply` accepts it.
  </action>
  <verify>
    <automated>test -f contracts/team-servers/binding.yaml && grep -q "target_crate: pmcp-team-servers" contracts/team-servers/binding.yaml && grep -c "equation:" contracts/team-servers/binding.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/team-servers/binding.yaml` exists with `target_crate: pmcp-team-servers`
    - it contains a binding for each of the 4 equations (`fs_tool_surface`, `mem_tool_surface`, `approval_tool_surface`, `team_dispatch_surface`) with `module_path: pmcp_team_servers::...`
    - each binding has `contract: team-servers-v1.yaml`, `status`, `function`, `signature`, `notes`
    - the SUMMARY records the confirmed `pmat comply` invocation (or notes its absence)
  </acceptance_criteria>
  <done>binding.yaml authored to the confirmed pmat-comply schema, binding all four equations to concrete reference-server functions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `make comply` (pmat comply check) into quality-gate</name>
  <files>Makefile</files>
  <read_first>
    - Makefile lines 673-693 (quality-gate target) and lines 774-790 (pmat-quality guard pattern)
    - contracts/team-servers/binding.yaml (from Task 1)
  </read_first>
  <action>
    Add a `.PHONY: comply` target running `pmat comply check` for the team-servers binding (using the exact invocation confirmed in Task 1, e.g. `pmat comply check contracts/team-servers/binding.yaml`), guarded by the same `@if command -v pmat &> /dev/null; then ... else <warn> fi` pattern used by `pmat-quality` so a machine without `pmat` warns rather than hard-fails. Chain `$(MAKE) comply` into the `quality-gate` target (add the line alongside the existing `$(MAKE) validate-always` / `purity-check` steps). This closes the "DEFERRED to Phase 109" contract-metadata note (D-18). Do NOT weaken any existing quality-gate step.
  </action>
  <verify>
    <automated>grep -q "^comply:" Makefile && grep -q "comply" Makefile && make comply 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Makefile has a `comply:` target invoking `pmat comply check` on `contracts/team-servers/binding.yaml`
    - the `comply` target is guarded by a `command -v pmat` check (warns, does not hard-fail, when pmat absent)
    - `quality-gate` chains `$(MAKE) comply`
    - `make comply` exits 0 (either runs pmat comply, or warns pmat-absent) without breaking the gate
  </acceptance_criteria>
  <done>`make comply` runs pmat comply check for team-servers-v1 and is chained into quality-gate, guarded for pmat-absent environments.</done>
</task>

<task type="auto">
  <name>Task 3: Doc-review E2E example across all four servers (FixedSource)</name>
  <files>crates/pmcp-team-servers/examples/doc_review_team.rs</files>
  <read_first>
    - crates/pmcp-team-servers/src/compose/wiring.rs (TeamRuntime from 109-06)
    - crates/pmcp-team-servers/tests/small_team.rs (FixedSource TeamPackage construction from 109-06)
    - crates/pmcp-agent/src/adapter/factory.rs (FixedSourceFactory)
    - .planning/phases/109-team-reference-servers/109-CONTEXT.md (D-16 doc-review flow narrative)
  </read_first>
  <action>
    Create `examples/doc_review_team.rs`: a runnable, commented narrative (BA-followable) that, on `FixedSource` (CI-deterministic; real LLM optional), `TeamRuntime::start`s a 2-member team with a human role and team-fs + mem-mcp opt-ins, then walks the doc-review flow (D-16): member A drafts a file via team-fs → `fs__sync_to_review` → asks approval via `team_approval__ask_<role>` → resolve via `resolve_approval` (the human verdict) → member B reads the file and stores a summary in `mem__add` — the agent-facing dispatch going through `team_mcp__<member>` tools and surfacing `related_task` `_meta`. Print each step so a reader can follow the story. The example must exercise every contract surface (fs write/sync, approval ask/resolve, mem add, team dispatch with related_task) and exit 0 deterministically.
  </action>
  <verify>
    <automated>cargo run -p pmcp-team-servers --example doc_review_team --all-features 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p pmcp-team-servers --example doc_review_team --all-features` exits 0
    - the example drives team-fs write + sync_to_review, approval ask + resolve_approval, mem__add, and team_mcp__<member> dispatch
    - the example uses `FixedSourceFactory` (no live network/LLM) and prints the narrative steps
  </acceptance_criteria>
  <done>The doc-review E2E example runs the full four-server story deterministically on FixedSource and exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| binding.yaml → pmat comply | Trusted in-repo contract-binding metadata |
| example → in-process servers | FixedSource, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-08-01 | Repudiation | contract equations unbound / unverified | mitigate | binding.yaml binds every equation to a concrete function; `make comply` chained into quality-gate makes drift a gate failure (Task 1/2). |
| T-109-08-02 | Denial of Service | pmat absent breaks the gate | mitigate | `comply` target guarded by `command -v pmat` (warns, does not hard-fail) mirroring `pmat-quality` (Task 2). |
| T-109-08-SC | Tampering | dependency graph | mitigate | No new registry packages; example composes in-repo crates on FixedSource. |
</threat_model>

<verification>
- `contracts/team-servers/binding.yaml` binds all four equations; `make comply` runs (or warns) and is in quality-gate
- `cargo run -p pmcp-team-servers --example doc_review_team --all-features` exits 0
</verification>

<success_criteria>
- Contract-first loop closed (D-18): binding.yaml authored + pmat comply wired into quality-gate
- Doc-review E2E example (D-16) exercises every contract surface deterministically (ALWAYS runnable example)
</success_criteria>

<output>
Create `.planning/phases/109-team-reference-servers/109-08-SUMMARY.md` when done
</output>
