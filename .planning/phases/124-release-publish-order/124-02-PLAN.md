---
phase: 124-release-publish-order
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - Makefile
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs
  - cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs
  - crates/pmcp-package/tests/common/mod.rs
  - docs/design/package-portability-pmcp-run-handoff.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [PKGR-01]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "After the sync, `git merge-base --is-ancestor <upstream/main tip> HEAD` succeeds — the release PR will be diffed against the current main, not against the pre-squash merge base."
    - "After the sync, `RUSTFLAGS=\"\" make quality-gate` exits 0 — including the coverage gate and the guard self-test created in plan 01, both of which live in a file the merge conflicts on."
    - "After the sync, `make check-release-coverage-guard-selftest` still exits 0 and still reports 6 fixtures — the conflict resolution did not drop plan 01's Makefile target."
    - "No version literal in any Cargo.toml changed as part of the conflict resolution — the sync happens BEFORE any version bump so a resolution cannot silently revert one."
  artifacts:
    - "A merge commit (or equivalent) on feat/v2.6-package-portability carrying upstream/main's tip"
    - "A recorded `git merge-tree` conflict transcript in the plan SUMMARY, re-measured at execution time"
  key_links:
    - "Makefile is BOTH a measured conflict path and the file plan 01 just edited — the resolution must keep check-release-coverage-guard-selftest and its prerequisite edge"
    - "the two cargo-pmcp/src/deployment/targets/pmcp_run/graphql*.rs conflicts carry real behaviour from phase 123; they are not documentation"
    - "this sync must land before plan 05's version bumps (RESEARCH Pitfall 1): a conflict resolution that silently reverts a bumped manifest is the worst possible ordering"
  prohibitions:
    - "Must not resolve a conflict by discarding one side wholesale without reading both sides — every hunk gets an explicit resolution decision recorded in the SUMMARY."
    - "Must not bypass the project quality gate (for example `git commit --no-verify`) to land the merge."
    - "Must not report the sync as complete on the strength of a clean `git status`; the post-merge full quality gate is the evidence."
---

<objective>
Sync `main` into the milestone branch (D-08) before anything in this phase touches a version
number, resolving the conflict set that the squash-merge of PR #347 created.

Purpose: `main`'s tip is a **squash** of phases 120–122 taken from this very branch, so the
merge base is the pre-squash commit and the same content arrives from two directions. Left
un-synced, GitHub computes the release PR's diff from that old base and re-presents all of
phases 120–123 as if unmerged. Done AFTER the version bumps, a conflict resolution can
silently revert a bumped manifest — the worst possible ordering.
Output: a synced branch whose full local quality gate is green, with the conflict resolution
recorded hunk by hunk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-release-publish-order/124-CONTEXT.md
@.planning/phases/124-release-publish-order/124-RESEARCH.md
@.planning/phases/124-release-publish-order/124-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — plan 02)

This plan creates no new code symbols. It produces a git merge commit and a recorded
conflict-resolution transcript. Every file it touches already exists; the only additions it
must PRESERVE are plan 01's `check-release-coverage-guard-selftest` target and the
`check-release-coverage:` prerequisite edge in `Makefile`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the conflict set against the current main</name>
  <files>(no file writes — measurement only; output recorded in the SUMMARY)</files>
  <precondition>`git fetch upstream main` succeeds and `upstream` resolves to git@github.com:paiml/rust-mcp-sdk.git — the release PR targets paiml/rust-mcp-sdk, not the personal fork at `origin`. Halt and report if the remote is missing or the fetch fails.</precondition>
  <read_first>
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pitfall 1 — the measured 9-file conflict set, and the `git cat-file -p` evidence that main's tip is a squash with a single parent)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-08 — the PR vehicle is this branch as-is, `.planning/` commits included, no clean-branch cherry-pick of 239 commits)
  </read_first>
  <action>
RESEARCH's conflict measurement is explicitly stamped "valid ~3 days" because it was taken
against a moving `main`. Re-measure rather than trusting the recorded numbers.

Fetch `upstream` and establish, with absolute binary paths (`/opt/homebrew/bin/git`) so the
shell proxy cannot corrupt the evidence (RESEARCH Pitfall 4):

  - the ahead/behind counts between `HEAD` and `upstream/main`;
  - whether `upstream/main`'s tip is a squash — `git cat-file -p` on it, checking for a single
    `parent` line;
  - the merge base;
  - the conflict set, non-destructively, via `git merge-tree --write-tree --messages HEAD
    upstream/main`. This requires git >= 2.38; the local git is 2.47.1.

Classify each conflicted path into one of three buckets and record the classification:
  - **behavioural** — production source whose resolution changes what the code does. The
    previously measured members were `Makefile`,
    `cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs` and
    `cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs`.
  - **add/add** — the same new file created independently on both sides. Previously
    `crates/pmcp-package/tests/common/mod.rs` and
    `docs/design/package-portability-pmcp-run-handoff.md`.
  - **planning documents** — `.planning/*.md`. Resolvable by keeping this branch's content,
    which is strictly newer, but each still needs a read.

Do not perform the merge in this task. Its whole job is to produce the map the next task
resolves against, and to catch the case where `main` has moved far enough that the plan's
assumptions no longer hold. If the conflict set now includes a path outside the three buckets
above — in particular any `Cargo.toml`, `CHANGELOG.md`, `.github/workflows/release.yml`, or
`scripts/check-release-coverage.sh` — stop and report rather than proceeding: those are files
later plans in this phase own, and a conflict there changes the phase's ordering.
  </action>
  <verify>
    <automated>/opt/homebrew/bin/git merge-tree --write-tree --messages HEAD upstream/main > /tmp/124-merge-tree.txt; /usr/bin/grep -c 'CONFLICT' /tmp/124-merge-tree.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/124-merge-tree.txt` exists and the conflict count is recorded in the SUMMARY as a number, alongside the full list of conflicted paths
    - The SUMMARY records the ahead/behind counts and states explicitly whether `upstream/main`'s tip has one parent (squash) or two (merge)
    - Every conflicted path is classified as behavioural, add/add, or planning document
    - `/usr/bin/grep -c 'Cargo.toml' /tmp/124-merge-tree.txt` returns 0; if it does not, the task halts and reports instead of continuing
    - `/usr/bin/grep -c 'check-release-coverage' /tmp/124-merge-tree.txt` returns 0; if it does not, the task halts and reports
  </acceptance_criteria>
  <done>The current conflict set is measured non-destructively, classified, and checked against the paths later plans own — with an explicit halt if the shape has changed.</done>
</task>

<task type="auto">
  <name>Task 2: Perform the sync, resolve every conflict, prove the tree with the full gate</name>
  <files>Makefile, cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs, cargo-pmcp/src/deployment/targets/pmcp_run/graphql_contract.rs, crates/pmcp-package/tests/common/mod.rs, docs/design/package-portability-pmcp-run-handoff.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/WINDOWS.md</files>
  <read_first>
    - /tmp/124-merge-tree.txt (Task 1's measured conflict set — resolve against the current measurement, not against RESEARCH's recorded one)
    - Makefile (the current file, including plan 01's check-release-coverage-guard-selftest target and the `check-release-coverage:` prerequisite edge — both must survive)
    - cargo-pmcp/src/deployment/targets/pmcp_run/graphql.rs and graphql_contract.rs (both sides of each conflicted hunk; these carry phase 123's save/load/pull seam work)
    - crates/pmcp-package/tests/common/mod.rs (the add/add conflict — read both versions before choosing)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pitfall 1's "how to avoid" guidance and Pitfall 4's absolute-binary-path rule)
  </read_first>
  <action>
Merge `upstream/main` into `feat/v2.6-package-portability` and resolve every conflict
explicitly. This is the branch that becomes the release PR (D-08): the vehicle is this branch
as-is with `.planning/` commits included, following the v2.17 / PR #302 precedent — there is
no clean-branch cherry-pick of the ~240 commits.

Resolution rules by bucket:

  - **Planning documents** (`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`,
    `.planning/STATE.md`, `.planning/WINDOWS.md`): this branch's content is strictly newer —
    `main` carries the squashed 120–122 snapshot, this branch carries 120–123 plus this
    phase. Keep this branch's version, but read each conflicted hunk first and confirm the
    incoming side introduces nothing this branch lacks. Record any hunk where the incoming
    side had unique content.
  - **add/add** (`crates/pmcp-package/tests/common/mod.rs`,
    `docs/design/package-portability-pmcp-run-handoff.md`): read both versions in full.
    These are the same file authored twice — once here, once in the squash. Keep the version
    that is a superset; if neither is, merge the content by hand and say so.
  - **Behavioural source** (`Makefile`, the two `pmcp_run/graphql*.rs` files): resolve hunk by
    hunk with both sides read. For `Makefile`, the resolution MUST retain plan 01's
    `check-release-coverage-guard-selftest` target, its fixture table comment, and the
    `check-release-coverage: check-release-coverage-guard-selftest` prerequisite line — a
    "take theirs" on this file silently deletes the gate this phase exists to build. For the
    two graphql files, the incoming side is the squashed 120–122 content and this branch's
    side additionally carries phase 123's work; keep phase 123's behaviour and fold in any
    incoming change that is genuinely new.

Do not touch any `Cargo.toml` version literal during the resolution. If the merge presents
one, stop — that means the conflict set changed shape since Task 1 and the phase ordering
needs revisiting.

Use absolute binary paths for any command whose output is load-bearing evidence
(`/opt/homebrew/bin/git`, `/usr/bin/grep`) — the shell proxy has been measured corrupting
`git diff` and inverse greps.

After committing the merge, run the full local gate with `RUSTFLAGS=""` (RUSTFLAGS is
exported in CI and absent locally, which is recorded local-gate blind spot #9), and run
`rustup update stable` first per the project Pre-Flight Checklist so the local toolchain
matches CI's `dtolnay/rust-toolchain@stable`.

Record in the SUMMARY: one line per conflicted file stating which side won and why, plus the
post-merge gate transcript.
  </action>
  <verify>
    <automated>RUSTFLAGS="" make quality-gate</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTFLAGS="" make quality-gate` exits 0
    - `make check-release-coverage-guard-selftest` exits 0 and reports 6 fixtures (plan 01's target survived the Makefile resolution)
    - `/usr/bin/grep -c 'check-release-coverage: check-release-coverage-guard-selftest' Makefile` returns 1
    - `/opt/homebrew/bin/git merge-base --is-ancestor upstream/main HEAD` exits 0
    - `/opt/homebrew/bin/git status --porcelain` produces no output (no unresolved paths, no stray files)
    - `/opt/homebrew/bin/git diff --name-only upstream/main...HEAD > /tmp/124-pr-files.txt` then `/usr/bin/grep -c 'Cargo.toml' /tmp/124-pr-files.txt` returns 0 for any version-line change — confirm by `/opt/homebrew/bin/git diff upstream/main...HEAD -- '*Cargo.toml' > /tmp/124-manifest-diff.txt` and `/usr/bin/grep -c '^[+-]version = ' /tmp/124-manifest-diff.txt` returning 0
    - `/usr/bin/grep -c '<<<<<<<' Makefile` returns 0, and the same check returns 0 for both `pmcp_run/graphql*.rs` files
    - The SUMMARY carries one resolution line per conflicted file naming the winning side and the reason
  </acceptance_criteria>
  <done>The milestone branch contains `upstream/main`'s tip, every conflict is resolved with a recorded rationale, no version literal moved, and the full local quality gate is green.</done>
  <reversibility rating="costly">A squash-merge of this branch means future branches must be cut from `upstream/main` or the v2.4 squash-divergence trap repeats. The mitigation is procedural, not structural — record it in the SUMMARY so the next branch author inherits it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream/main -> working tree | Content arriving from a squash commit of this branch's own work; identical-looking hunks from two directions is exactly the shape that makes a careless resolution silently drop work. |
| conflict resolution -> release payload | Whatever survives this merge is what the tag publishes. A dropped hunk here becomes a shipped defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-05 | Tampering | `main` sync conflict resolution | high | mitigate | Non-destructive `git merge-tree` re-measure first (Task 1); per-hunk resolution with both sides read; explicit acceptance criteria that plan 01's Makefile target and prerequisite edge survive; full `RUSTFLAGS="" make quality-gate` after (Task 2) |
| T-124-10 | Tampering | version literals in manifests | high | mitigate | The sync is sequenced BEFORE plan 05's bumps, and a diff assertion proves no `version = ` line moved during the resolution (Task 2 acceptance criteria) |
| T-124-11 | Repudiation | the sync's own evidence | medium | mitigate | Absolute binary paths for every evidence-producing command; the measured conflict transcript is written to a file and counted, not eyeballed |
</threat_model>

<verification>
- Conflict set re-measured non-destructively and classified before any merge is attempted
- Every conflicted path resolved with a recorded rationale
- `upstream/main` is an ancestor of `HEAD`
- No `version = ` line changed in the merge
- `RUSTFLAGS="" make quality-gate` green, and plan 01's gate + self-test intact
</verification>

<success_criteria>
- The release PR opened in plan 06 will diff against the current `upstream/main`, showing phase 123 + phase 124 rather than all of 120–124
- No version bump in this phase can be silently reverted by a later conflict resolution, because the resolution has already happened
</success_criteria>

<output>
Create `.planning/phases/124-release-publish-order/124-02-SUMMARY.md` when done
</output>
