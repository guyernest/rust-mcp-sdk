---
phase: 124-release-publish-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-release-coverage.sh
  - Makefile
autonomous: true
requirements: [PKGR-01]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Running scripts/check-release-coverage.sh against a copy of release.yml whose `cargo publish --manifest-path crates/pmcp-package/Cargo.toml` line has been deleted exits NON-ZERO and names pmcp-package in its output (D-02 / SC2)."
    - "Running `make check-release-coverage` against the real .github/workflows/release.yml exits 0 and its final line reports a member total that is one higher than the pre-phase total, because the workspace-excluded crate is now counted (D-01)."
    - "Running the gate against a copy of release.yml in which pmcp-package's publish command appears AFTER cargo-pmcp's exits NON-ZERO (D-10 / SC4)."
    - "Running the gate against a copy of release.yml in which pmcp-package's publish command line is commented out exits NON-ZERO — the comment-strip discipline covers the new matcher as well as the old one."
    - "`make check-release-coverage` executes check-release-coverage-guard-selftest first, as a declared prerequisite, so the gate is never trusted before its own red direction is proven."
    - "The excluded-crate discovery is a filesystem scan over crates/*/Cargo.toml and contains no hand-written list of crate names or paths (D-01)."
  artifacts:
    - scripts/check-release-coverage.sh (extended in place — root loop, excluded loop, order assertion, one shared reporting block)
    - Makefile target check-release-coverage-guard-selftest
    - Makefile prerequisite edge `check-release-coverage: check-release-coverage-guard-selftest`
  key_links:
    - "discovery scan path spelling (crates/<name>/Cargo.toml) MUST be the same string the --manifest-path matcher searches for — derive the matcher argument from the scan variable, never re-spell it"
    - "the excluded loop MUST feed the SAME total / missing_count / missing_list as the root loop, so both halves report through one block"
    - "make quality-gate (Makefile:1486) -> make check-release-coverage -> the script; .github/workflows/ci.yml:233 calls the same make target, so CI inherits the extension with no workflow edit"
    - "the order assertion reads PUBLISH_LINES (the comment-stripped text) and derives ordinals from that same text — never from grep -n over the raw file"
  prohibitions:
    - "Must not weaken, shorten or delete any of the gate's four documented disciplines (explicit cargo/jq failure arms, empty-list-is-failure, comment stripping before matching, here-string matching instead of a pipe into grep -q) in order to make the extension simpler."
    - "Must not introduce a hand-maintained allowlist or hard-coded list of workspace-excluded crate names or paths — the discovery stays a scan (D-01)."
    - "Must not report coverage that was not actually measured: any reading of zero (zero publishable members, zero excluded manifests, a step that cannot be located) is an explicit failure, never a pass."
    - "Must not bypass the project quality gate (for example `git commit --no-verify`) to land any change in this phase."
---

<objective>
Close the coverage gate's structural blind spot: teach `scripts/check-release-coverage.sh`
to see workspace-**excluded** publishable crates (today it cannot see `crates/pmcp-package`
at all), assert the `pmcp-package`-precedes-its-consumers publish order mechanically, and
prove the red direction with a permanent fixture-driven self-test declared as a prerequisite
of the gate it proves.

Purpose: PKGR-01's residual work (a). Today the gate affirmatively prints full coverage
while the crate this milestone bumps has no publish step at all — measured, exit 0. Every
later plan in this phase depends on a gate that can actually see what it claims to check.
Output: an extended gate script, a `check-release-coverage-guard-selftest` Make target with
six fixtures and a fixture-count assertion, and a prerequisite edge wiring it ahead of the
gate in both `make quality-gate` and CI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-release-publish-order/124-CONTEXT.md
@.planning/phases/124-release-publish-order/124-RESEARCH.md
@.planning/phases/124-release-publish-order/124-PATTERNS.md
@scripts/check-release-coverage.sh
@Makefile
</context>

<flagged_assumptions>
## Unresolved edge-coverage row (surfaced, not dismissed)

The spec-less probe fallback classified this phase's single requirement row as
**unclassified → unresolved**:

| requirement_id | category | status | probe |
|---|---|---|---|
| PKGR-01 | unclassified | **unresolved** | unclassified — review manually |

**What that means here, concretely.** PKGR-01's requirement text in
`.planning/REQUIREMENTS.md:37` ("`pmcp-openapi-server` is added to CLAUDE.md's publish
order. It is absent today (zero occurrences)") is *superseded* by the measured-drift
correction block immediately below it at `:42-49`. The original premise is already closed
on this branch. No automated classifier can reconcile a requirement whose own body is
contradicted by its own correction block, which is why the row stays unresolved rather than
being auto-resolved into a truth.

**Disposition for execution:** treat the correction block (`:42-49`) as the requirement,
never the bullet at `:37`. SC1 is therefore a **re-verification with a recorded transcript**,
not new work — the gate already exits 0 and `pmcp-openapi-server` already holds slot 9b.
This assumption is carried openly rather than silently resolved; if the re-verification in
Task 1 finds the premise has drifted again, stop and report rather than adapting the plan.
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — plan 01)

Created by this plan and therefore expected to be absent from the pre-phase source:

- `scripts/check-release-coverage.sh`: shell locals `excluded_seen`, `EX_META`, `EX_NAME`,
  loop variable `m`; shell function `step_line`; local `pkg_line`, `cl`, `consumer`.
- `Makefile`: target `check-release-coverage-guard-selftest`; recipe-local shell function
  `check`; recipe-local variables `fail`, `ran`, `tmp`, `fixture`, `expected`, `doctored`,
  `actual`; the prerequisite edge on `check-release-coverage`.
- No new committed fixture files — fixtures are doctored copies made in a `mktemp -d`
  scratch directory and removed by the recipe.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — the gate sees crates/pmcp-package, and its red direction is proven</name>
  <files>scripts/check-release-coverage.sh, Makefile</files>
  <read_first>
    - scripts/check-release-coverage.sh (all 89 lines — the four disciplines in the header at :18-29 are load-bearing and each was earned by a real failure)
    - .planning/phases/124-release-publish-order/124-PATTERNS.md (section "scripts/check-release-coverage.sh" — the exact blocks to mirror, and the Makefile self-test analog)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 3, and the "Extending the gate: the complete new block, bash-3.2 safe" code example — prototyped and executed against this tree)
    - Makefile lines 592-627 (test-openapi-server-guard-selftest) and 1375-1414 (no-crypto-allowlist-guard-selftest + the `no-crypto-check:` prerequisite edge) — the two in-repo precedents
    - Makefile lines 883-896 (lint-plans, check-release-coverage) and 1479-1490 (the quality-gate chain)
    - .github/workflows/release.yml lines 440-455 (the pmcp-package publish step — the exact literal the new matcher must hit)
  </read_first>
  <action>
This is the phase's tracer: ONE thin path wired through every layer this phase touches —
discovery, classification, matcher, reporting, exit code, Make chain, quality-gate — for the
single crate `pmcp-package`, with a real end-to-end verification. Production quality, not a
prototype: it ships as-is and later tasks expand outward from it.

Step A — extend `scripts/check-release-coverage.sh` in place. Do not rewrite the file and do
not restructure the existing root-member loop. Insert a second discovery loop AFTER the
existing `while ... done <<<"$PUBLISHABLE"` block (which ends at :78) and BEFORE the
`if [ "$missing_count" -gt 0 ]` reporting block (:80), so both halves report through the one
existing block. The new loop:

  - iterates `for m in crates/*/Cargo.toml`, skips non-files, and skips any manifest that
    does not match `^\[workspace\]` — that regex is the whole of the discovery rule (D-01).
    There is no list of names and no list of paths.
  - increments a counter `excluded_seen` for every manifest that passes the discovery test.
  - classifies via `cargo metadata --no-deps --format-version 1 --manifest-path "$m"` into
    `EX_META`, then `jq -r '.packages[] | select(.publish == null) | .name'` into `EX_NAME`.
    Each of those two invocations gets its own explicit failure arm in the same shape as the
    existing arms at :35-45 — a `::error::` line naming the manifest and stating that
    coverage was NOT checked, then `exit 1`. Reuse the `.publish == null` predicate exactly:
    it is the same rule the root half uses, so a filesystem heuristic can never disagree with
    Cargo about what "publishable" means. (Measured: `crates/pmcp-package/Cargo.toml:20` says
    `publish = true` and Cargo reports `publish=null` for it, so the predicate transfers.)
  - `continue`s when `EX_NAME` is empty (a publish-restricted crate is correctly not this
    gate's problem).
  - increments the shared `total`, then matches with a here-string against `$PUBLISH_LINES`
    — never a pipe into `grep -q`, for the SIGPIPE reason documented at :65-72. The matcher
    is the `--manifest-path` form, not the `-p <name>` form: the crate NAME never appears in
    that command. Search for `cargo publish --manifest-path ${m}` with a trailing-space
    boundary, deriving `${m}` from the loop variable so the scan and the matcher cannot
    disagree about the path spelling (Pitfall 3). A miss appends to the shared
    `missing_list` with a message that names `EX_NAME` and states that the crate is
    workspace-excluded and needs a `--manifest-path` step.
  - after the loop, an explicit failure when `excluded_seen` is 0, in the same shape and for
    the same reason as the empty-`PUBLISHABLE` arm at :47-51: this repo has had at least one
    workspace-excluded publishable crate since Phase 108, so zero hits means the glob or the
    working directory is wrong, not that coverage holds.

Constraints that bind every line you add: bash 3.2 only (no `mapfile`, no process
substitution, no empty-array expansion under `set -u`); `set -euo pipefail` is already in
force at :30; every tool invocation gets an explicit failure arm.

Step B — add the Make target `check-release-coverage-guard-selftest` following the shape of
`no-crypto-allowlist-guard-selftest` (Makefile:1375-1400): a `$(BLUE)` announce line, then a
single recipe line initialising `fail=0; ran=0;` and a shell function `check`. Because this
gate's logic is not extracted into a separate awk file, `check` cannot feed inline fixtures
to an extractor the way both precedents do. Instead it takes a fixture name, an expected
outcome and a path to a doctored workflow copy, runs `./scripts/check-release-coverage.sh
"$$doctored"` with `set +e` around it, captures the exit status and the combined output into
files under a `mktemp -d` scratch directory, increments `ran`, and sets `fail=1` with a
`$(RED)✗` message when the outcome does not match. Assert on **exit status** (zero versus
non-zero) plus, for the red fixtures, that the string `pmcp-package` appears in the captured
output — never on the full message text, so a wording change does not break the self-test.

For this task wire exactly TWO fixtures, which is the tracer's single path proven in both
directions:
  - `intact` — an unmodified copy of `.github/workflows/release.yml`, expected exit 0.
  - `pmcp_package_step_removed` — the same copy with the line containing
    `cargo publish --manifest-path crates/pmcp-package/Cargo.toml` deleted, expected non-zero.
    Build it with `grep -v` inside the recipe (Make recipes run under `/bin/sh` directly, so
    the shell-proxy corruption recorded in RESEARCH Pitfall 4 does not apply here).
End the recipe with the `fail` check, then a fixture-count assertion `if [ "$$ran" -ne 2 ]`
carrying the "a fixture was lost" message, then a `$(GREEN)✓` success line. Clean up the
scratch directory. Add the `.PHONY:` line immediately above the target, per house style.

Step C — declare the self-test a prerequisite of the gate it proves, exactly as
`no-crypto-check: no-crypto-allowlist-guard-selftest` does at Makefile:1414 and
`test-openapi-server: test-openapi-server-guard-selftest` does at Makefile:684. Change the
`check-release-coverage:` target line to carry `check-release-coverage-guard-selftest` as its
prerequisite. This satisfies D-02's same-commit registration rule automatically, because
`check-release-coverage` is already invoked by `quality-gate` at Makefile:1486 and by
`.github/workflows/ci.yml:233` — neither of those files needs an edit.

Step D — record the SC1 re-verification transcript in the plan summary: the gate's exit
status and final line against the real workflow, plus the occurrence counts of
`pmcp-openapi-server` in `CLAUDE.md` and in `.github/workflows/release.yml`. SC1 is a
re-verification on this branch, not new work; if either count is zero, stop and report
rather than adapting.
  </action>
  <verify>
    <automated>make check-release-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `make check-release-coverage` exits 0, and its output contains the line printed by check-release-coverage-guard-selftest before the line printed by the gate itself (prerequisite ordering is observable)
    - `./scripts/check-release-coverage.sh` exits 0 and its final line reports a member total of 25 (24 root members plus the one workspace-excluded crate); if the root count has moved, the total must be exactly one higher than a run of the pre-phase script
    - `/usr/bin/grep -c 'excluded_seen' scripts/check-release-coverage.sh` returns at least 3 (initialiser, increment, zero-check)
    - `/usr/bin/grep -c 'crates/\*/Cargo.toml' scripts/check-release-coverage.sh` returns at least 1
    - `/usr/bin/grep -c 'pmcp-package' scripts/check-release-coverage.sh` returns 0 outside comment lines — verified with `/usr/bin/grep -v '^[[:space:]]*#' scripts/check-release-coverage.sh > /tmp/124-gate-code.txt` then `/usr/bin/grep -c 'pmcp-package' /tmp/124-gate-code.txt` returning 0 (D-01: no crate name is hard-coded in executable code)
    - `make check-release-coverage-guard-selftest` exits 0 and reports 2 fixtures
    - `/usr/bin/grep -n 'check-release-coverage: check-release-coverage-guard-selftest' Makefile` returns exactly one line
    - `/usr/bin/grep -c 'mapfile' scripts/check-release-coverage.sh` returns 0 (bash 3.2 constraint preserved)
    - `/usr/bin/grep -c 'grep -q' scripts/check-release-coverage.sh` returns 0 for any occurrence preceded by a pipe — verified by `/usr/bin/grep -c "| grep -q" scripts/check-release-coverage.sh` returning 0 (here-string discipline preserved)
    - The four discipline bullets in the script header at :18-29 are still present verbatim: `/usr/bin/grep -c 'Failure discipline' scripts/check-release-coverage.sh` returns 1
    - SC1 transcript recorded in the plan SUMMARY: gate exit status, gate final line, and non-zero occurrence counts of `pmcp-openapi-server` in both ledgers
  </acceptance_criteria>
  <done>The single path — discover crates/pmcp-package by scan, classify it with Cargo's own predicate, match its --manifest-path publish step, report through the shared block, exit accordingly — works end to end, is exercised by a Make self-test in both directions, and runs inside `make quality-gate` and CI with no further wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Machine-check the publish ORDER for the pmcp-package cluster (D-10)</name>
  <files>scripts/check-release-coverage.sh</files>
  <read_first>
    - scripts/check-release-coverage.sh (as extended by Task 1 — in particular where PUBLISH_LINES is computed at :56 and where the reporting block sits)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pattern 3 — the measured step line numbers and the step_line prototype, including why the not-found arms must be explicit)
    - .github/workflows/release.yml lines 426-540 (the ordering comment block and the five publish steps of the cluster)
    - CLAUDE.md, "Release & Publish Workflow" item 13's ⚠ ORDERING CONSTRAINT FOR PHASE 124 — the type-crossing inventory that explains why the order is load-bearing rather than cosmetic
  </read_first>
  <action>
Add a bounded order assertion to `scripts/check-release-coverage.sh`, placed after the
existing reporting block so a missing step is reported as missing before it is reported as
mis-ordered.

Define a shell function `step_line` taking one argument — an exact publish-command fragment
— which prints the 1-based ordinal of that fragment's first occurrence within
`$PUBLISH_LINES`. It must read the **comment-stripped** text and derive its ordinal from that
same text, so a prose comment naming a crate above a step can never shift the reading. Use a
fixed-string search (`grep -nF`) for the fragment, since a manifest path contains `.` and `/`
which are regex-live.

Resolve `pkg_line` from the fragment `cargo publish --manifest-path crates/pmcp-package/Cargo.toml`.
An empty result is an explicit `::error::` failure naming the step that could not be located,
then `exit 1` — a `head -1` into empty silently yielding an empty string and then failing an
arithmetic comparison is still a failure, but an unreadable one, and this script's stated
discipline is that a gate which cannot see must say so.

Then loop over the four consumers `pmcp-cfn-renderer`, `pmcp-agent`, `pmcp-team-servers`,
`cargo-pmcp`, resolving each from the fragment `cargo publish -p <consumer>`, with the same
explicit not-found failure arm. When `pkg_line` is greater than or equal to a consumer's
ordinal, emit a `::error::` explaining that the consumer pins `pmcp-package` so `cargo
publish` would fail with a no-matching-package error, then `exit 1`.

Scope is deliberately the cluster only. Do not build a general topological order model of all
25 crates — that is explicitly deferred by CONTEXT.

Measured today (RESEARCH Pattern 3): the ordinals are pmcp-package 440, pmcp-cfn-renderer
466, pmcp-agent 484, pmcp-team-servers 499, cargo-pmcp 520 in the RAW file, so the assertion
passes on the current workflow. Those raw numbers are context, not values to hard-code — the
assertion computes its own ordinals from the stripped text at run time.
  </action>
  <verify>
    <automated>make check-release-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `make check-release-coverage` still exits 0 on the real workflow
    - `/usr/bin/grep -c 'step_line' scripts/check-release-coverage.sh` returns at least 4 (definition plus the pmcp-package resolution plus the consumer resolution plus at least one comment reference)
    - `/usr/bin/grep -c 'grep -nF' scripts/check-release-coverage.sh` returns at least 1 (fixed-string search for the path fragment)
    - All four consumer names appear in the script: `/usr/bin/grep -c 'pmcp-cfn-renderer' scripts/check-release-coverage.sh`, `... 'pmcp-agent' ...`, `... 'pmcp-team-servers' ...` and `... 'cargo-pmcp' ...` each return at least 1
    - Behavioural red check, run by hand and recorded in the SUMMARY: copy `.github/workflows/release.yml` to a scratch file, delete the pmcp-package publish command line from it and append that same line at end of file, then run `./scripts/check-release-coverage.sh <scratch>` — it exits non-zero and its output names `pmcp-package`
    - `./scripts/check-release-coverage.sh /nonexistent/release.yml` exits non-zero (the pre-existing file-exists guard at :33 still fires)
  </acceptance_criteria>
  <done>`pmcp-package`'s publish step is machine-checked to precede all four of its in-repo consumers' publish steps, the check reads the comment-stripped text, and both "step not found" directions fail explicitly rather than silently.</done>
  <reversibility rating="reversible">A gate assertion in a repo script; deleting it restores the prior behaviour with no external consequence.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Expand the self-test to six fixtures and retire the gate's stale header note</name>
  <files>Makefile, scripts/check-release-coverage.sh</files>
  <read_first>
    - Makefile (the check-release-coverage-guard-selftest target as created in Task 1)
    - Makefile lines 1351-1400 — the fixture-table-in-a-comment convention: each fixture names the failure mode it pins, and the harness carries a fixture-count assertion
    - Makefile lines 592-627 — the second precedent, including its own fixture-count assertion
    - scripts/check-release-coverage.sh header lines 1-30 (as extended by Tasks 1 and 2)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pattern 2's six-fixture table, and Open Question 3 on doctoring a real copy rather than using inline fixtures)
  </read_first>
  <action>
Step A — grow `check-release-coverage-guard-selftest` from two fixtures to six, and raise the
fixture-count assertion from 2 to 6. Above the target, add a fixture-table comment block in
the shape of Makefile:1351-1374 — one line per fixture naming the failure mode it pins, not
merely what it does. The six:

  - `intact` — unmodified copy, expect exit 0. Pins: the gate still passes on good input, so
    the extension introduced no false red.
  - `excluded_step_removed` — the `cargo publish --manifest-path crates/pmcp-package/Cargo.toml`
    line deleted, expect non-zero. Pins the headline blind spot. This is the fixture that
    matters most: before this phase the same input produced a full-coverage report and exit 0.
  - `root_step_removed` — a root member's `cargo publish -p pmcp-widget-utils` line deleted,
    expect non-zero. Pins: the original half still works after the edit.
  - `excluded_step_commented` — the `--manifest-path` publish line prefixed with a `#`, expect
    non-zero. Pins: the comment-strip discipline extends to the new matcher, so a
    commented-out step never counts as coverage.
  - `order_inverted` — the `--manifest-path` publish line deleted from its position and
    appended at end of file, expect non-zero. Pins: the D-10 assertion is live, not
    decorative.
  - `workflow_absent` — a path that does not exist, expect non-zero. Pins: the pre-existing
    file-exists guard survives the extension.

Keep the assertion shape from Task 1 — exit status plus, for the red fixtures that name a
crate, the crate name appearing in captured output. `workflow_absent` asserts on exit status
only. Build every doctored copy inside the `mktemp -d` scratch directory with `grep -v`,
`sed` and `cat` — no committed fixture files.

Step B — rewrite the three-line header paragraph currently at
`scripts/check-release-coverage.sh:14-16`, the one that declares workspace-excluded crates
uncovered and names this phase as the future fix. Replace it with a paragraph documenting the
now-implemented behaviour: two discovery sources (root `cargo metadata --no-deps` for members,
a scan of `crates/*/Cargo.toml` for manifests carrying their own `[workspace]` table), one
shared classification predicate (`.publish == null`, from Cargo itself, in both halves), two
matcher forms (`-p <name>` for members, `--manifest-path <path>` for excluded crates because
the crate name never appears in that command), and the bounded cluster order assertion. Add a
fifth bullet to the "Failure discipline" list at :18-29 stating that a zero-result scan is a
failure for the same reason a zero-length member list is. Do not shorten or reword the four
existing bullets.

Step C — run the full local gate to confirm nothing downstream regressed.
  </action>
  <verify>
    <automated>make check-release-coverage-guard-selftest</automated>
  </verify>
  <acceptance_criteria>
    - `make check-release-coverage-guard-selftest` exits 0 and its success line reports 6 fixtures
    - Falsifiability check, recorded in the SUMMARY: temporarily delete one `check` invocation from the recipe and confirm the target exits non-zero with the "a fixture was lost" message, then restore it
    - `/usr/bin/grep -c 'BLIND SPOT' scripts/check-release-coverage.sh` returns 0
    - `/usr/bin/grep -c 'Phase 124' scripts/check-release-coverage.sh` returns 0 (the header no longer defers work to this phase)
    - `/usr/bin/grep -c 'Failure discipline' scripts/check-release-coverage.sh` returns 1 and the bullet list below it has five `# - ` entries: `/usr/bin/sed -n '18,34p' scripts/check-release-coverage.sh > /tmp/124-gate-header.txt` then `/usr/bin/grep -c '^# - ' /tmp/124-gate-header.txt` returns 5
    - `/usr/bin/grep -c 'order_inverted' Makefile` returns at least 2 (the fixture-table comment and the check invocation)
    - `/usr/bin/grep -c 'ran" -ne 6' Makefile` returns at least 2 (this target plus the two pre-existing precedents keep their own counts; the new one is present)
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>The gate's red direction is permanently proven across six failure modes with a fixture-count assertion, the header documents what the script now does instead of what it cannot do, and the whole local quality gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files -> gate script | `.github/workflows/release.yml` and `crates/*/Cargo.toml` are parsed as gate input. Trusted in-repo files, but the comment-stripping rule is a genuine validation control: it prevents a *commented* publish step from counting as coverage. |
| gate verdict -> merge decision | `ci.yml:233` feeds `gate.needs`, so this script's exit status is a merge-blocking authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-01 | Repudiation | scripts/check-release-coverage.sh (extended) | high | mitigate | Empty-scan-is-failure arm; explicit `::error::` + exit 1 arms on every `cargo metadata` and `jq` invocation; six-fixture red-direction self-test declared as a prerequisite of the gate (Task 1 Step C, Task 3 Step A) |
| T-124-02 | Denial (of the release) | release.yml publish ledger | high | mitigate | Two-source discovery so a workspace-excluded publishable crate cannot silently lack a publish step — the `pmcp-tasks` failure class, one layer up (Task 1 Step A) |
| T-124-03 | Tampering (semantic) | published `cargo-pmcp` resolving two semver-incompatible `pmcp-package` copies | high | mitigate | D-10 line-ordinal order assertion bounded to the cluster (Task 2), composed with the one-set version rule and the two existing pin tripwires enforced in plan 05 |
| T-124-09 | Spoofing (of coverage) | the `--manifest-path` matcher | medium | mitigate | Matcher argument derived from the discovery loop variable, so scan and matcher cannot disagree about path spelling; fixed-string search for the path portion; `excluded_step_commented` fixture pins the comment-strip path (Task 1 Step A, Task 3 Step A) |
</threat_model>

<verification>
- `make check-release-coverage` exits 0 on the real workflow and runs the self-test first
- `make check-release-coverage-guard-selftest` exits 0 across 6 fixtures
- `RUSTFLAGS="" make quality-gate` exits 0
- No crate name or path is hard-coded in the script's executable (non-comment) lines
- The script's four original discipline bullets survive verbatim; a fifth is added
</verification>

<success_criteria>
- SC2 is proven by execution and stays proven: deleting `crates/pmcp-package`'s publish step from `release.yml` makes the gate fail, asserted on every future `make quality-gate` run rather than demonstrated once
- SC4's mechanical half is live: `pmcp-package`'s publish step is asserted to precede all four of its consumers'
- SC1 is re-verified on this branch with a recorded transcript
- The gate remains bash-3.2 clean and keeps every documented failure discipline
</success_criteria>

<output>
Create `.planning/phases/124-release-publish-order/124-01-SUMMARY.md` when done
</output>
