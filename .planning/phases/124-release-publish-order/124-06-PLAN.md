---
phase: 124-release-publish-order
plan: 06
type: execute
wave: 6
depends_on: ["124-05"]
files_modified: []
autonomous: false
requirements: [PKGR-01]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A pull request exists from `feat/v2.6-package-portability` to `main` on paiml/rust-mcp-sdk, and its changed-file list contains phase 123's and phase 124's work rather than all of phases 120-124 — which is the observable signature of the plan-02 sync having held."
    - "Every required CI check on that PR is green, including the aggregate `gate` job, whose `needs:` includes the quality-gate job that runs `make check-release-coverage` — so the extended gate blocks the merge if it fails."
    - "The merge is performed by the user, not by an agent, and is confirmed live on `main` before this plan closes."
  artifacts:
    - "An open (then merged) PR against paiml/rust-mcp-sdk"
    - "A recorded CI check transcript taken from the GitHub API, not from a terminal-rendered summary"
  key_links:
    - "the local gate and the CI quality-gate job call the SAME `make check-release-coverage`, so plan 01's extension is already enforced on this PR with no workflow edit"
    - "the PR's file count is the tripwire for the plan-02 sync: a diff showing all of phases 120-124 means GitHub is computing from the pre-squash merge base"
    - "the tag in plan 07 must be pushed to a merged main, not to this branch — CLAUDE.md's Release Steps sequence the tag after the merge"
  prohibitions:
    - "Must not merge the pull request. The merge is a user-fired action; an agent may open the PR, watch CI and report, and nothing more."
    - "Must not force-push, rebase or otherwise rewrite the branch once the PR is open — the PR is the release vehicle and its history is the record."
    - "Must not report CI as green from a rendered summary; read the check-run states from the GitHub API, because the shell proxy has been measured corrupting terminal-rendered check output on this repo."
    - "Must not bypass the project quality gate (for example `git commit --no-verify`) to land any change in this phase."
---

<objective>
Open the release pull request and get it merged, with CI proving the extended coverage gate on
the way in.

Purpose: D-08 makes the milestone branch itself the release vehicle — `feat/v2.6-package-portability`
straight to `main` with `.planning/` commits included, the v2.17 / PR #302 precedent, and no
clean-branch cherry-pick of roughly 240 commits. D-07 makes the merge a human checkpoint,
because everything downstream of it is one-way.
Output: a merged `main` carrying the whole v2.6 milestone plus this phase's release hygiene,
with a recorded CI transcript.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-release-publish-order/124-CONTEXT.md
@.planning/phases/124-release-publish-order/124-RESEARCH.md
@.planning/phases/124-release-publish-order/124-02-SUMMARY.md
@.planning/phases/124-release-publish-order/124-05-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — plan 06)

No code symbols and no file writes. This plan produces a GitHub pull request against
paiml/rust-mcp-sdk and a recorded CI check transcript, both of which live outside the working
tree.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Open the release PR and drive CI to green</name>
  <files>(no file writes — git and gh operations only)</files>
  <precondition>The `gh` CLI is authenticated against github.com with permission to open a pull request on paiml/rust-mcp-sdk, and `upstream` resolves to that repository. Halt and report if `gh auth status` fails or the remote is wrong — the PR must target the upstream repo, not the personal fork at `origin`.</precondition>
  <read_first>
    - CLAUDE.md, "Release Steps" and "Pre-Flight Checklist" (the toolchain-update step, and the `gh pr create --repo paiml/rust-mcp-sdk --head <fork>:<branch> --base main` form)
    - .planning/phases/124-release-publish-order/124-02-SUMMARY.md (what the sync resolved, so the PR description can state it and so the file-count tripwire has a baseline)
    - .planning/phases/124-release-publish-order/124-05-SUMMARY.md (the exact set of crates and versions this release ships — the PR body should name them)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pitfall 4 on evidence corruption, and Pitfall 8 on the MCP-registry job)
    - .github/workflows/ci.yml (the `gate` aggregate job's `needs:` array, and the quality-gate job that runs `make check-release-coverage` at :233)
  </read_first>
  <action>
Pre-flight first, in this order, because a local/CI toolchain mismatch is this project's
recorded number-one cause of CI failures: run `rustup update stable`, confirm the local
`rustc` version, then run `RUSTFLAGS="" make quality-gate` one final time on the branch as it
stands. `RUSTFLAGS` is exported in CI and absent locally, which is why the explicit empty
assignment is part of the command rather than an afterthought.

Push the branch to `origin` and open the pull request against `paiml/rust-mcp-sdk`'s `main`
with `gh pr create`. Write a body that states, concretely:
  - what this PR ships: the v2.6 AI-Package Portability milestone (phases 120–124) plus this
    phase's release hygiene;
  - the exact `(crate, version)` list being released, taken from plan 05's summary;
  - that the coverage gate now covers workspace-excluded publishable crates and that the
    `pmcp-package` publish order is machine-checked;
  - that `main` was synced into the branch first, naming the conflict count resolved;
  - that the tag will be pushed separately after merge, by the user.

Then verify the sync held. Compare the PR's changed-file list against the branch's own diff
versus `upstream/main`. A PR presenting all of phases 120–124 rather than 123–124 means
GitHub is computing the diff from the pre-squash merge base and the sync did not take — stop
and report rather than merging a mis-based PR.

Watch CI to completion. Read check-run states from the GitHub API rather than from a rendered
terminal summary: the shell proxy has been measured corrupting that output on this repo, and a
release decision must not rest on corrupted evidence. Confirm specifically that the aggregate
`gate` job is green and that the quality-gate job — which runs the same
`make check-release-coverage` this phase extended — passed.

If a check fails, fix forward on the branch and re-push. Do not force-push or rebase: the PR
is the release vehicle and its history is the record.

Note for reading the results: the `publish-mcp` job in `release.yml` is a *release-time* job,
not a CI job, and its OIDC publish has been broken since v2.17. It does not appear on this PR
and must not be confused with a CI failure here.
  </action>
  <verify>
    <automated>gh pr checks --repo paiml/rust-mcp-sdk $(gh pr view --repo paiml/rust-mcp-sdk --json number --jq .number) --required</automated>
  </verify>
  <acceptance_criteria>
    - A PR exists on paiml/rust-mcp-sdk from this branch to `main`; its number and URL are recorded in the SUMMARY
    - Every required check reports success, read from the GitHub API check-runs endpoint and reproduced in the SUMMARY as a name/state list
    - The aggregate `gate` job is present in that list and green
    - The PR's changed-file count matches `/opt/homebrew/bin/git diff --name-only upstream/main...HEAD > /tmp/124-pr-files.txt` followed by `/usr/bin/wc -l /tmp/124-pr-files.txt`, and the SUMMARY states both numbers
    - `/usr/bin/grep -c 'phases/120' /tmp/124-pr-files.txt` and `/usr/bin/grep -c 'phases/121' /tmp/124-pr-files.txt` are consistent with a synced branch — if either is large, the sync did not hold and the task halts
    - `RUSTFLAGS="" make quality-gate` exited 0 on the final pre-push tree, with the transcript recorded
    - The local `rustc --version` is recorded alongside the CI toolchain the workflow resolves
  </acceptance_criteria>
  <done>The release PR is open against the upstream repository, its diff is based on the current `main`, every required check including the extended coverage gate is green, and the evidence is read from the API rather than from rendered output.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: HUMAN ACTION — merge the release pull request</name>
  <files>(no file writes)</files>
  <instructions>
Already done by the agent, and reported in the Task 1 summary:
  - `rustup update stable` and a final `RUSTFLAGS="" make quality-gate` on the branch
  - branch pushed to `origin`, PR opened against `paiml/rust-mcp-sdk`'s `main`
  - CI driven to green; the required-check list, including the aggregate `gate` job, read
    from the GitHub API check-runs endpoint and reproduced verbatim
  - the PR's changed-file list compared against the branch's own diff versus `upstream/main`,
    confirming the plan-02 sync held and the diff is not being computed from the pre-squash
    merge base

The ONE thing requiring you:
  **Merge the PR** (number and URL in the Task 1 summary).

Before you do, note what the merge puts on the publication path — the exact
`(crate, version)` list from plan 05's summary is reproduced with this checkpoint. The merge
itself is not the one-way door; the tag push in plan 07 is. But nothing between here and
there will re-ask which crates are shipping.

If you squash-merge: that is the project's normal style and it is fine, but it permanently
changes the base every future branch must be cut from. New branches must then be cut from
`upstream/main`, or the squash-divergence trap this project has already paid for repeats —
it is what made plan 02's sync a multi-file conflict rather than a fast-forward. Tell the
agent which style you used so it goes into the record.
  </instructions>
  <verification>
After you report the merge, the agent verifies it independently rather than accepting the
report: fetch `upstream`, confirm the PR head is reachable from `upstream/main` (or, for a
squash, that `upstream/main` carries plan 05's bumped version literals), and record the merge
commit SHA and merge style.
  </verification>
  <resume-signal>Reply "merged" plus the merge style (squash or merge commit), or describe what blocked it.</resume-signal>
  <read_first>
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-07: merge and tag-push are human checkpoints, the user fires them; D-08's reversibility note on the squash-divergence trap)
    - The Task 1 SUMMARY entry (PR number, URL, check-run list)
  </read_first>
  <action>
Merge the release pull request on paiml/rust-mcp-sdk. Everything automatable is already done
by Task 1; the merge itself is what D-07 reserves to the user, and an agent must not perform
it.

Present, concisely:
  - the PR number and URL;
  - the required-check list with every state, so the user is confirming a fact rather than
    trusting a claim;
  - the exact `(crate, version)` list this merge puts on the path to publication;
  - what happens next: the user pushes the tag in plan 07, which triggers `release.yml`, which
    publishes to crates.io — permanently.

State the consequence of the merge style plainly. A squash-merge means future branches must be
cut from `upstream/main` or the squash-divergence trap this project has already hit repeats —
the same shape that made plan 02's sync a nine-file conflict. If the user squash-merges, that
follow-on rule goes into the SUMMARY as a standing instruction for the next branch author.

After the user reports the merge, verify it independently rather than accepting the report:
fetch `upstream` and confirm the PR's head commit is reachable from `upstream/main`, and
confirm the merged `main` carries the bumped version literals from plan 05. Record the merge
commit and its style (squash or merge) in the SUMMARY.

Do not proceed to plan 07 until the merge is confirmed live on `upstream/main`.
  </action>
  <verify>
    <human-check>The user has merged the pull request and confirmed it.</human-check>
    <automated>/opt/homebrew/bin/git fetch upstream main; /opt/homebrew/bin/git merge-base --is-ancestor HEAD upstream/main</automated>
  </verify>
  <acceptance_criteria>
    - `/opt/homebrew/bin/git merge-base --is-ancestor HEAD upstream/main` exits 0, OR — if the merge was a squash — the SUMMARY records the squash commit SHA and shows that `upstream/main` carries the bumped version literals: `/opt/homebrew/bin/git show upstream/main:Cargo.toml > /tmp/124-main-cargo.toml` then `/usr/bin/grep -c '^version = "2.19.1"' /tmp/124-main-cargo.toml` returns 1
    - The merge commit SHA and merge style are recorded in the SUMMARY
    - If the merge was a squash, the SUMMARY carries the standing instruction that new branches must be cut from `upstream/main`
    - No agent-performed merge appears in the repository history
  </acceptance_criteria>
  <done>The release is on `upstream/main`, merged by the user, and independently verified rather than taken on report.</done>
  <reversibility rating="costly">A merge to `main` is revertible in git, but a squash-merge permanently changes the base every future branch must be cut from, and this project has already paid for getting that wrong once. The merge itself is not the one-way door — the tag push in plan 07 is.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch -> upstream main | Content crosses from a personal fork into the published project's default branch, from which the release tag is cut. |
| CI verdict -> merge authority | `gate` is the org-required status check; its `needs:` array is what makes the extended coverage gate merge-blocking. |
| rendered CLI output -> release decision | Measured on this repo: the shell proxy corrupts terminal-rendered check output. The API is the oracle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-04 | Elevation of Privilege | who may put code on the publication path | medium | mitigate | The merge is a blocking human checkpoint; the plan's prohibitions forbid an agent-performed merge and the acceptance criteria assert none appears in history (Task 2) |
| T-124-11 | Repudiation | CI evidence | medium | mitigate | Check-run states read from the GitHub API and reproduced in the SUMMARY as a name/state list, never from a rendered summary (Task 1) |
| T-124-05 | Tampering | a mis-based PR re-presenting phases 120-122 | high | mitigate | The changed-file list is compared against the branch's own diff versus `upstream/main`, and a mismatch halts the task rather than merging (Task 1) |
| T-124-17 | Denial | misreading the known-broken MCP-registry job as a CI failure | low | accept | The job is release-time, not CI, and does not appear on this PR; the plan names it so the reader does not conflate the two. Accepted rather than mitigated because fixing the OIDC publish is explicitly deferred out of this phase and must not gate crates.io publication. |
</threat_model>

<verification>
- PR open against paiml/rust-mcp-sdk from this branch to `main`
- Required checks green, read from the API, including the aggregate `gate` job
- PR diff based on the current `upstream/main`, not the pre-squash base
- Merge performed by the user and verified live on `upstream/main`
</verification>

<success_criteria>
- The whole v2.6 milestone plus this phase's release hygiene is on `main`, having passed the extended coverage gate as a merge-blocking check
- Nothing irreversible has happened yet — the tag is still unpushed
</success_criteria>

<output>
Create `.planning/phases/124-release-publish-order/124-06-SUMMARY.md` when done
</output>
