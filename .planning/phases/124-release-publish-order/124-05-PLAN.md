---
phase: 124-release-publish-order
plan: 05
type: execute
wave: 5
depends_on: ["124-03", "124-04"]
files_modified:
  - Cargo.toml
  - CHANGELOG.md
  - crates/pmcp-package/Cargo.toml
  - cargo-pmcp/Cargo.toml
  - cargo-pmcp/src/templates/agent.rs
  - cargo-pmcp/tests/pmcp_package_pin.rs
  - crates/pmcp-agent/Cargo.toml
  - crates/pmcp-team-servers/Cargo.toml
  - crates/pmcp-cfn-renderer/Cargo.toml
  - crates/pmcp-openapi-server/tests/pmcp_package_pin.rs
  - crates/pmcp-code-mode-derive/Cargo.toml
  - crates/pmcp-workbook-runtime/Cargo.toml
  - crates/pmcp-workbook-compiler/Cargo.toml
autonomous: true
requirements: [PKGR-01]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The root `pmcp` crate's version is 2.19.1, one patch above the version currently published on crates.io (D-03)."
    - "Exactly the set of `(crate, target version)` pairs recorded at plan 03's checkpoint has been bumped — no crate outside that list has a changed version literal."
    - "If `pmcp-package`'s version moved, every one of its nine version emitters moved in the SAME commit: the crate's own `[package].version`, the four consuming manifests, `PMCP_PACKAGE_VERSION_REQ` in cargo-pmcp's agent template, and the two pin-tripwire constants."
    - "`cargo test -p cargo-pmcp --test pmcp_package_pin` passes, and the cargo-pmcp library test asserting the scaffold's emitted pmcp-package requirement matches the workspace version passes — that emitter is invisible to `cargo build`, so a green build does not cover it."
    - "`crates/pmcp-openapi-server`'s `pmcp-package` dev-dependency is still path-only with no version key — that crate publishes several steps before pmcp-package, and a retained version requirement cannot resolve at publish time."
    - "Running `.github/workflows/release.yml`'s own changelog-extraction awk for the shipped version against CHANGELOG.md yields NON-EMPTY output."
  artifacts:
    - "CHANGELOG.md section `## [2.19.1] - <date>`"
    - "Bumped version literals in exactly the manifests the checkpoint authorised"
  key_links:
    - "the CHANGELOG heading form `## [X.Y.Z] - YYYY-MM-DD` is load-bearing: release.yml awk-matches `## \\[<version-without-v>\\]` and yields an empty string, exit 0, on no match — the GitHub Release is then created with empty notes, silently"
    - "pmcp-package's version <-> four consumer manifest pins <-> PMCP_PACKAGE_VERSION_REQ <-> the two pin-tripwire constants: nine emitters, one set, one commit"
    - "the tag chosen in D-06 (v2.19.1) must equal the version in the CHANGELOG heading, or the extraction misses"
  prohibitions:
    - "Must not bump any crate that was not explicitly authorised at plan 03's checkpoint, and must not skip one that was. Every published version number is consumed permanently."
    - "Must not bump only part of a version-emitter set. One emitter — the scaffold template constant — is invisible to `cargo build`; a partial bump can commit with a green build and fail only in a library test."
    - "Must not add a `version` key to `crates/pmcp-openapi-server`'s `pmcp-package` dev-dependency while editing pins elsewhere; it must remain path-only."
    - "Must not add a crypto or signing dependency to `crates/pmcp-package` while editing its manifest — that crate's no-crypto boundary is machine-checked and must stay clean."
    - "Must not bypass the project quality gate (for example `git commit --no-verify`) to land any change in this phase."
---

<objective>
Consume exactly the version numbers plan 03's checkpoint authorised, move every emitter of a
bumped version as one set, and write the CHANGELOG section without which the GitHub Release is
created with empty notes.

Purpose: this is the point of no return's last reversible step. The bumps decided here are what
the tag publishes; a missed emitter ships a CLI pinned to a package version it cannot read, and
a missed CHANGELOG heading silently empties the release notes.
Output: bumped manifests, a synchronised emitter set, and a `## [2.19.1]` section proven
non-empty by re-running the workflow's own extraction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-release-publish-order/124-CONTEXT.md
@.planning/phases/124-release-publish-order/124-RESEARCH.md
@.planning/phases/124-release-publish-order/124-PATTERNS.md
@.planning/phases/124-release-publish-order/124-03-SUMMARY.md
@.planning/phases/124-release-publish-order/124-04-SUMMARY.md
@CHANGELOG.md
@Cargo.toml
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — plan 05)

- `CHANGELOG.md`: a new `## [2.19.1] - <date>` heading and its section body.
- No new code symbols. The constants touched (`EXPECTED_PIN` in
  `cargo-pmcp/tests/pmcp_package_pin.rs`, `EXPECTED_VERSION_LINE` in
  `crates/pmcp-openapi-server/tests/pmcp_package_pin.rs`, `PMCP_PACKAGE_VERSION_REQ` in
  `cargo-pmcp/src/templates/agent.rs`) all already exist; this plan changes their values only
  if the checkpoint authorised a `pmcp-package` bump.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump the pmcp core to 2.19.1 and every other crate the checkpoint authorised</name>
  <files>Cargo.toml, crates/pmcp-code-mode-derive/Cargo.toml, crates/pmcp-workbook-runtime/Cargo.toml, crates/pmcp-workbook-compiler/Cargo.toml</files>
  <read_first>
    - .planning/phases/124-release-publish-order/124-03-SUMMARY.md (the closed list of `(crate, target version)` pairs this plan is authorised to bump — this is the ONLY source for what moves)
    - Cargo.toml (the root manifest: the `pmcp` `version` line, and the `jsonwebtoken` requirement whose in-tree move D-03 ships)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-03, D-05, D-06)
    - CLAUDE.md, "Version Bump Rules" — a downstream crate that pins a bumped dependency must also be bumped
  </read_first>
  <action>
Bump the root `pmcp` crate from its current version to 2.19.1 (D-03). The change being shipped
is the in-tree `jsonwebtoken` requirement move that landed under an already-published 2.19.0 —
exactly the phantom-delta class this phase exists to catch. The patch axis is justified by
plan 03's public-API guard, which must have come back with zero `jsonwebtoken` occurrences; if
it did not, plan 03's checkpoint will have recorded a revised decision and this task follows
that instead.

Then bump each additional crate on plan 03's authorised list to its recorded target version,
and only those. Read that list as a closed set: a crate not on it does not move, and a crate on
it is not skipped. RESEARCH's prior measurement suggested candidates
(`pmcp-code-mode-derive`, `pmcp-workbook-runtime`, `pmcp-workbook-compiler`) but the authority
is the checkpoint record, not the research table.

For each crate bumped, apply CLAUDE.md's Version Bump Rules: any crate in this workspace that
pins the bumped crate by version must also be bumped in the same change. Determine those by
searching the workspace manifests for a requirement on the bumped crate's name; do not rely on
recollection.

Confirm the semver axis for each bumped crate with `cargo semver-checks check-release` against
its published baseline where the tool supports the crate, and record the verdict. For a
pre-1.0 crate gaining public API, the crate-family precedent in this repo is a minor bump, not
a patch — but the axis for each crate is whatever the checkpoint recorded.

Do not touch `crates/pmcp-package`'s version in this task; that is Task 2's audit-driven
decision and it carries a nine-emitter obligation.
  </action>
  <verify>
    <automated>cargo metadata --no-deps --format-version 1 --offline > /tmp/124-meta.json; /usr/bin/grep -c '"version":"2.19.1"' /tmp/124-meta.json</automated>
  </verify>
  <acceptance_criteria>
    - The root manifest's version line reads 2.19.1: `/usr/bin/grep -c '^version = "2.19.1"' Cargo.toml` returns 1
    - `/opt/homebrew/bin/git diff --unified=0 -- '*Cargo.toml' > /tmp/124-bump-diff.txt` then `/usr/bin/grep -c '^+version = ' /tmp/124-bump-diff.txt` equals the number of crates on plan 03's authorised list, excluding pmcp-package
    - Every crate name appearing as a changed version line in `/tmp/124-bump-diff.txt` is present on plan 03's authorised list — checked crate by crate and recorded in the SUMMARY
    - `cargo semver-checks check-release --baseline-version <published pmcp version>` exits 0 for `pmcp` and the verdict is recorded
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>Exactly the authorised crates carry their authorised new versions, the semver axis of each is confirmed rather than assumed, and the workspace still builds and passes every gate.</done>
  <reversibility rating="costly">A version literal is trivially revertible in the repo, but once the tag publishes it the number is consumed on crates.io forever. It is only reversible while it is still un-tagged — which is why the deciding checkpoint sits in plan 03, upstream of this task.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Audit pmcp-package (D-04) and, if it moves, move all nine emitters in one commit</name>
  <files>crates/pmcp-package/Cargo.toml, cargo-pmcp/Cargo.toml, cargo-pmcp/src/templates/agent.rs, cargo-pmcp/tests/pmcp_package_pin.rs, crates/pmcp-agent/Cargo.toml, crates/pmcp-team-servers/Cargo.toml, crates/pmcp-cfn-renderer/Cargo.toml, crates/pmcp-openapi-server/tests/pmcp_package_pin.rs</files>
  <read_first>
    - .planning/phases/122-attestation-carriage-contract-first-parked-on-the-pmcp-run-b/122-08-SUMMARY.md (the full nine-emitter inventory with each emitter's guard — this is D-04's sweep checklist)
    - crates/pmcp-package/Cargo.toml (its own `[package].version`, and the `publish = true` / own `[workspace]` table that makes it workspace-excluded)
    - cargo-pmcp/Cargo.toml (the `pmcp-package` requirement, around :88), crates/pmcp-agent/Cargo.toml (around :18), crates/pmcp-team-servers/Cargo.toml (around :24), crates/pmcp-cfn-renderer/Cargo.toml (around :10)
    - cargo-pmcp/src/templates/agent.rs (the `PMCP_PACKAGE_VERSION_REQ` constant at :67 — the compiler-invisible ninth emitter; its drift test is its only tripwire)
    - cargo-pmcp/tests/pmcp_package_pin.rs (`EXPECTED_PIN` at :39) and crates/pmcp-openapi-server/tests/pmcp_package_pin.rs (`EXPECTED_VERSION_LINE` at :89, `EXPECTED_DEP_PATH` at :92, and `pmcp_package_dev_dep_is_path_only`)
    - CLAUDE.md item 13 (the ⚠ ORDERING CONSTRAINT and the one-set rule) and item 9b (the CR-01 path-only constraint on the openapi-server dev-dep)
    - .planning/phases/124-release-publish-order/124-CONTEXT.md (D-04 — the decision procedure, not a locked number)
  </read_first>
  <action>
D-04 encodes a decision *procedure*, not a version. Execute the procedure, then act on what it
returns.

Step A — audit. Diff `crates/pmcp-package` since the commit that set its current version
(`6430afae` set 0.3.0; confirm that is still the version-setting commit rather than assuming
it). Classify every added line: a change confined to doc comments, tests and fixtures means
ship the current version as-is; a source-visible additive API change means a minor bump on the
pre-1.0 axis, per this crate's own Phase-122 precedent. Note that a second commit
(`85ee222f`) also appears in the crate's path log — determine whether it actually touched
crate source rather than inferring from the path. Record the audit's evidence, not just its
verdict: which files changed, and what the added non-doc lines were.

Step B — if the audit says ship as-is, change nothing in this task beyond recording the
finding, and confirm all nine emitters are already mutually consistent.

Step C — if the audit says bump, move ALL NINE emitters in this one commit:
  1. `crates/pmcp-package/Cargo.toml`'s own `[package].version`
  2. `cargo-pmcp/Cargo.toml`'s `pmcp-package` requirement
  3. `crates/pmcp-agent/Cargo.toml`'s `pmcp-package` requirement
  4. `crates/pmcp-team-servers/Cargo.toml`'s `pmcp-package` requirement
  5. `crates/pmcp-cfn-renderer/Cargo.toml`'s `pmcp-package` requirement
  6. `PMCP_PACKAGE_VERSION_REQ` in `cargo-pmcp/src/templates/agent.rs`
  7. `EXPECTED_PIN` in `cargo-pmcp/tests/pmcp_package_pin.rs`
  8. `EXPECTED_VERSION_LINE` in `crates/pmcp-openapi-server/tests/pmcp_package_pin.rs`
  9. `cargo-pmcp`'s own `[package].version`, because it pins a bumped dependency (Version
     Bump Rules), together with `pmcp-agent`, `pmcp-team-servers` and `pmcp-cfn-renderer` if
     the pin move is semver-incompatible
Four of these fail `cargo build` if left behind and two fail `cargo test`, but
`PMCP_PACKAGE_VERSION_REQ` is invisible to the compiler — it is emitted into projects created
by the scaffold — so a partial bump can commit with a green build and fail only in a
`cargo-pmcp` library test. Treat that emitter as the one most likely to be missed.

Step D — regardless of the verdict, verify the CR-01 constraint is intact:
`crates/pmcp-openapi-server`'s `pmcp-package` dev-dependency must carry a `path` and NO
`version` key, because that crate publishes several steps before `pmcp-package` and Cargo
retains a dev-dep in the published manifest whenever it carries a version requirement. Do not
"restore a pin" there while editing pins elsewhere.

Step E — verify the crate's no-crypto boundary is unaffected by any manifest edit.

Record in the SUMMARY: the audit verdict with its evidence, the emitter table with each one's
before/after value and the guard that covers it, and the CR-01 confirmation.
  </action>
  <verify>
    <automated>cargo test -p cargo-pmcp --test pmcp_package_pin</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cargo-pmcp --test pmcp_package_pin` exits 0
    - `cargo test -p cargo-pmcp --lib emitted_package_requirement_matches_workspace` exits 0 and reports a NON-ZERO number of tests run — a filter that selects zero tests passes vacuously
    - `make test-openapi-server` exits 0 (this is the harness that runs `pmcp_package_dev_dep_is_path_only`)
    - `/usr/bin/grep -n 'pmcp-package' crates/pmcp-openapi-server/Cargo.toml > /tmp/124-cr01.txt` then `/usr/bin/grep -c 'version' /tmp/124-cr01.txt` returns 0 (the dev-dep is path-only)
    - The SUMMARY contains a nine-row emitter table with before/after values and, for each row, the guard that would catch it being left behind
    - If the audit verdict is "ship as-is": `/opt/homebrew/bin/git diff --unified=0 -- crates/pmcp-package/Cargo.toml > /tmp/124-pkg-diff.txt` then `/usr/bin/grep -c '^+version = ' /tmp/124-pkg-diff.txt` returns 0
    - If the audit verdict is "bump": all nine emitters show the new value, verified one by one and listed in the SUMMARY
    - `make no-crypto-check` exits 0
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>`pmcp-package`'s ship version is decided by measurement rather than by assumption, and if it moved, every emitter of it moved in the same commit — including the one the compiler cannot see.</done>
  <reversibility rating="costly">Same as Task 1: revertible until the tag publishes, permanent afterwards. The nine-emitter obligation is what makes a partial change dangerous rather than merely wrong.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Write the CHANGELOG section and prove the release workflow can extract it</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-40 (the file header and the current head section `## [2.19.0] - 2026-08-20` — the heading form is the contract)
    - .github/workflows/release.yml lines 20-45 (the `Read changelog` step: the awk that matches `## \[<version-without-v>\]`, and the fact that an unmatched version yields an empty string with exit 0)
    - .planning/phases/124-release-publish-order/124-RESEARCH.md (Pitfall 7 — why an absent section fails silently, and the requirement to re-run that exact awk as the verification)
    - .planning/phases/124-release-publish-order/124-03-SUMMARY.md and 124-05 Tasks 1-2 findings (what actually ships, so the section describes the real release)
  </read_first>
  <action>
Add a `## [2.19.1] - <today's date in YYYY-MM-DD>` section at the head of `CHANGELOG.md`,
immediately above the existing `## [2.19.0]` section. The heading form is load-bearing, not
stylistic: `release.yml`'s `Read changelog` step awk-matches `## \[<version-without-v>\]` and,
on no match, yields an empty string and exits 0 — so the GitHub Release is created with empty
notes and nobody notices until someone reads the release page.

Content: describe what this release actually ships, sourced from this phase's own measurements
rather than composed from imagination.
  - The `pmcp` core patch: the `jsonwebtoken` requirement move that had landed under an
    already-published 2.19.0 and would otherwise have shipped silently never. State that no
    `jsonwebtoken` type crosses the public API, which is why it is a patch.
  - Every other crate bumped in Tasks 1 and 2, one line each, naming what changed.
  - The crates already ahead of the registry that ride this tag as-is (phases 120–123's work:
    config-server packaging, the local round-trip, attestation carriage, the save/load/pull
    verbs) — a reader of the release page should be able to see the whole surface.
  - The release-hygiene work itself: the coverage gate now covers workspace-excluded
    publishable crates, the publish order for the `pmcp-package` cluster is machine-checked,
    and `make release-sweep` reports version drift against the registry.
Follow the existing sections' structure and heading conventions.

Then prove the extraction works by running `release.yml`'s own awk — the same program, not a
paraphrase of it — against `CHANGELOG.md` for the version being tagged, and asserting the
output is non-empty. Save the extracted text to a file and record its first lines in the
SUMMARY, so the release notes are seen before the tag is pushed rather than after.

Confirm the version in the heading equals the tag chosen in D-06 (`v2.19.1`) with the leading
`v` removed. A mismatch here is the silent-empty-notes failure.
  </action>
  <verify>
    <automated>awk -v ver="## \\[2.19.1\\]" '$0 ~ ver {p=1; next} /^## \[/ && p {exit} p {print}' CHANGELOG.md > /tmp/124-notes.txt; test -s /tmp/124-notes.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c '^## \[2.19.1\] - ' CHANGELOG.md` returns 1
    - The extraction file `/tmp/124-notes.txt` is non-empty and its first 20 lines are reproduced in the SUMMARY
    - `/usr/bin/grep -c '^## \[2.19.0\]' CHANGELOG.md` returns 1 (the previous section is intact, not overwritten)
    - The new section appears BEFORE the 2.19.0 section: `/usr/bin/grep -n '^## \[' CHANGELOG.md > /tmp/124-headings.txt` and the 2.19.1 line number is smaller than the 2.19.0 line number
    - Every crate bumped in Tasks 1 and 2 is named in the new section — checked crate by crate against the SUMMARY's bump list
    - `RUSTFLAGS="" make quality-gate` exits 0
  </acceptance_criteria>
  <done>The release notes exist in the form the workflow can read, proven by running the workflow's own extraction rather than by inspection, and they describe the release that is actually shipping.</done>
  <reversibility rating="reversible">A CHANGELOG section is editable until the tag is pushed; after that it is baked into a GitHub Release, which is itself editable.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-repo version literals -> published crates.io artifacts | Whatever is in these manifests at tag time becomes permanent registry state. |
| CHANGELOG heading -> GitHub Release notes | An unmatched heading is an empty-notes release created with exit 0 — a silent failure at a boundary nobody watches. |
| pmcp-package version -> four consuming crates | A partial emitter move ships a CLI pinned to a package version it cannot read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-03 | Tampering (semantic) | `pmcp-package` version emitter set | high | mitigate | Nine-emitter one-set rule executed in a single commit; the two pin tripwires and the scaffold drift test are asserted to pass, and the compiler-invisible emitter is called out as the one most likely to be missed (Task 2) |
| T-124-04 | Elevation of Privilege | permanent version-number consumption | medium | mitigate | This plan is authorised only for the closed list recorded at plan 03's checkpoint; a diff assertion counts the changed version lines against that list (Task 1) |
| T-124-08 | Denial | CHANGELOG heading vs release.yml awk | medium | mitigate | The workflow's own awk is re-run against the file and the output asserted non-empty and reviewed before the tag exists (Task 3) |
| T-124-15 | Tampering | `crates/pmcp-openapi-server` dev-dep on `pmcp-package` | high | mitigate | CR-01 path-only constraint re-verified while pins are being edited elsewhere; `pmcp_package_dev_dep_is_path_only` asserted green (Task 2 Step D) |
| T-124-16 | Tampering | `crates/pmcp-package` dependency surface | medium | mitigate | `make no-crypto-check` asserted green after any manifest edit to that crate; the plan's prohibitions forbid adding a crypto or signing dependency (Task 2 Step E) |
</threat_model>

<verification>
- Root `pmcp` version is 2.19.1 and no unauthorised crate version moved
- Every emitter of a bumped `pmcp-package` version moved in one commit, tripwires green
- The openapi-server dev-dep is still path-only
- `## [2.19.1]` extracts non-empty via the workflow's own awk
- `RUSTFLAGS="" make quality-gate` exits 0
</verification>

<success_criteria>
- SC3 is met: `pmcp-package`'s ship version, `cargo-pmcp`'s caret pin and the pin tripwire move in the same change, so the CLI cannot ship pinned to a package version it cannot read
- The release notes are non-empty and describe the release that is actually shipping
- The version numbers about to be consumed are exactly the ones the user authorised
</success_criteria>

<output>
Create `.planning/phases/124-release-publish-order/124-05-SUMMARY.md` when done
</output>
