---
phase: 104-task-augmented-tool-results-dx
plan: 05
type: execute
wave: 4
depends_on: ["104-01", "104-02", "104-03", "104-04"]
files_modified:
  - examples/s47_task_augmented_result.rs
  - tests/tool_output_result_http.rs
  - docs/design/sep-1686-task-augmented-results.md
  - pmcp-book/src/SUMMARY.md
  - Cargo.toml
  - README.md
autonomous: true
requirements: [TOUT-04]
must_haves:
  truths:
    - "A runnable BEFORE/AFTER example (s47) shows a hand-rolled _meta task tool and the same tool on native with_task_store() + ToolOutput"
    - "A live HTTP loopback test proves ToolOutput::Result carries _meta at the result top level over a real transport and is NOT stringified into content[0].text"
    - "The migration guide documents the hand-rolled to native path and confirms native CreateTaskResult _meta emission is _meta-sniffing-client compatible"
  artifacts:
    - path: "examples/s47_task_augmented_result.rs"
      provides: "BEFORE/AFTER migration example"
      contains: "ToolOutput"
    - path: "tests/tool_output_result_http.rs"
      provides: "D-14 live HTTP _meta-at-top-level acceptance gate"
      contains: "_meta"
    - path: "docs/design/sep-1686-task-augmented-results.md"
      provides: "SEP-1686 junction rationale + wire-compat note"
    - path: "Cargo.toml"
      provides: "[[example]] s47 block"
      contains: "s47_task_augmented_result"
  key_links:
    - from: "tests/tool_output_result_http.rs"
      to: "StreamableHttpServer + StreamableHttpTransport"
      via: "real HTTP round-trip consuming REAL dispatch output"
      pattern: "StreamableHttp"
    - from: "docs/design/sep-1686-task-augmented-results.md"
      to: "src/server/core_tests.rs native _meta emission proof"
      via: "wire-compat confirmation"
      pattern: "related-task"
---

<objective>
Deliver the TOUT-04 documentation + acceptance gate: a runnable BEFORE/AFTER migration example (`s47`), the D-14 live-HTTP `_meta`-at-top-level regression test consuming REAL dispatch output, and the migration guide (docs/design companion + pmcp-book chapter + README pointer). The example diff IS the migration guide; the HTTP test is the "never hand-authored fixtures" acceptance gate.

Purpose: Let the pmcp.run team delete their hand-rolled `_meta` intercepts and migrate their three pre-2.11 task servers onto native `with_task_store()`; lock the wire shape in CI.
Output: `examples/s47`, `tests/tool_output_result_http.rs`, docs/design doc, pmcp-book chapter, README pointer, Cargo.toml example block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-task-augmented-tool-results-dx/104-CONTEXT.md
@.planning/phases/104-task-augmented-tool-results-dx/104-RESEARCH.md
@.planning/phases/104-task-augmented-tool-results-dx/104-PATTERNS.md
@.planning/phases/104-task-augmented-tool-results-dx/104-01-SUMMARY.md
@.planning/phases/104-task-augmented-tool-results-dx/104-02-SUMMARY.md
@.planning/phases/104-task-augmented-tool-results-dx/104-04-SUMMARY.md

<interfaces>
<!-- Verified against live source pmcp 2.11.0. -->

tests/tool_as_task_lifecycle_http.rs:
  #![cfg(all(feature = "streamable-http", not(target_arch = "wasm32")))]
  use pmcp::server::streamable_http_server::StreamableHttpServer;
  use pmcp::shared::streamable_http::{StreamableHttpTransport, StreamableHttpTransportConfig};
  // reliability conventions (header :19-31): EPHEMERAL PORT 127.0.0.1:0, read local_addr from start(), readiness = listener bound before start() returns, SHUTDOWN = JoinHandle::abort()

examples/s46_http_tool_as_task.rs, examples/s45_tool_as_task_lifecycle.rs:
  file-header doc-comment style + TypedTool/with_task_store() server setup + in-process-then-assert structure to copy

Cargo.toml:
  [[example]] name = "s46_http_tool_as_task" / path = "examples/s46_http_tool_as_task.rs" (:561-562)  — mirror with required-features = ["full"]

src/server/core_tests.rs:855-896:
  PROVEN: result["_meta"][RELATED_TASK_META_KEY]["taskId"] == store-minted id ⇒ pmcp.run detect_task_response works UNCHANGED against native with_task_store() — the wire-compat fact the guide documents

src/client/mod.rs:629-640:
  parse_task_payload already WARNs on deserialize failure ⇒ SDK client is clean (note in guide; the pmcp.run ask #5 gap is in THEIR durable client)

pmcp-book/src/SUMMARY.md — in-tree mdBook table of contents (add a chapter entry)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: s47 BEFORE/AFTER runnable example + Cargo.toml block</name>
  <files>examples/s47_task_augmented_result.rs, Cargo.toml</files>
  <read_first>
    - examples/s45_tool_as_task_lifecycle.rs (with_task_store server setup, doc-header style)
    - examples/s46_http_tool_as_task.rs (task-augmented result over transport)
    - Cargo.toml (the s46 [[example]] block :561-562)
    - src/server/mod.rs (ToolOutput, tool_with_result — Plan 02/04)
    - src/types/tools.rs (with_related_task — Plan 01)
  </read_first>
  <action>
    Create examples/s47_task_augmented_result.rs demonstrating the migration in one file: a BEFORE section (a hand-rolled tool that stuffs `_meta[related-task]` into a Value payload the old way — showing it getting text-wrapped / the workaround) and an AFTER section (the SAME tool on native `with_task_store()` returning `ToolOutput::Result(CallToolResult::new(..).with_related_task(TaskMetadata{..}))` via `tool_with_result`, plus a client using `related_task()` to detect it). Assert in-process that the AFTER result exposes `related_task()` == Some and the content is not the stringified envelope. Copy the doc-header comment style from s45/s46. Add the `[[example]]` block to Cargo.toml mirroring s46 with `required-features = ["full"]`. Keep it runnable end-to-end with no external services.
  </action>
  <verify>
    <automated>cargo run --example s47_task_augmented_result --features full 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - examples/s47_task_augmented_result.rs exists and references `ToolOutput` and `with_related_task`
    - Cargo.toml contains an `[[example]]` block with `name = "s47_task_augmented_result"` and `required-features = ["full"]`
    - `cargo run --example s47_task_augmented_result --features full` exits 0
    - the example asserts `related_task()` returns Some on the AFTER path
  </acceptance_criteria>
  <done>The BEFORE/AFTER diff is a runnable, self-contained migration demonstration (doubles as the ALWAYS example).</done>
</task>

<task type="auto">
  <name>Task 2: D-14 live-HTTP _meta-at-top-level acceptance gate</name>
  <files>tests/tool_output_result_http.rs</files>
  <read_first>
    - tests/tool_as_task_lifecycle_http.rs (WHOLE file — copy the harness: cfg gate, StreamableHttpServer/StreamableHttpTransport imports, ephemeral port, readiness, JoinHandle::abort shutdown)
    - src/server/mod.rs (tool_with_result / ToolOutput::Result — Plan 02/04)
    - src/types/tools.rs (with_related_task — Plan 01)
  </read_first>
  <action>
    Create tests/tool_output_result_http.rs starting with `#![cfg(all(feature = "streamable-http", not(target_arch = "wasm32")))]`. Copy the Phase 102 HTTP loopback harness structure verbatim (ephemeral `127.0.0.1:0`, read `local_addr` back from `StreamableHttpServer::start()`, readiness via bound-listener, `JoinHandle::abort()` shutdown). Register a tool returning `ToolOutput::Result(CallToolResult::new(vec![Content::text("done")]).with_related_task(TaskMetadata{ task_id: "t-http", .. }))` on a high-level `pmcp::Server` served over the real transport. Drive a `tools/call` over the real `StreamableHttpTransport` and assert on the RAW wire JSON: `raw["result"].get("_meta").is_some()` (the `_meta` survives at result TOP LEVEL) AND that `raw["result"]["content"][0]["text"]`, if present, does NOT contain a stringified `"_meta"` (the agent-lake double-wrap bug). Consume REAL dispatch output — never a hand-authored fixture. Do NOT hardcode a port or use a fixed sleep for readiness.
  </action>
  <verify>
    <automated>cargo test --features full --test tool_output_result_http 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - tests/tool_output_result_http.rs exists with the `streamable-http` + `not(wasm32)` cfg gate
    - grep shows `StreamableHttpServer` and `StreamableHttpTransport` used (real transport, not in-process shim)
    - the test asserts `result._meta` present at top level AND content[0].text has no stringified `_meta`
    - no hardcoded port (grep shows `127.0.0.1:0`) and no fixed `sleep` for readiness
    - `cargo test --features full --test tool_output_result_http` exits 0
  </acceptance_criteria>
  <done>The wire shape is CI-locked over a real HTTP round-trip consuming real dispatch output.</done>
</task>

<task type="auto">
  <name>Task 3: Migration guide (docs/design + pmcp-book chapter + README pointer)</name>
  <files>docs/design/sep-1686-task-augmented-results.md, pmcp-book/src/SUMMARY.md, README.md</files>
  <read_first>
    - .planning/phases/104-task-augmented-tool-results-dx/104-CONTEXT.md (D-12 guide shape; specifics: pmcp.run detect_task_response works unchanged; reply mapping asks 6/7/8/9 → TOUT-01/02/03/04)
    - src/server/core_tests.rs (native _meta emission proof :855-896)
    - pmcp-book/src/SUMMARY.md (existing chapter list + link format)
    - README.md (MCP Tasks bullets :267/:452 — where the pointer goes)
    - docs/design/tasks-feature-design.md (existing tasks design doc to cross-link, not duplicate)
  </read_first>
  <action>
    Create docs/design/sep-1686-task-augmented-results.md recording: (1) the SEP-1686 junction rationale (the mod.rs:1493 double-wrap bug class, 5 incident variants, the agent-lake wire probe); (2) the migration path from the three hand-rolled pre-2.11 `_meta` task patterns to native `with_task_store()` + `ToolOutput::Result` / `tool_with_result` / `set_result_meta`; (3) the wire-compat confirmation: native `CreateTaskResult` carries `_meta[related-task]` with the store-minted id (cite src/server/core_tests.rs:855-896) so pmcp.run's `detect_task_response` (`result._meta[related-task]`) works UNCHANGED, and note the Required-without-store build validation plus that the SDK client already WARNs on task deserialize failure (client/mod.rs:629-640 — the ask #5 gap is on their side). Add a user-facing pmcp-book chapter (new file under pmcp-book/src/, e.g. `task-augmented-results.md`) covering the same migration with the s47 BEFORE/AFTER as the worked example, and register it in pmcp-book/src/SUMMARY.md using the existing link format. Add a one-line pointer from the README MCP Tasks section to the book chapter. Reference (do not duplicate) docs/design/tasks-feature-design.md.
  </action>
  <verify>
    <automated>test -f docs/design/sep-1686-task-augmented-results.md && grep -q "related-task" docs/design/sep-1686-task-augmented-results.md && grep -qi "task-augmented\|SEP-1686" pmcp-book/src/SUMMARY.md && grep -qi "task-augmented\|SEP-1686" README.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/design/sep-1686-task-augmented-results.md exists, references `related-task` and cites core_tests.rs native emission
    - a new pmcp-book chapter file exists and is registered in pmcp-book/src/SUMMARY.md
    - README.md MCP Tasks section contains a pointer to the migration chapter
    - the guide confirms native CreateTaskResult _meta is _meta-sniffing-client compatible (D-08/D-09)
    - the verify command prints DOCS_OK
  </acceptance_criteria>
  <done>Migration guide is durable (book + design doc) with a README pointer and the wire-compat proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server over real HTTP transport | the acceptance-gate test drives a real StreamableHttp round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-05-01 | Tampering | wire shape silently regresses (envelope re-stringified into content[0].text) | mitigate | D-14 live-HTTP test consumes REAL dispatch output and asserts _meta at top level + no stringified _meta in content; CI-enforced, no hand-authored fixture. |
| T-104-05-02 | Information Disclosure | migration guide leaks internal-only incident detail | accept | Guide reframes the pmcp.run incident at the SDK-API level; no secrets/credentials reproduced. |
| T-104-05-03 | Denial of Service | HTTP test hangs CI on lingering server / fixed port | mitigate | Ephemeral 127.0.0.1:0, readiness via bound listener, JoinHandle::abort() shutdown (copied Phase 102 reliability conventions). |
| T-104-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (in-tree example/test/docs only). |
</threat_model>

<verification>
- `cargo run --example s47_task_augmented_result --features full` exits 0.
- `cargo test --features full --test tool_output_result_http` green (live HTTP _meta-at-top-level).
- Docs verify command prints DOCS_OK.
- `make doc-check` green (book/doc links resolve).
- Phase gate: `make quality-gate` green; ALWAYS coverage complete across the phase (unit — Plans 01/03/04; property/fuzz — Plan 03 tripwire proptest; runnable example — s47; integration — passthrough + HTTP).
</verification>

<success_criteria>
The SEP-1686 junction is documented end-to-end, the wire shape is CI-locked over a real transport, and pmcp.run has a runnable BEFORE/AFTER migration path plus the wire-compat proof to delete their hand-rolled intercepts.
</success_criteria>

<output>
Create `.planning/phases/104-task-augmented-tool-results-dx/104-05-SUMMARY.md` when done
</output>
