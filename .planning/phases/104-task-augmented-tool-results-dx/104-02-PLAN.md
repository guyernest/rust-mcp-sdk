---
phase: 104-task-augmented-tool-results-dx
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/mod.rs
  - src/server/core.rs
  - src/server/task_dispatch.rs
  - tests/tool_output_passthrough.rs
autonomous: true
requirements: [TOUT-01]
must_haves:
  truths:
    - "A ToolHandler that returns ToolOutput::Result(CallToolResult) lands that result on the wire verbatim — _meta preserved, no text-wrap, no widget enrichment"
    - "A ToolHandler that does not override handle_output behaves exactly as before (default delegates to handle() and wraps as Payload)"
    - "The Payload path still runs the Phase 102 create-path gate (maybe_build_task_created) FIRST, preserving D-STORE-MINTS-ID"
    - "Server (mod.rs) and ServerCore (core.rs) produce identical output for the same ToolOutput::Result"
    - "The wasm dispatch path also honors handle_output (browser-served tools are not left text-wrapping)"
  artifacts:
    - path: "src/server/mod.rs"
      provides: "ToolOutput enum + ToolHandler::handle_output default + Payload/Result branch in handle_call_tool"
      contains: "enum ToolOutput"
    - path: "src/server/task_dispatch.rs"
      provides: "shared pass-through helper so both dispatchers branch identically (D-05)"
    - path: "src/server/core.rs"
      provides: "ServerCore twin ToolOutput branch"
    - path: "tests/tool_output_passthrough.rs"
      provides: "verbatim passthrough + default-delegation + dispatch-parity tests"
  key_links:
    - from: "src/server/mod.rs::handle_call_tool"
      to: "handler.handle_output"
      via: "dispatch call site swap (native + wasm)"
      pattern: "handle_output"
    - from: "src/server/mod.rs Payload arm"
      to: "task_dispatch::maybe_build_task_created"
      via: "create-path gate keeps precedence (D-04)"
      pattern: "maybe_build_task_created"
---

<objective>
Add the core TOUT-01 mechanism: a `ToolOutput` enum + a default-impl `ToolHandler::handle_output()` method, and a shared dispatch branch (honored identically by `Server` and `ServerCore`, D-05) that sends `ToolOutput::Result` to the wire verbatim while `ToolOutput::Payload` keeps today's create-path gate + text-wrap behavior. Closes the `src/server/mod.rs:1493` double-wrap junction without any breaking change.

Purpose: A hand-written `ToolHandler` (the pmcp.run case) can own the full `CallToolResult` envelope, `_meta` included, through the normal front door.
Output: `ToolOutput`, `handle_output`, shared pass-through helper, both dispatch tails branched, passthrough test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-task-augmented-tool-results-dx/104-CONTEXT.md
@.planning/phases/104-task-augmented-tool-results-dx/104-RESEARCH.md
@.planning/phases/104-task-augmented-tool-results-dx/104-PATTERNS.md

<interfaces>
<!-- Verified against live source pmcp 2.11.0. -->

src/server/mod.rs:
  #[cfg(not(target_arch = "wasm32"))] #[async_trait] pub trait ToolHandler  // :229
    async fn handle(&self, args: Value, extra: cancellation::RequestHandlerExtra) -> Result<Value>;  // :231
    fn metadata(&self) -> Option<ToolInfo> { None }  // :235 — precedent for a default method existing impls never override
  async fn handle_call_tool(...)  // :1282 — dispatch. native call site handler.handle at :1394; wasm call site (#[cfg(target_arch="wasm32")]) at :1422
  Err(Error::ToolRejected{..}) => return Ok(serde_json::to_value(CallToolResult::rejected(..))?)  // :1438 — the verbatim-early-return shape ToolOutput::Result mirrors
  create-path gate call maybe_build_task_created(..)  // :1463-1490
  unconditional wrap: let text = result.to_string(); CallToolResult::new(vec![Content::text(text)]); with_widget_enrichment(info, result)  // :1493-1497

src/server/core.rs:
  enum ToolCallOutcome { .., Result(CallToolResult), TaskCreated{task_value} }  // :303
  handler.handle call sites :519 / :549 / :852
  wrap tail :594-628 (own task-shape gate :601-607; summarize_structured_output + to_string_pretty branch :621-624); ToolCallOutcome::Result at :628, consumed :1127

src/server/task_dispatch.rs:
  pub(crate) async fn maybe_build_task_created(&self, id, value:&Value, task_support, task_requested, auth) -> Option<JSONRPCResponse>  // :311 — gates on value.get("taskId") && value.get("status"); a CallToolResult has NEITHER at top level ⇒ passes for free
  // Why: production-reachability annotation template at :308-310
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToolOutput enum + ToolHandler::handle_output default (native + wasm)</name>
  <files>src/server/mod.rs</files>
  <read_first>
    - src/server/mod.rs (ToolHandler trait + metadata default :226-238; the cfg gating and #[async_trait] convention; the wasm cfg branches of handle_call_tool around :1394/:1422)
    - src/types/tasks.rs (#[non_exhaustive] convention on Task :91)
    - src/server/typed_tool.rs (TypedTool/TypedToolWithOutput impl ToolHandler :253/:429 — confirm existing impls only define handle+metadata, so the default handle_output covers them)
  </read_first>
  <behavior>
    - a ToolHandler that implements only handle() (no handle_output override) routed through handle_output returns ToolOutput::Payload(v) equal to what handle() returned
    - ToolOutput is #[non_exhaustive]
  </behavior>
  <action>
    Add a public `ToolOutput` enum near the `ToolHandler` trait in src/server/mod.rs with variants `Payload(serde_json::Value)` and `Result(crate::types::CallToolResult)`; annotate `#[non_exhaustive]`; derive `Debug` (do NOT derive Serialize/Deserialize — it is a control enum, not a wire type). Add a THIRD trait method to `ToolHandler` (mod.rs:229): `async fn handle_output(&self, args: Value, extra: cancellation::RequestHandlerExtra) -> Result<ToolOutput>` with a DEFAULT body `self.handle(args, extra).await.map(ToolOutput::Payload)` (mirror how `metadata()` at :235 is a default existing impls never override). Locate the wasm ToolHandler surface: the trait at :229 is `#[cfg(not(target_arch = "wasm32"))]`; find the counterpart the wasm `handle_call_tool` branch (:1422) dispatches against (search wasm_core.rs / wasm_server.rs and any `#[cfg(target_arch = "wasm32")]` ToolHandler) and add the same default `handle_output` there so the wasm build is not left calling only `handle`. Do NOT yet change the dispatch tails (Task 2). Add a unit test asserting the default delegates to handle → Payload.
  </action>
  <verify>
    <automated>cargo test --features full --lib server::mod 2>&1 | tail -5 && cargo check --target wasm32-unknown-unknown --lib 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/server/mod.rs contains `enum ToolOutput` with `#[non_exhaustive]`
    - src/server/mod.rs contains `async fn handle_output` with a default body referencing `ToolOutput::Payload`
    - existing TypedTool/TypedToolWithOutput impls compile unchanged (no override added)
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0 (wasm ToolHandler mirrors the default)
    - default-delegation unit test passes
  </acceptance_criteria>
  <done>ToolOutput + handle_output default exist on native and wasm ToolHandler surfaces; existing handlers untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared pass-through branch in both dispatch tails</name>
  <files>src/server/task_dispatch.rs, src/server/mod.rs, src/server/core.rs, tests/tool_output_passthrough.rs</files>
  <read_first>
    - src/server/mod.rs (dispatch call sites :1394/:1422; ToolRejected early-return :1438; create-path gate :1463-1490; wrap tail :1492-1500)
    - src/server/core.rs (ToolCallOutcome :303; wrap tail + own task-shape gate :594-628; outcome consumption :1117-1130)
    - src/server/task_dispatch.rs (maybe_build_task_created :311-336; // Why annotation template :308-310)
    - tests/tool_as_task_lifecycle.rs (in-process duplex Server + ServerCore setup for the parity test analog)
  </read_first>
  <behavior>
    - a handler returning ToolOutput::Result(CallToolResult::new(vec![Content::text("x")]).with_meta(map with _meta[related-task])) produces a wire result whose top-level `_meta` is present and whose content is NOT the stringified envelope
    - a handler returning ToolOutput::Payload(json) behaves byte-identically to the pre-change text-wrap (regression: existing example/tool output unchanged)
    - a Payload value that IS task-shaped ({taskId,status}) still routes through maybe_build_task_created (create-path precedence preserved)
    - Server and ServerCore produce the same serialized result for the same ToolOutput::Result
  </behavior>
  <action>
    Swap the dispatch call sites `handler.handle(...)` → `handler.handle_output(...)` at mod.rs:1394 (native) and mod.rs:1422 (wasm), and at core.rs:519/:549/:852. Introduce a single shared helper (a `pub(crate)` free fn or `TaskDispatch` method in src/server/task_dispatch.rs) that both dispatchers call to resolve a `ToolOutput` into the final wire value, so the Payload-vs-Result decision exists in ONE place (D-05 anti-drift). Semantics: for `ToolOutput::Payload(v)` — run `maybe_build_task_created` FIRST (D-04, unchanged precedence), and if it returns None fall through to each dispatcher's EXISTING wrap tail (mod.rs `to_string`+widget enrichment; core.rs `summarize_structured_output`/`to_string_pretty`) — preserve each tail's current serialization, only route the branch through the shared decision. For `ToolOutput::Result(r)` — return `serde_json::to_value(r)` VERBATIM: NO `maybe_build_task_created` routing, NO text-wrap, NO `with_widget_enrichment` (mirror the ToolRejected early-return shape at mod.rs:1438). Add a `// Why:` reachability annotation to the shared helper and keep it ≤ cognitive complexity 25. Do NOT add implicit `from_value::<CallToolResult>` sniffing on Payload (D-02 — explicitly rejected). Create tests/tool_output_passthrough.rs covering: verbatim _meta survival, Payload regression, task-shaped-Payload create-path precedence, and Server-vs-ServerCore parity for the same ToolOutput::Result.
  </action>
  <verify>
    <automated>cargo test --features full --test tool_output_passthrough 2>&1 | tail -8 && cargo test --features full --test tool_as_task_lifecycle 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep mod.rs and core.rs show dispatch call sites now use `handle_output`
    - the Payload/Result decision is implemented once in src/server/task_dispatch.rs and called by both dispatchers (single copy)
    - `cargo test --features full --test tool_output_passthrough` exits 0
    - passthrough test asserts `result._meta` present at top level AND content[0].text does NOT contain a stringified `_meta`
    - parity test asserts Server and ServerCore emit equal serialized results
    - Phase 101/102 regression `cargo test --features full --test tool_as_task_lifecycle` exits 0
    - no `from_value::<CallToolResult>` sniffing appears on the Payload path
  </acceptance_criteria>
  <done>ToolOutput::Result reaches the wire verbatim from both dispatchers; Payload path + create-path precedence unchanged; no drift, no sniffing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool handler → dispatch → wire | handler-owned CallToolResult crosses to the wire un-re-wrapped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02-01 | Spoofing/Tampering | handler smuggles a task envelope via ToolOutput::Result to bypass D-STORE-MINTS-ID | mitigate | ToolOutput::Result is NOT the create-path — it emits a CallToolResult, never mints a store task; a real task still requires task-shaped Payload through maybe_build_task_created; _meta[related-task].taskId in a Result is a client hint, tasks/get still enforces owner scoping server-side. |
| T-104-02-02 | Tampering | branch relocated above auth/middleware | accept/mitigate | The ToolOutput branch stays AFTER the existing auth/middleware path (mod.rs:1367-1394); the Result arm touches no auth. Do not relocate the branch above auth validation. |
| T-104-02-03 | Information Disclosure | handler leaks cross-owner data in verbatim _meta | accept | Handler owns the envelope, same trust level as today's Value return; no new SDK-side surface. Documented in the migration guide (Plan 05). |
| T-104-02-04 | Tampering | two dispatchers drift (D-05) | mitigate | Single shared decision helper in task_dispatch.rs + parity test asserting byte-identical Server/ServerCore output. |
</threat_model>

<verification>
- `cargo test --features full --test tool_output_passthrough` green.
- `cargo test --features full --test tool_as_task_lifecycle` green (no Phase 101/102 regression).
- `cargo check --target wasm32-unknown-unknown --lib` green.
- `pmat analyze complexity --max-cognitive 25` shows no new violation for the shared helper (or a `// Why:`-justified allow within the cog-50 hard cap).
</verification>

<success_criteria>
ToolOutput::Result lands verbatim through both Server and ServerCore; default handle_output preserves existing behavior; create-path precedence and wire shapes unchanged; wasm compiles.
</success_criteria>

<output>
Create `.planning/phases/104-task-augmented-tool-results-dx/104-02-SUMMARY.md` when done
</output>
