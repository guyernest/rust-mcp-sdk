---
phase: 91-workbook-runtime-purity-gate-dialect-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/pmcp-workbook-runtime/Cargo.toml
  - crates/pmcp-workbook-runtime/src/lib.rs
  - crates/pmcp-workbook-runtime/src/excel_error.rs
  - crates/pmcp-workbook-runtime/src/range_ref.rs
  - crates/pmcp-workbook-runtime/src/formula.rs
  - crates/pmcp-workbook-runtime/src/finding.rs
  - crates/pmcp-workbook-runtime/src/dag.rs
  - crates/pmcp-workbook-runtime/src/resolve.rs
  - crates/pmcp-workbook-runtime/src/manifest_model.rs
  - crates/pmcp-workbook-runtime/src/artifact_model.rs
  - crates/pmcp-workbook-runtime/src/scalar_eval.rs
  - crates/pmcp-workbook-runtime/src/changelog.rs
  - crates/pmcp-workbook-runtime/src/sheet_ir/
  - crates/pmcp-workbook-runtime/src/render/
autonomous: false
requirements: [WBRT-01, WBRT-02, WBRT-03]
user_setup: []

must_haves:
  truths:
    - "A developer can depend on pmcp-workbook-runtime as a reader-free leaf crate and deserialize the shared model types (Manifest, CellMap, BundleLock, VersionChangelog, IR Cell/Expr) identically to how the offline emitter produces them"
    - "The runtime runs a compiled IR through a deterministic topo executor producing typed outputs plus per-cell derivation traces; a dependency cycle returns a LintFinding (not a panic)"
    - "The runtime renders a computed workbook to byte-identical deterministic .xlsx via the writer-only rust_xlsxwriter renderer"
    - "The LintReport round-trips through JSON (serde Deserialize present on Severity/LintFinding/LintReport — D-08)"
    - "The crate compiles clean under thiserror = 2 and the crate-level #![deny(clippy::unwrap_used, expect_used, panic)] (D-10)"
  artifacts:
    - path: "crates/pmcp-workbook-runtime/Cargo.toml"
      provides: "Reader-free leaf manifest with literal version 0.1.0, thiserror=2, rust_xlsxwriter writer-only"
      contains: "rust_xlsxwriter"
    - path: "crates/pmcp-workbook-runtime/src/lib.rs"
      provides: "Crate root with panic-freedom lints + module re-export surface"
      contains: "deny(clippy::unwrap_used"
    - path: "crates/pmcp-workbook-runtime/src/finding.rs"
      provides: "LintFinding/Severity/LintReport with Deserialize round-trip contract"
      contains: "Deserialize"
    - path: "crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs"
      provides: "Deterministic topo run() executor returning RunResult + EvalTrace"
      contains: "run"
    - path: "crates/pmcp-workbook-runtime/src/render/mod.rs"
      provides: "Writer-only render_xlsx via rust_xlsxwriter"
      contains: "rust_xlsxwriter"
  key_links:
    - from: "crates/pmcp-workbook-runtime/src/sheet_ir/executor.rs"
      to: "crates/pmcp-workbook-runtime/src/finding.rs"
      via: "run() returns LintFinding on a dependency cycle (D-03)"
      pattern: "LintFinding"
    - from: "crates/pmcp-workbook-runtime/Cargo.toml"
      to: "rust_xlsxwriter 0.95 (default-features = false)"
      via: "writer-only render dependency"
      pattern: "rust_xlsxwriter.*default-features = false"
    - from: "Cargo.toml"
      to: "crates/pmcp-workbook-runtime"
      via: "workspace members array"
      pattern: "crates/pmcp-workbook-runtime"
---

<objective>
Lift the reader-free `pmcp-workbook-runtime` leaf crate near-verbatim from the lighthouse `crates/workbook-runtime/` (owned IR/model types, deterministic topo executor + per-cell traces, pure-Rust `scalar_eval`, writer-only `.xlsx` renderer, finding model). Apply exactly the SDK-convention deltas and the one code delta (D-08 `Deserialize`). This is slot 2a — the leaf every downstream consumer depends on.

Purpose: Establish the reader-free served-binary side of the boundary (RFC §7 smallest cut) so the purity gate (Plan 03) has something to defend, and Phases 92-96 have a stable model/eval/render foundation.

Output: A compiling, tested `crates/pmcp-workbook-runtime` crate registered in the workspace; satisfies WBRT-01, WBRT-02, WBRT-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-CONTEXT.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-RESEARCH.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-VALIDATION.md

<interfaces>
<!-- SDK manifest convention the lift MUST conform to. Lighthouse uses version.workspace + {workspace=true}; the SDK does NOT. -->

From crates/pmcp-server-toolkit/Cargo.toml (SDK package-header + dep convention):
- version = "0.1.0"   (literal — NOT version.workspace; Pitfall 2)
- edition = "2021", license = "MIT OR Apache-2.0", repository = "https://github.com/paiml/rust-mcp-sdk"
- serde = { version = "1", features = ["derive"] }, serde_json = "1", thiserror = "2"

From root Cargo.toml:541 (workspace members array — append the new crate path):
  members = ["pmcp-macros", ..., "crates/pmcp-sql-server", "crates/pmcp-openapi-server", <APPEND HERE>, "examples/25-oauth-basic", ...]

From lighthouse crates/workbook-runtime/src/lib.rs (lift verbatim, rename only):
  #![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]
  #![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic))]
  pub mod {excel_error, range_ref, formula, finding, dag, resolve, manifest_model, artifact_model, scalar_eval, sheet_ir, render, changelog};

From lighthouse crates/workbook-runtime/src/sheet_ir/mod.rs:
  pub use executor::{build_dag, run, EvalTrace, RunResult};
  // run() walks Cells in Kahn topo order; on a dependency cycle returns a LintFinding (D-03 — finding.rs must live in runtime)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Provenance gate for rust_xlsxwriter (T-91-SC)</name>
  <read_first>
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-RESEARCH.md (§Package Legitimacy Audit — slopcheck was UNAVAILABLE this session; registry-existence confirmed, provenance verify recommended)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-runtime/Cargo.toml (the rust_xlsxwriter provenance comment block ~lines 34-42 — the lighthouse's own T-12-SC gate)
  </read_first>
  <files>(no files — human provenance verification before Task 2 adds the dependency)</files>
  <action>This is a checkpoint: a human verifies rust_xlsxwriter provenance (see <what-built> / <how-to-verify>) before Task 2 lands the dependency in Cargo.toml. No code is written in this task.</action>
  <what-built>
    rust_xlsxwriter 0.95 is the ONLY new third-party crate this phase introduces (all others — serde, serde_json, schemars, thiserror, sha2, hex — are already vendored in the SDK workspace). slopcheck could not run in the research session, so this is a one-time human provenance confirmation before the dependency lands in Cargo.toml.
  </what-built>
  <how-to-verify>
    1. Open https://crates.io/crates/rust_xlsxwriter — confirm latest non-yanked version is 0.95.x, author/owner is `jmcnamara`, license is "MIT OR Apache-2.0".
    2. Confirm the source repo is https://github.com/jmcnamara/rust_xlsxwriter (jmcnamara is the author of libxlsxwriter / Python XlsxWriter — established provenance).
    3. After Task 2 adds the dependency, run `cargo audit` at the workspace root and confirm no advisory is attributed to rust_xlsxwriter or its transitive `zip 7.2.0`.
    4. Run `cargo tree -p pmcp-workbook-runtime -i zip` and confirm `zip` enters ONLY via `rust_xlsxwriter` (writer-only — a writer is not a reader).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms crates.io owner `jmcnamara` + repo `github.com/jmcnamara/rust_xlsxwriter` + license MIT/Apache.
    - `cargo audit` reports no advisory implicating rust_xlsxwriter / zip.
    - `cargo tree -p pmcp-workbook-runtime -i zip` shows zip reachable only through rust_xlsxwriter.
  </acceptance_criteria>
  <verify>
    <human-check>Human confirms crates.io owner jmcnamara + repo github.com/jmcnamara/rust_xlsxwriter + MIT/Apache; cargo audit clean; cargo tree -i zip shows writer-only entry.</human-check>
  </verify>
  <done>Human has approved rust_xlsxwriter provenance; the dependency may be added in Task 2.</done>
  <resume-signal>Type "approved" once crates.io provenance + cargo audit are confirmed, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the runtime crate manifest + register in workspace</name>
  <files>Cargo.toml, crates/pmcp-workbook-runtime/Cargo.toml</files>
  <read_first>
    - crates/pmcp-server-toolkit/Cargo.toml (SDK package-header + dep convention — literal versions, thiserror = "2", exclude + docs.rs metadata blocks)
    - crates/pmcp-sql-server/Cargo.toml (literal version "0.1.0" + path-dep convention reference)
    - Cargo.toml (root — the [workspace] members array at line ~541; the [workspace.dependencies] / [workspace.package] absence that makes version.workspace ILLEGAL here — Pitfall 2)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-runtime/Cargo.toml (lighthouse dep list — the source to rewrite into SDK literal form; preserve the rust_xlsxwriter provenance comment block verbatim)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§crates/pmcp-workbook-runtime/Cargo.toml — the exact resulting manifest shape)
  </read_first>
  <action>
    Create `crates/pmcp-workbook-runtime/Cargo.toml`. [package]: name `pmcp-workbook-runtime`, literal `version = "0.1.0"` (NOT version.workspace — Pitfall 2), `edition = "2021"`, `license = "MIT OR Apache-2.0"`, `repository = "https://github.com/paiml/rust-mcp-sdk"`, `description = "Reader-free workbook IR + deterministic executor + writer-only .xlsx renderer"`, `exclude = [".planning/", ".pmat/", "fuzz/", "tests/"]`, plus a `[package.metadata.docs.rs]` block (`all-features = true`, `rustdoc-args = ["--cfg", "docsrs"]`). [lib]: `name = "pmcp_workbook_runtime"` (renamed from lighthouse `workbook_runtime`), `path = "src/lib.rs"`. [dependencies] in SDK literal form (NOT `{ workspace = true }`): `serde = { version = "1", features = ["derive"] }`, `serde_json = "1"`, `schemars = "1.0"`, `thiserror = "2"` (bumped from lighthouse 1.0.69 — Pitfall 3), `sha2 = "0.11"`, `hex = "0.4"`, `rust_xlsxwriter = { version = "0.95", default-features = false }` (WRITER-ONLY). Preserve verbatim the lighthouse rust_xlsxwriter provenance comment block above that dep line. Do NOT add a `pmcp` dependency (research A1 / Anti-Pattern — the runtime is functionally pmcp-free; D-09 only loosens the ban, it does not mandate the dep). Then append `"crates/pmcp-workbook-runtime"` to the root `Cargo.toml` `[workspace] members` array (after `"crates/pmcp-openapi-server"`); do NOT add it to the `exclude` list.
  </action>
  <verify>
    <automated>test -f crates/pmcp-workbook-runtime/Cargo.toml && grep -q 'version = "0.1.0"' crates/pmcp-workbook-runtime/Cargo.toml && grep -q 'thiserror = "2"' crates/pmcp-workbook-runtime/Cargo.toml && grep -q 'rust_xlsxwriter = { version = "0.95", default-features = false }' crates/pmcp-workbook-runtime/Cargo.toml && grep -q 'crates/pmcp-workbook-runtime' Cargo.toml && ! grep -q 'version.workspace' crates/pmcp-workbook-runtime/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pmcp-workbook-runtime/Cargo.toml` exists with literal `version = "0.1.0"` and NO `version.workspace`.
    - Manifest pins `thiserror = "2"` (not "1") and `rust_xlsxwriter = { version = "0.95", default-features = false }`.
    - Manifest has NO `pmcp` dependency line.
    - Root `Cargo.toml` `members` array contains `crates/pmcp-workbook-runtime` and it is NOT in `exclude`.
  </acceptance_criteria>
  <done>The new crate is a registered workspace member with an SDK-convention manifest; thiserror is 2, the writer is writer-only, no pmcp dep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lift runtime source verbatim + apply D-08 Deserialize delta</name>
  <files>crates/pmcp-workbook-runtime/src/lib.rs, crates/pmcp-workbook-runtime/src/excel_error.rs, crates/pmcp-workbook-runtime/src/range_ref.rs, crates/pmcp-workbook-runtime/src/formula.rs, crates/pmcp-workbook-runtime/src/finding.rs, crates/pmcp-workbook-runtime/src/dag.rs, crates/pmcp-workbook-runtime/src/resolve.rs, crates/pmcp-workbook-runtime/src/manifest_model.rs, crates/pmcp-workbook-runtime/src/artifact_model.rs, crates/pmcp-workbook-runtime/src/scalar_eval.rs, crates/pmcp-workbook-runtime/src/changelog.rs, crates/pmcp-workbook-runtime/src/sheet_ir/, crates/pmcp-workbook-runtime/src/render/</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-runtime/src/lib.rs (re-export surface + crate-level lints — lift verbatim, rename crate in doc-comments only)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-runtime/src/finding.rs (THE one code delta — D-08: add Deserialize to Severity/LintFinding/LintReport; keep rule: String; preserve the three existing tests + constructors)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-runtime/src/sheet_ir/ (mod, executor, semantics, eval_bridge, eval_value, value, rounding — lift verbatim; rounding is round-half-away-from-zero, NOT f64::round)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-runtime/src/render/ (mod, layout — writer-only render_xlsx; pins fixed creation datetime + empty author for byte-determinism; lift the byte-equality test)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§finding.rs THE ONE CODE DELTA — exact derive lines + the round-trip test to ADD; §sheet_ir; §render; §manifest_model et al. "lift verbatim")
  </read_first>
  <behavior>
    - Test (lifted): the three existing finding tests pass — `has_errors_gates_on_error_severity_only`, `has_errors_false_when_only_warnings_and_info`, `lint_finding_serializes_with_repair_field`.
    - Test (ADD — Wave 0 gap, D-08): `lint_report_round_trips_through_json` — build a LintReport with one Error finding, `serde_json::to_value` then `serde_json::from_value` back into `LintReport`, assert `findings.len() == 1` and `has_errors()`.
    - Test (lifted): `sheet_ir::executor` run() determinism + per-cell traces; a dependency cycle yields a LintFinding (not a panic).
    - Test (lifted): render byte-equality — two renders of the same (layout, run) produce byte-identical `.xlsx` output.
    - Test (lifted): `excel_round`/`excel_ceiling`/`excel_floor` round-half-away-from-zero cases.
  </behavior>
  <action>
    Copy every file under lighthouse `crates/workbook-runtime/src/` into `crates/pmcp-workbook-runtime/src/` preserving the directory structure (top-level `*.rs` + `sheet_ir/` + `render/`). Lift VERBATIM except: (1) in `lib.rs`, keep the `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]` + `#![cfg_attr(test, allow(...))]` lines exactly and the full `pub mod` re-export surface; change only crate-name references in doc-comments (`workbook_runtime` → `pmcp_workbook_runtime`). (2) In `finding.rs` apply the D-08 delta: change `use serde::Serialize;` to `use serde::{Deserialize, Serialize};` and ADD `Deserialize` to the derive lists of `Severity`, `LintFinding`, and `LintReport` (keep `rule: String` — do NOT make it an enum); update the module doc-comment line that says the types derive `serde::Serialize` to also mention `Deserialize`; ADD the `lint_report_round_trips_through_json` test. (3) Anywhere a file `use`s the old crate name `workbook_runtime::`, rewrite to `pmcp_workbook_runtime::` (most use `crate::`, so likely zero edits). Do NOT introduce `petgraph` (lift the owned `Dag` + Kahn toposort in `dag.rs`); do NOT hand-roll hashing (lift `artifact_model.rs::{build_bundle_lock, sha256_hex, update_field}` verbatim — sha2 0.11 + hex 0.4); do NOT replace Excel rounding with `f64::round`. Verify `RenderError` (`#[derive(thiserror::Error)]`) compiles clean under `thiserror = "2"` (Pitfall 3 — the simple `#[error("...")]` form is source-compatible across the major).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-runtime --lib -- --test-threads=1 && cargo test -p pmcp-workbook-runtime finding::lint_report_round_trips_through_json -- --test-threads=1 && grep -q 'Deserialize' crates/pmcp-workbook-runtime/src/finding.rs && grep -q 'deny(clippy::unwrap_used' crates/pmcp-workbook-runtime/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-runtime --lib -- --test-threads=1` exits 0 (all lifted module tests + the new round-trip test pass).
    - `crates/pmcp-workbook-runtime/src/finding.rs` contains `Deserialize` on all three finding types AND the `lint_report_round_trips_through_json` test.
    - `crates/pmcp-workbook-runtime/src/lib.rs` contains `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]`.
    - `cargo build -p pmcp-workbook-runtime` compiles clean under thiserror = 2 (no thiserror v1 in `cargo tree -p pmcp-workbook-runtime`).
    - `cargo clippy -p pmcp-workbook-runtime -- -D warnings` reports no unwrap/expect/panic violation on non-test code.
  </acceptance_criteria>
  <done>The runtime crate compiles and all lifted tests plus the new D-08 round-trip test pass; the panic-freedom lints and writer-only render are in place; thiserror is the single major version 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party crate → served-binary dependency cone | A new dependency (rust_xlsxwriter) crosses into the reader-free runtime tree; must remain writer-only, no reader/JS transitive |
| compiled IR (JSON) → runtime deserialization | Serde deserialization of bundle/model types into typed structs (typed enums, rename_all — no untyped passthrough). Untrusted-.xlsx parsing is NOT in this phase (reader is Phase 93). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | rust_xlsxwriter transitive (zip) entering runtime tree | mitigate | Task 1 provenance checkpoint (author jmcnamara, cargo audit, `cargo tree -i zip` confirms writer-only entry); Plan 03 purity gate negatively asserts reader/JS absence and positively asserts rust_xlsxwriter present |
| T-91-02 | Denial of Service | panic on malformed IR/coordinate reaching the served value path | mitigate | Crate-level `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]` (D-10); a dependency cycle returns a LintFinding, never panics; render errors surface as RenderError |
| T-91-03 | Tampering | non-deterministic render_xlsx output (audit/provenance integrity) | mitigate | Lifted render pins fixed creation datetime + empty author; the lifted byte-equality determinism test gates it |
| T-91-SC | Tampering | npm/pip/cargo install of rust_xlsxwriter (slopcheck unavailable) | mitigate | Blocking human-verify checkpoint (Task 1) before the dep lands; registry-existence confirmed in research |
</threat_model>

<verification>
- `cargo test -p pmcp-workbook-runtime --lib -- --test-threads=1` green (all lifted tests + new round-trip test).
- `cargo build -p pmcp-workbook-runtime` clean; `cargo tree -p pmcp-workbook-runtime` shows a single `thiserror v2.x` (no v1) and `rust_xlsxwriter` present.
- `cargo clippy -p pmcp-workbook-runtime -- -D warnings` clean on non-test code (panic-freedom lints hold).
- The crate is a workspace member (`cargo test --workspace` picks it up).
</verification>

<success_criteria>
- `pmcp-workbook-runtime` exists, compiles, and is a registered workspace member (slot 2a).
- WBRT-01: shared model types deserialize identically (serde/schemars-clean); the finding types round-trip (D-08).
- WBRT-02: the topo executor produces typed outputs + per-cell traces; a cycle returns a LintFinding.
- WBRT-03: writer-only `render_xlsx` produces byte-identical output via rust_xlsxwriter.
- thiserror is a single major (2); no `pmcp` dependency was added.
</success_criteria>

<output>
Create `.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-01-SUMMARY.md` when done.
</output>
