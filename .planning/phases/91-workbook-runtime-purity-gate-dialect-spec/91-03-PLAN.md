---
phase: 91-workbook-runtime-purity-gate-dialect-spec
plan: 03
type: execute
wave: 3
depends_on: ["91-01", "91-02"]
files_modified:
  - Makefile
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/workbook-dialect-spec.md
autonomous: true
requirements: [WBRT-04, WBDL-03]

must_haves:
  truths:
    - "make purity-check fails the build if umya/quick-xml/calamine/swc_/pmcp-code-mode appears in the pmcp-workbook-runtime or pmcp-workbook-dialect dependency tree, per feature-combination"
    - "make purity-check positively asserts rust_xlsxwriter IS present in the runtime tree (a deleted renderer cannot make the negative gate vacuously pass)"
    - "make purity-check exits 0 against the current reader-free workspace; zip is permitted (enters via the writer)"
    - "A CI purity-check job runs the gate per feature-combination and is wired into the merge-blocking gate job's needs array"
    - "WBDL-03 is re-mapped from Phase 91 to Phase 93 in both REQUIREMENTS.md and ROADMAP.md (not silently dropped — D-02)"
    - "The cargo-deny Layer 2 backstop is documented as a deferred/flagged item (NOT a claimed clean three-layer gate) — deny.toml is NOT edited (infra-managed + workspace-global scoping conflicts with Phase 93)"
  artifacts:
    - path: "Makefile"
      provides: "purity-check target — per-crate per-feature cargo-tree reader-absence + writer-presence gate"
      contains: "purity-check"
    - path: ".github/workflows/ci.yml"
      provides: "merge-blocking purity-check CI job wired into the gate job"
      contains: "purity-check"
    - path: ".planning/REQUIREMENTS.md"
      provides: "WBDL-03 traceability re-mapped to Phase 93"
      contains: "WBDL-03 | Phase 93"
  key_links:
    - from: ".github/workflows/ci.yml gate job"
      to: "purity-check job"
      via: "needs array + result-evaluation script (makes it merge-blocking)"
      pattern: "needs:.*purity-check"
    - from: "Makefile purity-check"
      to: "pmcp-workbook-runtime / pmcp-workbook-dialect dependency trees"
      via: "cargo tree | grep -Ei reader/JS ban tokens (per feature-combo)"
      pattern: "umya\\|calamine\\|quick-xml\\|swc_\\|pmcp-code-mode"
---

<objective>
Stand up the mechanically-provable purity gate (WBRT-04) BEFORE any `umya` code exists: a `make purity-check` target (cargo-tree per-crate, per-feature reader-absence + writer-presence assertions, adapted from the lighthouse justfile recipe with `pmcp` removed from the ban set per D-09) and a merge-blocking CI `purity-check` job wired into the `gate` job. Honestly scope the three-layer gate: Layer 1 (cargo-tree) + Layer 3 (crate split) FULLY satisfy WBRT-04; Layer 2 (cargo-deny) is documented as a deferred backstop (deny.toml is infra-managed AND a workspace-global ban would break Phase 93 — do NOT edit it). Also execute the blocking WBDL-03 → Phase 93 traceability re-map (D-02).

Purpose: Defend the reader/served-binary boundary from day one (Pitfall 1 — THE phase-91 risk) and record the deliberate WBDL-03 re-map so it is not silently lost.

Output: A green merge-blocking purity gate + updated REQUIREMENTS/ROADMAP traceability; satisfies WBRT-04 and covers WBDL-03 (via re-map).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-CONTEXT.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-RESEARCH.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-01-SUMMARY.md
@.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-02-SUMMARY.md

<interfaces>
<!-- This plan depends on 91-01 + 91-02 (both crates must exist for cargo tree -p to resolve). -->

The ban token set (D-09 — pmcp REMOVED; SDK runtime may depend on pmcp):
  umya|calamine|quick-xml|swc_|pmcp-code-mode
The positive assertion: rust_xlsxwriter MUST be present in the runtime tree.
zip is PERMITTED — do NOT grep for it (enters legitimately via rust_xlsxwriter, zip 7.2.0).

From Makefile (in-repo convention analog):
  .PHONY: quality-gate  (line ~460) — .PHONY + @$(MAKE) composition shape to mirror

From .github/workflows/ci.yml:
  quality-gate: job (line ~160) — checkout + dtolnay/rust-toolchain@stable + cargo cache scaffold to mirror
  gate: job (line ~281) — needs: [test, quality-gate]  ← APPEND purity-check + add result eval

From .planning/REQUIREMENTS.md (line 103):
  | WBDL-03 | Phase 91 | Pending |   ← CHANGE Phase 91 → Phase 93
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WBDL-03 re-map — REQUIREMENTS.md + ROADMAP.md (BLOCKING doc edit, D-02)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the traceability table — the `| WBDL-03 | Phase 91 | Pending |` row at line ~103)
    - .planning/ROADMAP.md (Phase 91 section ~line 1734 `**Requirements**: ...WBDL-03`; Phase 93 section ~line 1761 `**Requirements**: WBCO-01...`)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-CONTEXT.md (D-02 — the re-map rationale: WorkbookMap + linter execution need umya, belong in Phase 93)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, change the traceability row `| WBDL-03 | Phase 91 | Pending |` to `| WBDL-03 | Phase 93 | Pending |`. In `.planning/ROADMAP.md`: (a) in the Phase 91 `**Requirements**:` line remove `WBDL-03` so it reads `WBRT-01, WBRT-02, WBRT-03, WBRT-04, WBDL-01`; (b) in the Phase 93 `**Requirements**:` line add `WBDL-03` to the list. This records the deliberate D-02 re-map (the `WorkbookMap` ingest model + linter execution need a real `.xlsx` via umya and belong where the reader lives — Phase 93). Phase 91 delivers only the dialect *contract* (the WHITELIST + spec doc + binding test from Plan 02), not a running linter.
  </action>
  <verify>
    <automated>grep -q 'WBDL-03 | Phase 93' .planning/REQUIREMENTS.md && ! grep -q 'WBDL-03 | Phase 91' .planning/REQUIREMENTS.md && grep -A1 'Phase 91: Workbook Runtime' .planning/ROADMAP.md | grep -v 'WBDL-03' && grep -n 'WBDL-03' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `WBDL-03 | Phase 93` and NO `WBDL-03 | Phase 91`.
    - `.planning/ROADMAP.md` Phase 91 `**Requirements**:` line no longer lists `WBDL-03`.
    - `.planning/ROADMAP.md` Phase 93 `**Requirements**:` line now lists `WBDL-03`.
  </acceptance_criteria>
  <done>WBDL-03 is recorded as re-mapped to Phase 93 in both source artifacts — the requirement is not silently dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Add the make purity-check target (Layer 1 cargo-tree + Layer 3 crate split)</name>
  <files>Makefile, docs/workbook-dialect-spec.md</files>
  <read_first>
    - Makefile (the `.PHONY: quality-gate` target at ~line 460 — the .PHONY + recipe shape to mirror; note CLAUDE.md mandates `make quality-gate` so the SDK analogue is a Makefile target, NOT a justfile recipe — Pitfall 5)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/justfile (the `purity-check` recipe at ~lines 54-92 — the proven cargo-tree reader-vs-writer gate to lift + adapt)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§Makefile purity-check target — the exact target body + the three deltas from the lighthouse recipe)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-RESEARCH.md (§Purity Gate Mechanics — the exact cargo tree invocations; ban set = umya|calamine|quick-xml|swc_|pmcp-code-mode with pmcp REMOVED; zip permitted)
  </read_first>
  <action>
    Add a `.PHONY: purity-check` target to the Makefile. For each crate in `pmcp-workbook-runtime pmcp-workbook-dialect`, for each feature mode in `"" "--no-default-features" "--all-features"`: run `cargo tree -p $$crate $$feat 2>/dev/null | grep -Ei 'umya|calamine|quick-xml|swc_|pmcp-code-mode'` and FAIL (exit 1) on any match. Then run the POSITIVE assertion: `cargo tree -p pmcp-workbook-runtime | grep -qi 'rust_xlsxwriter'` and FAIL if ABSENT (guards against a vacuous negative pass if the renderer is deleted). Three deltas from the lighthouse recipe: (1) the ban set is exactly `umya|calamine|quick-xml|swc_|pmcp-code-mode` — `pmcp` is REMOVED (D-09, the SDK runtime may depend on pmcp); (2) do NOT grep `zip` (it enters legitimately via the writer — Anti-Pattern); (3) DROP the lighthouse engine-value-path grep arm (that is D-10's crate-level clippy job, not the dep boundary). Echo a clear pass message naming reader-free + writer-present + zip-permitted. Then add a short "## Purity Gate (cargo-deny Layer 2 — DEFERRED)" note to the bottom of `docs/workbook-dialect-spec.md` (or a dedicated docs note) stating: WBRT-04 is fully satisfied by the cargo-tree per-crate arm (Layer 1) + the crate split (Layer 3); the cargo-deny `[bans]` backstop (Layer 2) is deferred because `deny.toml` is infra-managed ("do not edit manually") AND cargo-deny ban scoping is workspace-global (a `quick-xml`/`umya` ban would break Phase 93's legitimate compiler use) — it requires a paiml/infra change to land. Do NOT edit `deny.toml`.
  </action>
  <verify>
    <automated>make purity-check && grep -q 'rust_xlsxwriter' Makefile && grep -Eq 'umya.*calamine.*quick-xml.*swc_.*pmcp-code-mode|umya\|calamine\|quick-xml\|swc_\|pmcp-code-mode' Makefile && ! grep -E "grep .*['\"].*\bzip\b" Makefile && git diff --quiet deny.toml</automated>
  </verify>
  <acceptance_criteria>
    - `make purity-check` exits 0 against the current reader-free workspace.
    - The target greps the ban set `umya|calamine|quick-xml|swc_|pmcp-code-mode` (and does NOT include `pmcp` alone or `zip` as ban tokens).
    - The target includes the positive `rust_xlsxwriter`-present assertion.
    - `deny.toml` is unchanged (`git diff --quiet deny.toml`); the cargo-deny Layer 2 limitation is documented as deferred.
    - Manual negative check: temporarily adding a reader token to the grep input would fail — the gate is not vacuous (positive assertion present).
  </acceptance_criteria>
  <done>make purity-check is a green, non-vacuous per-crate per-feature reader-absence + writer-presence gate; cargo-deny Layer 2 is honestly documented as deferred; deny.toml untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the merge-blocking purity-check CI job + wire into the gate job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the `quality-gate` job at ~line 160 — checkout + dtolnay/rust-toolchain@stable + cargo cache scaffold to mirror; the `gate` job at ~line 281 — `needs: [test, quality-gate]` + its result-evaluation script/env)
    - .planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-PATTERNS.md (§.github/workflows/ci.yml — the purity-check job shape + the three required gate-wiring edits; the strategy.matrix recommendation over [crate × feature])
    - CLAUDE.md (§CI Quality Gates — how a job becomes merge-blocking ONLY by being in the gate job's needs array AND its result-evaluation script)
  </read_first>
  <action>
    Add a new `purity-check` job to `.github/workflows/ci.yml` mirroring the `quality-gate` job scaffold (checkout@v6 → `dtolnay/rust-toolchain@stable` → cargo cache → run step). The run step executes the per-crate per-feature gate; use a `strategy.matrix` over `crate: [pmcp-workbook-runtime, pmcp-workbook-dialect]` × `feature: ["", "--no-default-features", "--all-features"]` (future-proofs against a later [features] leak — the runtime has no [features] today so the trees are identical, but wiring the matrix now is correct when features arrive), OR a single step calling `make purity-check` (which already loops crate×feature). Then make it MERGE-BLOCKING by editing the `gate` job: (a) append `purity-check` to its `needs:` array → `needs: [test, quality-gate, purity-check]`; (b) add `PURITY_RESULT: ${{ needs.purity-check.result }}` to the job's `env:`; (c) add `|| [[ "$PURITY_RESULT" != "success" ]]` to the result-evaluation `if` condition. Without these three edits the job is advisory, not blocking (CLAUDE.md / Pattern: CI gate-propagation).
  </action>
  <verify>
    <automated>grep -q 'purity-check:' .github/workflows/ci.yml && grep -Eq 'needs: \[test, quality-gate, purity-check\]|needs:.*purity-check' .github/workflows/ci.yml && grep -q 'PURITY_RESULT' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` defines a `purity-check` job running the per-crate per-feature gate.
    - The `gate` job's `needs:` array contains `purity-check`.
    - The `gate` job's `env:` contains `PURITY_RESULT` and its `if` condition fails on a non-success purity result.
    - The YAML parses cleanly (`python3 yaml.safe_load`).
  </acceptance_criteria>
  <done>The purity gate runs in CI per feature-combination and blocks merge via the org-required gate job — the reader can never silently reach the served tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any workspace crate → served-binary dependency cone | The purity gate is THE control proving the Excel reader (umya/quick-xml) never enters the runtime/dialect (served) trees |
| CI gate configuration → merge eligibility | A purity job that is not in the gate job's needs array is advisory only; the wiring is the actual enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91P-01 | Elevation of Privilege / Tampering | Excel reader (umya/quick-xml) entering the served binary via feature unification | mitigate | Per-crate per-feature `cargo tree` ban (Layer 1) + crate split (Layer 3); run per feature-combination, not just defaults (Pitfall 1) |
| T-91P-02 | Tampering | purity gate bypassable — only checks default features, or non-merge-blocking CI | mitigate | Gate runs across `""`/`--no-default-features`/`--all-features`; wired into the merge-blocking `gate` job's needs + result-eval (Task 3); positive rust_xlsxwriter assertion prevents a vacuous negative pass |
| T-91P-03 | Repudiation | WBDL-03 silently dropped during the Phase 91→93 re-map | mitigate | Task 1 records the re-map in both REQUIREMENTS.md and ROADMAP.md (D-02); covered in this plan's `requirements` field |
| T-91P-SC | Tampering | cargo-deny Layer 2 falsely claimed as active, masking the real (cargo-tree) boundary | accept | Documented honestly as deferred (infra-managed deny.toml + workspace-global scoping conflicts with Phase 93); WBRT-04 fully met by Layers 1+3 — no false assurance recorded |
</threat_model>

<verification>
- `make purity-check` exits 0; greps the correct ban set; positive rust_xlsxwriter assertion present; `deny.toml` unchanged.
- `.github/workflows/ci.yml` parses; `purity-check` job present and wired into `gate` (needs + PURITY_RESULT + if condition).
- `.planning/REQUIREMENTS.md` shows `WBDL-03 | Phase 93`; ROADMAP Phase 91/93 requirement lists updated.
- `make quality-gate` still passes (the new crates from 91-01/91-02 are workspace members and build clean).
</verification>

<success_criteria>
- WBRT-04: a merge-blocking, per-feature, per-crate cargo-tree purity gate (Layer 1) + crate split (Layer 3) prove the reader can never enter the served tree; cargo-deny Layer 2 honestly documented as deferred.
- WBDL-03: re-mapped to Phase 93 in REQUIREMENTS.md + ROADMAP.md (D-02) — recorded, not dropped.
- The gate is non-vacuous (positive writer assertion) and the CI wiring is complete (gate job needs + result eval).
</success_criteria>

<output>
Create `.planning/phases/91-workbook-runtime-purity-gate-dialect-spec/91-03-SUMMARY.md` when done.
</output>
