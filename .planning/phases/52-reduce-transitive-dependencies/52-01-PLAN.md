---
phase: 52-reduce-transitive-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
autonomous: true
requirements:
  - DEP-REDUCE-01
  - DEP-REDUCE-02
  - DEP-REDUCE-03
  - DEP-REDUCE-04
  - DEP-REDUCE-05
must_haves:
  truths:
    - "pmcp crate builds with no default features"
    - "pmcp crate builds with full features"
    - "Workspace builds with all members"
    - "lazy_static and pin-project are no longer in Cargo.toml dependencies"
    - "reqwest is declared optional behind http-client feature"
    - "tokio uses explicit feature list instead of full"
    - "jsonschema uses default-features = false"
  artifacts:
    - path: "Cargo.toml"
      provides: "Updated dependency declarations with slimmed features and optional reqwest"
      contains: "http-client"
  key_links:
    - from: "Cargo.toml [features] http-client"
      to: "dep:reqwest"
      via: "feature implication"
      pattern: 'http-client.*=.*\[.*"dep:reqwest"'
    - from: "Cargo.toml [features] oauth"
      to: "http-client"
      via: "feature implication"
      pattern: 'oauth.*=.*\[.*"http-client"'
    - from: "Cargo.toml [features] jwt-auth"
      to: "http-client"
      via: "feature implication"
      pattern: 'jwt-auth.*=.*\[.*"http-client"'
    - from: "Cargo.toml [features] sse"
      to: "http-client"
      via: "feature implication"
      pattern: 'sse.*=.*\[.*"http-client"'
---

<objective>
Modify Cargo.toml to remove unused dependencies, slim feature flags on heavy dependencies, make reqwest optional behind an `http-client` feature, clean up the phantom `validation` feature, and make `tracing-subscriber` optional behind a `logging` feature.

Purpose: Reduce the transitive dependency count for consumers who use pmcp as a library (e.g., basic stdio MCP servers that don't need HTTP clients, JWT validation, or SDK-provided logging).

Output: Updated Cargo.toml with all dependency and feature changes. Source code is NOT modified in this plan -- cfg gates come in Plan 02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-reduce-transitive-dependencies/52-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused deps, slim features, make reqwest and tracing-subscriber optional</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current state of all dependencies and features)
    - .planning/phases/52-reduce-transitive-dependencies/52-RESEARCH.md (exact changes needed)
  </read_first>
  <action>
Edit the root Cargo.toml with these exact changes:

**1. Remove unused dependencies from [dependencies]:**
- DELETE the line: `lazy_static = "1.5"`
- DELETE the line: `pin-project = "1.1"`

**2. Slim tokio features (in [target.'cfg(not(target_arch = "wasm32"))'.dependencies]):**
- CHANGE: `tokio = { version = "1.46", features = ["full"] }`
- TO: `tokio = { version = "1.46", features = ["rt-multi-thread", "macros", "net", "io-util", "io-std", "fs", "sync", "time"] }`

**3. Make reqwest optional (in [target.'cfg(not(target_arch = "wasm32"))'.dependencies]):**
- CHANGE: `reqwest = { version = "0.13", default-features = false, features = ["json", "rustls", "form"] }`
- TO: `reqwest = { version = "0.13", optional = true, default-features = false, features = ["json", "rustls", "form"] }`

**4. Slim hyper features (already optional, in [dependencies]):**
- CHANGE: `hyper = { version = "1.6", features = ["full"], optional = true }`
- TO: `hyper = { version = "1.6", features = ["http1", "server"], optional = true }`
- CHANGE: `hyper-util = { version = "0.1", features = ["full"], optional = true }`
- TO: `hyper-util = { version = "0.1", features = ["tokio", "http1", "server-auto"], optional = true }`

**5. Set jsonschema default-features = false:**
- CHANGE: `jsonschema = { version = "0.45", optional = true }`
- TO: `jsonschema = { version = "0.45", optional = true, default-features = false }`

**6. Slim chrono features:**
- CHANGE: `chrono = { version = "0.4", features = ["serde"] }`
- TO: `chrono = { version = "0.4", default-features = false, features = ["clock", "serde", "std"] }`

**7. Make tracing-subscriber optional:**
- CHANGE: `tracing-subscriber = { version = "0.3.20", features = ["env-filter"] }`
- TO: `tracing-subscriber = { version = "0.3.20", features = ["env-filter"], optional = true }`

**8. Update [features] section:**
Add these new features (insert after `jwt-auth` line):
```
http-client = ["dep:reqwest"]
logging = ["dep:tracing-subscriber"]
```

Update existing features that need reqwest to imply http-client:
- CHANGE: `oauth = ["dep:webbrowser", "dep:dirs", "dep:rand"]`
- TO: `oauth = ["http-client", "dep:webbrowser", "dep:dirs", "dep:rand"]`
- CHANGE: `jwt-auth = ["dep:jsonwebtoken"]`
- TO: `jwt-auth = ["http-client", "dep:jsonwebtoken"]`
- CHANGE: `sse = ["dep:bytes"]`
- TO: `sse = ["http-client", "dep:bytes"]`

Update `full` feature to include new features:
- CHANGE: `full = ["websocket", "http", "streamable-http", "sse", "validation", "resource-watcher", "rayon", "schema-generation", "jwt-auth", "composition", "mcp-apps"]`
- TO: `full = ["websocket", "http", "streamable-http", "sse", "validation", "resource-watcher", "rayon", "schema-generation", "jwt-auth", "composition", "mcp-apps", "http-client", "logging"]`

Remove `validation` from default features (it's phantom -- no code uses cfg(feature = "validation")):
- CHANGE: `default = ["validation"]`
- TO: `default = ["logging"]`

All other lines in Cargo.toml remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo check -p pmcp --features full 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml does NOT contain `lazy_static = "1.5"`
    - Cargo.toml does NOT contain `pin-project = "1.1"`
    - Cargo.toml contains `reqwest = { version = "0.13", optional = true, default-features = false, features = ["json", "rustls", "form"] }`
    - Cargo.toml contains `tokio = { version = "1.46", features = ["rt-multi-thread", "macros", "net", "io-util", "io-std", "fs", "sync", "time"] }`
    - Cargo.toml contains `hyper = { version = "1.6", features = ["http1", "server"], optional = true }`
    - Cargo.toml contains `hyper-util = { version = "0.1", features = ["tokio", "http1", "server-auto"], optional = true }`
    - Cargo.toml contains `jsonschema = { version = "0.45", optional = true, default-features = false }`
    - Cargo.toml contains `chrono = { version = "0.4", default-features = false, features = ["clock", "serde", "std"] }`
    - Cargo.toml contains `tracing-subscriber = { version = "0.3.20", features = ["env-filter"], optional = true }`
    - Cargo.toml contains `http-client = ["dep:reqwest"]`
    - Cargo.toml contains `logging = ["dep:tracing-subscriber"]`
    - Cargo.toml [features] oauth line contains `"http-client"`
    - Cargo.toml [features] jwt-auth line contains `"http-client"`
    - Cargo.toml [features] sse line contains `"http-client"`
    - Cargo.toml [features] full line contains `"http-client"` and `"logging"`
    - Cargo.toml [features] default line is `default = ["logging"]`
    - `cargo check -p pmcp --features full` succeeds (note: will have unused import warnings until Plan 02 adds cfg gates -- that is expected)
  </acceptance_criteria>
  <done>Cargo.toml updated with all dependency removals, feature slimming, and new feature flags. Build with --features full succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Verify feature matrix builds</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (verify Task 1 changes are applied)
  </read_first>
  <action>
Run the following build verifications in sequence. Since reqwest is now optional and source code still has unconditional `use reqwest::*` statements, the `--no-default-features` build will fail with missing imports. This is EXPECTED -- Plan 02 will add the cfg gates. The goal of this task is to verify that `--features full` builds cleanly, and to document the exact list of source files that need cfg gating in Plan 02.

Run these commands:

1. `cargo check -p pmcp --features full` -- MUST succeed (all deps present)
2. `cargo check --workspace` -- MAY fail on pmcp if default features don't include http-client; document errors
3. `cargo check -p pmcp --no-default-features` -- EXPECTED to fail; capture the list of files with `use reqwest` and `use tracing_subscriber` errors
4. `cargo check -p mcp-tester` -- MUST succeed (mcp-tester depends on pmcp with its own features)
5. `cargo check -p mcp-preview` -- MUST succeed
6. `cargo check -p cargo-pmcp` -- MUST succeed (it enables full features on pmcp)

For any build failures on commands 1, 4, 5, 6: fix them in Cargo.toml immediately. These are regressions.

For expected failures on commands 2 and 3: document the error file list in the SUMMARY. These are the files Plan 02 must add cfg gates to.

If `cargo check -p pmcp --features full` produces clippy-level warnings about unused deps (lazy_static, pin-project), that confirms they were truly unused -- no action needed.

If workspace members (mcp-tester, mcp-preview, cargo-pmcp) fail because they depend on pmcp features that now need `http-client`, add `http-client` to their pmcp dependency feature lists in their respective Cargo.toml files.
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/sdk/rust-mcp-sdk && cargo check -p pmcp --features full 2>&1 | tail -3 && echo "---" && cargo check -p mcp-tester 2>&1 | tail -3 && echo "---" && cargo check -p mcp-preview 2>&1 | tail -3 && echo "---" && cargo check -p cargo-pmcp 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p pmcp --features full` exits 0
    - `cargo check -p mcp-tester` exits 0
    - `cargo check -p mcp-preview` exits 0
    - `cargo check -p cargo-pmcp` exits 0
    - If any workspace member Cargo.toml was modified, it contains `http-client` in its pmcp features
  </acceptance_criteria>
  <done>Feature matrix verified: full-feature build succeeds for pmcp and all workspace members. Expected no-default-features failures documented for Plan 02.</done>
</task>

</tasks>

<verification>
- `cargo check -p pmcp --features full` passes
- `cargo check -p mcp-tester` passes
- `cargo check -p mcp-preview` passes
- `cargo check -p cargo-pmcp` passes
- Cargo.toml contains no lazy_static or pin-project entries
- Cargo.toml has http-client and logging features defined
- reqwest is marked optional in Cargo.toml
</verification>

<success_criteria>
All Cargo.toml dependency and feature changes are applied. The full-feature build succeeds for the entire workspace. The expected breakage list for no-default-features builds is documented for Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/52-reduce-transitive-dependencies/52-01-SUMMARY.md`
</output>
