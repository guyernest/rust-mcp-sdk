---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 07
type: execute
wave: 6
depends_on: ["93-06"]
files_modified:
  - crates/pmcp-workbook-compiler/src/stage1.rs
  - crates/pmcp-workbook-compiler/src/lib.rs
  - crates/pmcp-workbook-compiler/examples/compile_a_workbook.rs
  - crates/pmcp-workbook-compiler/tests/reemit_tax_calc_golden.rs
  - crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx
autonomous: false
requirements: [WBCO-02, WBCO-05]

must_haves:
  truths:
    - "stage1 composes the collect-all analysis pass (lint + synth + freshness + drift) in a single ingest"
    - "The generic compile_workbook driver routes ingest → stage1 → synth → ratify → parse+DAG → reconcile → gate → emit with the manifest coming SOLELY from synthesize→ratify (no build_reference_manifest)"
    - "A neutral tax-calc.xlsx (authored NOT via umya) compiles through the pipeline and reproduces the committed tax-calc@1.1.0 golden (byte-identical, or structural-equivalence with the golden regenerated)"
    - "cargo run --example compile_a_workbook demonstrates ingest→emit on the neutral fixture"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/lib.rs"
      provides: "Wired generic compile_workbook driver (no build_reference_manifest)"
      contains: "compile_workbook"
    - path: "crates/pmcp-workbook-compiler/tests/reemit_tax_calc_golden.rs"
      provides: "Producer/consumer proof: re-emit reproduces the golden"
      contains: "tax-calc"
    - path: "crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx"
      provides: "Neutral source workbook authored without umya"
    - path: "crates/pmcp-workbook-compiler/examples/compile_a_workbook.rs"
      provides: "Library-level ingest→emit demonstration"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/lib.rs compile_workbook"
      to: "manifest::synthesize + ratify"
      via: "synth-driven manifest (no per-workbook Rust)"
      pattern: "synthesize|ratify"
    - from: "crates/pmcp-workbook-compiler/tests/reemit_tax_calc_golden.rs"
      to: "crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/"
      via: "byte/structural diff against the committed golden"
      pattern: "tax-calc@1.1.0"
---

<objective>
Wire the whole pipeline together: lift `stage1.rs` (the composed collect-all pass), wire the
generic `compile_workbook` driver in `lib.rs` (replacing the deleted `build_reference_manifest`
with `ingest → stage1 → synthesize → ratify → parse+DAG → reconcile → gate → emit`), author a
neutral `tax-calc.xlsx` (NOT via umya), prove the producer/consumer loop (re-emit reproduces the
committed `tax-calc@1.1.0` golden), and ship the library-level example.

Purpose: This is the capstone that proves the §5 manifest-driven generalization. The driver MUST
route the manifest solely through synth→ratify — proving a second workbook would be served ITS OWN
schema (the whole point of deleting build_reference_manifest). The byte-identical re-emit is the
strongest producer/consumer proof; the fixture must NOT be umya-authored (the provenance gate would
refuse it — Plan 02). O-2 decides byte-identical vs structural-equivalence; either way the golden is
the agreement target.
Output: wired driver, neutral fixture, producer/consumer proof test, example; full pipeline green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md

<interfaces>
Lift source (lighthouse, absolute, PRIVATE — scrub):
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/stage1.rs (composed collect-all pass)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/lib.rs (lines 443 build_candidate_model — the driver shape WITHOUT the hand-built manifest; lines 769-858 build_reference_manifest to DELETE)

In-repo target (the producer/consumer agreement):
- crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/ — the committed seven-member golden to reproduce (manifest.json, executable.ir.json, cell_map.json, layout.json, BUNDLE.lock, evidence/). The golden was synthesized from runtime types via tests/support/fixture_gen.rs (no source .xlsx exists — Pitfall 3).
- crates/pmcp-workbook-runtime render/rust_xlsxwriter — available for authoring the neutral .xlsx fixture (NOT umya).

Prior plans (all modules now exist): ingest (P02), provenance (P02), dialect/formula/dag (P03), manifest/reconcile/sheet_ir (P04), artifact/change_class (P05), gate (P06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift stage1 + wire the generic compile_workbook driver (WBCO-02)</name>
  <files>crates/pmcp-workbook-compiler/src/stage1.rs, crates/pmcp-workbook-compiler/src/lib.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/stage1.rs (the composed lint+synth+freshness+drift pass)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/lib.rs (lines 443 build_candidate_model driver; lines 769-858 build_reference_manifest — DELETE)
    - crates/pmcp-workbook-compiler/src/lib.rs (the compile_workbook stub + module declarations from Plan 01)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (## Code Examples — the generic compile_workbook driver shape; Anti-Pattern: never copy build_reference_manifest onto the emit path)
  </read_first>
  <action>
    Lift stage1.rs (the composed collect-all analysis pass — lint + synth + freshness + drift in one
    ingest, emitting collect-all located findings, D-01). Wire the generic compile_workbook driver in
    lib.rs replacing the Plan-01 stub: read ORIGINAL bytes (provenance) → ingest::ingest (umya) →
    stage1::run_stage1 → manifest::synthesize → manifest::ratify → formula::parse_all + dag::build_dag
    (whitelist-at-parse) → reconcile::reconcile (named-out ERROR, helper WARNING) → change_class gate
    vs prior baseline → emit on a clean gate (first version: gate no-op, D-12). The manifest comes
    SOLELY from synthesize→ratify — build_reference_manifest must NOT appear on this path (it is the
    one surviving §5 gap; keep ONLY a scrubbed #[cfg(test)] anti-drift fixture if genuinely needed).
    Errors block emit; warnings are advisory (D-01/D-02). Scrub all customer identifiers; the driver
    takes a workbook_path argument (NO REFERENCE_WORKBOOK_PATH/WORKFLOW_NAME consts).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler stage1:: 2>&1 | tail -5 && ! grep -rn 'build_reference_manifest' crates/pmcp-workbook-compiler/src/ --include='*.rs' | grep -v test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler stage1::` passes; the crate builds with the wired driver.
    - GREP GATE: `! grep -rn 'build_reference_manifest' crates/pmcp-workbook-compiler/src/ --include='*.rs' | grep -v test` returns nothing.
    - compile_workbook routes through synthesize+ratify: `grep -qE 'synthesize|ratify' crates/pmcp-workbook-compiler/src/lib.rs`
    - No REFERENCE_WORKBOOK_PATH/WORKFLOW_NAME consts: `! grep -rn 'REFERENCE_WORKBOOK_PATH\|WORKFLOW_NAME' crates/pmcp-workbook-compiler/src/`
    - Zero customer identifiers.
  </acceptance_criteria>
  <done>stage1 lifted; generic compile_workbook wired (synth-driven, no build_reference_manifest, no reference-path consts); pipeline builds; tests green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide the producer/consumer proof shape (O-2)</name>
  <read_first>
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Pitfall 3 / O-2 — no tax-calc.xlsx exists; byte-identical vs structural-equivalence)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/manifest.json + BUNDLE.lock (the committed golden the re-emit must reproduce)
  </read_first>
  <action>Do not write code in this task. Compare the byte-identical and structural-regenerate options below against the committed tax-calc@1.1.0 golden and the no-source-.xlsx constraint, and record the chosen proof shape with rationale for Task 3 to implement.</action>
  <decision>How does the producer/consumer proof reproduce the committed tax-calc@1.1.0 golden, given no source .xlsx exists and the golden was synthesized from runtime types?</decision>
  <context>The CONTEXT calls the golden "the producer/consumer agreement target." A neutral tax-calc.xlsx must be authored (NOT via umya — the provenance gate refuses umya-authored fixtures). Byte-identical re-emit is the strongest proof but may be costly to hit exactly; structural-equivalence is weaker but tractable.</context>
  <options>
    <option id="byte-identical">
      <name>Author tax-calc.xlsx → compile → diff byte-identical against the committed golden</name>
      <pros>Strongest proof; the committed golden stays the frozen agreement target unchanged</pros>
      <cons>Authoring an .xlsx whose compiled output is byte-for-byte the golden may require iterating the fixture; higher effort</cons>
    </option>
    <option id="structural-regenerate">
      <name>Author tax-calc.xlsx → compile → assert structural equivalence, then regenerate the golden FROM the compiler and re-commit</name>
      <pros>Tractable; makes the compiler the source of truth for the golden going forward</pros>
      <cons>Weaker (the golden is no longer independently frozen); must update the Phase 92 fixture and confirm the Phase 92 consumer tests still pass</cons>
    </option>
  </options>
  <acceptance_criteria>
    - A decision is recorded (byte-identical or structural-regenerate) with rationale, to be implemented in Task 3.
  </acceptance_criteria>
  <resume-signal>Select: byte-identical or structural-regenerate</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author neutral tax-calc.xlsx, write the producer/consumer proof test + the example (WBCO-05)</name>
  <files>crates/pmcp-workbook-compiler/tests/fixtures/tax-calc.xlsx, crates/pmcp-workbook-compiler/tests/reemit_tax_calc_golden.rs, crates/pmcp-workbook-compiler/examples/compile_a_workbook.rs</files>
  <read_first>
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/ (the full seven-member golden to reproduce — manifest.json shows the inputs/enum/outputs the fixture .xlsx must contain)
    - crates/pmcp-workbook-runtime render/ rust_xlsxwriter (the writer to author the .xlsx — NOT umya)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Pitfall 3 — fixture must NOT be umya-authored; the provenance gate refuses umya identity)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-VALIDATION.md (## ALWAYS — EXAMPLE compile_a_workbook on the neutral fixture)
  </read_first>
  <action>
    Author a neutral tax-calc.xlsx (3 inputs incl. an inline-DV enum, a governed bracket table,
    multiple named outputs, real formulas with cached values) — author it with real Excel or
    rust_xlsxwriter, NEVER via umya (the provenance gate would refuse a umya-authored fixture). Use
    only neutral domain names (tax-calc; zero customer identifiers). Write
    tests/reemit_tax_calc_golden.rs implementing the Task-2 decision: compile the fixture through
    compile_workbook and either (byte-identical) diff every emitted member against the committed
    tax-calc@1.1.0 golden, or (structural-regenerate) assert structural equivalence and regenerate +
    re-commit the golden, then confirm the Phase 92 consumer tests still pass. Write
    examples/compile_a_workbook.rs demonstrating ingest→emit on the neutral fixture (library-level;
    the CLI is Phase 94). If structural-regenerate was chosen, run the Phase 92 toolkit workbook tests
    to confirm the regenerated golden still loads.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler --test reemit_tax_calc_golden 2>&1 | tail -5 && cargo run -p pmcp-workbook-compiler --example compile_a_workbook 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler --test reemit_tax_calc_golden` passes (re-emit reproduces the golden per the chosen shape).
    - `cargo run -p pmcp-workbook-compiler --example compile_a_workbook` runs and emits a bundle on the neutral fixture.
    - The fixture is NOT umya-authored (the provenance gate accepts it — a test compiles it without an oracle/non-excel-app refusal).
    - If structural-regenerate: the Phase 92 toolkit workbook tests still pass (`cargo test -p pmcp-server-toolkit workbook`).
    - Zero customer identifiers in the fixture/test/example: `! grep -rEi 'ufh|towelrad|coil|heat_source|heat_pump|underfloor|radiator|plot3' crates/pmcp-workbook-compiler/tests/ crates/pmcp-workbook-compiler/examples/`
  </acceptance_criteria>
  <done>Neutral tax-calc.xlsx authored (not via umya); producer/consumer proof green; example runs; Phase 92 consumer still loads the golden.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite gate — quality-gate + purity-check + all grep gates green</name>
  <files>crates/pmcp-workbook-compiler/src/lib.rs</files>
  <read_first>
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-VALIDATION.md (## Sampling Rate — phase gate: full suite + byte-identical re-emit + umya-fabrication-refused + grep gates)
    - CLAUDE.md (make quality-gate is the CI-equivalent gate; ALWAYS requirements)
  </read_first>
  <action>
    Run the phase-gate suite and fix any residual issues: `make quality-gate` (fmt + clippy
    pedantic/nursery + build + test + audit) then `make purity-check`. Confirm the full per-requirement
    test map is green and all four grep gates pass: no delta.abs() in reconcile/classifier.rs; no
    build_reference_manifest on non-test paths; zero customer identifiers in non-fixture paths; the
    umya-fabrication-refused regression. Confirm both fuzz targets build. Resolve any clippy
    cognitive-complexity violations per the project's refactor techniques (cog ≤25; allow with a
    `// Why:` annotation only for irreducibly-complex parser/AST/protocol functions, hard cap 50).
  </action>
  <verify>
    <automated>make quality-gate && make purity-check && ! grep -rEi 'ufh|towelrad|coil|heat_source|heat_pump|underfloor|radiator|plot3' crates/pmcp-workbook-compiler/src/</automated>
  </verify>
  <acceptance_criteria>
    - `make quality-gate` exits 0 (fmt/clippy/build/test/audit).
    - `make purity-check` exits 0 (compiler positive umya assertion + served-crate negatives + single-version guard).
    - All four grep gates pass (delta.abs / build_reference_manifest / customer-identifier / umya-fabrication-refused).
    - Both fuzz targets build (`cargo fuzz build fuzz_formula_parser` and `cargo fuzz build fuzz_provenance_reader`).
  </acceptance_criteria>
  <done>Full quality-gate + purity-check green; all grep gates pass; fuzz targets build; phase ready for verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| neutral fixture .xlsx → provenance gate | a umya-authored fixture would be refused; the proof requires a non-umya-authored workbook |
| compiler output → Phase 92 consumer | the emitted bytes must satisfy the frozen seven-member boot contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-07-FIXTURE | Spoofing | a umya-authored fixture (fabricated provenance) used as the proof source | mitigate | Author tax-calc.xlsx via real Excel / rust_xlsxwriter, NEVER umya; a test compiles it without an oracle/non-excel-app refusal (Task 3) |
| T-93-07-DRIFT | Tampering | build_reference_manifest sneaking back onto the wired emit path (a second workbook served the wrong schema) | mitigate | Grep gate on non-test paths in the wired driver (Task 1) + the phase-gate suite (Task 4) |
| T-93-07-SCRUB | Information Disclosure | a surviving customer identifier in scrubbed code/comments/fixtures/docs | mitigate | Customer-identifier grep gate across src/ tests/ examples/ (Tasks 1, 3, 4) |
| T-93-07-SC | Tampering | npm/pip/cargo installs (none new here; reader deps gated in Plan 01) | accept | No new package-manager installs in this plan; reader-dep legitimacy already gated in Plan 01 Task 1 |
</threat_model>

<verification>
- stage1 + wired driver build; producer/consumer proof green; example runs; Phase 92 consumer still loads the golden.
- make quality-gate + make purity-check green; all four grep gates pass; both fuzz targets build.
- Zero customer identifiers anywhere in the new crate.
</verification>

<success_criteria>
- WBCO-02: the wired generic compile_workbook driver is synth-driven (no build_reference_manifest), proving the §5 generalization.
- WBCO-05: the producer/consumer proof reproduces the committed tax-calc@1.1.0 golden (byte-identical or structural per O-2).
- The full phase gate (quality-gate + purity-check + grep gates + fuzz builds) is green.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-07-SUMMARY.md` when done
</output>
