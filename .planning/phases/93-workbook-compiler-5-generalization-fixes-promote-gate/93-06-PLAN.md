---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 06
type: execute
wave: 5
depends_on: ["93-05"]
files_modified:
  - crates/pmcp-workbook-compiler/src/gate/mod.rs
  - crates/pmcp-workbook-compiler/src/gate/corpus.rs
  - crates/pmcp-workbook-compiler/src/gate/accept.rs
  - crates/pmcp-workbook-compiler/src/gate/governed_artifact.rs
autonomous: true
requirements: [WBGV-04, WBGV-05, WBGV-06]

must_haves:
  truths:
    - "The golden-corpus gate auto-derives input cases from manifest defaults + enum domains (the BA authors NO cases) and replays them through BOTH the prior accepted version and the candidate"
    - "An over-tolerance named-output delta blocks promotion unless a content-hash-fingerprinted ApprovalRecord covers the candidate"
    - "On a block the gate prints the deltas, the change class, and the exact copy-pasteable approve command"
    - "accept(--approver, --effective-date) records a fingerprint-bound ApprovalRecord, re-baselines the corpus, and lets the version through"
    - "Promotion writes the new bundle to its own @<next_version> dir and never overwrites the baseline (promote-twice → two distinct dirs, prior baseline byte-identical)"
    - "On the first version the gate is a no-op that establishes the baseline (D-12)"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/gate/corpus.rs"
      provides: "Auto-derived corpus generator + candidate_fingerprint + ApprovalRecord"
      contains: "candidate_fingerprint"
    - path: "crates/pmcp-workbook-compiler/src/gate/accept.rs"
      provides: "accept(): records fingerprint-bound ApprovalRecord, re-baselines corpus"
      contains: "ApprovalRecord"
    - path: "crates/pmcp-workbook-compiler/src/gate/mod.rs"
      provides: "Gate-before-write; EmitLane Seed/GatedUpdate; CR-02 versioned non-overwriting write"
      contains: "GatedUpdate"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/gate/corpus.rs"
      to: "manifest defaults + enum domains"
      via: "auto-derived case grid (D-09)"
      pattern: "default|enum|allowed_values"
    - from: "crates/pmcp-workbook-compiler/src/gate/accept.rs"
      to: "candidate_fingerprint"
      via: "content-hash binding (WR-04)"
      pattern: "candidate_fingerprint|fingerprint"
---

<objective>
Lift the promote gate (corpus replay + content-hash-fingerprinted `ApprovalRecord` +
`accept` + CR-02 versioned non-overwriting write) and apply the one net-new gate design:
the D-09 auto-derived regression corpus (the BA authors no cases — the corpus is the prior
version's own behavior captured automatically). The `candidate_fingerprint`/`ApprovalRecord`/
`accept`/CR-02 promote machinery is lifted verbatim; only case GENERATION is new.

Purpose: This is the promote-time governance. WBGV-06 (CR-02 versioned non-overwriting) is
ALREADY implemented in the lighthouse — lift + verify the promote-twice test stays green.
WBGV-05's content-hash fingerprint closes the inherit-an-old-approval hole. The auto-corpus
is the genuine net-new design (O-4: bound the grid). The `--accept` FLAG is Phase 94; the gate
BEHAVIOR, ApprovalRecord shape, and corpus mechanics are built HERE as library verbs.
Output: gate/ module — auto-corpus, fingerprint-bound approval, CR-02 versioned promote.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md

<interfaces>
Lift source (lighthouse, absolute, PRIVATE — scrub; CR-02 already implemented, lift WITH tests):
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/gate/{mod,corpus,accept,governed_artifact}.rs (corpus.rs:96-160 candidate_fingerprint + ApprovalRecord + ApprovalCase; accept.rs:265 LOC accept)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/commands/compile_workbook.rs (lines 603-611 CR-02 candidate.version = next_version → GatedUpdate write; the promote-twice integration test)

In-repo re-export source:
- crates/pmcp-workbook-runtime/src/changelog.rs — VersionChangelog/ChangeClass (from_version/to_version shape; EmitLane Seed/GatedUpdate constraint).
- crates/pmcp-workbook-compiler/src/manifest/ (Plan 04) — the synthesized Manifest with defaults + allowed_values the corpus generator reads.
- crates/pmcp-workbook-compiler/src/change_class/ (Plan 05) — effective_policy + classify the gate consumes.
- crates/pmcp-workbook-compiler/src/artifact/ (Plan 05) — write_candidate_bundle / EmitLane the CR-02 promote uses.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-derive the regression corpus (D-09) + lift candidate_fingerprint/ApprovalRecord (WBGV-04)</name>
  <files>crates/pmcp-workbook-compiler/src/gate/corpus.rs, crates/pmcp-workbook-compiler/src/gate/governed_artifact.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/gate/{corpus,governed_artifact}.rs (corpus.rs:96-160 candidate_fingerprint, ApprovalRecord, ApprovalCase, expected_outputs BTreeMap — the BA-curated cases.json loader to REPLACE)
    - crates/pmcp-workbook-compiler/src/manifest/ (the synthesized Manifest: per-input default + allowed_values the generator reads)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Pitfall 6 / O-4 — bound the grid: default + one case per enum member + numeric boundary, cap at N)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md (D-09 corpus = auto-derived baseline; D-12 first version no-op)
  </read_first>
  <behavior>
    - Test corpus_auto_derives_from_manifest: the generator builds the case grid from manifest
      defaults + enum domains alone (no cases.json, no BA input).
    - Test corpus_grid_is_bounded: the grid is default + one case per enum member + numeric
      boundary cases, capped at the documented N (O-4 policy) — assert it never combinatorially explodes.
    - Test fingerprint_binds_content: candidate_fingerprint is a sha256 over prev-hash + candidate-hash
      + region deltas — a different candidate content yields a different fingerprint (WR-04).
    - Test over_tolerance_blocks: a named output that moved beyond tolerance with no covering approval blocks (returns a block decision with the deltas).
  </behavior>
  <action>
    Lift gate/{corpus,governed_artifact}.rs. REPLACE the BA-curated cases.json loader with an
    AUTO-DERIVED corpus generator (D-09): build the input grid from the synthesized manifest's
    per-input defaults + enum domains — bounded per O-4 (default case + one case per enum member
    holding others at default + numeric boundary cases for numeric inputs, capped at a small
    documented N; document the chosen policy in the SUMMARY). The corpus replays the grid through
    BOTH the prior accepted version's IR and the candidate's IR; the PRIOR version's outputs ARE
    the golden (captured automatically). Reuse candidate_fingerprint (sha256 over prev-hash +
    candidate-hash + region deltas), ApprovalRecord, ApprovalCase, and the expected_outputs BTreeMap
    shape VERBATIM — only case generation is new. An over-tolerance named-output delta with no
    covering fingerprint-matched approval = block. Scrub all customer identifiers; lift the corpus
    tests, scrubbed, plus the four behavior tests; add the "no seeded default outside allowed_values"
    property test (ALWAYS).
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler gate::corpus corpus_auto_derives over_tolerance_blocks 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler gate::corpus` passes including the four behavior tests.
    - No BA-curated cases.json on the gate path: `! grep -rn 'cases.json' crates/pmcp-workbook-compiler/src/gate/`
    - candidate_fingerprint is content-bound (the fingerprint_binds_content test passes).
    - The grid-size policy is documented and bounded (corpus_grid_is_bounded passes).
    - Zero customer identifiers in gate/corpus.rs + governed_artifact.rs.
  </acceptance_criteria>
  <done>Auto-derived corpus (D-09) replacing cases.json; fingerprint-bound ApprovalRecord lifted; over-tolerance blocking; bounded grid; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift accept() + CR-02 versioned non-overwriting promote + the block-loop output (WBGV-05/06)</name>
  <files>crates/pmcp-workbook-compiler/src/gate/mod.rs, crates/pmcp-workbook-compiler/src/gate/accept.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/gate/{mod,accept}.rs (accept.rs:265 LOC; the block-loop output formatting)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/commands/compile_workbook.rs (lines 603-611 CR-02 candidate.version = next_version → GatedUpdate; the promote-twice test)
    - crates/pmcp-workbook-compiler/src/artifact/ (write_candidate_bundle + EmitLane Seed/GatedUpdate from Plan 05)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### …/src/gate/ — Pattern 3 gate-before-write; CR-02 verify don't re-implement)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md (D-10 block loop prints exact --accept command; D-11 workbook-declared version; D-12 first version no-op)
  </read_first>
  <behavior>
    - Test promote_twice_two_dirs: promoting twice yields two distinct {bundle_id}@{version}/ dirs;
      the prior baseline survives BYTE-IDENTICAL; BUNDLE.lock version == changelog.to_version (CR-02/WBGV-06).
    - Test first_version_gate_noop: on the first version (no prior baseline) the gate is a no-op that establishes the baseline (D-12).
    - Test block_prints_accept_command: on a block, the output includes the changed outputs + deltas,
      the change class, and the exact copy-pasteable `--accept --approver <X> --effective-date <D>` command (D-10).
    - Test accept_records_and_passes: running accept(approver, effective_date) writes a fingerprint-bound ApprovalRecord, re-baselines the corpus, and lets the version through (WBGV-05).
    - Test malformed_lane_writes_nothing: a malformed EmitLane (wrong from_version shape) writes ZERO bytes.
  </behavior>
  <action>
    Lift gate/{mod,accept}.rs and the CR-02 promote logic from commands/compile_workbook.rs (lift the
    PROMOTE mechanics, NOT the customer command shell). CR-02 is ALREADY implemented — set
    candidate.version = next_version before a GatedUpdate write to a NEW {bundle_id}@{next_version}/
    dir; lift + verify the promote-twice test (two dirs, baseline byte-identical) green, do NOT
    re-implement. accept(approver, effective_date) records a fingerprint-bound ApprovalRecord
    (content-hash), re-baselines the corpus, and lets the version through (WBGV-05). The block-loop
    output (D-10) prints the changed outputs + deltas + change class + the exact copy-pasteable
    `--accept --approver <X> --effective-date <D>` command as STRUCTURED output (the CLI in Phase 94
    renders it; this is the library-level structured result). EmitLane Seed/GatedUpdate enforces the
    changelog from_version shape — a malformed lane writes ZERO bytes. D-12: first version no-op
    establishes the baseline. D-11: version is workbook-declared (BUNDLE.lock version == declared ==
    changelog.to_version). Scrub all customer identifiers; lift the gate/accept tests, scrubbed, plus
    the five behavior tests.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler gate:: accept:: promote_twice 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler gate:: accept::` passes including the five behavior tests.
    - promote_twice_two_dirs passes: two distinct @version dirs, prior baseline byte-identical (CR-02/WBGV-06).
    - block_prints_accept_command: the structured block output contains the `--accept --approver` command (D-10).
    - accept_records_and_passes: an ApprovalRecord with a content-hash fingerprint is written (WBGV-05).
    - malformed_lane_writes_nothing passes (fail-closed write).
    - Zero customer identifiers in gate/mod.rs + accept.rs.
  </acceptance_criteria>
  <done>accept() + CR-02 versioned non-overwriting promote + block-loop structured output lifted + scrubbed; promote-twice baseline byte-identical; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate content → approval coverage | an approval must cover exactly the content it reviewed |
| promote → on-disk baseline | promoting must not destroy the prior version's audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-06-INHERIT | Repudiation | an approval inherited across unrelated bundle contents (forgeable version label) | mitigate | candidate_fingerprint binds prev+candidate content hashes — an approval can never cover content it did not review (Task 1, WR-04/WBGV-05) |
| T-93-06-DESTROY | Repudiation | baseline destruction on promote (audit-trail loss) | mitigate | CR-02 versioned non-overwriting write to a NEW @version dir; promote-twice baseline byte-identical (Task 2, WBGV-06) |
| T-93-06-BYPASS | Elevation | an over-tolerance breaking change promoted without an approval | mitigate | The gate blocks over-tolerance deltas absent a fingerprint-matched ApprovalRecord; first-version no-op only when no prior baseline (Tasks 1, 2, WBGV-04) |
| T-93-06-PARTIAL | Tampering | a malformed emit lane writing a partial/corrupt bundle | mitigate | EmitLane Seed/GatedUpdate from_version-shape check → ZERO bytes on a malformed lane (Task 2) |
</threat_model>

<verification>
- gate/corpus + gate/accept tests green (auto-corpus, fingerprint binding, promote-twice byte-identical, block-loop output).
- No cases.json on the gate path; candidate_fingerprint content-bound; bounded grid documented.
- Zero customer identifiers.
</verification>

<success_criteria>
- WBGV-04: auto-derived corpus blocks over-tolerance named-output deltas absent a matching ApprovalRecord.
- WBGV-05: accept() records a fingerprint-bound ApprovalRecord, re-baselines the corpus.
- WBGV-06: CR-02 versioned non-overwriting promote, prior baseline byte-identical — lifted WITH the promote-twice test, green.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-06-SUMMARY.md` when done
</output>
