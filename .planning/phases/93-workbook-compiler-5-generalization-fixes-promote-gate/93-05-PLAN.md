---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 05
type: execute
wave: 4
depends_on: ["93-04"]
files_modified:
  - crates/pmcp-workbook-compiler/src/artifact/mod.rs
  - crates/pmcp-workbook-compiler/src/artifact/bundle_lock.rs
  - crates/pmcp-workbook-compiler/src/artifact/cell_map.rs
  - crates/pmcp-workbook-compiler/src/artifact/evidence.rs
  - crates/pmcp-workbook-compiler/src/artifact/executable.rs
  - crates/pmcp-workbook-compiler/src/artifact/layout.rs
  - crates/pmcp-workbook-compiler/src/change_class/mod.rs
  - crates/pmcp-workbook-compiler/src/change_class/ir_identity.rs
  - crates/pmcp-workbook-compiler/src/change_class/schema_diff.rs
autonomous: true
requirements: [WBCO-05, WBGV-01, WBGV-02, WBGV-03, WBGV-07]

must_haves:
  truths:
    - "The compiler emits the seven-member bundle (manifest/IR/cell_map/layout/BUNDLE.lock/evidence) using the runtime's build_bundle_lock + fold_evidence_hash"
    - "BUNDLE.lock emits bundle_id (Phase 92 D-17 rename), accepted by the Phase 92 BundleLoader"
    - "ratify_tiers skips frozen-enum inputs so no out-of-enum empty-string default is seeded (verified against the COMMITTED manifest)"
    - "The change-class classifier is symmetric — demotion-direction changes each produce a non-empty class (never silent HotReload); assumption changes hard-block via the strictest-policy reducer"
    - "A stable canonical IR sub-DAG identity hash distinguishes numeric drift from semantic redefinition"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/artifact/mod.rs"
      provides: "emit_bundle (seven members) + ratify_tiers WR-01 skip"
      contains: "ratify_tiers"
    - path: "crates/pmcp-workbook-compiler/src/change_class/mod.rs"
      provides: "Symmetric demotion-aware classifier + strictest-policy reducer"
      contains: "effective_policy"
    - path: "crates/pmcp-workbook-compiler/src/change_class/ir_identity.rs"
      provides: "Canonical IR sub-DAG identity hash"
      contains: "ir_subdag_hash"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/artifact/bundle_lock.rs"
      to: "pmcp_workbook_runtime::build_bundle_lock"
      via: "shared combined-hash fold"
      pattern: "build_bundle_lock|fold_evidence_hash"
    - from: "crates/pmcp-workbook-compiler/src/change_class/mod.rs"
      to: "pmcp_workbook_runtime::ChangeClass"
      via: "re-exported change-class enum"
      pattern: "ChangeClass"
---

<objective>
Lift the bundle-emit artifact group (seven members + BUNDLE.lock via the runtime's
shared hash helpers, WBCO-05; the WR-01 enum-tier skip, WBGV-07) and the change-class
group (symmetric demotion-aware classifier WBGV-01, strictest-policy reducer WBGV-02,
canonical IR sub-DAG identity hash WBGV-03). Per the research, the CR-01/WR-01 fixes are
ALREADY implemented in the lighthouse — these are invariants to LIFT WITH THEIR TESTS and
verify stay green, NOT net-new bug fixes.

Purpose: The emit path is the compiler↔server contract (must satisfy the Phase 92
seven-member golden, emitting `bundle_id` per D-17). The change-class classifier
completeness is a security property — a demotion or assumption change must NEVER silently
auto-HotReload past the gate. The hash helpers must come from the runtime so the served
loader recomputes the same combined hash at boot.
Output: artifact/, change_class/ modules with their lifted symmetric/skip/identity tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md
@.planning/phases/92-bundlesource-served-tool-toolkit-module/92-CONTEXT.md

<interfaces>
Lift source (lighthouse, absolute, PRIVATE — scrub; CR-01/WR-01 already implemented, lift WITH tests):
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/artifact/{mod,bundle_lock,cell_map,evidence,executable,layout}.rs (mod.rs:121-130 ratify_tiers WR-01 skip)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/change_class/{mod,ir_identity,schema_diff}.rs (mod.rs:165-255 symmetric classifier; mod.rs:351-359 effective_policy; ir_identity.rs:421 LOC)

In-repo re-export source + output contract:
- crates/pmcp-workbook-runtime/src/artifact_model.rs — build_bundle_lock, fold_evidence_hash, sha256_hex, BundleLock (NEVER hand-roll the combined hash; the served loader recomputes with these).
- crates/pmcp-workbook-runtime/src/changelog.rs — ChangeClass/OutputDelta/VersionChangelog (re-export; the served diff_version tool reads the SAME enum).
- crates/pmcp-server-toolkit/src/workbook/ (mod.rs, handler.rs, input.rs, schema.rs) — the CONSUMER contract the emitted bytes must satisfy at boot.
- crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/ — the seven-member golden (manifest.json, executable.ir.json, cell_map.json, layout.json, BUNDLE.lock, evidence/) to reproduce; BUNDLE.lock uses bundle_id (D-17), Manifest carries annotations (D-18).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift artifact emit (seven members + bundle_id BUNDLE.lock) + WR-01 enum-tier skip (WBCO-05/WBGV-07)</name>
  <files>crates/pmcp-workbook-compiler/src/artifact/mod.rs, crates/pmcp-workbook-compiler/src/artifact/bundle_lock.rs, crates/pmcp-workbook-compiler/src/artifact/cell_map.rs, crates/pmcp-workbook-compiler/src/artifact/evidence.rs, crates/pmcp-workbook-compiler/src/artifact/executable.rs, crates/pmcp-workbook-compiler/src/artifact/layout.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/artifact/{mod,bundle_lock,cell_map,evidence,executable,layout}.rs (mod.rs:121-130 ratify_tiers; emit_bundle)
    - crates/pmcp-workbook-runtime/src/artifact_model.rs (build_bundle_lock/fold_evidence_hash/sha256_hex/BundleLock to reuse)
    - crates/pmcp-server-toolkit/tests/fixtures/tax-calc@1.1.0/BUNDLE.lock + manifest.json (the bundle_id rename + annotations field the emit must match)
    - crates/pmcp-server-toolkit/src/workbook/ (loader/handler/input/schema — the boot contract the bytes must satisfy)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### …/src/artifact/ — three analogs + ratify_tiers excerpt)
  </read_first>
  <behavior>
    - Test emit_seven_members: emit_bundle writes manifest.json, executable.ir.json, cell_map.json, layout.json, BUNDLE.lock, and an evidence/ dir.
    - Test bundle_lock_uses_bundle_id: the emitted BUNDLE.lock carries bundle_id (D-17 rename), NOT the lighthouse workflow field.
    - Test bundle_lock_via_runtime_helpers: the combined hash is computed via build_bundle_lock/fold_evidence_hash (not a hand-rolled fold).
    - Test ratify_skips_frozen_enum_inputs: an input with allowed_values (frozen enum) is NOT assigned a Variable tier — verified against the COMMITTED (serialized) manifest, not the in-memory builder (WBGV-07/WR-01).
  </behavior>
  <action>
    Lift artifact/{mod,bundle_lock,cell_map,evidence,executable,layout}.rs. Emit the seven-member
    bundle; compute BUNDLE.lock via the re-exported runtime build_bundle_lock/fold_evidence_hash/
    sha256_hex (NEVER hand-roll the combined hash — the served loader recomputes with these). Rename
    the lock's workflow field to bundle_id (Phase 92 D-17) so the Phase 92 BundleLoader accepts it.
    Lift ratify_tiers (mod.rs:121-130) VERBATIM — it ALREADY skips allowed_values.is_some() inputs
    (WR-01); do NOT re-implement. Carry over its ratify_skips_frozen_enum_inputs test and re-point it
    at the COMMITTED manifest (WBGV-07). Reconcile the emitted Manifest's annotations field. Scrub all
    customer identifiers; lift the artifact tests, scrubbed.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler artifact:: ratify_skips_frozen_enum 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler artifact::` passes including the four behavior tests.
    - Combined hash via runtime helpers: `grep -qE 'build_bundle_lock|fold_evidence_hash' crates/pmcp-workbook-compiler/src/artifact/bundle_lock.rs`
    - BUNDLE.lock emits bundle_id (a test serializes the lock and asserts the bundle_id key).
    - ratify_skips_frozen_enum_inputs passes against the committed manifest (WBGV-07).
    - Zero customer identifiers in artifact/.
  </acceptance_criteria>
  <done>Seven-member emit lifted + scrubbed; bundle_id rename + annotations reconciled; runtime hash helpers reused; WR-01 enum-tier skip verified against the committed manifest; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Lift the symmetric change-class classifier + strictest-policy reducer + IR identity hash (WBGV-01/02/03)</name>
  <files>crates/pmcp-workbook-compiler/src/change_class/mod.rs, crates/pmcp-workbook-compiler/src/change_class/ir_identity.rs, crates/pmcp-workbook-compiler/src/change_class/schema_diff.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/change_class/{mod,ir_identity,schema_diff}.rs (mod.rs:165-255 symmetric classifier; mod.rs:351-359 effective_policy Ord; the 8 demotion tests)
    - crates/pmcp-workbook-runtime/src/changelog.rs (ChangeClass/OutputDelta/VersionChangelog to re-export — do NOT re-declare)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### …/src/change_class/ — Pattern 4 assumption-first; verify don't re-implement)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Pitfall 1 — plan as invariants to verify, NOT net-new fixes; State of the Art table)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md (D-08 material changes; locked §5 classifier behaviors)
  </read_first>
  <action>
    Lift change_class/{mod,ir_identity,schema_diff}.rs VERBATIM — the CR-01 symmetric classifier is
    ALREADY implemented (assumption involvement on EITHER side → Assumption/NeverAutoPromote; role
    flips away from Input/Output → InputSchema/OutputSchema; the Constant|Formula arm no longer
    silently drops a demotion). Re-export ChangeClass/OutputDelta/VersionChangelog from the runtime —
    do NOT re-declare (the served diff_version tool shares the enum). WBGV-02: effective_policy is the
    derived-Ord strictest-policy reducer (assumption → NeverAutoPromote wins amid hot-reloadable
    deltas). WBGV-03: ir_identity.rs ir_subdag_hash is the stable canonical IR sub-DAG identity hash
    distinguishing numeric drift from semantic redefinition; schema_diff.rs diff_outputs surfaces
    material changes only (D-08). CRITICALLY: lift the 8 demotion tests
    (assumption_demotion_to_plain_constant, input_demoted_to_constant, output_demoted_to_formula,
    enum_drop_with_role_flip, any_assumption_forces*, …) and the symmetry-cardinality property test;
    they MUST come over green — this is verification of an existing fix, not re-implementation. Add a
    classify(A→B)/classify(B→A) symmetry-cardinality PROPERTY test (ALWAYS requirement). Scrub all
    customer identifiers.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler change_class:: ir_identity:: any_assumption_forces 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler change_class:: ir_identity::` passes including the 8 demotion tests.
    - No local ChangeClass re-declaration: `! grep -rnE 'pub enum ChangeClass\b' crates/pmcp-workbook-compiler/src/change_class/`
    - A demotion (Input→Constant, assumption flip) produces a non-empty class — asserted by the lifted tests (never silent HotReload).
    - effective_policy returns NeverAutoPromote when any assumption change is present (any_assumption_forces test).
    - ir_subdag_hash distinguishes a numeric-only change from a formula redefinition (ir_identity test).
    - The symmetry-cardinality property test passes; zero customer identifiers in change_class/.
  </acceptance_criteria>
  <done>Symmetric classifier + strictest-policy reducer + IR identity hash lifted + scrubbed; 8 demotion tests + symmetry property test green; ChangeClass re-exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate bundle → on-disk emit | the emitted bytes are the compiler↔server boot contract |
| re-compile diff → auto-promote decision | a breaking change must not silently auto-apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-05-PROMO | Repudiation / Elevation | a breaking schema/assumption change auto-promoted with no approval | mitigate | CR-01 symmetric classifier + strictest-policy reducer — a demotion/assumption change can NEVER auto-HotReload (Task 2, WBGV-01/02); classifier completeness IS a security property |
| T-93-05-HASH | Tampering | a hand-rolled combined hash diverging from the served loader's, breaking boot integrity | mitigate | Reuse runtime build_bundle_lock/fold_evidence_hash; grep gate (Task 1) |
| T-93-05-ENUM | Input Validation | an out-of-enum empty-string default seeded into the served schema | mitigate | ratify_tiers skips frozen-enum inputs; verified against the COMMITTED manifest (Task 1, WBGV-07/WR-01) |
</threat_model>

<verification>
- artifact + change_class tests green (incl. the 8 demotion tests + WR-01 skip + symmetry property).
- Combined hash via runtime helpers; BUNDLE.lock emits bundle_id; ChangeClass re-exported.
- Zero customer identifiers.
</verification>

<success_criteria>
- WBCO-05: seven-member bundle emit satisfying the Phase 92 contract (bundle_id + annotations).
- WBGV-01/02/03: symmetric classifier, strictest-policy reducer, IR identity hash — lifted WITH tests, green.
- WBGV-07: WR-01 enum-tier skip verified against the committed manifest.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-05-SUMMARY.md` when done
</output>
