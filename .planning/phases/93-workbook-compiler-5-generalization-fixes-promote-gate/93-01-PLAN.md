---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pmcp-workbook-compiler/Cargo.toml
  - crates/pmcp-workbook-compiler/src/lib.rs
  - Cargo.toml
  - Makefile
autonomous: false
requirements: [WBCO-01, WBCO-02, WBDL-03]
user_setup:
  - service: crates-io packages (umya-spreadsheet, quick-xml, zip)
    why: "New third-party reader deps confined to the compiler crate; slopcheck was unavailable at research time so each is [ASSUMED] and must be verified before first install"
    dashboard_config:
      - task: "Verify umya-spreadsheet on crates.io/crates/umya-spreadsheet, quick-xml on crates.io/crates/quick-xml, zip on crates.io/crates/zip"
        location: "crates.io"

must_haves:
  truths:
    - "The new crate pmcp-workbook-compiler exists in the workspace and builds with umya/quick-xml/zip as direct deps"
    - "lib.rs re-exports runtime + dialect shared types (Manifest, ChangeClass, Dag, Expr, WHITELIST, finding, rounding) rather than re-declaring them"
    - "build_reference_manifest does NOT appear on any non-test path"
    - "make purity-check positively asserts umya IS present in the compiler tree AND re-confirms umya is ABSENT in every served-tree crate"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/Cargo.toml"
      provides: "Compiler crate manifest with reader deps + path deps on runtime/dialect"
      contains: "umya-spreadsheet"
    - path: "crates/pmcp-workbook-compiler/src/lib.rs"
      provides: "Crate-deny header + re-export surface + generic compile_workbook driver signature"
      contains: "pmcp_workbook_runtime"
    - path: "Makefile"
      provides: "Extended purity-check with compiler positive assertion + single-version guard"
      contains: "pmcp-workbook-compiler"
  key_links:
    - from: "crates/pmcp-workbook-compiler/Cargo.toml"
      to: "pmcp-workbook-runtime"
      via: "path dependency"
      pattern: "path = \"../pmcp-workbook-runtime\""
    - from: "Makefile purity-check"
      to: "pmcp-workbook-compiler"
      via: "cargo tree -i umya positive assertion"
      pattern: "cargo tree -p pmcp-workbook-compiler -i umya"
---

<objective>
Stand up the `pmcp-workbook-compiler` crate skeleton: the Cargo.toml (path deps on
runtime + dialect, the three reader deps with pins re-derived from umya's lock), the
`lib.rs` crate-deny header + re-export surface (re-export runtime/dialect shared types,
NEVER re-declare), the generic `compile_workbook` driver signature (with
`build_reference_manifest` deliberately absent), and the purity-gate extension that
asserts the reader lives HERE and nowhere served.

Purpose: This is the Wave 1 foundation every other plan builds on. The re-export
surface is the keystone — a second copy of `Manifest`/`ChangeClass`/`WHITELIST` would
make the served loader read a different definition. The purity-gate exception (compiler
is the ONE crate where umya is allowed) is the milestone's #1 trap and must be wired on
day one.
Output: A compiling (stub-bodied) compiler crate, workspace-registered, purity-gated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md

<interfaces>
<!-- Runtime re-export source (read these exact files to extract the symbols to re-export). -->
From crates/pmcp-workbook-runtime/src/lib.rs — the curated re-export surface to MIRROR
(the compiler re-exports the SAME symbols downstream): Manifest, CellRole, Dtype, Role,
InputTier, AnnotationDecl (manifest_model); Expr, BinOp, UnOp, CellValue (formula);
Dag, toposort (dag/resolve); ChangeClass, OutputDelta, OutputMeta, VersionChangelog
(changelog); LintFinding, LintReport, Severity (finding); build_bundle_lock,
fold_evidence_hash, sha256_hex, BundleLock (artifact_model); sheet_ir::rounding::{excel_round, excel_roundup, excel_ceiling}.

From crates/pmcp-workbook-dialect/src/lib.rs — WHITELIST (13-fn slice), DialectRules,
CandidateRole.

From crates/pmcp-workbook-dialect/Cargo.toml — the package/lib/exclude/docs.rs shape to copy
and the `pmcp-workbook-runtime = { version = "0.1.0", path = "../pmcp-workbook-runtime" }` dep form.

From Makefile lines 493-497 — PURITY_CRATES / PURITY_WRITER_CRATES lists + the documented
extension procedure (the compiler is the EXCEPTION; do NOT append it to PURITY_CRATES).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify reader-dep legitimacy (umya-spreadsheet, quick-xml, zip)</name>
  <read_first>
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (## Package Legitimacy Audit — all three tagged [ASSUMED]; slopcheck unavailable)
  </read_first>
  <action>Do not install or build anything yet. Present the three reader packages (umya-spreadsheet, quick-xml, zip) for human legitimacy verification on crates.io per the package-legitimacy gate; proceed to Task 2 only after explicit human approval. [ASSUMED] packages are never auto-approved.</action>
  <what-built>Nothing yet — this gate precedes the first install of the three new third-party reader crates. They are [ASSUMED] (slopcheck unavailable at research time); the planner is required to gate first install per the package-legitimacy protocol.</what-built>
  <how-to-verify>
    1. Open https://crates.io/crates/umya-spreadsheet — confirm 3.0.x line, established downloads, source github.com/MathNya/umya-spreadsheet.
    2. Open https://crates.io/crates/quick-xml — confirm 0.37 line, source github.com/tafia/quick-xml.
    3. Open https://crates.io/crates/zip — confirm 8.x stable line (NOT 9.0.0-pre), source github.com/zip-rs/zip2.
    4. Confirm none is a typosquat (exact names, high downloads, real repos).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all three packages are legitimate and approves the install, OR identifies a substitution. This checkpoint is NOT auto-approvable (legitimacy gate).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or name a replacement.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the compiler crate manifest + workspace registration</name>
  <files>crates/pmcp-workbook-compiler/Cargo.toml, Cargo.toml</files>
  <read_first>
    - crates/pmcp-workbook-dialect/Cargo.toml (the package/lib/exclude/docs.rs shape + path-dep form to mirror exactly)
    - crates/pmcp-workbook-runtime/Cargo.toml (the thiserror/serde/schemars/sha2/hex/chrono pins to match: thiserror "2", serde/serde_json "1", schemars "1.0", sha2 "0.11", hex "0.4", chrono "0.4")
    - Cargo.toml (workspace members list — add the new crate)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### crates/pmcp-workbook-compiler/Cargo.toml — the exact package + dep block)
  </read_first>
  <action>
    Create crates/pmcp-workbook-compiler/Cargo.toml mirroring the dialect crate's package
    block: name pmcp-workbook-compiler, version 0.1.0, edition 2021, license "MIT OR Apache-2.0",
    repository the SDK repo, description "Offline Excel→MCP compiler: umya-isolated ingest→lint→synth→DAG→reconcile→emit→promote-gate",
    exclude [".planning/", ".pmat/", "fuzz/", "tests/"], the docs.rs metadata block, and a
    [lib] with name pmcp_workbook_compiler / path src/lib.rs.
    Deps: pmcp-workbook-runtime = { version = "0.1.0", path = "../pmcp-workbook-runtime" } and
    an identical line for pmcp-workbook-dialect. Match runtime's pins for thiserror = "2"
    (NOT lighthouse's "1"), serde/serde_json = "1", schemars = "1.0", sha2 = "0.11",
    hex = "0.4", chrono = "0.4". Add the three reader deps: umya-spreadsheet = "3.0", plus
    quick-xml and zip pinned to the EXACT versions printed by the cargo tree commands in Task 3
    (do NOT guess; use the resolved transitive pins). Do NOT add pmcp-code-mode/SWC.
    Register the crate in the workspace [workspace.members] in the root Cargo.toml.
  </action>
  <verify>
    <automated>cargo metadata --no-deps --format-version 1 | grep -q pmcp-workbook-compiler && grep -q 'umya-spreadsheet' crates/pmcp-workbook-compiler/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo metadata` lists pmcp-workbook-compiler as a workspace member.
    - Cargo.toml contains path deps on pmcp-workbook-runtime AND pmcp-workbook-dialect.
    - thiserror is pinned to "2" (not "1"); no pmcp-code-mode dep present:
      `! grep -q 'pmcp-code-mode' crates/pmcp-workbook-compiler/Cargo.toml`
    - umya-spreadsheet present; quick-xml/zip versions match the cargo tree output.
  </acceptance_criteria>
  <done>Crate manifest committed, workspace-registered, reader deps + path deps present, thiserror=2, no SWC.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive reader pins, write lib.rs re-export surface + compile_workbook driver signature, extend purity-check</name>
  <files>crates/pmcp-workbook-compiler/src/lib.rs, Makefile</files>
  <read_first>
    - crates/pmcp-workbook-runtime/src/lib.rs (re-export style + crate-deny header lines 18-19; the changelog::Severity vs finding::Severity name-collision rule)
    - crates/pmcp-workbook-dialect/src/lib.rs (WHITELIST/DialectRules/CandidateRole to re-export)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### …/src/lib.rs — re-export rename workbook_runtime:: → pmcp_workbook_runtime::; ### Makefile purity-check extension)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Pitfall 4 purity-boundary; ## Code Examples — compile_workbook driver shape)
    - Makefile (lines 493-516 — PURITY_CRATES + extension procedure to modify)
  </read_first>
  <action>
    First run `cargo tree -p umya-spreadsheet -i quick-xml` and `cargo tree -p umya-spreadsheet -i zip`
    and record the resolved versions; if Task 2 used placeholder pins, correct them now so
    Cargo.lock holds a SINGLE quick-xml and a SINGLE zip version (no forked second copy).
    Write src/lib.rs with the crate-deny header copied verbatim from runtime lib.rs
    (deny clippy::unwrap_used/expect_used/panic, with the cfg(test) allow). Add the
    re-export surface: pub use pmcp_workbook_runtime::{toposort, BinOp, CellValue, Dag,
    ExcelError, Expr, UnOp}; pub use pmcp_workbook_runtime::changelog; pub use
    pmcp_workbook_runtime::{ChangeClass, OutputDelta, OutputMeta, VersionChangelog}; and the
    manifest/finding/artifact/rounding re-exports listed in <interfaces>. Re-export
    pmcp_workbook_dialect::{WHITELIST, DialectRules, CandidateRole}. Preserve the
    changelog::Severity (module-path-only) vs finding::Severity (bare) collision rule.
    Declare the generic driver signature `pub fn compile_workbook(workbook_path: &Path,
    out_root: &Path, approver: &str) -> Result<BundleLock, CompileError>` with a `todo!()`/
    unimplemented stub body marked for the downstream plans (NO build_reference_manifest,
    NO REFERENCE_WORKBOOK_PATH/WORKFLOW_NAME consts). Stub-declare the empty module tree
    (mod ingest; mod dialect; mod manifest; mod formula; mod dag; mod sheet_ir; mod reconcile;
    mod provenance; mod artifact; mod change_class; mod gate; mod stage1;) so downstream plans
    fill them. Add a CompileError enum (thiserror) placeholder.
    Extend Makefile purity-check: add a POSITIVE assertion that
    `cargo tree -p pmcp-workbook-compiler -i umya` is non-empty (reader IS here), add a guard
    that `cargo tree -i quick-xml` and `cargo tree -i zip` each print exactly one version
    (fail on a forked second copy), and re-run the existing per-served-crate negative
    assertions. Do NOT append pmcp-workbook-compiler to PURITY_CRATES.
  </action>
  <verify>
    <automated>cargo build -p pmcp-workbook-compiler 2>&1 | grep -qv 'error\[' && make purity-check</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs contains the crate-deny header: `grep -q 'deny(clippy::unwrap_used' crates/pmcp-workbook-compiler/src/lib.rs`
    - lib.rs re-exports from pmcp_workbook_runtime AND pmcp_workbook_dialect (grep both crate paths).
    - No re-declared Manifest/ChangeClass/WHITELIST in lib.rs:
      `! grep -qE 'pub (struct|enum) (Manifest|ChangeClass)\b' crates/pmcp-workbook-compiler/src/lib.rs`
    - build_reference_manifest absent on non-test paths:
      `! grep -rn 'build_reference_manifest' crates/pmcp-workbook-compiler/src/ --include='*.rs' | grep -v test`
    - No customer identifiers: `! grep -rEi 'ufh|towelrad|coil|heat_source|heat_pump|underfloor|radiator|plot3' crates/pmcp-workbook-compiler/src/`
    - `make purity-check` exits 0 (positive umya assertion for compiler + negatives for served crates + single-version guard).
    - Cargo.lock holds exactly one quick-xml and one zip version.
  </acceptance_criteria>
  <done>lib.rs re-export surface compiles, compile_workbook stub present, purity-check green with the compiler positive assertion + single-version guard, zero customer identifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party crate install → workspace | umya/quick-xml/zip are [ASSUMED] (slopcheck unavailable); supply-chain risk on first install |
| compiler crate → served-tree crates | the reader (umya/quick-xml/zip) must NOT leak across the purity boundary into runtime/dialect/toolkit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-SC | Tampering | umya/quick-xml/zip installs | mitigate | Task 1 blocking-human legitimacy checkpoint (verify on crates.io); [ASSUMED] packages are never auto-approved |
| T-93-01-PURITY | Tampering / Elevation | reader leaking into a served crate via shared runtime dep or Cargo.lock fork | mitigate | Makefile positive `cargo tree -p pmcp-workbook-compiler -i umya` + re-run served-crate negatives + single-version guard for quick-xml/zip (Task 3) |
| T-93-01-DUP | Tampering | a re-declared Manifest/ChangeClass/WHITELIST diverging from the served definition | mitigate | re-export-only enforced by grep gate in Task 3 acceptance criteria |
</threat_model>

<verification>
- `make purity-check` green (compiler positive assertion + served-crate negatives + single-version guard).
- `cargo build -p pmcp-workbook-compiler` succeeds (stub bodies allowed).
- Zero customer identifiers; no build_reference_manifest; no re-declared shared types.
</verification>

<success_criteria>
- New crate exists, builds, workspace-registered with reader deps confined to it.
- Re-export surface in place; purity gate extended and green.
- The single surviving §5 gap (build_reference_manifest) is structurally absent from the new crate.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-01-SUMMARY.md` when done
</output>
