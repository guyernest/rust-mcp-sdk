---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 03
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - crates/pmcp-workbook-compiler/src/dialect/mod.rs
  - crates/pmcp-workbook-compiler/src/dialect/linter.rs
  - crates/pmcp-workbook-compiler/src/formula/mod.rs
  - crates/pmcp-workbook-compiler/src/formula/token.rs
  - crates/pmcp-workbook-compiler/src/formula/parser.rs
  - crates/pmcp-workbook-compiler/src/formula/rebase.rs
  - crates/pmcp-workbook-compiler/src/dag/mod.rs
  - crates/pmcp-workbook-compiler/src/dag/graph.rs
  - crates/pmcp-workbook-compiler/src/dag/resolve.rs
  - crates/pmcp-workbook-compiler/src/dag/topo.rs
  - crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_formula_parser.rs
autonomous: true
requirements: [WBDL-03, WBCO-03]

must_haves:
  truths:
    - "The dialect linter runs against pmcp-workbook-dialect::WHITELIST + DialectRules, collect-all, located (cell-addressed), with repair guidance"
    - "An out-of-whitelist function is rejected AT PARSE TIME (not a post-parse filter)"
    - "The formula parser builds runtime Expr and the DAG topo-sorts via Kahn (no petgraph)"
    - "The formula parser never panics on attacker-controlled formula bytes"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/dialect/linter.rs"
      provides: "Running WBDL-03 linter over a WorkbookMap against the dialect contract"
      contains: "WHITELIST"
    - path: "crates/pmcp-workbook-compiler/src/formula/parser.rs"
      provides: "Pratt parser with whitelist-at-parse-time rejection"
      contains: "Expr"
    - path: "crates/pmcp-workbook-compiler/src/dag/topo.rs"
      provides: "Kahn toposort over the formula DAG"
    - path: "crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_formula_parser.rs"
      provides: "Fuzz target over the untrusted formula parser"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/dialect/linter.rs"
      to: "pmcp_workbook_dialect::WHITELIST"
      via: "re-exported dialect contract"
      pattern: "WHITELIST|DialectRules"
    - from: "crates/pmcp-workbook-compiler/src/formula/parser.rs"
      to: "pmcp_workbook_runtime::Expr"
      via: "re-exported IR type"
      pattern: "Expr"
---

<objective>
Lift the dialect-linter execution (WBDL-03 — re-mapped here from Phase 91), the
formula parser (Pratt, whitelist-AT-PARSE-time — the dialect security primitive), and
the DAG compile (Kahn toposort, no petgraph) — WBCO-03. The linter runs against the
already-shipped `pmcp-workbook-dialect` WHITELIST/DialectRules contract; the parser
builds the runtime's `Expr` IR. Fuzz the untrusted formula parser.

Purpose: This is the parse-and-analyze middle of the pipeline. The whitelist-at-parse-time
gate is THE core security property (an out-of-whitelist function must be a parse-time
REJECTION, never a silent accept). The linter must consume the dialect crate's contract
directly — re-declaring WHITELIST would defeat the dialect crate's drift-protection test.
Output: dialect/, formula/, dag/ modules + the formula-parser fuzz target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md

<interfaces>
Lift source (lighthouse, absolute, PRIVATE — scrub):
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dialect/{linter,finding,rules,mod}.rs (596 LOC linter)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/formula/{token,parser,rebase,mod}.rs (Pratt, whitelist-at-parse)
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dag/{graph,resolve,topo,mod}.rs (Kahn)

In-repo contract + re-export source:
- crates/pmcp-workbook-dialect/src/lib.rs — WHITELIST (13-fn slice), DialectRules, CandidateRole
  (the lighthouse dialect/rules.rs re-declares these — DELETE the local copies, re-export from
  pmcp_workbook_dialect; the dialect crate's rules.rs:248-336 spec-binding test asserts the
  WHITELIST cannot drift, a second copy would defeat it).
- crates/pmcp-workbook-runtime/src/finding.rs — LintFinding/LintReport/Severity (re-export; the
  linter emits these collect-all located findings — D-01).
- crates/pmcp-workbook-runtime/src/formula.rs — Expr/BinOp/UnOp/CellValue (the parser BUILDS these — re-export, do not re-declare).
- crates/pmcp-workbook-runtime/src/dag.rs + resolve.rs — Dag/toposort (re-use).
- crates/pmcp-workbook-compiler/src/ingest/ (Plan 02) — WorkbookMap the linter + parser consume.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the WBDL-03 dialect linter — runs against the dialect contract, collect-all located findings</name>
  <files>crates/pmcp-workbook-compiler/src/dialect/mod.rs, crates/pmcp-workbook-compiler/src/dialect/linter.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dialect/{linter,rules,mod}.rs (the running linter + its local WHITELIST copy to DELETE)
    - crates/pmcp-workbook-dialect/src/lib.rs (WHITELIST/DialectRules/CandidateRole — the contract to re-export and run against; the spec-binding drift test)
    - crates/pmcp-workbook-runtime/src/finding.rs (LintFinding/LintReport/Severity to emit)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### …/src/dialect/ — consume the contract, delete local copies)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-CONTEXT.md (D-01 collect-all never fail-fast; D-02 dialect violation = ERROR)
  </read_first>
  <action>
    Lift dialect/{linter,mod}.rs. DELETE the lighthouse's local WHITELIST/DialectRules/
    CandidateRole copies (dialect/rules.rs) and re-export from pmcp_workbook_dialect instead —
    the linter runs against the shipped contract. Use the re-exported runtime finding types
    (LintFinding/LintReport/Severity) for collect-all, cell-addressed findings with repair
    guidance (D-01 — report the whole pass at once, never fail-fast). A non-whitelist function /
    dialect violation is an ERROR (D-02, blocks emit). The linter consumes the WorkbookMap from
    Plan 02. Scrub all customer identifiers; lift the linter tests, scrubbed.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler dialect:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler dialect::` passes.
    - No re-declared WHITELIST in the compiler: `! grep -rn 'WHITELIST.*=.*&\[' crates/pmcp-workbook-compiler/src/dialect/` (it is re-exported from the dialect crate).
    - Linter emits collect-all located findings — a test asserts multiple violations report in one pass (not fail-fast).
    - Zero customer identifiers in dialect/.
  </acceptance_criteria>
  <done>WBDL-03 linter runs against the dialect contract; collect-all located findings; tests green; no WHITELIST duplication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift formula parser (whitelist-at-parse) + DAG Kahn toposort (WBCO-03)</name>
  <files>crates/pmcp-workbook-compiler/src/formula/mod.rs, crates/pmcp-workbook-compiler/src/formula/token.rs, crates/pmcp-workbook-compiler/src/formula/parser.rs, crates/pmcp-workbook-compiler/src/formula/rebase.rs, crates/pmcp-workbook-compiler/src/dag/mod.rs, crates/pmcp-workbook-compiler/src/dag/graph.rs, crates/pmcp-workbook-compiler/src/dag/resolve.rs, crates/pmcp-workbook-compiler/src/dag/topo.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/formula/{token,parser,rebase,mod}.rs (Pratt parser; whitelist-at-parse rejection point)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/dag/{graph,resolve,topo,mod}.rs (Kahn toposort)
    - crates/pmcp-workbook-runtime/src/formula.rs (Expr/BinOp/UnOp/CellValue the parser builds — re-export)
    - crates/pmcp-workbook-runtime/src/dag.rs + resolve.rs (Dag/toposort to reuse)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Don't Hand-Roll — whitelist-AT-PARSE-time is the security primitive; petgraph rejected)
  </read_first>
  <behavior>
    - Test whitelist_at_parse_rejects: a formula calling a function NOT in WHITELIST is rejected
      at parse time (the parser returns an error / pushes a dialect ERROR finding), not parsed-then-filtered.
    - Test whitelisted_function_parses: a whitelisted function (e.g. ROUND) parses into runtime Expr.
    - Test kahn_toposort_orders_deps: a 3-cell dependency chain topo-sorts in dependency order.
    - Test cycle_detected: a circular reference is detected and surfaced as an error finding (not an infinite loop).
  </behavior>
  <action>
    Lift formula/{token,parser,rebase,mod}.rs and dag/{graph,resolve,topo,mod}.rs. The parser
    BUILDS the re-exported runtime Expr (do NOT re-declare Expr/BinOp/UnOp). Whitelist
    enforcement happens AT PARSE TIME — an out-of-whitelist function is a parse-time REJECTION
    (consume the dialect WHITELIST). The DAG uses the lifted Kahn toposort (no petgraph); reuse
    the runtime Dag/toposort where the runtime already provides them. Detect cycles → error
    finding (no infinite loop). Scrub all customer identifiers; lift the existing parser/dag
    tests, scrubbed, plus the four behavior tests.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler formula:: dag:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler formula:: dag::` passes, including whitelist_at_parse_rejects and cycle_detected.
    - Parser builds the re-exported runtime Expr (no local Expr): `! grep -rnE 'pub (enum|struct) Expr\b' crates/pmcp-workbook-compiler/src/formula/`
    - No petgraph dependency: `! grep -q petgraph crates/pmcp-workbook-compiler/Cargo.toml`
    - Zero customer identifiers in formula/ and dag/.
  </acceptance_criteria>
  <done>formula parser (whitelist-at-parse) + Kahn DAG lifted + scrubbed; cycles detected; tests green; runtime Expr reused.</done>
</task>

<task type="auto">
  <name>Task 3: Fuzz target over the formula parser (ALWAYS: FUZZ)</name>
  <files>crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_formula_parser.rs</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/formula/parser.rs (the parse entry the fuzz target drives)
    - crates/pmcp-workbook-compiler/fuzz/Cargo.toml (created in Plan 02 Task 3 — add the second target)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-VALIDATION.md (## ALWAYS — FUZZ the formula parser)
  </read_first>
  <action>
    Add a cargo-fuzz target fuzz_formula_parser feeding arbitrary UTF-8/byte strings as formula
    text into the parser. Invariant: any input yields Ok(Expr) or a typed parse error — NEVER a
    panic, hang, or unbounded recursion/allocation (guard recursion depth if the Pratt parser is
    recursive). Reuse the fuzz Cargo.toml from Plan 02.
  </action>
  <verify>
    <automated>cargo fuzz build fuzz_formula_parser 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fuzz build fuzz_formula_parser` exits 0.
    - A smoke run (`cargo fuzz run fuzz_formula_parser -- -runs=10000`) finds no panic/crash.
  </acceptance_criteria>
  <done>Formula-parser fuzz target builds and survives a smoke run with no crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BA-authored formula text → parser | untrusted formula strings cross into the IR builder |
| WorkbookMap → linter/parser | workbook-sourced function names + cell refs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-INJ | Tampering / Elevation | a non-whitelisted/dangerous function in a cell | mitigate | Whitelist-AT-PARSE-time rejection — out-of-whitelist = parse-time REJECT, never silent accept (Task 2) |
| T-93-03-DOS | Denial of Service | malicious formula text (deep nesting / cycle) crashing or hanging the parser/DAG | mitigate | Cycle detection in the DAG; recursion-depth guard + fuzz target on the parser (Tasks 2, 3); #![deny(panic)] |
| T-93-03-DRIFT | Tampering | a re-declared WHITELIST diverging from the shipped dialect contract | mitigate | re-export from pmcp_workbook_dialect; grep gate forbids a local WHITELIST (Task 1) |
</threat_model>

<verification>
- dialect/formula/dag tests green; whitelist-at-parse rejection + cycle detection verified.
- Both fuzz targets build and survive smoke runs.
- Zero customer identifiers; no WHITELIST/Expr duplication; no petgraph.
</verification>

<success_criteria>
- WBDL-03: running linter over a WorkbookMap against the dialect contract, collect-all located findings.
- WBCO-03: whitelist-at-parse parser building runtime Expr + Kahn DAG with cycle detection.
- The untrusted-formula surface is fuzzed and panic-free.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-03-SUMMARY.md` when done
</output>
