---
phase: 93-workbook-compiler-5-generalization-fixes-promote-gate
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - crates/pmcp-workbook-compiler/src/ingest/mod.rs
  - crates/pmcp-workbook-compiler/src/ingest/cell_map.rs
  - crates/pmcp-workbook-compiler/src/provenance/mod.rs
  - crates/pmcp-workbook-compiler/src/provenance/gate.rs
  - crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs
  - crates/pmcp-workbook-compiler/src/provenance/region_hash.rs
  - crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_provenance_reader.rs
autonomous: true
requirements: [WBCO-01, WBCO-07]

must_haves:
  truths:
    - "Ingest reads a .xlsx via umya and produces an owned WorkbookMap capturing cached cell values as the trusted oracle (no umya types leak across the API)"
    - "The provenance gate reads ORIGINAL on-disk bytes via the quarantined quick-xml/zip reader (never umya metadata)"
    - "A umya-authored/stamped workbook is REFUSED with oracle/non-excel-app, not silently accepted"
    - "Malformed/oversize .xlsx bytes fail closed to an oracle/* Error finding with no panic"
  artifacts:
    - path: "crates/pmcp-workbook-compiler/src/ingest/mod.rs"
      provides: "umya read → owned WorkbookMap with cached-value oracle"
      contains: "WorkbookMap"
    - path: "crates/pmcp-workbook-compiler/src/provenance/gate.rs"
      provides: "Anchored Excel-identity check + umya-fabrication refusal"
      contains: "non-excel-app"
    - path: "crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs"
      provides: "pub(crate) quick-xml/zip raw reader of docProps/app.xml + calcPr"
      contains: "pub(crate)"
    - path: "crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_provenance_reader.rs"
      provides: "Fuzz target over the untrusted .xlsx ZIP/XML provenance reader"
  key_links:
    - from: "crates/pmcp-workbook-compiler/src/provenance/gate.rs"
      to: "raw_parts.rs"
      via: "raw byte read of original file"
      pattern: "raw_parts::"
    - from: "ingest WorkbookMap"
      to: "provenance OracleCorpus"
      via: "cached <v> values"
      pattern: "cached|oracle"
---

<objective>
Lift the two umya/quick-xml/zip-owning module groups — `ingest/` (umya read →
owned `WorkbookMap` + cached-value oracle, WBCO-01) and `provenance/` (quarantined
raw-bytes reader + anchored Excel-identity gate, WBCO-07) — and apply the one net-new
provenance design: REFUSE umya-fabricated provenance (upgrade from "record" to hard
refuse with `oracle/non-excel-app`). Fuzz the untrusted-bytes reader.

Purpose: These are the reader-bearing modules with no in-repo analog — they justify the
purity boundary. WBCO-07's refusal is genuinely net-new: umya stamps
`<Application>Microsoft Excel</Application>`/`calcId=122211`, so the existing
`.starts_with` check PASSES umya — we must detect the fabrication and refuse. The reader
ingests attacker-controlled bytes, so fail-closed + no-panic + fuzz are security gates.
Output: ingest + provenance modules, the WBCO-07 refusal regression test, and a fuzz
target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md
@.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md
@.claude/skills/spike-findings-rust-mcp-sdk/SKILL.md

<interfaces>
Lift source (lighthouse, absolute, PRIVATE — scrub all customer identifiers per Phase 92 D-13):
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/ingest/{mod,cell_map}.rs
- /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/provenance/{gate,raw_parts,region_hash}.rs

In-repo re-export source: crates/pmcp-workbook-runtime/src/finding.rs — LintFinding/LintReport/Severity
(the oracle/* Error findings emit these; re-export from the compiler lib, do not redeclare).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift ingest/ — umya read → owned WorkbookMap with cached-value oracle (WBCO-01)</name>
  <files>crates/pmcp-workbook-compiler/src/ingest/mod.rs, crates/pmcp-workbook-compiler/src/ingest/cell_map.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/ingest/mod.rs (734 LOC — the umya read + WorkbookMap construction)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/ingest/cell_map.rs (WorkbookMap/CellRecord types)
    - crates/pmcp-workbook-compiler/src/lib.rs (module declarations + re-export surface from Plan 01)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (## No In-Repo Analog — ingest: expose owned WorkbookMap/CellRecord, no umya types leak)
  </read_first>
  <action>
    Lift ingest/{mod,cell_map}.rs into the compiler crate. Expose an OWNED WorkbookMap /
    CellRecord (cells, formulas, the cached `<v>` values, colours, DV lists, named ranges) —
    no umya type may appear in the public API (the reader stays confined). The cached values
    are captured as the trusted oracle (WBCO-01). Scrub every customer identifier (ufh/towelrad/
    coil/heat_source/heat_pump/underfloor/radiator/plot3) from code, comments, and test names.
    Bump thiserror call sites to 2 if needed. Emit collect-all located findings via the
    re-exported runtime finding types for read errors (fail-closed, no panic). Lift the
    accompanying unit tests, scrubbed.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler ingest:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler ingest::` passes.
    - No umya type in the public WorkbookMap API: `! grep -rn 'umya' crates/pmcp-workbook-compiler/src/ingest/ | grep -E 'pub (fn|struct|enum).*umya'`
    - Zero customer identifiers: `! grep -rEi 'ufh|towelrad|coil|heat_source|heat_pump|underfloor|radiator|plot3' crates/pmcp-workbook-compiler/src/ingest/`
    - WorkbookMap captures cached cell values (oracle) — confirmed by an ingest test asserting a cached value round-trips.
  </acceptance_criteria>
  <done>ingest/ lifted + scrubbed; WorkbookMap owns cells + cached oracle; tests green; no umya leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift provenance/ + UPGRADE to REFUSE umya-fabricated provenance (WBCO-07)</name>
  <files>crates/pmcp-workbook-compiler/src/provenance/mod.rs, crates/pmcp-workbook-compiler/src/provenance/gate.rs, crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs, crates/pmcp-workbook-compiler/src/provenance/region_hash.rs</files>
  <read_first>
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/provenance/gate.rs (lines 250-275 — anchored .starts_with identity; the "version recorded NOT gated" comment to upgrade)
    - /Users/guy/Development/AI-on-Cloud/projects/towelrads/mcp-servers/quote-pricing/crates/workbook-compiler/src/provenance/{raw_parts,region_hash}.rs (the pub(crate) quick-xml/zip raw reader)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-RESEARCH.md (Pitfall 5 / O-3 — umya stamps calcId=122211; decide the fabrication signal; Security Domain DoS row)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-PATTERNS.md (### …/src/provenance/ — refuse-upgrade)
  </read_first>
  <behavior>
    - Test umya_fabricated_refused: a workbook authored/round-tripped via umya (carrying
      `<Application>Microsoft Excel</Application>` + calcId=122211) is REFUSED with the
      oracle/non-excel-app finding (resolve O-3: detect the calcId=122211 sentinel AND/OR
      absence of an Excel build/app-version string — document the chosen signal).
    - Test real_excel_accepted: a fixture saved by real Excel (anchored .starts_with passes,
      no umya sentinel) is ACCEPTED.
    - Test malformed_xlsx_fails_closed: truncated/garbage ZIP bytes produce an oracle/* Error
      finding (UnreadableZip/UnreadableXml/PartTooLarge), never a panic.
    - Test non_excel_app_refused: a non-Excel `<Application>` is REFUSED (existing behavior preserved).
  </behavior>
  <action>
    Lift provenance/{gate,raw_parts,region_hash}.rs. Keep the raw reader pub(crate) — it must
    NEVER be re-exported and never enter the served tree (read ORIGINAL on-disk bytes via
    quick-xml/zip, NOT umya metadata). Preserve the anchored `.starts_with("Microsoft Excel")`
    check. ADD the net-new WBCO-07 refusal: beyond the anchored name match, detect umya's
    fabricated identity (per O-3, the calcId=122211 sentinel and/or the absence of a positive
    Excel recalc/build marker) and REFUSE with oracle/non-excel-app (or a distinct
    oracle/umya-fabricated class). Guard the raw reader against oversize/malformed parts
    (PartTooLarge / UnreadableZip / UnreadableXml) → fail-closed Error finding, NO panic
    (the crate-deny header forbids panic on value paths). Scrub all customer identifiers.
    Lift the existing provenance tests, scrubbed, and add the four behavior tests above.
  </action>
  <verify>
    <automated>cargo test -p pmcp-workbook-compiler provenance::umya_fabricated_refused provenance::malformed_xlsx_fails_closed 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pmcp-workbook-compiler provenance::umya_fabricated_refused` passes (umya-stamped → REFUSED).
    - `cargo test -p pmcp-workbook-compiler provenance::malformed_xlsx_fails_closed` passes (no panic; oracle/* Error).
    - Raw reader is pub(crate)-only: `! grep -rn 'pub fn' crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs | grep -v 'pub(crate)'`
    - No calcId==122211 in any ACCEPTED OracleProvenance path (asserted by the refusal test).
    - Zero customer identifiers in provenance/.
  </acceptance_criteria>
  <done>provenance/ lifted + scrubbed; umya fabrication REFUSED with oracle/non-excel-app; malformed bytes fail closed; raw reader quarantined pub(crate); tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Fuzz target over the provenance raw reader (ALWAYS: FUZZ)</name>
  <files>crates/pmcp-workbook-compiler/fuzz/fuzz_targets/fuzz_provenance_reader.rs, crates/pmcp-workbook-compiler/fuzz/Cargo.toml</files>
  <read_first>
    - crates/pmcp-workbook-compiler/src/provenance/raw_parts.rs (the pub(crate) entry the fuzz target drives — expose a pub(crate) or #[cfg(fuzzing)] hook)
    - .claude/skills/spike-findings-rust-mcp-sdk/SKILL.md (proven fuzz/test landmines)
    - .planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-VALIDATION.md (## ALWAYS Requirements — FUZZ over the provenance raw reader)
  </read_first>
  <action>
    Create a cargo-fuzz target fuzz_provenance_reader that feeds arbitrary byte slices into the
    provenance raw reader (the .xlsx ZIP/XML parsing path). The invariant: ANY input either
    yields a structured result/finding or a typed error — NEVER a panic, hang, or unbounded
    allocation. Add the fuzz crate Cargo.toml if absent. Keep the fuzz harness behind the
    compiler's existing pub(crate)/#[cfg(fuzzing)] surface so the raw reader stays quarantined.
  </action>
  <verify>
    <automated>cargo +nightly fuzz build fuzz_provenance_reader 2>&1 | tail -3 || cargo fuzz build fuzz_provenance_reader 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The fuzz target builds: `cargo fuzz build fuzz_provenance_reader` exits 0.
    - A short smoke run (e.g. `cargo fuzz run fuzz_provenance_reader -- -runs=10000`) finds no panic/crash.
  </acceptance_criteria>
  <done>Provenance fuzz target builds and survives a smoke run with no crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted .xlsx bytes → provenance raw reader | attacker-controlled ZIP/XML (zip bomb, malformed XML) |
| untrusted .xlsx bytes → umya ingest | attacker-controlled spreadsheet contents |
| umya-stamped identity → trusted oracle | umya fabricates Excel provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02-DOS | Denial of Service | provenance raw_parts.rs ZIP/XML parse | mitigate | PartTooLarge/UnreadableZip/UnreadableXml guards → fail-closed oracle/* Error, no panic (#![deny(panic)]); fuzz target (Task 3) |
| T-93-02-SPOOF | Spoofing | umya-fabricated `<Application>`/calcId admitted as oracle | mitigate | Read ORIGINAL bytes; detect calcId=122211 sentinel + missing Excel marker; REFUSE with oracle/non-excel-app (Task 2, WBCO-07) |
| T-93-02-LEAK | Tampering / Elevation | raw reader escaping the purity boundary | mitigate | raw_parts kept pub(crate)-only (grep gate in Task 2 acceptance); never re-exported |
</threat_model>

<verification>
- ingest + provenance tests green; umya-fabrication refused; malformed bytes fail closed.
- Fuzz target builds and survives a smoke run.
- Zero customer identifiers; raw reader pub(crate)-only.
</verification>

<success_criteria>
- WBCO-01: ingest captures cached values as the trusted oracle with no umya leakage.
- WBCO-07: umya-stamped workbooks are REFUSED with oracle/non-excel-app (net-new upgrade verified by regression test).
- Untrusted-bytes surface fuzzed and fail-closed.
</success_criteria>

<output>
Create `.planning/phases/93-workbook-compiler-5-generalization-fixes-promote-gate/93-02-SUMMARY.md` when done
</output>
