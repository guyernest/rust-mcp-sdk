{
  "schema_version": "2",
  "kind": "tool_call",
  "case_id": "approval-mcp.team_approval__ask_unknown.negative-unknown-role",
  "server": "approval-mcp",
  "request": {
    "name": "team_approval__ask_nonexistent",
    "arguments": { "question": "who am I?", "options": ["yes"] }
  },
  "expect": {
    "outcome": "error",
    "match": "subset",
    "response": {
      "error": { "message": "not found" },
      "_note": "an unknown human role is never advertised, so the ask tool is absent and dispatch yields a tool-not-found error, never a panic"
    }
  }
}
